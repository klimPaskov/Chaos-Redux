# Chaos Redux migration decision category. Hidden until a migration problem is sustained.

migration_decision_category = {
	visible = {
		hidden_trigger = {
			NOT = { is_special_chaos_country = yes }
			uses_normal_civilian_systems = yes
		}
		OR = {
			has_country_flag = migration_decision_emerging
			has_country_flag = migration_decision_active
			has_country_flag = migration_decision_resolution
			has_country_flag = humanitarian_corridor_offer_pending
			migration_decision_problem_is_active = yes
		}
	}
	visible_when_empty = no
	scripted_gui = migration_report_header_scripted_gui
	icon = GFX_migration_category
	picture = GFX_migration_category_picture
	priority = constant:migration_decision_timing.category_priority
}
