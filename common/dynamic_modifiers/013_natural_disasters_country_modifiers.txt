# ============================================================================
# CHAOS REDUX - EVENT 013 NATURAL DISASTER COUNTRY MODIFIERS
# ============================================================================
# Concurrent warning preparations reserve civilian capacity through a counted
# country burden. Each completed timed release removes exactly one layer.
# ============================================================================

natural_disaster_warning_civilian_burden = {
	icon = GFX_idea_013_disaster_recovery_mobilization
	enable = { has_country_flag = natural_disaster_warning_civilian_burden_active }
	remove_trigger = { NOT = { has_country_flag = natural_disaster_warning_civilian_burden_active } }
	consumer_goods_factor = var:natural_disaster_warning_civilian_burden_value
}
