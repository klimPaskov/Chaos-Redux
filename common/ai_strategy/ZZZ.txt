ZZZ_unit_production = {
    allowed = {
        always = yes
    }
    enable = {
        OR = {
            original_tag = ZZZ
            has_country_flag = zombie_outbreak_dynamic_country
        }

    }

    abort_when_not_enabled = yes

    # Never stockpile equipment - always use it immediately
    ai_strategy = {
        type = equipment_stockpile_surplus_ratio
        value = -1000
    }

    # Ignore army incompetence - attack regardless of experience
    ai_strategy = {
        type = ignore_army_incompetence
        value = 1000
    }

    # Extreme aggression - minimal garrison, maximum attack
    ai_strategy = {
        type = garrison
        value = -1000
    }

    ai_strategy = {
        type = spare_unit_factor
        value = 1000  # Use ALL spare units for attacks
    }

    ai_strategy = {
        type = template_prio
        id = zombies
        value = 1000
    }

    # Make AI extremely aggressive in all major population centers
    ai_strategy = {
        type = area_priority
        id = europe
        value = 1000
    }

    ai_strategy = {
        type = area_priority
        id = north_america
        value = 1000
    }

    ai_strategy = {
        type = area_priority
        id = asia
        value = 1000
    }

    ai_strategy = {
        type = area_priority
        id = africa
        value = 1000
    }

    ai_strategy = {
        type = area_priority
        id = oceania
        value = 1000
    }

    ai_strategy = {
        type = area_priority
        id = middle_east
        value = 1000
    }

    # Force concentration on all fronts for maximum aggression
    ai_strategy = {
        type = force_concentration_factor
        value = 100  # Use maximum concentration for attacks
    }

    # Maximize zombie unit production ratio
    ai_strategy = {
        type = role_ratio
        id = zombies
        value = 1
    }

    # Minimize all other unit types to focus purely on zombies
    ai_strategy = {
        type = role_ratio
        id = garrison
        value = -1000
    }

    ai_strategy = {
        type = role_ratio
        id = paratroopers
        value = -1000
    }

    ai_strategy = {
        type = role_ratio
        id = mountaineers
        value = -1000
    }

    ai_strategy = {
        type = role_ratio
        id = marines
        value = -1000
    }

    ai_strategy = {
        type = role_ratio
        id = cavalry
        value = -1000
    }

    ai_strategy = {
        type = role_ratio
        id = motorized
        value = -1000
    }

    ai_strategy = {
        type = role_ratio
        id = mechanized
        value = -1000
    }

    ai_strategy = {
        type = role_ratio
        id = armor
        value = -1000
    }

    ai_strategy = {
        type = role_ratio
        id = infantry
        value = -1000
    }

    # Force AI to want maximum divisions
    ai_strategy = {
        type = ai_wanted_divisions_factor
        value = 1000
    }
}