# Chaos Redux 3D map-building spawn pools.
# Dedicated provincial pools prevent custom map consumers from reusing the
# vanilla shared special-project positions.

spawn_points = {
	chaosx_biowarfare_facility_spawn = {
		type = province
		max = 1
		disable_auto_nudging = yes
	}

	chaosx_cw_facility_spawn = {
		type = province
		max = 1
		disable_auto_nudging = yes
	}

	chaosx_camp_visual_anchor_spawn = {
		type = province
		max = 1
		disable_auto_nudging = yes
	}
}
