# Chaos Redux 3D map-building spawn pools.
# Dedicated pools prevent custom map consumers from reusing the vanilla shared
# special-project positions. Every spawn name below must have a
# matching building_<spawn_name> entity in gfx/entities/*.asset.

spawn_points = {
	chaosx_biowarfare_facility_spawn = {
		type = province
		max = 1
		disable_auto_nudging = yes
	}

	chaosx_cw_facility_spawn = {
		type = province
		max = 1
		disable_auto_nudging = yes
	}

	chaosx_concentration_camp_visual_anchor_spawn = {
		type = state
		max = 5
		disable_auto_nudging = yes
	}

	chaosx_extermination_camp_visual_anchor_spawn = {
		type = state
		max = 5
		disable_auto_nudging = yes
	}
}
