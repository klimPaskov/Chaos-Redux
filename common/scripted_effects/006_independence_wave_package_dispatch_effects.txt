# ============================================================================
# CHAOS REDUX - EVENT 006 RUNTIME PACKAGE DISPATCH
# ============================================================================
# Central adapter chain joining the frozen release transaction to audited
# regional country packages. Regional files own package setup, live validation,
# and cleanup; this file owns the one-result dispatch contract used by every
# Event 006 execution mode. No effect performs a world iteration.
# ============================================================================

# COUNTRY scope. Inputs are the published `independence_wave_setup_*` values
# and setup event targets. Output is temporary
# `independence_wave_package_setup_success`.
independence_wave_dispatch_package_setup = {
	set_temp_variable = { independence_wave_package_setup_success = constant:independence_wave_planner.no_candidates }
	independence_wave_dispatch_wallonia_frisia_package_setup = yes
}

# COUNTRY scope. Called after shared active/network registries are live. Output
# is temporary `independence_wave_package_final_validation_success`.
independence_wave_dispatch_package_final_validation = {
	set_temp_variable = { independence_wave_package_final_validation_success = constant:independence_wave_planner.no_candidates }
	independence_wave_dispatch_wallonia_frisia_package_final_validation = yes
}

# COUNTRY scope. Called before the shared reset clears package provenance.
independence_wave_dispatch_package_cleanup = {
	independence_wave_dispatch_wallonia_frisia_package_cleanup = yes
}
