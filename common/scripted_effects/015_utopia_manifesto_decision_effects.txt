# ============================================================================
# CHAOS REDUX - EVENT 015: UTOPIA MANIFESTO DECISION EFFECTS
# ============================================================================
# Decision-only helpers for material payment, dynamic mission duration, and
# bounded project resolution. The shared Event 015 kernel remains authoritative
# for Ledger clamping, targets, cases, stewardship, the League, and formation.
# ============================================================================

# Expects any positive temporary inputs named below. Every supplied value is
# negated before payment so callers never use unary minus on a variable token.
# All inputs are consumed and reset before this effect returns.
# Inputs: utopia_manifesto_cost_{manpower,infantry,support,motorized,trains,
# convoys,army_xp,command_power,stability,war_support}.
utopia_manifesto_pay_prepared_decision_cost = {
	if = {
		limit = { has_variable = utopia_manifesto_cost_manpower }
		multiply_temp_variable = { utopia_manifesto_cost_manpower = constant:utopia_manifesto_decision_value.cost_negation_multiplier }
		add_manpower = utopia_manifesto_cost_manpower
	}
	if = {
		limit = { has_variable = utopia_manifesto_cost_infantry }
		multiply_temp_variable = { utopia_manifesto_cost_infantry = constant:utopia_manifesto_decision_value.cost_negation_multiplier }
		add_equipment_to_stockpile = {
			type = infantry_equipment
			amount = utopia_manifesto_cost_infantry
		}
	}
	if = {
		limit = { has_variable = utopia_manifesto_cost_support }
		multiply_temp_variable = { utopia_manifesto_cost_support = constant:utopia_manifesto_decision_value.cost_negation_multiplier }
		add_equipment_to_stockpile = {
			type = support_equipment
			amount = utopia_manifesto_cost_support
		}
	}
	if = {
		limit = { has_variable = utopia_manifesto_cost_motorized }
		multiply_temp_variable = { utopia_manifesto_cost_motorized = constant:utopia_manifesto_decision_value.cost_negation_multiplier }
		add_equipment_to_stockpile = {
			type = motorized_equipment
			amount = utopia_manifesto_cost_motorized
		}
	}
	if = {
		limit = { has_variable = utopia_manifesto_cost_trains }
		multiply_temp_variable = { utopia_manifesto_cost_trains = constant:utopia_manifesto_decision_value.cost_negation_multiplier }
		add_equipment_to_stockpile = {
			type = train_equipment
			amount = utopia_manifesto_cost_trains
		}
	}
	if = {
		limit = { has_variable = utopia_manifesto_cost_convoys }
		multiply_temp_variable = { utopia_manifesto_cost_convoys = constant:utopia_manifesto_decision_value.cost_negation_multiplier }
		add_equipment_to_stockpile = {
			type = convoy_1
			amount = utopia_manifesto_cost_convoys
		}
	}
	if = {
		limit = { has_variable = utopia_manifesto_cost_army_xp }
		multiply_temp_variable = { utopia_manifesto_cost_army_xp = constant:utopia_manifesto_decision_value.cost_negation_multiplier }
		army_experience = utopia_manifesto_cost_army_xp
	}
	if = {
		limit = { has_variable = utopia_manifesto_cost_command_power }
		multiply_temp_variable = { utopia_manifesto_cost_command_power = constant:utopia_manifesto_decision_value.cost_negation_multiplier }
		add_command_power = utopia_manifesto_cost_command_power
	}
	if = {
		limit = { has_variable = utopia_manifesto_cost_stability }
		multiply_temp_variable = { utopia_manifesto_cost_stability = constant:utopia_manifesto_decision_value.cost_negation_multiplier }
		add_stability = utopia_manifesto_cost_stability
	}
	if = {
		limit = { has_variable = utopia_manifesto_cost_war_support }
		multiply_temp_variable = { utopia_manifesto_cost_war_support = constant:utopia_manifesto_decision_value.cost_negation_multiplier }
		add_war_support = utopia_manifesto_cost_war_support
	}
	set_temp_variable = { utopia_manifesto_cost_manpower = constant:utopia_manifesto_decision_value.none }
	set_temp_variable = { utopia_manifesto_cost_infantry = constant:utopia_manifesto_decision_value.none }
	set_temp_variable = { utopia_manifesto_cost_support = constant:utopia_manifesto_decision_value.none }
	set_temp_variable = { utopia_manifesto_cost_motorized = constant:utopia_manifesto_decision_value.none }
	set_temp_variable = { utopia_manifesto_cost_trains = constant:utopia_manifesto_decision_value.none }
	set_temp_variable = { utopia_manifesto_cost_convoys = constant:utopia_manifesto_decision_value.none }
	set_temp_variable = { utopia_manifesto_cost_army_xp = constant:utopia_manifesto_decision_value.none }
	set_temp_variable = { utopia_manifesto_cost_command_power = constant:utopia_manifesto_decision_value.none }
	set_temp_variable = { utopia_manifesto_cost_stability = constant:utopia_manifesto_decision_value.none }
	set_temp_variable = { utopia_manifesto_cost_war_support = constant:utopia_manifesto_decision_value.none }
}

# Inputs: utopia_manifesto_duration_base_input and
# utopia_manifesto_duration_maximum_input. Output: temporary
# utopia_manifesto_duration_result. Duration grows with war and country size,
# and falls when the Ledger can credibly support administration.
utopia_manifesto_prepare_dynamic_decision_duration = {
	set_temp_variable = {
		utopia_manifesto_duration_result = utopia_manifesto_duration_base_input
	}
	if = {
		limit = { has_war = yes }
		add_to_temp_variable = {
			utopia_manifesto_duration_result = constant:utopia_manifesto_decision_duration.war_addition
		}
	}
	if = {
		limit = { check_variable = { var = num_of_owned_states value = constant:utopia_manifesto_decision_threshold.large_country_states compare = greater_than } }
		add_to_temp_variable = {
			utopia_manifesto_duration_result = constant:utopia_manifesto_decision_duration.large_country_addition
		}
	}
	if = {
		limit = { check_variable = { var = num_of_owned_states value = constant:utopia_manifesto_decision_threshold.very_large_country_states compare = greater_than } }
		add_to_temp_variable = {
			utopia_manifesto_duration_result = constant:utopia_manifesto_decision_duration.very_large_country_addition
		}
	}
	if = {
		limit = { utopia_manifesto_plenty_is_stable = yes }
		subtract_from_temp_variable = {
			utopia_manifesto_duration_result = constant:utopia_manifesto_decision_duration.stable_plenty_reduction
		}
	}
	if = {
		limit = { utopia_manifesto_concord_is_trusted = yes }
		subtract_from_temp_variable = {
			utopia_manifesto_duration_result = constant:utopia_manifesto_decision_duration.trusted_concord_reduction
		}
	}
	clamp_temp_variable = {
		var = utopia_manifesto_duration_result
		min = constant:utopia_manifesto_decision_duration.minimum
		max = utopia_manifesto_duration_maximum_input
	}
}

# Input: temporary utopia_manifesto_advisor_duration_profile. The public
# offices reduce only the mission families named by their remit. Compatible
# offices may cooperate, but the global duration floor still applies.
utopia_manifesto_apply_advisor_duration_support = {
	if = {
		limit = {
			has_country_flag = utopia_manifesto_advisor_public_interpreter_active
			check_variable = { utopia_manifesto_advisor_duration_profile = constant:utopia_manifesto_advisor_duration_profile.formation }
		}
		subtract_from_temp_variable = { utopia_manifesto_duration_result = constant:utopia_manifesto_advisor_duration.standard_reduction_days }
	}
	if = {
		limit = {
			has_country_flag = utopia_manifesto_advisor_general_provisioner_active
			OR = {
				check_variable = { utopia_manifesto_advisor_duration_profile = constant:utopia_manifesto_advisor_duration_profile.store }
				check_variable = { utopia_manifesto_advisor_duration_profile = constant:utopia_manifesto_advisor_duration_profile.long_supply }
				check_variable = { utopia_manifesto_advisor_duration_profile = constant:utopia_manifesto_advisor_duration_profile.stewardship }
			}
		}
		subtract_from_temp_variable = { utopia_manifesto_duration_result = constant:utopia_manifesto_advisor_duration.standard_reduction_days }
	}
	if = {
		limit = {
			has_country_flag = utopia_manifesto_advisor_secretary_of_callings_active
			check_variable = { utopia_manifesto_advisor_duration_profile = constant:utopia_manifesto_advisor_duration_profile.calling }
		}
		subtract_from_temp_variable = { utopia_manifesto_duration_result = constant:utopia_manifesto_advisor_duration.standard_reduction_days }
	}
	if = {
		limit = {
			has_country_flag = utopia_manifesto_advisor_surveyor_of_shores_active
			OR = {
				check_variable = { utopia_manifesto_advisor_duration_profile = constant:utopia_manifesto_advisor_duration_profile.survey }
				check_variable = { utopia_manifesto_advisor_duration_profile = constant:utopia_manifesto_advisor_duration_profile.island }
			}
		}
		subtract_from_temp_variable = { utopia_manifesto_duration_result = constant:utopia_manifesto_advisor_duration.standard_reduction_days }
	}
	if = {
		limit = {
			has_country_flag = utopia_manifesto_advisor_civic_engineer_active
			OR = {
				check_variable = { utopia_manifesto_advisor_duration_profile = constant:utopia_manifesto_advisor_duration_profile.district }
				check_variable = { utopia_manifesto_advisor_duration_profile = constant:utopia_manifesto_advisor_duration_profile.island }
				check_variable = { utopia_manifesto_advisor_duration_profile = constant:utopia_manifesto_advisor_duration_profile.defense }
			}
		}
		subtract_from_temp_variable = { utopia_manifesto_duration_result = constant:utopia_manifesto_advisor_duration.standard_reduction_days }
	}
	if = {
		limit = {
			has_country_flag = utopia_manifesto_advisor_keeper_of_stores_active
			OR = {
				check_variable = { utopia_manifesto_advisor_duration_profile = constant:utopia_manifesto_advisor_duration_profile.store }
				check_variable = { utopia_manifesto_advisor_duration_profile = constant:utopia_manifesto_advisor_duration_profile.long_supply }
			}
		}
		subtract_from_temp_variable = { utopia_manifesto_duration_result = constant:utopia_manifesto_advisor_duration.standard_reduction_days }
	}
	if = {
		limit = {
			has_country_flag = utopia_manifesto_advisor_league_envoy_active
			OR = {
				check_variable = { utopia_manifesto_advisor_duration_profile = constant:utopia_manifesto_advisor_duration_profile.league }
				check_variable = { utopia_manifesto_advisor_duration_profile = constant:utopia_manifesto_advisor_duration_profile.association }
				check_variable = { utopia_manifesto_advisor_duration_profile = constant:utopia_manifesto_advisor_duration_profile.settlement_agreement }
			}
		}
		subtract_from_temp_variable = { utopia_manifesto_duration_result = constant:utopia_manifesto_advisor_duration.standard_reduction_days }
	}
	if = {
		limit = {
			has_country_flag = utopia_manifesto_advisor_advocate_of_limits_active
			OR = {
				check_variable = { utopia_manifesto_advisor_duration_profile = constant:utopia_manifesto_advisor_duration_profile.case }
				check_variable = { utopia_manifesto_advisor_duration_profile = constant:utopia_manifesto_advisor_duration_profile.stewardship }
				check_variable = { utopia_manifesto_advisor_duration_profile = constant:utopia_manifesto_advisor_duration_profile.property }
			}
		}
		subtract_from_temp_variable = { utopia_manifesto_duration_result = constant:utopia_manifesto_advisor_duration.standard_reduction_days }
	}
	if = {
		limit = {
			has_country_flag = utopia_manifesto_advisor_public_auditor_active
			OR = {
				check_variable = { utopia_manifesto_advisor_duration_profile = constant:utopia_manifesto_advisor_duration_profile.property }
				check_variable = { utopia_manifesto_advisor_duration_profile = constant:utopia_manifesto_advisor_duration_profile.case }
				check_variable = { utopia_manifesto_advisor_duration_profile = constant:utopia_manifesto_advisor_duration_profile.formation }
			}
		}
		subtract_from_temp_variable = { utopia_manifesto_duration_result = constant:utopia_manifesto_advisor_duration.standard_reduction_days }
	}
	if = {
		limit = {
			has_country_flag = utopia_manifesto_advisor_constitutional_jurist_active
			OR = {
				check_variable = { utopia_manifesto_advisor_duration_profile = constant:utopia_manifesto_advisor_duration_profile.property }
				check_variable = { utopia_manifesto_advisor_duration_profile = constant:utopia_manifesto_advisor_duration_profile.association }
				check_variable = { utopia_manifesto_advisor_duration_profile = constant:utopia_manifesto_advisor_duration_profile.formation }
			}
		}
		subtract_from_temp_variable = { utopia_manifesto_duration_result = constant:utopia_manifesto_advisor_duration.standard_reduction_days }
	}
	if = {
		limit = {
			has_country_flag = utopia_manifesto_advisor_council_organizer_active
			OR = {
				check_variable = { utopia_manifesto_advisor_duration_profile = constant:utopia_manifesto_advisor_duration_profile.calling }
				check_variable = { utopia_manifesto_advisor_duration_profile = constant:utopia_manifesto_advisor_duration_profile.district }
			}
		}
		subtract_from_temp_variable = { utopia_manifesto_duration_result = constant:utopia_manifesto_advisor_duration.standard_reduction_days }
	}
	if = {
		limit = {
			has_country_flag = utopia_manifesto_advisor_social_workshop_planner_active
			OR = {
				check_variable = { utopia_manifesto_advisor_duration_profile = constant:utopia_manifesto_advisor_duration_profile.calling }
				check_variable = { utopia_manifesto_advisor_duration_profile = constant:utopia_manifesto_advisor_duration_profile.district }
			}
		}
		subtract_from_temp_variable = { utopia_manifesto_duration_result = constant:utopia_manifesto_advisor_duration.standard_reduction_days }
	}
	if = {
		limit = {
			has_country_flag = utopia_manifesto_advisor_chief_surveyor_active
			OR = {
				check_variable = { utopia_manifesto_advisor_duration_profile = constant:utopia_manifesto_advisor_duration_profile.survey }
				check_variable = { utopia_manifesto_advisor_duration_profile = constant:utopia_manifesto_advisor_duration_profile.district }
			}
		}
		subtract_from_temp_variable = { utopia_manifesto_duration_result = constant:utopia_manifesto_advisor_duration.standard_reduction_days }
	}
	if = {
		limit = {
			has_country_flag = utopia_manifesto_advisor_standards_engineer_active
			OR = {
				check_variable = { utopia_manifesto_advisor_duration_profile = constant:utopia_manifesto_advisor_duration_profile.district }
				check_variable = { utopia_manifesto_advisor_duration_profile = constant:utopia_manifesto_advisor_duration_profile.island }
			}
		}
		subtract_from_temp_variable = { utopia_manifesto_duration_result = constant:utopia_manifesto_advisor_duration.standard_reduction_days }
	}
	if = {
		limit = {
			has_country_flag = utopia_manifesto_advisor_steward_of_service_active
			OR = {
				check_variable = { utopia_manifesto_advisor_duration_profile = constant:utopia_manifesto_advisor_duration_profile.stewardship }
				check_variable = { utopia_manifesto_advisor_duration_profile = constant:utopia_manifesto_advisor_duration_profile.defense }
				check_variable = { utopia_manifesto_advisor_duration_profile = constant:utopia_manifesto_advisor_duration_profile.island }
			}
		}
		subtract_from_temp_variable = { utopia_manifesto_duration_result = constant:utopia_manifesto_advisor_duration.standard_reduction_days }
	}
	if = {
		limit = {
			has_country_flag = utopia_manifesto_advisor_contract_broker_active
			OR = {
				check_variable = { utopia_manifesto_advisor_duration_profile = constant:utopia_manifesto_advisor_duration_profile.case }
				check_variable = { utopia_manifesto_advisor_duration_profile = constant:utopia_manifesto_advisor_duration_profile.settlement_agreement }
				check_variable = { utopia_manifesto_advisor_duration_profile = constant:utopia_manifesto_advisor_duration_profile.long_supply }
			}
		}
		subtract_from_temp_variable = { utopia_manifesto_duration_result = constant:utopia_manifesto_advisor_duration.standard_reduction_days }
	}
	clamp_temp_variable = {
		var = utopia_manifesto_duration_result
		min = constant:utopia_manifesto_decision_duration.minimum
		max = utopia_manifesto_duration_maximum_input
	}
	set_temp_variable = { utopia_manifesto_advisor_duration_profile = constant:utopia_manifesto_advisor_duration_profile.none }
}

utopia_manifesto_prepare_survey_decision_duration = {
	set_temp_variable = { utopia_manifesto_duration_base_input = constant:utopia_manifesto_decision_duration.survey }
	set_temp_variable = { utopia_manifesto_duration_maximum_input = constant:utopia_manifesto_decision_duration.survey_maximum }
	utopia_manifesto_prepare_dynamic_decision_duration = yes
	set_temp_variable = { utopia_manifesto_advisor_duration_profile = constant:utopia_manifesto_advisor_duration_profile.survey }
	utopia_manifesto_apply_advisor_duration_support = yes
	set_variable = { utopia_manifesto_survey_days = utopia_manifesto_duration_result }
}

utopia_manifesto_prepare_store_decision_duration = {
	set_temp_variable = { utopia_manifesto_duration_base_input = constant:utopia_manifesto_decision_duration.store }
	set_temp_variable = { utopia_manifesto_duration_maximum_input = constant:utopia_manifesto_decision_duration.store_maximum }
	utopia_manifesto_prepare_dynamic_decision_duration = yes
	set_temp_variable = { utopia_manifesto_advisor_duration_profile = constant:utopia_manifesto_advisor_duration_profile.store }
	utopia_manifesto_apply_advisor_duration_support = yes
	set_variable = { utopia_manifesto_store_days = utopia_manifesto_duration_result }
}

utopia_manifesto_prepare_calling_decision_duration = {
	set_temp_variable = { utopia_manifesto_duration_base_input = constant:utopia_manifesto_decision_duration.calling }
	set_temp_variable = { utopia_manifesto_duration_maximum_input = constant:utopia_manifesto_decision_duration.calling_maximum }
	utopia_manifesto_prepare_dynamic_decision_duration = yes
	set_temp_variable = { utopia_manifesto_advisor_duration_profile = constant:utopia_manifesto_advisor_duration_profile.calling }
	utopia_manifesto_apply_advisor_duration_support = yes
	set_variable = { utopia_manifesto_calling_days = utopia_manifesto_duration_result }
}

utopia_manifesto_prepare_calling_sustainment_duration = {
	set_temp_variable = { utopia_manifesto_duration_base_input = constant:utopia_manifesto_decision_duration.calling_sustainment }
	set_temp_variable = { utopia_manifesto_duration_maximum_input = constant:utopia_manifesto_decision_duration.calling_sustainment_maximum }
	utopia_manifesto_prepare_dynamic_decision_duration = yes
	set_temp_variable = { utopia_manifesto_advisor_duration_profile = constant:utopia_manifesto_advisor_duration_profile.calling }
	utopia_manifesto_apply_advisor_duration_support = yes
	set_variable = { utopia_manifesto_calling_sustainment_days = utopia_manifesto_duration_result }
}

utopia_manifesto_prepare_property_decision_duration = {
	set_temp_variable = { utopia_manifesto_duration_base_input = constant:utopia_manifesto_decision_duration.property }
	set_temp_variable = { utopia_manifesto_duration_maximum_input = constant:utopia_manifesto_decision_duration.property_maximum }
	utopia_manifesto_prepare_dynamic_decision_duration = yes
	set_temp_variable = { utopia_manifesto_advisor_duration_profile = constant:utopia_manifesto_advisor_duration_profile.property }
	utopia_manifesto_apply_advisor_duration_support = yes
	set_variable = { utopia_manifesto_property_days = utopia_manifesto_duration_result }
}

utopia_manifesto_prepare_district_decision_duration = {
	set_temp_variable = { utopia_manifesto_duration_base_input = constant:utopia_manifesto_decision_duration.district }
	set_temp_variable = { utopia_manifesto_duration_maximum_input = constant:utopia_manifesto_decision_duration.district_maximum }
	utopia_manifesto_prepare_dynamic_decision_duration = yes
	set_temp_variable = { utopia_manifesto_advisor_duration_profile = constant:utopia_manifesto_advisor_duration_profile.district }
	utopia_manifesto_apply_advisor_duration_support = yes
	set_variable = { utopia_manifesto_district_days = utopia_manifesto_duration_result }
}

utopia_manifesto_prepare_island_decision_duration = {
	set_temp_variable = { utopia_manifesto_duration_base_input = constant:utopia_manifesto_decision_duration.island }
	set_temp_variable = { utopia_manifesto_duration_maximum_input = constant:utopia_manifesto_decision_duration.island_maximum }
	utopia_manifesto_prepare_dynamic_decision_duration = yes
	set_temp_variable = { utopia_manifesto_advisor_duration_profile = constant:utopia_manifesto_advisor_duration_profile.island }
	utopia_manifesto_apply_advisor_duration_support = yes
	set_variable = { utopia_manifesto_island_days = utopia_manifesto_duration_result }
}

utopia_manifesto_prepare_archipelago_site_decision_duration = {
	set_temp_variable = { utopia_manifesto_duration_base_input = constant:utopia_manifesto_decision_duration.archipelago_site }
	set_temp_variable = { utopia_manifesto_duration_maximum_input = constant:utopia_manifesto_decision_duration.archipelago_site_maximum }
	utopia_manifesto_prepare_dynamic_decision_duration = yes
	set_temp_variable = { utopia_manifesto_advisor_duration_profile = constant:utopia_manifesto_advisor_duration_profile.island }
	utopia_manifesto_apply_advisor_duration_support = yes
	set_variable = { utopia_manifesto_archipelago_site_days = utopia_manifesto_duration_result }
}

utopia_manifesto_prepare_case_decision_duration = {
	set_temp_variable = { utopia_manifesto_duration_base_input = constant:utopia_manifesto_decision_duration.case }
	set_temp_variable = { utopia_manifesto_duration_maximum_input = constant:utopia_manifesto_decision_duration.case_maximum }
	utopia_manifesto_prepare_dynamic_decision_duration = yes
	set_temp_variable = { utopia_manifesto_advisor_duration_profile = constant:utopia_manifesto_advisor_duration_profile.case }
	utopia_manifesto_apply_advisor_duration_support = yes
	set_variable = { utopia_manifesto_case_days = utopia_manifesto_duration_result }
}

utopia_manifesto_prepare_settlement_agreement_duration = {
	set_temp_variable = { utopia_manifesto_duration_base_input = constant:utopia_manifesto_decision_duration.settlement_agreement }
	set_temp_variable = { utopia_manifesto_duration_maximum_input = constant:utopia_manifesto_decision_duration.settlement_agreement_maximum }
	utopia_manifesto_prepare_dynamic_decision_duration = yes
	set_temp_variable = { utopia_manifesto_advisor_duration_profile = constant:utopia_manifesto_advisor_duration_profile.settlement_agreement }
	utopia_manifesto_apply_advisor_duration_support = yes
	set_variable = { utopia_manifesto_settlement_agreement_days = utopia_manifesto_duration_result }
}

utopia_manifesto_prepare_long_supply_contract_duration = {
	set_temp_variable = { utopia_manifesto_duration_base_input = constant:utopia_manifesto_decision_duration.long_supply_contract }
	set_temp_variable = { utopia_manifesto_duration_maximum_input = constant:utopia_manifesto_decision_duration.long_supply_contract_maximum }
	utopia_manifesto_prepare_dynamic_decision_duration = yes
	set_temp_variable = { utopia_manifesto_advisor_duration_profile = constant:utopia_manifesto_advisor_duration_profile.long_supply }
	utopia_manifesto_apply_advisor_duration_support = yes
	set_variable = { utopia_manifesto_long_supply_contract_days = utopia_manifesto_duration_result }
}

utopia_manifesto_prepare_association_duties_duration = {
	set_temp_variable = { utopia_manifesto_duration_base_input = constant:utopia_manifesto_decision_duration.association_duties }
	set_temp_variable = { utopia_manifesto_duration_maximum_input = constant:utopia_manifesto_decision_duration.association_duties_maximum }
	utopia_manifesto_prepare_dynamic_decision_duration = yes
	set_temp_variable = { utopia_manifesto_advisor_duration_profile = constant:utopia_manifesto_advisor_duration_profile.association }
	utopia_manifesto_apply_advisor_duration_support = yes
	set_variable = { utopia_manifesto_association_duties_days = utopia_manifesto_duration_result }
}

utopia_manifesto_prepare_stewardship_decision_duration = {
	set_temp_variable = { utopia_manifesto_duration_base_input = constant:utopia_manifesto_decision_duration.stewardship }
	set_temp_variable = { utopia_manifesto_duration_maximum_input = constant:utopia_manifesto_decision_duration.stewardship_maximum }
	utopia_manifesto_prepare_dynamic_decision_duration = yes
	set_temp_variable = { utopia_manifesto_advisor_duration_profile = constant:utopia_manifesto_advisor_duration_profile.stewardship }
	utopia_manifesto_apply_advisor_duration_support = yes
	set_variable = { utopia_manifesto_stewardship_days = utopia_manifesto_duration_result }
}

utopia_manifesto_prepare_league_decision_duration = {
	set_temp_variable = { utopia_manifesto_duration_base_input = constant:utopia_manifesto_decision_duration.league }
	set_temp_variable = { utopia_manifesto_duration_maximum_input = constant:utopia_manifesto_decision_duration.league_maximum }
	utopia_manifesto_prepare_dynamic_decision_duration = yes
	set_temp_variable = { utopia_manifesto_advisor_duration_profile = constant:utopia_manifesto_advisor_duration_profile.league }
	utopia_manifesto_apply_advisor_duration_support = yes
	set_variable = { utopia_manifesto_league_days = utopia_manifesto_duration_result }
}

utopia_manifesto_prepare_defense_decision_duration = {
	set_temp_variable = { utopia_manifesto_duration_base_input = constant:utopia_manifesto_decision_duration.defense }
	set_temp_variable = { utopia_manifesto_duration_maximum_input = constant:utopia_manifesto_decision_duration.defense_maximum }
	utopia_manifesto_prepare_dynamic_decision_duration = yes
	set_temp_variable = { utopia_manifesto_advisor_duration_profile = constant:utopia_manifesto_advisor_duration_profile.defense }
	utopia_manifesto_apply_advisor_duration_support = yes
	set_variable = { utopia_manifesto_defense_days = utopia_manifesto_duration_result }
}

utopia_manifesto_prepare_formation_decision_duration = {
	set_temp_variable = { utopia_manifesto_duration_base_input = constant:utopia_manifesto_decision_duration.formation }
	set_temp_variable = { utopia_manifesto_duration_maximum_input = constant:utopia_manifesto_decision_duration.formation_maximum }
	utopia_manifesto_prepare_dynamic_decision_duration = yes
	set_temp_variable = { utopia_manifesto_advisor_duration_profile = constant:utopia_manifesto_advisor_duration_profile.formation }
	utopia_manifesto_apply_advisor_duration_support = yes
	set_variable = { utopia_manifesto_formation_days = utopia_manifesto_duration_result }
}

utopia_manifesto_prepare_total_repeal_duration = {
	set_temp_variable = { utopia_manifesto_duration_base_input = constant:utopia_manifesto_decision_duration.total_repeal }
	set_temp_variable = { utopia_manifesto_duration_maximum_input = constant:utopia_manifesto_decision_duration.total_repeal_maximum }
	utopia_manifesto_prepare_dynamic_decision_duration = yes
	set_temp_variable = { utopia_manifesto_advisor_duration_profile = constant:utopia_manifesto_advisor_duration_profile.formation }
	utopia_manifesto_apply_advisor_duration_support = yes
	set_variable = { utopia_manifesto_total_repeal_days = utopia_manifesto_duration_result }
}

utopia_manifesto_complete_national_survey = {
	clr_country_flag = utopia_manifesto_survey_active
	set_country_flag = utopia_manifesto_foundation_survey_complete
	utopia_manifesto_refresh_ledger = yes
	if = {
		limit = { NOT = { has_country_flag = utopia_manifesto_first_survey_event_seen } }
		set_country_flag = utopia_manifesto_first_survey_event_seen
		country_event = { id = chaosx.nr15.3 }
	}
	utopia_manifesto_schedule_prefire_shores_survey_report = yes
}

utopia_manifesto_fail_national_survey = {
	clr_country_flag = utopia_manifesto_survey_active
	set_country_flag = utopia_manifesto_survey_incomplete
	set_temp_variable = { utopia_manifesto_need_delta = constant:utopia_manifesto_decision_change.ledger_step }
	set_temp_variable = { utopia_manifesto_plenty_delta = constant:utopia_manifesto_decision_value.none }
	set_temp_variable = { utopia_manifesto_concord_delta = constant:utopia_manifesto_decision_change.ledger_step_loss }
	set_temp_variable = { utopia_manifesto_assignment_delta = constant:utopia_manifesto_decision_change.ledger_nudge }
	utopia_manifesto_apply_prepared_ledger_delta = yes
}

utopia_manifesto_complete_capital_store = {
	clr_country_flag = utopia_manifesto_capital_store_project_active
	set_country_flag = utopia_manifesto_capital_store_complete
	remove_mission = mission_utopia_prefire_perfect_store_deadline
	utopia_manifesto_complete_prefire_store_deadline = yes
	add_to_variable = { utopia_reserve_score = constant:utopia_manifesto_decision_change.reserve_shift }
	clamp_variable = {
		var = utopia_reserve_score
		min = constant:utopia_manifesto_ledger_limits.minimum
		max = constant:utopia_manifesto_ledger_limits.maximum
	}
	set_temp_variable = { utopia_manifesto_need_delta = constant:utopia_manifesto_decision_change.ledger_step_loss }
	set_temp_variable = { utopia_manifesto_plenty_delta = constant:utopia_manifesto_decision_change.ledger_shift }
	set_temp_variable = { utopia_manifesto_concord_delta = constant:utopia_manifesto_decision_change.ledger_step }
	set_temp_variable = { utopia_manifesto_assignment_delta = constant:utopia_manifesto_decision_value.none }
	utopia_manifesto_apply_prepared_ledger_delta = yes
	if = {
		limit = { NOT = { has_country_flag = utopia_manifesto_first_store_event_seen } }
		set_country_flag = utopia_manifesto_first_store_event_seen
		country_event = { id = chaosx.nr15.5 }
	}
}

utopia_manifesto_fail_store_project = {
	clr_country_flag = utopia_manifesto_capital_store_project_active
	clr_country_flag = utopia_manifesto_seasonal_reserve_active
	set_country_flag = utopia_manifesto_store_project_failed
	set_temp_variable = { utopia_manifesto_need_delta = constant:utopia_manifesto_decision_change.ledger_step }
	set_temp_variable = { utopia_manifesto_plenty_delta = constant:utopia_manifesto_decision_change.ledger_step_loss }
	set_temp_variable = { utopia_manifesto_concord_delta = constant:utopia_manifesto_decision_change.ledger_step_loss }
	set_temp_variable = { utopia_manifesto_assignment_delta = constant:utopia_manifesto_decision_change.ledger_nudge }
	utopia_manifesto_apply_prepared_ledger_delta = yes
}

utopia_manifesto_complete_seasonal_reserve = {
	clr_country_flag = utopia_manifesto_seasonal_reserve_active
	set_country_flag = utopia_manifesto_seasonal_reserve_complete
	add_to_variable = { utopia_reserve_score = constant:utopia_manifesto_decision_change.reserve_shift }
	clamp_variable = {
		var = utopia_reserve_score
		min = constant:utopia_manifesto_ledger_limits.minimum
		max = constant:utopia_manifesto_ledger_limits.maximum
	}
	set_temp_variable = { utopia_manifesto_need_delta = constant:utopia_manifesto_decision_change.ledger_step_loss }
	set_temp_variable = { utopia_manifesto_plenty_delta = constant:utopia_manifesto_decision_change.ledger_step }
	set_temp_variable = { utopia_manifesto_concord_delta = constant:utopia_manifesto_decision_change.ledger_nudge }
	set_temp_variable = { utopia_manifesto_assignment_delta = constant:utopia_manifesto_decision_value.none }
	utopia_manifesto_apply_prepared_ledger_delta = yes
}

utopia_manifesto_complete_two_year_reserve = {
	clr_country_flag = utopia_manifesto_two_year_reserve_active
	set_country_flag = utopia_manifesto_two_year_reserve_complete
	set_variable = { utopia_reserve_score = constant:utopia_manifesto_reserve_band.band_five }
	set_temp_variable = { utopia_manifesto_need_delta = constant:utopia_manifesto_decision_change.ledger_shift_loss }
	set_temp_variable = { utopia_manifesto_plenty_delta = constant:utopia_manifesto_decision_change.ledger_shift }
	set_temp_variable = { utopia_manifesto_concord_delta = constant:utopia_manifesto_decision_change.ledger_step }
	set_temp_variable = { utopia_manifesto_assignment_delta = constant:utopia_manifesto_decision_value.none }
	utopia_manifesto_apply_prepared_ledger_delta = yes
	utopia_manifesto_refresh_formation_proof = yes
}

utopia_manifesto_fail_two_year_reserve = {
	clr_country_flag = utopia_manifesto_two_year_reserve_active
	set_country_flag = utopia_manifesto_two_year_reserve_partial
	utopia_manifesto_record_achievement_reserve_reset = yes
	set_temp_variable = { utopia_manifesto_need_delta = constant:utopia_manifesto_decision_change.ledger_nudge }
	set_temp_variable = { utopia_manifesto_plenty_delta = constant:utopia_manifesto_decision_change.ledger_step_loss }
	set_temp_variable = { utopia_manifesto_concord_delta = constant:utopia_manifesto_decision_change.ledger_nudge_loss }
	set_temp_variable = { utopia_manifesto_assignment_delta = constant:utopia_manifesto_decision_value.none }
	utopia_manifesto_apply_prepared_ledger_delta = yes
}

utopia_manifesto_clear_selected_calling_shortage = {
	set_temp_variable = { utopia_manifesto_calling_family_input = utopia_manifesto_selected_calling }
	set_temp_variable = { utopia_manifesto_calling_method_input = utopia_manifesto_selected_calling_method }
	set_temp_variable = { utopia_manifesto_calling_severity_delta = constant:utopia_manifesto_calling_resolution_change.open_call }
	if = {
		limit = { check_variable = { utopia_manifesto_selected_calling_method = constant:utopia_manifesto_calling_method.guaranteed_placement } }
		set_temp_variable = { utopia_manifesto_calling_severity_delta = constant:utopia_manifesto_calling_resolution_change.guaranteed_placement }
	}
	else_if = {
		limit = { check_variable = { utopia_manifesto_selected_calling_method = constant:utopia_manifesto_calling_method.assignment_quota } }
		set_temp_variable = { utopia_manifesto_calling_severity_delta = constant:utopia_manifesto_calling_resolution_change.assignment_quota }
	}
	else_if = {
		limit = { check_variable = { utopia_manifesto_selected_calling_method = constant:utopia_manifesto_calling_method.emergency_levy } }
		set_temp_variable = { utopia_manifesto_calling_severity_delta = constant:utopia_manifesto_calling_resolution_change.emergency_levy }
		set_variable = { utopia_manifesto_emergency_levy_family = utopia_manifesto_selected_calling }
		set_country_flag = utopia_manifesto_emergency_levy_active
	}
	if = {
		limit = { check_variable = { utopia_manifesto_selected_calling_method = constant:utopia_manifesto_calling_method.emergency_levy } }
		utopia_manifesto_refresh_calling_state = yes
		set_temp_variable = { utopia_manifesto_calling_family_input = constant:utopia_manifesto_calling_family.none }
		set_temp_variable = { utopia_manifesto_calling_severity_delta = constant:utopia_manifesto_math.zero }
		set_temp_variable = { utopia_manifesto_calling_method_input = constant:utopia_manifesto_calling_method.unset }
	}
	else = { utopia_manifesto_apply_prepared_calling_change = yes }
	if = {
		limit = { check_variable = { utopia_manifesto_selected_calling_method = constant:utopia_manifesto_calling_method.emergency_levy } }
		utopia_manifesto_start_emergency_calling_levy_expiry = yes
	}
}

utopia_manifesto_start_emergency_calling_levy_expiry = {
	set_variable = { utopia_manifesto_emergency_levy_days = constant:utopia_manifesto_durations.emergency_levy_days }
	activate_mission = mission_utopia_emergency_calling_levy_expiry
}

utopia_manifesto_expire_emergency_calling_levy = {
	if = {
		limit = { has_country_flag = utopia_manifesto_emergency_levy_active }
		clr_country_flag = utopia_manifesto_emergency_levy_active
		set_country_flag = utopia_manifesto_emergency_levy_expired
		clear_variable = utopia_manifesto_emergency_levy_family
		clear_variable = utopia_manifesto_emergency_levy_days
		utopia_manifesto_refresh_calling_state = yes
	}
}

utopia_manifesto_extend_emergency_calling_levy = {
	if = {
		limit = {
			has_country_flag = utopia_manifesto_emergency_levy_active
			check_variable = {
				var = utopia_manifesto_emergency_levy_extensions
				value = constant:utopia_manifesto_calling_limits.emergency_levy_extension_limit
				compare = less_than
			}
		}
		if = {
			limit = { has_active_mission = mission_utopia_emergency_calling_levy_expiry }
			remove_mission = mission_utopia_emergency_calling_levy_expiry
		}
		add_to_variable = { utopia_manifesto_emergency_levy_extensions = constant:utopia_manifesto_math.one }
		set_variable = { utopia_manifesto_emergency_levy_days = constant:utopia_manifesto_durations.emergency_levy_days }
		set_temp_variable = { utopia_manifesto_emergency_levy_recent_days = constant:utopia_manifesto_durations.emergency_levy_reuse_days }
		set_country_flag = {
			flag = utopia_manifesto_emergency_levy_recent
			days = utopia_manifesto_emergency_levy_recent_days
		}
		activate_mission = mission_utopia_emergency_calling_levy_expiry
		set_temp_variable = { utopia_manifesto_need_delta = constant:utopia_manifesto_decision_change.ledger_nudge_loss }
		set_temp_variable = { utopia_manifesto_plenty_delta = constant:utopia_manifesto_decision_value.none }
		set_temp_variable = { utopia_manifesto_concord_delta = constant:utopia_manifesto_decision_change.ledger_step_loss }
		set_temp_variable = { utopia_manifesto_assignment_delta = constant:utopia_manifesto_decision_change.ledger_step }
		utopia_manifesto_apply_prepared_ledger_delta = yes
	}
}

utopia_manifesto_mark_selected_calling_method_recent = {
	set_temp_variable = { utopia_manifesto_calling_method_cooldown_days = constant:utopia_manifesto_decision_duration.cooldown_medium }
	if = {
		limit = { check_variable = { utopia_manifesto_selected_calling = constant:utopia_manifesto_calling_family.provisioning } }
		set_country_flag = { flag = utopia_manifesto_calling_provisioning_method_recent days = utopia_manifesto_calling_method_cooldown_days }
	}
	else_if = {
		limit = { check_variable = { utopia_manifesto_selected_calling = constant:utopia_manifesto_calling_family.workshops } }
		set_country_flag = { flag = utopia_manifesto_calling_workshops_method_recent days = utopia_manifesto_calling_method_cooldown_days }
	}
	else_if = {
		limit = { check_variable = { utopia_manifesto_selected_calling = constant:utopia_manifesto_calling_family.civic_works } }
		set_country_flag = { flag = utopia_manifesto_calling_civic_works_method_recent days = utopia_manifesto_calling_method_cooldown_days }
	}
	else_if = {
		limit = { check_variable = { utopia_manifesto_selected_calling = constant:utopia_manifesto_calling_family.learning_and_care } }
		set_country_flag = { flag = utopia_manifesto_calling_learning_and_care_method_recent days = utopia_manifesto_calling_method_cooldown_days }
	}
	else_if = {
		limit = { check_variable = { utopia_manifesto_selected_calling = constant:utopia_manifesto_calling_family.maritime_and_settlement } }
		set_country_flag = { flag = utopia_manifesto_calling_maritime_and_settlement_method_recent days = utopia_manifesto_calling_method_cooldown_days }
	}
	else_if = {
		limit = { check_variable = { utopia_manifesto_selected_calling = constant:utopia_manifesto_calling_family.defense_and_watches } }
		set_country_flag = { flag = utopia_manifesto_calling_defense_and_watches_method_recent days = utopia_manifesto_calling_method_cooldown_days }
	}
}

utopia_manifesto_resolve_unpopular_calling = {
	clr_country_flag = utopia_manifesto_calling_mission_active
	utopia_manifesto_mark_selected_calling_method_recent = yes
	if = {
		limit = {
			OR = {
				AND = {
					check_variable = { utopia_manifesto_selected_calling_method = constant:utopia_manifesto_calling_method.open_call }
					utopia_manifesto_concord_is_cooperative = yes
					utopia_manifesto_plenty_is_stable = yes
				}
				AND = {
					check_variable = { utopia_manifesto_selected_calling_method = constant:utopia_manifesto_calling_method.guaranteed_placement }
					utopia_manifesto_plenty_is_stable = yes
				}
				AND = {
					check_variable = { utopia_manifesto_selected_calling_method = constant:utopia_manifesto_calling_method.assignment_quota }
					utopia_manifesto_assignment_is_planned = yes
				}
				check_variable = { utopia_manifesto_selected_calling_method = constant:utopia_manifesto_calling_method.emergency_levy }
			}
		}
		utopia_manifesto_clear_selected_calling_shortage = yes
		set_temp_variable = { utopia_manifesto_need_delta = constant:utopia_manifesto_decision_change.ledger_step_loss }
		set_temp_variable = { utopia_manifesto_plenty_delta = constant:utopia_manifesto_decision_change.ledger_nudge }
		set_temp_variable = { utopia_manifesto_concord_delta = constant:utopia_manifesto_decision_change.ledger_nudge }
		set_temp_variable = { utopia_manifesto_assignment_delta = constant:utopia_manifesto_decision_value.none }
		if = {
			limit = {
				OR = {
					check_variable = { utopia_manifesto_selected_calling_method = constant:utopia_manifesto_calling_method.assignment_quota }
					check_variable = { utopia_manifesto_selected_calling_method = constant:utopia_manifesto_calling_method.emergency_levy }
				}
			}
			set_temp_variable = { utopia_manifesto_concord_delta = constant:utopia_manifesto_decision_change.ledger_step_loss }
			set_temp_variable = { utopia_manifesto_assignment_delta = constant:utopia_manifesto_decision_change.ledger_shift }
		}
		utopia_manifesto_apply_prepared_ledger_delta = yes
	}
	else_if = {
		limit = {
			OR = {
				utopia_manifesto_concord_is_cooperative = yes
				utopia_manifesto_plenty_is_stable = yes
				utopia_manifesto_assignment_is_planned = yes
			}
		}
		set_temp_variable = { utopia_manifesto_need_delta = constant:utopia_manifesto_decision_change.ledger_nudge_loss }
		set_temp_variable = { utopia_manifesto_plenty_delta = constant:utopia_manifesto_decision_value.none }
		set_temp_variable = { utopia_manifesto_concord_delta = constant:utopia_manifesto_decision_change.ledger_nudge_loss }
		set_temp_variable = { utopia_manifesto_assignment_delta = constant:utopia_manifesto_decision_change.ledger_nudge }
		utopia_manifesto_apply_prepared_ledger_delta = yes
	}
	else = {
		set_temp_variable = { utopia_manifesto_need_delta = constant:utopia_manifesto_decision_change.ledger_step }
		set_temp_variable = { utopia_manifesto_plenty_delta = constant:utopia_manifesto_decision_change.ledger_nudge_loss }
		set_temp_variable = { utopia_manifesto_concord_delta = constant:utopia_manifesto_decision_change.ledger_step_loss }
		set_temp_variable = { utopia_manifesto_assignment_delta = constant:utopia_manifesto_decision_change.ledger_step }
		utopia_manifesto_apply_prepared_ledger_delta = yes
		if = {
			limit = { check_variable = { utopia_manifesto_selected_calling_method = constant:utopia_manifesto_calling_method.open_call } }
			country_event = { id = chaosx.nr15.20 }
		}
		else_if = {
			limit = {
				OR = {
					check_variable = { utopia_manifesto_selected_calling_method = constant:utopia_manifesto_calling_method.assignment_quota }
					check_variable = { utopia_manifesto_selected_calling_method = constant:utopia_manifesto_calling_method.emergency_levy }
				}
			}
			country_event = { id = chaosx.nr15.22 }
		}
	}
}

utopia_manifesto_fail_unpopular_calling = {
	clr_country_flag = utopia_manifesto_calling_mission_active
	utopia_manifesto_mark_selected_calling_method_recent = yes
	set_temp_variable = { utopia_manifesto_need_delta = constant:utopia_manifesto_decision_change.ledger_step }
	set_temp_variable = { utopia_manifesto_plenty_delta = constant:utopia_manifesto_decision_value.none }
	set_temp_variable = { utopia_manifesto_concord_delta = constant:utopia_manifesto_decision_change.ledger_step_loss }
	set_temp_variable = { utopia_manifesto_assignment_delta = constant:utopia_manifesto_decision_change.ledger_nudge }
	utopia_manifesto_apply_prepared_ledger_delta = yes
}

utopia_manifesto_complete_calling_sustainment = {
	clr_country_flag = utopia_manifesto_calling_mission_active
	clr_country_flag = utopia_manifesto_calling_sustainment_active
	set_country_flag = utopia_manifesto_calling_sustainment_complete
	utopia_manifesto_record_achievement_callings_sustained = yes
	set_temp_variable = { utopia_manifesto_need_delta = constant:utopia_manifesto_decision_value.none }
	set_temp_variable = { utopia_manifesto_plenty_delta = constant:utopia_manifesto_decision_value.none }
	set_temp_variable = { utopia_manifesto_concord_delta = constant:utopia_manifesto_decision_change.ledger_step }
	set_temp_variable = { utopia_manifesto_assignment_delta = constant:utopia_manifesto_decision_value.none }
	utopia_manifesto_apply_prepared_ledger_delta = yes
}

utopia_manifesto_fail_calling_sustainment = {
	clr_country_flag = utopia_manifesto_calling_mission_active
	clr_country_flag = utopia_manifesto_calling_sustainment_active
	set_country_flag = utopia_manifesto_calling_sustainment_failed
}

utopia_manifesto_select_land_trust_property_project = {
	set_variable = { utopia_manifesto_property_project_type = constant:utopia_manifesto_decision_property_project.land_trust }
}

utopia_manifesto_select_worker_council_property_project = {
	set_variable = { utopia_manifesto_property_project_type = constant:utopia_manifesto_decision_property_project.worker_council }
}

utopia_manifesto_select_productive_tenure_property_project = {
	set_variable = { utopia_manifesto_property_project_type = constant:utopia_manifesto_decision_property_project.productive_tenure }
}

utopia_manifesto_select_idle_grant_property_project = {
	set_variable = { utopia_manifesto_property_project_type = constant:utopia_manifesto_decision_property_project.idle_grant }
}

utopia_manifesto_clear_district_role_suitability_flags = {
	FROM = {
		clr_state_flag = utopia_manifesto_district_market_garden_suitable
		clr_state_flag = utopia_manifesto_district_industrial_housing_suitable
		clr_state_flag = utopia_manifesto_district_rail_junction_suitable
		clr_state_flag = utopia_manifesto_district_refugee_municipality_suitable
	}
}

utopia_manifesto_record_district_role_suitability = {
	utopia_manifesto_clear_district_role_suitability_flags = yes
	if = {
		limit = { FROM = { utopia_manifesto_state_is_suitable_for_market_garden = yes } }
		FROM = { set_state_flag = utopia_manifesto_district_market_garden_suitable }
	}
	if = {
		limit = { FROM = { utopia_manifesto_state_is_suitable_for_industrial_housing = yes } }
		FROM = { set_state_flag = utopia_manifesto_district_industrial_housing_suitable }
	}
	if = {
		limit = { FROM = { utopia_manifesto_state_is_suitable_for_rail_junction = yes } }
		FROM = { set_state_flag = utopia_manifesto_district_rail_junction_suitable }
	}
	if = {
		limit = { FROM = { utopia_manifesto_state_is_suitable_for_refugee_municipality = yes } }
		FROM = { set_state_flag = utopia_manifesto_district_refugee_municipality_suitable }
	}
}

utopia_manifesto_register_district_state = {
	if = {
		limit = {
			FROM = { exists = yes }
			NOT = { is_in_array = { array = utopia_manifesto_district_states value = FROM } }
		}
		add_to_array = { array = utopia_manifesto_district_states value = FROM }
	}
}

utopia_manifesto_start_district_survey = {
	utopia_manifesto_register_district_state = yes
	set_country_flag = utopia_manifesto_district_survey_active
	set_variable = { utopia_manifesto_district_project_phase = constant:utopia_manifesto_district_phase.surveying }
	set_variable = { utopia_manifesto_district_project_state = FROM }
	FROM = {
		set_state_flag = utopia_manifesto_district_survey_active
		clr_state_flag = utopia_manifesto_district_project_delayed
		clr_state_flag = utopia_manifesto_district_obligations_breached
	}
}

utopia_manifesto_register_district_project_state = {
	utopia_manifesto_register_district_state = yes
	set_variable = { utopia_manifesto_district_project_state = FROM }
	set_variable = { utopia_manifesto_district_project_phase = constant:utopia_manifesto_district_phase.building }
	set_temp_variable = { utopia_manifesto_district_plan_card_duration = constant:utopia_manifesto_durations.district_plan_card_days }
	set_country_flag = {
		flag = utopia_manifesto_district_plan_committed_recent
		days = utopia_manifesto_district_plan_card_duration
	}
	FROM = {
		clr_state_flag = utopia_manifesto_district_role_market_garden_active
		clr_state_flag = utopia_manifesto_district_role_industrial_housing_active
		clr_state_flag = utopia_manifesto_district_role_rail_junction_active
		clr_state_flag = utopia_manifesto_district_role_refugee_municipality_active
		clr_state_flag = utopia_manifesto_district_project_delayed
		clr_state_flag = utopia_manifesto_district_obligations_breached
		if = { limit = { has_dynamic_modifier = { modifier = utopia_manifesto_incomplete_district_state } } remove_dynamic_modifier = { modifier = utopia_manifesto_incomplete_district_state } }
		clear_variable = utopia_manifesto_state_district_incomplete_supply
		clear_variable = utopia_manifesto_state_district_incomplete_construction
		clear_variable = utopia_manifesto_state_district_incomplete_resistance
		set_state_flag = utopia_manifesto_district_housing_commitment
		set_state_flag = utopia_manifesto_district_transport_commitment
		set_state_flag = utopia_manifesto_district_role_plan_commitment
	}
	if = {
		limit = { check_variable = { utopia_manifesto_district_project_type = constant:utopia_manifesto_decision_district_role.market_garden } }
		FROM = { set_state_flag = utopia_manifesto_district_role_market_garden_active }
	}
	else_if = {
		limit = { check_variable = { utopia_manifesto_district_project_type = constant:utopia_manifesto_decision_district_role.industrial_housing } }
		FROM = { set_state_flag = utopia_manifesto_district_role_industrial_housing_active }
	}
	else_if = {
		limit = { check_variable = { utopia_manifesto_district_project_type = constant:utopia_manifesto_decision_district_role.rail_junction } }
		FROM = { set_state_flag = utopia_manifesto_district_role_rail_junction_active }
	}
	else_if = {
		limit = { check_variable = { utopia_manifesto_district_project_type = constant:utopia_manifesto_decision_district_role.refugee_municipality } }
		FROM = { set_state_flag = utopia_manifesto_district_role_refugee_municipality_active }
	}
}

utopia_manifesto_clear_active_district_project_state = {
	utopia_manifesto_end_penal_works_method = yes
	FROM = {
		clr_state_flag = utopia_manifesto_district_build_active
		clr_state_flag = utopia_manifesto_district_charter_active
		clr_state_flag = utopia_manifesto_district_role_market_garden_active
		clr_state_flag = utopia_manifesto_district_role_industrial_housing_active
		clr_state_flag = utopia_manifesto_district_role_rail_junction_active
		clr_state_flag = utopia_manifesto_district_role_refugee_municipality_active
		clr_state_flag = utopia_manifesto_district_housing_commitment
		clr_state_flag = utopia_manifesto_district_transport_commitment
		clr_state_flag = utopia_manifesto_district_role_plan_commitment
	}
}

# Country scope. Required temporary input:
# utopia_manifesto_penal_works_loss_requested. The active district is held in
# the persistent utopia_manifesto_penal_works_project_state scope variable.
utopia_manifesto_apply_penal_works_loss_to_active_district = {
	set_temp_variable = { utopia_manifesto_penal_works_applied_loss = constant:chaos_meter_deaths.zero }
	if = {
		limit = {
			has_variable = utopia_manifesto_penal_works_project_state
			var:utopia_manifesto_penal_works_project_state = {
				exists = yes
				has_state_flag = utopia_manifesto_penal_works_method_active
			}
		}
		var:utopia_manifesto_penal_works_project_state = {
			set_temp_variable = { state_civilian_population_loss_requested = utopia_manifesto_penal_works_loss_requested }
			set_temp_variable = { state_civilian_population_loss_minimum_remaining = constant:utopia_manifesto_penal_method.minimum_state_population_remaining }
			set_temp_variable = { state_civilian_population_loss_reason = constant:chaos_meter_deaths_reason.gulag_repression }
			set_temp_variable = { state_civilian_population_loss_log_deaths = constant:chaos_meter_deaths.one }
			set_temp_variable = { state_civilian_population_loss_target_country = ROOT }
			set_temp_variable = { state_civilian_population_loss_has_target_country = constant:chaos_meter_deaths.one }
			set_temp_variable = { state_civilian_population_loss_applied = constant:chaos_meter_deaths.zero }
			set_temp_variable = { state_civilian_population_loss_result = constant:chaos_meter_deaths.zero }
			set_temp_variable = { state_civilian_population_loss_contract_supplied = constant:chaos_meter_deaths.one }
			apply_exact_state_civilian_population_loss = yes
			set_temp_variable = { utopia_manifesto_penal_works_applied_loss = state_civilian_population_loss_applied }
			ROOT = {
				set_variable = { utopia_manifesto_penal_works_last_civilian_loss = utopia_manifesto_penal_works_applied_loss }
				add_to_variable = { utopia_manifesto_penal_works_total_civilian_loss = utopia_manifesto_penal_works_applied_loss }
			}
		}
	}
	set_temp_variable = { utopia_manifesto_penal_works_loss_requested = constant:chaos_meter_deaths.zero }
}

utopia_manifesto_start_penal_works_method = {
	set_country_flag = utopia_manifesto_penal_works_method_active
	set_country_flag = utopia_manifesto_conduct_penal_labor
	set_country_flag = utopia_manifesto_penal_labour_used
	set_country_flag = utopia_manifesto_coercive_conduct_used
	set_variable = { utopia_manifesto_penal_works_project_state = FROM }
	FROM = {
		set_state_flag = utopia_manifesto_penal_works_method_active
		set_variable = { utopia_manifesto_state_penal_works_construction = constant:utopia_manifesto_penal_method.construction_speed }
		set_variable = { utopia_manifesto_state_penal_works_supply = constant:utopia_manifesto_penal_method.local_supply }
		set_variable = { utopia_manifesto_state_penal_works_garrison = constant:utopia_manifesto_penal_method.required_garrison }
		set_variable = { utopia_manifesto_state_penal_works_resistance = constant:utopia_manifesto_penal_method.resistance_target }
		add_dynamic_modifier = { modifier = utopia_manifesto_penal_works_active_state }
	}
	subtract_from_variable = { utopia_reserve_score = constant:utopia_manifesto_penal_method.reserve_cost }
	utopia_manifesto_refresh_reserve_band = yes
	set_temp_variable = { utopia_manifesto_penal_works_loss_requested = constant:utopia_manifesto_penal_method.start_civilian_loss }
	utopia_manifesto_apply_penal_works_loss_to_active_district = yes
	set_temp_variable = { utopia_manifesto_need_delta = constant:utopia_manifesto_decision_change.ledger_nudge_loss }
	set_temp_variable = { utopia_manifesto_plenty_delta = constant:utopia_manifesto_decision_change.ledger_nudge }
	set_temp_variable = { utopia_manifesto_concord_delta = constant:utopia_manifesto_decision_change.ledger_shift_loss }
	set_temp_variable = { utopia_manifesto_assignment_delta = constant:utopia_manifesto_decision_change.ledger_shift }
	utopia_manifesto_apply_prepared_ledger_delta = yes
	every_neighbor_country = {
		limit = { uses_normal_civilian_systems = yes }
		add_opinion_modifier = { target = ROOT modifier = utopia_manifesto_opinion_wary }
	}
	if = {
		limit = { NOT = { has_country_flag = utopia_manifesto_penal_works_incident_seen } }
		set_country_flag = utopia_manifesto_penal_works_incident_seen
		country_event = { id = chaosx.nr15.47 }
	}
}

utopia_manifesto_end_penal_works_method = {
	if = {
		limit = { has_variable = utopia_manifesto_penal_works_project_state }
		var:utopia_manifesto_penal_works_project_state = {
			clr_state_flag = utopia_manifesto_penal_works_method_active
			if = { limit = { has_dynamic_modifier = { modifier = utopia_manifesto_penal_works_active_state } } remove_dynamic_modifier = { modifier = utopia_manifesto_penal_works_active_state } }
			clear_variable = utopia_manifesto_state_penal_works_construction
			clear_variable = utopia_manifesto_state_penal_works_supply
			clear_variable = utopia_manifesto_state_penal_works_garrison
			clear_variable = utopia_manifesto_state_penal_works_resistance
		}
	}
	clr_country_flag = utopia_manifesto_penal_works_method_active
	clear_variable = utopia_manifesto_penal_works_project_state
}

utopia_manifesto_complete_penal_works_method = {
	if = {
		limit = {
			has_country_flag = utopia_manifesto_penal_works_method_active
			has_variable = utopia_manifesto_penal_works_project_state
			var:utopia_manifesto_penal_works_project_state = { has_state_flag = utopia_manifesto_penal_works_method_active }
		}
		set_temp_variable = { utopia_manifesto_penal_works_loss_requested = constant:utopia_manifesto_penal_method.completion_civilian_loss }
		utopia_manifesto_apply_penal_works_loss_to_active_district = yes
		set_country_flag = utopia_manifesto_penal_works_project_completed
		add_to_variable = { utopia_manifesto_penal_works_projects_completed = constant:utopia_manifesto_decision_value.project_increment }
	}
	utopia_manifesto_end_penal_works_method = yes
}

utopia_manifesto_halt_penal_works_method = {
	set_country_flag = utopia_manifesto_penal_works_halted_after_first_use
	utopia_manifesto_end_penal_works_method = yes
	set_temp_variable = { utopia_manifesto_concord_delta = constant:utopia_manifesto_decision_change.ledger_step }
	set_temp_variable = { utopia_manifesto_assignment_delta = constant:utopia_manifesto_decision_change.ledger_step_loss }
	utopia_manifesto_apply_prepared_ledger_delta = yes
}

utopia_manifesto_clear_district_state_package = {
		clr_state_flag = utopia_manifesto_district_survey_active
		clr_state_flag = utopia_manifesto_district_site_surveyed
		clr_state_flag = utopia_manifesto_district_project_refused
		clr_state_flag = utopia_manifesto_district_survey_reopened
		clr_state_flag = utopia_manifesto_district_market_garden_suitable
		clr_state_flag = utopia_manifesto_district_industrial_housing_suitable
		clr_state_flag = utopia_manifesto_district_rail_junction_suitable
		clr_state_flag = utopia_manifesto_district_refugee_municipality_suitable
		clr_state_flag = utopia_manifesto_district_build_active
		clr_state_flag = utopia_manifesto_district_charter_active
		clr_state_flag = utopia_manifesto_district_role_market_garden_active
		clr_state_flag = utopia_manifesto_district_role_industrial_housing_active
		clr_state_flag = utopia_manifesto_district_role_rail_junction_active
		clr_state_flag = utopia_manifesto_district_role_refugee_municipality_active
		clr_state_flag = utopia_manifesto_district_housing_commitment
		clr_state_flag = utopia_manifesto_district_transport_commitment
		clr_state_flag = utopia_manifesto_district_role_plan_commitment
		clr_state_flag = utopia_manifesto_district_project_delayed
		clr_state_flag = utopia_manifesto_district_obligations_breached
		clr_state_flag = utopia_manifesto_district_project_lost
		clr_state_flag = utopia_manifesto_district_built
		clr_state_flag = utopia_manifesto_market_garden_district
		clr_state_flag = utopia_manifesto_industrial_housing_district
		clr_state_flag = utopia_manifesto_rail_junction_town
		clr_state_flag = utopia_manifesto_refugee_municipality
		clr_state_flag = utopia_manifesto_district_chartered
		clr_state_flag = utopia_manifesto_district_consent_charter
		clr_state_flag = utopia_manifesto_district_common_table_charter
		clr_state_flag = utopia_manifesto_district_guardians_charter
		clr_state_flag = utopia_manifesto_district_closed_island_charter
		clr_state_flag = utopia_manifesto_district_humanist_review_charter
		clr_state_flag = utopia_manifesto_district_assigned_charter_appeals
		clr_state_flag = utopia_manifesto_district_assigned_charter_observers
		clr_state_flag = utopia_manifesto_penal_works_method_active
		clr_state_flag = utopia_manifesto_penal_works_supervised
		clr_state_flag = utopia_manifesto_penal_works_observed
		clr_state_flag = utopia_manifesto_penal_works_hardened
		if = { limit = { has_dynamic_modifier = { modifier = utopia_manifesto_incomplete_district_state } } remove_dynamic_modifier = { modifier = utopia_manifesto_incomplete_district_state } }
		if = { limit = { has_dynamic_modifier = { modifier = utopia_manifesto_district_consent_charter_state } } remove_dynamic_modifier = { modifier = utopia_manifesto_district_consent_charter_state } }
		if = { limit = { has_dynamic_modifier = { modifier = utopia_manifesto_district_common_table_charter_state } } remove_dynamic_modifier = { modifier = utopia_manifesto_district_common_table_charter_state } }
		if = { limit = { has_dynamic_modifier = { modifier = utopia_manifesto_district_guardians_charter_state } } remove_dynamic_modifier = { modifier = utopia_manifesto_district_guardians_charter_state } }
		if = { limit = { has_dynamic_modifier = { modifier = utopia_manifesto_district_closed_island_charter_state } } remove_dynamic_modifier = { modifier = utopia_manifesto_district_closed_island_charter_state } }
		if = { limit = { has_dynamic_modifier = { modifier = utopia_manifesto_district_humanist_charter_state } } remove_dynamic_modifier = { modifier = utopia_manifesto_district_humanist_charter_state } }
		if = { limit = { has_dynamic_modifier = { modifier = utopia_manifesto_penal_works_active_state } } remove_dynamic_modifier = { modifier = utopia_manifesto_penal_works_active_state } }
		clear_variable = utopia_manifesto_state_district_incomplete_supply
		clear_variable = utopia_manifesto_state_district_incomplete_construction
		clear_variable = utopia_manifesto_state_district_incomplete_resistance
		clear_variable = utopia_manifesto_state_district_consent_supply
		clear_variable = utopia_manifesto_state_district_consent_resistance
		clear_variable = utopia_manifesto_state_district_common_supply
		clear_variable = utopia_manifesto_state_district_common_resources
		clear_variable = utopia_manifesto_state_district_guardians_construction
		clear_variable = utopia_manifesto_state_district_guardians_resistance
		clear_variable = utopia_manifesto_state_district_closed_supply
		clear_variable = utopia_manifesto_state_district_closed_resistance
		clear_variable = utopia_manifesto_state_district_humanist_supply
		clear_variable = utopia_manifesto_state_district_humanist_resistance
		clear_variable = utopia_manifesto_state_penal_works_construction
		clear_variable = utopia_manifesto_state_penal_works_supply
		clear_variable = utopia_manifesto_state_penal_works_garrison
		clear_variable = utopia_manifesto_state_penal_works_resistance
	}

# Country scope. The changed state is saved as the regular event target
# utopia_manifesto_lost_district_state before this helper is called. District
# calling relief is state-derived, so it is reversed exactly once before the
# state charter flags are removed. Ledger movements from the charter choice
# remain in the durable policy layer as the historical consequence of that
# public decision.
utopia_manifesto_reverse_lost_district_calling_relief = {
	if = {
		limit = {
			has_event_target = utopia_manifesto_lost_district_state
			event_target:utopia_manifesto_lost_district_state = {
				has_state_flag = utopia_manifesto_district_chartered
				has_state_flag = utopia_manifesto_market_garden_district
			}
		}
		set_temp_variable = { utopia_manifesto_calling_family_input = constant:utopia_manifesto_calling_family.provisioning }
		set_temp_variable = { utopia_manifesto_calling_severity_delta = constant:utopia_manifesto_district_calling_relief.primary }
		multiply_temp_variable = { utopia_manifesto_calling_severity_delta = constant:utopia_manifesto_math.negative_one }
		utopia_manifesto_apply_prepared_calling_change = yes
		set_temp_variable = { utopia_manifesto_calling_family_input = constant:utopia_manifesto_calling_family.civic_works }
		set_temp_variable = { utopia_manifesto_calling_severity_delta = constant:utopia_manifesto_district_calling_relief.secondary }
		multiply_temp_variable = { utopia_manifesto_calling_severity_delta = constant:utopia_manifesto_math.negative_one }
		utopia_manifesto_apply_prepared_calling_change = yes
	}
	else_if = {
		limit = {
			has_event_target = utopia_manifesto_lost_district_state
			event_target:utopia_manifesto_lost_district_state = {
				has_state_flag = utopia_manifesto_district_chartered
				has_state_flag = utopia_manifesto_industrial_housing_district
			}
		}
		set_temp_variable = { utopia_manifesto_calling_family_input = constant:utopia_manifesto_calling_family.workshops }
		set_temp_variable = { utopia_manifesto_calling_severity_delta = constant:utopia_manifesto_district_calling_relief.primary }
		multiply_temp_variable = { utopia_manifesto_calling_severity_delta = constant:utopia_manifesto_math.negative_one }
		utopia_manifesto_apply_prepared_calling_change = yes
		set_temp_variable = { utopia_manifesto_calling_family_input = constant:utopia_manifesto_calling_family.civic_works }
		set_temp_variable = { utopia_manifesto_calling_severity_delta = constant:utopia_manifesto_district_calling_relief.secondary }
		multiply_temp_variable = { utopia_manifesto_calling_severity_delta = constant:utopia_manifesto_math.negative_one }
		utopia_manifesto_apply_prepared_calling_change = yes
	}
	else_if = {
		limit = {
			has_event_target = utopia_manifesto_lost_district_state
			event_target:utopia_manifesto_lost_district_state = {
				has_state_flag = utopia_manifesto_district_chartered
				has_state_flag = utopia_manifesto_rail_junction_town
			}
		}
		set_temp_variable = { utopia_manifesto_calling_family_input = constant:utopia_manifesto_calling_family.civic_works }
		set_temp_variable = { utopia_manifesto_calling_severity_delta = constant:utopia_manifesto_district_calling_relief.primary }
		multiply_temp_variable = { utopia_manifesto_calling_severity_delta = constant:utopia_manifesto_math.negative_one }
		utopia_manifesto_apply_prepared_calling_change = yes
		set_temp_variable = { utopia_manifesto_calling_family_input = constant:utopia_manifesto_calling_family.maritime_and_settlement }
		set_temp_variable = { utopia_manifesto_calling_severity_delta = constant:utopia_manifesto_district_calling_relief.secondary }
		multiply_temp_variable = { utopia_manifesto_calling_severity_delta = constant:utopia_manifesto_math.negative_one }
		utopia_manifesto_apply_prepared_calling_change = yes
	}
	else_if = {
		limit = {
			has_event_target = utopia_manifesto_lost_district_state
			event_target:utopia_manifesto_lost_district_state = {
				has_state_flag = utopia_manifesto_district_chartered
				has_state_flag = utopia_manifesto_refugee_municipality
			}
		}
		set_temp_variable = { utopia_manifesto_calling_family_input = constant:utopia_manifesto_calling_family.maritime_and_settlement }
		set_temp_variable = { utopia_manifesto_calling_severity_delta = constant:utopia_manifesto_district_calling_relief.primary }
		multiply_temp_variable = { utopia_manifesto_calling_severity_delta = constant:utopia_manifesto_math.negative_one }
		utopia_manifesto_apply_prepared_calling_change = yes
		set_temp_variable = { utopia_manifesto_calling_family_input = constant:utopia_manifesto_calling_family.learning_and_care }
		set_temp_variable = { utopia_manifesto_calling_severity_delta = constant:utopia_manifesto_district_calling_relief.secondary }
		multiply_temp_variable = { utopia_manifesto_calling_severity_delta = constant:utopia_manifesto_math.negative_one }
		utopia_manifesto_apply_prepared_calling_change = yes
	}
}

# Country scope. Rebuilds current district capacity and achievement proof from
# owned, controlled core states. This bounded actor scan makes repeated calls
# idempotent and never iterates over countries.
utopia_manifesto_rebuild_district_network_proof = {
	if = {
		limit = { has_country_flag = utopia_manifesto_district_system_proven }
		set_country_flag = utopia_manifesto_district_network_proof_before_rebuild
	}
	else = { clr_country_flag = utopia_manifesto_district_network_proof_before_rebuild }
	set_variable = { utopia_manifesto_completed_district_projects = constant:utopia_manifesto_math.zero }
	set_variable = { utopia_manifesto_chartered_districts = constant:utopia_manifesto_math.zero }
	clr_country_flag = utopia_manifesto_district_system_proven
	clr_country_flag = utopia_manifesto_achievement_five_district_roles_proven
	clr_country_flag = utopia_manifesto_achievement_district_role_market_garden
	clr_country_flag = utopia_manifesto_achievement_district_role_industrial_housing
	clr_country_flag = utopia_manifesto_achievement_district_role_rail_junction
	clr_country_flag = utopia_manifesto_achievement_district_role_refugee_municipality
	save_event_target_as = utopia_manifesto_district_network_actor
	every_owned_state = {
		limit = {
			is_controlled_by = event_target:utopia_manifesto_district_network_actor
			is_core_of = event_target:utopia_manifesto_district_network_actor
		}
		if = {
			limit = { has_state_flag = utopia_manifesto_district_built }
			event_target:utopia_manifesto_district_network_actor = {
				add_to_variable = { utopia_manifesto_completed_district_projects = constant:utopia_manifesto_decision_value.project_increment }
			}
		}
		if = {
			limit = { has_state_flag = utopia_manifesto_district_chartered }
			event_target:utopia_manifesto_district_network_actor = {
				add_to_variable = { utopia_manifesto_chartered_districts = constant:utopia_manifesto_decision_value.project_increment }
			}
			if = { limit = { has_state_flag = utopia_manifesto_market_garden_district } event_target:utopia_manifesto_district_network_actor = { set_country_flag = utopia_manifesto_achievement_district_role_market_garden } }
			if = { limit = { has_state_flag = utopia_manifesto_industrial_housing_district } event_target:utopia_manifesto_district_network_actor = { set_country_flag = utopia_manifesto_achievement_district_role_industrial_housing } }
			if = { limit = { has_state_flag = utopia_manifesto_rail_junction_town } event_target:utopia_manifesto_district_network_actor = { set_country_flag = utopia_manifesto_achievement_district_role_rail_junction } }
			if = { limit = { has_state_flag = utopia_manifesto_refugee_municipality } event_target:utopia_manifesto_district_network_actor = { set_country_flag = utopia_manifesto_achievement_district_role_refugee_municipality } }
		}
	}
	if = {
		limit = {
			check_variable = {
				var = utopia_manifesto_chartered_districts
				value = constant:utopia_manifesto_decision_threshold.district_project_limit
				compare = greater_than_or_equals
			}
		}
		set_country_flag = utopia_manifesto_district_system_proven
	}
	if = {
		limit = {
			has_country_flag = utopia_manifesto_district_network_proof_before_rebuild
			NOT = { has_country_flag = utopia_manifesto_district_system_proven }
			OR = {
				has_country_flag = utopia_manifesto_garden_district_stage_founding
				has_country_flag = utopia_manifesto_garden_district_stage_mitigated
				has_country_flag = utopia_manifesto_garden_district_stage_final
			}
		}
		utopia_manifesto_break_garden_district_network = yes
	}
	clr_country_flag = utopia_manifesto_district_network_proof_before_rebuild
	utopia_manifesto_refresh_achievement_planned_district_proof = yes
	utopia_manifesto_refresh_formation_proof = yes
}

# Country scope. Clears only the displayed/current project record that points
# to the exact lost state. Other country-wide district history and conduct
# remain untouched.
utopia_manifesto_clear_exact_lost_district_project_pointer = {
	if = {
		limit = {
			has_event_target = utopia_manifesto_lost_district_state
			has_variable = utopia_manifesto_district_project_state
			var:utopia_manifesto_district_project_state = {
				state = event_target:utopia_manifesto_lost_district_state
			}
		}
		clr_country_flag = utopia_manifesto_district_survey_active
		clr_country_flag = utopia_manifesto_district_build_active
		clr_country_flag = utopia_manifesto_district_charter_active
		clr_country_flag = utopia_manifesto_district_survey_found_no_role
		clr_country_flag = utopia_manifesto_district_project_debt
		clr_country_flag = utopia_manifesto_district_shortfall_published
		clr_country_flag = utopia_manifesto_district_remediation_declared
		clr_country_flag = utopia_manifesto_district_scope_reduced
		clr_country_flag = utopia_manifesto_district_obligation_dispute_unresolved
		clear_variable = utopia_manifesto_district_project_state
		clear_variable = utopia_manifesto_district_project_type
		clear_variable = utopia_manifesto_district_project_phase
		clear_variable = utopia_manifesto_district_days
	}
}

# Country scope. Reconciles the one changed district state saved by the
# on-action or the bounded actor pulse. Physical construction remains on the
# map, while Event 15 role, charter, modifier, calling, and proof surfaces end.
utopia_manifesto_handle_district_state_control_loss = {
	utopia_manifesto_clear_exact_lost_district_project_pointer = yes
	if = {
		limit = {
			has_event_target = utopia_manifesto_lost_district_state
			event_target:utopia_manifesto_lost_district_state = {
				OR = {
					has_state_flag = utopia_manifesto_district_survey_active
					has_state_flag = utopia_manifesto_district_build_active
					has_state_flag = utopia_manifesto_district_charter_active
					has_state_flag = utopia_manifesto_penal_works_method_active
				}
			}
		}
		utopia_manifesto_fail_district_project_after_state_loss = yes
	}
	utopia_manifesto_reverse_lost_district_calling_relief = yes
	if = {
		limit = { has_event_target = utopia_manifesto_lost_district_state }
		event_target:utopia_manifesto_lost_district_state = { utopia_manifesto_clear_district_state_package = yes }
		remove_from_array = { array = utopia_manifesto_district_states value = event_target:utopia_manifesto_lost_district_state }
	}
	set_country_flag = utopia_manifesto_district_project_failed_on_state_loss
	utopia_manifesto_rebuild_district_network_proof = yes
	utopia_manifesto_refresh_ledger = yes
	utopia_manifesto_refresh_dynamic_costs = yes
}

# Country scope. The existing actor pulse calls this bounded tracked-state
# reconciliation so a peaceful ownership transfer cannot retain a route
# charter even if no combat control callback was emitted.
utopia_manifesto_reconcile_tracked_district_state_control = {
	clear_temp_array = utopia_manifesto_district_state_reconciliation_snapshot
	save_event_target_as = utopia_manifesto_district_reconciliation_actor
	for_each_scope_loop = {
		array = utopia_manifesto_district_states
		add_to_temp_array = { utopia_manifesto_district_state_reconciliation_snapshot = THIS }
	}
	for_each_scope_loop = {
		array = utopia_manifesto_district_state_reconciliation_snapshot
		if = {
			limit = {
				OR = {
					NOT = { is_owned_by = event_target:utopia_manifesto_district_reconciliation_actor }
					NOT = { is_controlled_by = event_target:utopia_manifesto_district_reconciliation_actor }
					NOT = { is_core_of = event_target:utopia_manifesto_district_reconciliation_actor }
				}
			}
			save_event_target_as = utopia_manifesto_lost_district_state
			event_target:utopia_manifesto_district_reconciliation_actor = { utopia_manifesto_handle_district_state_control_loss = yes }
		}
	}
	clear_temp_array = utopia_manifesto_district_state_reconciliation_snapshot
}

utopia_manifesto_clear_all_district_state_runtime = {
	utopia_manifesto_end_penal_works_method = yes
	for_each_scope_loop = {
		array = utopia_manifesto_district_states
		utopia_manifesto_clear_district_state_package = yes
	}
	clear_array = utopia_manifesto_district_states
	clear_variable = utopia_manifesto_completed_district_projects
	clear_variable = utopia_manifesto_chartered_districts
	clr_country_flag = utopia_manifesto_district_system_proven
	clr_country_flag = utopia_manifesto_district_network_proof_before_rebuild
	clr_country_flag = utopia_manifesto_achievement_five_district_roles_proven
	clr_country_flag = utopia_manifesto_achievement_district_role_market_garden
	clr_country_flag = utopia_manifesto_achievement_district_role_industrial_housing
	clr_country_flag = utopia_manifesto_achievement_district_role_rail_junction
	clr_country_flag = utopia_manifesto_achievement_district_role_refugee_municipality
	clr_country_flag = utopia_manifesto_achievement_district_role_provision_ring
	clr_country_flag = utopia_manifesto_district_survey_active
	clr_country_flag = utopia_manifesto_district_build_active
	clr_country_flag = utopia_manifesto_district_charter_active
	clr_country_flag = utopia_manifesto_district_survey_found_no_role
	clr_country_flag = utopia_manifesto_district_project_debt
	clr_country_flag = utopia_manifesto_district_shortfall_published
	clr_country_flag = utopia_manifesto_district_remediation_declared
	clr_country_flag = utopia_manifesto_district_scope_reduced
	clr_country_flag = utopia_manifesto_district_obligation_dispute_unresolved
	clr_country_flag = utopia_manifesto_district_project_failed_on_state_loss
	clr_country_flag = utopia_manifesto_district_data_exposure
	clr_country_flag = utopia_manifesto_district_assigned_charter_used
	clr_country_flag = utopia_manifesto_district_assigned_charter_appeals_funded
	clr_country_flag = utopia_manifesto_district_assigned_charter_observed
	clr_country_flag = utopia_manifesto_district_assignment_repaired
	clr_country_flag = utopia_manifesto_district_assignment_unresolved
	clr_country_flag = utopia_manifesto_district_review_clause_recorded
	clr_country_flag = utopia_manifesto_district_plan_committed_recent
	clr_country_flag = utopia_manifesto_penal_works_method_active
	clr_country_flag = utopia_manifesto_penal_works_incident_seen
	clr_country_flag = utopia_manifesto_penal_works_rolls_published
	clr_country_flag = utopia_manifesto_penal_works_foreign_inspection
	clr_country_flag = utopia_manifesto_penal_works_timetable_hardened
	clr_country_flag = utopia_manifesto_penal_works_halted_after_first_use
	clr_country_flag = utopia_manifesto_penal_works_project_completed
	clr_country_flag = utopia_manifesto_foreign_opposition_from_penal_works
	clr_country_flag = utopia_manifesto_foreign_opposition_from_district_assignment
	clr_country_flag = utopia_manifesto_conduct_district_timetable_imposed
	clr_country_flag = utopia_manifesto_conduct_district_assignment
	clr_country_flag = utopia_manifesto_conduct_penal_labor
	clr_country_flag = utopia_manifesto_penal_labour_used
	clear_variable = utopia_manifesto_district_project_state
	clear_variable = utopia_manifesto_district_project_type
	clear_variable = utopia_manifesto_district_project_phase
	clear_variable = utopia_manifesto_district_visual_role
	clear_variable = utopia_manifesto_district_days
	clear_variable = utopia_manifesto_penal_works_project_state
	clear_variable = utopia_manifesto_penal_works_last_civilian_loss
	clear_variable = utopia_manifesto_penal_works_total_civilian_loss
	clear_variable = utopia_manifesto_penal_works_projects_completed
}

utopia_manifesto_select_market_garden_district_project = {
	set_variable = { utopia_manifesto_district_project_type = constant:utopia_manifesto_decision_district_role.market_garden }
	set_variable = { utopia_manifesto_district_visual_role = constant:utopia_manifesto_decision_district_role.market_garden }
	utopia_manifesto_register_district_project_state = yes
}

utopia_manifesto_select_industrial_housing_district_project = {
	set_variable = { utopia_manifesto_district_project_type = constant:utopia_manifesto_decision_district_role.industrial_housing }
	set_variable = { utopia_manifesto_district_visual_role = constant:utopia_manifesto_decision_district_role.industrial_housing }
	utopia_manifesto_register_district_project_state = yes
}

utopia_manifesto_select_rail_junction_district_project = {
	set_variable = { utopia_manifesto_district_project_type = constant:utopia_manifesto_decision_district_role.rail_junction }
	set_variable = { utopia_manifesto_district_visual_role = constant:utopia_manifesto_decision_district_role.rail_junction }
	utopia_manifesto_register_district_project_state = yes
}

utopia_manifesto_select_refugee_municipality_district_project = {
	set_variable = { utopia_manifesto_district_project_type = constant:utopia_manifesto_decision_district_role.refugee_municipality }
	set_variable = { utopia_manifesto_district_visual_role = constant:utopia_manifesto_decision_district_role.refugee_municipality }
	utopia_manifesto_register_district_project_state = yes
}

utopia_manifesto_complete_property_transition = {
	clr_country_flag = utopia_manifesto_property_project_active
	FROM = { clr_state_flag = utopia_manifesto_property_project_active }
	add_to_variable = { utopia_manifesto_completed_property_projects = constant:utopia_manifesto_decision_value.project_increment }
	if = {
		limit = { check_variable = { utopia_manifesto_property_project_type = constant:utopia_manifesto_decision_property_project.land_trust } }
		FROM = {
			set_state_flag = utopia_manifesto_land_trust_established
			add_building_construction = { type = infrastructure level = constant:utopia_manifesto_decision_building.basic instant_build = yes }
		}
	}
	else_if = {
		limit = { check_variable = { utopia_manifesto_property_project_type = constant:utopia_manifesto_decision_property_project.worker_council } }
		FROM = {
			set_state_flag = utopia_manifesto_worker_council_established
			add_extra_state_shared_building_slots = constant:utopia_manifesto_decision_value.shared_slot_increment
			add_building_construction = { type = industrial_complex level = constant:utopia_manifesto_decision_building.basic instant_build = yes }
		}
	}
	else_if = {
		limit = { check_variable = { utopia_manifesto_property_project_type = constant:utopia_manifesto_decision_property_project.productive_tenure } }
		FROM = {
			set_state_flag = utopia_manifesto_productive_tenure_established
			add_building_construction = { type = infrastructure level = constant:utopia_manifesto_decision_building.basic instant_build = yes }
		}
	}
	else_if = {
		limit = { check_variable = { utopia_manifesto_property_project_type = constant:utopia_manifesto_decision_property_project.idle_grant } }
		FROM = {
			set_state_flag = utopia_manifesto_idle_grant_revoked
			add_extra_state_shared_building_slots = constant:utopia_manifesto_decision_value.shared_slot_increment
			add_building_construction = { type = arms_factory level = constant:utopia_manifesto_decision_building.basic instant_build = yes }
		}
	}
	set_temp_variable = { utopia_manifesto_need_delta = constant:utopia_manifesto_decision_change.ledger_nudge_loss }
	set_temp_variable = { utopia_manifesto_plenty_delta = constant:utopia_manifesto_decision_change.ledger_step }
	set_temp_variable = { utopia_manifesto_concord_delta = constant:utopia_manifesto_decision_change.ledger_nudge }
	set_temp_variable = { utopia_manifesto_assignment_delta = constant:utopia_manifesto_decision_value.none }
	if = {
		limit = {
			OR = {
				check_variable = { utopia_manifesto_property_project_type = constant:utopia_manifesto_decision_property_project.productive_tenure }
				check_variable = { utopia_manifesto_property_project_type = constant:utopia_manifesto_decision_property_project.idle_grant }
			}
		}
		set_temp_variable = { utopia_manifesto_concord_delta = constant:utopia_manifesto_decision_change.ledger_nudge_loss }
		set_temp_variable = { utopia_manifesto_assignment_delta = constant:utopia_manifesto_decision_change.ledger_step }
	}
	utopia_manifesto_apply_prepared_ledger_delta = yes
	if = {
		limit = { has_idea = utopia_manifesto_inherited_order }
		utopia_manifesto_register_inherited_property = yes
	}
}

utopia_manifesto_fail_property_transition = {
	clr_country_flag = utopia_manifesto_property_project_active
	FROM = {
		clr_state_flag = utopia_manifesto_property_project_active
		set_state_flag = utopia_manifesto_property_transition_disputed
	}
	if = {
		limit = { check_variable = { utopia_manifesto_property_project_type = constant:utopia_manifesto_decision_property_project.productive_tenure } }
		set_country_flag = utopia_manifesto_data_scandal
	}
	set_temp_variable = { utopia_manifesto_need_delta = constant:utopia_manifesto_decision_value.none }
	set_temp_variable = { utopia_manifesto_plenty_delta = constant:utopia_manifesto_decision_change.ledger_nudge_loss }
	set_temp_variable = { utopia_manifesto_concord_delta = constant:utopia_manifesto_decision_change.ledger_step_loss }
	set_temp_variable = { utopia_manifesto_assignment_delta = constant:utopia_manifesto_decision_change.ledger_nudge }
	utopia_manifesto_apply_prepared_ledger_delta = yes
}

utopia_manifesto_complete_district_survey = {
	clr_country_flag = utopia_manifesto_district_survey_active
	FROM = { clr_state_flag = utopia_manifesto_district_survey_active }
	if = {
		limit = { FROM = { utopia_manifesto_state_has_any_suitable_district_role = yes } }
		utopia_manifesto_record_district_role_suitability = yes
		FROM = { set_state_flag = utopia_manifesto_district_site_surveyed }
		set_variable = { utopia_manifesto_district_project_phase = constant:utopia_manifesto_district_phase.surveyed }
		set_temp_variable = { utopia_manifesto_need_delta = constant:utopia_manifesto_decision_change.ledger_nudge_loss }
		set_temp_variable = { utopia_manifesto_plenty_delta = constant:utopia_manifesto_decision_value.none }
		set_temp_variable = { utopia_manifesto_concord_delta = constant:utopia_manifesto_decision_change.ledger_nudge }
		set_temp_variable = { utopia_manifesto_assignment_delta = constant:utopia_manifesto_decision_value.none }
	}
	else = {
		FROM = {
			clr_state_flag = utopia_manifesto_district_site_surveyed
			set_state_flag = utopia_manifesto_district_project_refused
		}
		set_country_flag = utopia_manifesto_district_survey_found_no_role
		set_variable = { utopia_manifesto_district_project_phase = constant:utopia_manifesto_district_phase.none }
		set_temp_variable = { utopia_manifesto_need_delta = constant:utopia_manifesto_decision_change.ledger_nudge }
		set_temp_variable = { utopia_manifesto_plenty_delta = constant:utopia_manifesto_decision_change.ledger_nudge_loss }
		set_temp_variable = { utopia_manifesto_concord_delta = constant:utopia_manifesto_decision_change.ledger_nudge_loss }
		set_temp_variable = { utopia_manifesto_assignment_delta = constant:utopia_manifesto_decision_value.none }
		country_event = { id = chaosx.nr15.45 }
	}
	utopia_manifesto_apply_prepared_ledger_delta = yes
}

utopia_manifesto_fail_district_project = {
	if = {
		limit = { utopia_manifesto_is_guardians_of_measure_route = yes }
		set_country_flag = utopia_manifesto_assignment_revolt
	}
	clr_country_flag = utopia_manifesto_district_survey_active
	clr_country_flag = utopia_manifesto_district_build_active
	clr_country_flag = utopia_manifesto_district_charter_active
	utopia_manifesto_clear_active_district_project_state = yes
	utopia_manifesto_clear_district_role_suitability_flags = yes
	FROM = {
		clr_state_flag = utopia_manifesto_district_survey_active
		set_state_flag = utopia_manifesto_district_project_refused
	}
	set_variable = { utopia_manifesto_district_project_phase = constant:utopia_manifesto_district_phase.none }
	clear_variable = utopia_manifesto_district_project_state
	clear_variable = utopia_manifesto_district_project_type
	set_temp_variable = { utopia_manifesto_need_delta = constant:utopia_manifesto_decision_change.ledger_nudge }
	set_temp_variable = { utopia_manifesto_plenty_delta = constant:utopia_manifesto_decision_change.ledger_step_loss }
	set_temp_variable = { utopia_manifesto_concord_delta = constant:utopia_manifesto_decision_change.ledger_step_loss }
	set_temp_variable = { utopia_manifesto_assignment_delta = constant:utopia_manifesto_decision_change.ledger_nudge }
	utopia_manifesto_apply_prepared_ledger_delta = yes
	if = {
		limit = {
			OR = {
				has_country_flag = utopia_manifesto_garden_district_stage_founding
				has_country_flag = utopia_manifesto_garden_district_stage_mitigated
				has_country_flag = utopia_manifesto_garden_district_stage_final
			}
		}
		utopia_manifesto_break_garden_district_network = yes
	}
	country_event = { id = chaosx.nr15.43 }
}

# Country scope. Used by state-control callbacks, where the changed state is not
# a stable FROM scope for nested scripted effects. The stored project-state
# scope is authoritative and no completed district output is granted.
utopia_manifesto_fail_district_project_after_state_loss = {
	utopia_manifesto_end_penal_works_method = yes
	if = {
		limit = { has_variable = utopia_manifesto_district_project_state }
		var:utopia_manifesto_district_project_state = {
			clr_state_flag = utopia_manifesto_district_survey_active
			clr_state_flag = utopia_manifesto_district_build_active
			clr_state_flag = utopia_manifesto_district_charter_active
			clr_state_flag = utopia_manifesto_district_role_market_garden_active
			clr_state_flag = utopia_manifesto_district_role_industrial_housing_active
			clr_state_flag = utopia_manifesto_district_role_rail_junction_active
			clr_state_flag = utopia_manifesto_district_role_refugee_municipality_active
			clr_state_flag = utopia_manifesto_district_housing_commitment
			clr_state_flag = utopia_manifesto_district_transport_commitment
			clr_state_flag = utopia_manifesto_district_role_plan_commitment
			clr_state_flag = utopia_manifesto_district_market_garden_suitable
			clr_state_flag = utopia_manifesto_district_industrial_housing_suitable
			clr_state_flag = utopia_manifesto_district_rail_junction_suitable
			clr_state_flag = utopia_manifesto_district_refugee_municipality_suitable
			set_state_flag = utopia_manifesto_district_project_lost
		}
	}
	clr_country_flag = utopia_manifesto_district_survey_active
	clr_country_flag = utopia_manifesto_district_build_active
	clr_country_flag = utopia_manifesto_district_charter_active
	set_country_flag = utopia_manifesto_district_project_failed_on_state_loss
	set_variable = { utopia_manifesto_district_project_phase = constant:utopia_manifesto_district_phase.none }
	clear_variable = utopia_manifesto_district_project_state
	clear_variable = utopia_manifesto_district_project_type
	set_temp_variable = { utopia_manifesto_need_delta = constant:utopia_manifesto_decision_change.ledger_nudge }
	set_temp_variable = { utopia_manifesto_plenty_delta = constant:utopia_manifesto_decision_change.ledger_step_loss }
	set_temp_variable = { utopia_manifesto_concord_delta = constant:utopia_manifesto_decision_change.ledger_step_loss }
	set_temp_variable = { utopia_manifesto_assignment_delta = constant:utopia_manifesto_decision_value.none }
	utopia_manifesto_apply_prepared_ledger_delta = yes
	if = {
		limit = {
			OR = {
				has_country_flag = utopia_manifesto_garden_district_stage_founding
				has_country_flag = utopia_manifesto_garden_district_stage_mitigated
				has_country_flag = utopia_manifesto_garden_district_stage_final
			}
		}
		utopia_manifesto_break_garden_district_network = yes
	}
}

utopia_manifesto_resolve_data_scandal = {
	clr_country_flag = utopia_manifesto_data_scandal
	set_country_flag = utopia_manifesto_data_scandal_resolved
	set_temp_variable = { utopia_manifesto_need_delta = constant:utopia_manifesto_decision_change.ledger_nudge_loss }
	set_temp_variable = { utopia_manifesto_plenty_delta = constant:utopia_manifesto_decision_change.ledger_nudge_loss }
	set_temp_variable = { utopia_manifesto_concord_delta = constant:utopia_manifesto_decision_change.ledger_step }
	set_temp_variable = { utopia_manifesto_assignment_delta = constant:utopia_manifesto_decision_change.ledger_nudge_loss }
	utopia_manifesto_apply_prepared_ledger_delta = yes
	country_event = { id = chaosx.nr15.91 }
	utopia_manifesto_refresh_formation_proof = yes
}

utopia_manifesto_resolve_assignment_revolt = {
	clr_country_flag = utopia_manifesto_assignment_revolt
	set_country_flag = utopia_manifesto_assignment_revolt_resolved
	set_temp_variable = { utopia_manifesto_need_delta = constant:utopia_manifesto_decision_change.ledger_nudge_loss }
	set_temp_variable = { utopia_manifesto_plenty_delta = constant:utopia_manifesto_decision_change.ledger_nudge_loss }
	set_temp_variable = { utopia_manifesto_concord_delta = constant:utopia_manifesto_decision_change.ledger_shift }
	set_temp_variable = { utopia_manifesto_assignment_delta = constant:utopia_manifesto_decision_change.ledger_step_loss }
	utopia_manifesto_apply_prepared_ledger_delta = yes
	country_event = { id = chaosx.nr15.93 }
	utopia_manifesto_refresh_formation_proof = yes
}

utopia_manifesto_resolve_district_build = {
	if = {
		limit = { FROM = { utopia_manifesto_district_project_obligations_met = yes } }
		utopia_manifesto_complete_garden_district = yes
	}
	else = { utopia_manifesto_complete_partial_district = yes }
}

utopia_manifesto_complete_partial_district = {
	clr_country_flag = utopia_manifesto_district_build_active
	utopia_manifesto_clear_active_district_project_state = yes
	set_variable = { utopia_manifesto_district_project_phase = constant:utopia_manifesto_district_phase.delayed }
	set_country_flag = utopia_manifesto_district_project_debt
	FROM = {
		set_state_flag = utopia_manifesto_district_project_delayed
		set_state_flag = utopia_manifesto_district_obligations_breached
		set_variable = { utopia_manifesto_state_district_incomplete_supply = constant:utopia_manifesto_district_state_modifier.incomplete_supply }
		set_variable = { utopia_manifesto_state_district_incomplete_construction = constant:utopia_manifesto_district_state_modifier.incomplete_construction }
		set_variable = { utopia_manifesto_state_district_incomplete_resistance = constant:utopia_manifesto_district_state_modifier.incomplete_resistance }
		add_dynamic_modifier = { modifier = utopia_manifesto_incomplete_district_state }
	}
	set_temp_variable = { utopia_manifesto_need_delta = constant:utopia_manifesto_decision_change.ledger_nudge }
	set_temp_variable = { utopia_manifesto_plenty_delta = constant:utopia_manifesto_decision_change.ledger_step_loss }
	set_temp_variable = { utopia_manifesto_concord_delta = constant:utopia_manifesto_decision_change.ledger_nudge_loss }
	set_temp_variable = { utopia_manifesto_assignment_delta = constant:utopia_manifesto_decision_value.none }
	utopia_manifesto_apply_prepared_ledger_delta = yes
	country_event = { id = chaosx.nr15.45 }
}

utopia_manifesto_complete_garden_district = {
	utopia_manifesto_complete_penal_works_method = yes
	clr_country_flag = utopia_manifesto_district_build_active
	clr_country_flag = utopia_manifesto_district_project_debt
	clr_country_flag = utopia_manifesto_district_remediation_declared
	clr_country_flag = utopia_manifesto_district_obligation_dispute_unresolved
	add_to_variable = { utopia_manifesto_completed_district_projects = constant:utopia_manifesto_decision_value.project_increment }
	FROM = {
		set_state_flag = utopia_manifesto_district_built
		clr_state_flag = utopia_manifesto_district_project_delayed
		clr_state_flag = utopia_manifesto_district_obligations_breached
		if = { limit = { has_dynamic_modifier = { modifier = utopia_manifesto_incomplete_district_state } } remove_dynamic_modifier = { modifier = utopia_manifesto_incomplete_district_state } }
		clear_variable = utopia_manifesto_state_district_incomplete_supply
		clear_variable = utopia_manifesto_state_district_incomplete_construction
		clear_variable = utopia_manifesto_state_district_incomplete_resistance
	}
	if = {
		limit = { check_variable = { utopia_manifesto_district_project_type = constant:utopia_manifesto_decision_district_role.market_garden } }
		FROM = {
			set_state_flag = utopia_manifesto_market_garden_district
			add_building_construction = { type = infrastructure level = constant:utopia_manifesto_decision_building.basic instant_build = yes }
		}
	}
	else_if = {
		limit = { check_variable = { utopia_manifesto_district_project_type = constant:utopia_manifesto_decision_district_role.industrial_housing } }
		FROM = {
			set_state_flag = utopia_manifesto_industrial_housing_district
			add_extra_state_shared_building_slots = constant:utopia_manifesto_decision_value.shared_slot_increment
			add_building_construction = { type = industrial_complex level = constant:utopia_manifesto_decision_building.basic instant_build = yes }
		}
	}
	else_if = {
		limit = { check_variable = { utopia_manifesto_district_project_type = constant:utopia_manifesto_decision_district_role.rail_junction } }
		FROM = {
			set_state_flag = utopia_manifesto_rail_junction_town
			add_building_construction = { type = infrastructure level = constant:utopia_manifesto_decision_building.expanded instant_build = yes }
		}
	}
	else_if = {
		limit = { check_variable = { utopia_manifesto_district_project_type = constant:utopia_manifesto_decision_district_role.refugee_municipality } }
		FROM = {
			set_state_flag = utopia_manifesto_refugee_municipality
			add_extra_state_shared_building_slots = constant:utopia_manifesto_decision_value.shared_slot_increment
			add_building_construction = { type = industrial_complex level = constant:utopia_manifesto_decision_building.basic instant_build = yes }
		}
	}
	utopia_manifesto_clear_active_district_project_state = yes
	utopia_manifesto_clear_district_role_suitability_flags = yes
	set_variable = { utopia_manifesto_district_project_phase = constant:utopia_manifesto_district_phase.built }
	set_temp_variable = { utopia_manifesto_need_delta = constant:utopia_manifesto_decision_change.ledger_step_loss }
	set_temp_variable = { utopia_manifesto_plenty_delta = constant:utopia_manifesto_decision_change.ledger_step }
	set_temp_variable = { utopia_manifesto_concord_delta = constant:utopia_manifesto_decision_change.ledger_nudge }
	set_temp_variable = { utopia_manifesto_assignment_delta = constant:utopia_manifesto_decision_value.none }
	utopia_manifesto_apply_prepared_ledger_delta = yes
	if = {
		limit = { NOT = { has_country_flag = utopia_manifesto_first_district_shape_seen } }
		set_country_flag = utopia_manifesto_first_district_shape_seen
		country_event = { id = chaosx.nr15.40 }
	}
}

utopia_manifesto_set_district_project_type_from_state = {
	if = { limit = { FROM = { has_state_flag = utopia_manifesto_market_garden_district } } set_variable = { utopia_manifesto_district_project_type = constant:utopia_manifesto_decision_district_role.market_garden } }
	else_if = { limit = { FROM = { has_state_flag = utopia_manifesto_industrial_housing_district } } set_variable = { utopia_manifesto_district_project_type = constant:utopia_manifesto_decision_district_role.industrial_housing } }
	else_if = { limit = { FROM = { has_state_flag = utopia_manifesto_rail_junction_town } } set_variable = { utopia_manifesto_district_project_type = constant:utopia_manifesto_decision_district_role.rail_junction } }
	else_if = { limit = { FROM = { has_state_flag = utopia_manifesto_refugee_municipality } } set_variable = { utopia_manifesto_district_project_type = constant:utopia_manifesto_decision_district_role.refugee_municipality } }
}

utopia_manifesto_start_district_charter = {
	utopia_manifesto_set_district_project_type_from_state = yes
	set_variable = { utopia_manifesto_district_project_state = FROM }
	set_variable = { utopia_manifesto_district_project_phase = constant:utopia_manifesto_district_phase.chartering }
	set_country_flag = utopia_manifesto_district_charter_active
	FROM = { set_state_flag = utopia_manifesto_district_charter_active }
	if = {
		limit = { utopia_manifesto_is_consent_of_households_route = yes }
		add_to_variable = { utopia_manifesto_district_days = constant:utopia_manifesto_district_duration.consent_charter_addition }
		clamp_variable = { var = utopia_manifesto_district_days min = constant:utopia_manifesto_decision_duration.minimum max = constant:utopia_manifesto_decision_duration.district_maximum }
	}
}

utopia_manifesto_apply_district_role_calling_relief = {
	if = {
		limit = { FROM = { has_state_flag = utopia_manifesto_market_garden_district } }
		set_temp_variable = { utopia_manifesto_calling_family_input = constant:utopia_manifesto_calling_family.provisioning }
		set_temp_variable = { utopia_manifesto_calling_severity_delta = constant:utopia_manifesto_district_calling_relief.primary }
		utopia_manifesto_apply_prepared_calling_change = yes
		set_temp_variable = { utopia_manifesto_calling_family_input = constant:utopia_manifesto_calling_family.civic_works }
		set_temp_variable = { utopia_manifesto_calling_severity_delta = constant:utopia_manifesto_district_calling_relief.secondary }
		utopia_manifesto_apply_prepared_calling_change = yes
	}
	else_if = {
		limit = { FROM = { has_state_flag = utopia_manifesto_industrial_housing_district } }
		set_temp_variable = { utopia_manifesto_calling_family_input = constant:utopia_manifesto_calling_family.workshops }
		set_temp_variable = { utopia_manifesto_calling_severity_delta = constant:utopia_manifesto_district_calling_relief.primary }
		utopia_manifesto_apply_prepared_calling_change = yes
		set_temp_variable = { utopia_manifesto_calling_family_input = constant:utopia_manifesto_calling_family.civic_works }
		set_temp_variable = { utopia_manifesto_calling_severity_delta = constant:utopia_manifesto_district_calling_relief.secondary }
		utopia_manifesto_apply_prepared_calling_change = yes
	}
	else_if = {
		limit = { FROM = { has_state_flag = utopia_manifesto_rail_junction_town } }
		set_temp_variable = { utopia_manifesto_calling_family_input = constant:utopia_manifesto_calling_family.civic_works }
		set_temp_variable = { utopia_manifesto_calling_severity_delta = constant:utopia_manifesto_district_calling_relief.primary }
		utopia_manifesto_apply_prepared_calling_change = yes
		set_temp_variable = { utopia_manifesto_calling_family_input = constant:utopia_manifesto_calling_family.maritime_and_settlement }
		set_temp_variable = { utopia_manifesto_calling_severity_delta = constant:utopia_manifesto_district_calling_relief.secondary }
		utopia_manifesto_apply_prepared_calling_change = yes
	}
	else_if = {
		limit = { FROM = { has_state_flag = utopia_manifesto_refugee_municipality } }
		set_temp_variable = { utopia_manifesto_calling_family_input = constant:utopia_manifesto_calling_family.maritime_and_settlement }
		set_temp_variable = { utopia_manifesto_calling_severity_delta = constant:utopia_manifesto_district_calling_relief.primary }
		utopia_manifesto_apply_prepared_calling_change = yes
		set_temp_variable = { utopia_manifesto_calling_family_input = constant:utopia_manifesto_calling_family.learning_and_care }
		set_temp_variable = { utopia_manifesto_calling_severity_delta = constant:utopia_manifesto_district_calling_relief.secondary }
		utopia_manifesto_apply_prepared_calling_change = yes
	}
}

utopia_manifesto_apply_district_route_charter = {
	set_temp_variable = { utopia_manifesto_need_delta = constant:utopia_manifesto_decision_value.none }
	set_temp_variable = { utopia_manifesto_plenty_delta = constant:utopia_manifesto_decision_value.none }
	set_temp_variable = { utopia_manifesto_concord_delta = constant:utopia_manifesto_decision_value.none }
	set_temp_variable = { utopia_manifesto_assignment_delta = constant:utopia_manifesto_decision_value.none }
	if = {
		limit = { utopia_manifesto_is_consent_of_households_route = yes }
		FROM = {
			set_state_flag = utopia_manifesto_district_consent_charter
			set_variable = { utopia_manifesto_state_district_consent_supply = constant:utopia_manifesto_district_state_modifier.consent_supply }
			set_variable = { utopia_manifesto_state_district_consent_resistance = constant:utopia_manifesto_district_state_modifier.consent_resistance }
			add_dynamic_modifier = { modifier = utopia_manifesto_district_consent_charter_state }
		}
		set_temp_variable = { utopia_manifesto_plenty_delta = constant:utopia_manifesto_decision_change.ledger_nudge }
		set_temp_variable = { utopia_manifesto_concord_delta = constant:utopia_manifesto_decision_change.ledger_step }
		set_temp_variable = { utopia_manifesto_assignment_delta = constant:utopia_manifesto_decision_change.ledger_nudge_loss }
	}
	else_if = {
		limit = { utopia_manifesto_is_common_table_route = yes }
		FROM = {
			set_state_flag = utopia_manifesto_district_common_table_charter
			set_variable = { utopia_manifesto_state_district_common_supply = constant:utopia_manifesto_district_state_modifier.common_table_supply }
			set_variable = { utopia_manifesto_state_district_common_resources = constant:utopia_manifesto_district_state_modifier.common_table_resources }
			add_dynamic_modifier = { modifier = utopia_manifesto_district_common_table_charter_state }
		}
		subtract_from_variable = { utopia_reserve_score = constant:utopia_manifesto_decision_change.reserve_nudge }
		set_temp_variable = { utopia_manifesto_plenty_delta = constant:utopia_manifesto_decision_change.ledger_step }
		set_temp_variable = { utopia_manifesto_concord_delta = constant:utopia_manifesto_decision_change.ledger_nudge }
		set_temp_variable = { utopia_manifesto_assignment_delta = constant:utopia_manifesto_decision_change.ledger_nudge }
	}
	else_if = {
		limit = { utopia_manifesto_is_guardians_of_measure_route = yes }
		FROM = {
			set_state_flag = utopia_manifesto_district_guardians_charter
			set_variable = { utopia_manifesto_state_district_guardians_construction = constant:utopia_manifesto_district_state_modifier.guardians_construction }
			set_variable = { utopia_manifesto_state_district_guardians_resistance = constant:utopia_manifesto_district_state_modifier.guardians_resistance }
			add_dynamic_modifier = { modifier = utopia_manifesto_district_guardians_charter_state }
		}
		set_country_flag = utopia_manifesto_district_data_exposure
		set_temp_variable = { utopia_manifesto_plenty_delta = constant:utopia_manifesto_decision_change.ledger_step }
		set_temp_variable = { utopia_manifesto_concord_delta = constant:utopia_manifesto_decision_change.ledger_nudge_loss }
		set_temp_variable = { utopia_manifesto_assignment_delta = constant:utopia_manifesto_decision_change.ledger_step }
	}
	else_if = {
		limit = { utopia_manifesto_is_closed_island_route = yes }
		FROM = {
			set_state_flag = utopia_manifesto_district_closed_island_charter
			set_variable = { utopia_manifesto_state_district_closed_supply = constant:utopia_manifesto_district_state_modifier.closed_island_supply }
			set_variable = { utopia_manifesto_state_district_closed_resistance = constant:utopia_manifesto_district_state_modifier.closed_island_resistance }
			add_dynamic_modifier = { modifier = utopia_manifesto_district_closed_island_charter_state }
		}
		set_country_flag = utopia_manifesto_district_assigned_charter_used
		set_country_flag = utopia_manifesto_conduct_district_assignment
		set_temp_variable = { utopia_manifesto_plenty_delta = constant:utopia_manifesto_decision_change.ledger_step }
		set_temp_variable = { utopia_manifesto_concord_delta = constant:utopia_manifesto_decision_change.ledger_step_loss }
		set_temp_variable = { utopia_manifesto_assignment_delta = constant:utopia_manifesto_decision_change.ledger_shift }
		country_event = { id = chaosx.nr15.46 }
	}
	else_if = {
		limit = { utopia_manifesto_is_joke_understood_route = yes }
		FROM = {
			set_state_flag = utopia_manifesto_district_humanist_review_charter
			set_variable = { utopia_manifesto_state_district_humanist_supply = constant:utopia_manifesto_district_state_modifier.humanist_supply }
			set_variable = { utopia_manifesto_state_district_humanist_resistance = constant:utopia_manifesto_district_state_modifier.humanist_resistance }
			add_dynamic_modifier = { modifier = utopia_manifesto_district_humanist_charter_state }
		}
		set_country_flag = utopia_manifesto_district_review_clause_recorded
		set_temp_variable = { utopia_manifesto_plenty_delta = constant:utopia_manifesto_decision_change.ledger_nudge }
		set_temp_variable = { utopia_manifesto_concord_delta = constant:utopia_manifesto_decision_change.ledger_step }
		set_temp_variable = { utopia_manifesto_assignment_delta = constant:utopia_manifesto_decision_change.ledger_step_loss }
	}
	utopia_manifesto_apply_prepared_ledger_delta = yes
	utopia_manifesto_refresh_reserve_band = yes
}

utopia_manifesto_resolve_district_charter = {
	if = {
		limit = { FROM = { utopia_manifesto_district_charter_can_complete = yes } }
		utopia_manifesto_complete_district_charter = yes
	}
	else = { utopia_manifesto_fail_district_project = yes }
}

utopia_manifesto_complete_district_charter = {
	clr_country_flag = utopia_manifesto_district_charter_active
	FROM = {
		clr_state_flag = utopia_manifesto_district_charter_active
		set_state_flag = utopia_manifesto_district_chartered
	}
	set_variable = { utopia_manifesto_district_project_phase = constant:utopia_manifesto_district_phase.chartered }
	add_to_variable = { utopia_manifesto_chartered_districts = constant:utopia_manifesto_decision_value.project_increment }
	if = { limit = { FROM = { has_state_flag = utopia_manifesto_market_garden_district } } set_country_flag = utopia_manifesto_achievement_district_role_market_garden }
	if = { limit = { FROM = { has_state_flag = utopia_manifesto_industrial_housing_district } } set_country_flag = utopia_manifesto_achievement_district_role_industrial_housing }
	if = { limit = { FROM = { has_state_flag = utopia_manifesto_rail_junction_town } } set_country_flag = utopia_manifesto_achievement_district_role_rail_junction }
	if = { limit = { FROM = { has_state_flag = utopia_manifesto_refugee_municipality } } set_country_flag = utopia_manifesto_achievement_district_role_refugee_municipality }
	if = {
		limit = { check_variable = { var = utopia_manifesto_chartered_districts value = constant:utopia_manifesto_decision_threshold.district_project_limit compare = greater_than_or_equals } }
		set_country_flag = utopia_manifesto_district_system_proven
	}
	utopia_manifesto_apply_district_role_calling_relief = yes
	utopia_manifesto_apply_district_route_charter = yes
	if = { limit = { FROM = { has_state_flag = utopia_manifesto_industrial_housing_district } } country_event = { id = chaosx.nr15.41 } }
	else_if = { limit = { FROM = { has_state_flag = utopia_manifesto_rail_junction_town } } country_event = { id = chaosx.nr15.42 } }
	utopia_manifesto_refresh_achievement_planned_district_proof = yes
	utopia_manifesto_refresh_dynamic_costs = yes
	utopia_manifesto_refresh_formation_proof = yes
}

utopia_manifesto_register_from_as_island_project_state = {
	if = {
		limit = {
			FROM = { exists = yes }
			NOT = { is_in_array = { array = utopia_manifesto_island_project_states value = FROM } }
		}
		add_to_array = { array = utopia_manifesto_island_project_states value = FROM }
		FROM = { set_state_flag = utopia_manifesto_island_project_site }
	}
	if = {
		limit = {
			FROM = { exists = yes }
			NOT = {
				any_of_scopes = {
					array = utopia_manifesto_island_project_primary_states
					exists = yes
				}
			}
		}
		add_to_array = { array = utopia_manifesto_island_project_primary_states value = FROM }
		set_variable = { utopia_manifesto_island_project_primary_state_id = FROM.id }
		FROM = { set_state_flag = utopia_manifesto_island_project_primary_site }
	}
}

utopia_manifesto_register_capital_as_island_project_state = {
	capital_scope = {
		if = {
			limit = { NOT = { is_in_array = { array = ROOT.utopia_manifesto_island_project_states value = THIS } } }
			set_state_flag = utopia_manifesto_island_project_site
			set_state_flag = utopia_manifesto_island_project_primary_site
			ROOT = {
				add_to_array = { array = utopia_manifesto_island_project_states value = PREV }
				add_to_array = { array = utopia_manifesto_island_project_primary_states value = PREV }
				set_variable = { utopia_manifesto_island_project_primary_state_id = PREV.id }
			}
		}
	}
}

utopia_manifesto_refresh_archipelago_project_state_count = {
	set_variable = { utopia_manifesto_archipelago_project_state_count = utopia_manifesto_island_project_states^num }
	if = {
		limit = {
			check_variable = {
				var = utopia_manifesto_archipelago_project_state_count
				value = constant:utopia_manifesto_island_project_limits.archipelago_required_states
				compare = greater_than_or_equals
			}
		}
		set_country_flag = utopia_manifesto_island_site_secured
		clr_country_flag = utopia_manifesto_island_project_stranded
	}
	else = {
		clr_country_flag = utopia_manifesto_island_site_secured
	}
}

utopia_manifesto_complete_archipelago_project_site = {
	clr_country_flag = utopia_manifesto_archipelago_site_mission_active
	FROM = {
		clr_state_flag = utopia_manifesto_archipelago_site_mission_active
		set_state_flag = utopia_manifesto_archipelago_project_site
	}
	utopia_manifesto_register_from_as_island_project_state = yes
	utopia_manifesto_refresh_archipelago_project_state_count = yes
	set_temp_variable = { utopia_manifesto_need_delta = constant:utopia_manifesto_decision_change.ledger_nudge_loss }
	set_temp_variable = { utopia_manifesto_plenty_delta = constant:utopia_manifesto_decision_change.ledger_nudge }
	set_temp_variable = { utopia_manifesto_concord_delta = constant:utopia_manifesto_decision_change.ledger_nudge }
	set_temp_variable = { utopia_manifesto_assignment_delta = constant:utopia_manifesto_decision_value.none }
	utopia_manifesto_apply_prepared_ledger_delta = yes
}

utopia_manifesto_fail_archipelago_project_site = {
	clr_country_flag = utopia_manifesto_archipelago_site_mission_active
	FROM = { clr_state_flag = utopia_manifesto_archipelago_site_mission_active }
	set_country_flag = utopia_manifesto_archipelago_site_attempt_failed
	set_temp_variable = { utopia_manifesto_need_delta = constant:utopia_manifesto_decision_change.ledger_nudge }
	set_temp_variable = { utopia_manifesto_plenty_delta = constant:utopia_manifesto_decision_change.ledger_nudge_loss }
	set_temp_variable = { utopia_manifesto_concord_delta = constant:utopia_manifesto_decision_change.ledger_nudge_loss }
	set_temp_variable = { utopia_manifesto_assignment_delta = constant:utopia_manifesto_decision_value.none }
	utopia_manifesto_apply_prepared_ledger_delta = yes
}

utopia_manifesto_clear_island_project_downstream_proof = {
	clr_country_flag = utopia_manifesto_island_site_secured
	clr_country_flag = utopia_manifesto_common_harbor_complete
	clr_country_flag = utopia_manifesto_inland_terminal_complete
	clr_country_flag = utopia_manifesto_provision_ring_complete
	clr_country_flag = utopia_manifesto_achievement_district_role_provision_ring
	clr_country_flag = utopia_manifesto_achievement_five_district_roles_proven
	clr_country_flag = utopia_manifesto_island_open_fortification_complete
	clr_country_flag = utopia_manifesto_island_gates_closed
	clr_country_flag = utopia_manifesto_island_material_proof
	clr_country_flag = utopia_manifesto_make_island_mission_active
	if = { limit = { has_active_mission = mission_utopia_make_an_island } remove_mission = mission_utopia_make_an_island }
	utopia_manifesto_refresh_achievement_planned_district_proof = yes
}

utopia_manifesto_reconcile_island_project_scopes = {
	if = {
		limit = { has_country_flag = utopia_manifesto_geography_archipelago_network }
		for_each_scope_loop = {
			array = utopia_manifesto_island_project_states
			if = {
				limit = {
					OR = {
						NOT = { is_owned_and_controlled_by = ROOT }
						NOT = { is_core_of = ROOT }
						NOT = { is_island_state = yes }
					}
				}
				clr_state_flag = utopia_manifesto_island_project_site
				clr_state_flag = utopia_manifesto_island_project_primary_site
				clr_state_flag = utopia_manifesto_archipelago_project_site
				ROOT = {
					remove_from_array = { array = utopia_manifesto_island_project_states value = PREV }
					remove_from_array = { array = utopia_manifesto_island_project_primary_states value = PREV }
				}
			}
		}
		utopia_manifesto_refresh_archipelago_project_state_count = yes
		if = {
			limit = { NOT = { utopia_manifesto_archipelago_project_is_valid = yes } }
			set_country_flag = utopia_manifesto_island_project_stranded
			utopia_manifesto_clear_island_project_downstream_proof = yes
		}
	}
	else_if = {
		limit = {
			has_country_flag = utopia_manifesto_island_project_lease_active
			NOT = { has_country_flag = utopia_manifesto_island_lease_return_in_progress }
			NOT = { utopia_manifesto_island_project_lease_is_valid = yes }
		}
		utopia_manifesto_expire_island_project_lease = yes
	}
	else_if = {
		limit = {
			has_country_flag = utopia_manifesto_island_variant_selected
			NOT = { has_country_flag = utopia_manifesto_island_lease_return_in_progress }
			NOT = { utopia_manifesto_selected_island_project_is_valid = yes }
		}
		set_country_flag = utopia_manifesto_island_project_stranded
		utopia_manifesto_clear_island_project_downstream_proof = yes
	}
	utopia_manifesto_refresh_formation_proof = yes
}

utopia_manifesto_clear_island_project_scopes = {
	for_each_scope_loop = {
		array = utopia_manifesto_island_project_states
		remove_from_array = { array = utopia_manifesto_island_project_lease_state_founders value = ROOT }
		if = {
			limit = { check_variable = { var = utopia_manifesto_island_project_lease_state_founders^num value = constant:utopia_manifesto_math.zero compare = equals } }
			clr_state_flag = utopia_manifesto_island_project_site
			clr_state_flag = utopia_manifesto_island_project_primary_site
			clr_state_flag = utopia_manifesto_archipelago_project_site
			clr_state_flag = utopia_manifesto_leased_island_project_site
			clr_state_flag = utopia_manifesto_island_site_secured
			clr_state_flag = utopia_manifesto_common_harbor_complete
			clr_state_flag = utopia_manifesto_inland_terminal_complete
			clr_state_flag = utopia_manifesto_provision_ring_complete
			clr_state_flag = utopia_manifesto_island_open_fortification_complete
			clr_state_flag = utopia_manifesto_island_gates_closed
			clr_state_flag = utopia_manifesto_island_project_stage_active
			clr_state_flag = utopia_manifesto_archipelago_site_mission_active
			clr_state_flag = utopia_manifesto_island_project_lease_state
			if = { limit = { has_dynamic_modifier = { modifier = utopia_manifesto_island_project_lease_state } } remove_dynamic_modifier = { modifier = utopia_manifesto_island_project_lease_state } }
			clear_variable = utopia_manifesto_island_project_lease_supply
			clear_variable = utopia_manifesto_island_project_lease_resources
			clear_array = utopia_manifesto_island_project_lease_state_founders
		}
	}
	clear_array = utopia_manifesto_island_project_states
	clear_array = utopia_manifesto_island_project_primary_states
	clear_variable = utopia_manifesto_island_project_primary_state_id
	clear_variable = utopia_manifesto_archipelago_project_state_count
}

utopia_manifesto_clear_island_project_runtime = {
	if = {
		limit = { has_country_flag = utopia_manifesto_island_project_lease_active }
		utopia_manifesto_return_island_project_lease = yes
	}
	if = { limit = { has_active_mission = mission_utopia_build_island_stage } remove_mission = mission_utopia_build_island_stage }
	if = { limit = { has_active_mission = mission_utopia_secure_archipelago_project_site } remove_mission = mission_utopia_secure_archipelago_project_site }
	if = { limit = { has_active_mission = mission_utopia_make_an_island } remove_mission = mission_utopia_make_an_island }
	if = { limit = { has_active_mission = mission_utopia_hold_island_project_lease } remove_mission = mission_utopia_hold_island_project_lease }
	utopia_manifesto_clear_island_project_scopes = yes
	utopia_manifesto_clear_island_project_downstream_proof = yes
	clr_country_flag = utopia_manifesto_island_project_stage_active
	clr_country_flag = utopia_manifesto_archipelago_site_mission_active
	clr_country_flag = utopia_manifesto_make_island_mission_active
	clr_country_flag = utopia_manifesto_island_project_stranded
	clr_country_flag = utopia_manifesto_island_project_built
	clr_country_flag = utopia_manifesto_island_lease_renewal_attempted
	clr_country_flag = utopia_manifesto_island_lease_renewed
	clr_country_flag = utopia_manifesto_island_lease_counteroffer_accepted
	clr_country_flag = utopia_manifesto_island_lease_renewal_refused
	clr_country_flag = utopia_manifesto_island_lease_expired
	clr_country_flag = utopia_manifesto_island_lease_return_deferred
	clr_country_flag = utopia_manifesto_island_lease_return_in_progress
	clear_variable = utopia_manifesto_island_project_stage
	clear_variable = utopia_manifesto_island_project_stages
	clear_variable = utopia_manifesto_island_lease_days
}

utopia_manifesto_select_island_site_stage = {
	set_variable = { utopia_manifesto_island_project_stage = constant:utopia_manifesto_decision_island_stage.site }
}

utopia_manifesto_select_island_harbor_stage = {
	set_variable = { utopia_manifesto_island_project_stage = constant:utopia_manifesto_decision_island_stage.harbor }
}

utopia_manifesto_select_island_terminal_stage = {
	set_variable = { utopia_manifesto_island_project_stage = constant:utopia_manifesto_decision_island_stage.terminal }
}

utopia_manifesto_select_island_provision_ring_stage = {
	set_variable = { utopia_manifesto_island_project_stage = constant:utopia_manifesto_decision_island_stage.provision_ring }
}

utopia_manifesto_select_island_open_fortification_stage = {
	set_variable = { utopia_manifesto_island_project_stage = constant:utopia_manifesto_decision_island_stage.open_fortification }
}

utopia_manifesto_select_island_closed_gates_stage = {
	set_variable = { utopia_manifesto_island_project_stage = constant:utopia_manifesto_decision_island_stage.closed_gates }
}

utopia_manifesto_complete_island_project_stage = {
	clr_country_flag = utopia_manifesto_island_project_stage_active
	add_to_variable = { utopia_manifesto_island_project_stages = constant:utopia_manifesto_decision_value.project_increment }
	if = {
		limit = { check_variable = { utopia_manifesto_island_project_stage = constant:utopia_manifesto_decision_island_stage.site } }
		utopia_manifesto_register_from_as_island_project_state = yes
		set_country_flag = utopia_manifesto_island_site_secured
		FROM = {
			clr_state_flag = utopia_manifesto_island_project_stage_active
			set_state_flag = utopia_manifesto_island_site_secured
		}
	}
	else_if = {
		limit = { check_variable = { utopia_manifesto_island_project_stage = constant:utopia_manifesto_decision_island_stage.harbor } }
		set_country_flag = utopia_manifesto_common_harbor_complete
		for_each_scope_loop = {
			array = utopia_manifesto_island_project_states
			if = {
				limit = { NOT = { has_state_flag = utopia_manifesto_common_harbor_complete } }
				add_building_construction = { type = naval_base level = constant:utopia_manifesto_decision_building.basic instant_build = yes }
			}
			clr_state_flag = utopia_manifesto_island_project_stage_active
			set_state_flag = utopia_manifesto_common_harbor_complete
		}
	}
	else_if = {
		limit = { check_variable = { utopia_manifesto_island_project_stage = constant:utopia_manifesto_decision_island_stage.terminal } }
		set_country_flag = utopia_manifesto_inland_terminal_complete
		FROM = {
			clr_state_flag = utopia_manifesto_island_project_stage_active
			set_state_flag = utopia_manifesto_inland_terminal_complete
			add_building_construction = { type = infrastructure level = constant:utopia_manifesto_decision_building.expanded instant_build = yes }
		}
	}
	else_if = {
		limit = { check_variable = { utopia_manifesto_island_project_stage = constant:utopia_manifesto_decision_island_stage.provision_ring } }
		set_country_flag = utopia_manifesto_provision_ring_complete
		set_country_flag = utopia_manifesto_achievement_district_role_provision_ring
		for_each_scope_loop = {
			array = utopia_manifesto_island_project_states
			if = {
				limit = {
					has_state_flag = utopia_manifesto_island_project_primary_site
					NOT = { has_state_flag = utopia_manifesto_provision_ring_complete }
				}
				add_extra_state_shared_building_slots = constant:utopia_manifesto_decision_value.shared_slot_increment
				add_building_construction = { type = industrial_complex level = constant:utopia_manifesto_decision_building.basic instant_build = yes }
			}
			clr_state_flag = utopia_manifesto_island_project_stage_active
			set_state_flag = utopia_manifesto_provision_ring_complete
		}
	}
	else_if = {
		limit = { check_variable = { utopia_manifesto_island_project_stage = constant:utopia_manifesto_decision_island_stage.open_fortification } }
		set_country_flag = utopia_manifesto_island_open_fortification_complete
		for_each_scope_loop = {
			array = utopia_manifesto_island_project_states
			if = {
				limit = { NOT = { has_state_flag = utopia_manifesto_island_open_fortification_complete } }
				add_building_construction = { type = bunker level = constant:utopia_manifesto_decision_building.basic instant_build = yes }
			}
			clr_state_flag = utopia_manifesto_island_project_stage_active
			set_state_flag = utopia_manifesto_island_open_fortification_complete
		}
	}
	else_if = {
		limit = { check_variable = { utopia_manifesto_island_project_stage = constant:utopia_manifesto_decision_island_stage.closed_gates } }
		set_country_flag = utopia_manifesto_island_gates_closed
		for_each_scope_loop = {
			array = utopia_manifesto_island_project_states
			if = {
				limit = { NOT = { has_state_flag = utopia_manifesto_island_gates_closed } }
				if = {
					limit = { has_state_flag = utopia_manifesto_island_project_primary_site }
					add_building_construction = { type = bunker level = constant:utopia_manifesto_decision_building.expanded instant_build = yes }
				}
				else = {
					add_building_construction = { type = bunker level = constant:utopia_manifesto_decision_building.basic instant_build = yes }
				}
			}
			clr_state_flag = utopia_manifesto_island_project_stage_active
			set_state_flag = utopia_manifesto_island_gates_closed
		}
	}
	set_temp_variable = { utopia_manifesto_need_delta = constant:utopia_manifesto_decision_change.ledger_nudge_loss }
	set_temp_variable = { utopia_manifesto_plenty_delta = constant:utopia_manifesto_decision_change.ledger_step }
	set_temp_variable = { utopia_manifesto_concord_delta = constant:utopia_manifesto_decision_change.ledger_nudge }
	set_temp_variable = { utopia_manifesto_assignment_delta = constant:utopia_manifesto_decision_value.none }
	utopia_manifesto_apply_prepared_ledger_delta = yes
	utopia_manifesto_refresh_achievement_planned_district_proof = yes
	if = {
		limit = { utopia_manifesto_selected_island_project_is_valid = yes }
		clr_country_flag = utopia_manifesto_island_project_stranded
	}
	if = {
		limit = {
			has_country_flag = utopia_manifesto_geography_inland_island
			NOT = { has_country_flag = utopia_manifesto_inland_island_event_seen }
		}
		set_country_flag = utopia_manifesto_inland_island_event_seen
		country_event = { id = chaosx.nr15.44 }
	}
}

utopia_manifesto_fail_island_project = {
	clr_country_flag = utopia_manifesto_island_project_stage_active
	clr_country_flag = utopia_manifesto_make_island_mission_active
	FROM = { clr_state_flag = utopia_manifesto_island_project_stage_active }
	set_country_flag = utopia_manifesto_island_project_stranded
	set_temp_variable = { utopia_manifesto_need_delta = constant:utopia_manifesto_decision_change.ledger_step }
	set_temp_variable = { utopia_manifesto_plenty_delta = constant:utopia_manifesto_decision_change.ledger_shift_loss }
	set_temp_variable = { utopia_manifesto_concord_delta = constant:utopia_manifesto_decision_change.ledger_step_loss }
	set_temp_variable = { utopia_manifesto_assignment_delta = constant:utopia_manifesto_decision_change.ledger_nudge }
	utopia_manifesto_apply_prepared_ledger_delta = yes
}

utopia_manifesto_complete_make_an_island = {
	clr_country_flag = utopia_manifesto_make_island_mission_active
	set_country_flag = utopia_manifesto_island_material_proof
	remove_mission = mission_utopia_prefire_perfect_island_deadline
	utopia_manifesto_complete_prefire_island_deadline = yes
	clr_country_flag = utopia_manifesto_island_project_stranded
	set_temp_variable = { utopia_manifesto_need_delta = constant:utopia_manifesto_decision_change.ledger_shift_loss }
	set_temp_variable = { utopia_manifesto_plenty_delta = constant:utopia_manifesto_decision_change.ledger_shift }
	set_temp_variable = { utopia_manifesto_concord_delta = constant:utopia_manifesto_decision_change.ledger_step }
	set_temp_variable = { utopia_manifesto_assignment_delta = constant:utopia_manifesto_decision_value.none }
	utopia_manifesto_apply_prepared_ledger_delta = yes
	utopia_manifesto_refresh_formation_proof = yes
}

utopia_manifesto_resolve_domestic_alternatives = {
	clr_country_flag = utopia_manifesto_domestic_alternative_survey_active
	clr_country_flag = utopia_manifesto_domestic_alternative_found
	clr_country_flag = utopia_manifesto_domestic_alternative_partial
	clr_country_flag = utopia_manifesto_no_domestic_alternative_found
	clr_country_flag = utopia_manifesto_domestic_review_external_case_authorized
	clr_country_flag = utopia_manifesto_domestic_review_solution_suppressed
	if = {
		limit = {
			utopia_manifesto_plenty_is_stable = yes
			utopia_manifesto_concord_is_cooperative = yes
		}
		set_country_flag = utopia_manifesto_domestic_alternative_found
		set_temp_variable = { utopia_manifesto_domestic_review_relief_input = constant:utopia_manifesto_case_limits.domestic_solution_relief }
		utopia_manifesto_apply_domestic_review_relief = yes
		set_temp_variable = { utopia_manifesto_need_delta = constant:utopia_manifesto_decision_change.ledger_shift_loss }
		set_temp_variable = { utopia_manifesto_plenty_delta = constant:utopia_manifesto_decision_change.ledger_nudge }
		set_temp_variable = { utopia_manifesto_concord_delta = constant:utopia_manifesto_decision_change.ledger_step }
		set_temp_variable = { utopia_manifesto_assignment_delta = constant:utopia_manifesto_decision_value.none }
	}
	else_if = {
		limit = {
			OR = {
				utopia_manifesto_plenty_is_stable = yes
				utopia_manifesto_concord_is_cooperative = yes
			}
		}
		set_country_flag = utopia_manifesto_domestic_alternative_partial
		set_temp_variable = { utopia_manifesto_domestic_review_relief_input = constant:utopia_manifesto_case_limits.domestic_partial_relief }
		utopia_manifesto_apply_domestic_review_relief = yes
		set_temp_variable = { utopia_manifesto_need_delta = constant:utopia_manifesto_decision_change.ledger_nudge_loss }
		set_temp_variable = { utopia_manifesto_plenty_delta = constant:utopia_manifesto_decision_value.none }
		set_temp_variable = { utopia_manifesto_concord_delta = constant:utopia_manifesto_decision_change.ledger_nudge }
		set_temp_variable = { utopia_manifesto_assignment_delta = constant:utopia_manifesto_decision_value.none }
	}
	else = {
		set_country_flag = utopia_manifesto_no_domestic_alternative_found
		utopia_manifesto_prepare_dominant_case_family = yes
		set_temp_variable = { utopia_manifesto_need_delta = constant:utopia_manifesto_decision_value.none }
		set_temp_variable = { utopia_manifesto_plenty_delta = constant:utopia_manifesto_decision_change.ledger_nudge_loss }
		set_temp_variable = { utopia_manifesto_concord_delta = constant:utopia_manifesto_decision_change.ledger_nudge }
		set_temp_variable = { utopia_manifesto_assignment_delta = constant:utopia_manifesto_decision_value.none }
	}
	utopia_manifesto_apply_prepared_ledger_delta = yes
	set_country_flag = utopia_manifesto_domestic_alternatives_reviewed
	utopia_manifesto_prepare_case_candidate_targets = yes
	country_event = { id = chaosx.nr15.51 }
}

utopia_manifesto_fail_domestic_alternatives = {
	utopia_manifesto_clear_domestic_review_state = yes
	set_country_flag = utopia_manifesto_domestic_alternative_survey_failed
	set_temp_variable = { utopia_manifesto_need_delta = constant:utopia_manifesto_decision_change.ledger_nudge }
	set_temp_variable = { utopia_manifesto_plenty_delta = constant:utopia_manifesto_decision_change.ledger_nudge_loss }
	set_temp_variable = { utopia_manifesto_concord_delta = constant:utopia_manifesto_decision_change.ledger_step_loss }
	set_temp_variable = { utopia_manifesto_assignment_delta = constant:utopia_manifesto_decision_change.ledger_nudge }
	utopia_manifesto_apply_prepared_ledger_delta = yes
}

utopia_manifesto_complete_stewardship_provision = {
	clr_country_flag = utopia_manifesto_stewardship_provision_active
	clr_country_flag = utopia_manifesto_stewardship_objective_active
	set_temp_variable = { utopia_manifesto_stewardship_provision_input = constant:utopia_manifesto_decision_value.standard_provision }
	utopia_manifesto_record_stewardship_provision = yes
	utopia_manifesto_apply_provision_to_active_case_state = yes
	utopia_manifesto_provision_stewardship = yes
	set_country_flag = utopia_manifesto_stewardship_provision_complete
	set_temp_variable = { utopia_manifesto_need_delta = constant:utopia_manifesto_decision_change.ledger_step_loss }
	set_temp_variable = { utopia_manifesto_plenty_delta = constant:utopia_manifesto_decision_change.ledger_nudge_loss }
	set_temp_variable = { utopia_manifesto_concord_delta = constant:utopia_manifesto_decision_change.ledger_nudge }
	set_temp_variable = { utopia_manifesto_assignment_delta = constant:utopia_manifesto_decision_value.none }
	utopia_manifesto_apply_prepared_ledger_delta = yes
	utopia_manifesto_record_achievement_common_use_financed_provision = yes
	country_event = { id = chaosx.nr15.60 }
}

utopia_manifesto_complete_stewardship_route = {
	clr_country_flag = utopia_manifesto_stewardship_route_active
	clr_country_flag = utopia_manifesto_stewardship_objective_active
	set_country_flag = utopia_manifesto_stewardship_route_restored
	set_temp_variable = { utopia_manifesto_stewardship_provision_input = constant:utopia_manifesto_decision_value.standard_provision }
	utopia_manifesto_record_stewardship_provision = yes
	utopia_manifesto_apply_route_restoration_to_active_case_state = yes
	set_temp_variable = { utopia_manifesto_need_delta = constant:utopia_manifesto_decision_change.ledger_nudge_loss }
	set_temp_variable = { utopia_manifesto_plenty_delta = constant:utopia_manifesto_decision_change.ledger_step }
	set_temp_variable = { utopia_manifesto_concord_delta = constant:utopia_manifesto_decision_change.ledger_nudge }
	set_temp_variable = { utopia_manifesto_assignment_delta = constant:utopia_manifesto_decision_value.none }
	utopia_manifesto_apply_prepared_ledger_delta = yes
}

utopia_manifesto_fail_stewardship_project = {
	clr_country_flag = utopia_manifesto_stewardship_provision_active
	clr_country_flag = utopia_manifesto_stewardship_route_active
	clr_country_flag = utopia_manifesto_stewardship_charter_period_active
	clr_country_flag = utopia_manifesto_stewardship_integration_active
	clr_country_flag = utopia_manifesto_stewardship_objective_active
	set_country_flag = utopia_manifesto_stewardship_failed
	set_country_flag = utopia_manifesto_unresolved_stewardship_failure
	set_temp_variable = { utopia_manifesto_need_delta = constant:utopia_manifesto_decision_change.ledger_step }
	set_temp_variable = { utopia_manifesto_plenty_delta = constant:utopia_manifesto_decision_change.ledger_step_loss }
	set_temp_variable = { utopia_manifesto_concord_delta = constant:utopia_manifesto_decision_change.ledger_shift_loss }
	set_temp_variable = { utopia_manifesto_assignment_delta = constant:utopia_manifesto_decision_change.ledger_step }
	utopia_manifesto_apply_prepared_ledger_delta = yes
}

utopia_manifesto_complete_charter_period = {
	clr_country_flag = utopia_manifesto_stewardship_charter_period_active
	clr_country_flag = utopia_manifesto_stewardship_objective_active
	set_country_flag = utopia_manifesto_stewardship_charter_period_complete
	utopia_manifesto_refresh_stewardship_proof = yes
	set_temp_variable = { utopia_manifesto_need_delta = constant:utopia_manifesto_decision_change.ledger_nudge_loss }
	set_temp_variable = { utopia_manifesto_plenty_delta = constant:utopia_manifesto_decision_change.ledger_nudge }
	set_temp_variable = { utopia_manifesto_concord_delta = constant:utopia_manifesto_decision_change.ledger_step }
	set_temp_variable = { utopia_manifesto_assignment_delta = constant:utopia_manifesto_decision_change.ledger_nudge_loss }
	utopia_manifesto_apply_prepared_ledger_delta = yes
}

utopia_manifesto_complete_long_integration = {
	clr_country_flag = utopia_manifesto_stewardship_integration_active
	clr_country_flag = utopia_manifesto_stewardship_objective_active
	set_country_flag = utopia_manifesto_stewardship_long_integration_complete
	utopia_manifesto_record_achievement_peaceful_case_resolution = yes
	utopia_manifesto_integrate_stewardship = yes
	set_temp_variable = { utopia_manifesto_need_delta = constant:utopia_manifesto_decision_change.ledger_step_loss }
	set_temp_variable = { utopia_manifesto_plenty_delta = constant:utopia_manifesto_decision_change.ledger_nudge }
	set_temp_variable = { utopia_manifesto_concord_delta = constant:utopia_manifesto_decision_change.ledger_nudge }
	set_temp_variable = { utopia_manifesto_assignment_delta = constant:utopia_manifesto_decision_value.none }
	utopia_manifesto_apply_prepared_ledger_delta = yes
	utopia_manifesto_refresh_formation_proof = yes
}

utopia_manifesto_record_stewardship_return_achievement_evidence = {
	set_country_flag = utopia_manifesto_partner_autonomy_retained
	utopia_manifesto_record_achievement_peaceful_case_resolution = yes
	utopia_manifesto_record_achievement_status_vote = yes
}

utopia_manifesto_complete_league_legitimacy = {
	clr_country_flag = utopia_manifesto_league_legitimacy_mission_active
	clr_country_flag = utopia_manifesto_league_objective_active
	set_country_flag = utopia_manifesto_league_legitimacy_proven
	set_country_flag = utopia_manifesto_broad_recognition_proven
	set_temp_variable = { utopia_manifesto_league_cohesion_delta = constant:utopia_manifesto_decision_change.ledger_shift }
	utopia_manifesto_apply_prepared_league_cohesion_delta = yes
	set_temp_variable = { utopia_manifesto_need_delta = constant:utopia_manifesto_decision_value.none }
	set_temp_variable = { utopia_manifesto_plenty_delta = constant:utopia_manifesto_decision_change.ledger_nudge }
	set_temp_variable = { utopia_manifesto_concord_delta = constant:utopia_manifesto_decision_change.ledger_step }
	set_temp_variable = { utopia_manifesto_assignment_delta = constant:utopia_manifesto_decision_change.ledger_nudge_loss }
	utopia_manifesto_apply_prepared_ledger_delta = yes
	utopia_manifesto_refresh_formation_proof = yes
}

utopia_manifesto_fail_league_legitimacy = {
	clr_country_flag = utopia_manifesto_league_legitimacy_mission_active
	clr_country_flag = utopia_manifesto_league_objective_active
	set_country_flag = utopia_manifesto_league_mask_exposed
	utopia_manifesto_record_league_failure = yes
	set_temp_variable = { utopia_manifesto_need_delta = constant:utopia_manifesto_decision_change.ledger_nudge }
	set_temp_variable = { utopia_manifesto_plenty_delta = constant:utopia_manifesto_decision_change.ledger_nudge_loss }
	set_temp_variable = { utopia_manifesto_concord_delta = constant:utopia_manifesto_decision_change.ledger_shift_loss }
	set_temp_variable = { utopia_manifesto_assignment_delta = constant:utopia_manifesto_decision_change.ledger_nudge }
	utopia_manifesto_apply_prepared_ledger_delta = yes
	country_event = { id = chaosx.nr15.74 }
}

utopia_manifesto_complete_citizen_watch_training = {
	clr_country_flag = utopia_manifesto_citizen_watch_training_active
	set_temp_variable = { utopia_manifesto_growth_tier_input = constant:utopia_manifesto_decision_value.growth_foundation }
	if = {
		limit = { has_country_flag = utopia_manifesto_route_common_table }
		set_temp_variable = { utopia_manifesto_military_formation_input = constant:utopia_manifesto_military_formation.worker_defense }
	}
	else = {
		set_temp_variable = { utopia_manifesto_military_formation_input = constant:utopia_manifesto_military_formation.citizen_watch }
	}
	utopia_manifesto_refresh_dynamic_costs = yes
	if = {
		limit = { utopia_manifesto_can_pay_military_growth = yes }
		utopia_manifesto_apply_paid_military_growth = yes
		if = {
			limit = { NOT = { has_country_flag = utopia_manifesto_military_growth_payment_failed } }
			set_country_flag = utopia_manifesto_citizen_watch_trained
			set_temp_variable = { utopia_manifesto_need_delta = constant:utopia_manifesto_decision_value.none }
			set_temp_variable = { utopia_manifesto_plenty_delta = constant:utopia_manifesto_decision_change.ledger_nudge_loss }
			set_temp_variable = { utopia_manifesto_concord_delta = constant:utopia_manifesto_decision_change.ledger_nudge }
			set_temp_variable = { utopia_manifesto_assignment_delta = constant:utopia_manifesto_decision_change.ledger_nudge }
			utopia_manifesto_apply_prepared_ledger_delta = yes
		}
	}
	else = {
		set_country_flag = utopia_manifesto_citizen_watch_understrength
		set_temp_variable = { utopia_manifesto_need_delta = constant:utopia_manifesto_decision_change.ledger_nudge }
		set_temp_variable = { utopia_manifesto_plenty_delta = constant:utopia_manifesto_decision_change.ledger_nudge_loss }
		set_temp_variable = { utopia_manifesto_concord_delta = constant:utopia_manifesto_decision_change.ledger_nudge_loss }
		set_temp_variable = { utopia_manifesto_assignment_delta = constant:utopia_manifesto_decision_value.none }
		utopia_manifesto_apply_prepared_ledger_delta = yes
	}
}

utopia_manifesto_fail_citizen_watch_training = {
	clr_country_flag = utopia_manifesto_citizen_watch_training_active
	set_country_flag = utopia_manifesto_citizen_watch_training_wasted
	set_temp_variable = { utopia_manifesto_need_delta = constant:utopia_manifesto_decision_change.ledger_nudge }
	set_temp_variable = { utopia_manifesto_plenty_delta = constant:utopia_manifesto_decision_change.ledger_step_loss }
	set_temp_variable = { utopia_manifesto_concord_delta = constant:utopia_manifesto_decision_change.ledger_nudge_loss }
	set_temp_variable = { utopia_manifesto_assignment_delta = constant:utopia_manifesto_decision_value.none }
	utopia_manifesto_apply_prepared_ledger_delta = yes
}

utopia_manifesto_complete_engineer_company_training = {
	clr_country_flag = utopia_manifesto_engineer_company_training_active
	set_temp_variable = { utopia_manifesto_growth_tier_input = constant:utopia_manifesto_decision_value.growth_foundation }
	set_temp_variable = { utopia_manifesto_military_formation_input = constant:utopia_manifesto_military_formation.engineer_corps }
	utopia_manifesto_refresh_dynamic_costs = yes
	if = {
		limit = { utopia_manifesto_can_pay_military_growth = yes }
		utopia_manifesto_apply_paid_military_growth = yes
		if = {
			limit = { NOT = { has_country_flag = utopia_manifesto_military_growth_payment_failed } }
			set_country_flag = utopia_manifesto_engineer_companies_formed
			set_country_flag = utopia_manifesto_engineer_defense
			set_temp_variable = { utopia_manifesto_need_delta = constant:utopia_manifesto_decision_value.none }
			set_temp_variable = { utopia_manifesto_plenty_delta = constant:utopia_manifesto_decision_change.ledger_nudge }
			set_temp_variable = { utopia_manifesto_concord_delta = constant:utopia_manifesto_decision_value.none }
			set_temp_variable = { utopia_manifesto_assignment_delta = constant:utopia_manifesto_decision_change.ledger_nudge }
			utopia_manifesto_apply_prepared_ledger_delta = yes
			country_event = { id = chaosx.nr15.81 }
		}
	}
	else = {
		set_country_flag = utopia_manifesto_engineer_cadres_limited
		set_temp_variable = { utopia_manifesto_need_delta = constant:utopia_manifesto_decision_change.ledger_nudge }
		set_temp_variable = { utopia_manifesto_plenty_delta = constant:utopia_manifesto_decision_change.ledger_nudge_loss }
		set_temp_variable = { utopia_manifesto_concord_delta = constant:utopia_manifesto_decision_value.none }
		set_temp_variable = { utopia_manifesto_assignment_delta = constant:utopia_manifesto_decision_value.none }
		utopia_manifesto_apply_prepared_ledger_delta = yes
	}
}

utopia_manifesto_fail_engineer_company_training = {
	clr_country_flag = utopia_manifesto_engineer_company_training_active
	set_country_flag = utopia_manifesto_engineer_company_training_delayed
	set_temp_variable = { utopia_manifesto_need_delta = constant:utopia_manifesto_decision_change.ledger_nudge }
	set_temp_variable = { utopia_manifesto_plenty_delta = constant:utopia_manifesto_decision_change.ledger_step_loss }
	set_temp_variable = { utopia_manifesto_concord_delta = constant:utopia_manifesto_decision_change.ledger_nudge_loss }
	set_temp_variable = { utopia_manifesto_assignment_delta = constant:utopia_manifesto_decision_value.none }
	utopia_manifesto_apply_prepared_ledger_delta = yes
}

utopia_manifesto_record_household_referendum = {
	set_country_flag = utopia_manifesto_household_referendum_held
	set_temp_variable = { utopia_manifesto_need_delta = constant:utopia_manifesto_decision_change.ledger_nudge_loss }
	set_temp_variable = { utopia_manifesto_plenty_delta = constant:utopia_manifesto_decision_value.none }
	set_temp_variable = { utopia_manifesto_concord_delta = constant:utopia_manifesto_decision_change.ledger_step }
	set_temp_variable = { utopia_manifesto_assignment_delta = constant:utopia_manifesto_decision_change.ledger_nudge_loss }
	utopia_manifesto_apply_prepared_ledger_delta = yes
	country_event = { id = chaosx.nr15.93 }
}

utopia_manifesto_record_calling_councils = {
	set_country_flag = utopia_manifesto_calling_councils_convened
	set_country_flag = utopia_manifesto_household_councils_proven
	set_temp_variable = { utopia_manifesto_need_delta = constant:utopia_manifesto_decision_change.ledger_step_loss }
	set_temp_variable = { utopia_manifesto_plenty_delta = constant:utopia_manifesto_decision_change.ledger_nudge }
	set_temp_variable = { utopia_manifesto_concord_delta = constant:utopia_manifesto_decision_change.ledger_nudge }
	set_temp_variable = { utopia_manifesto_assignment_delta = constant:utopia_manifesto_decision_change.ledger_nudge }
	utopia_manifesto_apply_prepared_ledger_delta = yes
	country_event = { id = chaosx.nr15.92 }
}

utopia_manifesto_record_new_forecast = {
	set_country_flag = utopia_manifesto_forecast_revised
	set_temp_variable = { utopia_manifesto_need_delta = constant:utopia_manifesto_decision_change.ledger_step_loss }
	set_temp_variable = { utopia_manifesto_plenty_delta = constant:utopia_manifesto_decision_change.ledger_step }
	set_temp_variable = { utopia_manifesto_concord_delta = constant:utopia_manifesto_decision_change.ledger_nudge_loss }
	set_temp_variable = { utopia_manifesto_assignment_delta = constant:utopia_manifesto_decision_change.ledger_step }
	utopia_manifesto_apply_prepared_ledger_delta = yes
	country_event = { id = chaosx.nr15.91 }
}

utopia_manifesto_record_service_register_extension = {
	set_country_flag = utopia_manifesto_service_register_extended
	set_temp_variable = { utopia_manifesto_need_delta = constant:utopia_manifesto_decision_change.ledger_step_loss }
	set_temp_variable = { utopia_manifesto_plenty_delta = constant:utopia_manifesto_decision_change.ledger_nudge }
	set_temp_variable = { utopia_manifesto_concord_delta = constant:utopia_manifesto_decision_change.ledger_step_loss }
	set_temp_variable = { utopia_manifesto_assignment_delta = constant:utopia_manifesto_decision_change.ledger_shift }
	utopia_manifesto_apply_prepared_ledger_delta = yes
	country_event = { id = chaosx.nr15.94 }
}

utopia_manifesto_record_sunset_clause = {
	set_country_flag = utopia_manifesto_sunset_clause_adopted
	set_temp_variable = { utopia_manifesto_need_delta = constant:utopia_manifesto_decision_change.ledger_nudge_loss }
	set_temp_variable = { utopia_manifesto_plenty_delta = constant:utopia_manifesto_decision_value.none }
	set_temp_variable = { utopia_manifesto_concord_delta = constant:utopia_manifesto_decision_change.ledger_shift }
	set_temp_variable = { utopia_manifesto_assignment_delta = constant:utopia_manifesto_decision_change.ledger_shift_loss }
	utopia_manifesto_apply_prepared_ledger_delta = yes
	country_event = { id = chaosx.nr15.95 }
}

utopia_manifesto_complete_constitutional_correction = {
	clr_country_flag = utopia_manifesto_constitutional_correction_active
	remove_mission = mission_utopia_constitutional_correction
	if = {
		limit = {
			OR = {
				has_country_flag = utopia_manifesto_foreign_opposition_from_case_shortcut
				has_country_flag = utopia_manifesto_district_assignment_unresolved
				has_country_flag = utopia_manifesto_foreign_opposition_from_penal_works
			}
		}
		if = {
			limit = { has_country_flag = utopia_manifesto_penal_works_method_active }
			utopia_manifesto_end_penal_works_method = yes
		}
		set_country_flag = utopia_manifesto_external_conduct_corrected
		set_country_flag = utopia_manifesto_district_assignment_repaired
		set_country_flag = utopia_manifesto_penal_works_halted_after_first_use
		clr_country_flag = utopia_manifesto_district_assignment_unresolved
		clr_country_flag = utopia_manifesto_foreign_opposition_from_penal_works
		clr_country_flag = utopia_manifesto_foreign_opposition_from_case_shortcut
	}
	utopia_manifesto_resolve_constitutional_crisis = yes
	set_country_flag = utopia_manifesto_constitutional_correction_complete
	utopia_manifesto_record_achievement_crisis_recovery = yes
	utopia_manifesto_refresh_formation_proof = yes
}

utopia_manifesto_fail_constitutional_correction = {
	clr_country_flag = utopia_manifesto_constitutional_correction_active
	set_country_flag = utopia_manifesto_constitutional_correction_failed
	set_temp_variable = { utopia_manifesto_need_delta = constant:utopia_manifesto_decision_change.ledger_step }
	set_temp_variable = { utopia_manifesto_plenty_delta = constant:utopia_manifesto_decision_change.ledger_step_loss }
	set_temp_variable = { utopia_manifesto_concord_delta = constant:utopia_manifesto_decision_change.ledger_shift_loss }
	set_temp_variable = { utopia_manifesto_assignment_delta = constant:utopia_manifesto_decision_change.ledger_nudge }
	utopia_manifesto_apply_prepared_ledger_delta = yes
	country_event = { id = chaosx.nr15.90 }
}

utopia_manifesto_complete_store_guard = {
	clr_country_flag = utopia_manifesto_store_guard_mission_active
	set_country_flag = utopia_manifesto_store_guard_complete
	set_temp_variable = { utopia_manifesto_need_delta = constant:utopia_manifesto_decision_value.none }
	set_temp_variable = { utopia_manifesto_plenty_delta = constant:utopia_manifesto_decision_change.ledger_step }
	set_temp_variable = { utopia_manifesto_concord_delta = constant:utopia_manifesto_decision_change.ledger_nudge }
	set_temp_variable = { utopia_manifesto_assignment_delta = constant:utopia_manifesto_decision_value.none }
	utopia_manifesto_apply_prepared_ledger_delta = yes
	country_event = { id = chaosx.nr15.80 }
}

utopia_manifesto_fail_store_guard = {
	clr_country_flag = utopia_manifesto_store_guard_mission_active
	set_country_flag = utopia_manifesto_store_guard_failed
	set_temp_variable = { utopia_manifesto_need_delta = constant:utopia_manifesto_decision_change.ledger_step }
	set_temp_variable = { utopia_manifesto_plenty_delta = constant:utopia_manifesto_decision_change.ledger_step_loss }
	set_temp_variable = { utopia_manifesto_concord_delta = constant:utopia_manifesto_decision_change.ledger_nudge_loss }
	set_temp_variable = { utopia_manifesto_assignment_delta = constant:utopia_manifesto_decision_change.ledger_nudge }
	utopia_manifesto_apply_prepared_ledger_delta = yes
}

utopia_manifesto_complete_auxiliary_demobilization = {
	clr_country_flag = utopia_manifesto_auxiliary_contract_active
	clr_country_flag = utopia_manifesto_auxiliary_demobilization_active
	clr_country_flag = utopia_manifesto_auxiliary_dependency_active
	set_country_flag = utopia_manifesto_auxiliary_contract_ended
	utopia_manifesto_resolve_auxiliary_dependency = yes
	set_temp_variable = { utopia_manifesto_need_delta = constant:utopia_manifesto_decision_value.none }
	set_temp_variable = { utopia_manifesto_plenty_delta = constant:utopia_manifesto_decision_change.ledger_nudge_loss }
	set_temp_variable = { utopia_manifesto_concord_delta = constant:utopia_manifesto_decision_change.ledger_step }
	set_temp_variable = { utopia_manifesto_assignment_delta = constant:utopia_manifesto_decision_change.ledger_nudge_loss }
	utopia_manifesto_apply_prepared_ledger_delta = yes
}

utopia_manifesto_fail_auxiliary_demobilization = {
	clr_country_flag = utopia_manifesto_auxiliary_demobilization_active
	set_country_flag = utopia_manifesto_auxiliary_contract_betrayal
	set_temp_variable = { utopia_manifesto_need_delta = constant:utopia_manifesto_decision_change.ledger_nudge }
	set_temp_variable = { utopia_manifesto_plenty_delta = constant:utopia_manifesto_decision_change.ledger_nudge_loss }
	set_temp_variable = { utopia_manifesto_concord_delta = constant:utopia_manifesto_decision_change.ledger_shift_loss }
	set_temp_variable = { utopia_manifesto_assignment_delta = constant:utopia_manifesto_decision_change.ledger_step }
	utopia_manifesto_apply_prepared_ledger_delta = yes
	country_event = { id = chaosx.nr15.84 }
}

utopia_manifesto_complete_formation_proof = {
	clr_country_flag = utopia_manifesto_formation_proof_mission_active
	utopia_manifesto_refresh_formation_proof = yes
	if = {
		limit = { utopia_manifesto_can_form_current_route = yes }
		set_country_flag = utopia_manifesto_formation_proven
		set_temp_variable = { utopia_manifesto_need_delta = constant:utopia_manifesto_decision_change.ledger_step_loss }
		set_temp_variable = { utopia_manifesto_plenty_delta = constant:utopia_manifesto_decision_change.ledger_step }
		set_temp_variable = { utopia_manifesto_concord_delta = constant:utopia_manifesto_decision_change.ledger_step }
		set_temp_variable = { utopia_manifesto_assignment_delta = constant:utopia_manifesto_decision_value.none }
		utopia_manifesto_apply_prepared_ledger_delta = yes
	}
	else = {
		set_country_flag = utopia_manifesto_formation_proof_partial
		set_temp_variable = { utopia_manifesto_need_delta = constant:utopia_manifesto_decision_change.ledger_nudge }
		set_temp_variable = { utopia_manifesto_plenty_delta = constant:utopia_manifesto_decision_change.ledger_nudge_loss }
		set_temp_variable = { utopia_manifesto_concord_delta = constant:utopia_manifesto_decision_change.ledger_nudge_loss }
		set_temp_variable = { utopia_manifesto_assignment_delta = constant:utopia_manifesto_decision_value.none }
		utopia_manifesto_apply_prepared_ledger_delta = yes
	}
}

utopia_manifesto_fail_formation_proof = {
	clr_country_flag = utopia_manifesto_formation_proof_mission_active
	set_country_flag = utopia_manifesto_formation_proof_failed
	set_temp_variable = { utopia_manifesto_need_delta = constant:utopia_manifesto_decision_change.ledger_step }
	set_temp_variable = { utopia_manifesto_plenty_delta = constant:utopia_manifesto_decision_change.ledger_step_loss }
	set_temp_variable = { utopia_manifesto_concord_delta = constant:utopia_manifesto_decision_change.ledger_step_loss }
	set_temp_variable = { utopia_manifesto_assignment_delta = constant:utopia_manifesto_decision_value.none }
	utopia_manifesto_apply_prepared_ledger_delta = yes
}
