# ============================================================================
# CHAOS REDUX - EVENT 006 IW-096 BIA COMPATIBILITY EFFECTS
# ============================================================================
# Dormant cleanup-safe wrappers for the registered BIA carrier. IW-096 is
# intentionally unbound on the installed map. No setup, release, history,
# state, core, leader, focus, asset, localisation, or central-dispatch effect
# belongs here.
#
# The companion trigger file is the source-backed boundary. These effects only
# clear a future Benin selector after the complete BIA/Event 006/IW-096
# preservation contract passes. Vanilla BIA history, core 900, Akenzua II,
# and the generic focus tree remain untouched.
# ============================================================================

# COUNTRY scope. The selector is set only by a future source-backed owner;
# this adapter never grants it. Clearing it is idempotent and does not clear
# the Event 006 origin or package identity.
independence_wave_iw_096_bia_compatibility_clear_identity_selection = {
	if = {
		limit = {
			has_independence_wave_iw_096_bia_compatibility_contract = yes
		}
		clr_country_flag = independence_wave_iw_096_benin_identity_selected
	}
}

# COUNTRY scope. Public cleanup wrapper for a future Benin route. It remains
# inert unless the exact registered tag, living Event 006 origin, package id,
# selector, release anchor, and vanilla preservation witnesses all hold.
independence_wave_cleanup_iw_096_bia_compatibility = {
	if = {
		limit = {
			has_independence_wave_iw_096_bia_compatibility_contract = yes
		}
		independence_wave_iw_096_bia_compatibility_clear_identity_selection = yes
	}
}
