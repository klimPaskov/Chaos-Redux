# ============================================================================
# CHAOS REDUX - EVENT 006 IW-042 GAL COMPATIBILITY EFFECTS
# ============================================================================
# Dormant cleanup-safe wrappers for the registered vanilla GAL carrier.
# IW-042 is not a promoted playable Event 006 package yet. No setup, release,
# history, state, core, leader, portrait, flag, route, or central-dispatch
# behavior belongs here.
#
# The companion trigger file proves the exact GAL/Event 006/IW-042 origin,
# state-91/state-89, generic-tree, and future institution contract. Vanilla
# GAL history and capital 88, the vanilla CZE state-88 GAL-core/release route,
# and the absence of a GAL character remain untouched.
# ============================================================================

# COUNTRY scope. The selector is set only by a future source-backed named
# institution owner; this adapter never grants it. Clearing is idempotent and
# does not clear the Event 006 origin or package identity.
independence_wave_iw_042_gal_compatibility_clear_institution_selection = {
	if = {
		limit = {
			has_independence_wave_iw_042_gal_compatibility_contract = yes
		}
		clr_country_flag = independence_wave_iw_042_gal_institution_selected
	}
}

# COUNTRY scope. Public cleanup wrapper for future named-institution callers.
# It remains inert unless the exact registered tag, living Event 006 origin,
# package id, selector, two anchors, and vanilla GAL surface all hold.
independence_wave_cleanup_iw_042_gal_compatibility = {
	if = {
		limit = {
			has_independence_wave_iw_042_gal_compatibility_contract = yes
		}
		independence_wave_iw_042_gal_compatibility_clear_institution_selection = yes
	}
}
