# Bounded, idempotent migration fixture for the CXT extension bus.

migration_register_cxt_test_content = {
	set_temp_variable = {
		var = chaosx_test_country_registration_extension_effect
		value = token:chaosx_cxt_extension_migration
	}
	chaosx_test_country_register_extension_effect = yes
}

chaosx_cxt_extension_migration_apply = {
	if = {
		limit = {
			has_country_flag = chaosx_test_country_initialized
			NOT = { has_country_flag = migration_cxt_fixture_initialized }
		}
		set_country_flag = migration_cxt_fixture_initialized
		set_temp_variable = { migration_reception_capacity_request = constant:migration_decision_threshold.category_reception_load }
		migration_refresh_reception_capacity = yes
	}
}
