# ============================================================================
# CHAOS REDUX - EVENT 006 INDEPENDENCE WAVE ROSTER EFFECTS
# ============================================================================
# Overview:
# - Applies the synchronous Event 006 package-roster checkpoint.
# - Preserves the compatibility behavior of chaosx.nr6.350 for older callers.
# - Keeps approved portrait overrides character-scoped and idempotent.
# - Does not release countries, alter package costs, or touch pre-event UI.
# ============================================================================

independence_wave_apply_roster_checkpoint = {
	if = {
		# TRA reuses the vanilla Iuliu Maniu roster. The checkpoint is
		# explicit so the additive carrier does not silently treat the
		# setup event as a no-op when no generated character is required.
		limit = {
			is_independence_wave_tra_package = yes
			has_independence_wave_tra_command_roster = yes
		}
		set_country_flag = independence_wave_tra_vanilla_roster_checkpoint
	}
	if = {
		# Event 006 uses dedicated source placeholders for the two vanilla
		# MNT political carriers without mutating their shared vanilla
		# portraits. Keep this override character-scoped and leave Dukanovic
		# untouched; Martinovic owns his dedicated portraits in his character
		# definition above.
		limit = {
			is_independence_wave_mnt_package = yes
		}
		set_portraits = {
			character = MNT_kristo_popovic
			civilian = {
				large = GFX_portrait_MNT_independence_wave_kristo_popovic
			}
			army = {
				large = GFX_portrait_MNT_independence_wave_kristo_popovic
			}
		}
		set_portraits = {
			character = MNT_blazo_jovanovic
			civilian = {
				large = GFX_portrait_MNT_independence_wave_blazo_jovanovic
			}
			army = {
				large = GFX_portrait_MNT_independence_wave_blazo_jovanovic
			}
		}
	}
	if = {
		limit = {
			is_independence_wave_mnt_package = yes
			has_independence_wave_mnt_command_roster = yes
		}
		set_country_flag = independence_wave_mnt_roster_checkpoint
	}
	if = {
		limit = {
			is_independence_wave_kos_package = yes
			has_independence_wave_kos_command_roster = yes
		}
		set_country_flag = independence_wave_kos_roster_checkpoint
	}
	if = {
		# KUB retains its vanilla Ivanis command roster. The checkpoint is
		# explicit so a setup retry cannot treat the vanilla-only package as
		# an unverified empty roster.
		limit = {
			is_independence_wave_kub_package = yes
			has_independence_wave_kub_command_roster = yes
		}
		set_country_flag = independence_wave_kub_roster_checkpoint
	}
	if = {
		# TAT retains its vanilla Aleksandr Alemasov command roster. The
		# checkpoint keeps the vanilla-only package idempotent across retries.
		limit = {
			is_independence_wave_tat_package = yes
			has_independence_wave_tat_command_roster = yes
		}
		set_country_flag = independence_wave_tat_roster_checkpoint
	}
	if = {
		# BSK retains the vanilla Yakov Bykin roster. The checkpoint is
		# explicit so a package retry cannot treat a vanilla-only roster as
		# an unverified empty setup.
		limit = {
			is_independence_wave_bashkiria_package = yes
			has_independence_wave_bsk_command_roster = yes
		}
		set_country_flag = independence_wave_bsk_roster_checkpoint
		if = {
			limit = { NOT = { has_country_flag = independence_wave_bsk_portrait_override } }
			set_portraits = {
				character = BSK_yakov_bykin
				civilian = {
					large = GFX_portrait_BSK_independence_wave_yakov_bykin
				}
			}
			set_country_flag = independence_wave_bsk_portrait_override
		}
	}
	if = {
		# The Event 006 portrait remains character-scoped so ordinary vanilla
		# RUT keeps its installed portrait outside the Independence Wave.
		limit = { is_independence_wave_rut_package = yes }
		set_portraits = {
			character = RUT_augustin_voloshyn
			civilian = {
				large = GFX_portrait_RUT_independence_wave_augustin_voloshyn
			}
		}
	}
	if = {
		limit = {
			is_independence_wave_rut_package = yes
			has_independence_wave_rut_command_roster = yes
		}
		set_country_flag = independence_wave_rut_roster_checkpoint
	}
	if = {
		limit = {
			is_independence_wave_mac_package = yes
			has_independence_wave_mac_command_roster = yes
		}
		set_country_flag = independence_wave_mac_roster_checkpoint
	}
	if = {
		limit = {
			is_independence_wave_axx_package = yes
			has_independence_wave_axx_command_roster = yes
		}
		set_country_flag = independence_wave_axx_roster_checkpoint
	}
	if = {
		limit = {
			is_independence_wave_bax_package = yes
			has_independence_wave_bax_command_roster = yes
		}
		set_country_flag = independence_wave_bax_roster_checkpoint
	}
	if = {
		limit = {
			is_independence_wave_bbx_package = yes
			has_independence_wave_bbx_command_roster = yes
		}
		set_country_flag = independence_wave_bbx_roster_checkpoint
	}
	if = {
		limit = {
			is_independence_wave_bos_package = yes
			has_independence_wave_bos_command_roster = yes
		}
		set_country_flag = independence_wave_bos_roster_checkpoint
	}
	if = {
		limit = {
			is_independence_wave_nav_package = yes
			has_independence_wave_nav_command_roster = yes
		}
		set_country_flag = independence_wave_nav_roster_checkpoint
	}
	if = {
		limit = {
			is_independence_wave_glc_package = yes
			has_independence_wave_glc_command_roster = yes
		}
		set_country_flag = independence_wave_glc_roster_checkpoint
	}
}
