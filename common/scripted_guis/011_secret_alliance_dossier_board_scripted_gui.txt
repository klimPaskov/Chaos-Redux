scripted_gui = {
	secret_alliance_dossier_board_scripted_gui = {
		context_type = decision_category
		window_name = "secret_alliance_dossier_board_container"

		ai_enabled = { always = no }
		visible = {
			is_ai = no
			secret_alliance_target_can_open_dossier = yes
		}

		effects = {
			secret_alliance_select_known_member_button_click = {
				secret_alliance_select_known_member_card = yes
			}

			secret_alliance_select_wavering_member_button_click = {
				secret_alliance_select_wavering_member_card = yes
			}

			secret_alliance_select_neighbor_member_button_click = {
				secret_alliance_select_neighbor_member_card = yes
			}

			secret_alliance_select_patron_member_button_click = {
				secret_alliance_select_patron_member_card = yes
			}

			secret_alliance_clear_member_selection_button_click = {
				secret_alliance_clear_selected_member_card = yes
			}
		}

		triggers = {
			secret_alliance_select_known_member_button_click_enabled = {
				any_country = {
					has_country_flag = secret_alliance_member
					has_country_flag = secret_alliance_exposed
				}
			}

			secret_alliance_select_wavering_member_button_click_enabled = {
				any_country = {
					has_country_flag = secret_alliance_member
					has_country_flag = secret_alliance_wavering
				}
			}

			secret_alliance_select_neighbor_member_button_click_enabled = {
				secret_alliance_has_neighboring_exposed_member = yes
			}

			secret_alliance_select_patron_member_button_click_enabled = {
				has_event_target = secret_alliance_patron
			}

			secret_alliance_clear_member_selection_button_click_enabled = {
				has_event_target = secret_alliance_selected_card_member
			}

			secret_alliance_thread_glow_visible = {
				check_variable = {
					var = pact_pressure
					value = constant:secret_alliance_threshold.pressure_high
					compare = greater_than_or_equals
				}
			}

			secret_alliance_evidence_meter_fill_25_visible = {
				check_variable = {
					var = pact_evidence
					value = constant:secret_alliance_threshold.meter_min
					compare = greater_than_or_equals
				}
				NOT = {
					check_variable = {
						var = pact_evidence
						value = constant:secret_alliance_threshold.evidence_medium
						compare = greater_than_or_equals
					}
				}
			}

			secret_alliance_evidence_meter_fill_50_visible = {
				check_variable = {
					var = pact_evidence
					value = constant:secret_alliance_threshold.evidence_medium
					compare = greater_than_or_equals
				}
				NOT = {
					check_variable = {
						var = pact_evidence
						value = constant:secret_alliance_threshold.evidence_high
						compare = greater_than_or_equals
					}
				}
			}

			secret_alliance_evidence_meter_fill_75_visible = {
				check_variable = {
					var = pact_evidence
					value = constant:secret_alliance_threshold.evidence_high
					compare = greater_than_or_equals
				}
				NOT = {
					check_variable = {
						var = pact_evidence
						value = constant:secret_alliance_threshold.public_reveal_evidence
						compare = greater_than_or_equals
					}
				}
			}

			secret_alliance_evidence_meter_fill_100_visible = {
				check_variable = {
					var = pact_evidence
					value = constant:secret_alliance_threshold.public_reveal_evidence
					compare = greater_than_or_equals
				}
			}

			secret_alliance_pressure_meter_fill_25_visible = {
				check_variable = {
					var = pact_pressure
					value = constant:secret_alliance_threshold.meter_min
					compare = greater_than_or_equals
				}
				NOT = {
					check_variable = {
						var = pact_pressure
						value = constant:secret_alliance_threshold.open_category_suspicion
						compare = greater_than_or_equals
					}
				}
			}

			secret_alliance_pressure_meter_fill_50_visible = {
				check_variable = {
					var = pact_pressure
					value = constant:secret_alliance_threshold.open_category_suspicion
					compare = greater_than_or_equals
				}
				NOT = {
					check_variable = {
						var = pact_pressure
						value = constant:secret_alliance_threshold.pressure_high
						compare = greater_than_or_equals
					}
				}
			}

			secret_alliance_pressure_meter_fill_75_visible = {
				check_variable = {
					var = pact_pressure
					value = constant:secret_alliance_threshold.pressure_high
					compare = greater_than_or_equals
				}
				NOT = {
					check_variable = {
						var = pact_pressure
						value = constant:secret_alliance_threshold.campaign_reveal_pressure
						compare = greater_than_or_equals
					}
				}
			}

			secret_alliance_pressure_meter_fill_100_visible = {
				check_variable = {
					var = pact_pressure
					value = constant:secret_alliance_threshold.campaign_reveal_pressure
					compare = greater_than_or_equals
				}
			}

			secret_alliance_preparedness_meter_fill_25_visible = {
				check_variable = {
					var = pact_preparedness
					value = constant:secret_alliance_threshold.meter_min
					compare = greater_than_or_equals
				}
				NOT = {
					check_variable = {
						var = pact_preparedness
						value = constant:secret_alliance_threshold.open_category_suspicion
						compare = greater_than_or_equals
					}
				}
			}

			secret_alliance_preparedness_meter_fill_50_visible = {
				check_variable = {
					var = pact_preparedness
					value = constant:secret_alliance_threshold.open_category_suspicion
					compare = greater_than_or_equals
				}
				NOT = {
					check_variable = {
						var = pact_preparedness
						value = constant:secret_alliance_threshold.preparedness_high
						compare = greater_than_or_equals
					}
				}
			}

			secret_alliance_preparedness_meter_fill_75_visible = {
				check_variable = {
					var = pact_preparedness
					value = constant:secret_alliance_threshold.preparedness_high
					compare = greater_than_or_equals
				}
				NOT = {
					check_variable = {
						var = pact_preparedness
						value = constant:secret_alliance_threshold.meter_full
						compare = greater_than_or_equals
					}
				}
			}

			secret_alliance_preparedness_meter_fill_100_visible = {
				check_variable = {
					var = pact_preparedness
					value = constant:secret_alliance_threshold.meter_full
					compare = greater_than_or_equals
				}
			}

			secret_alliance_radio_pulse_visible = {
				secret_alliance_target_can_use_hidden_actions = yes
				OR = {
					can_pay_secret_alliance_trace_pouches_cost = yes
					can_pay_secret_alliance_break_radio_cost = yes
				}
			}

			secret_alliance_seal_crack_visible = {
				secret_alliance_evidence_high = yes
			}

			secret_alliance_border_warning_visible = {
				secret_alliance_target_can_use_counterplay = yes
				any_country = {
					exists = yes
					has_country_flag = secret_alliance_member
					is_neighbor_of = ROOT
				}
			}
		}
	}
}
