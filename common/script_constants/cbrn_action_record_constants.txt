# CHAOS REDUX - UNIFIED CBRN ACTION RECORD CONSTANTS
# ============================================================================
# Overview:
# - Owns the immutable action-record status values and the reserved Event Log
#   system-entry identifier used by deliberate chemical and biological use.
# - The record ledger is package-wide; chemical and biological subsystem files
#   only adapt their native receipts into this schema.
# ============================================================================

cbrn_action_record_status = {
	schema = {
		any_key = yes
		data = int
	}

	unknown = 0
	first_use = 1
	repeated_use = 2
}

cbrn_action_record_math = {
	schema = {
		any_key = yes
		data = int
	}

	first_row = 0
	invalid_row = -1
	repeat_use_single_upper_bound = 2
	repeat_use_campaign_upper_bound = 5
}

cbrn_action_record_event_log = {
	schema = {
		any_key = yes
		data = int
	}

	event_id = 991
}
