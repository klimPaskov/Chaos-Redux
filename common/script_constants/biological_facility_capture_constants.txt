# ============================================================================
# CHAOS REDUX - CAPTURED BIOLOGICAL FACILITY SCRIPT CONSTANTS
# ============================================================================
# Inputs: none.
# Outputs: exact-state capture timing, payload fractions, hazard thresholds,
#   evidence values, Condemnation values, and history increments.
# Side effects: none. Agent, route, source, result, proof, visibility, severity,
#   and context enums remain owned by their existing subsystem constants.
# ============================================================================

bio_facility_capture_math = {
	schema = {
		any_key = yes
		data = fixed_point
	}

	zero = 0
	one = 1
	negation = -1
}

bio_facility_capture_timing = {
	schema = {
		any_key = yes
		data = fixed_point
	}

	initial_hazard_delay_days = 45
	recurrent_hazard_delay_days = 30
}

bio_facility_capture_payload = {
	schema = {
		any_key = yes
		data = fixed_point
	}

	minimum_release = 1
	deadline_release_fraction = 0.10
	raid_failure_release_fraction = 0.15
	secure_limited_fraction = 0.40
	destroy_limited_fraction = 0.50
	full_fraction = 1
	high_hazard_threshold = 100
}

bio_facility_capture_evidence = {
	schema = {
		any_key = yes
		data = fixed_point
	}

	secure_limited = 25
	secure_success = 50
	secure_critical = 80
	destroy_limited = 10
	destroy_success = 15
	destroy_critical = 40
}

bio_facility_capture_condemnation = {
	schema = {
		any_key = yes
		data = fixed_point
	}

	secure_limited = 8
	secure_success = 15
	secure_critical = 25
	destroy_limited = 3
	destroy_success = 5
	destroy_critical = 12
}

bio_facility_capture_history = {
	schema = {
		any_key = yes
		data = fixed_point
	}

	one_incident = 1
	one_recovery = 1
}
