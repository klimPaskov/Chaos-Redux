# ============================================================================
# CHAOS REDUX - INDIVIDUAL CRISIS TARGETING CONSTANTS
# ============================================================================
# Shared cap and target-pressure curve for country-scoped event packages.
# Global and all-eligible systems never consume these slots.
# ============================================================================

individual_crisis_targeting = {
	schema = {
		any_key = yes
		data = fixed_point
	}

	zero = 0
	one = 1
	load_one_upper_bound = 2
	active_cap = 3
	load_zero_factor = 1.00
	load_one_factor = 0.50
	load_two_factor = 0.25
	load_zero_tickets = 4
	load_one_tickets = 2
	load_two_tickets = 1
}

time_traveler_event = {
	schema = {
		any_key = yes
		data = int
	}

	id = 30
	prefire_reservation_days = 3
}
