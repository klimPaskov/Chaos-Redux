# ============================================================================
# CHAOS REDUX - REUSABLE COUNTRY API REGISTRY
# ============================================================================
# Engine country identifiers remain three-character tags. The public API uses
# the `chaosx_country_*` collection names defined in
# common/collections/chaosx_country_collections.txt; this category supplies
# the stable all-mod and regional arrays behind those views.
#
# Event 006's 206-row ledger remains authoritative for package provenance,
# readiness, reservation groups, and dormant carriers. These arrays are only
# lookup surfaces for later events and crisis systems. A caller must still
# record its own origin and pass its own package/content gates before loading
# a tree, history setup, focus overlay, or diplomacy package.
# ============================================================================

chaosx_country_registry = {
	schema = {
		any_key = yes
		array = country
	}

	# Every country currently declared by Chaos Redux, including reusable
	# carriers owned by other events. This is the broad API view.
	all_chaosx_country_tags = {
		AAX ABX ACX ADX AEX AFX AGX AJX AKX ANX AOX INX ARD ARX ASX ATX AXX BAX BBH BBX BFX BHX BJX BKX BWX CBA CBB CBC CBD CBE CBF CBG CBH CBL CFR CIX CJX CKX CLX COX CPX CQX CUX CVX CWX CXX CYX DAX DBX DFX DHC DHO DHX DJX DKX DLX DMX DNX DOX DPX DSC DSX DTH DUX DVX DYX DZX EBX EEX EHX ELX EMX ENX EQX ERX ESX EUX EWX EXX EYX FAX FBX FDX FEV FLX FNX FOX FPX FSX FTH FUX FVX FXX GAC GBX GCX GDX GGX GHX GIX GLX GMX GRX GTX GYX GZX HAX HBX HCX HDX HEX HFX HGX HHX HKX HMX HPX HQX HSX HTX HUX HWX HXX HYX HZX IAX IBX ICD ICX IUL KHC KMB KRG MFR IMX NLC NRF IJX PRA REV IKX RTA RTB RTC RTD RTE RTF RTG RTH RTI RTJ RTK RTL RTM RTX SDZ SOG SZA TNC ILX UDC UWD UWR ZIN ZZZ
	}

	# Event 005 / Soviet Collapse carriers. AAX, ABX, ADX, AEX, ANX, AOX,
	# and INX are the collision-free replacements for the legacy IDs AAX,
	# ABX, ADX, AEX, ANX, AOX, and INX.
	soviet_collapse_tags = {
		AAX ABX ADX AEX ANX AOX INX ARD BBH CFR DHC DSC FEV FTH GAC ICD IUL KHC KMB MFR IMX NLC NRF IJX PRA IKX SDZ SOG SZA TNC ILX UDC UWD UWR
	}

	# The four African Event 006 region arrays are merged here for consumers
	# such as the Africa event. Overlays and dormant carriers remain visible in
	# this lookup; readiness and map-binding checks stay with the caller.
	africa_tags = {
		DFX DHX DJX DKX DLX DMX DNX HYX RIF BIA DAH DOX DPX DSX DUX DVX DYX DZX EBX SOK AFA BRD EEX EHX ELX EMX ENX HAR ORO RWA SOM TIG UGA BAR EQX ERX ESX EUX EWX EXX EYX FAX FBX HZX MAD
	}
}
