# ============================================================================
# CHAOS REDUX - SHARED CLONE INFANTRY SYSTEM CONSTANTS
# ============================================================================
# Shared tuning for reusable clone equipment, reserve manpower, templates,
# and provider refinements. Static unit, equipment, and technology databases
# mirror these values with file-local constants where those databases do not
# accept script-constant tokens.
# ============================================================================

clone_system = {
	schema = {
		any_key = yes
		data = int
	}

	weekly_manpower_per_equipment = 10
	default_template_battalions = 10
	default_template_width = 20
	default_template_clone_equipment = 10
	default_template_infantry_equipment = 900
}
