# ============================================================================
# CHAOS REDUX - MIGRATION OWNER-ADAPTER TUNING
# ============================================================================
# Migration-only condemnation gains. These values annotate exact cohort and
# route receipts; they do not apply food pressure, population loss, or route
# transfer themselves.
# ============================================================================

migration_condemnation_gain = {
	schema = {
		any_key = yes
		data = fixed_point
	}

	deportation = 20
	forced_return = 16
	violent_pushback = 22
}

migration_adapter_pressure = {
	schema = {
		any_key = yes
		data = fixed_point
	}

	nuclear_strike_population_fraction = 0.05
}

migration_to_famine_reception_demand = {
	schema = {
		any_key = yes
		data = fixed_point
	}

	schema_version = 1
	minimum_revision = 1
	cause_trapped_population = 1
}
