# ============================================================================
# CHAOS REDUX - FAMINE AND MIGRATION OWNER-ADAPTER TUNING
# ============================================================================
# Values passed to existing owner systems. Condemnation retains its own
# aggregation, evidence, sanctions, and decay logic.
# ============================================================================

famine_migration_condemnation_gain = {
	deliberate_starvation = 25
	relief_obstruction = 18
	deportation = 20
	forced_return = 16
	violent_pushback = 22
	concealment = 12
}

# Fractions are used only where the owner callback proves the affected state
# but does not expose an applied civilian-loss amount (for example, a nuclear
# strike callback). All death-bearing owners pass their exact applied amount.
famine_migration_adapter_pressure = {
	nuclear_strike_population_fraction = 0.05
}
