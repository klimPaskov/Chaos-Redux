# ============================================================================
# CHAOS REDUX - FAMINE OWNER-ADAPTER TUNING
# ============================================================================
# Famine-only condemnation gains and the one bounded nuclear aftermath
# population estimate. Exact-death owners pass applied amounts directly;
# these values never create migration or alter population.
# ============================================================================

famine_condemnation_gain = {
	schema = {
		any_key = yes
		data = fixed_point
	}

	deliberate_starvation = 25
	relief_obstruction = 18
	concealment = 12
}

famine_adapter_pressure = {
	schema = {
		any_key = yes
		data = fixed_point
	}

	nuclear_strike_population_fraction = 0.05
}

famine_to_migration_food_safety = {
	schema = {
		any_key = yes
		data = fixed_point
	}

	schema_version = 1
	minimum_revision = 1
	minimum_stage = 0
	maximum_stage = 4
	minimum_component = 0
	maximum_component = 200
}
