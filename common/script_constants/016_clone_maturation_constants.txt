# ============================================================================
# EVENT 016 - KRG CLONE MATURATION TRANSACTION PROFILE
# ============================================================================
# Native decisions own CIC reservation, duration and cooldown. The transaction
# snapshots direct costs and physical clone output so tuning cannot alter an
# already paid receipt. No lifetime production cap belongs to this profile.
# ============================================================================
clone_maturation = {
	schema = {
		any_key = yes
		data = fixed_point
	}
	political_power = 65
	support_equipment = 150
	fuel = 1000
	civilian_factories = 4
	clone_equipment = 100
	duration_days = 90
	cooldown_days = 30
	zero = 0
	one = 1
	negative_one = -1
}
