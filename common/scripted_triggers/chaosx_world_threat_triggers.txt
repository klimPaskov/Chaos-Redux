# Overview:
# - Shared scripted triggers for the mod-wide world-threat framework.
# - `world_in_threat` is the aggregate flag used by other systems to decide
#   whether normal inter-state competition should be suppressed in favor of
#   cooperation against an existential threat.

is_world_in_threat = {
	has_global_flag = world_in_threat
}

has_world_threat_source_zombies = {
	has_global_flag = world_threat_source_zombies
}
