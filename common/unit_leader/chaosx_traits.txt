# Static database fields in this file do not accept script-constant tokens.
# These file-scoped values mirror the authoritative shared constants.
@CR_SC_CBRN_DOCTRINE_AI_HIGH_COMMAND_BASE = 1
@CR_SC_CBRN_DOCTRINE_LEADER_OPERATIONS_COMMANDER_TRAIT_COST = 500

leader_traits = {
	in_antarctica = {
		type = all
		trait_type = status_trait

		attack_skill = -3
		defense_skill = -3
		logistics_skill = -3
		planning_skill = -3
	}

	# Move vanilla utility traits slightly lower so chemical trait sits between
	# core general traits and the staffer/delegator branch.
	skilled_staffer = {
		gui_row = 16
	}

	expert_delegator = {
		gui_row = 16
	}

	chemical_operations_commander = {
		type = corps_commander
		trait_type = assignable_trait
		override_effect_tooltip = chemical_operations_commander_tt
		custom_prerequisite_tooltip = chemical_operations_commander_prerequisite_tt
		prerequisites = {
			FROM = { has_doctrine = chaos_warfare }
		}
		gain_xp = {
			always = no
		}
		cost = @CR_SC_CBRN_DOCTRINE_LEADER_OPERATIONS_COMMANDER_TRAIT_COST
		ai_will_do = {
			factor = @CR_SC_CBRN_DOCTRINE_AI_HIGH_COMMAND_BASE
			modifier = {
				factor = constant:cbrn_doctrine_ai.nonhuman_factor
				FROM = { is_actual_nonhuman_country = yes }
			}
			modifier = {
				factor = constant:cbrn_doctrine_ai.prepared_profile_factor
				FROM = { cbrn_profile_is_prepared_power = yes }
			}
			modifier = {
				factor = constant:cbrn_doctrine_ai.military_profile_factor
				FROM = { cbrn_profile_is_military_first = yes }
			}
		}
		gui_row = 15
	}

	chemical_cylinder_active = {
		type = all
		trait_type = status_trait
		gain_xp = { always = no }
	}

	chemical_cylinder_active_chlorine = {
		type = all
		trait_type = status_trait
		gain_xp = { always = no }
	}

	chemical_cylinder_active_phosgene = {
		type = all
		trait_type = status_trait
		gain_xp = { always = no }
	}

	chemical_cylinder_active_mustard = {
		type = all
		trait_type = status_trait
		gain_xp = { always = no }
	}

	chemical_cylinder_active_lewisite = {
		type = all
		trait_type = status_trait
		gain_xp = { always = no }
	}
}
