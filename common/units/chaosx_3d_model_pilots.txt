# Chaos Redux 3D model pilot unit consumer.
# The unit reuses the vanilla infantry combat envelope and supplies a unique
# sprite token so the runtime entity resolves to the validated PDX humanoid.

sub_units = {
	chaosx_anomaly_recon_trooper = {
		abbreviation = "AXR"
		sprite = infantry
		map_icon_category = infantry

		priority = 610
		ai_priority = 50
		active = yes

		type = {
			infantry
		}

		group = infantry

		categories = {
			category_front_line
			category_light_infantry
			category_all_infantry
			category_army
			category_infantry_and_bicycle
		}

		combat_width = 2
		max_strength = 25
		max_organisation = 60
		default_morale = 0.3
		manpower = 1000

		training_time = 90
		suppression = 1.5
		weight = 0.5
		supply_consumption = 0.06

		need = {
			infantry_equipment = 100
		}

		# Make this sub-unit win the division model selection when a consumer
		# does not provide an explicit template override.
		division_3d_model_priority = 99999
	}
}
