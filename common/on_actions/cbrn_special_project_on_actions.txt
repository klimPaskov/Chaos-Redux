# The country completing a special project is ROOT. This bounded callback
# handles either completion order for the two vanilla vehicle integrations.
on_actions = {
	on_project_completion = {
		effect = {
			ROOT = { cbrn_sync_vanilla_project_integrations = yes }
		}
	}
}
