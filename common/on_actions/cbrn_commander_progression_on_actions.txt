# ============================================================================
# CHAOS REDUX - CBRN COMMANDER FIELD SERVICE
# ============================================================================
# Overview:
# - Vanilla's on_army_leader_won_combat fires for the winning army leader;
#   FROM is the owner's country. This records a proven leader-scoped victory
#   only when that leader commands a protected CBRN detachment.
# - No country-wide daily, weekly, or monthly iteration is registered.
# ============================================================================

on_actions = {
	on_army_leader_won_combat = {
		effect = { cbrn_commander_record_protected_combat_victory = yes }
	}
}
