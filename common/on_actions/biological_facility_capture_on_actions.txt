# ============================================================================
# CHAOS REDUX - CAPTURED BIOLOGICAL FACILITY ON ACTIONS
# ============================================================================
# on_state_control_changed scopes: ROOT new controller, FROM old controller,
# and FROM.FROM the exact changed state. Only that exact state is evaluated.
# No country/state replacement scan and no periodic pulse are used.
# ============================================================================

on_actions = {
	on_state_control_changed = {
		effect = {
			FROM.FROM = {
				bio_facility_capture_handle_state_control_changed = yes
			}
		}
	}
}
