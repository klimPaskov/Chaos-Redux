# ============================================================================
# CHAOS REDUX - EVENT 020 RAT AI TARGET TEMPLATES
# ============================================================================
# RTA and RTX use the same non-human target roles. The runtime pulse creates
# and locks the matching division templates; these targets keep the AI's
# template_prio and role_ratio strategies pointed at valid rat battalions.
# Both country tags are listed explicitly so no generic country receives a
# rat-only role or tries to build the inactive rat sub-units.
# ============================================================================

rat_swarm_RTA_RTX = {
	available_for = { RTA RTX }
	role = rat_swarm
	upgrade_prio = { base = 1 }

	rat_swarm = {
		upgrade_prio = { base = 1 }
		target_template = {
			regiments = { rat_swarm = 4 }
		}
	}
}

rat_brutes_RTA_RTX = {
	available_for = { RTA RTX }
	role = rat_brutes
	upgrade_prio = { base = 1 }

	rat_brutes = {
		upgrade_prio = { base = 1 }
		target_template = {
			regiments = { rat_brutes = 4 }
			support = { rat_tunnelers = 1 }
		}
	}
}

rat_burrowers_RTA_RTX = {
	available_for = { RTA RTX }
	role = rat_burrowers
	upgrade_prio = { base = 1 }

	rat_burrowers = {
		upgrade_prio = { base = 1 }
		target_template = {
			regiments = { rat_burrowers = 4 }
			support = { rat_tunnelers = 1 }
		}
	}
}

rat_carrion_guard_RTA_RTX = {
	available_for = { RTA RTX }
	role = rat_carrion_guard
	upgrade_prio = { base = 1 }

	rat_carrion_guard = {
		upgrade_prio = { base = 1 }
		target_template = {
			regiments = { rat_carrion_guard = 4 }
		}
	}
}

rat_dock_stowaways_RTA_RTX = {
	available_for = { RTA RTX }
	role = rat_dock_stowaways
	upgrade_prio = { base = 1 }

	rat_dock_stowaways = {
		upgrade_prio = { base = 1 }
		target_template = {
			regiments = { rat_dock_stowaways = 4 }
			support = { rat_tunnelers = 1 }
		}
	}
}
