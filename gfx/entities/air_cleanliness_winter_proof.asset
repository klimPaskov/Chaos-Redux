# ============================================================================
# CHAOS REDUX - AIR WINTER NORMAL-MAP TECHNICAL PROOF
# ============================================================================
# Proof-only entity used before final regional climate assets are produced.
# It reuses the vanilla light-snow particle so the runtime placement, zoom,
# replacement, destruction, and recovery route can be evaluated independently
# from final art. The final Air Winter entities must use dedicated assets.
# ============================================================================

entity = {
	name = "air_winter_normal_map_proof_entity"
	cull_radius = 30

	default_state = "idle"
	state = {
		name = "idle"
		state_time = 2
		looping = no
		event = {
			time = 0
			particle = "snow_small_particle"
			keep_particle = yes
		}
	}
}
