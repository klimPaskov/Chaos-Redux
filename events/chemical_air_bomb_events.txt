add_namespace = chemical_air_bomb

# ============================================================================
# CHAOS REDUX - RETIRED CHEMICAL AIR ESTIMATOR EVENT
# ============================================================================
# The stable event ID remains as a cleanup endpoint for a previously scheduled
# targeted tick. It cannot calculate or dispatch exposure and never reschedules.
# ============================================================================

country_event = {
	id = chemical_air_bomb.1
	hidden = yes
	is_triggered_only = yes

	immediate = {
		clear_variable = chem_air_ground_ops_heat
		clr_country_flag = chem_air_bomb_targeted_tick_active
	}
}
