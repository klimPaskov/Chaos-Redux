add_namespace = chaosx.nr13

# ============================================================================
# CHAOS REDUX - EVENT 013 RESERVED PLACEHOLDER
# ============================================================================
# Event 013 is intentionally inert. The namespace and root event remain defined
# so stale manual calls fail harmlessly instead of referencing missing ids.
# ============================================================================

country_event = {
	id = chaosx.nr13.1
	hidden = yes
	is_triggered_only = yes
}
