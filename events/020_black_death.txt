add_namespace = chaosx.nr20

# ============================================================================
# CHAOS REDUX - EVENT 020 BLACK PLAGUE ENTRY AND RUNTIME
# ============================================================================
# The root remains chaosx.nr20.1 for the shared event dispatcher. It delegates
# every natural start to one canonical weighted-origin effect. Event .900 is the
# generation- and due-day-guarded state-owned scheduler callback; it is never a
# world-iterating periodic on_action.
# ============================================================================

country_event = {
	id = chaosx.nr20.1
	hidden = yes
	is_triggered_only = yes

	immediate = {
		hidden_effect = {
			black_plague_start_natural_outbreak = yes
		}
	}
}

state_event = {
	id = chaosx.nr20.900
	hidden = yes
	is_triggered_only = yes

	immediate = {
		hidden_effect = {
			black_plague_run_scheduled_callback = yes
		}
	}
}
