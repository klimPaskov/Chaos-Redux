# CHAOS REDUX - BIOLOGICAL NATIVE RAID COUNTRY-ROOT BRIDGES
# Native raid-instance callbacks save exact regular targets and chain-local markers.
# Immediate hidden events restore actor-country ROOT for the entire transaction.
# Native essential equipment is the sole payload debit.

add_namespace = biological_native_raid_bridge

country_event = {
	id = biological_native_raid_bridge.1
	is_triggered_only = yes
	hidden = yes
	immediate = {
		set_temp_variable = { bio_battle_bridge_agent_count = constant:bio_lifecycle_math.zero }
		set_temp_variable = { bio_battle_bridge_outcome_count = constant:bio_lifecycle_math.zero }
		if = {
			limit = { has_event_target = bio_battle_bridge_agent_anthrax }
			add_to_temp_variable = { bio_battle_bridge_agent_count = constant:bio_lifecycle_math.one }
			set_temp_variable = { bio_battlefield_agent = constant:bio_lifecycle_agent.anthrax }
		}
		if = {
			limit = { has_event_target = bio_battle_bridge_agent_plague }
			add_to_temp_variable = { bio_battle_bridge_agent_count = constant:bio_lifecycle_math.one }
			set_temp_variable = { bio_battlefield_agent = constant:bio_lifecycle_agent.plague }
		}
		if = {
			limit = { has_event_target = bio_battle_bridge_agent_tularemia }
			add_to_temp_variable = { bio_battle_bridge_agent_count = constant:bio_lifecycle_math.one }
			set_temp_variable = { bio_battlefield_agent = constant:bio_lifecycle_agent.tularemia }
		}
		if = {
			limit = { has_event_target = bio_battle_bridge_agent_smallpox }
			add_to_temp_variable = { bio_battle_bridge_agent_count = constant:bio_lifecycle_math.one }
			set_temp_variable = { bio_battlefield_agent = constant:bio_lifecycle_agent.smallpox }
		}
		if = {
			limit = { has_event_target = bio_battle_bridge_outcome_failure }
			add_to_temp_variable = { bio_battle_bridge_outcome_count = constant:bio_lifecycle_math.one }
			set_temp_variable = { bio_battlefield_engine_outcome = constant:bio_battlefield_engine_outcome.failure }
		}
		if = {
			limit = { has_event_target = bio_battle_bridge_outcome_limited_success }
			add_to_temp_variable = { bio_battle_bridge_outcome_count = constant:bio_lifecycle_math.one }
			set_temp_variable = { bio_battlefield_engine_outcome = constant:bio_battlefield_engine_outcome.limited_success }
		}
		if = {
			limit = { has_event_target = bio_battle_bridge_outcome_success }
			add_to_temp_variable = { bio_battle_bridge_outcome_count = constant:bio_lifecycle_math.one }
			set_temp_variable = { bio_battlefield_engine_outcome = constant:bio_battlefield_engine_outcome.success }
		}
		if = {
			limit = { has_event_target = bio_battle_bridge_outcome_critical_success }
			add_to_temp_variable = { bio_battle_bridge_outcome_count = constant:bio_lifecycle_math.one }
			set_temp_variable = { bio_battlefield_engine_outcome = constant:bio_battlefield_engine_outcome.critical_success }
		}
		if = {
			limit = {
				has_event_target = bio_battlefield_actor
				has_event_target = bio_battlefield_target_state
				has_event_target = bio_battlefield_victim
				tag = event_target:bio_battlefield_actor
				check_variable = { var = bio_battle_bridge_agent_count value = constant:bio_lifecycle_math.one compare = equals }
				check_variable = { var = bio_battle_bridge_outcome_count value = constant:bio_lifecycle_math.one compare = equals }
			}
			bio_resolve_battlefield_dissemination = yes
		}
	}
}

country_event = {
	id = biological_native_raid_bridge.2
	is_triggered_only = yes
	hidden = yes
	immediate = {
		set_temp_variable = { bio_sabotage_bridge_agent_count = constant:bio_lifecycle_math.zero }
		set_temp_variable = { bio_sabotage_bridge_outcome_count = constant:bio_lifecycle_math.zero }
		if = {
			limit = { has_event_target = bio_sabotage_bridge_agent_anthrax }
			add_to_temp_variable = { bio_sabotage_bridge_agent_count = constant:bio_lifecycle_math.one }
			set_temp_variable = { bio_raid_agent = constant:bio_lifecycle_agent.anthrax }
		}
		if = {
			limit = { has_event_target = bio_sabotage_bridge_agent_plague }
			add_to_temp_variable = { bio_sabotage_bridge_agent_count = constant:bio_lifecycle_math.one }
			set_temp_variable = { bio_raid_agent = constant:bio_lifecycle_agent.plague }
		}
		if = {
			limit = { has_event_target = bio_sabotage_bridge_agent_tularemia }
			add_to_temp_variable = { bio_sabotage_bridge_agent_count = constant:bio_lifecycle_math.one }
			set_temp_variable = { bio_raid_agent = constant:bio_lifecycle_agent.tularemia }
		}
		if = {
			limit = { has_event_target = bio_sabotage_bridge_agent_smallpox }
			add_to_temp_variable = { bio_sabotage_bridge_agent_count = constant:bio_lifecycle_math.one }
			set_temp_variable = { bio_raid_agent = constant:bio_lifecycle_agent.smallpox }
		}
		if = {
			limit = { has_event_target = bio_sabotage_bridge_outcome_failure }
			add_to_temp_variable = { bio_sabotage_bridge_outcome_count = constant:bio_lifecycle_math.one }
			set_temp_variable = { bio_raid_engine_outcome = constant:bio_strategic_raid_engine_outcome.failure }
		}
		if = {
			limit = { has_event_target = bio_sabotage_bridge_outcome_limited_success }
			add_to_temp_variable = { bio_sabotage_bridge_outcome_count = constant:bio_lifecycle_math.one }
			set_temp_variable = { bio_raid_engine_outcome = constant:bio_strategic_raid_engine_outcome.limited_success }
		}
		if = {
			limit = { has_event_target = bio_sabotage_bridge_outcome_success }
			add_to_temp_variable = { bio_sabotage_bridge_outcome_count = constant:bio_lifecycle_math.one }
			set_temp_variable = { bio_raid_engine_outcome = constant:bio_strategic_raid_engine_outcome.success }
		}
		if = {
			limit = { has_event_target = bio_sabotage_bridge_outcome_critical_success }
			add_to_temp_variable = { bio_sabotage_bridge_outcome_count = constant:bio_lifecycle_math.one }
			set_temp_variable = { bio_raid_engine_outcome = constant:bio_strategic_raid_engine_outcome.critical_success }
		}
		if = {
			limit = {
				has_event_target = bio_sabotage_actor_target
				has_event_target = bio_sabotage_state_target
				has_event_target = bio_sabotage_victim_target
				tag = event_target:bio_sabotage_actor_target
				check_variable = { var = bio_sabotage_bridge_agent_count value = constant:bio_lifecycle_math.one compare = equals }
				check_variable = { var = bio_sabotage_bridge_outcome_count value = constant:bio_lifecycle_math.one compare = equals }
			}
			bio_resolve_supply_chain_raid_outcome = yes
		}
	}
}

country_event = {
	id = biological_native_raid_bridge.3
	is_triggered_only = yes
	hidden = yes
	immediate = {
		set_temp_variable = { bio_strategic_bridge_agent_count = constant:bio_lifecycle_math.zero }
		set_temp_variable = { bio_strategic_bridge_outcome_count = constant:bio_lifecycle_math.zero }
		if = {
			limit = { has_event_target = bio_strategic_bridge_agent_anthrax }
			add_to_temp_variable = { bio_strategic_bridge_agent_count = constant:bio_lifecycle_math.one }
			set_temp_variable = { bio_raid_agent = constant:bio_lifecycle_agent.anthrax }
		}
		if = {
			limit = { has_event_target = bio_strategic_bridge_agent_plague }
			add_to_temp_variable = { bio_strategic_bridge_agent_count = constant:bio_lifecycle_math.one }
			set_temp_variable = { bio_raid_agent = constant:bio_lifecycle_agent.plague }
		}
		if = {
			limit = { has_event_target = bio_strategic_bridge_agent_tularemia }
			add_to_temp_variable = { bio_strategic_bridge_agent_count = constant:bio_lifecycle_math.one }
			set_temp_variable = { bio_raid_agent = constant:bio_lifecycle_agent.tularemia }
		}
		if = {
			limit = { has_event_target = bio_strategic_bridge_agent_smallpox }
			add_to_temp_variable = { bio_strategic_bridge_agent_count = constant:bio_lifecycle_math.one }
			set_temp_variable = { bio_raid_agent = constant:bio_lifecycle_agent.smallpox }
		}
		if = {
			limit = { has_event_target = bio_strategic_bridge_outcome_failure }
			add_to_temp_variable = { bio_strategic_bridge_outcome_count = constant:bio_lifecycle_math.one }
			set_temp_variable = { bio_raid_engine_outcome = constant:bio_strategic_raid_engine_outcome.failure }
		}
		if = {
			limit = { has_event_target = bio_strategic_bridge_outcome_limited_success }
			add_to_temp_variable = { bio_strategic_bridge_outcome_count = constant:bio_lifecycle_math.one }
			set_temp_variable = { bio_raid_engine_outcome = constant:bio_strategic_raid_engine_outcome.limited_success }
		}
		if = {
			limit = { has_event_target = bio_strategic_bridge_outcome_success }
			add_to_temp_variable = { bio_strategic_bridge_outcome_count = constant:bio_lifecycle_math.one }
			set_temp_variable = { bio_raid_engine_outcome = constant:bio_strategic_raid_engine_outcome.success }
		}
		if = {
			limit = { has_event_target = bio_strategic_bridge_outcome_critical_success }
			add_to_temp_variable = { bio_strategic_bridge_outcome_count = constant:bio_lifecycle_math.one }
			set_temp_variable = { bio_raid_engine_outcome = constant:bio_strategic_raid_engine_outcome.critical_success }
		}
		if = {
			limit = {
				has_event_target = bio_raid_actor
				has_event_target = bio_raid_target_state
				has_event_target = bio_raid_victim
				tag = event_target:bio_raid_actor
				check_variable = { var = bio_strategic_bridge_agent_count value = constant:bio_lifecycle_math.one compare = equals }
				check_variable = { var = bio_strategic_bridge_outcome_count value = constant:bio_lifecycle_math.one compare = equals }
			}
			bio_resolve_strategic_raid_outcome = yes
		}
	}
}
# Facility recovery keeps the exact site pointer and the operation identity.
country_event = {
	id = biological_native_raid_bridge.4
	is_triggered_only = yes
	hidden = yes
	immediate = {
		set_temp_variable = { bio_facility_bridge_operation_count = constant:bio_lifecycle_math.zero }
		set_temp_variable = { bio_facility_bridge_outcome_count = constant:bio_lifecycle_math.zero }
		if = {
			limit = { has_event_target = bio_facility_bridge_operation_secure }
			add_to_temp_variable = { bio_facility_bridge_operation_count = constant:bio_lifecycle_math.one }
		}
		if = {
			limit = { has_event_target = bio_facility_bridge_operation_destroy }
			add_to_temp_variable = { bio_facility_bridge_operation_count = constant:bio_lifecycle_math.one }
		}
		if = {
			limit = { has_event_target = bio_facility_bridge_outcome_failure }
			add_to_temp_variable = { bio_facility_bridge_outcome_count = constant:bio_lifecycle_math.one }
		}
		if = {
			limit = { has_event_target = bio_facility_bridge_outcome_limited }
			add_to_temp_variable = { bio_facility_bridge_outcome_count = constant:bio_lifecycle_math.one }
		}
		if = {
			limit = { has_event_target = bio_facility_bridge_outcome_success }
			add_to_temp_variable = { bio_facility_bridge_outcome_count = constant:bio_lifecycle_math.one }
		}
		if = {
			limit = { has_event_target = bio_facility_bridge_outcome_critical }
			add_to_temp_variable = { bio_facility_bridge_outcome_count = constant:bio_lifecycle_math.one }
		}
		if = {
			limit = {
				has_event_target = bio_facility_bridge_actor
				has_event_target = bio_facility_bridge_state
				tag = event_target:bio_facility_bridge_actor
				check_variable = { var = bio_facility_bridge_operation_count value = constant:bio_lifecycle_math.one compare = equals }
				check_variable = { var = bio_facility_bridge_outcome_count value = constant:bio_lifecycle_math.one compare = equals }
				bio_facility_capture_native_context_is_valid = yes
			}
			if = {
				limit = { has_event_target = bio_facility_bridge_operation_secure has_event_target = bio_facility_bridge_outcome_failure }
				bio_facility_capture_secure_failure = yes
			}
			if = {
				limit = { has_event_target = bio_facility_bridge_operation_secure has_event_target = bio_facility_bridge_outcome_limited }
				bio_facility_capture_secure_limited = yes
			}
			if = {
				limit = { has_event_target = bio_facility_bridge_operation_secure has_event_target = bio_facility_bridge_outcome_success }
				bio_facility_capture_secure_success = yes
			}
			if = {
				limit = { has_event_target = bio_facility_bridge_operation_secure has_event_target = bio_facility_bridge_outcome_critical }
				bio_facility_capture_secure_critical = yes
			}
			if = {
				limit = { has_event_target = bio_facility_bridge_operation_destroy has_event_target = bio_facility_bridge_outcome_failure }
				bio_facility_capture_destroy_failure = yes
			}
			if = {
				limit = { has_event_target = bio_facility_bridge_operation_destroy has_event_target = bio_facility_bridge_outcome_limited }
				bio_facility_capture_destroy_limited = yes
			}
			if = {
				limit = { has_event_target = bio_facility_bridge_operation_destroy has_event_target = bio_facility_bridge_outcome_success }
				bio_facility_capture_destroy_success = yes
			}
			if = {
				limit = { has_event_target = bio_facility_bridge_operation_destroy has_event_target = bio_facility_bridge_outcome_critical }
				bio_facility_capture_destroy_critical = yes
			}
		}
	}
}
