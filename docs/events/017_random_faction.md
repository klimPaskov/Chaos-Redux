# Event 017 Placeholder

Event 017 is reserved. The Random faction gameplay implementation is not active in the mod.

## Current State

- `events/017_join_faction.txt` keeps only the inert `chaosx.nr17.1` placeholder event.
- Event ID `17` is not registered in the random-event pools or the Diplomatic Panic cluster.
- No Event 017 decisions, ideas, on-actions, achievements, news events, event-log details, evolutions, or script constants are active.
- Source specs remain in `docs/specs/017_random_faction_specs/`.
- Generated assets remain in `docs/assets/017_random_faction/`, the Event 017 runtime asset folders under `gfx/`, and the root achievement DDS files.

## Future Reimplementation

Use the preserved specs and assets as source material if Event 017 is rebuilt. Reintroduce gameplay wiring only when event script, settings dispatch, event log, cluster membership, achievements, documentation, and spreadsheet rows are all aligned.
