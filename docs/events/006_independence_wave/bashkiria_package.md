# IW-045 Bashkiria Frontier Compact

IW-045 uses the dormant vanilla BSK carrier, compact anchor state 651 (Ufa), and the living former-host relation captured by the shared Event 006 release setup. The package uses the existing `mountain_or_frontier` package archetype with the separate `mounted_mobile` p45 force profile: mounted frontier guards, oilfield and rail depots, defecting units, and no inherited navy or air force.

## Runtime contract

The package-local gate is `is_independence_wave_bashkiria_package`. Setup initializes the visible ledgers `independence_wave_bsk_congress_cohesion` and `independence_wave_bsk_frontier_readiness`, clamps them to 0-100, and exposes the category `independence_wave_bashkiria_frontier_compact_category` only after setup. The founding mission requires a route government, both ledgers at the stable threshold, and continued ownership and control of state 651. Ten paid projects serialize through the active-project trigger and cancel on compact-crisis failure.

Five shared focus hooks cover the frontier congress, oilfield communities, mounted guards, former-host ledgers, and the Volga-Ural network corridor. The package preserves vanilla `BSK_yakov_bykin`. Its grounded source-placeholder portrait uses a dedicated package sprite during the roster checkpoint and restores the original vanilla token during cleanup, so the global vanilla sprite is never overridden.

## Routes and presentation

The constitutional, agrarian, socialist, and emergency routes use package-owned party names, ideas, route flags, and route-specific cosmetic tags. The installed vanilla BSK ideology flag ladder remains untouched. The 1918 Bashkurdistan blue-green-white flag is documented as a route-owned restoration reference only, not as a neutral 1936 baseline. The generated route ladders are alternate-history civic, agrarian, socialist, and emergency syntheses and are never presented as an attested 1936 universal flag.

## Validation and admission

Package-local source checks pass with balanced script blocks, complete scoped localisation keys, and the static Event 006 allocator, scenario, and tag audits unchanged. The four route ladders have 12/12 TGA and 12/12 DDS evidence passes in `docs/assets/006_independence_wave/iw045_bashkiria_flags_2026_08_14/validation/qa_results.json`. The grounded Bykin source-placeholder DDS and dedicated GFX sprite are recorded in the portrait manifest, while the .350 checkpoint and cleanup restore are character-scoped. State-651 map inspection and shared focus inspect/render completed with the documented workspace-wide diagnostics. BSK AI probability discovery reports no weighted surfaces, so no quantitative balance claim is made. Package-local setup, final-validation, and cleanup effects are present, but central adapter, content attestation, normal/scenario preflight, and Join admission remain fail-closed pending parent review of the complete package evidence and MCP typed scenarios.

The oilfield-communities reward now adds `bsk_oilfield_council` idempotently, while setup and both cleanup paths remove it with the other package ideas. The idea therefore has a complete package-local lifecycle without widening central admission.

## Future work

Review the source-placeholder portrait and route-owned flag evidence independently, then run the mandatory current-scenario probability compare and central package attestation. Preserve the Event 005 Soviet-collapse origin exclusion and do not reuse the Event 005 oilfield-workshop council portrait as an IW-045 leader.
