# Event 005 MTTH Release Family Audit

Audit date: 2026-05-24

## Scope

This audit checks the Part 4 required progressive release families against the current source. It covers the SOV-only MTTH scheduler, visible release-cause events, candidate selection, release or subject-freeing effects, setup package, focus-tree routing, and terminal fallback. It does not claim a live in-game playthrough.

## Scheduler Evidence

- `events/005_soviet_collapse.txt` keeps `chaosx.nr5.129` as the hidden SOV scheduler and `chaosx.nr5.130` through `chaosx.nr5.137` as visible report events.
- `common/mtth/005_soviet_collapse_mtth.txt` computes release and miss weights from threat, authority, obedience, breakaway cascades, depot pressure, foreign pressure, League pressure, old-movement pressure, mission failures, war pressure, regional cascades, and chaos tier.
- `soviet_collapse_maybe_release_threat_breakaway` blocks terminal collapse and progressive cooldown, keeps calm strong-center games out of extra-release rolls, routes terminal-level pressure through Union Unmade, computes a hidden continuous release-urgency score, and otherwise rolls release versus miss weights.
- `soviet_collapse_compute_progressive_release_urgency` adds continuous release pressure from Republic Momentum, Moscow Authority deficit, total threat, Foreign Penetration, depot vulnerability, League cohesion, old-movement pressure, breakaway count, and the prior month's failed objectives. This score gates sustained-pressure rolls and contributes a scaled, capped release weight after the MTTH band weights are read, so rising pressure makes new republics more likely before every value crosses a hard threshold without letting the opening first-wave state force an immediate extra release.
- Low-threat and strong-center miss modifiers clamp against `miss_weight_ceiling` rather than `miss_base`, so strong Soviet authority can actually suppress release odds above the default miss weight.
- `soviet_collapse_release_one_threat_breakaway_republic` selects one eligible candidate, releases or frees it, applies the normal breakaway setup package, loads the runtime focus tree, and applies the progressive cooldown.

## Family Matrix

| Required family | Tags or implementation | Progressive MTTH candidate evidence | Setup and routing evidence | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| Western and eastern European republics | `UKR`, `BLR`, `MOL` | Listed in `soviet_collapse_add_random_threat_breakaway_republic`; Ukraine, Belarus, and Moldova also participate in first-wave or extra-wave pools. | `soviet_collapse_release_selected_first_wave_republics` releases or frees the tag, then calls `soviet_collapse_setup_breakaway_country` and focus-tree loading. | source_pass | Ukraine and Belarus route to bespoke trees; Moldova routes to the Moldova tree. |
| Baltic republics | `LIT`, `LAT`, `EST` | Listed in first-wave western, extra-wave, and progressive MTTH pools. | Baltic event-created tags load `soviet_collapse_baltic_focus_tree`; terminal logic can place them into the Baltic local league where quorum exists. | source_pass | Local league formation is quorum-gated separately. |
| Caucasus republics | `GEO`, `ARM`, `AZR` | Listed in first-wave Caucasus, extra-wave, and progressive MTTH pools. | Caucasus event-created tags load `soviet_collapse_caucasus_focus_tree`; terminal logic can form the Caucasus compact where quorum exists. | source_pass | The high-chaos Mountain Republic remains a separate successor path. |
| Central Asian republics | `UZB`, `KYR`, `TAJ`, `TMS` | Listed in first-wave Central Asian, extra-wave, and progressive MTTH pools. | Central Asian event-created tags load `soviet_collapse_central_asia_focus_tree`; southern republic setup uses the shared breakaway package. | source_pass | Kazakhstan is deliberately not in the ordinary first-wave pool. |
| Kazakhstan after southern cascade | `KAZ` | Progressive selector includes Kazakhstan only when `has_soviet_collapse_three_smaller_central_asian_republics_free = yes`; opening release uses `can_soviet_collapse_open_kazakhstan_first_wave`. | Kazakhstan loads `soviet_collapse_kazakhstan_focus_tree` and can trigger the southern cascade setup for additional Central Asian republics. | source_pass | This preserves the Part 4 rule that Kazakhstan is not a normal first-wave default. |
| Karelia | `KAR` | Added to the progressive selector as an internal republic candidate behind moderate threat, existing breakaway, or higher chaos. | Event-created Karelia loads `soviet_collapse_internal_republic_focus_tree`; terminal fallback also releases or frees it. | source_pass | Internal republic route coverage is shared. |
| Komi | `KOM` | Added to the progressive selector as an internal republic candidate behind moderate threat, existing breakaway, or higher chaos. | Event-created Komi loads `soviet_collapse_internal_republic_focus_tree`; terminal fallback also releases or frees it. | source_pass | Focus audit records deeper Komi branch content. |
| Idel-Ural or Tatarstan | `TAT` plus high-chaos `IUL` | `TAT` is an internal progressive candidate; `IUL` remains a high-chaos successor spawned by its own pressure flag. | `TAT` loads the internal republic tree; `IUL` has a bespoke high-chaos setup and focus tree. | source_pass | The ordinary/internal release family is covered through Tatarstan. |
| Bashkiria | `BSK` | Added to the progressive selector as an internal republic candidate behind moderate threat, existing breakaway, or higher chaos. | Event-created Bashkiria loads `soviet_collapse_internal_republic_focus_tree`; terminal fallback also releases or frees it. | source_pass | Focus audit records deeper Bashkir branch content. |
| North Caucasus or Mountain Republic | `MRC` | Mountain Republic is not a vanilla release tag and is not released through `SOV = { release = ... }`; it is spawned by `soviet_collapse_spawn_mrc_if_enabled` when the mountain-council pressure flag and state-control gates are met. | `MRC` receives a bespoke setup, focus tree, decisions, and high-chaos successor handling. | source_pass | Covered as the implementation-defined Mountain Republic path rather than a normal MTTH release tag. |
| Crimea | `CRI` | Added to the progressive selector as an internal republic candidate behind moderate threat, existing breakaway, or higher chaos. | Event-created Crimea loads `soviet_collapse_internal_republic_focus_tree`; terminal fallback also releases or frees it. | source_pass | Internal tree includes Crimea settlement and Black Sea content. |
| Yakutia | `YAK` | Added to the progressive selector as an internal republic candidate behind moderate threat, existing breakaway, or higher chaos. | Event-created Yakutia loads `soviet_collapse_internal_republic_focus_tree`; terminal fallback also releases or frees it. | source_pass | Eastern survival route coverage is shared. |
| Buryatia | `BYA` | Added to the progressive selector as an internal republic candidate behind moderate threat, existing breakaway, or higher chaos. | Event-created Buryatia loads `soviet_collapse_internal_republic_focus_tree`; terminal fallback also releases or frees it. | source_pass | Eastern survival route coverage is shared. |
| Tuva | `TAN` | Added to the progressive selector as an internal republic candidate behind moderate threat, existing breakaway, or higher chaos. | Event-created Tuva loads `soviet_collapse_internal_republic_focus_tree`; terminal fallback also releases or frees it. | source_pass | Eastern survival route coverage is shared. |
| Other defined Soviet Collapse republic tags | `FER` plus high-chaos successors such as `TNC`, `ALA`, `BAC`, and others | `FER` is an internal progressive candidate; high-chaos successors use their own spawn helpers and pressure flags. | `FER` loads the internal republic tree; high-chaos tags use bespoke setup helpers and focus trees. | source_pass | Broader high-chaos package completeness is still tracked in the high-chaos package audit. |

## Remaining Boundaries

This audit closes the source-level MTTH family coverage blocker. It does not close the full Event 005 goal. Remaining ledger items still include country-package detail rows, faction-goal review, sponsor-by-sponsor AI and decision audits, asset reconciliation, high-chaos package completion, and the final completion report.
