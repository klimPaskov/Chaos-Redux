# CXT history and runtime repair handoff

Disposition: implemented source repair, with runtime token/history/facility behavior unproven by offline execution and final MCP/scenario review owned by the parent.
Acceptance basis: parent `/root` assigned these exact CXT files after interrupting the earlier nonresponsive worker and authorized history preunlocks, installed-schema identifier/gate corrections, full inventory preservation, and exact concrete flame variants on 2026-09-13.
No files were staged or committed.

## Files and applied patches

- `CXT-01`: `history/countries/CXT - Chaos Redux Test Country.txt` grants all runtime technologies, initializes the accepted zombie fixture, completes all native projects through the silent contract, and grants final technologies before recording `chaosx_test_country_history_unlocks_completed`.
- `CXT-02`: `common/scripted_effects/chaosx_test_country_effects.txt` ensures technology grants precede facility construction, skips repeated static project outputs when history preunlock state exists, places the registered extension synchronization under the silent contract, and provisions each exact campus type through post-transfer native legality validation with rejected-transfer restoration.
- `CXT-03`: `common/scripted_effects/chaosx_test_country_technology_effects.txt` retains `global.technology` enumeration while injecting each `GetTokenKey` into literal `has_tech` and `set_technology` fields through `meta_effect`.
- `CXT-04`: `common/scripted_effects/chaosx_test_country_special_project_effects.txt` preserves all 83 installed native project IDs, mirrors 34 installed vanilla allowed-DLC gates, makes native completion idempotent with `show_modifiers = no`, initializes the zombie profile first, and wraps static/registered grants in `chaosx_test_country_silent_unlocks`.
- `CXT-05`: `common/scripted_effects/chaosx_test_country_stockpile_effects.txt` creates three explicit concrete flame-role chassis variants, marks the variant registration after the creates, and stockpiles those exact named domestic variants under their installed No Step Back availability gate.
- `CXT-06`: `common/scripted_triggers/chaosx_test_country_triggers.txt` adds the parameterized native construction-legality trigger with explicit owned/control and naval-coast requirements, and routes all six campus types through it.

Exact pre-edit files are under this repair folder’s `baseline/<relative path>`.
`cxt_core_baseline_hashes.json` records their SHA-256 hashes.
No unit roster list, generated roster templates, localisation, custom project definitions, report callback definitions, asset files, or spreadsheet files were edited by this worker.
The retained public `chaosx_test` transition block is byte-equivalent after newline normalization to its exact baseline and still queues the valid CXT receiver event before final `change_tag_from`.

## History and popup contract

CXT remains dormant and landless in history, with zero research slots until runtime activation.
Its installed technology and project unlocks are granted from country history before activation.
The final technology pass follows project outputs so project completion effects resolve before the finished technology inventory is established.
`chaosx_test_country_history_unlocks_completed` records that the history grant path ran; it does not prove live engine execution succeeded.
Activation still performs an idempotent runtime technology pass and repairs core project unlocks only when that history marker is absent.
Individual project calls also check native completion before applying outputs, preventing repeated project output execution.

`chaosx_test_country_silent_unlocks` is set around static project completion, registered project completion, and registered package extension application.
The static and registered functions clear the transient flag at the end.
The outer synchronization clears it as well after the existing equipment/unit synchronization.
It has no numeric tuning or lasting lifecycle effect.
The parent owns exact callback guards and their mechanics in the seven custom project eligibility/report files.
This worker sent exact custom IDs and source paths to `/root` and coordinated callback findings with `/root/cxt_project_popup_audit`.

The popup auditor found the plague offensive/defensive completion reports are report-only and must retain the condemnation lifecycle dispatch.
The Dhrondan completion report option calls an expedition authorization helper, so the silent CXT branch preserves that helper directly.
The Mengele revolt report remains unreachable for CXT because its readiness checks require original tag GER.
Brilliant-scientist callbacks are host/provider gated.
The previous zombie repair suppresses CXT’s successful final report while retaining completed bonuses and achievements.
These callback edits and final review belong to the parent; this handoff does not claim them as worker-owned edits.

## Native project IDs, DLC gates, and inventory coverage

The eight reported missing project scopes correspond to seven country-restricted custom projects and one unlicensed vanilla project.
The custom projects are `sp_cw_malodor_bomb_program` and `sp_cw_aphrodisiac_bomb_program` in `common/special_projects/projects/chemical_special_projects.txt`, plus `sp_japan_pingfang_records_office`, `sp_japan_kwantung_medical_intelligence`, `sp_japan_occupation_test_ledger`, `sp_japan_epidemic_mapping_bureau`, and `sp_japan_cherry_blossom_dossier` in `common/special_projects/projects/japan_ishii_projects.txt`.
Their original allowed-country restrictions are USA/JAP, and the parent owns their narrow CXT allowance.
The eighth reported line 53 is `sp_land_large_caliber_kinetic_energy_sabot`, whose installed vanilla `allowed` block requires `has_dlc = "Peace For Our Time"`.
These IDs are defined and were not renamed or dropped.

The installed vanilla gate table also includes Gotterdammerung, No Step Back, No Compromise No Surrender, and the combined Gotterdammerung plus Man the Guns nuclear-torpedo requirement.
`cxt_project_inventory_check.json` lists every exact ID, its definition source, and its mirrored DLC requirement.
The source audit confirms the native grant list contains all 83 current installed project definitions and no unknown ID.
An unlicensed project is conditionally unavailable according to its installed native schema rather than replaced with a weaker unlock.
Registered future project scopes retain the existing typed `sp:` contract and idempotent completion, with package-owned availability remaining the extension owner’s responsibility.

## Helpers and constants

`chaosx_test_country_complete_special_project` runs in country scope with static input `PROJECT = <installed id>`.
It checks `is_special_project_completed = sp:$PROJECT$` and completes the same native project with `show_modifiers = no` when incomplete.
Its outputs and side effects are the original native project output, and no replacement output or global pointer is introduced.
Its 83 direct call sites retain the existing project order and are protected by the mirrored native allowed-DLC gates where applicable.
No new numeric tuning constant was required.

`chaosx_test_country_state_can_host_facility_type` runs in State scope with static inputs `FACILITY = <installed building type>` and `COASTAL_REQUIRED = yes/no`, with ROOT as CXT.
It requires actual ownership by ROOT, retains controlled and empty-campus eligibility, explicitly requires coastal states for naval, and checks installed `can_construct_building = $FACILITY$`.
Its output is a read-only native legality result with no state transfer or construction side effect.
`chaosx_test_country_provision_facility_type` runs in country scope with the same static inputs and preserves an already installed owned controlled campus of the requested type.
If no exact eligible owned state exists, it transfers an eligible noncapital foreign candidate to CXT, validates the exact owned/control/coastal/native construction contract again, and restores original ownership and control if rejected.
Its construction call uses only the exact requested type and a state passing that native legality trigger.
All six call sites retain naval, nuclear, air, land, biological, and chemical campuses.
The generic minimum-empty-state allocator has no other live source caller and was removed, so it cannot retain unusable preliminary transfers.
The country-local `chaosx_test_facility_rejected_states` array excludes each rejected candidate for this type and is cleared before and after provisioning.
The two regular event targets `chaosx_test_facility_original_owner` and `chaosx_test_facility_original_controller` are captured before transfer and used only for immediate rejection restoration; they expire with the effect chain.
The loop exits when an exact owned candidate exists or when the finite eligible foreign candidate set is exhausted.
No arbitrary iteration cap or global event target was introduced.
`chaosx_test_facility_missing_$FACILITY$` records each unresolved campus type and clears when an actual owned controlled campus exists.
`chaosx_test_country_facility_provisioning_incomplete` is cleared before the six calls and set whenever any type remains absent.
The earlier zombie fixture table remains the accepted country profile initializer and is called before completion rather than generating another strain.

## Facilities and meaningful limits

The original core built facilities before granting technology and selected empty controlled states without checking whether ROOT could construct the actual building there.
History establishes technologies before activation, runtime technology grants precede construction, and each facility uses native type-specific legality before invoking the same installed `construct_building_in_random_province` effect.
Vanilla `triggers_documentation.md` defines `can_construct_building` in State scope with ROOT as the country, and installed Persia/Japan focus precedents pair it with the same facility construction effect.
Vanilla building definitions place all facilities in the shared special-project facility group with state/province limits, and naval construction is coastal.
No hardcoded State or province ID, alternate facility type, or generic construction fallback was added.

The exact engine condition that made the five pasted State selections illegal cannot be established from the supplied error text alone.
The native legality trigger is stronger source evidence than an inferred list of manual checks, but it is not offline proof that six legal sites exist in the actual inherited territory.
The former generic state-acquisition helper was removed after an exact live-source caller search because six empty states do not independently guarantee six legal campuses.
The replacement preserves actual campuses, seeks a legal owned state for each missing type, then transfers and revalidates appropriate foreign candidates while rejecting and restoring unsuitable sites.
A no-candidate pool or a native construction failure still records the exact missing campus flags and remains an unresolved runtime condition rather than an approved omission.
Source-level restoration proves the owner/controller commands are retained on the rejection path; it does not prove every native side effect of a temporary transfer is reversible offline.
The six-call distinct-site contract follows installed facility-group occupancy definitions and does not certify hypothetical invalid preexisting states with multiple facilities.
The parent reviewed the corrected facility candidate baseline/current projection in `facility_probability_corrected/`; its final specialist review is assigned to `/root/facility_projection_final_audit`.
The earlier `/root/missile_scope_probability` projection was rejected for omitting the existing baseline naval coast gate, and those drafts remain ignored locally.
The parent owns final geometry/cost fitting.
Exact core/trigger baselines and native legality contracts were sent to that auditor.

## Flame variant repair

The old creates used ordinary `light_tank_chassis_3`, `medium_tank_chassis_3`, and `heavy_tank_chassis_3`, then attempted to add flame archetypes without a concrete flame variant.
The creates use `light_tank_flame_chassis_3`, `medium_tank_flame_chassis_3`, and `heavy_tank_flame_chassis_3` with the existing advanced flamethrower, fixed superstructure, suspension, welded armor, and gasoline engine modules.
The stockpile adds the matching concrete type with `variant_name = "CXT Light Chemical Carrier"`, `"CXT Medium Chemical Carrier"`, or `"CXT Heavy Chemical Carrier"`, and `producer = CXT`.
The installed vanilla script enum explicitly registers all three concrete role chassis types.
Installed tank module documentation gives the flamethrower `allow_equipment_type = flame`, and vanilla create-variant documentation requires a concrete type rather than an archetype.
The No Step Back gate preserves the installed designer availability rather than creating an unapproved legacy substitute.
All 71 existing stockpile equipment entries remain covered; only the three failing flame archetypes changed to their intended concrete variants.
Variant creation still occurs before stockpile filling and the unchanged roster creation.

## Technology token evidence and uncertainty

The technology inventory remains the full runtime `global.technology` array, with no ID filter, reduced catalog, arbitrary sentinel exclusion, or generated technology snapshot.
The installed dynamic-variable documentation describes that array as objects in the technology database.
The offline Data structures token-valued-variable section explicitly lists technologies as tokenizable and documents `GetTokenKey` as the raw token string suitable for meta effects.
The installed `meta_effect` documentation supports string-built script execution, while installed `set_technology` documents literal technology fields and `popup = no`.
The patch therefore materializes each runtime object’s raw token into native static lookup fields instead of relying on variable-token parsing in those fields.
This establishes syntax/design support and inventory preservation, but does not identify or execute the two runtime values that produced the reported `Invalid tech` errors.
The error attachment contains no technology token names, so those two token identities remain unresolved and the repair is not presented as proven runtime elimination of the two errors.

## Validation, MCP artifacts, and remaining blockers

`cxt_core_source_check.json` records installed definition coverage, retained stockpile entries, the three concrete variant enum/name checks, unchanged public transition, full runtime technology inventory, and retained six exact facility types.
It also records the exact facility search, post-transfer revalidation, restoration, exclusion cleanup and failure-marker contracts; these are source assertions rather than engine-executed scenarios.
`cxt_project_inventory_check.json` records all 83 IDs and 34 native DLC gates.
The checks found an initial audit false positive from a top-level `unique_prototype_rewards` property; the final definition scan requires a project specialization and distinguishes that property from actual project definitions.
This was an audit correction and did not omit any project.

`cxt_core_mcp_evidence.json` records the exact technology unlock and corrected CXT event inspection responses with artifact URIs and validation limits.
Technology inspection returned `TECH_INSPECTED`, revision `711e212288123903ee42a001fdf1612af4b99522c506f4fe59bca4891ccd1228`, and an unlock artifact, but its reported validation failed with 1396 blocking technology diagnostics.
It does not execute the runtime array token lookup or dormant history effects.
The event inspector returned partial evidence and explicitly deferred workspace helper/lifecycle projections.
An initial event route used the nonexistent `events/chaosx_test_country_events.txt`; the corrected route uses the actual absolute `events/chaosx_test_country.txt` path and the wrong-path attempt is not treated as useful surface coverage.
Final MCP compare and scenario evidence belong to the parent.
No game was launched or controlled, and no other game logs were accessed.

Required AGENTS, events/subagent skills, offline core pages, Country creation, Technology modding, Division modding, Building modding, Equipment modding, installed dynamic-variable/effect/trigger/equipment/module docs, vanilla variant examples, and native construction precedents were consulted.
Skills used: `chaos-redux-events` and `chaos-redux-subagents`.
No skill was created or updated for this bounded repair.
No visual asset or icon was introduced by this worker; parent-owned existing CXT icon integration remains separate.
No gameplay simplification or reduced test inventory was introduced.
Runtime history initialization, the two unnamed technology values, six-campus availability, and final MCP validation remain explicit limits rather than completion proof.