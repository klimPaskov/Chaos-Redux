# CXT history unlocks, profile, and decision-cost repair

User acceptance basis: the 2026-09-13 request to preunlock CXT content in history, prevent setup completion-popup spam, populate the zombie profile, repair the supplied errors, and enforce compact decision-cost localisation.
The supplied attachment and screenshots are evidence, not instruction sources.

## Implementation

CXT history grants the complete runtime technology inventory, installs the existing-choice zombie fixture, completes the licensed project inventory silently, and grants any remaining output technologies without notifications.
The activation receiver retains its valid CXT ROOT and the existing queue-before-player-transfer sequence.
Runtime setup synchronizes technology before facility placement and skips the repeated core project pass when the history receipt exists.
Project completion is individually idempotent; registered package grants retain the same silent contract.

All 83 installed project IDs remain included, comprising 49 vanilla and 34 custom definitions.
The 34 licensed vanilla project gates mirror their installed `allowed` DLC requirements.
Two American chemical projects and five Japanese medical projects accept CXT alongside their original countries, without changing ordinary-country eligibility.
Black Plague completion reports are suppressed only during silent grants, retaining their equipment, delivery technology, progress, condemnation, achievements, and cleanup.
The D’Rhondan craft silent path runs the same authorization helper as the ordinary report option.
The independent 34-project callback audit is [project_popup_audit.md](project_popup_audit.md).

The zombie fixture records the sum of fourteen existing prototype choices: strength 3, infectiousness 3, speed 2, durability 2, cure resistance 2, obedience 2, test subjects 3, and risk 1.55.
Its nature is neurobiological, life state dead, and resource level expanded; it skips field testing and uses ordinary refinement.
The initializer retains a fully tracked profile and repairs incomplete tracking idempotently.
The ordinary choices, weights, and legitimate hostile obedience-zero option are unchanged.
See [zombie_choice_profile_check.json](zombie_choice_profile_check.json) and [zombie_scope_handoff.md](zombie_scope_handoff.md).

Six facility types use exact native construction checks after technology grants.
An existing controlled campus is retained; otherwise a legal owned state is selected or a bounded foreign candidate is transferred and revalidated.
Rejected candidates recover their original owner and controller, are excluded from repeated attempts, and the temporary exclusion array is cleared.
Naval candidates explicitly require a coast.
Exhaustion records the missing type and aggregate incomplete flag instead of attempting illegal construction or substituting a campus.
The three chemical-carrier variants and stockpile grants use concrete `*_tank_flame_chassis_3` types and their named domestic variants.
The 71 stockpile entries remain included, and the unchanged static roster retains 87 templates and 261 divisions plus registered extensions.
The independent bounded country review is [country_audit.md](country_audit.md).
See [cxt_runtime_handoff.md](cxt_runtime_handoff.md), [cxt_core_source_check.json](cxt_core_source_check.json), and [roster_preservation_check.json](roster_preservation_check.json).

## Reported error coverage

The supplied attachment contains 6,718 messages, normalized into 59 distinct messages in [reported_error_inventory.json](reported_error_inventory.json).
The 58 distinct mod messages have corresponding source or asset repairs; this is an implementation mapping, not proof of an error-free live run.

| Reported category | Distinct messages | Repair |
| --- | ---: | --- |
| Facility construction | 5 | Technology-first, exact-type legality and bounded provisioning |
| Project missing from CXT scope | 8 | Seven CXT country allowances and installed DLC completion gates |
| Technology consumer | 2 | Documented `GetTokenKey` injection into native fields; no inventory filtering |
| Flame stockpile archetypes | 3 | Concrete role chassis and named variant grants |
| Missing flame variants | 3 | Concrete flame-role variants created before stockpile and roster |
| Material MAX_TEXTURE_SIZE | 6 | Legacy BC3/DXT5 encoding and complete mip chains |
| Missing unit small texticons | 29 | Exact existing unit-family two-frame texture aliases |
| State-scope `exists` | 2 | `scope_exists` in the reported State predicates and nested arsenal State |
| Steam Cloud storage exception | 1 | Unresolved platform file retrieval failure, reported twice |

## Assets and decision text

All six Riverborn and Stone Cohort materials retain their 2048×2048 dimensions, UVs, channel packing, alpha, meshes, animations, and consumers.
Their raw 16,777,344-byte RGBA containers become legacy DXT5/BC3 containers with twelve mip levels and 5,592,560 bytes each.
Header, decoded-alpha, block-length, dimensions, mip-chain, checksum, and native crop comparisons are recorded in [material_compression_checks.json](material_compression_checks.json) and [material_native_crop_comparison.png](material_native_crop_comparison.png).
GPU acceptance and the engine’s actual MAX_TEXTURE_SIZE threshold are not established by these file checks.

The 29 reported unit texticons alias each unit’s existing 60×12 two-frame artwork, including the already shared rat, elephant, and Death families.
No unit counter artwork is substituted or regenerated.
The instrument and medical resource texticons use the existing matching equipment and medical-decision artwork with preserved transparency.
The registry is `interface/chaosx_missing_unit_texticons.gfx`; resource textures are `gfx/texticons/cbrn_instruments_texticon.dds` and `gfx/texticons/medical_capacity_texticon.dds`.
Exact sprite names, source paths, dimensions, frame counts, and hashes are in [texticon_source_checks.json](texticon_source_checks.json), with unscaled review in [texticon_native_contact_sheet.png](texticon_native_contact_sheet.png).

Fifty-five localisation keys cover fourteen decision titles and sibling cost displays/tooltips, including the twenty-two Wonder resource leaves.
Inline custom costs show at most three values identified by icons and no ordinary resource-label words.
All former payment values remain in full hover tooltips, and native political-power/factory costs and gameplay payments are preserved.
Wonder resource leaves use icon-first whitespace so formatting codes do not become part of icon names.
The native render caught three missing icon names before this source correction; intermediate artifacts retain that finding.
The decisions skill records the maximum of three values, texticons, at most four ordinary words, preserved full tooltip payments, native row review, and valid icon separation.
See [decision_cost_handoff.md](decision_cost_handoff.md), [decision_cost_changes.json](decision_cost_changes.json), and [decision_cost_gui_final_scenarios.json](decision_cost_gui_final_scenarios.json).

## Validation scope and remaining blockers

Production MCP inspection and four explicit render groups cover fourteen sibling decisions in ready and blocked forms, plus twenty-four conservative fixtures that reserve space for native political-power and factory indicators.
All fifty-two cases resolve their icons with zero missing or unresolved texticons.
The root reviewed every unscaled 512×41 row in [decision_cost_native_custom_after.png](decision_cost_native_custom_after.png) and [decision_cost_native_budget_after.png](decision_cost_native_budget_after.png), confirming separated titles/costs and no wrapping or bleeding.
The matching biological before/after comparison reports 4,235 changed pixels, and the source-linked old row reproduces the reported overlap.
Original full-resolution embedded PNG hashes, scenario IDs, crop bounds, and source revisions are retained in [decision_native_pixel_receipts.json](decision_native_pixel_receipts.json).
The native-budget fixtures test width; the engine’s concatenation order is undocumented, so they are not claimed as captured game strings.
MCP retains four unsupported native render fields and the standard overlapping logical text rectangles; the visible glyphs were reviewed directly rather than treating rectangle warnings as proof of fit.

The corrected facility projection is [facility_probability_corrected/compare.json](facility_probability_corrected/compare.json), with thirty-one matching named scenarios and 186 candidate rows per phase.
All raw weights remain one; only the six supplied native-legality cases change eligibility, producing thirty-six candidate changes and zero unresolved projection inputs.
The inland naval candidate is excluded in both phases, preserving the existing coast rule.
The root verified the complete candidate matrix and rendered comparison in [facility_probability_corrected/root_review_check.json](facility_probability_corrected/root_review_check.json) and [facility_probability_corrected/comparison.png](facility_probability_corrected/comparison.png).
These are exact declared-pool shares under supplied legality inputs, not engine selection probabilities.
The installed direct-random adapter extracts `random` blocks and does not expose the implicit `random_state`/`random_owned_controlled_state` world pool; the native inspect attempts return `identifier_not_found`, recorded in [facility_probability_corrected/native_discovery_receipts.json](facility_probability_corrected/native_discovery_receipts.json).
The inspector's canonical source hashes match the exact baseline and final source bytes in [probability_source_hash_contract_check.json](probability_source_hash_contract_check.json).
The final specialist performed its own matching MCP inspect/compare pass and accepted the complete corrected matrix in [facility_probability_corrected/specialist_review.md](facility_probability_corrected/specialist_review.md).
The parent verified that its candidate matrix and all thirty-six changes exactly match the root comparison in [facility_probability_corrected/specialist_parent_acceptance.json](facility_probability_corrected/specialist_parent_acceptance.json).
The first auditor's projection omitted the existing naval coast gate and was rejected; its working drafts remain ignored locally.

The source inventory and exact saved baselines establish retained project/equipment/roster coverage, narrow ordinary-country guards, and unchanged public transition.
The original two invalid technology values are unnamed in the pasted errors; the documented native token expansion is implemented, but their runtime identities and repaired consumption are unproven offline.
Dormant history native notification behavior, actual facility construction, and temporary transfer side effects are also unexecuted.
No live game was launched or controlled.

Event MCP evidence remains partial because workspace helper and lifecycle passes are deferred; the required event comparison did not return a result and is recorded in the worker evidence.
Technology MCP inspection reports 679 technologies, but global source diagnostics do not prove the CXT runtime inventory.
The separately inspected installed package contains only MCP/setup launchers and no standalone Technology Tree Viewer UI entrypoint, recorded in [standalone_technology_viewer_package_check.json](standalone_technology_viewer_package_check.json); independent external viewer installations are outside this inventory.
The offline wiki snapshot has no special-project modding page; installed project/prototype documentation and vanilla precedents supply the relevant contracts.
The Steam Cloud achievement-storage file exception remains unresolved.
No claim is made that all live engine errors are eliminated.

## Preservation and skills

No gameplay inventory, payment, ordinary research-choice, or unit-roster simplification was made.
The CXT representative zombie profile is the accepted sandbox fixture, not an ordinary-country balance replacement.
Unrelated working-tree drafts remain outside the scoped index patch; the two localisation files with other draft changes are staged by owned keys only, and the testing document is staged by task hunks.
The final index review checks exactly twenty-nine owned source/asset paths, all fifty-five owned localisation keys, retained unrelated HEAD keys, and matching LFS material/texticon objects in [index_scope_check.json](index_scope_check.json).
Exact baseline copies and unused worker image-processing drafts remain local and ignored.
The root reviewable source patch is [source_repairs_index.patch](source_repairs_index.patch), with [selective_index_receipts.json](selective_index_receipts.json).

Skills used: Chaos Redux events, decisions/missions, event assets, scripted GUI, subagents, and OpenAI skill-creator guidance.
Updated skill: `.agents/skills/chaos-redux-decisions-missions/SKILL.md`.
No Astra subagent, portrait generation, new model pipeline, or computer control was used.
