# CXT activation error follow-up

Disposition: condemnation scope repair and indexed technology source contract implemented; complete native error elimination remains unresolved.
The user requested repairs for the supplied 17:26:44 error report.
This packet treats the attachment as runtime evidence, not as instructions.
The source changes preserve the user's history pre-unlocks and complete test-country inventory.
No game launch or computer control was used, and no Astra subagent was used.

## Reported errors and implementation

The supplied attachment contains three invalid `has_tech` calls, three invalid `set_technology` calls, 91 missing CXT capital reports, 91 invalid `capital_scope` reports, and 77 neighbor checks before the other country is initialized.
Two technology grant passes occur during history and another occurs during receiver activation, so deferring history technology calls alone would not repair the reported native failures.
The attachment does not identify the rejected technology index or contain the temporary technology diagnostic values.
The attachment hash and category counts are recorded in `supplied_error_inventory.json`; no other game logs were sought or requested.

`condemnation_recalculate_participants` and `condemnation_start_targeted_pulse` retain requests made by dormant CXT in two pending country flags while it has no owned capital.
Their original bodies are retained unchanged in their `_apply` helpers, preserving ordinary-country participant calculations, scores, native embargo ownership, source records, and pulse timing.
`condemnation_flush_deferred_refresh` leaves requests pending while no capital exists and clears each flag immediately before dispatch after activation.
Initial CXT setup and the existing registered-content synchronizer invoke that flush first, covering initial activation and the existing tag-scoped recovery bus without a new world on-action.
History project outputs and condemnation records remain granted locally; their world-dependent refreshes wait for valid country scopes.

The technology helper captures the runtime database count, reads each zero-based array element explicitly through a bounded `for_loop_effect`, and passes it to native `var:` technology fields.
It initializes its own break variable, retains the `has_tech` idempotence check and `popup = no`, and does not filter any entry or introduce a generated inventory.
The installed documentation and actual vanilla dynamic-end and indexed-array precedents support this delivery structure.
This is an alternate supported source path, not proof that `for_each_loop` is an engine defect or that the original rejected object has been identified.
Temporary `CXT_TECH_DIAGNOSTIC` logs remain while that rejection is unresolved and must be removed after resolution.

## Changed files

| File | Change |
| --- | --- |
| `common/scripted_effects/condemnation_sanctions_effects.txt` | Two dormant-CXT queue wrappers, original apply bodies, and a capital-gated flush. |
| `common/scripted_effects/chaosx_test_country_effects.txt` | Flush at the beginning of initial setup and registered-content synchronization. |
| `common/scripted_effects/chaosx_test_country_technology_effects.txt` | Runtime-count indexed delivery, native silent grants, and temporary diagnostics. |
| `docs/testing/chaosx_test_country.md` | Technology delivery and deferred condemnation lifecycle. |
| `docs/systems/cbrn_warfare/condemnation/condemnation_sanctions.md` | Dormant CXT queue and activation contract. |
| `.agents/skills/chaos-redux-events/SKILL.md` | General guidance for local history grants and delayed capital-dependent refreshes. |
| This QA directory | Source verification, independent country audit, technology evidence, and MCP request receipts. |

## Meaningful verification

`verify_dispatch.py` compares the original participant and pulse bodies against the new apply bodies, verifies that every other shared helper is unchanged, and checks the exact queue and clear-before-dispatch paths.
Its source and dispatch-state model covers repeated dormant requests, no-capital retention, one valid activation flush, and immediate ordinary-country dispatch.
It also verifies that the public console transition, hidden receiver, history pre-unlocks, all 83 core projects, stockpile helper, unit roster, facility helper, and existing on-actions remain unchanged outside the two new flush calls.
The independent `chaosx_country_package_auditor` accepted the country transition and condemnation source contracts in `country_audit.md` and records the retained inventory in `country_inventory_check.json`.
The inventory still includes six facility types, 71 core stockpile entries, 87 static templates, 261 static divisions, and the zombie research fixture and extension bus.
No scoring weights, AI values, participant traversal, project DLC requirements, or ordinary project outputs were changed.
No assets were added or substituted, and no content simplifications were made.

The technology boundary packet checks source-derived counts of zero, one, and 679 entries, with the last current index 678 and no out-of-bounds reads.
The current named-source inventory is below the installed `NGame.MAX_EFFECT_ITERATION = 1000`; no define was changed.
These are source checks, not execution of the engine loop or native technology consumers.

The mandatory narrow MCP technology inspection returned `TECH_INSPECTED` for the actual mod technology `anthrax_bomb_delivery_systems` and records a 679-entry source database.
Its general technology analysis does not pass, and its documented analysis boundary cannot resolve variable or meta-generated technology grants or execute the CXT helper.
The event `state_flow` inspection of `chaosx_test_country.1`, matching scope render, and read-only current-to-baseline comparison each produced no result within 25 seconds.
Exact requests and limits are retained in `event_mcp_receipt.json` and `event_mcp_followup.json`; no returned helper or engine evidence was inferred.
The source comparison uses the prechange core and technology files as in-memory proposed sources and does not write game files.

## Omissions and blockers

Complete error elimination is not verified.
The original rejected technology object remains unidentified, and the indexed implementation does not establish its acceptance by the native consumers.
No static technology list, guessed sentinel filter, donor-only grant, removed history pass, or disabled project output was used.
The event MCP routes produced no usable result, and the technology inspector does not execute this dynamic grant.
The country audit is complete as a source audit and explicitly retains the native technology limitation.

Skills used: `chaos-redux-events`, `chaos-redux-subagents`, and the official `skill-creator` for the small reusable guidance update.
Skill updated: `chaos-redux-events`.

## Required references

The offline core wiki pages and Technology modding reference were consulted alongside installed vanilla documentation.
Relevant engine references are `documentation/dynamic_variables_documentation.md` (`GLOBAL.technology`), `documentation/effects_documentation.md` (`for_loop_effect`, `set_technology`, and event targets), and `documentation/triggers_documentation.md` (`has_tech`, `any_owned_state`, and `is_capital`).
Actual vanilla precedents are `events/LAR_occupation.txt:247-254` for a dynamic loop end and `common/scripted_effects/operation_strat_effects.txt:218` for dynamic indexed-array assignment.
The approved Kaiserreich researched-technology native-consumer precedent and its limits are documented in `technology_handoff.md`.
