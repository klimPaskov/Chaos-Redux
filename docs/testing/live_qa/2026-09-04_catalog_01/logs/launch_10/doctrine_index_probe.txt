# Temporary launch-10 parser probe, unreferenced and never executed.
# Nonzero mastery avoids the separate amount=0 rejection in launch 09.
# Parser acceptance alone does not establish exact-track runtime behavior.
# Parent removes this exact file after archiving launch 10.
codex_qa_doctrine_track_index_parser_probe = {
	add_mastery = {
		amount = 1
		folder = land
		sub_doctrine = mobile_infantry
		track = infantry
		track_index = 0
	}
}
