# Decision trigger alias pre-patch inventory

This directory is the requested pre-patch archive for the bounded decision trigger alias tranche.

The fresh log and the eligible decision-source scan found no exact requested invalid alias in `common/decisions` after excluding Event 029. Event 032 decision and mission files were scanned; only the separate Event 032 operations-effects sources remain parent-owned. No decision source bytes required a patch, so there are no source files to copy into this archive.

The exact native error counts, source-scan evidence, exclusions, and validation are recorded in [`decision_trigger_aliases10_handoff.md`](../decision_trigger_aliases10_handoff.md).
