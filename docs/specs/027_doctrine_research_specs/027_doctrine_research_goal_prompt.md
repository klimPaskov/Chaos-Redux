Implement the full rework of Chaos Redux Event 027, Doctrine Research, using every file in `docs/specs/027_doctrine_research_specs/` as acceptance criteria. Read `AGENTS.md` and all relevant project skills. Inspect the live repository, offline Paradox wiki, vanilla documentation and doctrine definitions, Chaos Warfare, event logs, cluster registry, achievements, assets, AI, and catalog workbook before editing.

Replace the stale one-major-country Fire-Once concept with a global Minor Repeatable event. One firing snapshots every live country with at least one valid doctrine action and creates a country-owned batch. Baseline grants one choice. Evolutions I through IV grant two, three, four, and five choices. Batch size and stage are fixed at creation. Later firings append separate queued batches without overwriting or merging the active batch.

Each choice selects one valid doctrine domain. When the domain has no active Grand Doctrine, the country can adopt one eligible Grand Doctrine for free. This consumes the choice and grants no Event 027 mastery step. When the domain has an active Grand Doctrine, the country selects one eligible track and advances exactly one mastery level in its selected subdoctrine. An empty track may select one eligible subdoctrine and receive the first event mastery step in the same choice only after local engine evidence proves the transaction safe. Preserve banked mastery, revalidate at confirmation, and consume a choice only after success. Never replace an active doctrine, grant generic military experience, use a mastery-point dump that can cross several levels, or substitute technology helpers.

Build a fail-closed adapter registry for Army, Navy, Air, verified Special Forces content, Chaos Warfare, and future custom domains. Chaos Warfare must use its owning establishment gates, four track identities, mastery transitions, downstream requirements, and AI state. A broken custom adapter must not remove valid ordinary domains.

Human countries use the full chained flow in the specs, including every opening, selection, confirmation, navigation, continuation, summary, and no-option page. AI countries use the same valid pool, resolve silently, and recalculate after every choice. Score choices from forces, production, war, geography, theater, AI strategy, completion value, and owner-system readiness. Run baseline and final probability audits with every named scenario in the package.

Preserve batches through save, reload, tag changes, subject changes, and controller changes. Use one-time receipts to prevent duplicate effects. Annexation removes unused batches without transfer. Countries created after a firing receive no retroactive batch. Use one bounded global fanout at firing and no recurring whole-world on-action.

Wire Event 027 across event registration, enable state, one no-actor History row, Event Details, evolution records, debug names, localization, National Breakthroughs, assets, three achievements, docs, and the XLSX. Cluster firing can call the fanout once and still counts as one pacing event. Produce the accepted report image and achievement icon triplets, reuse verified doctrine-owned icons, and treat missing final assets as blockers.

Mandatory evidence includes event comparison, doctrine and technology inspect, render, and compare for every domain, probability comparison, localization audit, completion audit, and spreadsheet export from the XLSX. Spawn project subagents with `fork_context=false` and write handoffs under `docs/plans/027_doctrine_research_plans/subagent_handoffs/`. Report every blocker or deviation. Do not claim completion with Army-only coverage, unresolved banked mastery, unsafe queues, weak AI, missing achievement tracking, placeholder assets, stale Event Details, or stale catalog data. Commit only the complete Event 027 change set.
