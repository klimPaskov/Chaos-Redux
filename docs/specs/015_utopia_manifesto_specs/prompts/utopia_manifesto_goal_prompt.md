Implement Event 015 `utopia_manifesto` to the fullest extent from `docs/specs/015_utopia_manifesto_specs/`. Use `prompts/utopia_manifesto_coding_prompt.md` as the main handoff. Also use the asset, super-event, achievement, decision, and subagent routing prompts in `prompts/`.

Register Event 15 as Minor Fire-Once with no cluster, target only eligible minors and player minors that are not majors or strong industrial powers, and load a comprehensive Utopian focus tree on acceptance.

The tree must be deep. Build a large tree with opening, public doctrine routes, economy, work, housing, education, military, diplomacy, expansion, Need Ledger, puppet utopia, hidden Outopia, late enforcement, and Ultimate Utopia convergence. All major branches must eventually connect into route-colored ultimate outcomes.

Implement late decisions and goals to export, enforce, or renounce Utopia abroad. Implement utopian subject forms such as Charter Commonwealths, Surveyor Protectorates, Necessary Wards, Daughter Commonwealths, and hidden No-Place Precincts. These subjects must have visible mechanics, values, decisions, failure states, and final fates.

Preserve Land Need as the claim and demand gate. Implement Common Stores, Vocational Rolls, Outopia Fracture, wardship, integration, claim decay, renunciation, AI behavior, evolutions, achievements, route identities, assets, docs, event log details, and spreadsheet alignment.

Use concrete costs and missions. Do not rely mostly on political power or command power. Do not use ordinary permanent claim spam, instant core spam, placeholder art, default super-event audio, unresearched quotes, or working labels as final localisation.

Follow AGENTS.md and the relevant skills. Use `fork_context=false` for project custom subagents and pass explicit event paths and constraints to each one. Follow `prompts/utopia_manifesto_subagent_routing_prompt.md` for routing.

Near-completion rule: spawning `chaosx_improvement_loop_planner` is mandatory. Spawn it with `fork_context=false` after a meaningful implementation tranche and before the final completion audit.

Keep iterating until the implemented files satisfy the spec. Report every simplification, blocker, skipped route, missing asset, missing AI, missing localisation, unresolved loop item, or queued plan before claiming completion.
