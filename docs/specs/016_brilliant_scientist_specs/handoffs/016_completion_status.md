# Event 016 planning completion status

## Reconciled status, 2026-08-02

The default-enabled Event 016 core runtime is implemented. The opening selects one valid host, creates one fixed Doctor Warren Kruger identity, and prevents duplicate appointment, transfer, and reward paths. Public appointment grants the promised `+100%` research speed, AI accepts, and the player can appoint publicly, appoint secretly, or refer Kruger to another valid country.

The decision-led Directorate, fifteen project families, four logged evolutions, foreign contest, containment board, dynamic Kruger State formation, 100-focus country tree, project-derived forces, six super-events, seventeen achievements, aftermath system, event log, details window, assets, sound, localisation, and catalog records are statically wired. The host focus tree is never replaced. The sovereignty deadline now derives from Mandate, the dangerous-project portfolio, war, good-faith cooperation, and recent dangerous incidents. The bounded `.4`, `.5`, and `.6` report slice now includes the `.7` primary-facility reaction, `.8` second-Prototype custody reaction, and `.9` named foreign-operation response, each with a host and character-level receipt guard so transfers cannot replay a reaction. The existing `.10` loyalty and `.11` relocation dossiers now carry the same host-archetype presentation clause, the first four Kruger State foundation reports, existing clone, machine, xenobiological, temporal, and Singularity route dossiers, and foreign-integration reports `.60` through `.64` carry carried-portfolio presentation, and the `.307` formation headline now identifies the retained sovereignty route. The four initial institutional-form decisions and six existing facility decisions also weight their AI choice with the assigned host archetype while leaving human choice and effects unchanged. The fixed Kruger token is now recruited in the tracked dormant KRG history, the existing KRG country-definition mapping remains pointed at `common/countries/Kruger State KRG.txt`, and opening nationality transfer no longer names the unrelated DJX reservation.

The current implementation map and acceptance boundary are recorded in `docs/plans/016_brilliant_scientist_plans/016_core_runtime_handoff_map.md`. The implementation is not a whole-content completion claim. A bounded host-context and first-prototype flavour slice is implemented, with transfer and formation persistence for context flags, pending context events, queued family reports, governance receipts, character-level reaction receipts, and attenuated repeated report deltas. The current host is assigned one mutually exclusive country-shaped archetype at appointment or transfer. That state drives dynamic clauses across the existing `.4` through `.18` reports and supplies the associated AI preferences without creating a new fire path or meter. The base and evolved opening and referral reports also append a governing-ideology clause for democratic, communist, fascist, non-aligned, or safe-default hosts, and now append the authored country clause for ten named tags with a safe general branch. The `.4` through `.9` descriptions additionally append authored tag clauses for Germany (`GER`), Britain (`ENG`), France (`FRA`), the Soviet Union (`SOV`), the United States (`USA`), Japan (`JAP`), Italy (`ITA`), China (`CHI`), Poland (`POL`), and Czechoslovakia (`CZE`), with a general clause for all other countries; the `.10` loyalty and `.11` relocation dossiers, the `.12` through `.18` early-context reports, the four logged evolution reports `.21` through `.24`, and the sovereignty deadline and settlement reports `.30` through `.32` now carry the same tag-specific sentence alongside their existing presentation clauses. The `.4` through `.9` group remains the bounded causal host-context slice. The `.10` through `.18` clauses are presentation continuations, and the first four KRG foundation, route, and foreign-integration dossiers now append the retained formation-origin and carried-portfolio clauses. Unresolved `.7` and `.8` ownership moves to an ordinary-transfer recipient; resolved facility and custody consequences remain with the old host and physical state. The fixed-tag sovereignty transaction now seeds the verified capital, uses the documented `release = KRG` instantiation effect, and then executes the existing route-specific transfer. Seven one-time milestone news headlines (`chaosx.nr16.304` through `.310`) now have registered images, localisation, and guarded dispatch for public appointment, international recognition, breakthrough, formation, project-army deployment, global containment, and regional defeat. The recognition headline now identifies the first recorded public cause of international visibility through joint-laboratory, public-method or exposed-research, foreign-protection, failed-assassination, or neutral clauses without changing the owning recognition transaction. Project-family breakthrough, incident, and remnant report cards are now routed through the reviewed presentation selectors, with eight breakthrough cards and eight incident/security cards covering every project family before the existing recovery instructions. Broader country-specific event chains, quantitative route-balance evidence, seven bespoke 3D entity packages, and user-owned live acceptance scenarios remain blocked.

The KRG project-force package has a real biological history and native-delivery bridge, but it does not yet own a bounded stockpile quantity, reservation receipt, or production and consumption lifecycle. The design contract for that missing surface is queued in `docs/plans/016_brilliant_scientist_plans/016_krg_biological_stockpile_delivery_addendum.md`; it requires a separate reviewed callback at the shared CBRN reservation and outcome boundary and does not authorize a free payload or a model dependency.

The parent terminology reconciliation recorded in the 2026-08-02 completion-audit round-2 follow-up is now reflected in the accepted source-spec parts: temporal mechanics use synchronization capacity and temporal debt, takeover requires an independently proven institutional-capture contract, and the former broad Bestiary wording is separated into paleogenetic and xenobiological synthesis language. The round-2 stale-register bullets remain historical evidence of the pre-reconciliation review; they are not current blockers. The ten named country clauses above include `CZE` (Czechoslovakia); broader country-specific chains beyond these finite presentation clauses remain queued.

The accepted 2026-08-02 country-specific institutional settlement addendum is now promoted into the four named source-spec parts, the event-chain and AI matrices, the acceptance criteria, the balance review, and the Event 016 overview and Directorate documents. Its bounded source implementation is complete in the working tree: static inspection confirms the ten `.5` options, settlement resolvers, host receipts, scripted-localisation selectors, constants, AI factors, and localisation keys in the named runtime files. Targeted transfer, cleanup, balance, and live-acceptance evidence remain pending. The finite layer covers `ENG`, `USA`, `SOV`, `JAP`, `GER`, `FRA`, `ITA`, `CHI`, `POL`, and `CZE` inside the existing `.5` -> `.7`/`.8` sequence; unnamed countries retain the generic pool, and the no-model boundary remains active.

The mandatory improvement-loop planner ran before the core-runtime acceptance pass. Its R1 through R7 recommendations were promoted, rejected, or queued in the accepted source-of-truth map. The second persistence addendum promoted V2-R1 through V2-R5, kept broader flavour and bespoke art queued, and rejected scope expansion into new routes, countries, GUI systems, animations, or 3D packages. It must not be rerun until a genuinely new design gap exists.

The 2026-08-02 continuation adds durable cross-event receipts and reachability guards, serializes cross-domain review with containment, and extends bounded opening, incident-risk, incident-family, rejection-consequence, settlement-outcome, foreign-result, news, cross-event contact, opening-country, loyalty-relocation country, evolution-country, and containment-country presentation. The latest bounded presentation handoffs are `docs/plans/016_brilliant_scientist_plans/subagent_handoffs/016_incident_family_flavor_tranche_2026-08-02.md`, `016_rejection_consequence_host_flavor_2026-08-02.md`, `016_settlement_outcome_host_flavor_2026-08-02.md`, `016_foreign_result_host_context_content_2026-08-02.md`, `016_news_host_context_content_2026-08-02.md`, `016_news_project_army_family_content_2026-08-02.md`, `016_news_breakthrough_family_content_2026-08-02.md`, `016_news_containment_basis_content_2026-08-02.md`, `016_news_formation_origin_content_2026-08-02.md`, `016_news_recognition_basis_content_2026-08-02.md`, `016_report_art_expansion_assets_2026-08-02.md`, `016_opening_country_flavor_content_2026-08-02.md`, `016_loyalty_relocation_country_flavor_2026-08-02.md`, `016_evolution_country_flavor_2026-08-02.md`, and `016_containment_country_flavor_2026-08-02.md`, alongside the earlier family-risk and cross-event contact records. These changes remain bounded and do not alter the whole-event boundary.

The same continuation also adds a loader-compatibility tranche in `016_static_field_constant_compatibility_extension_2026-08-02.md`. Sixteen decision files, two scripted-effect files, and three scripted-trigger files replace 451 static-field `constant:` uses with 162 file-scoped macros whose values were checked against the shared constant registry. The follow-up `016_scientist_skill_constant_loader_followup_2026-08-02.md` applies one additional value-equivalent macro to the six static scientist specialization fields. The current project-force equipment gates are documented in `016_project_force_equipment_loader_and_causality_2026-08-02.md`; they preserve family-specific history, facilities, operational state, and failure-state exclusions. Supported dynamic constant fields, gameplay logic, assets, and model contracts remain unchanged.

The 2026-08-03 high-speed materials continuation is implemented as a bounded Advanced Materials plus Rocketry consumer. A paid 180-day state-targeted test corridor requires both project receipts, expanded prototype works, two valid facilities, concrete equipment, fuel, manpower, Air Experience, political authority, and civilian capacity before opening `chaosx.nr16.195`. The national qualification-board result remains with the former host, while Kruger's proprietary flight envelope follows the fixed character through ordinary transfer and fixed-tag formation without replaying costs or state selection. The decision, delayed event, option localisation, dynamic modifiers, transfer and terminal cleanup, read-only Event Inspector evidence, and parent validation handoff are aligned; quantitative balance, transfer/formation scenarios, and live acceptance remain open.

A read-only probability continuation now inspects the four evolution event-option pools and the foreign-operation decision pool, then evaluates four named host states. The analyzer confirms source parsing while explicitly retaining incomplete candidate pools and unresolved state vocabulary; the linked ranking and matrix artifacts are recorded in `docs/plans/016_brilliant_scientist_plans/subagent_handoffs/016_probability_evolution_balance_evidence_2026-08-02.md`. This is evidence for future balance work, not a normalized probability or live campaign claim.

The former-host recovery tranche now supplies the missing Phase H board. `common/decisions/016_brilliant_scientist_former_host_recovery_decisions.txt` exposes exactly four one-time actions after departure: independent research reconstruction, one selected abandoned archive security operation, assistant amnesty, and international inspection. Concrete equipment, manpower, factory, political, state-control, supply-node, and foreign-access gates are paired with distinct success/failure receipts and AI weights. Three successful receipts are required for the lifecycle to suppress Scientific Vacuum; four failed or completed receipts close the board without repeating rewards. The existing reconstruction category seal is reused and documented; no model or new asset package is introduced.

The KRG route-command continuation adds four fixed institutional offices (`KRG_general_staff_office`, `KRG_machine_command_node`, `KRG_clone_officer_corps`, and `KRG_project_command_council`) with route-gated advisors, corps-command candidates, and later leader roles. A hidden recruitment event and a centralized command-idea reset preserve one active office per route and prevent duplicate recruitment; all four offices reuse the vanilla generic scientist portrait shelf, so no model or new portrait asset is required.

The university and institutional-conflict continuation now exposes the complete Phase F choice surface: a mutually exclusive national chair, rotating grants, or Kruger-selected appointment method, followed by mutually exclusive protection of dissenting scientists or dismissal of the ethics chair. Each action has centralized political-power, equipment, manpower, time, and production-burden values, host-only persistent modifiers, AI weights, hidden-state deltas, and a one-time method lock. The containment score reads the receipts so public governance and personal appointment authority change later sovereignty outcomes. The same policy history is carried through verified Kruger State formation without activating host-only modifiers outside the current host.

The former-host recovery board now requires free civilian project factories in addition to its existing equipment and manpower gates, matching each action's reserved factory burden. The KRG takeover and post-audit AI plans no longer use the start-only `original_tag = KRG` gate, so a host-tag takeover can reach its consolidation AI while retaining the sovereign-country enable and abort checks. The KRG synthesis plan also queues `KRG_define_the_states_purpose` before its dependent synthesis focus. These are loader-safe AI and resource-gate fixes; no models or new assets were introduced.

The foreign-content continuation adds the evidence-gated public challenge specified in the world-reactions and decision matrices. A foreign actor with the public-challenge threshold and authenticated evidence can challenge each host once; the host receives six distinct answers in `chaosx.nr16.190`, and the actor receives the persistent result report `chaosx.nr16.191`. The operation records normal foreign history and public detection, changes only existing Directorate and diplomatic state, and never transfers Kruger, grants a project stage, adds an evolution, or introduces a model reference. The public-challenge host response `.190` and actor report `.191` now use the existing sovereignty-confrontation and Directorate dossier cards, while the counter-Kruger host response `.193` and actor report `.194` use the existing machine-security incident and Directorate dossier cards instead of the opening appointment image; both changes remain presentation-only and keep their one-use contracts unchanged.

## Historical planning snapshot, 2026-07-14

The mandatory improvement-loop addendum was produced and received a complete parent disposition. R2, R3, R4, R5, and R7 are promoted. R1 and R6 are rejected. The reconciled source of truth preserves exactly seventeen achievements and all six mapped super-event packages.

Documentation planning was process-closed for implementation entry. The statements in this historical section describe the pre-implementation snapshot and are superseded by the 2026-08-01 status above.

Presentation work has advanced in two bounded tranches. Six-package text and audio research is complete, and the six final Event 016-owned WAVs use visible IDs `90` through `95`. The exact stage-0 leader and advisor DDS files are complete and registered in `interface/016_brilliant_scientist.gfx`. All super-event images, later portrait source art and runtime files, severe animation packages, achievement icons, flags, project art, UI art, final descriptions, and shared presentation wiring remain incomplete.

The exact continuation state is in `docs/plans/016_brilliant_scientist_plans/016_brilliant_scientist_resume_packet.md`. The next implementation action is the bounded scripted-system architecture pass, followed by Tranche 1 host selection and the persistent Kruger lifecycle.

## Completed in this package

- Every one of the 30 supplied project source files was read in full before drafting. The exact line counts, byte counts, and SHA-256 hashes are recorded in `research/016_source_reading_ledger.md`.
- The complete Event 16, cluster, and triggerable-scenario CSV catalogs were read directly.
- The current repository Event 16 script, localisation, idea, event registration, scientist examples, special-project surfaces, and a mature planning-package precedent were inspected.
- Historical research was completed for wartime scientific mobilization, university and industrial coordination, secret laboratories, scientist migration, foreign recruitment, and scientific dissent.
- Ten source-spec parts were written, covering the complete opening, Directorate, projects, evolutions, foreign reactions, Kruger State, focus tree, world conquest, terminal branches, assets, localisation direction, achievements, and completion rules.
- Eight implementation-support matrices were written for the event chain, project families, decisions and missions, country package, focus architecture, AI, assets, and route coverage.
- Research, balance, exploit, acceptance, source-reading, repo-inspection, and anti-bloat documents were written.
- Bounded prompts were written for assets, animation, super-events, achievements, decisions, country and focus work, scripted systems, localisation, improvement planning, documentation, spreadsheet alignment, completion auditing, main coding, and the compact goal.
- The compact goal prompt was originally delivered inside the requested range and was later reconciled with the binding parent dispositions. Its current character count is not a frozen package invariant.
- The mandatory continuation prompt and ordered subagent routing plan are included.

## Design status

The source design is complete at the parent-planning level. It contains no intentionally shortened route, omitted project family, placeholder country package, fallback focus tree, or truncated evolution. The design deliberately limits universal science-system scope, host focus replacement, unnecessary clusters, Evolution V, and duplicate scientist events because those additions would weaken the event rather than deepen it.

The package specifies exactly seventeen working achievements. The parent rejected the proposed merge because `public_method` and `clean_break` test distinct outcomes.

Six-package super-event title, button, quotation, and audio research is complete and sourced. The Event 016-owned WAVs are complete. Final descriptions, images, localisation, triggers, shared sound definitions, settings-aware playback, and live presentation wiring remain incomplete.

## Current blocked work

- Seven reusable 3D entity packages remain unproduced: portal raider, clone infantry, autonomous robot, paleogenetic creature, xenobiological organism, alien-interface infantry, and temporal guard.
- The bounded host-context and institutional-conflict slice is implemented in `chaosx.nr16.4` through `.9`; host-archetype presentation continues through existing dossiers `.10` through `.18`, while formation-origin and carried-portfolio presentation covers the first four KRG foundation reports, the existing clone, machine, xenobiological, temporal, and Singularity route dossiers, and foreign-integration reports `.60` through `.64`. The public challenge and counter-Kruger programme now complete the mapped public-exposure and countermeasure action surfaces with one-use ledgers, dedicated reports, AI weights, and no model dependency. The finite ten-country institutional settlement layer is statically implemented in the working tree; targeted balance, transfer, cleanup, and live-acceptance evidence remain pending, and broader country-specific flavour remains queued beyond these choices and the finite ten-tag presentation layer.
- The KRG biological force history bridge is present, but the bounded stockpile, production, native reservation debit, consumption receipt, transfer, and defeat cleanup lifecycle is not yet implemented. The exact design and cross-system boundary are recorded in `016_krg_biological_stockpile_delivery_addendum.md`; no free payload or placeholder decision was added, and the seven unit models remain deferred.
- Twenty-five institutional, breakthrough, incident/security, or remnant report scenes are now registered: the appointment, evolution, dossier, sovereignty, institutional, eight breakthrough-family, eight incident/security, four aftermath-family, and shared remnant cards. `chaosx.nr16.6` routes every project family through `GetBrilliantScientistBreakthroughPicture`; `chaosx.nr16.13` routes every incident family through `GetBrilliantScientistIncidentPicture`; and the aftermath chain routes through `GetBrilliantScientistAftermathRemnantPicture`, retaining the shared archive card for prototype-only records. Seven mapped black-and-white news surfaces are present and registered, including the guarded regional-defeat headline, with no missing texture reference.
- Quantitative small/medium/major research timelines, AI completion rates, force-production pacing, rebellion strength, and weak/normal/dominant Singularity timing evidence remain incomplete.
- User-owned live acceptance remains incomplete for opening/referral, all four evolutions, containment and territory loss, terminal/Fallout transitions, Event 019 provider lifecycles, and Directorate GUI interaction.

These are implementation tasks rather than omissions from the planning package.

## Resolved process history

The earlier blocker was cleared when `chaosx_improvement_loop_planner` produced `docs/plans/016_brilliant_scientist_plans/016_brilliant_scientist_improvement_loop_addendum.md`. The parent disposition is recorded in that addendum and in `016_source_of_truth_map.md`. Do not rerun the planner for Event 016 until the accepted addendum has been implemented or a later implementation finding creates a genuinely new depth gap.

## Honest completion label, 2026-07-29

- Source reading: complete.
- Historical and repository research: complete for planning.
- Full parent-authored source specification: complete.
- Prompt and continuation package: complete.
- Mandatory project-subagent closure pass: complete and dispositioned.
- Super-event text research: complete for six packages.
- Super-event audio research and Event 016-owned WAVs: complete for IDs `90` through `95`.
- Kruger stage-0 source, leader DDS, advisor DDS, and sprite registration: complete.
- Default-enabled core gameplay runtime: implemented and statically audited, including the fixed-tag KRG release repair; live formation acceptance remains open.
- Kruger State focus tree: 100 focuses, fully localized and asset-backed, with route-specific AI plans.
- Character, advisor, scientist, leader, event-log actor, and later-leader identity: one fixed identity wired.
- Project portfolio, evolutions, containment, terminals, aftermath, achievements, super-events, and Event 019 interoperability: implemented and statically wired.
- 3D model production: intentionally not started under the user's current instruction.
- Bounded context, first-prototype report, facility, custody, named foreign-reaction, host-archetype `.4` through `.11` presentation, institution and facility AI preference, KRG foundation formation-origin and carried-portfolio presentation, route-dossier formation-origin and carried-portfolio presentation, and foreign-integration formation-origin and carried-portfolio presentation: implemented and statically audited.
- The finite ten-country institutional settlement layer is statically implemented in the working tree. Targeted transfer, cleanup, balance, and live consumer acceptance remain pending. Broader country flavor beyond these choices and ten named tag clauses, quantitative balance proof, and live consumer acceptance: blocked. The seven milestone news surfaces, twenty-five generated report scenes, and project-family breakthrough, incident, and remnant selectors are implemented; no news fallback was substituted.
