# Goal Prompt: Implement Event 064 Border Fortifications

Implement Event 064 from `docs/specs/064_border_fortifications_specs/`. Read the full package, its matrices and prompts, and the improvement-loop blocker. Follow `AGENTS.md`, the named Chaos Redux skills, and current shared-system conventions. Use project subagents with `fork_context=false`.

Keep Event 064 Minor Repeatable, Chaos Level 1, with canonical entry `chaosx.nr64.1`. Replace the current flat two-level option with one hidden global transaction. Snapshot current control, process each valid country once, and give one land-fort level to every valid passable direct foreign land-frontier province below the active cap. Apply the exact inclusive land-boundary and non-land exclusion rules in Part 1. Deduplicate provinces, finish construction before reports, leave new borders for a later wave, and preserve buildings after transfer.

Implement all enabled and matured evolutions. Defense in Depth at 200+ adds bounded strategic anchors and real secondary approaches. Fortress States at 400+ adds bounded stronger sectors, role-based anti-air, radar, supply work, and selective coastal defenses. Fortress World at 600+ adds bounded capital and internal redoubts, including valid island positions. Use the tuning matrix caps, quotas, overlap rules, and gradual repeat model. Eligibility adds no Chaos. Disabled stages skip cleanly.

Implement three temporary response postures and all five project families from the decision prompt. Use a normal category, static picture, one active project, concrete dynamic costs, reserve floors, save-safe timers, target-loss cleanup, and one-time rewards. Preserve real offensive counterplay against forts. Write final player text from the localisation directions because working labels are not final copy.

Implement every AI scenario and spawn `chaosx_ai_probability_auditor`. Use the required probability tool workflow. Prove invalid choices are zero, emergency preferences work, target pools and reserves are correct, and both cluster memberships are reachable. Source inspection alone is insufficient.

Implement the complete Chaos map. Scale the first natural global premium by actual footprint, guard repeat gains, pay evolution premiums only on first concrete materialization, and pay cluster convergence only for a distinct combined military-abundance result. Pay zero for no-op, duplicate, debug, eligibility, and posture choice. Audit shared sources and prevent double counting.

Make Event 064 a Medium member of Sudden Abundance and Military Preparation. One automatic selection can enter at most one cluster context, and one cluster incident can execute Event 064 once. Keep exact manual cluster triggering, member history, normal event history, and standalone firing. Missing multi-cluster runtime support is a blocker.

Implement all four achievements with natural arming, stored targets, disqualifiers, save-load, cleanup, icons, docs, and tests. Produce and wire the report image, category picture and icon, three posture icons, five decision icons, and four achievement families. Use existing map building visuals and no placeholders.

Correct the authoritative workbook and both cluster memberships through `chaosx_spreadsheet_doc_worker`, then regenerate all CSV exports with `.tools/export_event_catalog_csv.py`. Align registry, settings, details, history, clusters, achievements, assets, localisation, and permanent documentation.

Spawn every specialist assigned by the coding prompt. Near completion, run `chaosx_improvement_loop_planner` using the preserved blocker handoff and resolve its addendum or closure. Run every acceptance scenario, save-load check, and large-world performance check. Keep iterating until the full spec is satisfied. Do not claim completion while any mapped surface, probability proof, cluster membership, asset, text key, catalog field, cleanup path, test, blocker, or review disposition remains unresolved.
