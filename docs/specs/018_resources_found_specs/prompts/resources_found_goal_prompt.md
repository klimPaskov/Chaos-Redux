/goal Implement Event 018 `Resources found` to the fullest extent using the planning package at `docs/specs/018_resources_found_specs/`.

Read `AGENTS.md`, required repo skills, offline wiki pages, vanilla docs, vanilla precedents, existing Chaos Redux patterns, and every Event 018 spec, matrix, diagram, prompt, note, and handoff before editing.

Preserve the baseline. Event 018 is Minor Repeatable. A random valid owned state receives around 100 of one random resource, and the owner receives a popup plus a field-management decision category. Keep the event in the economy positive cluster with medium severity. Ordinary firings must stay useful even if the Cave Host path never appears.

Implement the field system with visible dynamic values for richness, extraction pressure, survey confidence, worker safety, foreign interest, local dependence, public panic, and below pressure once public enough. Add staged decisions and missions from the decision map for survey, extraction, diplomacy, border crisis, safety, evacuation, hunts, closure, Host war, and aftermath. Use concrete costs beyond political power, plus AI equivalents and cleanup.

Implement Evolutions I through IV with active-event and pre-fire paths. Evolution I adds larger deposits and international pressure. Evolution II adds strange excavation incidents, worker sickness, corrosion, and population loss. Evolution III makes monsters public and adds city attacks, evacuation, hunts, and final closure stakes. Evolution IV creates the Cave Host only if the public danger field remains open through breach. Closure before Evolution IV must remove event-added resources and prevent the Cave Host.

Implement the Cave Host as a nonhuman special chaos country with leader, flags, focus tree, slow armored divisions, AI, diplomacy limits, world-threat integration, and immediate wars against neighbours. Cave Host divisions use no manpower and no equipment. They deploy automatically from captured resources. Every 10 total resources in a controlled non-origin state gives 1 division, capped at 10 divisions per non-origin state. Initial origin divisions scale from exploitation and are capped around 30.

Use the Part 7 focus blueprint. Map every final Cave Host focus id to the working blueprint or report accepted renames, merges, or omissions. Preserve the opening trunk, hunger, stone hide, tunnel, brood fork, surface terror, and continental maw lanes. Keep swarm and elder broods mutually exclusive. Do not reduce the tree to a vertical chain or ordinary country rewards.

Use the Part 8 scripted GUI plan if the richer panel is implemented. Field cards, button states, tooltips, value meters, cleanup rules, and AI equivalents must align with decision logic. Animated UI assets require real source frames, static fallbacks, sheet DDS files, preview GIFs, manifests, and GFX handoffs.

Create full assets, achievements, event logs, Event Details, docs, localisation, AI strategies, and spreadsheet alignment. Cave Host reveal, world-end, and defeat aftermath super-events require researched text, sourced quote, sourced remark where used, licensed audio, image, settings-aware playback, and docs. Treat unresearched title text, button remarks, quotes, slogans, allusions, and audio choices as blockers.

Use subagents with `fork_context=false` where appropriate. Use repo exploration, scripted-system architecture, asset and super-event researchers, focus, decision, country, localisation, completion audits, and a near-completion improvement loop. Resolve, queue with reason, or reject any addendum before completion.

Do not use fallbacks, placeholders, hidden simplifications, generic focus filler, missing AI, missing assets, missing localisation, or normal manpower training for cave monsters. Report every blocker or simplification. Finish only when the full spec is implemented, validated, documented, audited, and aligned across gameplay, UI, assets, audio, achievements, event logs, docs, and spreadsheet.
