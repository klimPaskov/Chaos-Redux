# 012 Africa final implementation handoff

Use the canonical package as the source design. The older pass-specific continuation prompts are intentionally not included in this final package.

## Start here

1. Read `README.md`.
2. Read `source_reading_inventory.md`.
3. Read specs parts 1 through 32 in order.
4. Use parts 24 through 32 as closure rules when earlier files mention research gates.
5. Fill the state-id resolver before scripting regional requirements.
6. Implement Tier 0 and Tier 1 country packages before claiming event completion.
7. Implement grounded routes before high-chaos routes.
8. Resolve super-event quote and audio research through the super-event workflow before wiring.
9. Use final canonical prompts in `prompts/` for subtask routing.

## Known blockers outside this package

- exact state ids
- exact tags and cosmetic tags
- source image selection for historical flags
- source image selection for real portraits
- final audio download and conversion
- final localisation
- OneDrive spreadsheet update

## Main warning

Do not convert this planning package into a thin implementation. The event is complete only when the actual mechanics match the depth of the package.
