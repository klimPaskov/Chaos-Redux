# Event 58 goal prompt

```text
/goal Implement Chaos Redux Event 58, Random Buildings, to the fullest extent defined by `docs/specs/058_random_buildings_specs/`.

Read every file in that folder. Treat the five files under `specs/` as source design and `prompts/058_random_buildings_coding_prompt.md` as the implementation brief. Use the other prompt files for their named passes. Resolve the acceptance matrix and probability scenarios.

Read `AGENTS.md`, required skills, offline wiki pages, vanilla documentation, repository precedents, and all Event 58 plans. Run the supplied repo-explorer prompt first to map live dependencies.

Keep `chaosx.nr58.1` as the entry event. Register ID 58 as Minor Repeatable, Chaos level 1, and a Medium member of Positive Economy. Replace the stale `The Industrial Complex` identity on Event 58 surfaces.

One successful firing is one bounded global transaction. Each loaded land state independently attempts one valid baseline state-building result. Evolution I at `200+` adds a second expanded state-building result. Evolution II at `400+` adds one valid province package per state. Evolution III at `600+` adds a limited global set of exceptional structures. Higher layers stack. Revalidate every placement. An exhausted layer may return no result and must never receive an invented substitute.

Build an owner-extensible provider registry. Every provider needs a stable ID, owner, layer, risk band, weight, availability, location and capacity validation, placement callback, complete owner initialization callback, responsibility, uniqueness, display family, and cleanup contract. Incomplete providers fail closed. Event 58 owns selection and reporting. Provider owners retain normal mechanics, lifecycle, evidence, map entities, removal, and cleanup. A grant must not mark its source event as fired.

Select risk band before candidate and fall only toward safer bands when a band is empty. Centralize tuning. Run `chaosx_ai_probability_auditor` before weighted changes and again afterward with `hoi4.probability_compare` on the same named scenarios.

Evolution II must use real province geography. Fort packages cover relevant valid border or coastal provinces. Naval bases use verified coastal provinces. Railways use useful verified segments or short connections. Supply hubs require strict logistics evidence. Use HOI4 MCP map tools. Report a blocker when an accepted map operation cannot be implemented safely. Never replace railway or supply work with infrastructure.

Give every human player one compact report. Wire Event Logs, Event Details, repeatable state, evolution logging and toggles, actor handling, cluster behavior, Event 58 Chaos milestones, localisation, debug names, and multiplayer behavior. The global incident must not show a false country actor, create per-state popup spam, or retain an unbounded state ledger.

Implement all three achievements with tracking, disqualifiers, localisation, icon triplets, documentation, and runtime consumers. Produce the accepted report image and achievement art.

Write final in-world localisation without probabilities, provider IDs, tuning notes, or implementation history. Update Event 58 documentation and `docs/spreadsheets/chaos_redux_events_catalog.xlsx`, then run `python .tools/export_event_catalog_csv.py`.

Near completion, run the supplied improvement-loop prompt and resolve its result. Then run the supplied read-only completion audit. Keep iterating until every accepted mechanic, provider contract, owner callback, placement rule, report surface, achievement, asset, probability comparison, map proof, document, workbook field, and plan disposition is complete. Do not claim completion until the implementation satisfies the full package. The final report must list changed files, coverage, map and probability evidence, assets, achievements, documentation, export results, validation, blockers, and simplifications. State explicitly when no simplifications remain.
```
