# Goal: Complete Event 065 Random Trait

Implement Event 65 to the fullest extent of the accepted specification.

Read `AGENTS.md` and every file under `docs/specs/065_random_trait_specs/`.

Use every implementation and audit prompt in that folder.

Run the repo explorer and scripted-system architect prompts before editing.

Keep Event ID `65`, namespace `chaosx.nr65`, root `chaosx.nr65.1`, Minor Repeatable registration, sprite `GFX_report_event_leader_trait`, and the current runtime image path.

Replace the visible-option mutation with one authoritative hidden executor.

Every eligible existing country-leader role must receive exactly one, two, three, or five distinct accepted source traits at Baseline, Evolution I, Evolution II, or Evolution III.

AI countries use the same distribution without visible report queues.

Multiplayer clients never roll independently.

Generate the pool from every loaded vanilla and Chaos Redux country-leader trait accepted by `add_country_leader_trait`.

Use stable indexes, a recipient-level grant ledger, a generated name selector, content-profile records, manifests, checksums, and a check mode that fails on stale output.

Do not use the current manual list or a small fallback pool.

Do not filter by country, ideology, personality, usefulness, harm, contradiction, route, rarity, or strange identity.

Every slot samples without replacement from remaining eligible source IDs.

Owned traits, prior Event 65 grants, and same-firing duplicates do not consume a successful slot.

Handle saturation, invalid targets, missing leaders, failed application, save and load, leader return, registry migration, and high-tag performance.

Use uniform weight `100` at Baseline and Evolution I.

Use ordinary `100` and featured `125` at Evolution II.

Use ordinary `100` and featured `150` at Evolution III.

Featured reasons never stack.


Begin weighted work with `hoi4.probability_inspect`.

Run every scenario in `quality/065_random_trait_probability_scenario_matrix.md`.

Use the exact evaluation, sweep, sequence, comparison, and render routes required there.

Run `hoi4.probability_compare` after any patch.

Do not claim probability coverage when the route is unavailable.

Implement persistent Evolution state and exact raw Chaos boundaries at `200`, `400`, and `600`.

Record Evolution history only after successful evolved effect.

Implement one-time successful manifestation Chaos of `+2`, `+3`, `+5`, and `+8`.

Do not multiply it by leaders or traits.

A first higher-form firing grants only that form's milestone and bypasses missed lower milestones.

Integrate Randomizations through the same executor.

Prefer cluster ID `9` only after a fresh workbook and source collision check.

Use optional membership, Medium danger, and named 60 percent participation.

Wire event history, Event Details, Evolution settings, cluster settings, global actor handling, debug paths, permanent docs, and the authoritative workbook.

The report shows the active form, global counts, pool size, local leader, local trait names, and saturation state.

Handle zero, singular, and plural results without raw IDs.

Keep acknowledgment non-mechanical.

Validate or replace the existing DDS and prove the `210x176` consumer.

Use `hoi4.event_inspect`, `hoi4.event_render`, and `hoi4.event_compare` before and after changes.

Provide route coverage for direct and cluster firing, all forms, human reports, AI mutation, debug paths, save and load, and multiplayer.

Near completion, run the probability auditor, localisation auditor, documentation curator, spreadsheet worker, mandatory improvement-loop planner with `fork_context=false`, and read-only completion auditor.

Move status to `Needs Testing` only after implementation validation.

Leave `Available` for user-led in-game acceptance.

Keep iterating until the goal is accomplished to its fullest extent.

Do not claim completion until every implementation-owned acceptance row passes.
