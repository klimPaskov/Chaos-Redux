# Event 47 BOOM completion audit prompt

Route this work to `chaosx_event_completion_auditor` as a read-only audit after implementation and specialist patches are complete.

Compare the final repository against every file under `docs/specs/047_boom_specs/` and every accepted plan under `docs/plans/047_boom_plans/`.

The audit must verify:

- Event 47 remains Minor Repeatable at Chaos level 1.
- Primary selection is state-uniform across the eligible pool.
- Every incident freezes targets and rings before damage.
- Overlap resolves to one strongest zone per state.
- Civilian deaths remove real population through the exact shared transaction.
- Applied civilian deaths enter Deaths once.
- Military deaths are recorded only from exact observed loss.
- All installed building families are classified and rail or supply damage is present.
- Force damage is state-local and deduplicated.
- The mushroom-cloud presentation does not call any nuclear gameplay path.
- Nuclear stockpile, use history, fallout, condemnation, Air Cleanliness, retaliation, and nuclear achievements remain unchanged.
- The global soundwave creates no universal gameplay modifier.
- Evolution activation changes zero Chaos.
- Event 47 adds no direct Chaos that duplicates Deaths.
- Evolution I supports pre-fire and active-history entry.
- Evolution II supports pre-fire and active-history entry.
- Two and three blast counts match the accepted probability contract.
- Geographic separation and every fallback tier work.
- One Event 47 firing produces one history row and one pacing transaction.
- The event has no attacker actor in Event History or evolution records.
- Reports are bounded and later blasts do not move the camera.
- Repeatable state, evolution state, and delayed incident state survive save and reload where relevant.
- Temporary targets, receipts, and generation data are cleared.
- The static report image is final, wired, documented, and period-consistent.
- Localisation, Event Details, Deaths cause text, docs, and workbook row agree.
- CSV exports were regenerated from the authoritative workbook and not edited directly.
- Various Anomalies cluster wiring was either completed against a stable ID or reported as blocked without a guessed ID.
- Required MCP event and probability evidence exists.
- No accepted fallback, omission, placeholder, or unimplemented addendum is hidden.

Return a finding table with severity, requirement, evidence, file or identifier, and required correction. Do not mark the event complete while any population, nuclear-isolation, overlap, building-coverage, probability, asset, or catalog blocker remains.
