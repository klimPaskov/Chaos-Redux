# Event 47 BOOM implementation goal

Implement Event 47 from `docs/specs/047_boom_specs/`. Read every source specification and handoff there, plus the accepted notes under `docs/plans/047_boom_plans/`.

Follow `AGENTS.md`, the event, asset, and subagent skills, plus relevant local skills. Inspect the required offline wiki, vanilla documentation, and matching repository precedents. Use the HOI4 MCP event tools before and after source changes. Audit each selector and MTTH surface through the probability workflow.

Keep `chaosx.nr47.1` as Minor Repeatable at Chaos level 1. Select the first epicenter uniformly from the eligible-state pool, never through a country-first roll. Exclude unsafe states and states whose owner does not use ordinary civilian systems. Rebuild the pool for each firing and allow an earlier target to return when it remains valid.

Freeze every epicenter, blast ring, strongest overlap zone, unit target, and receipt before damage begins. Apply civilian losses through `apply_exact_state_civilian_population_loss`. Register only the returned applied amount once in Deaths under an Event 47 cause. Centralize all tuning. Never substitute manpower, recruitable population, attrition, a modifier, or a BOOM-only counter.

Damage existing building levels across the installed inventory. Railways, supply, infrastructure, industry, dockyards, bases, anti-air, radar, and forts must use the accepted zone profiles. Classify every other state building family. Disrupt only units inside affected states and process each once. Record military deaths only from exact observed personnel loss.

Use a standalone mushroom-cloud effect and the report image. Never call a nuclear strike, responsibility, condemnation, retaliation, fallout, or history path. Prove that nuclear stockpiles, nuclear use, fallout, Air Cleanliness, condemnation, nuclear achievements, and nuclear missions stay unchanged. Add no Event 47 Chaos that duplicates Deaths.

Implement Bigger BOOM at 400 Chaos and BOOM BOOM BOOM at 800 Chaos. Support pre-fire evolved openings and paced active-history activation. Activation and logging add zero Chaos. Keep evolution toggles independent. Implement the accepted two and three epicenter distributions, separation ladder, bounded retries, safe degraded counts, and strongest-zone overlap. One firing creates one Event History row and one pacing transaction.

Move the camera to the first epicenter only. Later blasts use map effects and bounded notices without further camera movement. Send one main report and one global soundwave acknowledgment per normal country. Add no global gameplay penalty. Keep the cause unresolved and leave Event History and evolution entries without an attacker actor.

Do not add a BOOM meter, investigation, decisions, missions, custom GUI, focus tree, country package, super-event, event-owned AI strategy, or random-luck achievement. They conflict with the accepted event.

Wire registration, default enable state, Event History, Event Details, evolution views, Deaths cause, debug selectors, localisation, documentation, and the authoritative workbook together. Regenerate the catalog CSV exports with the repository tool. Never edit them directly. Do not invent a Various Anomalies cluster ID. Keep cluster integration blocked until a stable identity exists.

Route the image through the asset workflow. Route localisation, probability, and completion review through their named context-free specialists. Resolve every accepted finding and run the full matrix, including population conservation, overlap, nuclear isolation, multiplayer presentation, save and reload, and no-target rejection.

Produce a completion report with changed files, identifiers, task-specific evidence, asset wiring, probability comparisons, catalog alignment, and blockers. Do not claim completion with a placeholder, fallback, missing building family, duplicate death path, nuclear side effect, unresolved probability finding, or stale documentation.
