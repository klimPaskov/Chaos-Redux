# Goal Prompt: Implement Event 24 Video Game in Sweden

Implement the accepted package under `docs/specs/024_video_game_in_sweden_specs/`. Read every file first and treat it as binding design.

Keep Event 24 as an unclustered Chaos level 1 Minor Fire-Once event with entry `chaosx.nr24.1`. Select one principal Swedish host through the specified priority. Prevent duplicates after civil war, annexation, release, puppeting, or tag change. Show `N/A` without a valid host and preserve fire-once history.

Implement all three openings, one visible `Simulation Reliance` value with four public states, one staged idea lifecycle, the finite baseline loop, and every ordinary conclusion. Only one Event 24 idea may exist at a time, and baseline play must be able to conclude without evolving.

Implement all three named evolutions with their Chaos gates, delayed pacing, route conditions, incidents, high-Chaos openings, disabled handling, logs, and recovery. High Chaos may strengthen the opening but may not force an unanswerable final crisis. Evolution III must retain all four response paths, and Trust the Model must reach mandatory reassessment within the accepted limit.

Use one normal event-owned decision category with a static picture. Keep three to five primary actions per phase and no more than one active mission. Implement the full decision map, partial outcomes, cleanup, and persistence. Each action may use at most four spendable cost types with correct texticons. Avoid minor reward dust and store-like design.

Give Rulebook Commander to at most one valid Swedish commander and remove that temporary trait after successful field validation, Reality Audit, or event cleanup. The user's explicit 2026-09-20 instruction retires Field-Validated Planner. Use a bounded foreign-response pool with at most three meaningful countries and copy, study, ban, ridicule, or no-action outcomes. Foreign states do not receive Sweden's full mechanic.

Implement route-specific AI for every route and action using the campaign factors defined in the package. Run `chaosx_ai_probability_auditor` with the named scenarios before weighted edits and use `hoi4.probability_compare` after each patch. Near capitulation, broken supply, or severe instability must never favor the maximum-risk route.

Wire registration, normal firing, names, debug names, actor mapping, History, Event Details, all three evolution rows, and unavailable-weight display. Baseline stages are not evolutions. Write final in-world localisation from the specified direction. Use dry period satire and concrete institutional behavior. Avoid modern gamer slang, planning labels, implementation notes, em dashes, semicolons, staccato, and generic contrast formulas.

Produce every authorized asset through the asset prompt, with separate source art by type and final processed, manifested, placed, and wired DDS files. Leave no placeholders and keep the package surface list exhaustive.

Implement both achievements with full tracking, disqualifiers, icons, documentation, and save persistence. Update event docs and the authoritative XLSX only after final in-game wording exists, then regenerate the CSV exports. Never edit those CSVs directly.

Before coding, read `AGENTS.md`, all relevant skills, the offline wiki, installed vanilla documentation, vanilla precedents, and current Chaos Redux patterns. Use mandatory HOI4 MCP event and probability tools. Run the required decision, localisation, probability, and completion audits, then resolve every finding or report the exact blocker. Do not simplify, merge, replace, or omit accepted content without explicit approval. Report every unresolved item under `Simplifications, omissions, and blockers`. If none remain, state that and provide route, asset, achievement, probability, and acceptance coverage evidence.
