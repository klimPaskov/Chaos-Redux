# Subagent Prompt Pack: Event 011 Secret Alliance

All project subagents must be spawned with `fork_context=false`. Each prompt must include the relevant spec paths and the user constraints from the package.

## Scripted system architect

Read AGENTS.md, chaos-redux-events, hoi4-decisions-missions, chaos-redux-subagents, and these spec files. Design or implement narrow helpers for hidden pact membership, member scoring, member cleanup, reveal from war, evidence handling, and dynamic operation pulses. Return helper map, constants plan, event target lifecycle, cleanup plan, and risks.

## Decision mission auditor

Audit the Counter Pact Operations category after implementation. Check dynamic costs, target validity, visible requirement text, mission duration bands, objective quality, AI behavior, stale target cleanup, border-war exploit risk, and reveal transitions.

## Localisation auditor

Audit event, decision, GUI, idea, achievement, event-detail, evolution-detail, and spreadsheet-facing text. Check that hidden members are not spoiled, final text is not copied from working labels, dynamic values are formatted cleanly, and absent surfaces are not described.

## Icon artist

Create generated decision, idea, category, GUI, faction emblem, animated UI, and achievement assets from the asset prompt. Use the required reference folders, target sizes, static fallbacks, frame-animation workflow, manifest, contact sheet, and gfx handoff.

## Generated event art

Create generated period documentary report and news images when the asset prompt requests fictional or alternate-history scenes. Do not use readable generated text. Produce source PNG, processed PNG, final DDS, manifest entries, and gfx handoff.

## Event completion auditor

After implementation, compare repo files against this spec package. Report missing mechanics, simplifications, event log gaps, evolution gaps, decision gaps, AI gaps, asset gaps, localisation gaps, spreadsheet alignment gaps, and validation gaps.
