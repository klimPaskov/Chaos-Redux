# Subagent Prompt: Event 021 Scripted-System Architect

Spawn `chaosx_scripted_system_architect` with `fork_context=false` after the repository explorer handoff exists.

Read:

- repository `AGENTS.md`
- full Event 021 source-spec folder
- repository explorer handoff
- Event 006 package and country-collection implementation
- current dynamic effect and trigger registries
- current event, decision, cluster, scenario, and cleanup helpers

Design and implement only the narrow reusable script architecture required by Event 021.

Required contracts:

- hidden Fracture Pressure calculation
- visible State Authority change, clamp, stage, and breakdown
- valid-target evaluation
- opening severity
- archetype eligibility and selection inputs
- region-plan request and result
- parent-remnant safety
- force and stockpile allocation inputs and results
- actor initialization adapters
- Event 006 package call with Event 021 origin
- front registration and cleanup
- current priority-front selection
- active and prefire evolution entry
- neighbor Regional Exposure
- bounded Global Fracture eligibility, due-date review, queue, theater cap, and generation cap
- settlement and successor memory
- Wars cluster preparation
- manual scenario bypass and cleanup
- shared world-threat source activation and removal

Rules:

- reuse Event 006 helpers
- do not duplicate package initialization
- use script constants for tuning
- no unrestricted default daily, weekly, or monthly all-country loop
- no custom GUI helpers
- no animation helpers
- no super-event helpers
- no fallback actor or package
- fail closed on invalid territory, tag, leader, package, or capacity
- document every new public dynamic helper with scope, inputs, outputs, defaults, side effects, and example

Make only bounded helper and direct call-site changes inside the parent-approved surface.

Write:

`docs/plans/021_random_civil_war_plans/subagent_handoffs/scripted_system_architect_handoff.md`

List changed files, helper names, call sites, before and after behavior, task-specific validation, unresolved risks, and parent work.
