# /goal: Implement Event 059, The Offensive

Implement Event 059 to full completion from `docs/specs/059_the_offensive_specs/`. Read the complete package and use `prompts/059_the_offensive_coding_prompt.md` as the main brief, plus its AI, asset, and achievement prompts.

Follow `AGENTS.md`, `chaos-redux-events`, `chaos-redux-event-assets`, `chaos-redux-subagents`, and `chaos-redux-improvement-loop`. Inspect the existing Event 059 chain, registration, Event Logs, cluster system, achievement registry, AI plans, country-creation hooks, save identifiers, and vanilla AI precedents. Verify exact fields from local authoritative references. Do not invent AI strategy keys.

Event 059 remains Minor Fire-Once at Chaos level 1. It creates one permanent global state that changes AI choices only. Grant no direct combat, production-output, research, organisation, planning, supply, or equipment-stat bonuses. Human-controlled countries are unaffected. Control changes must suspend or restore active layers correctly, including new and released countries.

Do not add an unbounded daily, weekly, or monthly whole-world scan. Prefer declarative AI plans. If verified engine limits require country-local registration, use one-time activation and bounded existing creation or release hooks. Owner strategy wins when it conflicts with the generic posture.

Implement four independent layers. Baseline improves current-war activity, concentration, exploitation, feasible offensive production, air support, viable naval invasion, existing war-goal use, and useful war participation. Evolution I at 200+ adds persistence and follow-up. Evolution II at 400+ adds valid claim, intervention, and opportunity-war pressure. Evolution III at 600+ adds scale, theater ambition, and accepted risk. A higher layer must not reproduce a disabled lower layer. Supply, manpower, equipment, fuel, transport, reserve, homeland, legal-path, subject, and special-actor limits remain binding.

Support active-event MTTH evolution and pre-fire evolved openings. A high-Chaos first firing activates every enabled eligible layer in order, records each evolution, and sends one combined report. Evolution activation adds zero Chaos. The first worldwide manifestation adds one guarded 15 to 25 Chaos gain. Do not duplicate shared war, death, annexation, faction, or world-tension sources.

Assign Diplomacy membership with High severity. Treat Diplomatic Panic as the superseded name, not a second cluster. Read role, chance, and minimum tier from authoritative member arrays and use current defaults. Audit any weighted value against the full pool. Preserve cluster pacing and Fire-Once removal.

Spawn `chaosx_scripted_system_architect` before new AI plumbing. Spawn `chaosx_ai_probability_auditor` and run the inspect, evaluate, sweep, compare, and render workflow from the 46-scenario matrix. Route the report image through `chaosx_generated_event_art` and the achievement icon through `chaosx_icon_artist`. Run the mapped localisation, completion, and spreadsheet subagents. Near completion, spawn `chaosx_improvement_loop_planner` and resolve its addendum or closure handoff.

Wire complete Events, History, Evolutions, Event Details, and Clusters coverage with global actor handling and multiplayer-safe report fan-out. Implement the defensive achievement with every qualification and exploit guard from its prompt. Produce final assets and live consumer evidence. Update the authoritative workbook and regenerate CSV exports without direct CSV edits.

Use event inspection and comparison tools, repository validators, fresh logs, save migration, save and reload, control transitions, new-country tests, viable and inviable naval tests, legal and illegal target tests, evolution toggles, cluster tests, and achievement tests. Keep iterating until every acceptance row is satisfied. Do not claim completion while any required behavior, asset, log surface, workbook row, subagent handoff, or live validation remains unresolved.
