Read this prompt as an implementation handoff for Event 070 Cookie Click.
The design folder is `docs/specs/070_cookie_click_specs/`.
Its README indexes all twenty specification parts and the research notes.
The companion execution records belong in `docs/plans/070_cookie_click_plans/`.
This handoff has not been executed.

The supplied brief governs the requested feature.
The numbered specifications contain authored expansion and balance proposals.
They are not evidence that any game behavior, asset, subagent review or playtest already exists.
Read the complete applicable source and skill files before writing.
Record unavailable references rather than pretending their requirements were satisfied.

# Coding task

Implement every specified route and meaningful feature.
Do not reduce the persistent pet to an event popup, the dynamic registry to a short hardcoded reward list, the Empire to renamed vanilla divisions, or the large tree to seven branch names.
Do not claim completion until the implemented files satisfy the specification and the required evidence exists.

## Required reading

Read `AGENTS.md`, `CHAOS_REDUX_MECHANICS.md`, the documented dynamic effects and triggers, canonical catalog guidance, and all applicable current repo skills in full.
Use event planning, events, focus trees, decisions and missions, scripted GUI, assets, frame animation, 3D model pipeline, super-events, ComfyUI, MTTH, subagents, improvement loop, and the debug skill when a debug task is authorized.
Read the required offline `paradox_wiki/` core pages and the additional country, interface, unit, equipment, focus and technology pages.
Read current installed-vanilla documentation and relevant real examples.
The archive did not include those complete external references.
Do not use an online Paradox wiki as a shortcut around the repository rule.

The supplied config and some prose profiles disagree about models and firearm production.
Confirm actual tool availability and model configuration.
For firearms, the dedicated 3D skill's separated body and firearm pipeline governs until the conflict is explicitly resolved.
No planning report represents those worker calls as completed.

## Stage 1: establish current source and contracts

Use `chaosx_repo_explorer` for the current Event 070 identity, root namespace, picker status, prior remnants, shared helpers, catalog and scenario registries.
Inventory vanilla, Chaos Redux, installed Workshop and other local tag sources before selecting a new tag.
Do not overwrite the old Gods of Africa identity or a living country.
The current brief assigns Cookie Click to 070, while the supplied CSV leaves that row blank and unavailable.

Use `chaosx_scripted_system_architect` to lock the owner schema, cycle behavior, callback graph, neutral resource adapter boundary, actual-debit contracts and staged country commit.
Verify clickable GUI synchronization, partial population conversion, research-progress mutation, force-strength measurement and relevant ownership hooks before depending on them.
An unsupported capability needs a precise blocker and reproducer, not a silent smaller substitute.

## Stage 2: build a complete pet vertical slice

Implement acquisition, owner-aligned daily cycles, the 300-click initial target, frozen scaling, lifetime growth, milestone rewards, permanent maturity, hunger, early death and recovery.
Keep only Fullness and Level as persistent custom public values.
Show current clicks and daily target by Fullness and lifetime clicks only in its intended tooltip.
Do not grant unlimited rewards, automatic human feeding or extra full-day prizes after reload.
Every accepted click has feedback, while gameplay awards are batched at milestones.

Build a broad capability-gated reward and edible registry with explicit readers, bounds and actual mutation outputs.
Event 066 sharing is optional neutral registration, not an assumed existing Abundance API.
Country stockpiles, population, manpower and state buildings have distinct scopes and accounting.

## Stage 3: evolutions and danger

Implement 200, 400 and 600 Chaos evolution eligibility, active nominal 90-day MTTH through the verified adapter, and pre-fire evolved initialization.
No Chaos is added merely for evolution activation.
Implement the five-cycle warning, prescribed eligibility, recovery at 75 percent, truncated revolt draws and structural preflight.
Keep early death possible only below the permanent maturity thresholds.
Do not let self-feeding reset zero-click starvation or token clicks prevent danger forever.

## Stage 4: country and military

Use `chaosx_country_package_auditor` before and after country work.
Implement the full country matrix, compact initial territory, real capital and backup, additive compatible technology, exact startup force and reserve budgets, laws, industry, production, character and identity assets.
Preserve the host and meaningful subject trees.
Default the player to the host, with a separate explicit Empire control choice where permitted.

Implement six ordinary custom unit families and two world-end families with production, reinforcement, equipment, templates, caps and actual roles.
Use the current CXT registration and repair pathway.
Do not create free daily armies or overwrite ordinary research.
Use `hoi4.tech_inspect`, `hoi4.tech_render` and `hoi4.tech_compare` for every relevant technology graph change.

## Stage 5: empire economy and tree

Implement the two-value Empire phase, exact food recipes, real paid operations, post-uprising Level growth, three evolving spirits and four governing methods.
Build all FB, EB, LD, HS, EN, SE and FF path groups into a deep non-linear tree with useful filters, actual institutions, missions, regional work and postwar handling.
Use `chaosx_focus_tree_auditor` to prove each method has a viable early, middle and late path.
Do not enforce a generic focus count or copy identical rewards across branches.

Use `chaosx_decision_mission_auditor` and the separate decision prompt.
Show a small relevant action set, valid target previews, no more than four spendable resources, Command Power at most 60, sensible durations and explicit partial, failed and void results.

## Stage 6: aggression and scenarios

Implement host and external-neighbor wars, real Cookie-domain exemptions, bounded border updates, subjects and the global-war queue.
Implement The Final Bite's qualification, irreversible commitment, late formations, method-specific consumption and actual terminal proof.
Implement all four manual presets without progression prerequisites.
Structural identity, territory, capital and engine validity still apply.
Synthetic manual history must not create fake deaths, rewards or normal achievements.

## Stage 7: presentation and assets

Follow the separate asset, super-event and achievement prompts.
Use `chaosx_event_ui_worker` only for the event-owned window, with mandatory GUI before-and-after inspect, render, hierarchy and hit-region evidence.
Exclude shared interfaces from that worker's scope.
Do not treat a screenshot as a clickable implementation.

Route frame work through the frame-animation skill and 3D work through `chaosx_3d_model_pipeline`.
Require actual changing frames, animated Monster portrait, static fallbacks, sourced sound, measured vanilla scale, complete actions, exported-file reimport and live evidence.
The user alone operates RunPod for grounded portrait finals.
Use the portrait worker for all portraits.
Do not replace required animation with a moving still.

Research final super-event titles, buttons, quotations, cultural references and recordings through the designated text and audio researchers.
The current research contains verified literary candidates and partial music discovery, not final clearance.
No unresearched placeholder text or unlicensed recording enters runtime files.

## Stage 8: shared systems and catalog

Implement every Chaos manifestation, country-growth milestone, reversal and anti-farming rule from part 14.
Prove generic war, death, annexation and other sources are not counted twice.
Register the Cookie threat source and clear only its own contribution.
Keep migration, transformation and civilian deaths distinct.

Edit the canonical workbook only through the spreadsheet worker after reading the spreadsheet task rules.
The supplied CSVs are export-only snapshots.
Update Event 070, scenario rows, event details and mechanics documentation from actual completed content, then regenerate exports through the repository's exporter.
Do not mark the event playable without the project's required approval.

## Stage 9: probability, tests and review

Spawn `chaosx_ai_probability_auditor`.
Begin with `hoi4.probability_inspect`, evaluate all named scenarios, sweep thresholds and use simulation only for declared uncertain inputs.
Compare after patches and render useful distributions.
Keep exact, bounded, sampled and unresolved evidence separate.
The included Python checks test authored arithmetic only and are not substitutes.

Test initial feeding, all maturity boundaries, early death, partial recovery, every evolution, every route, country creation, all units, manual setups, save migration, reload exploits, multiplayer ownership, DLC absence, large-world performance and cleanup.
Desktop debug/playtest tools are opt-in under the supplied debug skill.
Do not operate them without the required authorization.
Record unrun cases honestly.

## Stage 10: completion

Use localization, country, focus, decision, asset and event-completion audits with disjoint write ownership.
Spawn `chaosx_improvement_loop_planner` near completion.
Resolve every proposed addition as accepted, revised, rejected with reason, or blocked.
Promote accepted design changes into the source specs and record closure.
A missing delegation tool requires a blocked closure handoff, not a fabricated worker report.

Keep iterating until the complete design is implemented and supported by evidence.
Report modified files, route coverage, asset and source provenance, actual tool outputs, tests, remaining blockers and catalog status.
Remove temporary continuation prompts and unused production leftovers only after their evidence is preserved.
Never use an unfinished fallback as proof of full completion.
