Read this prompt as an implementation handoff for Event 070 Cookie Click.
The design folder is `docs/specs/070_cookie_click_specs/`.
Its README indexes all twenty specification parts and the research notes.
The companion execution records belong in `docs/plans/070_cookie_click_plans/`.
This handoff has not been executed.

The supplied brief governs the requested feature.
The numbered specifications contain authored expansion and balance proposals.
They are not evidence that any game behavior, asset, subagent review or playtest already exists.
Read the complete applicable source and skill files before writing.
Record unavailable references rather than pretending their requirements were satisfied.

# Asset task

Produce and wire the complete asset set from parts 05, 08, 17, 18, 19 and 20, with the full branch definitions in parts 09 through 12.
Read `chaos-redux-event-assets`, `chaos-redux-frame-animation`, `chaos-redux-scripted-gui`, `chaos-redux-3d-model-pipeline`, `chaos-redux-comfyui` and the assigned profiles in full.
Inspect actual installed consumers and dimensions before final export.

Use `chaosx_asset_source_researcher` for grounded source and license research, `chaosx_generated_event_art` for original event imagery, `chaosx_icon_artist` for icons and counters, `chaosx_portrait_creator` for every portrait, `chaosx_event_ui_worker` for this event's window only, and `chaosx_3d_model_pipeline` for models.
Their reports remain separate from parent approval.
Do not claim a worker ran when it did not.

Close a finite asset manifest against actual runtime IDs before generation.
Cover six Cookie body bands, eight expressions per band, three evolution treatments, every required animation, all reports and news, three super-event images, Monster portrait, three institutions, six commanders, five country flag identities, the real domain emblem, three spirit lifecycles, every focus and technology node, every equipment and unit family, decision families and fourteen achievement triplets.
Final focus count comes from the implemented graph, not an arbitrary icon quota.

Use the original living Cookie as the identity reference for its Monster and military.
Keep the native HOI4 framing and readable colorful central interaction.
Do not put small decorative details where the native icon size cannot show them.
Never bake dynamic labels, progress numbers or buttons into a screenshot background.

Create genuinely different frames for squash, facial reactions, hunger, threat, death, transformation and the leader portrait.
Frame counts, supported combinations, sprite consumers, timing and static fallbacks belong in the manifest.
A still image translated, scaled, warped, recolored or given a glow is not the requested animation.
The normal and reduced-motion modes both need readable state and exact click behavior.

Use one clean approved image per geometry job.
Follow the dedicated skill's Meshy-7 and separate-body/separate-firearm requirement.
Resolve conflicting older firearm and retry instructions before paid work.
Do not generate fused guns or send firearm characters through forbidden auto-animation.
Record provider request, model, paid-job authorization, outputs and recovery.
No API key, paid capability or credit balance is assumed from this prompt.

Measure installed-vanilla source geometry and effective scale for each class.
Include weapons, mounts, crew, machinery and every attachment in performance and scale checks.
Complete idle, movement, combat and death actions.
Fit and animate firearm hands, recoil and muzzle locators correctly.
Export, reimport the actual output and capture live moving, fighting and dying consumers.
A static 3D render is not completion.

Create bespoke large and small native counters for all eight military families.
Sample actual vanilla green and preserve the reference evidence.
Check the two-frame large-counter format and all template consumers.
Equipment, map models and counters are separate assets.
Do not reuse a renamed vanilla division as evidence of a custom military family.

Source final sound from permitted existing Internet recordings.
Record creator, original source, exact license, attribution, edits and runtime role.
No generated, synthesized or newly recorded substitute final audio under the supplied source-only rules.
Verify whether selection voices are country-level or family-specific before promising separate voice sets.
Align action audio with actual animation.

Every flag follows the source skill's flat reference-constrained generation and manual geometry review.
This fictional polity uses its approved original identity reference.
Historical subject flags and real portraits retain sourced provenance, not invented substitutes.
The user alone runs RunPod for grounded portrait finals.

Use the actual project DDS and TGA pipelines, including strict DDS byte-count and round-trip checks.
Keep source files, frames, references, provider lineage and review evidence in the event-owned temporary asset workspace until approved.
Promote only assets with a known runtime consumer.
Do not include font files in deliverables.
Remove unused files after preserving required provenance.

Return a complete manifest, missing-capability report, consumer coverage, previews, before-and-after GUI evidence, scale and animation evidence, licensing records, final paths and an explicit list of unvalidated assets.
Do not report a static fallback as the completed ordinary animation.
