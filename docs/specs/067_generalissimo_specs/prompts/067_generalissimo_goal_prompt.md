# Goal: Fully Implement Event 067 Generalissimo

Implement Event `067` from `docs/specs/067_generalissimo_specs/`. Read the entire pack before editing.

Event `067` is a Chaos Level 1 Minor Fire-Once event and a High member of Military Preparation. Select one valid major or player country and create one canonical fictional male Generalissimo. One character token must remain the commander, field marshal-rank commander, and later country leader. Give him maximum safe commander stats, every safe positive applicable commander trait, maximum useful capacity, and direct command use after takeover. Never create clones.

Track a hidden service record before Evolution I. At 200 or higher Chaos, expose Generalissimo Influence from 0 to 100 as the only public crisis value. At 400 or higher Chaos, unlock the State Within the State. At 600 or higher Chaos, unlock the ultimatum. Evolutions use paced MTTH and shared logging and add zero Chaos by themselves.

Implement all demands, concessions, counterweights, objectives, and removal methods. Displayed removal chances must match executed chances. Failed arrest, capture, assassination, or final removal starts the revolt immediately. Successful permanent removal ends the crisis and blocks later coup and world-end readiness.

Calculate revolt strength from Influence and hidden military power. Prefer a dynamic civil-war side. A fixed junta tag is forbidden unless engine evidence proves it necessary and a complete required collision scan passes. Both sides need viable capitals, forces, supply, and a route to victory.

Support peaceful submission without creating another country. After takeover, close Influence and open Command Cohesion from 0 to 100 as the only public post-takeover value. Keep the Generalissimo as ruler and commander. Preserve valid host technology, forces, territory, stockpiles, wars, and service assets. Apply the starting junta ideas and load the complete host-adaptive focus tree.

Implement every focus route with route AI, idea lifecycles, filters, Focus Navigation, conditional service content, hidden world-end content, and the read-only inlay. Use the mandatory HOI4 MCP focus and GUI inspect, render, rewrite, and comparison workflow.

Implement Generalissimo's Coup as a triggerable scenario. Confirm proposed ID `SCN-015` against the workbook and runtime registry. Include every specified type and intensity. Launch bypasses normal timing and evolution prerequisites, consumes the unique character opportunity, clears bypass state, and does not count as a normal event firing or pacing transaction.

Implement The Generalissimos' World as a public world-end branch. Require 1000 or higher Chaos, no active world end, an enabled branch toggle, and a living canonical Generalissimo who rules an active or victorious host. Use a bounded actor registry. Support every specified military, civilian, bloc, and civil-war outcome. Never add recurring whole-world scans.

Create every accepted gameplay, identity, visual, super-event, licensed audio, and achievement surface. No custom unit, equipment family, technology tree, doctrine tree, 3D model, unit counter, or unit audio package is required.

Use the named probability scenarios for every weighted surface. Run `chaosx_ai_probability_auditor` before each weight patch and require `hoi4.probability_compare` afterward. Run every required specialist audit. Run `chaosx_improvement_loop_planner` after a complete implementation tranche, resolve every accepted addendum, then run a closure review.

Wire all shared event, evolution, cluster, scenario, world-end, audio, AI, cleanup, documentation, and catalog surfaces. Edit only the XLSX and regenerate its CSV exports. Do not use unapproved fallbacks or hide missing work. The final report must list files, route and scenario coverage, assets, audits, probability evidence, improvement-loop dispositions, blockers, and every simplification or omission.
