# Event 066 Abundance Completion Audit Prompt

Use `chaosx_event_completion_auditor` with `fork_context=false` after the implementation owner reports the event ready.
This is a read-only audit.

## Sources

Read every file under `docs/specs/066_abundance_specs/`.
Read every Event 66 implementation file, documentation file, asset handoff, provider manifest, probability report, localisation file, achievement entry, cluster entry, workbook export result, and unresolved plan under `docs/plans/066_abundance_plans/`.
Read `AGENTS.md`, `chaos-redux-events`, `chaos-redux-subagents`, and `chaos-redux-improvement-loop`.

Use the current repository state, not an earlier handoff claim.
Use HOI4 MCP event and probability evidence where required.
Do not treat source-only review as a substitute when the mandatory route exists.

## Audit questions

### Event identity and registration

- Is `chaosx.nr66.1` the canonical root?
- Is the old `CIC` behavior gone?
- Is the event correctly registered as Minor Repeatable at Chaos level `1`?
- Is default enablement consistent with the current status?

### Dynamic provider space

- Does Event 66 avoid a small central candidate list?
- Does every provider have validity, presentation, application, AI, and receipt behavior?
- Is enumeration read-only?
- Are owner ledgers protected?
- Does the value inventory give every discovered family a disposition?
- Are hidden, retired, proof-only, and unsafe values excluded for real reasons?
- Are DLC and country-specific gates accurate?

### Four-card generation

- Does every country build its own pool?
- Are four card signatures stored and persistent?
- Are pairs and triples independently drawn without authored packages?
- Are harmful values eligible?
- Is utility absent from generation?
- Do uniqueness, small-pool, novelty, provider-fault, and revalidation rules match the spec?
- Is option index unbiased?

### Application and accounting

- Are magnitude, caps, persistence, and state breadth provider-specific and strong?
- Does every atomic item apply once?
- Are partial results handled without rerolls?
- Do Deaths, Air Cleanliness, Condemnation, famine, migration, population, equipment, and global ledgers use their owner APIs?
- Is direct Event 66 Chaos `+5` once and zero on ordinary repeats?
- Are shared Chaos sources free from duplication?

### Evolutions and cluster

- Are all three evolutions gated, enabled, logged, and applied correctly?
- Does disabling Evolution II suppress later cardinality?
- Are Low, Medium, High, and Standard profiles distinct?
- Does one cluster transaction coalesce every Event 66 slot hit into one wave?
- Are cluster details, history, pacing, cap, and effective probability correct?

### AI, multiplayer, and persistence

- Does AI score only stored cards?
- Can AI select harmful options under the intended conditions?
- Do baseline and comparison probability reports cover every named scenario?
- Does authority create random results once?
- Do player countries receive independent choices?
- Do save, load, control change, pending choices, and tag-switch safeguards work?

### Presentation, assets, achievements, and docs

- Does the standard event popup show four readable dynamic options?
- Are tooltips precise and spoiler-safe?
- Is one global history row recorded per wave?
- Are all report and achievement assets final and wired?
- Are all three achievements complete, difficult, and based on receipts?
- Do Event Details, evolution details, cluster details, localisation, docs, and workbook wording agree?
- Was the authoritative XLSX updated and the exporter run without direct CSV edits?

## Plan disposition

Find every Event 66 improvement addendum, subagent handoff, audit follow-up, blocker, and resume note.
For each one, state whether it was implemented, folded into specs, queued with reason, rejected with reason, superseded, or unresolved.
An unresolved accepted addendum blocks completion.

## Report format

Return:

- overall verdict
- spec requirement matrix with pass, fail, blocked, or unresolved status
- concrete source and evidence references
- missing mechanics
- missing provider families
- probability and AI gaps
- asset and achievement gaps
- localisation and catalog mismatches
- simplifications and fallbacks
- unresolved plans
- required fixes before completion

Do not patch gameplay.
Do not accept a completion claim based on compile success, one event popup, a small candidate list, or unverified prose.
