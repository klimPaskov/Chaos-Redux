# Conditional Prompt for `chaosx_focus_tree_auditor`

Spawn with `fork_context=false` only when a country focus tree is actually changed to interact with famine, relief, borders, evacuation, or integration.

Audit the named tree for route validity, focus effects, decision unlocks, idea lifecycle, AI weights, filters, navigation, layout, localisation, and no filler.

Use mandatory HOI4 focus MCP inspection and rendering.

Do not create a shared famine focus tree.

Any new route family requires an accepted plan before patching.
