# Prompt for `chaosx_scripted_system_architect`

Spawn with `fork_context=false`.

Repository root: `<MOD_ROOT>`.

Read:

- the complete source spec folder
- the repo explorer report
- `AGENTS.md`
- the dynamic effect and trigger registries
- the current Deaths, Air Cleanliness, Condemnation, camps, occupation, outbreak, nuclear, bombing, disaster, and event adapter source
- required offline wiki and vanilla documentation

Design and, where safely bounded, implement reusable contracts for the separate famine and migration mechanics, connecting them only through explicit causal adapters and neutral conservation primitives.

Required architecture:

- active food-security state registry
- active displacement country and state registry
- validated famine-pressure request adapter
- validated flight-pressure request adapter
- exact state-to-state civilian population transfer
- route resolution and route-death ownership
- border policy and trapped-population state
- reception capacity
- voluntary return, local integration, and third-country resettlement
- occupation, camp, gulag, forced-labor, deportation, bombing, nuclear, outbreak, disaster, war, peace, and event adapters
- new Deaths reason contracts
- centralized script constants
- cleanup after annexation, control change, invalid destination, route loss, peace, state conversion, and country classification change

Hard rules:

- no whole-world daily, weekly, or monthly scan
- no population duplication
- movement is not death
- one physical death has one reason
- invalid requests fail closed
- special and nonhuman country exclusions use shared classifiers
- public reusable effects and triggers are documented in their registries

Inspect positive state population behavior and manpower side effects directly before finalizing the transfer helper.

Write a handoff under:

```text
docs/plans/famine_and_migration_system_plans/subagent_handoffs/scripted_system_architect.md
```

List files changed, helper names, inputs, outputs, defaults, side effects, call sites, meaningful validation, remaining blockers, and parent wiring.
