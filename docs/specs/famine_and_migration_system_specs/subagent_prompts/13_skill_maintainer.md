# Conditional Prompt for `chaosx_skill_maintainer`

Spawn with `fork_context=false` only if implementation reveals a reusable workflow that is not already covered.

Potential reusable topics:

- exact state-to-state civilian population transfer
- active-state registry processing without global scans
- mixed-cause Deaths reason ownership
- shared humanitarian adapter contracts
- selected-state crisis decision lifecycle

Audit existing skills first. Update the closest existing skill when the workflow belongs there. Create a new skill only when the workflow is distinct and reusable across future systems.

Do not place famine-specific historical profiles, event connections, or one-off identifiers in a skill.

Write the handoff under:

```text
docs/plans/famine_and_migration_system_plans/subagent_handoffs/skill_maintenance.md
```
