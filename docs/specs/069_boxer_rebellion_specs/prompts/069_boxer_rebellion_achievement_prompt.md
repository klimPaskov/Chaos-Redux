# Event 069 achievement implementation prompt

Work on Chaos Redux Event 069, Boxer Rebellion, using the current repository instructions and `docs/specs/069_boxer_rebellion_specs/`.
Read `README.md`, `research/source_review.md`, the relevant specification parts, and all mandatory current source and skill files in full before editing your assigned surface.
The package is a proposed design with explicitly incomplete source review, no executed project subagents, and no HOI4 MCP validation from its authoring run.
Do not treat its expected results as evidence.
Preserve `chaosx.nr69.1`, use three-digit event filenames, keep the event Minor Fire-Once with Chaos level 1, and preserve Domestic Unrest Severe plus Diplomacy Medium membership.
Record adopted design, implementation, and validation separately.
Do not silently omit, merge, simplify, replace, or defer a requirement.
Report any unavailable source, tool, identity, asset, or engine capability honestly.


Implement and validate the twelve achievement designs in Part 14 against the actual Event 069 gameplay and historical receipts.
Read the current achievement registry, player-identity policy, eligibility rules, asset workflow, supplied templates, and relevant prior examples before assigning IDs or writing triggers.
Use the single root Chaos Redux achievement registry.
Do not create a second registry or reuse another achievement's unique ID.

Each achievement must preserve its difficult combination of starting context, player action, outcome, and verification period.
Implement the evidence when the fact occurs.
Do not infer a former coalition, preexisting privilege, surrendered equipment allocation, or civilian protection promise solely from the final map.

Enforce natural-play eligibility and the persistent disqualification for manual Event 069 scenarios, debug setup, and test fixtures under the project's actual rules.
Changing tag, closing a crisis, or relaunching the scenario must not clear that history.
Subjects and temporary release shells must not multiply independent-government or coalition counts.
A token declaration or unreachable foreign observer must not satisfy the credible-intervention requirement.

Audit every achievement for release-and-reannex loops, repeated repair rewards, artificial privileges, joining after success, fake coalition contributions, duplicate mission receipts, hidden protection breaches, and external conditions with no feasible route.
Where the design needs a physical deployment threshold, derive and test it against the installed unit data rather than selecting an unsupported variable.

Write final names and concise descriptions only after the mechanics and eligibility are correct.
The working labels A01 through A12 are design references, not final player-facing titles.
Provide a trigger-to-spec crosswalk showing every condition, historical receipt, exclusion, and hold period.

Produce the achievement images through the exact current asset skill.
Create a distinct native 64 by 64 subject for each achievement using the required ImageGen workflow and supplied template composition.
Build completed, grey, and not-eligible variants through the deterministic approved pipeline.
Keep the fixed overlay and template pixels unchanged and run the required strict decoded-image checks.
Place final DDS files in the engine's root achievement directory with the exact full runtime ID naming convention.
Do not resize a focus or idea icon into an achievement.

Return the final registry changes, historical-tracking dependencies, all three asset states per achievement, source and native-size review, exploit fixtures, actual validation evidence, and any unresolved implementation or sourcing blocker.
Do not award or report these achievements as tested merely because their conditions are listed in the spec.
