# Event 069 parent implementation prompt

Work on Chaos Redux Event 069, Boxer Rebellion, using the current repository instructions and `docs/specs/069_boxer_rebellion_specs/`.
Read `README.md`, `research/source_review.md`, the relevant specification parts, and all mandatory current source and skill files in full before editing your assigned surface.
The package is a proposed design with explicitly incomplete source review, no executed project subagents, and no HOI4 MCP validation from its authoring run.
Do not treat its expected results as evidence.
Preserve `chaosx.nr69.1`, use three-digit event filenames, keep the event Minor Fire-Once with Chaos level 1, and preserve Domestic Unrest Severe plus Diplomacy Medium membership.
Record adopted design, implementation, and validation separately.
Do not silently omit, merge, simplify, replace, or defer a requirement.
Report any unavailable source, tool, identity, asset, or engine capability honestly.


Implement the adopted Boxer Rebellion specification in the repository using the staged sequence in Part 16.
This is a large rework with shared-system dependencies.
Do not attempt to complete it by replacing the old event with one larger popup or by marking unimplemented branches as future flavor.

First close the reading and capability gaps listed in the package.
Inspect the current offline wiki, installed vanilla documentation, approved mod references, exact map, country and character definitions, current shared helpers, and all applicable skills.
Discover and use the actual project subagent and HOI4 tool schemas.
Do not invent a tool call, infer that a service is healthy from its name, or describe manual work as a named agent's execution.

Preserve the user concept and its three evolutions.
Replace the legacy CHI-only recipient, fixed fascist civil war, and arbitrary manpower deletion.
Build the dynamic Chinese theater, actual foreign-interest registry, local networks, two public values, independent Chinese stances, real military commitments, temporary coalition, mandates, differentiated victories, and bounded aftermath.
Keep coalition membership separate from engine faction membership and protect unrelated wars.

Implement the full decision and mission catalogs through their shared action contracts.
Build a viable origin-aware Boxer country with actual territory, capital, industry, technology, forces, institutions, and sourced identities.
Do not overwrite a living carrier or meaningful focus tree.
Implement B01 through B09 and all three political routes, including their military, industrial, diplomatic, integration, ritual, and postwar consequences.
Keep at most three Event 069 national spirits active at once.

Implement evolutions with proper enable checks, timing, actor context, logging, and zero Chaos for the evolution state itself.
Use real scoped consumers for ritual effects and preserve ordinary logistics and civilian consequences.
Implement the Chinese pact without fake ceasefires or automatic national reunification.

Complete all shared-system connections from Part 12 after inspecting their actual runtime interfaces.
Implement the four manual scenario types and sixteen intensity profiles in the shared framework, including prior-fire compatibility, active-crisis reuse, finite setup grants, and cleanup of launch bypasses.
Implement achievements through persistent historical evidence and the root registry.

Use the separate asset, super-event, achievement, and decision-mission prompts for their bounded work.
Give workers exact files, inputs, exclusions, and evidence outputs.
Keep shared event-log, settings, registries, and integrated runtime placement parent-owned unless an explicit coordinated grant says otherwise.
Use narrow worktrees and honor the user's existing merge policy.

Use actual event, focus, GUI, technology, map, and probability inspections where relevant.
Inspect rendered artifacts at their intended size and retain matched source and scenario identities.
Run the named improvement loop after a meaningful implemented tranche, resolve its handoff, and then complete the independent completion review.
Do not count source-only inspection as equivalent to required MCP or runtime evidence.

Finish the adopted presentation, localisation, country assets, unique licensed super-event audio, logs, details, settings, scenarios, documentation, and catalog alignment.
Only update the authoritative XLSX when the real implementation status supports it, then run the export script.
Never edit exported catalog CSV files directly.
Promote durable evidence before permitted temporary-workspace cleanup.

The final report must state the actual revision, changed files, route and requirement coverage, test and tool evidence, subagents actually used, and every omitted, renamed, merged, simplified, replaced, deferred, or blocked requirement.
Do not mark the event fully complete while an adopted requirement or required proof remains unresolved.
