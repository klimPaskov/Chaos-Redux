# Achievement implementation prompt

Implement all seven Event 036 achievements from `13_achievements.md` without weakening their proof requirements.

Read `AGENTS.md`, `chaos-redux-events`, `chaos-redux-event-assets`, the current achievement registry, current achievement localisation, and the Event 036 specs before editing.

Use these stable IDs:

- `chaosx_036_the_reservation_holds`
- `chaosx_036_a_convention_against_the_convention`
- `chaosx_036_small_state_large_ledger`
- `chaosx_036_without_the_incident`
- `chaosx_036_the_secret_article`
- `chaosx_036_masks_before_missiles`
- `chaosx_036_all_articles_invoked`

## Tracking rules

Use durable Event 036 posture history, treaty receipts, CBRN action records, victim and retaliation proof, project contribution receipts, project source-isolation proof, and achievement-specific snapshots.

Do not infer first use from a current idea.

Do not infer project participation from convention membership alone.

Do not infer source isolation from the absence of a visible popup.

Snapshot the small-state factory and major status when the relevant project is selected.

Preserve disqualifiers across save and reload.

A disqualified route must never become valid again because an idea was removed or a posture changed later.

## Asset rules

Route the seven achievement masters to `chaosx_icon_artist` under the Event 036 asset brief.

Each achievement needs its own normal, grey, and not-eligible DDS states.

Do not resize a decision, idea, or category icon to satisfy the achievement family.

Achievement filenames must match the full IDs and remain directly under `gfx/achievements/`.

## Localisation rules

Write final player-facing names and descriptions from the direction in the achievement spec.

Explain the challenge clearly without exposing raw flags, internal project points, or source-event implementation checks.

Avoid meta wording, update history, debug terms, and hidden future outcomes.

## Validation

Create a route-by-route proof table that lists trigger conditions, disqualifiers, tracking identifiers, asset filenames, and tested false-positive cases.

Test each achievement through the acceptance scenarios.

Confirm that thermonuclear use cannot satisfy `all_articles_invoked`, valid retaliation does not disqualify `the_reservation_holds`, non-contributors cannot unlock `without_the_incident`, and current posture alone cannot satisfy a route-history requirement.

Update documentation and the completion report.

Do not mark the achievement package complete while any icon, localisation key, disqualifier, or persistent proof is missing.
