# Event 54 Coding Prompt

Implement Event 54, Gift from Scientists, from the complete specification pack at `docs/specs/054_gift_from_scientists_specs/`.

## Required preparation

Read `AGENTS.md`, every specification part, both matrices, the acceptance criteria, the catalog handoff, and the relevant current event, cluster, technology, event-log, Chaos Meter, achievement, asset, and documentation sources before editing.

Read the repository skills for events, event assets, subagents, and improvement review. Read the decision and focus skills only when an existing dependency requires them. Consult the mandatory offline Paradox wiki pages and installed vanilla documentation for technologies, events, effects, triggers, scopes, localization, on actions, AI, achievements, and graphical assets. Inspect at least one current vanilla technology-grant precedent and the closest Chaos Redux custom-technology provider precedent.

Use the installed HOI4 MCP routes for every supported surface. Begin event work with `hoi4.event_inspect`. Use `hoi4.event_render` and `hoi4.event_compare` around the completed chain. Inspect the active technology graph with `hoi4.tech_inspect`, render every affected folder or branch with `hoi4.tech_render`, and use `hoi4.tech_compare` after source changes. Route every weighted candidate, member-participation, or AI probability surface through the required probability audit workflow.

## Event result

Keep `chaosx.nr54.1` as the canonical entry event. Register Event 54 as Minor Repeatable with Chaos level 1 and default weight behavior from the shared event system.

One firing resolves one bounded global transaction. Every valid research participant builds its own current safe missing-technology pool and draws independently. Baseline grants one technology. Evolution I grants up to three. Evolution II grants up to five and admits owner-registered Chaos technologies. Evolution III grants up to ten from the same expanded pool.

Draw without replacement. Revalidate the recipient and pool after every grant. Stop only that country's sequence when its pool is exhausted. Never substitute excluded content to fill the requested count. A rejected owner candidate is removed from the current attempt, and the slot is rerolled while another safe candidate remains.

Allow ahead-of-time ordinary technologies. Exclude doctrines, hidden setup nodes, debug technologies, country bootstrap markers, invalid DLC nodes, unsafe project gates, owner-locked rewards without registration, and every branch candidate that would leave an incompatible technology state. Respect the active graph and current DLC set.

## Owner registration contract

Implement the provider contract in the dedicated technology-registry prompt. Event 54 owns selection, receipts, reporting, and final recipient safety. Each owner owns candidate registration, owner-side eligibility, compatibility data, declared dependency packages, optional callback behavior, and lifecycle preservation.

A grant through Event 54 must not mark the owner event fired, complete the owner project, advance an owner stage, create owner assets, or award unrelated owner rewards. Event 27 retains all doctrine grants.

Document any reusable public owner API in its authoritative event-owned documentation. Add only the needed API index entry to a shared registry. Avoid a hardcoded central switch when owner-side registration can supply the candidate.

## Reports, history, and persistence

Commit every selected grant before opening human reports. Suppress the normal technology popup flood. Give each human recipient one consolidated country report with its complete grant list or a clear pool-exhaustion result. AI countries receive the same grants without a popup.

Create one normal Event 54 history entry for the global firing. Do not create one history or pacing transaction per country or per technology. Record actual evolution milestones through the shared evolution pipeline. Keep cluster history, normal event history, and evolution history distinct.

Persist committed results and report receipts across save and reload. Reopening a pending report must never reroll or regrant. Country annexation or transformation during the bounded transaction must fail closed and cannot corrupt later recipients.

## Chaos feedback

Implement the guarded milestone map from Part 4. The first completed world transaction adds 2 Chaos once. The first qualifying Evolution I firing adds 1 once. The first external registered custom-technology grant adds 1 once. The first qualifying Evolution III firing adds 2 once. Evolution state alone adds no Chaos. Repeat firings cannot farm any milestone.

Do not duplicate Chaos from wars, deaths, contamination, or other shared systems.

## Scientific Research cluster

Replace the incomplete Scientific Research placeholder after confirming its stable ID. The cluster is Minor Repeatable, unlocks at Chaos level 2, and contains Events 16, 24, 27, 54, and 60 with the accepted per-member severities.

Preserve mixed event types and Event 27's separate Medium membership in Military Preparation. Each member checks its own enable state, event level, fire-once state, evolution state, and event-specific eligibility. One cluster firing creates one pacing commitment while participating members retain their own effects, history, and event-state changes.

Implement the participation model from the spec, then run the named probability scenarios before accepting it.

## Assets and achievements

Create and wire the report-event image from the asset prompt. Implement the two achievements from the achievement prompt with their full tracking, disqualifiers, localization, icons, docs, and failure-closed provider gates.

Borrowed Future cannot become normally available until at least two owner providers expose the complete grant and use-milestone contract.

## Completion work

Write final in-world localization from the specification direction. Keep event reports, Event Details, evolution entries, cluster details, achievement text, technology names, and catalog wording aligned.

Update event documentation and the authoritative catalog workbook only after implementation facts and final localization exist. Regenerate the CSV exports through the repository exporter.

Run the scripted-system, localization, probability, spreadsheet, asset, and completion review roles defined by the package. Review every returned patch or report. Fix all blockers before completion.

Do not use an unapproved fallback, placeholder, reduced technology pool, static candidate list that owners cannot extend, or partial cluster implementation. Report every omission or unresolved dependency clearly.
