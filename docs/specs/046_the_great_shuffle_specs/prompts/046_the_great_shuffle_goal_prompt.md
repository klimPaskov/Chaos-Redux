# /goal Event 046: The Great Shuffle

Implement Event 046 to the fullest extent of `docs/specs/046_the_great_shuffle_specs/`. Read every spec and matrix. Use `prompts/046_the_great_shuffle_coding_prompt.md` as the main handoff and the separate registry, probability, asset, achievement, localisation, and catalog prompts for those surfaces.

Follow `AGENTS.md`, the relevant project skills, the mechanics guide, the shared registries, the offline wiki, vanilla documentation. Use `chaosx_repo_explorer` with `fork_turns="none"` only when file locations, patterns, dependencies, vanilla precedents, or edit order remain unclear. Use the required HOI4 MCP event inspect, render, lint, and compare workflow.

Keep Event 46 a Chaos level 1 Minor Repeatable event. One firing is one global transaction. Generate independent new values for each valid scope. Never swap values, conserve totals, compensate earlier results, preserve rankings, restore the old world, or let previous firings alter later selection or ranges. Human countries stay eligible. Special actors use shared classifiers and owner adapters.

Build an Event 46-owned allowlist registry and logical family-atomic transaction. Fix selected results before the first write. A pre-commit failure leaves the family untouched. A started commit finishes the exact plan after save and load. Never reroll during recovery or claim unsupported rollback. Do not scan arbitrary memory or add a periodic whole-world on-action. Keep owner adapters in owner files. Share helpers only when they are neutral and documented.

Implement the Baseline and five evolution capabilities. Evolutions I through IV can activate before the first firing through normal pacing. Activation gives zero Chaos. Disabled evolutions cannot record or unlock content. Evolution V activates at 1000+ and remains reachable through normal selection or the bounded pre-freeze route. It means every proven safe family, not every number in the save. Unproven graph, relationship, map, and identity surfaces remain protected or conditional.

Protect framework, history, identity, settings, and achievement state. This includes Chaos, Deaths, atmospheric and diplomatic ledgers, humanitarian systems, event-system data, clusters, scenarios, Event Logs, world-end data, stable IDs, objects, registries, and lifecycle targets. Population rewrite must support gains and losses without recording Deaths, migration, births, or unintended national manpower. Suppress setter side effects that would double count the transaction.

Create one bounded direct Chaos entry and one Event Log entry per successful transaction. Every human receives one concise report from the same shared result. AI receives no favorable roll or compensation. Create Randomizations with Event 46 as Medium and prevent one-member duplication. Add the accepted Domestic Unrest memberships while keeping Event 21 in Wars. Verify Event 2 and resolve Event 43 from authoritative source. Preserve multiple cluster relationships. Edit only the XLSX and regenerate CSV exports.

Implement all six achievements, the report image, achievement variants, final localisation, Event Details, evolution records, the Event 046 document, and catalog alignment. Do not create a custom GUI, decisions, focus tree, super-event, portraits, flags, 3D, or animation. Use the scripted-system, probability, asset, localisation, spreadsheet, improvement, and completion subagents with explicit prompts and `fork_turns="none"`. Run the probability baseline before weight changes and compare the same scenarios afterward.

Near completion, spawn `chaosx_improvement_loop_planner` and resolve its output, then run the completion audit. Keep iterating until every accepted core family, report, evolution, cluster relationship, achievement, asset, document, and audit is complete. Report blocked conditional families and simplifications. Do not claim completion before the full pack is satisfied. Use Needs Testing unless the user approves Playable. Provide a concrete completion report with evidence, blockers, and no hidden fallback.
