# Goal prompt

Implement Chaos Redux Event 050, The Great Embargo, from every file under `docs/specs/050_the_great_embargo_specs/`.

Read the complete package, `AGENTS.md`, relevant project skills, required offline wiki and vanilla references. Use the installed HOI4 MCP event and probability tools for every supported surface. Record missing routes as blockers.

Preserve the accepted identity: Event ID `50`, entry root `chaosx.nr50.1`, Minor Repeatable, Chaos level 1, Negative Economy cluster, Medium member severity. CSV files are export snapshots and must not be edited. Reconcile the authoritative XLSX after final wording exists, then run the catalog exporter. Do not invent the missing cluster ID or copy the incomplete cluster export's conflicting fields.

Each ordinary firing creates one fresh crisis against a valid player country or major power. Exclude active targets, special or nonhuman Chaos actors, countries outside normal civilian systems, and invalid hosts. Evolution II may create two or three distinct ledgers, with a hard cap of three active crises. Each ledger must isolate all target, coalition, Pressure, response, resolution, and cleanup state.

Expose only Embargo Pressure as a persistent custom value. Keep dependence, coalition contribution, fatigue, route quality, exposure, and world fragmentation internal. Coalition effectiveness must value important suppliers, shipping states, financial centers, land corridors, and core enforcers. Economic damage must scale with the target's resources, fuel, convoys, routes, stockpiles, and industrial exposure. Avoid unsupported trade precision.

Implement one compact decision category with no more than five primary actions and one active mission. Cover Self-Sufficiency, Smuggling Networks, Neutral Intermediaries, Diplomatic Concessions, Defy the World, Resource Seizure, and Secure Replacement Supply. Use real costs, targets, deadlines, outcome bands, AI, exploit controls, and cleanup. Do not create a dedicated scripted GUI.

Implement Evolution I, Secondary Sanctions, at 400+ Chaos and Evolution II, The Embargoes Spread, at 800+ Chaos. Evolutions use proper pacing during an existing crisis, log once, respect enable state, and add zero Chaos by themselves. Event-owned Chaos changes occur only at concrete one-time milestones. Generic wars, peace, deaths, annexations, faction changes, and contamination remain shared-system owned.

Retain full event-owned behavior without DLC-specific embargo mechanics. Native mechanics can reinforce the crisis after their contract is verified. Do not promise cancellation of unsupported active agreements. Keep every shared system inside its owner boundary.

Create and wire exactly the static asset inventory in the asset matrix, including report scenes, category art, separate transparent icon families, achievement triplets, final DDS files, manifests, and runtime wiring. Any additional surface requires an accepted source-spec change.

Implement all five achievements, event and evolution history, Event Details, actor handling, reports, docs, AI, localisation, cluster behavior, and workbook alignment. Run every specialist and audit route required by the package. Each weighted patch needs a baseline audit and `hoi4.probability_compare` against the same scenarios.

Process only registered active crises and bounded participant ledgers. Do not add unrestricted all-country scans. Do not use placeholders, silent fallbacks, copied generic content, fake metrics, tiny reward dust, or unreported simplifications. Preserve unrelated changes. Do not claim live game proof unless the user explicitly invokes the autonomous debug playtest skill and the capability exists.

Finish every accepted surface and provide a concrete completion report. List every unresolved blocker, missing capability, skipped meaningful validation, fallback, or deviation. Claim no simplification only when final evidence and audits support it.
