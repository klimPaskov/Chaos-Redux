# Goal: Fully Rework Chaos Redux Event 052 Intel Leaked

Implement Event 052 from `docs/specs/052_intel_leaked_specs/`.

Read all six files under `specs/`, every matrix, handoff, research note, and quality file, plus:

- `prompts/052_intel_leaked_coding_prompt.md`
- `prompts/052_intel_leaked_decision_mission_prompt.md`
- `prompts/052_intel_leaked_asset_prompt.md`
- `prompts/052_intel_leaked_achievement_prompt.md`

Follow `AGENTS.md`, `chaos-redux-events`, `chaos-redux-decisions-missions`, `chaos-redux-event-assets`, `chaos-redux-improvement-loop`, and `chaos-redux-subagents`. Read the required offline Paradox wiki pages, installed vanilla documentation, and local precedents before coding.

Preserve Event ID 52, namespace `chaosx.nr52`, Minor Repeatable type, Chaos level 1, Intelligence cluster 10 membership, and Low severity. Replace the existing permanent 100-point intelligence grant with temporary, reversible, event-owned exposure. Baseline must compromise exactly one valid source-player country or random major. Total Compromise may affect several targets only through its 800+ evolution profile.

Implement one public value, Exposure. Keep archive depth, confidence, recipient Reliance, personnel-risk arithmetic, and weighted scores hidden. Every ordinary foreign government must receive meaningful temporary intelligence while Exposure is positive. Named exploiters must be bounded and selected through strategic relevance. Every applied advantage needs exact expiry and cleanup.

Implement all archive domains, rare profiles, foreign exploitation families, operative and network risk, phased damage control, reconstruction, Trace the Source, Poison the Leak, false deployments, success, partial success, failure, AI equivalents, DLC-safe behavior, target invalidation, repeat-safe cleanup, and separate state for every Total Compromise target. Preserve the action and mission visibility budgets and the four-cost hard limit.

Implement Deep Files at 400+ and Total Compromise at 800+ with paced activation, pre-fire evolved openings, mid-incident escalation, shared evolution logging, enable controls, and zero Chaos from evolution activation itself. Implement every mapped Event 52 Chaos source and reversal through one-shot proofs. Prove that generic war, tension, death, annexation, ideology, and buildup sources are not double counted.

Add Event 52 to Intelligence cluster 10. Preserve one cluster pacing transaction. Add only bounded public-adapter interactions with Event 039, Event 011, and other stable systems. Do not depend on unavailable catalog events.

Complete event registration, eligibility, manual firing, Event Details, actor mapping, history, evolution previews, cluster display, final localisation, scripted localisation, decisions, missions, AI, achievements, documentation, and catalog alignment. Implement all authorized assets through the asset prompt. Hydrate and inspect existing LFS assets before replacing them. Update only the authoritative XLSX and run the catalog exporter.

Use `hoi4.event_inspect`, `hoi4.event_render`, and `hoi4.event_compare` for the event chain. For every weighted surface, spawn `chaosx_ai_probability_auditor` with `fork_context=false`, begin with `hoi4.probability_inspect`, run the named scenario evaluations and sweeps, and finish with `hoi4.probability_compare`. Run the decision, localisation, asset, and completion audits.

Near completion, spawn `chaosx_improvement_loop_planner` with `fork_context=false`. Resolve its addendum or closure handoff before claiming completion. Keep iterating until the goal is accomplished to its fullest extent. Do not use silent fallbacks, simplifications, temporary substitutes, or good-enough approximations. Do not claim completion until the final files satisfy the specification and a concrete completion report proves the result.
