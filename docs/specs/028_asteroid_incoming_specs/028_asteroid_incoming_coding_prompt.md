# Coding-agent prompt for Event 028: Asteroid Incoming

Implement the complete Event 028 specification under `docs/specs/028_asteroid_incoming_specs/`. Read every file in the package before editing. Also read `AGENTS.md`, the event, decision, asset, super-event, improvement-loop, subagent, and relevant shared-system skills, the required offline wiki pages, current vanilla documentation, installed vanilla precedents, and existing Chaos Redux patterns.

Treat the supplied specification as acceptance criteria. Do not replace a mapped mechanic, percentage, armour value, decision phase, achievement, asset, report, or log surface with a smaller fallback. Report any blocker before claiming completion.

## Required implementation result

- Register Event 28 as a global fire-once Major with Calm World baseline eligibility and full shared major pacing.
- Construct exactly three valid distinct country and state targets plus miss. Show `N/A` when that pool cannot exist.
- Lock selected coordinates for two days, preserve them through control changes, and support one target-country preparedness event.
- Resolve the main center and three shortest land-adjacency rings with the accepted population losses, building damage, exact Deaths registration, capital relocation, and permanent nonradioactive crater state.
- Aggregate one report per affected country.
- Create Dust Load from actual destruction, visible stages, Air Cleanliness history, national protection, bounded Event 013 aftermath calls, and clean decay.
- Implement Global Fragmentation at 600 Chaos and Extraordinary Minerals at 800 Chaos with independent enable behavior and correct evolution logs.
- Apply plus 100 percent existing land armour from the main crater and plus 20 percent per fragment site, uncapped, transferable by current control, and free of stale duplicates.
- Implement the phased ordinary decision category, missions, AI, cleanup, and exploit controls.
- Wire Event History, Event Details, evolution views, dynamic target snapshots, news, reports, super-event, settings-aware audio, achievements, docs, and authoritative workbook alignment.
- Preserve strict separation from nuclear use, condemnation, retaliation, radioactive fallout, and nuclear history despite reused map visuals.

## Required tooling and subagents

Use the HOI4 MCP event inspection, render, and comparison route for the event chain. Use map inspection for adjacency and state behavior. Use probability tools through `chaosx_ai_probability_auditor` for every weighted surface. If a required route is unavailable, record the exact blocker and do not treat source-only review as equivalent.

Spawn project subagents with `fork_context=false` and complete prompts when their route is relevant.

- Repo explorer for uncertain file and precedent mapping
- Scripted-system architect for reusable transaction, ring, aggregation, dust, and crater-control helpers
- Decision and mission auditor after the category is implemented
- AI probability auditor before and after weighted patches
- Generated event-art and icon workers for all accepted assets
- Super-event text and audio researchers before final localisation and sound wiring
- Localisation auditor after broad text is complete
- Spreadsheet worker after final player-facing wording exists
- Event completion auditor before the completion claim

Do not route shared Event Details or settings work to the event UI worker. The accepted design uses no dedicated Event 028 window.

## Source architecture

Centralize tuning values and repeated logic. Use exact state population loss and Deaths contracts. Build all main and fragment footprints before mutating states. Merge overlaps by strongest profile. Keep reports country aggregated. Use bounded event-driven or monthly updates, not a new daily all-country scan. Derive mineral modifiers from current registered site control and replace stale values.

Reuse Event 013 only for validated secondary disasters. Do not let it own or duplicate the main impact.

## Assets and super-event

Produce every image and icon from the asset prompt. Complete the sourced quote, remark, unique licensed musical cue, final WAV, sound definitions, volume wrappers, image sprite, dynamic text, and settings-aware playback from the super-event prompt. Place no placeholder, default, mismatched, or unlicensed asset in a completed package.

## Documentation and catalog

Write the final Event 028 documentation from the implemented behavior. Update only the authoritative catalog XLSX, then run the repository CSV exporter. Replace the stale minor-repeatable Event 28 row. Keep spreadsheet detail wording aligned with Event Details.

## Completion proof

Run the scenarios and acceptance checks in Part 10, the tuning matrix, the decision prompt, and the AI prompt. Provide files changed, systems touched, meaningful scenario evidence, asset status, super-event research and rights status, probability comparisons, docs and catalog alignment, and every remaining blocker or simplification. Do not mark the event complete while an accepted route, asset, audit, or prompt requirement remains unresolved.
