# Goal prompt: Implement Event 028, Asteroid Incoming

Implement Event 028 from every file under `docs/specs/028_asteroid_incoming_specs/`. Read the full package first and treat it as the source design and acceptance contract. Follow `AGENTS.md`, relevant Chaos Redux skills, required offline wiki pages, current vanilla documentation, and verified project precedents.

Deliver the global fire-once Major event. Build exactly three valid distinct country and state targets plus a genuine miss option. Show `N/A` when three pairs cannot exist. Lock chosen coordinates for two days, preserve them through state and tag changes, give the target one preparedness choice, and relocate a destroyed capital to a valid backup.

Make the main state a permanent nonradioactive crater with all civilian population and buildings removed. Apply the accepted 50 percent, 25 percent, and 5 percent civilian losses through three shortest land-adjacency rings, with severity-scaled building, infrastructure, railway, supply, and military-site damage across borders. Process each state once at the strongest profile. Register actual losses through Deaths and send one consolidated report per affected country.

Create Atmospheric Dust Load from actual deaths, destruction, affected states, fragments, and supported secondary disasters. Implement visible stages, production and supply penalties, weather consequences, Air Cleanliness history, national protection, bounded Event 013 calls, mitigation, decay, reports, and cleanup. A miss creates no impact, fragment, dust, crater, evolution, or hidden punishment.

Implement Global Fragmentation at 600 Chaos with the three-to-six fragment plan, two damage rings, overlap handling, grouped reports, and added dust. Implement Extraordinary Minerals at 800 Chaos only when fragmentation applies. The main-crater controller receives plus 100 percent to existing land-division armour. Each controlled fragment site adds plus 20 percent. Stacking remains uncapped. Benefits transfer by current control without stale or duplicate modifiers.

Implement the phased ordinary recovery category and missions. Do not create a dedicated Event 028 window. Use exact state targets, clear blocked reasons, no more than four spendable cost types, meaningful logistical costs, AI reserve floors, cleanup, and exploit protection. Complete all five achievements with persistent tracking, disqualifiers, hold resets, final localisation, and full icon triplets.

Reuse thermonuclear and nuclear-scale map effects only for presentation. Never record nuclear use, condemnation, retaliation, radioactive fallout, nuclear Chaos, or nuclear Deaths. Keep asteroid sources distinct in every shared ledger.

Complete Event History, Event Details, evolution rows, dynamic target snapshots, reports, news, super-event text and image, verified quote and remark, unique licensed music, final WAV, settings-aware playback, all accepted icons, documentation, and workbook alignment. Replace the stale minor-repeatable Event 28 row, then regenerate CSV exports from the XLSX.

Use required HOI4 MCP event, map, and probability routes, plus other routes when their surface requires them. Spawn project subagents with `fork_context=false` and complete prompts for reusable systems, decisions, probability, assets, super-event research, localisation, spreadsheet alignment, and final audit. Record the exact blocker when a required route is unavailable. Source-only review is not equivalent evidence.

Run every scenario and acceptance item in Part 10, the tuning matrix, and the AI prompt. Keep iterating until the full package is satisfied. Do not claim completion because the visible impact works. Completion includes gameplay, AI, decisions, assets, audio, logs, achievements, docs, catalog, audits, transfer rules, and cleanup. Report every simplification, omission, fallback, placeholder, unresolved audit, and blocked asset. If none exist, say so with concrete task-specific evidence.
