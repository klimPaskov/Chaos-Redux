# Asset production prompt: Event 071 Persia

Produce the complete accepted visual and unit asset package for Event 071 using `chaos-redux-event-assets` and all relevant specialized skills. Read `docs/specs/071_persia_specs/071_persia_index.md`, parts 2, 4 to 11, 14, 16, and 17, plus the coding prompt and country-package matrix. The specifications contain design direction, not finished localisation or final art.

## Inspect before producing

Read the actual repository asset rules, converters, existing Event 071 assets, Iranian and regional assets, live consumers, and canonical reference catalog. The planning archive contained no asset reference images, converters, or installed game files. Do not claim they were inspected during planning.

The canonical reference root recorded by the supplied skill is `C:\Users\klimp\OneDrive\Documents\Paradox Interactive\Hearts of Iron IV\mod\chaos_redux\.agents\skills\chaos-redux-event-assets\assets\vanilla_reference`. Resolve its current actual repository mount. Inspect the matching focus, idea, decision, mission, achievement, flag, portrait, news, report, super-event, unit, and interface references before creating each family.

Use the actual consumer's required format and the prescribed conversion tools. Preserve source PNGs, prompts, generation provenance, licenses, processed sources, native-size reviews, and final paths. Do not use a visually plausible export that fails the required format checks.

## Identity and political assets

Create the opening imperial identity and the three routes' required ideology and government variants. Reuse valid existing identities and characters. Do not resurrect ancient rulers or invent a historical claimant. Coordinate living real characters and fictional or institutional representations with `chaosx_portrait_creator`.

Every flag uses ImageGen. Historical designs first require a reliable design reference and a faithful flat reconstruction. Alternate-history flags must be documented as fictional. No cloth, scenery, perspective, gradients, fake writing, or improvised heraldry presented as historical. Compare geometry, color, orientation, and symbols before generating the 82 by 52, 41 by 26, and 10 by 7 ladder.

All portraits are owned by the portrait worker. Grounded subjects require identity-preserving sourced head-and-shoulders material. Produce 156 by 210 leader output and separate 65 by 67 adviser output only for actual adviser consumers. Final styling follows the user's RunPod workflow. Institutional art must be visibly institutional, not a fake portrait of an invented historical person.

## Required 2D families

Produce distinct native families for focus icons, the three staged national spirit institutions, decisions and missions, GUI elements, guard counters and any new equipment consumers, news and report scenes, super-event scenes, and all achievement states. Do not resize one icon family into every other family.

Focus icons target 94 by 86, idea icons 64 by 64, decision and mission icons 32 by 32, news 397 by 153 in black and white, reports 210 by 176, and super-event art 457 by 328 under the supplied rules. Verify actual consumers before conversion.

The political routes need a coherent visual distinction without overwhelming detail. Achaemenid direction uses charters, regional administration, roads, and an imperial assembly. Sasanian direction uses commands, fortified approaches, armor, and a central court. Modern direction uses industry, ports, contracts, and a mechanized bloc. All depict a modern alternate-history campaign.

Persepolis scenes must distinguish conserved ancient remains from new ceremonial facilities. The opening scene must show the actual restoration government and mobilization. Do not depict an emperor when the current government has not adopted a crown.

## Achievement triplets

Create one original transparent subject for each of the eight IDs in part 17. Use the inspected achievement references for framing and density. Do not ask ImageGen for the frame, square background, red cross, text, or three independent state artworks.

Preserve `achievement_template.png`, `achievement_template_grey.png`, and `overlay.png` byte-for-byte and verify their hashes against the asset skill. Derive grey and not-eligible source states deterministically from the new subject and unchanged overlay. Use the required achievement processor with a complete source triplet. Do not independently redraw or reposition the state layers.

Final completed, `_grey`, and `_not_eligible` DDS files go directly under `gfx/achievements/` using the exact registered achievement ID. Retain the three composited review PNGs, native-size contact sheet, strict DDS decode checks, and pixel-equality evidence. Parent review is required before final wiring.

## GUI assets

Follow part 14 and the GUI worker's accepted reference images. Exact-state map pieces must derive from verified native geometry. Do not create an approximate historical map or flattened fake interface. Keep native controls functional.

The rich Event 071 category has a meter, selected-state puzzle, and custom panels, so do not add a decorative category picture beside it. If a later approved simple category needs a picture, first inspect its exact reference directory and create the required labeled contact sheet if missing. Update the reference README and catalog before production.

Static assets are sufficient for this plan. Do not add animation to increase the asset count. An approved later animation needs its prescribed frames, native consumer, static fallback, and frame-animation validation.

## Immortals 3D and sound

Delegate the actual body, firearm, rig, weights, animations, textures, and export work to `chaosx_3d_model_pipeline` through the supported runtime. Read the dedicated 3D pipeline fully. Follow its separate fresh Meshy 7 weapon-free body and firearm tasks, then the firearm-safe Blender rigging and action route. Resolve the older contradictory gun-production wording with the skill maintainer. No unauthorized paid generation or retry.

Use allowed modern professional, game, tabletop, or concept-art references with documented rights. Do not use a prohibited historical photo or museum object as direct 3D input. Respect NoAI and no-derivative restrictions. A reference refinement uses the permitted single-source process.

Validate actual vanilla scale, skeleton, attachments, hand grip, weapon control, action list, firing direction, movement, death, textures, entity, unit consumer, and sound routing. The counter's native green and dimensions must come from inspected references, not an arbitrary color choice.

Audio comes from licensed external material or an appropriate documented existing sound. Do not generate audio. Check actual country-tag voice routing before promising guard-specific voices. Do not create a duplicate country to work around sound scope.

## Manifest and completion

For every asset record its consumer, family, dimensions, source or generation method, rights, source path, processed path, final path, review state, conversion evidence, and wiring status. Mark missing assets blocked or planned. A placeholder never counts as a final asset.

Coordinate ownership with the icon artist, generated-event-art worker, portrait creator, 3D worker, and super-event researchers. Return the manifest, contact sheets, final files, and evidence under the repository's accepted paths. Report exact unresolved blockers. Do not call the asset package complete until every required consumer has a reviewed final asset and the prescribed validation evidence.
