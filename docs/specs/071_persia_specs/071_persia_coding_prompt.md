# Implementation prompt: Event 071 Persia

Implement the complete design in `docs/specs/071_persia_specs/`. Begin with `071_persia_index.md`, then fully read all eighteen sequential specification parts and the companion country matrix, research notes, source receipt, parent review, and role handoffs. The six prompt files are separate handoffs. Do not treat this prompt as a substitute for the specifications.

The work is Event 071, Persia, root `chaosx.nr71.1`, Minor Fire-Once, Chaos level 1, Formables Medium. The current catalog status is To Be Reworked. Use the normal status workflow only after evidence supports a change.

## Read and inspect before editing

Follow `AGENTS.md`, `README.md`, `CHAOS_REDUX_MECHANICS.md`, the supplied dynamic effects and triggers references, and all applicable skills. Required skills include event planning, events, focus trees, decisions and missions, scripted GUI, event assets, super-events, 3D model pipeline, frame animation when used, ComfyUI for the prescribed portrait route, debug and playtest, subagents, and the improvement loop.

Read the actual repository sources, all mandatory offline wiki pages, installed vanilla files, current Iranian and DLC trees, existing Event 071 files, unit-family and cost APIs, catalog workbook, asset references, and existing relevant systems. The archive used for planning did not contain these. Do not claim the planning session verified them. Do not substitute the online Paradox Wiki for required offline sources.

Inspect current changes and preserve unrelated user work. Use event-owned paths and the repository's established conventions. Do not create a parallel framework merely because a documented helper's implementation needs to be found.

## Country and event contract

Reuse the actual Iranian identity. Preserve its government, living characters, units, research, wars, diplomacy, and meaningful history. Audit installed Persian restoration paths and block an already completed equivalent restoration. Design a complete tree transition for incomplete prior content, preserving factual progress without duplicate rewards or stranded systems.

Implement all event registration, actor selection, pre-fire actor history, event details, evolution displays and logs, root repeat guards, and Formables cluster behavior. One member event must not receive a second independent timer roll. Inspect the authoritative workbook before updating its row and regenerate exports normally. Never edit only the supplied CSV snapshot.

Persia remains human and participates in ordinary civilian systems. Resolve the event-managed classification policy without applying nonhuman exemptions or automatic world-threat status. Do not introduce a custom country tag without a full vanilla, Chaos Redux, Workshop, and local-mod collision audit and a complete accepted country package.

## Actual army, elite family, and technology

Deliver the strong opening force from part 9 once. Resolve the active evolution and campaign factor, valid templates, deployed manpower and equipment, additional reserves, legal air wings, remaining aircraft stock, fuel capacity, logistics, and valid locations. Test full, remnant, landlocked, late, industrial, subject, and already-at-war Iran. A tiny remnant still receives its actual force immediately. No silent partial army, empty templates, or unspecified later delivery.

Later evolution activation must not repeat the opening grant. Paid guard institutions and recruitment expand the force afterward. Implement the real custom Immortals subunit family through the shared unit system, with equipment, training, production, reinforcement, capacity, counters, models, sounds, and documentation. Enforce manpower-based capacity across templates, queues, conversions, transfers, and disbanding. Stage equipment preparation and personnel training as specified to respect cost limits.

Inspect every affected technology and doctrine graph with `hoi4.tech_inspect`, render the relevant folder or branch with `hoi4.tech_render`, and compare with `hoi4.tech_compare`. Check prerequisites, exclusivity, production unlocks, and consumers. Do not copy a major country's full research tree or grant incompatible equipment.

## Focus tree and political systems

Build all three main routes and all shared branches from parts 4 to 8. Preserve their choices, tradeoffs, regional priorities, institutions, failure paths, and conclusions. Create the final exact node layout and connections during implementation. Use coherent non-linear progression, meaningful anchors, proper route locks, bypasses that do not repay rewards, focus filters, complete icons, and final localisation. Do not inflate the tree with empty modifier steps.

Implement the provisional settlement, government continuity, legitimacy, three staged tree-owned spirit slots, satrapies and clients, capital projects, the full Persepolis chain, Gulf development, all evolutions, and the specific crisis and recovery paths. Public custom readings are legitimacy and guard manpower capacity only. Do not add hidden public-management bars through tooltips.

Territorial bands require an explicit exact-state audit. Current owner, controller, access, charter, and settlement are distinct. Claims are not automatic cores. Strategic targets are deduplicated by actual country and diplomatic authority. Do not promise limited wars or forced peace outcomes the engine cannot deliver.

## Decisions, missions, and GUI

Implement the complete action and mission map in part 13 and the separate decision prompt. Use one cost quotation and transaction, no more than four spendable types per action, at most three compact inline values, and command power costs no greater than 60. Preserve actual material, manpower, experience, construction, and political sacrifices. No political-power-only simplification of material programs.

Missions need new actions, automatic success checks, partial results, valid expiry ordering, interrupted-target behavior, refunds only for unconsumed resources, and persistent reservations. Display a bounded action list and active mission set.

Before native GUI work, create and review the required reference images from part 14. Record acceptance and map them to native elements. Delegate the bounded Event 071 GUI to `chaosx_event_ui_worker`. Its allowed scope excludes shared event log, event details, settings, super-event framework, shared registries, and unrelated interfaces. Require `hoi4.gui_inspect`, comprehensive `hoi4.gui_render`, and post-change comparison evidence. A reference image is not an implemented GUI. Do not flatten controls into art or add a category picture beside the rich map and meter layout.

## AI and probability

Spawn `chaosx_ai_probability_auditor` through the available supported delegation runtime with an explicit handoff. Start its weighted-behavior work with `hoi4.probability_inspect`. Use `hoi4.probability_evaluate` for part 15's named scenarios, `hoi4.probability_sweep` for thresholds and rank reversals, and `hoi4.probability_compare` after implementation. Use `hoi4.probability_render` where it aids review.

Use `hoi4.probability_simulate` only for declared uncertain inputs and `hoi4.probability_sequence` only for a complete declared custom pool with cadence and transitions. Do not report exact selection probabilities from incomplete pools. Implement route preference, target assessment, supply preparation, guard recruitment, charters, investment, faction obligations, and crisis recovery.

## Assets, portraits, 3D, and super-events

Follow the separate asset and super-event prompts. Produce every required final static asset and any later accepted animation with a static fallback. Inspect canonical references before production. Every flag uses flat reference-constrained ImageGen. Historical designs need evidence and faithful geometry. Fictional designs need explicit documentation. All portraits go to `chaosx_portrait_creator`, preserving grounded identity and using the prescribed RunPod styling route.

Delegate required 3D and skeletal animation production to `chaosx_3d_model_pipeline` through Codex `collaboration.spawn_agent` with `fork_turns="none"` when that runtime is available. Use a self-contained handoff. Follow the dedicated 3D skill's separate Meshy 7 body and firearm generation rule, then the firearm-safe Blender route. Resolve the older gun-production wording conflict with the skill maintainer. Do not spend on external generation or recovery work without the required authorization.

Wire and validate final `.asset`, entity, `.gfx`, unit consumer, attachments, actions, scale, counters, and sound routing. Review available user-supplied live in-game evidence. A rendered model without a working unit consumer is incomplete.

Research and source final super-event wording, cultural references, quotations, and unique licensed audio through the designated text and audio researchers. Record composition and recording rights. Do not ship an unresearched quote, placeholder music, generated audio, or an audio collision. Use the shared super-event playback contract.

## Chaos, achievements, and validation

Implement every applicable unique Chaos source and reversal in part 18, including tiered manifestation, specialized network formation, advanced guard institution, coercive submission, broken guarantee, fragmentation, settlement, crisis resolution, and recovery. Evolution activation itself adds zero. Show anti-farming guards and prove that generic war, annexation, subject, faction, and buildup sources are not double counted.

Implement all eight achievements with full tracking and icon triplets. Test ownership versus control, valid subjects, duration boundaries, guard readiness, campaigns, save reload, and ineligible states. Use the shared achievement framework and exact root-only DDS naming.

Run the required event, decision, country, focus, technology, AI, GUI, localisation, asset, and persistence audits. Record actual tool outputs and source diffs. Distinguish static validation, MCP evidence, and live game evidence. Never claim a live playtest that did not occur.

## Delegation, improvement, and completion

Use the supplied role handoffs to coordinate all required specialties. Actual agent runs need returned handoffs and evidence. Reading TOMLs or assigning yourself an agent name is not independent review.

Before declaring the goal near complete, spawn `chaosx_improvement_loop_planner` with Codex `collaboration.spawn_agent` and `fork_turns="none"`. Resolve its accepted addendum or closure handoff. Put working plans under `docs/plans/071_persia_plans/` and fold accepted design into the source specs. Do not create `docs/planning/`.

Keep iterating until the goal is accomplished to its fullest extent. Do not claim completion until implemented files satisfy the full specification. Do not hide missing work behind temporary versions, placeholder assets, approximated maps, or untested fallbacks. If an essential source, tool, authorization, or engine capability is unavailable, identify the exact blocker and the affected acceptance gate. Report completed work and evidence separately from blocked work.
