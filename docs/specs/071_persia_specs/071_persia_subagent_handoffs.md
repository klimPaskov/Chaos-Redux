# 071 Persia: self-contained subagent handoffs

## Execution status

These are future dispatch prompts. None is a returned subagent report. All twenty supplied role definitions were read during planning, but this session did not expose a callable delegation runner. The current package therefore has a parent-only design review. Required independent planning and implementation reviews remain unperformed.

Use the actual supported delegation runtime. For Codex roles requiring it, use `collaboration.spawn_agent` with `fork_turns="none"` and an explicit self-contained handoff. Do not invent an unavailable tool or a runtime token. Role configuration files alone do not launch workers.

All workers receive this common context: implement or audit Event 071 Persia from `docs/specs/071_persia_specs/`, root `chaosx.nr71.1`, Minor Fire-Once, Formables Medium, with the user brief and source receipt as provenance. Read `AGENTS.md`, the role's supplied definition, relevant skills, and all named task inputs. Preserve unrelated changes. Write working reports under `docs/plans/071_persia_plans/`. Report actual evidence, blockers, changed files, and tests. Do not claim another worker's work or mutate shared files outside the assigned scope.

## 1. chaosx_repo_explorer

Read the index, source receipt, country matrix, and part 18. Locate actual Event 071 registration, existing Iranian identity and trees, DLC restoration paths, actor history, cluster records, shared unit and cost APIs, map data, and catalog workbook. Inspect mandatory local wiki and vanilla references. Return an evidence map with exact paths, current behavior, existing conflicts, and missing dependencies. This is a read-only discovery task. Do not implement a speculative replacement tree.

## 2. chaosx_scripted_system_architect

Read all system parts, especially 2, 3, and 9 to 18, plus the explorer report and actual shared APIs. Design the concrete integration for once-only grants, evolution extensions, legitimacy, charters, costs, guard capacity, crisis accounting, scheduling, and persistence. Preserve the two public readings and three-spirit limit. Return a scoped implementation plan, dependency order, engine verification questions, and finite asset-transfer rules. Do not add a new global polling framework without approval.

## 3. chaosx_country_package_auditor

Read the country matrix and parts 1 to 3, 9 to 12, and 18. Audit Iran identity reuse, meaningful tree carryover, ideology names, characters, subjects, ordinary human classification, and all crisis successors. Verify tag collisions across installed sources before accepting any new tag. Confirm that no fictional ruler is presented as historical and no player subject loses agency. Return pass, defect, and blocker entries tied to actual files. Do not manufacture missing country assets.

## 4. chaosx_focus_tree_auditor

Read parts 4 to 8, route diagrams, and the actual implemented tree. Audit route locks, alternatives and cumulative prerequisites, shared branch access, remnant entry, landlocked progression, bypass rewards, foreign-state conditions, filters, node spacing, and political conclusions. Use the required focus inspection and rendering tools from the skill. The planning diagrams are branch sketches only. Return actual disconnected or unreachable content and matched evidence, not an assertion based on the sketch.

## 5. chaosx_decision_mission_auditor

Read part 13, the decision prompt, GUI design, and actual transaction helpers. Audit every action and mission, including staged guard costs, construction commitments, availability, quoted costs, debits, refunds, reservations, cooldowns, targets, AI, and final-day success. Test save reload and simultaneous actions. Reject hidden fifth costs and missions without a performable action. Return the action inventory and evidence for each defect.

## 6. chaosx_ai_probability_auditor

Read part 15, route and diplomacy designs, and actual weights and candidate pools. Begin with probability inspection, then evaluate the named scenarios, sweep thresholds, compare implementations, and render useful evidence. Use simulation only for declared uncertain inputs and sequence analysis only for a complete declared pool. Verify route variation, enemy commitments, material affordability, supply priorities, and human-subject boundaries. Do not label raw scores as probabilities.

## 7. chaosx_asset_source_researcher

Read part 16, research notes, the asset prompt, and actual reference catalog. Find reliable references for historical flag variants, appropriate contemporary uniforms, institutional imagery, and allowed modern 3D source art. Record provenance, rights, NoAI or derivative restrictions, dates, and exact intended consumers. Historical research sources are not automatically approved model inputs. Return a source manifest and unresolved rights issues. Do not create portraits or final game art in this role.

## 8. chaosx_generated_event_art

Read the presentation table and asset prompt, then inspect the matching news, report, and super-event references and live consumers. Produce approved modern alternate-history scenes for the restoration and major milestones. Preserve government and route correctness. Coordinate researched super-event meaning before final scenes. Return native sources, generation provenance, processed files, final conversions, and contact sheets. Do not create portrait substitutes or flatten a GUI into event art.

## 9. chaosx_icon_artist

Read route branches, institutions, decision map, achievements, and asset prompt. Inspect each native icon family separately. Produce focus, idea, decision, mission, GUI-symbol, unit-counter, and achievement subjects within the assigned scope. Use the deterministic achievement triplet process and preserve templates and overlay exactly. Derive state puzzle geometry from verified map data, not ImageGen. Return final consumers, dimensions, strict format evidence, and native-size reviews.

## 10. chaosx_portrait_creator

Read the country matrix, actual roster, source researcher results, and portrait skill requirements. Own every real, fictional, and institutional portrait. Preserve a real subject's sourced identity and valid chronology. Use explicit head-and-shoulders framing, 156 by 210 leader output, and 65 by 67 adviser output only for an actual need. Follow the user's RunPod final styling route. Return sources, identity checks, crop and output evidence, and missing research. Do not invent biographies to fill a list.

## 11. chaosx_3d_model_pipeline

Read part 9, the full part 16 unit brief, the dedicated 3D skill, actual unit consumer, and allowed source references. Produce the required Immortals body and separate firearm through the current Meshy 7 and Blender pipeline. Follow the dedicated firearm rule despite older contradictory wording, and coordinate that discrepancy with the skill maintainer. Respect paid-work authorization and recovery limits. Validate scale, rig, attachments, actions, textures, export, entity, counters, sound, and unit wiring. Return source tasks, intermediate files, final assets, and actual consumer evidence. A rendered model alone is incomplete.

## 12. chaosx_event_ui_worker

Read part 14, part 13, the GUI skill, actual Event 071 GUI files, and required references. Create and review layout reference images before native work. Own only Event 071 category and attached GUI, event-owned assets and localisation, selection state, and the UI handoff report. The conceptual event-owned identifiers are the Persia category, attached window, region selector, subject selector, and guard panel. Resolve their final IDs with the coding lead before writing. Exclude shared event log, details, settings, super-event framework, registries, and unrelated UIs. Require inspect, comprehensive native renders, comparison, accepted adaptation notes, and the resolution and state matrix. Do not add a category picture to the rich layout.

## 13. chaosx_super_event_text_researcher

Read part 16, route conclusions, research notes, and the super-event prompt. Research titles, cultural context, original language, quotation attribution, translations, and final description direction for the three route milestones and earned collapse. Distinguish original writing from quoted material. Return sourced candidates and final text only after checks. Do not invent an ancient saying, a republican emperor, or a historical claim unsupported by the actual trigger.

## 14. chaosx_super_event_audio_researcher

Read the super-event prompt, actual registry, and accepted text meanings. Find distinct appropriate audio with separate composition and recording rights, exact usable source, license terms, and a structured 60 to 120 second segment. Listen before claiming timing or musical structure. Record collisions and source limitations. Do not generate audio or select placeholder tracks. Return rights and segment evidence for each accepted moment.

## 15. chaosx_localisation_auditor

Read the full specifications, accepted research, actual implementation, and project writing rules. Write or audit final event, focus, decision, mission, GUI, achievement, country, tooltip, event-details, and catalog-facing text. Planning labels are not final copy. Verify every cost, duration, target, requirement, and result against actual behavior. Keep historical references sourced, government forms correct, and language readable. Return missing keys, mismatches, misleading promises, and final text evidence.

## 16. chaosx_spreadsheet_doc_worker

Read part 18, the supplied snapshots, actual workbook, and project spreadsheet workflow. Update the authoritative Event 071 row and Formables membership to the accepted implementation state, preserving the user's design. Regenerate exports through the normal process. Do not edit CSV snapshots as the source of truth. Record the workbook path, row, changed fields, export evidence, and remaining status blockers. Do not mark implementation complete based on this plan.

## 17. chaosx_documentation_curator

Read the index, source receipt, accepted implementation reports, and actual documentation conventions. Update event-owned system documentation, asset manifests, helper consumer notes, country packages, costs, evolution effects, achievements, and acceptance evidence. Keep source specs in `docs/specs/071_persia_specs/` and temporary plans in `docs/plans/071_persia_plans/`. Fold accepted additions into the relevant source parts. Do not turn a blocked test into a passed one or copy private machine configuration into public docs.

## 18. chaosx_skill_maintainer

Read the supplied dedicated 3D skill, planning skill, role TOMLs, config, and the conflicts in the source receipt. Resolve contradictory firearm-generation and paid-recovery wording against the current intended workflow. Also review event-managed human-country classification and any newly exposed unit or cost API gaps. Propose narrowly scoped skill corrections with sources and affected roles. Do not silently broaden paid permissions or alter unrelated project policy.

## 19. chaosx_improvement_loop_planner

Run an independent review before planning acceptance when available and again near implementation completion as required by the skill. Read all parts and actual evidence. Look for missed branch depth, weak choices, insufficient opening force, subject agency problems, costly interface clutter, unearned super-events, poor recovery, and ways to make the event more coherent within scope. Produce a concrete addendum or closure handoff under the event plans folder. Separate accepted improvements from optional suggestions. Do not claim to have executed code, rendered assets, or run tests that were not part of your work.

## 20. chaosx_event_completion_auditor

Read the complete source spec, all accepted addenda, implementation diffs, and returned specialist evidence. Audit every promised system, country package, route, unit, cost, mission, asset, achievement, Chaos contribution, and integration. Confirm required independent improvement review has actually returned and been resolved. Distinguish static, MCP, and live game evidence. Return a completion decision with exact blockers. Do not accept a nice diagram, a source receipt, or an agent name as proof of implementation.

## Coordination order

Discovery and architecture establish actual source contracts. Country, focus, decision, and AI reviews shape implementation boundaries. Source research precedes final identity and presentation assets. Text and audio researchers coordinate before super-event acceptance. UI and 3D workers receive bounded consumer contracts. Localisation, workbook, and documentation work follow the accepted behavior. Independent improvement review and completion audit close the process only after their actual reports are resolved.

This order is a dependency guide, not a claim that the workers have been run in this session. Parallelize only genuinely independent file scopes and keep a single owner for shared registration, cost transactions, and root grant accounting.
