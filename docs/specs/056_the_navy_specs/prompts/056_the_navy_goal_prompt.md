# /goal: Implement Event 056 The Navy

Implement Chaos Redux Event 56 fully defined by `docs/specs/056_the_navy_specs/`.

Read all eight specification parts in order. Then follow the coding, commissioning, asset, and achievement prompts, plus the AI probability matrix, catalog alignment brief, and coverage matrix.

Follow `AGENTS.md` and every named event, decision, asset, subagent, and improvement skill. Consult the required local wiki, vanilla documentation, naval sources, and Chaos Redux precedents. Use the HOI4 MCP event and probability routes.

Event 56 must remain Chaos level 1 and Minor Repeatable. Every valid country with direct usable coast receives one coherent random fleet package. Package identity cannot be tailored to doctrine, ideology, enemy composition, production, or strategy. Implement every baseline identity, legal design compatibility, defining cores, local size bands, bounded world allocation, safe port commissioning, emergency no-port delivery, support grants, usable task forces, cohort memory, and invalid-recipient handling.

Implement Full Commissioning, Phased Commissioning, and conditional Break Up outcomes for humans and AI. Humans need one accurate personal report. AI countries need scenario-aware commissioning and naval use. Do not add a permanent decision category, public custom meter, dedicated scripted GUI, focus tree, country package, super-event, custom ship model, animation, portrait, flag, or audio.

Implement first through fourth-or-later recipient scaling. Implement Evolutions I, II, and III at 200, 400, and 600 Chaos through normal pacing and enable controls. Activation gives zero Chaos and affects future packages only. Experimental and impossible assets require explicit owner registration and fail closed. Event 56 grants no research.

Implement the one-time Chaos map: +5 for first manifestation, +5 for the first firing with at least 25 successful recipients, +10 for the first experimental delivery, +15 for the first impossible package, and +3 for the first previously landlocked country later receiving a package after gaining usable coast. Add repeat guards. Prove that shared generic sources are not counted again. Add no Chaos per ship, recipient, support item, battle, blockade, repeat, or evolution activation. Add no invented reversal.

Implement Event 56 as a Medium member of Sudden Abundance and Military Preparation. Add the proposed memberships and severities after registry and workbook inspection. One selected Event 56 can enter at most one cluster and apply once per transaction. Audit dual-cluster entry, member participation, and AI commissioning with `chaosx_ai_probability_auditor`, with a baseline pass and `hoi4.probability_compare`.

Implement bounded connections to Event 54 technology state, Event 55 ports and logistics, Event 42 support, Event 32 owner-registered naval assets, and Famine's live blockade requirements. Do not fire another event, duplicate grants, bypass owner permission, or apply famine directly.

Create and wire four progression report images and three achievement icon triplets. Implement all three achievements with tracking, disqualifiers, combat proof, persistence, localisation, and negative tests.

Update registration, all event-log surfaces, evolutions, clusters, localisation, documentation, assets, achievements, and the authoritative catalog XLSX. Correct the stale Event 56 row. Add both cluster rows with unused IDs only after registry inspection. Never edit exported CSV files directly. Run the catalog exporter after the workbook update.

Use the specified scripted-system, asset, localisation, spreadsheet, probability, and completion subagents. Near completion, spawn `chaosx_improvement_loop_planner` with a complete context-free prompt and resolve its result.

Keep iterating until Part 8 is satisfied. Report every blocker or simplification. Do not claim completion until the implementation and all supporting evidence satisfy the specification.
