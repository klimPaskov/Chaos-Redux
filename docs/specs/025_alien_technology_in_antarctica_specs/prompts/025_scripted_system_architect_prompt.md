# Event 025 scripted-system architect prompt

Spawn `chaosx_scripted_system_architect` with `fork_context=false` after the repository explorer has mapped the live owner files.

Read the full Event 025 spec package, Event 016 external technology implementation, Event 036 current state, and live repository patterns.

Design and, when the parent grants implementation scope, apply only the reusable and event-owned helper surfaces needed for:

- bounded participant registry and stable country resolution
- progress, readiness, exposure, and dependence clamping
- route burden and gateway evaluation
- mission launch and resolution
- selected-rival validation and evidence state
- fragment ownership and transfer
- same-day winner comparison
- one-shot winner and losing reward application
- duplicate-safe Event 016 external reward selection
- Event 025 and Event 036 overlap arbitration
- race cleanup and invalid participant cleanup

The duplicate-safe technology helper belongs with Event 016 or the shared custom-technology owner. It must try a valid base family, then a valid upgrade, then Alien Systems Integration. It must return explicit result state and must not create Kruger ownership, Directorate state, Event 016 history, evolutions, countries, or free formations.

Centralize thresholds, costs, durations, caps, and AI tuning in script constants. Do not add a recurring whole-world scan. Prefer event-owned scheduled pulses over broad on-actions.

Inspect the required offline wiki and vanilla documentation before engine-facing changes. Use event and technology MCP evidence where supported.

Write a handoff to:

`docs/plans/025_alien_technology_in_antarctica_plans/subagent_handoffs/025_scripted_system_architect_handoff.md`

List changed files, helper contracts, inputs, outputs, defaults, side effects, call sites, task-specific validation, skipped evidence, and remaining parent work.
