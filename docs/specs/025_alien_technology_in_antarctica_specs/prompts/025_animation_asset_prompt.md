# Event 025 animation asset prompt

Route this prompt to the assigned generated-event-art or icon subagent with `fork_context=false`.

Create only the animation families accepted after GUI precedent inspection:

- active signal pulse
- survivor trace
- fragment instability
- Alien Dependence activity

For each family, write an animation brief and frame plan before generation. Keep one subject, camera, palette, composition, and anchor. Generate or edit every meaningful state as its own source frame.

Required outputs:

- source frame PNGs
- processed exact-size frames
- static fallback PNG and DDS
- horizontal sheet PNG and DDS
- contact sheet
- review-only GIF
- frame count, frame size, sheet size, FPS, looping, `play_on_show`, anchor, and target consumer
- source evidence and manifest
- ready GFX handoff

Do not create final motion by shifting, scaling, rotating, blurring, recoloring, changing opacity, or adding a scripted glow to one still. Do not use a GIF as the runtime asset.

Write the handoff under:

`docs/plans/025_alien_technology_in_antarctica_plans/subagent_handoffs/`
