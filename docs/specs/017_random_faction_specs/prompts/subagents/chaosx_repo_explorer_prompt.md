# Prompt for chaosx_repo_explorer

Use `fork_context=false`. Explore Event 17 `Random faction` using the source spec at `docs/specs/017_random_faction_specs/`. Read `AGENTS.md`, `chaos-redux-events`, `chaos-redux-subagents`, `hoi4-decisions-missions`, and relevant offline wiki and vanilla references.

Map exact files and existing patterns for:

- event ID 17 registration and existing unreworked entry
- event log name, detail, and evolution selectors
- repeatable minor event handling
- faction join effects and scripted triggers
- decision categories with dynamic target lists
- cluster membership, especially diplomacy or diplomatic panic cluster
- relevant constants and dynamic helper files
- validation commands and edit order

Write a handoff under `docs/plans/017_random_faction_plans/subagent_handoffs/` or return it to the parent if no repo path is available. Do not edit gameplay files.
