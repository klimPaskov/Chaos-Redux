# Prompt for chaosx_scripted_system_architect

Use `fork_context=false`. Implement or plan reusable scripted logic for Event 17 `Random faction` from `docs/specs/017_random_faction_specs/`, especially `matrices/017_random_faction_scripted_system_architecture.md`.

Create a helper map for:

- eligible minor trigger
- valid faction leader trigger
- dynamic collection of one to four faction option targets
- AI faction option scoring
- shared join effect for player, AI, and cascade outcomes
- regional pressure application
- Evolution I, II, and III helpers
- cleanup of stale pressure, decision, mission, and target state
- constants and duration handling

Patch narrow helpers if safe and local. Write documentation for any new helper. Produce a handoff listing files changed, helpers, constants, call sites, validation, and remaining risks.
