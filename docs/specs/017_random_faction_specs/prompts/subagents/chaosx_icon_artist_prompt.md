# Prompt for chaosx_icon_artist

Use `fork_context=false`. Create Event 17 `Random faction` icon and animated seal package from `prompts/017_random_faction_asset_prompt.md`. Read only the asset prompt and the relevant sections of `chaos-redux-event-assets` and `chaos-redux-frame-animation`.

Produce source PNGs, processed PNGs, final DDS files, manifest entries, contact sheets, animation frame plan, source frames, processed frames, sheet DDS, static fallback DDS, preview GIF for review, and `gfx_handoff.md`.

Do not edit gameplay, localisation, `.gfx`, `.gui`, or spreadsheet files.
