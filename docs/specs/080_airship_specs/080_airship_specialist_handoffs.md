# Event 080: Specialist handoffs

All twenty supplied role definitions were read. None was executed in this session. A configuration file listing a role, a thread limit, or an MCP server does not make that capability callable here. The handoffs below are ready for the project's actual agent environment. They do not represent completed independent review.

When the configured Codex launcher is available, use `collaboration.spawn_agent` with `fork_turns="none"` and provide the context bundle explicitly. Each assignment must include the repository root, the seven numbered spec paths, its relevant evidence paths, the owner's allowed files, the remaining blockers, and a concrete handoff destination. Never assume a no-history worker can infer the user brief from this conversation.

## Shared context bundle

The user requires Event 80 Airship as Minor Fire-Once, Chaos 1, no cluster. Preserve the existing 121-step route, departure index 0, one step every two days, and 242 uninterrupted days. Use the actual state and current controller for host content and impact. Condition is the main value. Passengers and crew require actual population backing. A foreign territorial crash produces immediate American war against the exact controller without a war goal or popup gate. Event 013 or a verified shared fire owner owns later fires. Evolutions at 200, 400, and 600 Chaos are cumulative, with genuine installation and exposure requirements. There is no new country, focus tree, world end, or 3D runtime model.

Source folder: `docs/specs/080_airship_specs/`. Current package is a source-spec proposal. It has not received implementation approval, an independent completion audit, an MCP probability audit, or a live test. Review the source report before treating any external dependency as verified.

## Work sequence

Resolve route and shared-owner contracts first. Then the main implementation agent can implement mechanics while bounded specialists work on local decisions, localisation, and visuals. GUI implementation follows reference review and a clear ownership boundary. Probability and technology review must use the implemented contracts. Near completion, the improvement-loop planner and read-only completion auditor review the integrated result. Documentation and workbook updates reflect verified final state.

## Assignments

| Role | Bounded assignment | Required evidence or output | Boundary |
|---|---|---|---|
| chaosx_repo_explorer | Recover original route geometry, exact state and water bindings, original stop flags, final map frames, and the full active legacy Airship surfaces | A checked 122-position manifest including departure, source paths and hashes, discrepancy report, and no guessed locations | Read-first exploration, no route redesign |
| chaosx_scripted_system_architect | Review one voyage authority, backed passenger custody, Deaths, shared fire handoff, actual unit damage, and bilateral war preconditions | Owner-approved contracts, lifecycle diagram in text, named unresolved interfaces, and narrow proposed shared changes | Do not create an event-local duplicate population, wildfire, or war system |
| chaosx_decision_mission_auditor | Audit every mapped service, emergency, mission, payer, timer, partial delivery, and AI boundary | Phase decision map, exact cost and affordability checks, cancellation outcomes, and tested AI choices | Event 80 decisions and necessary owner requests only |
| chaosx_event_ui_worker | Implement the preserved event-owned dynamic map and task panel after reference acceptance | Native element mapping, `hoi4.gui_inspect`, matched `hoi4.gui_render` evidence, and before/after state comparisons | Exclude shared event log, event details, settings, super-event framework, and unrelated GUI |
| chaosx_ai_probability_auditor | Execute the full probability and sequence matrix against implemented behaviour | Inspection, evaluation, sweeps, comparisons, renders, simulations, seed sets, uncertainty, and discovered defects | Do not substitute hand arithmetic or unsupported AI-score percentages for MCP evidence |
| chaosx_asset_source_researcher | Inspect canonical style references and recover source map assets, then research suitable historical visual references and licensing | Source manifest, rights notes, actual reference files, and a clear distinction between archive research and fictional art | No claims that a historic photograph depicts the fictional Flying City |
| chaosx_generated_event_art | Produce approved fictional Airship reports, news art, and ship forms through the asset skill | Source art, correctly processed runtime files, decoded DDS checks, and visual comparisons | Follow final reference directions and do not invent final super-event text |
| chaosx_icon_artist | Produce decisions, ideas, and achievement icons from the exact templates | Native-size review, immutable template verification, full achievement state variants, and runtime path checks | No replacement of shared templates or invented nested achievement runtime paths |
| chaosx_super_event_text_researcher | Research final text for the rare confirmed Flying City crash presentation | Sourced title and quote or cultural-reference candidates, accurate attribution, rights and uncertainty notes, approved final direction | No role label or draft achievement phrase may become final super-event copy by accident |
| chaosx_super_event_audio_researcher | Research and license a suitable period-context audio choice | Separate composition and recording rights, source provenance, edited timing plan, and correct shared-framework delivery | No guessed track, unlicensed redistribution, fabricated archive provenance, or synthetic substitute |
| chaosx_localisation_auditor | Write and audit final country, passenger, news, decision, GUI, achievement, and event-detail text from the design directions | Complete keys, valid scope and grammar variants, factual cause wording, costs and casualty checks | Preserve user concepts without treating planning labels as finished text |
| chaosx_improvement_loop_planner | Review the integrated implementation before a near-complete claim | A saved addendum or closure handoff addressing mechanics, playability, assets, AI, systems, and full requested coverage | Suggestions require explicit acceptance and propagation into all affected files |
| chaosx_event_completion_auditor | Independently inspect the final implementation and evidence read-only | Pass, fail, or blocked per requirement with concrete file and test references | Never infer completion from another agent's summary or an asset filename |
| chaosx_documentation_curator | Reconcile accepted source specs, plans, event overview, owner references, and limitations after implementation | A concise final documentation package matching verified behaviour | Do not overwrite gameplay files or author the spreadsheet |
| chaosx_spreadsheet_doc_worker | Correct the authoritative Event 80 workbook row and export the catalogs after accepted implementation | Workbook location, exact row changes, export verification, and catalog consistency | The supplied CSV is export-only and is not the authoritative workbook |

## Roles read but not needed for this design

`chaosx_country_package_auditor`, `chaosx_focus_tree_auditor`, `chaosx_portrait_creator`, and `chaosx_3d_model_pipeline` are not assigned because this event introduces no country package, focus tree, grounded named leader portrait, or runtime 3D model. A 2D airship form belongs to the event asset workflow. Do not create unrelated content simply to exercise every skill.

`chaosx_skill_maintainer` becomes relevant only if implementation proves a reusable skill defect. Any such change is a separate bounded maintenance task with evidence. It is not required to rewrite the skill library for this event.

The ComfyUI skill is relevant only when an approved asset workflow needs that provider. The debug-playtest skill applies when the user explicitly invokes a live playtest and the environment is present. Neither was executed here.

## Handoff destination and acceptance

Use event-specific implementation working records under `docs/plans/080_airship_plans/` and the existing project conventions for specialist handoffs. Keep approved runtime assets out of documentation-only paths. Do not place temporary continuation prompts in this source-spec folder.

Each worker reports inspected files, changes within its authority, evidence produced, unresolved blockers, and exactly what it did not perform. The parent merges accepted results and reruns affected checks. A successful worker summary does not close another owner's missing contract.
