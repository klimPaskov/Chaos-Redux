# 068 ZIN: bounded subagent briefs

These are handoff briefs for a future environment with the actual project runtime. No worker listed here was executed during this planning run. Discover real callable tools and read the current subagent skill before using them. Every worker is started with `fork_context=false`, receives the shared contract below and a concrete role block, and reports actual evidence without inheriting unstated conversation context.

## Shared worker contract

Owner: Chaos Redux Event 068, ZIN. Source root: `docs/specs/068_zin_specs/`. Working handoffs: `docs/plans/068_zin_plans/`. Read current `AGENTS.md`, the role's owning skill, the source README, unchanged user brief, relevant numbered specifications and every named registry. The inspected planning revision is `d549536e869056a49890a08ff795fbc80659984e`, which is not permission to ignore later changes.

Keep the mandatory Afrit-caused king death and real Rush civil war. Keep Einendil's non-royal chosen leadership, Volgan's pre-existing Dead Lands realm and initial superiority, Afrit's independent third faction, ordinary Rush dominion boundaries, eligible military-only dragons, one controller-based castle artifact and Event 010's exclusive Edvoid ownership. New lore is proposed, not a discovered map transcription.

Write only to the role's declared area. Do not edit unrelated event systems, the authoritative catalog workbook, shared settings, other agents' outputs or final runtime consumers unless the parent assigns that exact surface. Read the relevant actual source and installed consumer before changing an interface. Return changed paths, hashes where appropriate, factual findings, performed checks, unperformed checks, blockers and parent-owned integration needs.

## Scripted-system architect

Role: `chaosx_scripted_system_architect`.

Inputs: documents 01, 03, 04, 07, 10, 11, 12 and 13, the country and action registries, actual release and population owners, country registry and current event state.

Task: define and implement only the parent-assigned Event 068 lifecycle contract. Prove the arrival identity, carrier and state reservation, external civilian and military receipts, typed reinforcement, actual bilateral peace demands, one arrival coordinator, unique ruler continuity, succession partition and castle-state control. Treat lower-Chaos terminal eligibility as an unresolved policy gate until approval.

Boundary: event-owned helpers and directly authorized adapters only. No visual production, final localization or unrelated global scans. Handoff: `docs/plans/068_zin_plans/system_contract_handoff.md`.

## Focus-tree auditor

Role: `chaosx_focus_tree_auditor`.

Inputs: documents 03 through 09 and 18, the 534-node logical blueprint, the actual native trees, country identities, action and mission consumers and current focus skill.

Task: verify every actual final focus, dependency, route lock, political gate, icon, tooltip, reward, AI strategy and complete route outcome. Check the compulsory death cannot be deferred by focus choice. Ensure all four Rush outcomes work and no rare creature is granted without physical eligibility. Render the actual trees when supported and distinguish graph-source checks from native render evidence.

Boundary: bounded event-owned tree fixes and direct local dependencies assigned by the parent. Handoff: `docs/plans/068_zin_plans/focus_audit_handoff.md`.

## Decision and mission auditor

Role: `chaosx_decision_mission_auditor`.

Inputs: document 11, country signature missions, action registry, actual decision and GUI definitions, current decision-mission and state-ledger skills.

Task: test cost parity, active visibility, exact target proof, repeated commitments, cancellation, ownership change, partial delivery, factory release, mission failure and no-farming. Verify ordinary decision, AI and custom GUI share one effect. Keep the visible action and mission limits and do not add public currencies.

Boundary: assigned Event 068 decision and mission surfaces. The parent owns unrelated shared GUI. Handoff: `docs/plans/068_zin_plans/decision_mission_audit_handoff.md`.

## Country-package auditor

Role: `chaosx_country_package_auditor`.

Inputs: all country documents, country, unit and asset registries, actual collision audit, carrier origins, histories, armies, diplomacy, focus loads and leader definitions.

Task: compare every admitted society and dominion against its whole playable promise. Inspect actual starting territory, supply, force, economy, research, leader, flag, institutions, decisions, routes and AI. Check the three-power military hierarchy, independent monster boundary and no duplicate unique rulers. Do not treat a tag with a generic tree as a complete package.

Boundary: bounded country-owned fixes and directly assigned dependencies. Handoff: `docs/plans/068_zin_plans/country_package_audit_handoff.md`.

## Probability auditor

Role: `chaosx_ai_probability_auditor`.

Inputs: document 16's named audit worlds, actual implemented MTTH entries, AI option pools, focuses, decisions, scripted helpers, settings and the current game-version probability adapter.

Task: begin with actual probability inspection, then evaluate named worlds, sweep thresholds, simulate only declared uncertain inputs and compare before and after corrections. Report exact, bounded, sampled and unresolved results separately. Test timing and ordering, not merely normalized weights. A focus race is not a simple sum of decision weights.

Boundary: read-only evidence unless the current owning skill and parent explicitly assign a bounded patch. Handoff: `docs/plans/068_zin_plans/probability_audit_handoff.md`.

## Localisation auditor

Role: `chaosx_localisation_auditor`.

Inputs: narrative directions, country and scenario identity, actual final localization, event recipients, dynamic variables and tooltips.

Task: verify public knowledge, correct homeland spelling status, full Einendil lineage, distinct Volgan and Afrit rule, real choice costs and no dead target strings. Check all text keys against actual consumers and inspect long text at native display sizes. Do not convert uncertain map labels or new proposed biographies into claimed user canon.

Boundary: assigned Event 068 localization and its direct consumer corrections. Handoff: `docs/plans/068_zin_plans/localisation_audit_handoff.md`.

## Event-owned UI worker

Role: `chaosx_event_ui_worker`.

Inputs: document 11's compact panel requirements, document 15's style and native-size contract, actual parent-owned decision effects and existing GUI references.

Task: build only accepted Rush and known-colony interfaces. Use two public Rush meters, five dominion cards, phase, deadline and the selected-target decision filter. Show known public colony information, not unexposed agents. Prove real hitboxes, disabled states, tooltip expansions and input routing through the supported native inspect/render workflow.

Boundary: Event 068-owned GUI and direct asset references only. Shared settings, Event Log, Event Details and the scenario framework remain parent-owned. Handoff: `docs/plans/068_zin_plans/event_ui_handoff.md`.

## Portrait and icon workers

Roles: `chaosx_portrait_creator` and `chaosx_icon_artist`, each in a separate bounded job.

Inputs: exact country, character or unit key, its approved fictional identity, source paths and hashes, actual installed consumer, target dimensions and every required variant. Use the dedicated asset prompt and current owning skills.

Task: create the assigned source art through the correct native ImageGen or source-preservation route, review identity and framing, process approved outputs, convert to required formats and provide native-size and round-trip evidence. The icon worker must inspect the exact unit-counter family and sampled vanilla green when assigned a counter. Do not infer a new subject from an inherited conversation.

Boundary: assigned portrait or icon production and narrowly allowed portrait wiring under its current owner. No unrelated gameplay or shared GUI. Parent assigns a deterministic per-key output folder and handoff path before dispatch.

## 3D model worker

Role: `chaosx_3d_model_pipeline`.

The parent must turn the asset specification into a complete individual job before dispatch. Include exact approved reference-image path and hash, deterministic job root, output and handoff paths, asset profile, named installed vanilla source mesh and entity, measured scale crosswalk, all rig and semantic action roles, dependency lock, authorized provider operations and recovery policy, custom counter consumers, exact native counter references, source audio roles, license requirements and forbidden substitutes.

The worker follows the current environment hard gate, verified provider, one-image preparation, immediate result preservation, faithful identity, real geometry and rig work, actual material channels, mesh and animation export/reimport, source audio and counter handoffs. It cannot claim final in-game success from a provider preview or export alone. Missing source approval, rig mapping, animation role, selection consumer or runtime proof remains explicit.

Boundary: production evidence and outputs permitted by the current owning skill. The parent owns final entities, GFX, sounds, gameplay and live validation.

## Improvement-loop planner

Role: `chaosx_improvement_loop_planner`.

Inputs: complete source package, actual implemented state, all prior role handoffs, acceptance outcomes and current improvement-loop skill.

Task: identify meaningful missing play, contradictions, shallow focus consequences, duplicate mechanics and cases where accepted design was silently simplified. Propose only justified additions or corrections. Pay particular attention to real one-state Rush succession readiness, the lower-Chaos ending policy, continued neutral play, typed population, unique ruler recovery, castle custody and the difference between a major creature and ordinary infantry.

Boundary: plan-only output under `docs/plans/068_zin_plans/`. Do not silently change source design or gameplay. Once the system is deep and coherent, write a closure handoff with remaining precise tasks instead of adding another broad layer. The parent decides and merges accepted changes, then verifies completion.
