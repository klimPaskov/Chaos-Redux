# 007 Fury event flow map

```text
Eligible AI minor found
  apply Fury package
  load shared tree
  start finite reserve pulse
  pick weaker eligible neighbor
  declare war without warning
    if Fury loses
      cleanup and defeat log
    if target falls
      conquest settlement
      first-conquest news if first victory
      add compliance and overextension
      unlock coring decisions
      pick next target
        if target exists
          repeat war loop
        if no target exists
          no-neighbor branch
            if terminal eligibility
              world-end branch
            if no terminal eligibility
              contained Fury state
    if Fury becomes major
      Fury-major super-event
      world-threat source
      anti-Fury response
```

## Evolution overlay

```text
Evolution I
  stronger idea
  better starting and capped reserve-spawned units
  hardened focus branch

Evolution II
  up to two Fury actors
  cooperation or rivalry branch
  Fury Pact Cohesion if pact type

Evolution III
  up to three Fury actors
  stronger packages
  all-neighbor declarations
  high-chaos branch
```

## Triggerable scenario overlay

```text
The World in Fury scenario
  read intensity and type
  exclude player
  select safe AI minors
  Low gives 2 Fury actors when safe
  Medium gives 5 Fury actors when safe
  High gives 9 Fury actors when safe
  Maximum gives up to 16 Fury actors when safe
  intensity changes initial armies and capped reserve pools
  Pact type enables cooperation
  Hostile type enables rivalry
```
