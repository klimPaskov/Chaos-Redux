# Spreadsheet Worker Prompt for Event 013 Natural Disasters

Use `chaosx_spreadsheet_doc_worker` only after implementation and final player-facing localisation exist. Do not use this planning prose as the final spreadsheet wording.

## Rows to update

- Event 013 Natural Disasters.
- Event 046 Earth Earthquake, after it becomes a placeholder or unknown row.
- Event 099 Sandstorm, after it becomes a placeholder or unknown row.
- Event 051 Heat Wave only if implementation adds explicit non-stacking detail or cross-reference in in-game detail text.
- SCN-007 Disaster Barrage scenario if scenario UI text changes.
- Natural Disasters cluster rows after stable cluster ids and final details exist.

## Event 013 fields

- Details should mirror the final Event Details localisation.
- Evolution I should mirror the final Evolution I detail.
- Evolution II should mirror the final Evolution II detail.
- Evolution III should mirror the final Evolution III detail.
- Evo IV should not carry the old meteor-shower note if the implementation follows the new spec.
- World-end field should remain aligned with final catalog policy for the absence of a terminal branch.
- Type remains Minor Repeatable unless the implementation changes classification with user approval.
- Status should change only when implementation and validation support it.

## Scenario fields

SCN-007 Disaster Barrage should say that it uses the same Event 013 sequence controller, type options, intensity scaling, recovery decisions, and news throttling.

## Cluster fields

Natural Disasters cluster rows need stable ids, final details, member ids or member profile references, type, chaos level, and status after implementation.
