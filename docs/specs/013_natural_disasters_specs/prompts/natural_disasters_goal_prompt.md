# Event 013 Natural Disasters goal prompt

Implement Event 013 Natural Disasters from `docs/specs/013_natural_disasters_specs/` as a fresh source design. Do not preserve old Natural Disasters logic. Do not reuse old Earth Earthquake logic. Event 046 stays an inactive unknown placeholder, while whole-earth rupture behavior belongs to Event 013 Evolution III. Event 099 Sandstorm becomes a placeholder or narrow bridge into the Event 013 dust and sandstorm family. Event 051 Heat Wave remains separate, and Event 013 heat must not stack with it.

Build the reusable dynamic disaster system first. Other events must be able to call a specific disaster family, random family, selected country, selected state, selected region, severity band, news setting, report setting, aftermath setting, follow-up setting, and death or damage scaling override without copying disaster logic. One Event 013 firing may contain many delayed disaster subevents, but it creates one Event 013 history row.

Implement baseline, Evolution I, Evolution II, and Evolution III behavior from the spec. Delayed subevents must not arrive on the same day. Affected countries must reliably receive delayed reports and visible aftermath category notifications, including when the disaster came from a reusable external call. Baseline disasters must cause meaningful building damage and Deaths-system population loss. Evolution II must scale deaths, neighboring-state damage, supply disruption, famine, disease, refugee pressure, and recovery strain harder. Evolution III may devastate regions through abnormal chains such as meteor showers, whole-earth rupture, massive eruption, moving storm corridors, global tornado pathing, tsunami paths, and related severe sequences.

Use the expanded family mini-specs. Each disaster family needs warning decisions, aftermath card values, AI priorities, report and news direction, state modifier direction, and follow-up routes. Do not implement a generic popup with swapped state names. Do not frame Event 013 as an institution, central agency, or generic announcer. The player should see specific disasters in specific places.

Implement the aftermath decision and mission system with early rescue, middle stabilization, late reconstruction, foreign relief variants, active mission caps, partial success, failure, cleanup, AI behavior, and staged visibility. The category must show active disasters, affected states, damage type, disruption, recovery needs, cleanup state, and next reassessment. Costs should use equipment, manpower, trains, convoys, fuel, construction burden, stability, war support, local support, and mission objectives where appropriate, not only political power.

Implement the abnormal disaster scripted GUI only for Evolution III and manual abnormal scenario variants. It must use frame-sheet animation with static fallbacks, state-driven cards, target sprite names, motion paths, warning states, and clear player interaction. The GUI should explain where abnormal disaster zones are going next and which states or regions are at risk. Never rely on GIFs as final assets.

Implement super-events only after the research workflow has produced sourced quote, cultural remark, image, and audio documentation. Do not turn working labels or research directions into final localisation. Keep achievements difficult, with tracking, disqualifiers, icons, localisation, docs, and hooks.

Update event registration, event log details, evolution views, cluster behavior, Disaster Barrage, docs, spreadsheet fields, prompts, assets, localisation, scripted localisation, AI behavior, and validation notes. Report any missing asset, missing research, fallback, simplification, unresolved plan, or skipped meaningful validation clearly. Do not call the event complete until every accepted spec surface is implemented or explicitly queued with a reason.
