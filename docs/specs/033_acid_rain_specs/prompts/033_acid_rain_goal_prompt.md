# Event 033 Acid Rain implementation goal

Build Event 33 from `docs/specs/033_acid_rain_specs/`. Read every package file, then follow `AGENTS.md` and the relevant project skills.

Make Event 33 a Major event at Chaos level 2 and the sixth optional Severe Natural Disasters member after Event 13's five logical slots. Independent and cluster firing use one runtime, and mixed Major pacing commits once. Remove repeatable registration and the daily world scan. Build a host-authoritative, save-safe system with frozen valid states, seven regions, exact coverage, stable fronts and warnings, generation IDs, bounded registries, and idempotent cleanup.

A first regional visit touches 60 to 85 percent of valid states and a second visit clears the rest. Favor untouched targets, handle islands, reveal the next region about one week ahead, and guarantee an endpoint after full coverage. GUI refresh cannot mutate gameplay.

Every positive mortality pulse must call `apply_exact_state_civilian_population_loss`. The state population must fall by `state_civilian_population_loss_applied`, and that same value must be used once for Deaths, Event 33 totals, reports, and achievements. `local_manpower`, recruitable-population modifiers, manpower debits, attrition, and counters cannot substitute. Record no deaths when the helper applies zero. Duplicate pulse keys must produce no second loss. Apply direct Chaos only where mapped and never count shared Chaos twice.

Implement Preparedness from 0 to 100 through four projects, five urgent actions, and four recovery actions. Use the exact bands, costs, commitments, refunds, durations, target proofs, and AI cases in the decision matrix. World Coverage and Preparedness remain the managed values. AI pays the same costs, protects reserves, and cannot use blocked or stale targets.

Add a dedicated Acid Rain Air source and track actual lifetime additions in basis points. Clamp each gain to the request, the remaining allowance below 1500 bp, and the remaining distance below 5000 bp global contamination. Add nothing at or above that ceiling. Preserve contamination after dissipation and prove that no pulse or evolution bypasses either cap.

Implement Severe Storm Cells at Rising Chaos, Multiple Weather Fronts at Chaos Tier, and Global Acid Rain at Totalen Chaos. They are cumulative and may activate during a live event. Keep fronts independent. The global transition touches every valid state, removes real population through the same mortality contract, retires regional fronts safely, allows bounded superstorms, and retains guaranteed dissipation. Evolution unlocks alone add no Chaos.

Use the asset and super-event prompts for art, UI, animation, static fallbacks, quote, and audio work. Final text and audio require source, rights, uniqueness, and audition proof. Use the achievement and decision prompts for all ten achievements and the full national response category.

Update the workbook first, regenerate CSV exports, update system docs, and provide a completion report. Begin weighted review with `hoi4.probability_inspect`, route it through `chaosx_ai_probability_auditor`, and follow the coding prompt's probability workflow. Run every Part 12 gate, including both launch routes, evolutions, AI, migration, reload, multiplayer, performance, population-loss proof, caps, assets, audio, achievements, interruption, and a second generation.

Before claiming near completion, run `chaosx_improvement_loop_planner` with `fork_context=false`, resolve its handoff, rerun affected checks, then obtain `chaosx_event_completion_auditor` evidence. Completion is blocked by any fallback, coverage softlock, broad daily scan, death record without matching state-population removal, modifier-only mortality, duplicate loss, Air cap leak, free AI action, missing asset, unverified right, stale catalog data, or unresolved migration.
