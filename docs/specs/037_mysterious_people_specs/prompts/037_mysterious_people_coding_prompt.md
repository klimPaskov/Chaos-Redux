# Coding prompt: Implement Event 037 Mysterious People

Implement Chaos Redux Event 037 from the complete source specification at:

```text
docs/specs/037_mysterious_people_specs/
```

Read every specification part, quality file, diagram, handoff, and prompt before editing. Treat the source specs as acceptance criteria. Read `AGENTS.md`, all relevant repository skills, current Famine, Migration, Deaths, Camp and Repression, Condemnation, Event Logs, achievements, civilian classifiers, dynamic effects and triggers, event system, assets, and workbook sources. Consult required offline wiki pages, installed vanilla documentation, installed vanilla precedents, and existing Chaos Redux patterns.

Do not silently simplify, merge, omit, or replace an accepted mechanic. Report every blocker and deviation.

## Repository preflight

Map the current Event 037 implementation and every touched dependency. Preserve unrelated changes. Use `chaosx_repo_explorer` only when locations or edit order are genuinely unclear.

Use mandatory HOI4 MCP event inspection, rendering, and comparison for the event chain. Use the probability MCP workflow through `chaosx_ai_probability_auditor` for every weight, MTTH, random list, policy score, target selector, and AI action.

## Event identity

- entry event `chaosx.nr37.1`
- Minor Repeatable
- Chaos level `1`
- Various Anomalies, Low severity
- one global firing transaction
- one pacing event
- one event-history entry
- repeatable weight behavior from the shared system

Wire registration, default enabled state after completion, names, actor handling, Event Details, history, evolution previews, and debug mappings in the same change.

## Population creation and ledger

Implement one bounded manifestation transaction that visits each valid inhabited state once.

Use current real state population to calculate stage-scaled grants from centralized tuning. Apply real state population gain, record the applied amount, increase state provenance by exactly that amount, and update country and world totals.

Prove the engine behavior for positive population mutation. Reuse a suitable neutral helper. If a new shared helper is required, document its complete contract in the dynamic-effects registry and keep Event 037 lifecycle in event-owned files.

Implement provenance invariants, incremental totals, transaction identity, save persistence, bounded reconciliation, ownership and control refresh, ordinary proportional mortality, targeted exact mortality, Migration composition, and no duplicate population or Deaths.

Use existing exact civilian-population loss helpers for real deaths.

## Pressure and integration

Implement one public country value, Overpopulation Pressure, from `0` to `100` with thresholds `20`, `40`, `60`, and `80`.

Use internal state inputs from mysterious share, density, housing, transport, supply, Famine, Migration, integration, war damage, occupation, and active projects. Aggregate national burden with population weighting and severe-state visibility.

Implement internal integration and a staged demographic-dividend or strain lifecycle. Real population remains authoritative. Immediate full recruitable manpower is forbidden. Any engine workaround must be proven and removed as integration completes.

Recurring work uses sparse active state and country registries. Do not add recurring full-world scans.

## Evolutions

Implement and log:

- Population Surge at `200+` Chaos, base timing near `90` days
- Overpopulation Crisis at `400+` Chaos, base timing near `120` days
- Humanity Multiplies at `600+` Chaos, base timing near `150` days

Evolution activation gives zero Chaos. Disabled lower evolutions cannot block a higher enabled stage. Pre-fire opening uses the highest enabled eligible stage once.

Each evolution must alter future grants, pressure, decisions, AI, and flavour as specified.

## Shared-system adapters

Keep ownership exact:

- Famine owns food values, relief, stages, decisions, missions, and famine deaths
- Migration owns cohorts, routes, reception, trapped people, settlement, return, and movement deaths
- Camp and Repression owns forced labor, sites, extermination, evidence, discovery, reform, and site mortality
- Condemnation owns public diplomatic consequences
- Deaths owns cause records and Chaos from deaths
- shared civilian classifiers own ordinary and nonhuman eligibility

Use versioned, proof-carrying adapters. Event 037 reconciles provenance only from applied results.

## Decisions and missions

Implement the ordinary category, static category picture, policy stances, selected-state workflow, phase visibility, projects, emergency actions, constructive Evolution III programs, and extreme routes from the decision prompt.

One action uses no more than four spendable cost types and correct texticons. Missions require active work, use mapped duration bands, and have distinct success, partial success, and failure.

Run the decision auditor after implementation. Run probability baseline and comparison around every weight patch.

## Events, text, and mystery

Implement one global report per firing, significant national reports, and the full varied flavour families with cooldown and diversity memory.

Preserve the explanation firewall. Do not confirm aliens, time travel, clones, returned dead, alternate universes, secret projects, or a shared origin with another event.

Write final player-facing localisation. Do not copy working labels mechanically. Follow project prose rules and avoid developer-facing, update-history, raw-variable, debug, and hidden-spoiler wording.

Use the localisation auditor before completion.

## Chaos

Implement one-shot direct milestones:

- first manifestation `+2`
- world living mysterious share at `1%` gives `+2`
- `5%` gives `+3`
- `10%` gives `+5`
- `25%` gives `+10`
- one firing creating at least `5%` of pre-fire world population gives `+5` once

Implement the guarded `-2` stable-world recovery cycle. Do not duplicate Chaos from deaths, famine, displacement, wars, annexations, or atrocities.

## Assets and achievements

Produce and wire the complete authorized asset inventory through the generated-art and icon workers. Preserve source evidence, separate icon families, required transparency, final DDS files, sprite handoff, manifests, and temporary-workspace rules.

Implement the three achievements with exact IDs, full triplets, timers, disqualifiers, save persistence, country continuation, and documentation.

## Catalog and documentation

Replace the obsolete Event 037 workbook premise with final implemented wording. Edit only the authoritative XLSX and run the CSV exporter.

Assign a Various Anomalies cluster ID only after live collision checks and full cluster design. Do not edit CSV exports directly.

Write or update durable event, mechanic, decision, adapter, asset, achievement, and completion documentation. Keep Event Details, evolution text, event log, docs, and workbook aligned.

## Audit sequence

Use the scripted-system architect for reusable logic. Use the probability auditor before and after every weight patch. Use decision, localisation, asset, spreadsheet, and completion workers at their mapped stages. Near completion, run the mandatory improvement-loop planner and resolve its result.

## Completion

Validate every scenario in the acceptance criteria, including baseline and evolved grants, caps and floors, repeated firing, ownership, ordinary deaths, migration, trapped cohorts, famine, camps, extermination, nonhuman exclusion, multiplayer, cluster behavior when registered, save and reload, Event Logs, achievements, sparse cleanup, and probability comparisons.

Keep iterating until the complete implementation satisfies the specification. Do not claim completion because the game parses, one popup works, or a partial mechanic exists. Report all simplifications, missing assets, missing evidence, and blockers clearly.
