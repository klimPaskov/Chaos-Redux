# Goal: Complete Event 063 Subjects Break Free

Implement Event 063 from `docs/specs/063_subjects_break_free_specs/`. Read every specification, diagram, research file, quality file, handoff, and prompt before editing. Use all specialized prompts for their owned surfaces.

Follow the required repository rules and project skills. Preserve the explicit authorization gate in `chaos-redux-debug-playtest`.

Pass or fail requirements:

- Replace old Event 063 with a Chaos level 1 Minor Repeatable event that frees a bounded pool-scaled batch of existing subjects in place. Preserve territory, country state and identity.
- Freeze selection before release, select without replacement, reserve the batch through the Liberation Release Coordinator, protect other owner transactions, preserve completed releases after late failure, and clean temporary state.
- Independence cannot be vetoed. Implement negotiated, recognized, contested, and armed settlements, agreement cleanup, narrow restoration claims, multiplayer timeouts, safe war gates, and restore-subject outcomes without default annexation.
- Implement Evolution I at 200+ with one same-overlord cohort, Evolution II at 400+ with compound wars and capped direct intervention, and Evolution III at 600+ with a congress and Liberation Pact. Support active-event and pre-fire entry. Evolution state alone gives zero Chaos.
- Preserve Event 006 and Event 005 ownership. Record first and latest liberation origin neutrally, suspend network status under subject rule, and never write another event's private ledger. Keep Event 006 congresses and the Free Republics' League separate from the Event 063 Pact.
- Expose only Liberation Cohesion as a persistent Event 063 value. Implement four bands, charter states, gains and losses, membership, partners, leadership, discipline, suspension, exit, and dissolution. Never pull countries from unrelated factions.
- Implement every mapped decision, mission, cost, idea, AI role, cleanup rule, report, news threshold, Event Log surface, and cluster hook. Add Event 063 as a Medium member of Liberations, Cluster ID 2. Do not guess a Domestic Unrest ID.
- Implement the Chaos impact map through shared liberation, war, peace, faction, puppeting, annexation, and death sources exactly once. Prove that no batch or cluster hook double counts a generic source.
- Spawn `chaosx_ai_probability_auditor`. Use `hoi4.probability_inspect`, `hoi4.probability_evaluate`, `hoi4.probability_sweep`, and `hoi4.probability_compare`, plus only declared simulations and sequences. Pass every named ordering, timing, dominance, and starvation scenario.
- Create and wire every accepted asset and achievement triplet with runtime registration, consumer, source, hash, manifest, and coverage row. Leave no placeholders.
- Implement all four achievements with exact conditions, disqualifiers, save persistence, multiplayer attribution, one-shot guards, final localisation, and icon states.
- Update the authoritative workbook, regenerate CSV exports, and align permanent event, system, decision, asset, achievement, AI, Chaos, and playtest documentation.

Run the complete acceptance matrix with repository checks, HOI4 MCP evidence, parser validation, event and probability comparisons, asset review, and documented cases for save and reload, multiplayer settlement, repeated firings, collisions, war topology, the Pact lifecycle, and achievements. Invoke autonomous desktop playtesting only after explicit user authorization. Before claiming near completion, spawn `chaosx_improvement_loop_planner` with `fork_context=false` and resolve its addendum or closure handoff. Then run the relevant completion, localisation, decision, system, documentation, probability, and asset audits.

Keep iterating until the goal is accomplished to its fullest extent. Do not claim completion until implemented files and live evidence satisfy the specification. Report every blocked or unimplemented requirement directly.
