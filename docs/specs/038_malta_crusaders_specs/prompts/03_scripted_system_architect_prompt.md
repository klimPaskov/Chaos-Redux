# Prompt for `chaosx_scripted_system_architect`

Work with no inherited conversation context. You are patch-capable only for reusable or event-owned scripted architecture needed by Event 38, **Malta Crusaders**.

Read `AGENTS.md`, `chaos-redux-events`, `chaos-redux-decisions-missions`, `chaos-redux-subagents`, the complete `docs/specs/038_malta_crusaders_specs/` pack, the parent implementation ledger, repository exploration report, and existing Event 38 source. Consult required offline wiki and vanilla documentation for effects, triggers, scopes, variables, event targets, script constants, units, and setup transactions.

Your bounded objective is to design and, when parent prerequisites are locked, implement the Event 38 scripted foundation. Do not create focus trees, decisions, localisation, GUI layout, assets, country history, workbook edits, or broad event prose.

Cover these contracts:

- stable constants for event, evolutions, scenario, public terminal row, values, setup packages, orders, units, campaign, principalities, relics, Teutonic route, Atlantis, Holy World, AI, and cleanup
- read-only valid-actor, valid-state, valid-country, route, order, principality, evolution, hidden-route, scenario, and terminal triggers
- one authoritative exact-state registry interface after the parent supplies map-inspected state groups
- idempotent opening release transaction with request proof, resolved targets, result outputs, rollback, and cleanup
- cumulative pre-fire package resolution
- Crusade Authority, Order Cohesion, and Sacred Legitimacy mutations with clamping and documented inputs
- sparse order, demand, relic, principality, and active-campaign ledgers
- owner-side provider registration hooks for every custom unit family, without extending a central consumer-maintained switch
- principality creation, charter, transfer, integration, dissolution, and cleanup helpers
- Eleventh Crusade eligibility and state transition helpers
- hidden Teutonic continuous-membership and Atlantis eligibility proof
- one Holy World preparation and terminal runtime gateway shared by normal and manual routes
- bounded staged regional war scheduler, no whole-world daily iteration
- scenario setup bypass that exists only during launch and clears afterward
- exact shared-system adapters for Deaths, Condemnation, migration, famine, camps, evidence, and special-country routing
- event target lifetime, global target cleanup, sequence IDs, idempotence, and multiplayer ownership
- public outputs needed by Event Details, GUI, decisions, AI, and audits

Use existing neutral shared helpers where their contracts fit. Keep one-event orchestration in Event 38 files. Add a new shared dynamic helper only when callers cross subsystem or event-family boundaries, then update its matching Markdown contract in the same patch.

Do not use whole-world daily, weekly, or monthly iteration. Do not use hardcoded state IDs outside the authoritative registry. Do not classify Malta or ordinary derivatives as nonhuman. Do not apply Chaos for evolution activation.

Write a handoff under `docs/plans/038_malta_crusaders_plans/subagent_handoffs/scripted_system_architect.md`. List files, helpers, constants, call sites, behavior, validation, missing parent inputs, and remaining risks. Mark any source change blocked when required map, tag, provider, or terminal identifiers are not locked.
