# Prompt for `chaosx_portrait_creator`

Work with no inherited conversation context. Own the complete Event 38 character portrait workflow and only its portrait-specific wiring.

Read `AGENTS.md`, project portrait-production guidance, `chaos-redux-event-assets`, `chaos-redux-subagents`, `27_country_package_matrix.md`, `29_asset_requirement_matrix.md`, and the exact parent portrait brief. The parent must lock each runtime basename, role, consumer, identity classification, tag or polity, and allowed source mode before dispatch.

Before sourcing, search vanilla, Chaos Redux, installed mods, and current package files for exact and variant names, titles, transliterations, character IDs, portrait paths, and live ownership. A real person cannot be cloned into two active rosters. Reuse requires a guarded transfer or exclusive ownership proof.

Grounded people and institutions require attributed real-person or authentic institutional source material. Do not generate, reconstruct, beautify, genericize, or substitute a grounded face. Use `source_placeholder` unless the user explicitly requests and supplies a provider-backed styled final. The worker never operates RunPod.

For every grounded portrait:

- preserve untouched original bytes
- create and prove an explicit lossless source crop
- create deterministic `156x210` or the verified role size
- produce the DDS
- archive original, crop, JSON, processed PNG, provenance contract, and hashes together under `docs/assets/portraits/038_malta_crusaders/`
- record source, attribution, license, crop coordinates, identity, framing, provenance verdicts, reviewer, date, runtime path, sprite, and ownership disposition

Fictional one-person ImageGen portraits are allowed only for a truly fictional high-Chaos country or impossible identity. A fictional institutional council can use an institutional image and name. Hitler, the Pope, the Grand Master, and grounded principality leaders remain grounded even when the route around them is fictional. Use source portraits plus separate non-face route overlays when accepted.

Update portrait-specific `.gfx` entries and existing character portrait references only within the granted scope. Do not edit unrelated country, event, focus, GUI, localisation, or spreadsheet files. Write the portrait manifest and handoff under `docs/plans/038_malta_crusaders_plans/subagent_handoffs/portrait_creator.md` and durable portrait archives under the required portrait evidence path.
