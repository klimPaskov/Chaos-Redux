# Event 016 counter-Kruger programme

Date: 2026-08-02

## Scope

This bounded continuation implements the foreign-decision-matrix counter-Kruger programme. It stays inside the Evolution II targeted foreign-operation category and does not add a country, project family, unit, model, or recurring world scan.

## Runtime changes

- `brilliant_scientist_foreign_build_counter_program` is a one-use targeted decision with a 120-day clock, 60 Political Power cost, 250 support-equipment burden, evidence gates, and an AI MTTH weight.
- `brilliant_scientist_foreign_operation.counter_program` selects the host's most advanced family through the existing project-target helper. The actor records `brilliant_scientist_foreign_counter_program_resolved_targets`, `brilliant_scientist_foreign_countermeasure_targets`, and `brilliant_scientist_foreign_countermeasure_families` alongside the ordinary operation history.
- A successful result records a family-specific countermeasure and adds the existing `countermeasure_previous` score bonus to later operations against the same host. Partial and failed results never grant a project stage, Theory reward, transfer, unit, evolution, or second Kruger.
- Detection opens the dedicated host response `chaosx.nr16.193` and then actor report `chaosx.nr16.194`. Counter-program detection is excluded from the generic `.9` controlled-exchange reaction so a countermeasure receipt cannot accidentally become a full project-theory exchange.
- Localisation, dynamic operation naming, constants, and documentation are registered in the existing foreign-operation surfaces.

## Validation

The parent ran the shared constant-resolution audit with zero missing references, brace balance and unsupported-operator scans on all nine touched script surfaces, duplicate-localisation and BOM checks, targeted localisation-reference checks with zero missing keys, and Event 016 event-ID/reference checks with unique `.193` and `.194` definitions. The existing foreign report events retain their `triggered_desc` blocks; the new `.194` report uses the same structure. No game launch, save, or live consumer validation is performed by this handoff. A focused event-inspector run may still hit the existing workspace scan-byte limit.

## Ownership and remaining risk

Parent-owned files remain responsible for final staging, live actor/host scenario validation, quantitative balance evidence, and catalog wording review. The programme intentionally uses the ordinary operation ledger and no bespoke countermeasure technology tree; broader multilateral coalition and country-flavour routes remain queued. The user-deferred seven 3D packages are unchanged.
