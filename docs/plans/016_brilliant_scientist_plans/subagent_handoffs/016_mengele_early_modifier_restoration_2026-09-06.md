# Event 016 private early-modifier restoration

## Disposition and authority

Implemented under the parent's explicit follow-up authority for idempotent runtime rebuilding and exact stage payoffs.
This supersedes the gate-only missing-modifier restoration residual in the two early-modifier gate handoffs.
The parent authorized one receipt-only restoration helper and a call from existing country-scoped private reconciliation, with no new polling, stage replay, learned-only private history, or changes to other owners.
No files were staged or committed.

## Files, entry point, and helper contract

- `common/scripted_effects/016_mengele_project_stage_effects.txt:1380` adds `brilliant_scientist_mengele_restore_early_project_modifiers`.
- The same file at line 1417 retains `brilliant_scientist_mengele_reconcile_project_availability`, with one added restoration call at the beginning.
- `common/scripted_effects/016_mengele_project_stage_effects.md` documents the helper and its invocation boundary.
- `016_mengele_early_modifier_gate_audit_2026-09-06.md` and `016_mengele_early_modifier_gate_integration_2026-09-06.md` mark their restoration residual as superseded by this implementation.
- This handoff records current source and scenario evidence.

The helper is country-scoped and takes no arguments or temporary selectors.
Its inputs are strict current private-provider validity and the thirty existing `mengele_event016_<family>_<theory|prototype>_completed` flags.
For each exact flag, it adds the matching `brilliant_scientist_<family>_<theory|prototype>` only if that modifier is absent.
It then requests a modifier refresh within the valid-owner guard.
An invalid owner is a no-op.
It never removes a modifier, changes a flag or variable, initializes arrays, debits or refunds payments, grants slots/equipment/technology, changes Directorate availability, or writes Kruger history.
There are no new constants, event targets, numeric modifiers, output selectors, or result variables.

The existing availability reconciliation retains all its original behavior after the added call.
Its source callers at this tranche were the successful private stage-output helper and `common/scripted_effects/016_alien_infantry_cxt_test_effects.txt:65`.
The subsequent accepted gameplay-invocation closure adds guarded victory and registry-focus call sites; see `016_mengele_lifecycle_reconciliation_2026-09-06.md` for exact source identities, lifecycle projections, and MCP limitations.
The restoration path therefore runs when that existing entry point is invoked.
Changing provider validity alone does not fire this helper, and no automatic validity-change detection is claimed.
No shared native completion hook or no-DLC component helper was changed.

## Source-model validation

The tests parsed the applied disk source, the unchanged private validity trigger, the corrected dynamic modifiers, and the existing conventional runtime reconciliation helper.
Native engine modifier removal during temporary invalidation was represented by evaluating the real `remove_trigger` definitions and deleting matching IDs from the model.
This simulates source semantics and is not proof of engine update timing.

| Scenario | Observed result |
| --- | --- |
| Each exact Theory or Prototype receipt with its modifier missing | Thirty independent fixtures restored only the one matching modifier |
| Present modifier and repeated restoration | Sixty fixtures retained one copy without changing flags, variables, arrays, resources, slots, or history sentinels |
| Directorate completed flag, conventional operational flag, and Deployment receipt without an early private receipt | Thirty fixtures created no early modifier or private history |
| All thirty exact receipts with all modifiers missing | Exactly thirty modifiers restored and a repeated call made no persistent change |
| Inactive, nonexistent, defeated, permanently closed, terminal-world, or Kruger-host owner | Six fixtures restored nothing across repeated calls |
| Valid owner, temporary invalidation, modeled modifier removal, then valid owner and existing reconciliation | All thirty modifiers restored through the actual existing entry point |
| Six conventional families, private restoration before or after neutral Deployment reconciliation | Twelve order fixtures converged on exactly Theory, Prototype, and Deployment IDs without stacking |
| Seven custom operational flags without private receipts | No early modifier or private history created |
| Seven custom families with exact early receipts and independent neutral operational flags | Two early modifier IDs restored per family, with repeated calls preserving neutral knowledge and every non-modifier state value |
| Existing scripted-effect bodies compared to prepatch AST | All identical except the one added call in availability reconciliation |
| New helper instruction allowlist | Only owner/flag/presence guards, `add_dynamic_modifier`, and modifier refresh are present |

The single-receipt cases also prove that Prototype alone does not fabricate Theory and one family's flag cannot restore another family's modifier.
The helper leaves stored active-payment arrays unchanged, including a seeded paid receipt, and does not affect research slots, stockpiles, neutral receipts, shared selectors, or Kruger stage arrays.
No private Biological Weapons or Singularity late-stage adapter exists to validate an end-to-end Deployment payoff, and no such claim is made.
Their early receipts are included in all thirty-identity restoration and invalidation tests.

## Source identities

| Source | SHA-256 |
| --- | --- |
| `common/scripted_effects/016_mengele_project_stage_effects.txt` | `635F1DB0534E0BC4A0016D364F7E0C1A4A3A45D186F166AA088C5BB50A2D3EC0` |
| Unchanged `common/dynamic_modifiers/016_brilliant_scientist_project_modifiers.txt` | `2071B9A5F034C40C996F03C0BD6A25BF66A5BC1948DA5A3BF5AEE2B604029C3D` |

The source comparison preserved every pre-existing private output helper and the exact generic payment, refund, finish, cancellation, native authentication, and receipt-migration APIs.
These hashes identify the bounded tested revision and not future concurrent changes.

## References and remaining limits

The offline Modifiers page and installed vanilla `common/dynamic_modifiers/wuw_dynamic_modifiers.txt` document enablement, removal, daily update, and forced refresh.
Installed `documentation/effects_documentation.md` documents `add_dynamic_modifier`, `remove_dynamic_modifier`, and `force_update_dynamic_modifier`.
The existing conventional API's missing-modifier checks are the repository precedent for idempotent reattachment.
The events and subagents skills governed the bounded output contract and source-versus-engine evidence distinction.
No skill was changed.

No new engine inspection is claimed.
The previously recorded Event016 state-flow and trace calls each timed out after 180 seconds without artifacts, and no identical retry loop was added.
The missing ordinary gameplay invocation is superseded by `016_mengele_lifecycle_reconciliation_2026-09-06.md` for the victory and registry-focus boundaries only.
Engine timing, automatic validity-change detection beyond explicit callers, broad portfolio presentation, and the remaining late-stage output adapters remain unresolved.
No fallback or simplification of the authorized receipt-only restoration contract was introduced.
