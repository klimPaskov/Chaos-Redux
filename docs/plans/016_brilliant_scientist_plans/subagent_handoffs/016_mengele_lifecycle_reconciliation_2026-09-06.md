# Event 016 private reconciliation lifecycle entry points

## Disposition and authority

Implemented under the parent's explicit acceptance of the exact guarded victory tail and registry-focus completion call within the accepted idempotent runtime-rebuild and stage-payoff contract.
This supersedes the ordinary-gameplay invocation residual in `016_mengele_early_modifier_restoration_2026-09-06.md` for these two named boundaries only.
No polling, route redesign, reward tuning, new receipt, or owner-transfer mechanic was authorized or added.
No files were staged or committed.

## Changed files and call contract

| Source | Bounded change |
| --- | --- |
| `common/scripted_effects/germany_mengele_effects.txt:2173` | One strict-provider guarded availability reconciliation at the end of `germany_mengele_mark_victory` |
| `common/national_focus/germany_mengele_clone_army.txt:1239` | One identical guarded call in `MCL_directorate_project_registry` after `make_all_directorate_special_projects_researchable` |
| `common/scripted_effects/016_mengele_project_stage_effects.md` | Documents the two gameplay callers and their limits |
| `016_mengele_early_modifier_restoration_2026-09-06.md` | Marks the prior invocation residual superseded for these exact boundaries |
| This handoff | Records the audit, implementation, source scenarios, and evidence limits |

Both calls are country-scoped, take no arguments, and use the existing `brilliant_scientist_mengele_project_stage_provider_is_valid` trigger before invoking `brilliant_scientist_mengele_reconcile_project_availability`.
That existing helper restores missing matching Theory/Prototype modifiers from exact private durable receipts and reconciles existing Computation availability.
Its existing temporary result variable and Computation availability flag behavior are preserved.
The new guarded calls do not initialize receipts, replay stage outputs, grant equipment or research slots, debit or refund payments, or write Kruger history.
No constants, tuning values, event targets, modifier IDs, icons, localisation keys, cleanup helpers, or public APIs were added or changed.
Existing symbols and presentation are reused; no asset work is required for these internal calls.

## Actual gameplay path and lifecycle audit

`events/germany_mengele.txt:830` defines `germany_mengele.33`, whose option calls `germany_mengele_mark_victory` at line 839.
The victory helper establishes the victorious flag, cosmetic identity, and victory-state idea and retains completed focuses when loading `mengele_clone_army_focus_tree`.
Its new guarded call is after all original master-claim handling, stage recording, world-threat refresh, and target cleanup.
Consequently a master-claim overthrow or another remaining invalidity condition fails the guard.
Victory alone does not fabricate the registry flag or any private project receipt.

The registry focus at `common/national_focus/germany_mengele_clone_army.txt:1225` is the gameplay writer of `mengele_clone_focus_directorate_project_registry`.
Its completion reward sets that flag, runs its existing tooltip and availability grant, invokes guarded reconciliation, and retains the existing breakthrough reward.
The call must follow `make_all_directorate_special_projects_researchable`, because that helper writes the Computation available flag; reconciliation then suppresses it when an authentic completed Prototype already exists.
This path remains useful when victory occurred without the registry flag: the later focus establishes the missing validity prerequisite and restores only already-held authentic early receipts.
The focus count remains 54; IDs, layout, prerequisites, costs, weights, and existing reward values are unchanged.

Before this patch, availability reconciliation had only successful private stage-output and CXT callers.
The audit found no existing country-scoped ongoing private maintenance invocation to extend.
`germany_mengele_authorize_full_program` and `germany_mengele_restrict_program` establish their program flags and ideas but do not establish the registry flag or necessarily satisfy Directorate identity.
`germany_mengele_setup_faction` establishes the faction and laboratory-state branch but does not explicitly establish the registry flag.
The reject, cancel, expire, and defeat helpers already invoke invalid-provider cleanup and remain unchanged.
No dedicated private project reactivation or owner-transfer reconciliation caller was found in these named lifecycle helpers.
The existing restricted weekly clone-equipment production hook in `common/on_actions/germany_mengele_clone_on_actions.txt` does not invoke this reconciliation and was not expanded.
No automatic validity-change detection is claimed.

## Source-model scenarios

Tests parsed the applied disk files and the existing private effects and strict provider trigger.
The real event option-to-victory-helper reference was checked directly.
The victory projection executes the actual ordered owner-flag and idea changes, models the existing master-claim overthrow outcome as an explicit fixture input, and executes the added guarded reconciliation.
The focus projection executes its actual registry flag, the actual existing make-all helper, and the added guard in source order.
Annexation, chaos, condemnation, history, world-threat, focus loading, and breakthrough engine effects are not emulated by these bounded projections.
Their source bodies and order are instead compared unchanged against the captured prepatch AST.
Thus preservation of stockpile and history sentinels refers to the added reconciliation and the tested projection, not a claim that the original victory event has no history effects.

| Scenario | Result |
| --- | --- |
| Real event 33 victory transition with each exact early receipt and a retained registry flag | 30 independent fixtures restored only the corresponding modifier |
| Registry-focus activation with each exact early receipt and a valid victory branch | 30 independent fixtures transitioned from registry-invalid to valid and restored only the corresponding modifier |
| Repeating the added guarded reconciliation after each restoration | 60 fixtures retained one modifier without additional persistent payoff |
| Nonexistent, wrong-identity, closed, defeated, Kruger-host, or world-ended owner at either boundary | 12 fixtures restored no modifier |
| Master-claim overthrow before the victory tail | No private modifier restored |
| Victory without registry, then actual registry-focus activation with all 30 receipts | Victory restored none; focus restored exactly 30 |
| Broader Directorate completion and private Deployment receipts without exact early receipts | Neither boundary fabricated early modifiers or private history |
| Completed Computation Prototype followed by registry-focus reward | Reconciliation cleared the available flag after the existing make-all grant |
| Resources, paid-receipt arrays, research slots, and Kruger-history sentinels | Unchanged by the added reconciliation in every tested projection |
| Prepatch and postpatch AST comparison | Every original lifecycle helper and the complete focus tree preserved, except the two exact guarded insertions |

Earlier restoration evidence covers missing/present modifier behavior, invalidation/removal followed by explicit reconciliation, and conventional/custom Deployment convergence.
This tranche adds actual gameplay invocation paths, not a new restoration algorithm or universal return-to-valid scheduler.

## MCP evidence and limits

The mandatory focused event inspection of `germany_mengele.33` returned `EVENT_INSPECTED_PARTIAL`, status `ok`, in workspace `mod_chaos_redux_ea3b2d67c2c0` before the edits.
The request used trace mode, depth 1, maximum 10 nodes and 15 edges, and helper expansion disabled.
Its graph revision was `20ec05eeb1c7f6af785ca008a80dd7ccafc5fa23fad69f6ddd2240527174cbb3`, with graph hash `0518174a71146718a8f9df04ce6d30be3f2db407c18b22294ba1b82e971aa55d`.
The returned artifact is `hoi4-agent://workspace/mod_chaos_redux_ea3b2d67c2c0/artifact/7563c228bc2e71c312bfda698a41bfccd2e58761aea3f2533411d8d002aba55c/3023acf5f173714a32aa9cb49f83acd8b327ec5631c2d3a1f052f631ba9e34b4/event-trace-20ec05eeb1c7.json`.
Its size is 1065845 bytes and SHA-256 is `7563c228bc2e71c312bfda698a41bfccd2e58761aea3f2533411d8d002aba55c`.
The response reported focused analysis with zero expanded helpers and deferred lifecycle projections; validation did not pass the broad workspace report.
This is partial source inventory, not proof of the helper's runtime lifecycle behavior.

The baseline `hoi4.focus_inspect` and `hoi4.focus_render` requests each targeted `common/national_focus/germany_mengele_clone_army.txt`, tree `mengele_clone_army_focus_tree`.
Each failed with `timed out awaiting tools/call after 180s` and returned no artifacts.
No matching baseline exists for a meaningful MCP before/after render or inspector comparison, so that validation is blocked and no identical timeout loop was repeated.
The applied source AST comparison is recorded separately and is not substituted for MCP or engine proof.
No game launch, live test, weighted-logic change, or balance inference was performed.

## Source identities

| Source | SHA-256 |
| --- | --- |
| `common/scripted_effects/germany_mengele_effects.txt` | `FBE41476B84F2B6D2C4FABA5CCAF1C82C45A714B0C814FBA782C8A80F02657AC` |
| `common/national_focus/germany_mengele_clone_army.txt` | `B0622DAE7F09C4A6D90C887F3F075E59F9D6637421AD6112B8F9025FB95A956B` |
| Unchanged private stage effects | `635F1DB0534E0BC4A0016D364F7E0C1A4A3A45D186F166AA088C5BB50A2D3EC0` |
| Unchanged private stage triggers | `9990812298092939C45070ACBE111F8C0D4248596B9648412019D04201B312AF` |

Hashes identify the tested bounded revision rather than later concurrent changes.

## References, remaining omissions, and follow-up

The events, focus-trees, and subagents skills governed callsite scope, required source/MCP review, and handoff boundaries; none was changed.
The required offline wiki core pages and National focus modding page were consulted, including ordered country-scoped completion rewards and the distinction between completion and bypass.
Installed vanilla `documentation/effects_documentation.md` documents country-scoped `load_focus_tree` and `keep_completed`; the German focus tree uses scripted-effect calls such as `GER_stop_rearmament` directly in completion rewards.
The prior accepted Event016 completion contract and restoration handoff remain the design and helper references.

No gameplay simplification or fallback was introduced into these two authorized calls.
Mandatory focus MCP before/after evidence remains blocked by the two exact timeouts, and engine execution and modifier refresh timing remain unproven.
Changes in validity outside explicit callers are not automatically detected; any broader maintenance behavior remains a separate design decision.
Broad portfolio presentation and remaining Biological Weapons/Singularity late-stage outputs remain outside this tranche and unresolved.
