# Event 016 Kruger State foundation portfolio flavour handoff

Date: 2026-08-02

## Scope

This bounded content tranche extends the existing Kruger State foundation reports `chaosx.brilliant_scientist_krg.1` through `.4`. It does not add a route, decision, focus, project family, reward, unit, technology, evolution, event-log entry, or model package.

## Changes

- `common/scripted_localisation/016_brilliant_scientist_kruger_state_scripted_localisation.txt` adds `GetBrilliantScientistKrgPortfolioClause`. It reads the active project-force flags reconstructed by `brilliant_scientist_apply_project_force_package_from_history` and selects one deterministic clause in the order temporal, alien arms, xenobiological synthesis, robotics, cloning, paleogenetics, teleportation, biological weapons, then a safe default.
- `localisation/english/016_brilliant_scientist_kruger_state_decisions_l_english.yml` appends the portfolio clause to the four existing foundation descriptions and defines nine player-facing clauses.
- `docs/events/016_brilliant_scientist/systems/kruger_state_decisions.md` documents the presentation-only contract and its carried-ledger source.

## Behaviour

The clauses are evaluated in KRG country scope after formation and after the existing runtime package reconstruction. They describe the institution that survived with the state. They do not expose raw flags or exact project stages and cannot grant a gameplay effect.

## Validation

- The eight active-family flag names match the existing runtime rebuild effect and no new flag is introduced.
- All four event descriptions retain their existing origin clause and append the new helper once.
- Localisation keys are unique within the file and the file retains UTF-8 BOM encoding.
- No 3D model or asset reference was created.

## Remaining boundary

Broader country-specific event chains, quantitative balance evidence, live consumer acceptance, and the seven deferred reusable Event 016 3D unit packages remain open.
