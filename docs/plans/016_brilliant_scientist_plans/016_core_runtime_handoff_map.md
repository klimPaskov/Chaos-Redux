# Event 016 core-runtime handoff map

Date: 2026-08-27

## Portal Raider Meshy 7 recovery update, 2026-08-27

The user-approved recovery was executed and fail-closed. The source-informed model, rig, nine genuine provider-authored actions, audio provenance, and counter evidence are preserved in `docs/assets/chaos_redux_3d_model_pilots/models_3d/portal_raider` and the dated handoff `subagent_handoffs/portal_raider_meshy7_handoff_2026-08-27.md`. Every permitted automatic retarget route distorted the firearm-bearing pack during firing, so no runtime Portal Raider entity, actions, or sounds are promoted.

Status: current documentation reconciliation for the core-runtime milestone, bounded flavour tranches, and the accepted finite country-specific settlement layer. This map is not a whole-event content-completion claim. Event 016 is default-enabled after the core runtime audit and provider bridge described below; the ten-country layer's static implementation is complete in the working tree, while targeted transfer, cleanup, probability, affordability, quantitative balance, Event 019 isolation, and live validation remain pending. The native raid framework is the current Event 016 raid architecture; the missing native CBRN callback blocks only the optional KRG biological stockpile and delivery ledger, not native Event 016 raids. The complete 28-idea KRG icon package is wired with bespoke 64x64 DDS art; the provider-neutral clone infantry and Autonomous Robot model/entity packages are installed and reusable, and the accepted Alien Infantry V13 Meshy model/action package plus static entity/GFX/animation/sound registrations are promoted for parent review. Its supported muzzle/effect locator is unavailable, particle/light definitions remain unbound, strict selection/acknowledgement/impact/special audio roles remain unresolved, and live consumer acceptance remains pending. The robot package includes purpose-built hidden-technology and equipment icons, bespoke counters, eight real skeletal actions, and licensed synchronized audio. Portal Raider counter art is complete and wired, the Portal Raider runtime model/entity remains rejected and unwired with no accepted recovery package, and the remaining route-specific 3D packages stay queued. Broader country chains are closed/rejected as filler unless a new accepted design reopens them.

## Current status correction, 2026-08-09

The current implementation evidence supersedes older rows that described all Event 016 raids as waiting on a native CBRN callback or all seven model packages as absent. The accepted Alien Infantry V13 Meshy package is recorded in `docs/assets/016_brilliant_scientist/models_3d/alien_infantry/attempts/v13_firearm_preset/final_manifest.md`, and commit `0e724fb8a` promotes its static entity/GFX/animation/sound registrations into the engine-facing runtime tree. Its supported muzzle/effect locator is unavailable, its registered particle/light definitions remain unbound, strict selection/acknowledgement/impact/special audio roles remain unresolved, and live consumer acceptance remains parent-owned. Native biological and Portal Warfare raids use the native raid framework for preparation, reservation, cancellation, expiry, outcome, and history; only the separate KRG stockpile and delivery ledger remains queued behind the missing idempotent CBRN callback. Portal Raider remains a generic reusable API with two native target surfaces: a state-installation raid for factories, reactors, and rocket sites, and an exact building-target raid for special-project facilities. Both prepare for seven days, cost ten Command Power, reserve sixty Teleportation Equipment, require a formation containing at least six Portal Raider battalions, consume the assigned formation on success or critical success, reconstruct the standard six-battalion formation in the seized hostile province, and transfer eligible buildings without duplicating the formation. Portal Raider counter art is complete and wired, while its runtime model/entity, actions, and sounds remain rejected and unwired with no accepted recovery package. The clone infantry and Autonomous Robot model/entity packages are installed and reusable. Every future Chaos Redux special project must be reviewed for inclusion in the reusable CBRN random and project registries before acceptance. No final GUI or whole Event 016 completion claim is made.

## Current MCP and presentation receipt, 2026-08-26

The current shared receipt `subagent_handoffs/016_current_mcp_audit_2026-08-26.md` is the status authority for the latest read-only evidence. It records `EVENT_INSPECTED_PARTIAL` with zero blocking diagnostics for `chaosx.nr16.47` and `chaosx.nr19.1`, a successful `.47` state render, successful D’Rhondan focus inspection and rendering, partial Alien Infantry technology inspection and rendering, and successful exact-window Directorate GUI inspection and rendering. The receipt retains large-workspace helper deferral, truncated GUI response and diagnostics, and no full Event 016 chain proof.

The named worker attestation `subagent_handoffs/016_dhrondan_gui_worker_attestation_2026-08-26.md` confirms Event 016 ownership of `kruger_directorate_container` and the exact decision-category entry point. Its renderer still approximates multi-frame controls with a primary frame and does not expose separate crop, annotated, hierarchy, click-region, per-state, or per-resolution artifacts. This is scoped presentation evidence, not whole-event or live acceptance.

The custom `chaosx_ai_probability_auditor` route and same-scenario `hoi4.probability_compare` remain unavailable. The retained direct probability receipt closes only the conditional DHR rebellion branch arithmetic; cumulative cadence and all other weighted surfaces remain incomplete.

## Purpose and authority

The ten-part specification and its matrices under `docs/specs/016_brilliant_scientist_specs/` remain the accepted Event 016 design source. The 2026-07-14 `016_source_of_truth_map.md`, resume packet, completion handoff, package manifest, and asset inventory remain useful design and historical evidence, but their implementation and asset counters are superseded for current-runtime decisions by this map. Current gameplay files, filesystem inventories, git history, and subagent handoffs are evidence of present work, not automatic approval of a whole-event completion claim.

The 2026-08-03 high-speed materials trial continuation is a bounded project-portfolio implementation, not a whole-event completion gate. Materials Deployment plus Rocketry Prototype, expanded prototype works, healthy ledgers, a valid facility pair, and a selected state now lead to the paid 180-day corridor and `chaosx.nr16.195`. National qualification is host-local; Kruger's proprietary envelope is portable through character and fixed-tag history. The implementation introduces no 3D, CBRN, unit, focus, Event Log, or catalog surface. Targeted transfer and terminal scenarios, static balance evidence, and user-owned live acceptance remain open.

The post-implementation closure handoff `016_nonmodel_content_closure_handoff_2026-08-03.md` records that no additional safe non-model, non-CBRN mechanic remains after the portal and high-speed consumers. The fresh improvement-loop closure `016_post_opening_guard_improvement_loop_closure_handoff_2026-08-03.md` confirms that the dormant-holder and pending-referral repairs close reachability and idempotence gaps without exposing a new content layer. Subsequent V13 work accepts the Alien Infantry model/action export for parent integration, and commit `0e724fb8a` promotes its static entity/GFX/animation/sound registrations into the engine-facing runtime tree; only the four remaining dedicated model packages stay deferred outside the current no-model scope. The Alien Infantry muzzle/effect locator is unavailable, its particle/light definitions remain unbound, strict audio-role gaps remain open, and live acceptance remains parent-owned. Remaining parent work is validation, quantitative balance, Event 019 provider isolation, final audits, the completed 21-icon 2D package, and the separately blocked CBRN obligation. Staff or art follow-ups retain their own queued or deferred dispositions, and broader country chains or filler synergies are closed/rejected unless a new accepted design reopens them.

The 2026-08-03 final audit tranche is now recorded in `subagent_handoffs/016_final_decision_mission_audit_2026-08-03.md`, `016_focus_audit_final_2026-08-03.md`, `016_country_audit_final_2026-08-03.md`, and `016_final_localisation_audit_2026-08-03.md`. These audits found no confirmed local decision, focus, country-package, provider-registration, or localisation-key defect; they retain quantitative, transfer/formation, provider-isolation, technology-projection, portrait-provenance, GUI/presentation, and user-owned live validation as open evidence rather than silently converting it into a completion claim.

Event 016 is included in the default-enabled allowlist for ordinary fire-once selection. The host-context and first-prototype report slice, including the finite facility, custody, and named foreign-operation reactions `.7` through `.9`, is accepted as bounded ordinary flavour. The later host-archetype continuation covers the existing loyalty and relocation dossiers `.10` and `.11`, and the formation-origin continuation covers the first four Kruger State foundation reports. Those foundation, route, and foreign-integration dossiers also describe the highest-priority active project-force institution reconstructed from the carried portfolio. The `.4` through `.9` descriptions add a second, tag-specific host sentence for ten named countries: Germany (`GER`), Britain (`ENG`), France (`FRA`), the Soviet Union (`SOV`), the United States (`USA`), Japan (`JAP`), Italy (`ITA`), China (`CHI`), Poland (`POL`), and Czechoslovakia (`CZE`), while retaining the archetype clause for every other host. The next report continuation applies the same selector to `.12` through `.18` without changing their existing effects or report contracts. The accepted ten-country institutional settlement layer has static implementation for its conditional `.5` choices, receipt-driven `.7` and `.8` clauses, and AI factors; its disposition is static implementation complete, with targeted transfer, cleanup, balance, probability, and live validation still pending. These bounded additions do not authorize separate country chains or new rewards. Broader country-specific chains beyond the finite ten-country layer are closed/rejected as filler by the non-model closure handoff unless a new accepted design reopens them; the shared clone, Autonomous Robot, and Alien Infantry V13 model packages are accepted for parent integration, with Alien Infantry's static runtime registrations promoted while its locator/effect/audio/live-acceptance gates remain open, and the remaining dedicated model packages retain their own pending or queued dispositions.

## Core-runtime milestone required now

| Surface | Current evidence | Core handoff requirement |
| --- | --- | --- |
| Opening and identity | `events/016_brilliant_scientist.txt`, `common/scripted_effects/016_brilliant_scientist_effects.txt`, and `016_opening_foundation_handoff.md` implement the weighted host, one-time referral, fixed `KRG_warren_kruger`, exact `+100%` advisor reward, six special-project roles, and event-log actor rebinding. | Keep the opening fire-once gate, actor correction, and default-enabled allowlist entry aligned while parent validates shared consumers. |
| Shared scripted architecture | `016_scripted_system_architecture_handoff.md` records constants, triggers, effects, MTTH, host values, chronology, temporal ledgers, terminal contracts, and cleanup boundaries. | Integrate the parent-owned Event Details, shared history, world-end, Fallout, super-event, achievement, and settings surfaces. |
| Directorate and project portfolio | `016_directorate_implementation_handoff.md` and `016_project_portfolio_implementation_handoff.md` describe bounded implementation tranches for the Directorate and all fifteen project families. | Preserve separate paleogenetic and xenobiological state, project history, capacity burden, costs, incidents, and terminal guards while wiring final consumers and audits. |
| KRG biological stockpile and delivery | `016_krg_biological_stockpile_delivery_addendum.md` defines the optional Event 016 quantity ledger, production decision, native reservation and outcome callback boundary, transfer/formation receipts, and defeat cleanup. | Accepted design, queued and blocked on the native CBRN system exposing a stable idempotent reservation/outcome/cancellation/expiry callback. This callback is not required by the already implemented native Event 016 raids. Add the Event 016-owned ledger and UI only after that contract is reviewed; no free payload, placeholder action, parallel ledger, or model dependency is approved. |
| Evolutions and foreign operations | `016_foreign_operations_implementation_handoff.md` and the current evolution files provide bounded Evolution II and later-chain surfaces. | Confirm chronology, Event Details rows, log entries, foreign callbacks, and AI contracts before enablement. |
| Containment and territory | `016_containment_implementation_handoff.md` and `016_territory_planner_handoff.md` provide the hosted sovereignty board, timed actions, causal resolver, and bounded territory planner. | Parent owns KRG creation, state transfer, country package, cleanup, and live territory scenarios. |
| Kruger State and focus | `016_kruger_state_focus_implementation_handoff.md` plus the producer-to-consumer ledger record the 100-focus tranche and 180 executable focus receipts. | Parent owns visual review, route balance, country integration, downstream decisions, localisation, AI, and mapped audits. |
| Terminal and aftermath | `016_super_event_aftermath_implementation_handoff.md` and `016_terminal_world_end_fallout_audit_handoff.md` cover the two mutually exclusive terminal commitments, world-threat lifecycle, and Fallout handoff. | Resolve the blocked competing-Fallout, capitulation, lost-state, no-remnant, and settlement scenarios before any completion claim. |
| Event 019 interoperability | Event 019 exposes a reusable provider registry and derivative lifecycle. Event 016 supplies generic providers 504-510 for clone infantry, autonomous robots, paleogenetic creatures, xenobiological organisms, alien-interface infantry, portal raiders, and temporal guards, plus the separate Germany/Mengele-gated Aryan clone provider 522. The clone infantry and Autonomous Robot model/entity packages are installed and reusable; Portal Raider counters are complete and wired, but its runtime model/entity remains rejected and unwired. Provider 505 grants the neutral robot operational package in the derivative country scope before installing its public identity. | Preserve provider registration, exact costs, parent isolation, derivative public packages, reports, and symmetrical cleanup; provider 522 must retain all five owner gates and the shared clone resource contract; live derivative validation remains user-owned. |

## Accepted assets and content tranches

The following are accepted bounded packages because their handoffs record complete production or parent review. “Accepted” means the package may be consumed by the parent; it does not mean every parent-owned runtime reference or audit is complete.

| Package | Evidence and current state |
| --- | --- |
| Opening identity and report | Stage-0 leader or scientist and advisor DDS files, the opening appointment report DDS, sprite registrations, and the `016_base_portrait_source_handoff.md` and `016_opening_report_asset_handoff.md` records are present. Internal use of the copied base is user-authorized; external redistribution rights remain unresolved. |
| Report and super-event art | Twenty-five report-event DDS files exist under `gfx/event_pictures/016_brilliant_scientist/`, including eight breakthrough-family cards and eight incident/security cards, and six super-event DDS files exist under `gfx/super_events/016_brilliant_scientist/`. The original `016_reports_and_super_art_handoff.md`, the bounded `016_report_art_wiring_2026-08-02.md`, the breakthrough-variant handoff, the remnant-hearing handoff, the family-report handoff, and the report-expansion asset handoff record source, processed, DDS, and validation evidence. Focused Event Inspector lint for `chaosx.nr16.6` and `.13` returns `status: ok` with zero blocking diagnostics; parent live presentation acceptance remains open. |
| Super-event text and audio | Six retained text packages and six role-distinct Event 016-owned WAVs at visible IDs 90 through 95 are accepted by the text and audio handoffs. Shared playback, settings, event, GUI, and localisation validation remain parent-owned. |
| Kruger staged portraits and severe animation | Stage I through IV static portraits, six route-specific frame sheets covering the five-family xenobiological-or-alien split, static fallbacks, and review evidence exist. The 2026-08-03 asset-manifest reconciliation supersedes the parent-review handoff's earlier pre-production note. External redistribution rights for the copied Stage-0 source remain unresolved, and final state-selection or live GUI animation acceptance remains parent-owned. |
| Achievement icons | The seventeen achievement identities have 51 final DDS variants in `gfx/achievements/`, with the package and validation record in `016_achievement_icon_asset_production_handoff.md`. Achievement registry, localisation, and final eligibility audits remain parent-owned. |
| KRG flags | `KRG` and six route-specific cosmetic flag families are present in the normal, medium, and small flag surfaces. `016_krg_flag_parent_review_handoff.md` accepts the identities; country and cosmetic-tag wiring remains parent-owned. |
| Focus and idea art | The runtime tree contains 100 KRG focus icon DDS files and 34 bespoke Event 016 idea or national-spirit DDS files. The 21-icon KRG country-idea extension adds all previously unassigned visible idea pictures, with source, processed, decoded, contact-sheet, hash, and DDS-header evidence under `docs/assets/016_brilliant_scientist/krg_country_idea_icons/`. |
| Decision, category, project, and Directorate UI art | The current worktree contains 40 decision-family icons, 10 category icons, the bounded 76-asset project-icon tranche, and 64 Directorate UI textures. Their handoffs record parent-owned `.gfx`, `.gui`, scripted-GUI, and gameplay wiring boundaries. |
| Aftermath art | The aftermath decision and category icon package is present under the Event 016 asset tree, with its handoff requiring producer evidence and parent visual acceptance before final closure. |

## Event 019 provider integration contract

The reusable Event 019 provider API is documented in `docs/events/019_infantry_spawn/systems/triggerable_scenario.md` and implemented in `common/scripted_effects/019_infantry_spawn_unit_registry_effects.txt`. Its current nonhuman providers are `501` zombie, `502` ghost, and `503` coal golem, while owner adapters extend the static census to 18 IDs (`501-514`, `518`, `520-522`). The callback family includes registration, eligibility, template construction, spawn, sustainment, management evaluation, payment, refund, management-cost display, derivative setup, derivative cleanup, and public-addition removal; each provider has 12 definitions including registration. The management-cost display callback writes the shared Muster Board profile cache without debiting resources, and profile 99 is reserved for ledger-backed zero-debit adapters whose tooltip must state that obligations are tracked by the Event 19 manifest.

Event 016 implements collision-free family/provider IDs `504` through `510` and strict provider `522`, with the complete Event 019 callback contract for these reusable consumers:

- `clone_infantry` for provider-neutral clone formations and clone identity history.
- `autonomous_robot` for provider-neutral autonomous robot formations, with `autonomous_robot_equipment_1` as its production consumer.
- `kruger_paleogenetic_beast` for dinosaur or megafauna formations and reserve, feed, handler, and transport history.
- `kruger_xenobiological_assault` for engineered organisms and their selected control channel, containment, and failure history.
- `alien_infantry` for provider-neutral D’Rhondan landing cohorts, source-counted contact, laser-equipment history, and Event 019 provider 508.
- `portal_raider` for generic portal-raider formations and portal-transit history.
- `kruger_temporal_guard` for temporal-continuity formations and temporal-anchor history.

- `aryan_clone_infantry` for the Germany/Mengele refinement only. Provider 522 requires `germany_mengele_is_germany_scope`, `germany_mengele_program_active`, the completed cloning-project and master-race-claim flags, and `mengele_aryan_clone_refinement_tech`; it never enters provider 504.

The adapters retain Event 016 project stage, capacity, equipment, sustainment, containment, route, and achievement receipts while satisfying Event 019 family/provider identity freeze, parent isolation, derivative setup, public package, management, report, and cleanup contracts. Providers 509 and 510 reuse the native Event 016 units and equipment without introducing a Portal Raider runtime-model claim. The clone infantry and Autonomous Robot model/entity packages are installed and reusable, Portal Raider counters are complete and wired, and the Portal Raider runtime model/entity remains rejected and unwired with no accepted recovery package; the remaining bespoke 3D entities remain queued.

## Explicitly deferred from this core milestone

| Deferred surface | Accepted boundary |
| --- | --- |
| Dedicated Kruger triggerable scenario | The accepted depth review says no new Kruger scenario is required. Existing Army of Clones `SCN-002` remains independent and receives only a connection hook; it must not be silently converted into Event 016. |
| Early and country-specific flavour events | `chaosx.nr16.4` through `.9` implement one bounded host-context, assistant-conflict, first-prototype, facility, custody, and named foreign-operation slice without a separate chain for every tag. Appointment and transfer now classify the host as a refugee network, colonial empire, university system, industrial power, militarized state, threatened small state, or default. The host-archetype clause continues through the existing `.10` and `.11` dossiers, the impossible-lecture, incident, rejection, temporal-contact, Antarctic-interface, and spacecraft reports `.12` through `.18`, while the first four Kruger State foundation reports, existing clone, machine, xenobiological, temporal, and Singularity route dossiers, and foreign-integration reports `.60` through `.64` append formation-origin or carried-portfolio presentation as appropriate. The four initial institution-form AI weights now use the same archetype flags, so universities and refugee networks favor the Public Science Council, militarized and threatened hosts favor the Compartmentalized Military Office, industrial and colonial hosts favor the Private Industrial Concession, and refugee or colonial hosts favor the Exile Scholar Network. These presentation and AI continuations add no route, reward, evolution, meter, or new fire path. The seven public-milestone news headlines `.304` through `.310` now extend this presentation layer; broader country-specific chains beyond the finite ten-country layer are closed/rejected as filler by the non-model closure handoff unless a new accepted design reopens them. |
| Report, news, defeat, and remnant art | `chaosx.nr16.6` supplies first-prototype report content through eight registered breakthrough-family cards, queues concurrent families, and does not add a second project reward. `chaosx.nr16.13` routes all fifteen incident families through eight reviewed incident/security cards with the dossier as its unresolved-family default. Seven generated black-and-white news DDS files cover appointment, recognition, public breakthrough, formation, first project-army deployment, containment-coalition, and regional-defeat milestones. The remnant-hearing picture routes clone or machine, biological or paleogenetic or xenobiological, portal or temporal, and alien or singularity remnants to four dedicated report cards, with the shared archive card retained for prototype-only hearings. |
| Final presentation and catalog surfaces | Shared super-event playback, localisation, Event Details, event-log wording, workbook rows, and the mapped static audits are aligned. The 2026-08-02 catalog export is recorded in `subagent_handoffs/016_catalog_alignment_2026-08-02.md`; live GUI/audio/event presentation remains user-owned. |
| Event016-specific 3D production | Shared Clone Infantry and Autonomous Robot model/entity packages are accepted, installed, and reusable by Event 016 and Event 019. The Alien Infantry V13 Meshy model, 24-bone rig, seven distinct actions, three PDX maps, and static entity/GFX/animation/sound registrations are promoted for parent review; its supported muzzle/effect locator is unavailable, particle/light definitions remain unbound, strict audio-role gaps remain open, and live acceptance is pending. Portal Raider counters are installed, but its rejected runtime model/entity/actions/sounds remain pending approved paid recovery. Paleogenetic, xenobiological, and temporal packages remain queued. |

## Current 3D model disposition

The shared Clone Infantry and Autonomous Robot packages are complete static runtime consumers, and the Alien Infantry V13 export package is complete through actual-byte PDX reimport with its static entity/GFX/animation/sound registrations promoted by commit `0e724fb8a`. Alien Infantry effect-point binding, strict audio-role decisions, and live acceptance remain parent-owned. The remaining packages are retained as an explicit production backlog and must not be represented by unapproved substitute models.

`common/units/016_brilliant_scientist_project_forces.txt` defines six project-derived sub-unit consumers: generic `portal_raider`, provider-neutral `autonomous_robot`, `kruger_paleogenetic_beast`, `kruger_xenobiological_assault`, provider-neutral `alien_infantry`, and `kruger_temporal_guard`. Clone formations use the shared `clone_infantry` consumer and its provider-neutral model package. Autonomous robots use the installed shared entity, eight-action animation package, counters, equipment art, purpose-built hidden-technology icons, and positional combat sounds. Event 019 provider 505 grants the shared robot operational technology to its derivative country before installing the public package, so its trainable formation and isolated production contract are reachable. Event 019 provider 508 grants only its own alien-contact receipt and uses the locked shared landing cohort.

The remaining production backlog covers Portal Raider, paleogenetic creatures, xenobiological organisms, and Temporal Guards. Each remaining package needs one-image source and provider lineage, rigged geometry, packed PDX textures, `.mesh`, real exported skeletal actions, entity `.asset` and `.gfx`, counter/icon registration, vanilla scale crosswalk, reimport proof, and parent-owned runtime wiring. The Alien Infantry V13 source, task lineage, seven action exports, three DDS maps, and actual-byte reimport evidence are recorded in `docs/assets/016_brilliant_scientist/models_3d/alien_infantry/attempts/v13_firearm_preset/final_manifest.md`. Provider registration does not substitute for any missing model package. Autonomous Robot selection audio remains registered but intentionally unconsumed because the installed engine surfaces expose country-wide infantry selection routing, not a provider-neutral per-subunit selection callback.

The `chaosx_anomaly_signal_beacon` pilot package under `docs/assets/chaos_redux_3d_model_pilots/` is an independent showcase consumer. It proves the static-building 3D pipeline workflow only and must not be counted as Event 016 model completion or reused as an unapproved substitute.

## 2026-07-29 final static-audit corrections

- The second improvement-loop persistence addendum is promoted for this tranche. Host-context flags and pending `.4` or `.5` obligations survive transfer and formation without replaying their value deltas. First-Prototype reports write a pending character receipt before presentation, reserve one family, queue concurrent families, carry active and governance receipts, apply full, half, or quarter potency by resolved count, and use context-aware AI weighting.

- The fixed-tag sovereignty transaction now cores only the verified formation capital immediately before the documented `release = KRG` effect, then reuses the route-specific state-transfer and portfolio initializer. This closes the static country-instantiation omission; live formation and takeover scenarios remain user-owned acceptance work.

- The sovereignty deadline is no longer fixed at sixty days. `brilliant_scientist_calculate_sovereignty_deadline` derives a bounded 30-to-120-day mission from Mandate, deployed or weaponized project burden, war, public or institutional good faith, and dangerous incidents.
- The first resolved Prototype and valid primary facility can schedule `.7`, the second resolved Prototype can schedule `.8`, and the first detected foreign operation resolved after a Prototype can schedule `.9`. Each report uses a once-only host and Kruger-character receipt; ordinary transfer carries only unresolved `.7` or `.8` ownership while resolved facility and custody consequences remain on the old host and physical state. Terminal or sovereignty cleanup clears pending obligations without adding an evolution or project reward.
- Generic Sovereign Directorate identity now uses `drop_cosmetic_tag = yes`; no active path references the unregistered base tag as a cosmetic tag.
- Formal-recognition decisions rely on the engine cost gate and no longer impose a stricter `political_power > cost` check.
- The Directorate Authority tab is hidden until a real sovereignty or terminal record exists. Unstarted project families are hidden from the project ledger, the primary laboratory names its recorded state, value tooltips identify visible causes, and the Singularity display distinguishes arming, fail-deadly, detonation, disarmament, hold, and terminal states.
- Temporal Continuum and Xenobiological Ascendancy use distinct route-administration ideas instead of sharing the Project Synthesis idea.
- Event 016 localisation passed key, duplicate, scripted-localisation, event-target, BOM, and workbook wording checks after the style and dynamic-deadline corrections.
- At this 2026-07-29 checkpoint, the focused Event Chain Viewer lint report returned no Event 016 issue records and the GUI inspector could not render the large workspace because it reached its configured scan-byte limit. The later 2026-08-26 exact-window receipt is recorded above and supersedes this checkpoint's renderer limitation for the Directorate surface.

## 2026-08-01 report-presentation continuation

- Commit `01b1a2f3d` extends the existing `GetBrilliantScientistHostFlavorClause` presentation to every finding in `chaosx.nr16.10` and every outcome in `chaosx.nr16.11`. The two dossiers retain their original resolver, option, meter, event-log, and asset contracts. The continuation is documented in `subagent_handoffs/016_remaining_directorate_report_flavor_handoff.md`.
- Commit `4b59b0adf` appends `GetBrilliantScientistKrgOriginClause` to the first four Kruger State foundation reports `chaosx.brilliant_scientist_krg.1` through `.4`. The helper reads the retained charter, rebellion, enclave, or takeover formation flag and has a safe default branch. The continuation is documented in `subagent_handoffs/016_kruger_state_origin_flavor_handoff.md`.
- The 2026-08-02 `016_krg_foundation_portfolio_flavor_2026-08-02.md` handoff adds `GetBrilliantScientistKrgPortfolioClause` to the same four descriptions. It selects one carried active project-force family in KRG scope and remains presentation-only.
- The 2026-08-02 `016_krg_route_portfolio_flavor_2026-08-02.md` handoff appends the carried-portfolio helper to the existing clone, machine, xenobiological, temporal, and Singularity route dossiers `.10`, `.11`, `.20`-`.22`, `.30`, `.40`-`.42`, and `.50`. The route-origin continuation now appends `GetBrilliantScientistKrgOriginClause` alongside it, preserving the charter, rebellion, enclave, or takeover memory in those later choices without changing the route.
- The 2026-08-02 `016_krg_foreign_portfolio_flavor_2026-08-02.md` handoff appends the carried-portfolio helper to the five foreign-integration reports `.60` through `.64`. The same route-origin continuation adds the retained formation clause to those reports, so recognition, reconciliation, compact, intelligence, and submission choices identify both the state's origin and the institution it carries. It remains presentation-only.
- These continuations add presentation text only. They do not produce models, add a fire path, create an evolution or reward, alter the host-context `.4` through `.9` causal slice, or close live acceptance, quantitative balance, broader art/news/remnant work, or any other deferred boundary.

## 2026-08-02 bounded continuations

- Commit `11e027bfe` and `subagent_handoffs/016_project_incident_family_risk_content_2026-08-02.md` add one readable family-risk clause to the existing `.13` incident report for all fifteen project families. The shared incident resolver, mission, costs, rewards, event log, asset surface, and model boundary are unchanged.
- The current continuation appends the existing host-archetype clause to all fifteen family-specific recovery mission descriptions. This keeps computation, creature, biological, temporal, alien, and Singularity recovery deadlines readable in the recorded host setting without changing any mission trigger, cost, deadline, damage, reward, AI weight, asset, or model contract. The exact handoff is `subagent_handoffs/016_project_recovery_mission_host_context_2026-08-02.md`.
- The host-archetype institution-AI continuation adds bounded weights to the four existing initial institutional-form decisions. Public Science Council favors university and refugee hosts, Compartmentalized Military Office favors militarized and threatened hosts, Private Industrial Concession favors industrial and colonial hosts, and Exile Scholar Network favors refugee and colonial hosts. Human choice, costs, effects, and route flags remain unchanged. The exact handoff is `subagent_handoffs/016_host_archetype_institution_ai_2026-08-02.md`.
- The host-archetype facility-AI continuation adds bounded weights to the six existing facility decisions. University and industrial hosts favor campus formalization, industrial and militarized hosts favor prototype works, university and refugee hosts favor a secondary laboratory, threatened and refugee hosts favor relocation, militarized and threatened hosts favor hardening, and industrial and colonial hosts favor repair mobilization. Human choice, costs, effects, map targets, and facility flags remain unchanged. The exact handoff is `subagent_handoffs/016_host_archetype_facility_ai_2026-08-02.md`.
- The incident-family flavour tranche adds `GetBrilliantScientistIncidentFamilyClause` to the existing `.13` standard, major, and severe descriptions. Its six presentation groups distinguish machine systems, physical plant, biological chains, living reserves, temporal boundaries, and alien or Singularity interfaces; the unresolved branch is explicit. It does not change the incident resolver, mission, receipt, picture, or project state. The exact handoff is `subagent_handoffs/016_incident_family_flavor_tranche_2026-08-02.md`.
- The rejection-consequence continuation appends the existing host-archetype clause to all three `.15` outcomes. The one-time referral receipt, recipient posture lookup, and no-second-Kruger guard remain unchanged. The exact handoff is `subagent_handoffs/016_rejection_consequence_host_flavor_2026-08-02.md`.
- The settlement-outcome continuation appends the existing host-archetype clause to all ten `.31` resolutions before the recorded sovereignty-policy clause. Release, exile, confinement, shutdown, defection, uprising, rebellion, charter, takeover, and non-country crisis effects remain unchanged. The exact handoff is `subagent_handoffs/016_settlement_outcome_host_flavor_2026-08-02.md`.
- The foreign-result continuation adds `GetBrilliantScientistForeignHostFlavorClause` to every actor after-action branch `.101`, `.111`, `.121`, `.131`, `.141`, `.151`, `.161`, `.171`, and `.181`. It reads the existing operation-host event target and preserves the same context after control returns to the foreign actor; no operation effect changes. The exact handoff is `subagent_handoffs/016_foreign_result_host_context_content_2026-08-02.md`.
- The news-context continuation adds `GetBrilliantScientistNewsHostFlavorClause` to the seven delayed milestone descriptions `.302` and `.304` through `.310`. It reads the global current-host target with a neutral cleanup branch, so delayed public coverage retains host context without changing any receipt, headline trigger, or world-state effect. The exact handoff is `subagent_handoffs/016_news_host_context_content_2026-08-02.md`.
- The project-army news continuation adds `GetBrilliantScientistNewsProjectArmyClause` to `.308`. It reads the existing current-host formation counters through a guarded, stable precedence selector for temporal, alien-interface, xenobiological, paleogenetic, machine, clone, and portal formations, with a neutral cleanup branch. It changes only the delayed headline wording and does not create a unit, provider, entity, receipt, trigger, or model dependency. The exact handoff is `subagent_handoffs/016_news_project_army_family_content_2026-08-02.md`.
- The public-breakthrough news continuation adds `GetBrilliantScientistNewsBreakthroughClause` to `.306`. It reads the current host's existing `brilliant_scientist_breakthrough_public_families` array, selects one of fifteen project-family clauses in a stable order, and falls back to the neutral impossible-research clause when the ledger is unavailable. It changes only delayed headline wording and does not grant a second reward, create a project, alter the news receipt, or introduce a model dependency. The exact handoff is `subagent_handoffs/016_news_breakthrough_family_content_2026-08-02.md`.
- The containment-coalition news continuation adds `GetBrilliantScientistNewsContainmentClause` to `.309`. It reads the current host's existing singularity, weaponization, and formation state in a stable threat-basis precedence, then falls back to a neutral coalition clause. It changes only delayed headline wording and does not activate world threat, alter the shared threshold, enter Fallout, grant a reward, or introduce a model dependency. The exact handoff is `subagent_handoffs/016_news_containment_basis_content_2026-08-02.md`.
- The Kruger State formation-news continuation adds `GetBrilliantScientistNewsFormationClause` to `.307`. It reads the retained charter, rebellion, enclave, or takeover flag through the current-host target, then falls back to a neutral declaration clause. It changes only delayed headline wording and does not alter the already-committed territory, government, cores, formation super-event, or model contract. The exact handoff is `subagent_handoffs/016_news_formation_origin_content_2026-08-02.md`.
- The international-recognition news continuation adds `GetBrilliantScientistNewsRecognitionClause` to `.305`. It reads the existing joint-laboratory, public-reputation or exposed-method, foreign-protection, and failed-assassination exposure receipts through the current-host target, then falls back to a neutral recognition clause. It changes only delayed headline wording and does not alter the recognition score, threshold, one-time guard, super-event, or model contract. The exact handoff is `subagent_handoffs/016_news_recognition_basis_content_2026-08-02.md`.
- The aftermath continuation adds one custodian-policy presentation clause to the archive, regional settlement, and nine project-remnant hearings. It uses the existing custodian government and leaves every treaty, inspection, reconstruction, remnant, AI, world-threat, event-log, asset, and model contract unchanged. The exact handoff is `subagent_handoffs/016_aftermath_custodian_policy_flavor_2026-08-02.md`.
- Commit `6079020c1` and `subagent_handoffs/016_cross_event_contact_context_content_2026-08-02.md` append the active host-context clause to existing Event 030, Event 025, and Event 036 evidence reports. Existing evidence gates, receipts, event-log state, rewards, assets, and models are unchanged.
- The same-date persistence, transition-gate, containment-exclusivity, opening-context, observer, and time-traveller corrections remain bounded runtime or presentation continuations under the grouped implementation handoffs below. None adds a route, evolution, reward, recurring world iteration, Event Details row, asset package, or 3D model.
- The static-field compatibility extension normalizes 451 loader-sensitive `constant:` field uses across sixteen decision files, two scripted-effect files, and three scripted-trigger files with 162 file-scoped macros that are value-equivalent to the shared `016_brilliant_scientist` constants. It changes no supported dynamic fields, gameplay gates, rewards, routes, assets, or model contracts; the exact handoff is `subagent_handoffs/016_static_field_constant_compatibility_extension_2026-08-02.md`.
- The scientist-skill loader follow-up replaces the six static specialization fields in `brilliant_scientist_add_kruger_roles` with one value-equivalent file-local macro. It is isolated from the earlier compatibility tranche and changes no role, reward, event, asset, or model contract; the exact handoff is `subagent_handoffs/016_scientist_skill_constant_loader_followup_2026-08-02.md`.
- The project-force equipment loader and causality continuation documents the six expanded `can_be_produced` gates now present in the current branch. Each gate rejects suspended, damaged, or dismantled families and requires its own ledger stage, carried history, facilities, and operational state; xenobiological equipment also requires one exact control mode. The exact handoff is `subagent_handoffs/016_project_force_equipment_loader_and_causality_2026-08-02.md`.
- The serialized project-stage index follow-up replaces the loader-sensitive `^@index_macro` paths in the focused Singularity, Kruger State, and project-force consumers with the matching fixed ledger positions. The named index declarations remain the crosswalk, and the foreign-trigger surface retains its separate accepted contract. The exact handoff is `subagent_handoffs/016_serialized_project_stage_index_loader_followup_2026-08-02.md`.
- The read-only probability continuation inspects the four evolution event-option pools and the foreign-operation decision pool, then evaluates four named host states. It records a clean source parse, an intentionally incomplete candidate pool, explicit unresolved state-model inputs, and linked ranking or matrix evidence without claiming normalized probabilities. The exact handoff is `subagent_handoffs/016_probability_evolution_balance_evidence_2026-08-02.md`.
- The former-host recovery continuation adds the four Phase H actions mapped by the decision matrix: independent research reconstruction, one selected archive security operation, assistant amnesty, and international inspection. Each action has concrete resource and factory burden, a one-time success/failure receipt, AI weighting, and cleanup. Three successful receipts deliberately suppress Scientific Vacuum through the existing lifecycle refresh; the exact implementation and asset-reuse note are in `common/decisions/016_brilliant_scientist_former_host_recovery_decisions.txt`, `common/scripted_effects/016_brilliant_scientist_recovery_effects.txt`, and `docs/events/016_brilliant_scientist/systems/idea_lifecycle.md`.
- The recovery factory-capacity follow-up adds exact free-project-factory gates matching the four existing reserved factory burdens (3/2/2/3). It prevents a former host from starting an action whose consumer-goods burden cannot be reserved; no new route, reward, asset, or model is introduced. The validation handoff is `subagent_handoffs/016_former_host_recovery_factory_capacity_gate_2026-08-02.md`.
- The KRG command-staff continuation adds four route-gated institutional offices, a hidden duplicate-safe recruitment event, corps-command candidates, and centralized mutually exclusive command-idea cleanup. It reuses the vanilla generic scientist portrait shelf and introduces no new model or portrait dependency; the exact handoff is `subagent_handoffs/016_krg_route_command_staff_patch_2026-08-02.md`.
- The university appointment and institutional-conflict continuation completes the Phase F governance surface. A university network chooses one of the national chair, rotating grants, or Kruger-selected institution methods, then separately protects dissent or dismisses the ethics chair. These one-time choices use centralized logistics, political-power, manpower, time, and production burdens, host-only modifiers, AI policy preferences, hidden-state deltas, a method lock, and containment score receipts. No 3D or new report asset is required; handoffs are `subagent_handoffs/016_institutional_appointments_content_2026-08-02.md` and `subagent_handoffs/016_institutional_conflict_content_2026-08-02.md`.
- The takeover AI follow-up removes the start-only `original_tag = KRG` gate from the two host-tag takeover plans while retaining sovereign-country enable/abort conditions, so a transformed host can enter consolidation and post-audit AI. The focus queue follow-up places `KRG_define_the_states_purpose` before its dependent synthesis focus. Handoffs are `subagent_handoffs/016_krg_country_package_takeover_ai_patch_2026-08-02.md` and `subagent_handoffs/016_synthesis_ai_queue_reachability_2026-08-02.md`.
- The public-challenge continuation adds the missing evidence-gated foreign operation with one-use actor/host history, an immediate six-answer host event `.190`, and actor report `.191`. It changes only the existing Directorate meters and diplomatic history, never grants a project stage, moves Kruger, creates a model dependency, or adds a new evolution; the exact handoff is `subagent_handoffs/016_public_challenge_foreign_operation_2026-08-02.md`.
- The counter-Kruger continuation adds the missing evidence-gated 120-day foreign programme with a support-equipment cost, family-specific countermeasure arrays, a detected host response `.193`, actor report `.194`, and a bounded later-operation score bonus. It never grants a project stage, moves Kruger, creates a unit or model dependency, or opens the generic controlled-exchange reaction; the exact handoff is `subagent_handoffs/016_counter_kruger_program_2026-08-02.md`.
- The counter-Kruger report-art continuation assigns the existing machine-security incident card to host response `.193` and the Directorate dossier card to actor report `.194`. It changes presentation only; the operation's one-use ledger, family receipt, cost, AI timing, and no-reward contract remain unchanged. The exact handoff is `subagent_handoffs/016_counter_kruger_report_art_wiring_2026-08-02.md`.
- The public-challenge report-art continuation assigns the existing sovereignty-confrontation card to host event `.190` and the Directorate dossier card to actor report `.191`. It changes presentation only; the six-answer response set, one-use ledger, meters, diplomacy, AI, and no-reward contract remain unchanged. The exact handoff is `subagent_handoffs/016_public_challenge_report_art_wiring_2026-08-02.md`.
- The country-context continuation adds `GetBrilliantScientistCountryFlavorClause` to the existing `.4` through `.9` report descriptions. Germany (`GER`), Britain (`ENG`), France (`FRA`), the Soviet Union (`SOV`), the United States (`USA`), Japan (`JAP`), Italy (`ITA`), China (`CHI`), Poland (`POL`), and Czechoslovakia (`CZE`) receive authored institutional sentences, while all other hosts receive a readable general clause. The selector is presentation-only and introduces no new event, route, meter, reward, asset, or model; the exact handoff is `subagent_handoffs/016_country_specific_context_flavor_2026-08-02.md`.
- The tracked-holder loader repair recruits `KRG_warren_kruger` in the dormant KRG history and transfers the character through `every_possible_country` and character-scope `set_nationality`. The existing vanilla-compatible KRG country-definition mapping remains pointed at `common/countries/Kruger State KRG.txt`, while the separate history file is `history/countries/KRG - Kruger State.txt`; the opening no longer depends on the unrelated DJX reservation. The exact handoff is `subagent_handoffs/016_tracked_kruger_holder_loader_repair_2026-08-02.md`.
- The dormant-holder guard repair keeps that intentional KRG history owner from blocking Event 016's fire-once availability, appointment, or initial forwarding transaction. A non-KRG country holding `KRG_warren_kruger` still blocks the transaction through the reusable `brilliant_scientist_has_non_dormant_kruger_holder` trigger. The follow-up `brilliant_scientist_has_pending_opening_transaction` gate also suppresses a second scheduler selection while the dispatcher-owned opening flags are active; handoffs are `subagent_handoffs/016_dormant_kruger_holder_guard_repair_2026-08-03.md` and `subagent_handoffs/016_pending_opening_duplicate_guard_2026-08-03.md`.
- The opening-country continuation appends the same country selector to every base, evolved, and host-archetype description in `chaosx.nr16.2` and `chaosx.nr16.3`. The player now sees the receiving country's institutional sentence before choosing public appointment, secret appointment, or referral, while the one-use transaction and fixed-identity guards remain unchanged. The exact handoff is `subagent_handoffs/016_opening_country_flavor_content_2026-08-02.md`.
- The loyalty-relocation continuation appends the same country selector to all five findings in `chaosx.nr16.10` and all five outcomes in `chaosx.nr16.11`. It preserves the existing host-archetype clause and changes only the recorded setting of each dossier. The exact handoff is `subagent_handoffs/016_loyalty_relocation_country_flavor_2026-08-02.md`.
- The evolution-country continuation appends the same selector to the four logged evolution reports `chaosx.nr16.21` through `.24`. It preserves the exactly-four evolution ledger and all option effects while giving each escalation a country-specific institutional sentence. The exact handoff is `subagent_handoffs/016_evolution_country_flavor_2026-08-02.md`.
- The containment-country continuation appends the same selector to the sovereignty deadline, all ten settlement outcomes, and the invalidated-settlement report `.30` through `.32`. It preserves the host-archetype and policy clauses and changes only the recorded national setting. The exact handoff is `subagent_handoffs/016_containment_country_flavor_2026-08-02.md`.
- These continuations do not change the completion boundary. Quantitative balance evidence and live consumer validation remain pending; the native CBRN callback remains unresolved; the 21-icon 2D package is wired and statically validated; broader country chains are closed/rejected as filler; the shared clone and Autonomous Robot 3D packages are installed, the Alien Infantry V13 export package and static runtime registrations are promoted for parent review, its effect/audio/live-acceptance gates remain open, and the other model packages retain their recorded pending or queued/rejected dispositions.

## Plan and handoff dispositions

| Document or group | Disposition | Reason and next owner |
| --- | --- | --- |
| `docs/specs/016_brilliant_scientist_specs/` | Accepted design source with current status reconciliation | All 44 files were read. Package, completion, and asset-status surfaces now distinguish the implemented core runtime from blocked optional content. |
| `016_source_of_truth_map.md` and `016_brilliant_scientist_resume_packet.md` | Historical snapshots with current pointer | Their 2026-07-14 design decisions remain valid, while current-runtime status now points here. |
| `016_brilliant_scientist_improvement_loop_addendum.md` | Closed with promoted and rejected dispositions | Existing R1-R7 decisions remain binding; do not rerun the planner for the same gap. |
| `016_krg_hazardous_mission_pressure_accepted_plan.md` | Accepted bounded implementation plan | The four-mission pressure slice is implemented locally; parent audit and final staging remain open. |
| `016_territory_planner.md` and its handoff | Accepted bounded planner tranche | Parent-owned KRG formation, transfer, country integration, and live territory scenarios remain. |
| Opening, scripted-architecture, Directorate, project, foreign, containment, focus, terminal, and aftermath implementation handoffs | Accepted bounded tranches | Each handoff explicitly limits ownership and does not claim whole-event completion; parent integration and audit remain. |
| Portrait, report, super-event, audio, achievement, flag, focus, idea, decision/category, project-icon, Directorate-UI, and aftermath asset handoffs | Accepted production or review packages | Asset packages may be consumed, but parent `.gfx`, `.gui`, gameplay, localisation, registry, and visual-review boundaries remain as written in each handoff. |
| Event 019 provider adapters for eight Event 016 route families | Implemented core interoperability | Providers 504-510 and strict provider 522 expose registration and all required callbacks, retain exact Event 016 ledgers/equipment, and own derivative authority, ideas, route state, reports, and cleanup. Provider 522 uses the existing Aryan clone infantry definition and the shared clone resource contract; Portal Raider provider 509 and Temporal Guard provider 510 reuse the existing native battalions and equipment. Live derivative validation and MCP lifecycle proof remain open. |
| `016_host_archetype_flavor_tranche_handoff.md` | Superseded scope, retained as evidence | Its initial `.4` through `.9` host-archetype surface remains valid for that tranche. The `.10` and `.11` continuation is recorded in `016_remaining_directorate_report_flavor_handoff.md`; it is not a replacement gameplay path. |
| `016_host_archetype_institution_ai_2026-08-02.md` | Accepted bounded AI continuation | The four initial institutional-form decisions now weight the existing host archetype without adding a route, meter, fire path, or asset. |
| `016_host_archetype_facility_ai_2026-08-02.md` | Accepted bounded AI continuation | The six existing facility decisions now weight the existing host archetype without adding a route, meter, fire path, or asset. |
| `016_remaining_directorate_report_flavor_handoff.md` | Accepted bounded presentation continuation | The loyalty and relocation dossier clauses are statically documented and remain presentation-only. No model, event-log row, reward, evolution, or new fire path is introduced. |
| `016_kruger_state_origin_flavor_handoff.md` | Accepted bounded presentation continuation | The first four KRG foundation descriptions select one retained formation-origin clause with a safe default. No route, decision, focus, idea, reward, model, or new fire path is introduced. |
| `016_krg_foundation_portfolio_flavor_2026-08-02.md` | Accepted bounded presentation continuation | The same four KRG foundation descriptions select one active project-force portfolio clause from the carried runtime package. No route, decision, focus, idea, reward, model, or new fire path is introduced. |
| `016_krg_route_portfolio_flavor_2026-08-02.md` | Accepted bounded presentation continuation | Existing clone, machine, xenobiological, temporal, and Singularity route dossiers reuse the active portfolio clause. No route, decision, focus, idea, reward, model, or new fire path is introduced. |
| `016_krg_foreign_portfolio_flavor_2026-08-02.md` | Accepted bounded presentation continuation | Existing foreign-integration dossiers reuse the active portfolio clause. No diplomatic action, decision, focus, idea, reward, model, or new fire path is introduced. |
| `016_krg_route_origin_flavor_2026-08-02.md` | Accepted bounded presentation continuation | Existing route and foreign-integration dossiers reuse the retained formation-origin clause alongside the portfolio clause. No route, decision, focus, idea, reward, model, or new fire path is introduced. |
| `016_static_field_constant_compatibility_extension_2026-08-02.md` | Accepted bounded loader-compatibility continuation | Sixteen decision files, two scripted-effect files, and three scripted-trigger files use 162 value-equivalent file-scoped macros for 451 fields that reject shared script-constant expansion. No gameplay or model contract changes. |
| `016_scientist_skill_constant_loader_followup_2026-08-02.md` | Accepted bounded loader-compatibility continuation | The six static scientist specialization fields use one value-equivalent file-local macro. No gameplay, reward, route, asset, or model contract changes. |
| `016_project_force_equipment_loader_and_causality_2026-08-02.md` | Accepted bounded equipment-causality continuation | Six project-force archetypes use loader-safe family and stage values plus family-specific history, facility, operational, and failure-state gates. No new unit family, event, model, or lend-lease path is introduced. |
| `016_serialized_project_stage_index_loader_followup_2026-08-02.md` | Accepted bounded loader-compatibility continuation | Focus and project-force stage checks use the matching serialized array positions instead of unsupported caret macros. No route, reward, project, event, asset, or model contract changes. |
| `016_probability_evolution_balance_evidence_2026-08-02.md` | Accepted read-only balance-evidence continuation | Evolution and foreign AI sources were inspected and evaluated against named states; incomplete pools and unresolved adapter state are recorded rather than converted into false probability claims. No gameplay or model contract changes. |
| `016_former_host_recovery_board_2026-08-02.md` | Accepted bounded gameplay continuation | Former-host recovery now has four one-time actions, concrete resource/factory gates, distinct success/failure receipts, AI weights, and a three-success Scientific Vacuum transition. Analyzer and live-scenario limitations remain explicit; no models or new assets were introduced. |
| `016_former_host_recovery_factory_capacity_gate_2026-08-02.md` | Accepted loader/resource continuation | The four former-host decisions now require free project factories equal to their existing reserved burden (3/2/2/3), preventing an impossible start state without changing their rewards or routes. |
| `016_institutional_appointments_content_2026-08-02.md` | Accepted bounded governance continuation | The university network now chooses one national-chair, rotating-grants, or Kruger-selection method with concrete logistics, hidden-state deltas, AI, host-only modifier, and containment receipts. No model or asset dependency. |
| `016_institutional_conflict_content_2026-08-02.md` | Accepted bounded governance continuation | The host can protect dissent or dismiss the ethics chair once, with mutually exclusive flags, concrete costs, hidden-state deltas, AI, host-only modifiers, and containment-score integration. No model or asset dependency. |
| `016_krg_country_package_takeover_ai_patch_2026-08-02.md` | Accepted bounded AI continuation | The two transformed-host takeover plans no longer use the start-only KRG original-tag gate; sovereign-country enable and abort conditions remain authoritative. Remaining route plans retain their original-tag loader gate as a documented risk. |
| `016_synthesis_ai_queue_reachability_2026-08-02.md` | Accepted bounded AI continuation | The synthesis plan now queues the purpose focus before its availability-dependent synthesis focus; the 100-focus tree and icon/localisation coverage remain unchanged. |
| `016_public_challenge_foreign_operation_2026-08-02.md` | Accepted bounded foreign-content continuation | Adds the evidence-gated public challenge action, six host responses, actor report, one-use ledger, AI MTTH, and localisation. It does not duplicate project rewards, transfer Kruger, add a route, or require models. |
| `016_counter_kruger_program_2026-08-02.md` | Accepted bounded foreign-content continuation | Adds the one-use counter-Kruger programme, 120-day support-equipment burden, family-specific receipt, dedicated detected response, actor report, AI MTTH, and later-operation bonus. It does not create a project reward, unit, transfer, route, or model dependency. |
| `016_counter_kruger_report_art_wiring_2026-08-02.md` | Accepted bounded presentation continuation | Reuses the existing machine-security incident and Directorate dossier cards for the counter-program's host and actor reports. No event, option, effect, trigger, receipt, reward, or model contract changes. |
| `016_public_challenge_report_art_wiring_2026-08-02.md` | Accepted bounded presentation continuation | Reuses the existing sovereignty-confrontation and Directorate dossier cards for the public challenge's host and actor reports. No event, option, effect, trigger, receipt, reward, or model contract changes. |
| `016_opening_ideology_flavor_2026-08-02.md` | Accepted bounded presentation continuation | Base and evolved opening and referral reports append governing-ideology clauses for democratic, communist, fascist, non-aligned, and safe-default hosts. No new event, option, reward, receipt, meter, asset, or model is introduced. |
| `016_opening_country_flavor_content_2026-08-02.md` | Accepted bounded presentation continuation | Base, evolved, and host-archetype opening and referral descriptions append the authored country-tag clause for ten named tags (`GER`, `ENG`, `FRA`, `SOV`, `USA`, `JAP`, `ITA`, `CHI`, `POL`, and `CZE`) and a safe general branch. No event, option, reward, receipt, meter, asset, or model is introduced. |
| `016_loyalty_relocation_country_flavor_2026-08-02.md` | Accepted bounded presentation continuation | The five loyalty findings and five relocation outcomes append the authored country-tag clause after their existing host-archetype clause. No mission, effect, route, reward, asset, or model is introduced. |
| `016_evolution_country_flavor_2026-08-02.md` | Accepted bounded presentation continuation | The four logged evolution reports append the authored country-tag clause after their existing host-archetype clause. No evolution, option, reward, asset, or model is introduced. |
| `016_containment_country_flavor_2026-08-02.md` | Accepted bounded presentation continuation | The sovereignty deadline, ten settlement outcomes, and invalidated-settlement report append the authored country-tag clause between existing presentation clauses. No containment effect, outcome, route, asset, or model is introduced. |
| `016_context_report_country_clause_extension_2026-08-02.md` | Accepted bounded presentation continuation | The impossible lecture, project incident, cross-domain review, rejection, temporal-contact, Antarctic-interface, and spacecraft reports `.12` through `.18` append the authored country-tag clause without changing their effects, report contracts, or asset requirements. |
| `016_country_specific_institutional_settlement_tranche_addendum.md` (commit `96fe7ed37`) | Static implementation complete; targeted validation pending | The finite ten-country layer has the conditional `.5` settlement choices, fixed bounded vectors, host-local receipts, and receipt-driven `.7`/`.8` description and AI factors in the working tree. Targeted transfer, cleanup, probability, balance, and live-acceptance evidence remain pending. Broader country chains are closed/rejected as filler, and the no-model boundary is preserved. |
| `subagent_handoffs/016_country_settlement_probability_inspect_2026-08-03.md` | Probability source inspection complete; named evaluation pending | The current `.5`, `.7`, and `.8` event-option source parses with 21 candidates, six required inputs, one unresolved source input, and no diagnostics. The incomplete pool is recorded as evidence only; no normalized probability or live-AI claim is made. |
| `016_high_speed_materials_trial_addendum_2026-08-03.md` and `subagent_handoffs/016_high_speed_materials_trial_implementation_2026-08-03.md` | Implemented bounded project synergy; targeted validation pending | The Materials plus Rocketry state-targeted trial, `.195` ownership split, host-local national board, portable proprietary envelope, ten-receipt AI bias, and transient-target cleanup are wired. No model or CBRN workaround was used. Quantitative balance, transfer/formation scenarios, and user-owned live acceptance remain open. |
| `016_nonmodel_content_closure_handoff_2026-08-03.md` | Non-model expansion loop closed | No additional non-CBRN mechanic was accepted after the portal and high-speed consumers because remaining candidates duplicate existing KRG project, recovery, foreign-countermeasure, or terminal surfaces. Parent work is validation, balance, provider isolation, and final audits; no filler route or CBRN workaround is authorized. |
| `subagent_handoffs/016_event19_portal_temporal_provider_extension_2026-08-03.md` | Implemented bounded provider continuation | Event 019 providers 509 and 510 now expose the native Portal Raider and Temporal Guard families with complete callback parity and hidden receipt cleanup. No model/entity wiring was added; live derivative and cleanup validation remains open. |
| `subagent_handoffs/016_event19_provider_localisation_audit_2026-08-03.md` | Provider localisation audit complete | Portal and Temporal hidden receipt ideas have unique declarations, add/remove calls, name and description keys, and BOM-safe localisation. The neutral profile-999 family-name fallback remains intentional under the shared provider contract; no source patch was required. |
| `subagent_handoffs/016_dormant_kruger_holder_guard_repair_2026-08-03.md` | Accepted loader-safety repair | The intentional dormant KRG holder no longer blocks fire-once availability, first appointment, or initial forwarding, while non-KRG holders still prevent duplicates. No route, reward, asset, model, or fallback change. |
| `subagent_handoffs/016_krg_cbrn_callback_boundary_reaudit_2026-08-03.md` | Native API blocker re-confirmed | The raid API still has no stable pre-reservation, operation identity, cancellation, or expiry callback. The KRG stockpile remains blocked; no parallel ledger or fallback was added. |
| `016_krg_ai_queue_loader_followup_2026-08-02.md` | Accepted loader-safety continuation | KRG AI plans use a direct `original_tag = KRG` database gate while retaining sovereign-country checks in `enable`, and the super-event queue resolves its first actor through the persisted `var:global` array scope. No route, focus, event, reward, asset, or model contract changes. |
| `subagent_handoffs/016_project_incident_family_risk_content_2026-08-02.md` | Accepted bounded presentation continuation | Commit `11e027bfe` adds fifteen family-risk clauses to the existing `.13` incident descriptions. No new incident path, reward, asset, or model is introduced. |
| `subagent_handoffs/016_incident_family_flavor_tranche_2026-08-02.md` | Accepted bounded presentation continuation | The existing `.13` descriptions now add six family-group clauses after the risk sentence. No trigger, mission, reward, receipt, asset, or model changes. |
| `subagent_handoffs/016_rejection_consequence_host_flavor_2026-08-02.md` | Accepted bounded presentation continuation | All three `.15` rejection outcomes now append the rejecting country's existing host-archetype clause. No referral transaction or receipt changes. |
| `subagent_handoffs/016_settlement_outcome_host_flavor_2026-08-02.md` | Accepted bounded presentation continuation | All ten `.31` settlement outcomes now append the host-archetype clause before the recorded policy clause. No containment effect or route changes. |
| `subagent_handoffs/016_foreign_result_host_context_content_2026-08-02.md` | Accepted bounded presentation continuation | Actor after-action branches `.101`, `.111`, `.121`, `.131`, `.141`, `.151`, `.161`, `.171`, and `.181` now read the original host target through a guarded helper. No foreign operation, transfer, receipt, or result changes. |
| `subagent_handoffs/016_news_host_context_content_2026-08-02.md` | Accepted bounded presentation continuation | Delayed milestone and regional-defeat news descriptions `.302` and `.304` through `.310` now read the global current-host target through a guarded helper. No news receipt or state transaction changes. |
| `subagent_handoffs/016_cross_event_contact_context_content_2026-08-02.md` | Accepted bounded presentation continuation | Commit `6079020c1` adds the existing host-context clause to Event 030, 025, and 036 evidence reports. No new gate, receipt, reward, asset, or model is introduced. |
| `subagent_handoffs/016_aftermath_family_report_art_wiring_2026-08-02.md` and `016_report_art_expansion_assets_2026-08-02.md` | Accepted bounded presentation and asset continuation | Four remnant report cards, eight breakthrough cards, eight incident/security cards, and the incident family selector are wired; the routed report cards were processed through the accepted sepia and transparent-card pipeline. No new fire path, reward, receipt, model, or project stage is introduced. |
| `subagent_handoffs/016_static_balance_matrix_2026-08-02.md` | Parent-owned static balance evidence | The handoff derives stage-time, capacity, cost, force-cap, foreign-operation, formation, and world-threat bounds from the live constants. It records scenario projections without claiming campaign observations, AI completion rates, live terminal timing, or whole-event balance completion. |
| Finite ten-country settlement layer | Static implementation complete; targeted validation pending | The settlement addendum has promoted documentation and static runtime implementation for conditional `.5` choices and receipt-driven `.7`/`.8` presentation and AI. Targeted transfer, cleanup, probability, balance, and live-acceptance evidence remain pending; broader country chains are closed/rejected as filler. |
| Event 016-specific 3D packages | Mixed status; Alien Infantry V13 export and static runtime registration promoted, four dedicated packages deferred | The Alien Infantry V13 model, rig, seven actions, three PDX maps, and static entity/GFX/animation/sound registrations are promoted for parent review. Its supported muzzle/effect locator is unavailable, particle/light definitions remain unbound, strict audio-role gaps remain open, and live acceptance is pending. Portal Raider, paleogenetic, xenobiological, and Temporal Guard packages remain a future approval-dependent backlog. |
| KRG biological stockpile and delivery lifecycle | Accepted design queued; blocked by native CBRN callback | The Event 016 history and native-delivery bridge are present, but quantity, reservation, production, consumption, transfer, and defeat receipts require an idempotent native reservation/outcome/cancellation/expiry callback before the isolated Event 016 tranche can start. No fallback or free payload is authorized. |
| 2D idea-icon coverage | Wired; 28 visible ideas have bespoke art | Thirty-four bespoke Event 016 idea/national-spirit DDS files are present, including the 21-icon KRG country-idea extension with all 28 visible idea `picture` assignments and technical validation evidence. |
| `docs/assets/016_brilliant_scientist/manifest.md` and `016_asset_inventory.md` | Reconciled to current core-runtime evidence | Current counters cover portraits, animations, UI, flags, focus and achievement assets, twenty-five report cards, seven news images, super-events, decisions, sound, and the complete 34-icon Event 016 idea/national-spirit surface. Quantitative evidence, live acceptance, native CBRN callback, and deferred 3D backlog remain explicit. |
| Event catalog workbook | Aligned and exported | Event 16, its four evolutions, both terminal routes, no-cluster status, and the absorbed Crazy Scientist row are aligned from the XLSX source and exported to the three CSV snapshots. The 2026-08-03 re-audit reran the exporter, found no workbook or CSV drift, and recorded the evidence in `subagent_handoffs/016_catalog_alignment_reaudit_2026-08-03.md`. |

## Contradictions and stale-document register

| File or evidence | Contradiction | Reconciliation status |
| --- | --- | --- |
| `docs/events/016_brilliant_scientist/overview.md` | Earlier presentation and blocker prose said all super-event images, later portraits, and severe animations were missing. | Status and presentation paragraphs now point to current accepted packages and the default-enabled core boundary. |
| `016_source_of_truth_map.md`, `016_brilliant_scientist_resume_packet.md`, and `docs/specs/016_brilliant_scientist_specs/handoffs/016_completion_status.md` | 2026-07-14 text described the placeholder opening and no Directorate, project, country, focus, or asset implementation. | The old records are explicitly historical; current bounded tranches are mapped here. The completion status now also records the ten-tag (`CZE` included) and terminology reconciliation, while older planning snapshots remain historical. |
| `docs/assets/016_brilliant_scientist/manifest.md` and `matrices/016_asset_inventory.md` | Historical counters showed zero for packages that now have runtime files and accepted handoffs. | Reconciled on 2026-07-29 while preserving the original production provenance tables. |
| `016_host_archetype_flavor_tranche_handoff.md` versus the current runtime | Its `.4` through `.9` list can be misread as the complete host-archetype surface after the `.10` and `.11` continuation. | Resolved by marking the initial handoff's scope as superseded and recording the continuation handoff and current `.4` through `.11` surface above. |
| Current host presentation versus KRG route reports | The first four KRG foundation reports, route-specific dossiers, and foreign-integration reports previously lacked a carried-portfolio presentation clause in the current map. | Resolved by recording `GetBrilliantScientistKrgPortfolioClause`, its active-family guards, the nineteen report consumers, and the safe default branch above. |
| Historical placeholder/default-disabled records versus current core runtime | Resume, research, prompt, and early integration records retain pre-implementation statements. | They remain historical evidence. The current pointer is this map, and the continuation handoff lists the stale prompt and status surfaces without deleting them. |
| Event 019 registry and Event 016 route forces | Event 019 providers 504-510 and 522 expose all eight Event 016 project-force families from Event 016 history, including Portal Raiders, Temporal Guards, and the separate Aryan clone refinement. | Resolved statically; live Anomalous Rising, management, defeat, final-cleanup, and MCP lifecycle validation remains user-owned. |
| Generic 3D pilot documentation versus Event 016 | The anomaly signal beacon is a complete pilot consumer but has no Event 016 route identity. | Resolved by explicitly excluding it from Event 016 model completion. |
| Default-enabled core versus deferred content | The core fire-once event is enabled while targeted scenarios, quantitative balance evidence, live acceptance, and the native CBRN callback remain incomplete. The 2D idea-icon package and the shared clone and Autonomous Robot 3D packages are wired; broader country chains are closed/rejected as filler, and the remaining 3D packages retain their individual queued or rejected dispositions. The mapped report and news surfaces are registered, including family-specific breakthrough, incident/security, and remnant cards. | Not a design contradiction: static implementation, validation-pending work, blocked native dependency, completed visual packages, closed filler scope, and remaining model backlog are separate gates. |
| Accepted country-specific settlement addendum versus current runtime evidence | The addendum promotes ten conditional `.5` choices and receipt-driven `.7`/`.8` presentation and AI, and the working tree now contains the new options, resolvers, receipts, selectors, constants, and localisation identifiers. | Resolved as static implementation complete for the bounded tranche. Targeted transfer, cleanup, probability, balance, and live-acceptance evidence remain open; no whole-event completion claim is made. The generic pool remains authoritative, broader chains are closed/rejected as filler, and the no-model boundary remains authoritative. |
| `subagent_handoffs/016_completion_audit_round2_2026-08-02.md` stale-register section | The round-2 audit recorded temporal-debt, institutional-capture, and paleogenetic/xenobiological terminology as unresolved because it predates the parent reconciliation; it also described only nine named country clauses in several current handoffs. | Historical audit evidence. The parent source-spec edits and this 2026-08-02 curator pass resolve those listed source-spec findings and add `CZE` as the tenth named tag. The audit remains retained with a dated resolution note rather than being deleted. |

## Duplicate or superseded documents and prompts

The following are retained for audit history and must not trigger duplicate production: the 2026-07-14 documentation-curator handoff, the old completion-status handoff, the old resume packet's next-action instructions, `prompts/016_brilliant_scientist_asset_prompt.md`, `prompts/016_brilliant_scientist_animation_prompt.md`, `prompts/016_brilliant_scientist_coding_prompt.md`, `handoffs/016_mandatory_continuation_prompt.md` where they say later assets or the scripted architecture are absent, and the round-2 completion audit's pre-reconciliation stale-register section. The old wording is superseded for current continuation but the files are not deleted; use this map and `subagent_handoffs/016_documentation_curator_pass_2026-08-02.md` for the current terminology and ten-tag status.

The accepted country-specific settlement addendum `016_country_specific_institutional_settlement_tranche_addendum.md` and the finite extension `016_broader_country_settlement_content_addendum.md` are current implementation inputs, not superseded planning material. Use their promoted contract and `subagent_handoffs/016_broader_country_settlement_content_implementation_2026-08-03.md` for the static-implementation-complete boundary; broader country chains are closed/rejected as filler by `016_nonmodel_content_closure_handoff_2026-08-03.md` unless a new accepted design reopens them, and no model production is authorized in the current scope.

## Remaining acceptance decisions

1. Validate Event 019 providers 504-510 and 522 through Anomalous Rising, management, defeat, and final cleanup without leaking Event 016 parent state.
2. Audit the statically implemented ten-country institutional settlement layer, including transfer, cleanup, probability, affordability, and live scenarios. Broader country chains remain closed/rejected as filler unless a new accepted design reopens them.
3. Assign an owner for the native CBRN reservation/outcome/cancellation/expiry callback, then revisit the queued KRG biological stockpile contract only after that idempotent boundary is reviewed. Do not add an Event 016 fallback or free payload.
4. Keep the completed 21-icon KRG country-idea package's parent wiring and visual evidence aligned; no further icon production is required for the current package.
5. Supply user-owned live acceptance evidence for opening/referral, all four evolutions, containment and territory loss, terminal/Fallout transitions, and Directorate GUI interaction, and review quantitative research, AI, force-production, rebellion, and terminal timing evidence before a whole-event balance-completion claim.
6. Do not route the four remaining 3D packages in the current no-model scope. The Alien Infantry V13 export and static runtime registrations are promoted for parent review, while its supported effect-point, strict audio-role, and live-acceptance gates remain parent-owned; reopen the remaining backlog only with explicit user instruction and an accepted design decision.

## Validation and limitations

- Read all 44 files under `docs/specs/016_brilliant_scientist_specs/` in full for this reconciliation, totaling 10,885 lines and 476,381 characters.
- Confirmed current filesystem counts of 25 report DDS files, 7 news DDS files, 6 super-event DDS files, 15 KRG leader portraits, 34 bespoke Event 016 idea/national-spirit DDS files, 6 severe portrait sheets, 7 KRG flag identities across normal, medium, and small surfaces, 100 focus icons, 51 achievement DDS variants, 136 decision-surface DDS files, 54 special-project DDS files, and 64 Directorate UI textures. The 21-icon KRG country-idea extension adds all previously unassigned visible idea pictures with complete static asset evidence.
- Confirmed Event 019 provider definitions for IDs 504-510 and 522, complete callback parity, the Event 016 runtime-rebuild registration caller, provider-owned derivative packages, and symmetrical cleanup. Provider 522 uses the existing Aryan clone infantry presentation and shared clone equipment contract, while providers 509 and 510 reuse the native Portal Raider and Temporal Guard battalions/equipment without adding models. MCP event inspection remains partial and normalized provider-pool odds remain unresolved.
- Confirmed the seven Event 016 route-specific sub-unit consumers. Shared clone infantry and Autonomous Robot entity/model declarations are installed outside the unit-definition file, and the Alien Infantry V13 export package has complete source/task and actual-byte reimport evidence with static entity/GFX/animation/sound registrations promoted by commit `0e724fb8a`; its supported effect point, strict audio roles, live acceptance, and the remaining packages retain their current parent-owned, queued, or rejected dispositions and are not implied by generic provider registration.
- Read the generic 3D pilot manifests and runtime handoffs and excluded their independent showcase consumers from Event 016 acceptance.
- No game launch or live save was performed. The super-event handoff records that event inspection stopped at the repository-wide issue limit, and live scenario validation remains user-owned.
- A focused Event Inspector trace of `chaosx.nr16.193` rendered the selected event but returned a partial unresolved manifest that marked three existing foreign scripted effects as missing from its active helper catalog. Direct source inspection confirms all three definitions in `common/scripted_effects/016_brilliant_scientist_foreign_effects.txt`; the analyzer limitation is retained as validation evidence rather than changing the already-wired helpers.
