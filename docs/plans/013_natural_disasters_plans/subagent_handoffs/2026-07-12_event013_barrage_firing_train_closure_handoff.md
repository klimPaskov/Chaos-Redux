# Event 013 final firing, Barrage, and train closure handoff

> Historical closure snapshot, 2026-07-12. The live-engine matrix remains queued, but the Event 013 source/provenance archive mentioned below was restored on 2026-07-26 under `docs/assets/013_natural_disasters/`. Use the current manifest and final completion audit for present retention status.

Date: 2026-07-12

## Scope

This closure responds to the final read-only firing and decision audits. It removes the remaining silent Disaster Barrage rejection path, makes direct hidden-entry rejection visible, makes every Event 013 train cost archetype-safe, and aligns the live Event 099 bridge across comments, localisation, documentation, and the event catalog workbook.

## Firing and Barrage closure

- `natural_disaster_current_family_matches_call_pool` keeps a complete family search inside the requested public family group or selected SCN-007 type.
- `natural_disaster_prepare_exhaustive_family_context` rebuilds land, atmospheric, or ocean Skyfall context for each exhaustive candidate instead of inheriting a failed path.
- `natural_disaster_exhaust_random_family_targets` runs after weighted misses for every random-family request, including Disaster Barrage and named public family groups. Specific-family calls still fail closed and are never substituted.
- `triggerable_scenario_disaster_barrage_has_selected_type_target` proves exact initiating geography before the launch button enables. Geological slope routes require their dry or wet cause; cyclone routes require a coast; wildfire requires both fuel and an initiating heat, drought, arid, wind, or convective condition; heat respects Event 051 and active heat aftermath; Weather excludes storm surge; Random and Full Catalogue retain their storm-surge leg.
- `GetTriggerableScenarioLaunchStatus` explains a terminal lock, unresolved Maximum Barrage, or absent type-compatible state on the disabled launch controls.
- `chaosx.nr13.126` and `chaosx.nr13.127` provide direct-entry and Barrage-specific rejection feedback without writing Event 013 history or pretending an impact occurred.

## Train archetype closure

All 49 Event 013 train references use `train_equipment`: 24 affordability gates and 25 negative stockpile deductions. No route grants a specific train model. Vanilla `PHI.txt` and `SOV.txt` use the same archetype check and negative archetype-removal pattern, so standard, civilian, and armored trains all contribute to warning, recovery, chain, routing, and engineer-relief costs.

## Event 099 and catalog alignment

Event 099 remains a narrow, fail-closed call into Event 013's Dust and Sandstorm family. It owns no damage logic and writes no separate history row. At the time of this historical handoff, the event name and workbook row identified it as `Dust and Sandstorm Front`, type `Bridge`, status `Implemented`; the three Event 070 comments identified the same bridge rather than an inactive placeholder. The current workbook deliberately supersedes that status with `Needs Testing` until the user-owned live-engine matrix is run.

## Meaningful validation and remaining risks

Static review covers balanced effect/trigger/event blocks, type-preserving pool membership, exact initiating predicates, all 49 train substitutions, localisation keys and BOMs, workbook readback, and direct/scenario output initialization. The live-engine matrix remains required for dynamic array-index state scoping, same-chain UI repaint, due-job execution, full warning/mission affordability, and concurrent abnormal/relief behavior. The 1,035-file Event 013 source/provenance archive was later restored under `docs/assets/013_natural_disasters/`; the port-lifeline positive-opinion relationship proxy remains an explicit engine-constrained simplification.
