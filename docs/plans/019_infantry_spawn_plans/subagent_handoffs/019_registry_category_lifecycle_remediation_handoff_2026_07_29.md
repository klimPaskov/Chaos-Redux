# Event 019 registry and ordinary-category lifecycle remediation handoff

> **Superseded provider-inventory notice (2026-08-09):** This remediation
> handoff predates provider 522, the expanded 18-ID owner-adapter census, and
> the provider-side management-cost display/profile-cache callback. Its 501-503
> lifecycle scope remains historical evidence only; use `source_of_truth_map.md`,
> `docs/events/019_infantry_spawn/systems/unit_family_coverage.md`, and
> `.tmp/event19_docs_curator_current.md` for current provider facts.

Date: 2026-07-29

Scope: resolve the focused completion audit findings for dynamic Chaos-family discovery and ordinary-country Formation Management lifecycle.

## Changed gameplay surfaces

- `common/scripted_triggers/chaos_unit_family_registry_triggers.txt`: `chaos_unit_family_current_row_allows_event19_native_draw` accepts `ordinary_mix`, `family_only`, or `both` rows when the provider-owned native receipt is positive.
- `common/scripted_effects/019_infantry_spawn_core_effects.txt`: both weighted passes use the registry-driven native-draw gate and retain provider-ID meta dispatch.
- `common/scripted_effects/019_infantry_spawn_unit_registry_effects.txt`: providers 501, 502, and 503 set `chaos_unit_family_candidate_native = 1` for their Evolution IV eligibility branches, while preserving zombie-outbreak, Death, KMB, derivative, industrial, and scenario gates.
- `common/scripted_triggers/019_infantry_spawn_triggers.txt`: the shared ordinary-category gate excludes direct-scenario actors and uses only live crisis signals, terminal takeover, success-gated revolt history, and operation state.
- `common/scripted_effects/019_infantry_spawn_muster_board_effects.txt`: the Board closes when the shared category gate becomes irrelevant, and an open Board rebuilds only while the same availability contract remains valid.
- `common/scripted_effects/019_infantry_spawn_derivative_package_effects.txt`: the parent revolt-history marker and Board close were removed from common package initialization and added only after the final exact-transfer or one-state takeover proof succeeds.
- `common/scripted_effects/019_infantry_spawn_scenario_effects.txt`: a proven dynamic breakaway marks and closes the former parent's ordinary surface only after the actor is at war and the scenario actor setup is valid.
- `common/scripted_effects/019_infantry_spawn_pulse_effects.txt`: the shared Board refresh runs before and after country management so passive quiet closeout clears `infantry_spawn_muster_board_open`.

## Registry contract

The only dedicated Event 019 registry file remains `common/scripted_effects/019_infantry_spawn_unit_registry_effects.txt`.

Future Chaos families add one aligned registry row and their complete provider callback surface from the existing parent startup path; no Event 019 family list or additional Event 019 registry file is required.

Automatic generation scans every aligned row and uses the provider's `candidate_native` receipt for weighted selection; direct requests, first-family reception, scenario, natural release, and derivative consumers use the separate `candidate_eligible` receipt.

Base zombies remain the only trainable zombie variant, while ghost and golem providers remain spawn-only through their availability rows.

## Category lifecycle contract

Evolution III and IV capability flags do not keep the ordinary category visible after the crisis has been intentionally settled and automatic generation is suppressed.

The category remains visible for unresolved generations, live or unaccounted Event 19 formations, claimants, pending openings, deferred actions, running management operations, and request cooldowns.

A completed claimant takeover, final-proven derivative revolt, or direct-scenario actor exclusion closes the ordinary surface, and the pulse clears any stale Board-open flag on passive closeout.

## Validation and disposition

The focused audit's provider-selection finding is resolved by the positive native receipts in all three Evolution IV provider callbacks and the unchanged aligned-row weighted selector.

The focused audit's ordering finding is resolved by moving the parent terminal marker and Board close to the final exact-transfer and one-state takeover success branches.

The stale Board flag finding is resolved by the pulse refresh before and after management work.

No fallback, fixed-tag substitute, family-list edit, or second Event 019 registry file was introduced.

No in-game validation was run because live consumer validation belongs to the user under repository rules; source-level registry, decision, pulse, and transaction traces were reviewed against the focused audit findings.
