# Event 019 Infantry Spawn Final Completion Audit

Date: 2026-07-18  
Role: `chaosx_event_completion_auditor`  
Mode: final read-only whole-event audit  
Audit-owned file: this handoff only

## Final verdict

**PASS — Event 019 Infantry Spawn and SCN-013 The Unbidden Muster satisfy the
current accepted specification and completion package.**

**Workbook/catalog status promotion is authorized.** The parent may promote
Event 19 and SCN-013 from `In progress` to the repository's completed status,
rerun `.tools/export_event_catalog_csv.py`, and perform the resulting
documentation/status closure pass.

Open findings after the final live-source audit:

| Severity | Count |
| --- | ---: |
| P0 | 0 |
| P1 | 0 |
| P2 | 0 |

The only accepted exceptions are the three owner-approved contracts already
promoted into the source-of-truth package:

1. controlled one-formation border trials for the four combat achievements;
2. exact recorded-formation recreate/prove/delete for loyal formation transfer;
3. the Event-019-only regional-flag route of one independent ImageGen full-flag
   raw per row followed by deterministic full-colour spot-palette normalization.

No other fallback, simplification, reduced substitute, omitted route, missing
asset class, or deferred implementation was found.

## Inputs and authority reviewed

The audit read `AGENTS.md`; the required Event 019, improvement-loop,
subagent, event-planning, event-assets, frame-animation, decision/mission,
focus-tree, super-event, and workbook skills; the mandatory offline wiki core
pages and the additional country, division, unit, technology, equipment,
achievement, graphical-asset, interface, scripted-GUI, focus, and character
pages; and the relevant official vanilla documentation for effects, triggers,
script concepts/constants, dynamic variables, localisation, arrays, unit
creation/deletion, dynamic countries, event targets, missions, and focus trees.
Vanilla dynamic-country, exact created-unit deletion, focus, mission,
scripted-GUI, on-action, and achievement patterns were used as comparison
precedents.

All 33 files in `docs/specs/019_infantry_spawn_specs/` were read, including the
eight sequential specifications, matrices, prompts, reviews, research, route
map, README, and package manifest. The accepted plan authorities were also
read:

- `docs/plans/019_infantry_spawn_plans/019_near_completion_improvement_addendum_2026_07_16.md`;
- `docs/plans/019_infantry_spawn_plans/019_mandatory_improvement_loop_closure_handoff.md`;
- `docs/plans/019_infantry_spawn_plans/source_of_truth_map.md`.

Historical handoffs were not treated as current proof where later live reaudits
or the source-of-truth map superseded them. In particular, the former regional
motif/composite pipeline, its old FAIL records, the pre-remediation regional
postprocess audit, the earlier recurring evolution-country-scan finding, and
the earlier decision-count snapshot are archival evidence rather than current
package state.

## Completion status by surface

| Surface | Status | Live evidence and result |
| --- | --- | --- |
| Event identity and firing | Pass | Event root is `chaosx.nr19.1` in `events/019_infantry_spawn.txt`; ID 19 is registered in the repeatable-event array in `common/scripted_effects/chaosx_logic_effects.txt`. The package remains Minor Repeatable, global, unclustered, non-terminal, and has no fixed country-tag dependency. |
| Baseline manifestation | Pass | `common/scripted_effects/019_infantry_spawn_core_effects.txt` implements the continuous diminishing-share state-selection curve, weighted state selection without replacement, all six ordinary formation families, one-to-three formation scaling, state/lot/template/unit/obligation ledgers, accounting, management, control, congestion, and country reporting. Larger countries receive more absolute coverage and a lower proportional share. |
| Evolutions I and II | Pass | `common/scripted_effects/019_infantry_spawn_evolution_effects.txt`, the core effects, constants, triggers, and `.101`/`.102` reports provide active and pre-fire application, organized lots, multiple serious/advanced/strange arsenal lots, and finite technology-locked equipment/template handling. |
| Evolution III | Pass | Automatic ordinary generation is suppressed at the stage while random lots use one-to-twenty-five combat battalions and zero-to-five support companies with separate quality/coherence. The live claimant package has a ceiling of three, twenty male identity profiles, eighty regional name variants, twenty army/muster scenes, claimant demands, failed-coup handling, and safe multi-state/one-state release behavior. |
| Evolution IV registry | Pass | One Event 19 registry implementation file, `common/scripted_effects/019_infantry_spawn_unit_registry_effects.txt`, consumes shared contract version 4. Providers 501/502/503 register exactly once from their existing parent startup surfaces and publish all nine callbacks. Profiles bind exactly as 501/1, 502/2, 503/3; future profile 999 is neutral and excludes all reserved IDs. Base zombie is the sole trainable-and-spawnable initial family; weak ghosts and coal golems are spawn-only. Parent-isolation, public-package, provider cleanup, and zombie training-revocation proofs fail closed. |
| First family reception | Pass | Visible pre-fire reception event `chaosx.nr19.105` freezes the strongest complete eligible row and offers cantonment, negotiated autonomy, or refusal. It contributes no ordinary manifestation history and is protected against duplicate dispatch. |
| Three natural release modes | Pass | Ordinary claimant, anomalous claimant, and claimant-independent family modes are separately frozen and proved. Ordinary claimant release does not depend on Evolution IV. Anomalous modes require the complete provider row. One-state independent-family takeover is allowed only when the complete live army is the exact frozen claimant-free family set. |
| Exact loyal transfer transaction | Pass under approved exception | The live derivative package freezes source rows and UIDs, recreates the exact recorded destination formations, proves destination identity/territory/ledgers, deletes and proves absence of only the exact source UIDs, then commits accounting. Pre-commit failure restores the source; post-commit uncertainty remains locked and fails closed. There is no fractional-transfer or fixed-tag substitute. |
| Derivative countries | Pass | Claimant, zombie, ghost, and golem actors use dynamic-country creation except the specifically designed and proved same-tag one-state takeover. They receive exact identity/region cosmetics, leadership or institutional councils, private ledgers, four idea lifecycles, management, decisions, missions, AI, former-parent wars, bounded adjacent expansion, defeat state, and proof-gated cleanup. Parent event flags, stages, progress, super-events, and world-end state are excluded. |
| Derivative focus tree | Pass | Direct brace-aware inventory found 45 unique focuses: 30 shared plus five per zombie/ghost/golem family, giving 35 visible nodes per family. All nodes are reachable; hierarchy and family transformations have complete symmetric locks; all focuses have icons, availability, rewards, player-facing reward tooltips, and nonzero AI weights. The current independent focus audit records P0/P1/P2 = 0. |
| Decisions and missions | Pass | Direct top-level inventory found 82 live Event 19 objects: 68 decisions and 14 missions. This supersedes the earlier 77-object handoff snapshot. Objects cover formation management, requests, claimants, anomalous families, derivative government/sustainment/expansion, standardization/demobilization transactions, achievements, and cleanup. Mission starts, timeouts, cancellation, ownership, costs, tooltips, and AI paths are present. |
| Ideas and country progression | Pass | The derivative package defines 42 route/lifecycle ideas. The four required tracks—government identity, doctrine/sustainment, family burden, and former-state pressure—advance through route-specific states and terminate in victory, replacement, or defeated-remnant packages without unintended stacking. |
| AI, balance, performance, and exploit safety | Pass | The live-final independent audit records zero P0/P1/P2 findings. Ordinary AI pays and manages lots, uses claimant and anomalous routes, and can reach ordinary hostile targets. Derivative AI has opening, government, family, sustainment, former-parent, and bounded-expansion profiles. Ghost decline is a bounded 180-day country-local process and uses the dedicated shared Deaths reason without Event 10 consumption/soul leakage. Evolution work uses maintained counters/receipts and bounded country pulses rather than a recurring global country scan. Transactions, trials, scenario launch, and cleanup are idempotent or fail closed. |
| Event 67 / Event 96 / absent events | Pass | Event 67 Generalissimo identity is used only when its exact country and living army-leader UID remain valid; takeover conflict uses the designed failed-coup resolver. Event 96 sets and clears `division_template_lock_event_active`, which gates Event 19 template mutations without removing Event 19's individual locks. Event 127 Warlords and Event 131 Widespread Mutiny have no live event implementations, so no invented hook or identifier was added. |
| Muster Board scripted UI | Pass | `common/scripted_guis/019_infantry_spawn_muster_board_scripted_gui.txt` and `interface/019_infantry_spawn_muster_board.gui` wire five panels—overview, formation lots, command, anomalous registry, and generation history—plus selected-lot navigation, costs, requests, claimant/family controls, invalid/locked states, and transaction gating. All 125 player-facing GUI references resolve according to the live localisation audit. |
| UI animation | Pass | The GFX file registers three real authored frame sequences and static fallbacks: muster seal 8 frames at 8 fps, critical command border 8 at 6 fps, and anomalous registry emblem 10 at 5 fps. All 26 source and processed frames are package-locally distinct; sheets, previews, contact sheets, static frame zero, DDS outputs, and animated/static visibility toggles are present. No transform-only animation substitute is used. |
| Achievements | Pass under approved trial exception | Eleven achievements, 22 name/description keys, condition tooltips, 33 DDS states, and 33 `.gfx` aliases are present. The four combat achievements use the approved one-formation border-trial contract with exact setup, callbacks, cleanup/quarantine, 14-day minimum, 45-day timeout, and 90-day cooldown. The other achievements use recorded Event 19 history and exact scenario/derivative evidence. |
| SCN-013 | Pass | Raw ID 13 is registered in the complete scenario inventory and all four view orders (name/id, ascending/descending). The shared launcher preserves four types and four intensities; the direct request wrapper and intentional no-second-confirmation bypass both validate the same 4x4 inputs. Conventional Flood, Arsenal Lottery, General Mutiny, and Anomalous Rising produce immediate formation/revolt/takeover packages. Intensity scales country share, state coverage, lots, formations, fronts, adjacent wars, and manpower. Multi-state actors receive connected noncapital regions and immediate former-parent wars; one-state/all-island hosts use proved same-tag takeover. Maximum never sets `world_end`. |
| Event Log, details, and evolution history | Pass | Event 19 name/premise, four evolution title/body records, 18 history payload title/description pairs, actor/no-actor routing, selected evolution imagery, and release/defeat outcomes are registered and resolve. Evolution activation reports intentionally have no actor; claimant, release, defeat, family, and scenario outcomes retain their relevant actor or source identity. |
| Localisation | Pass | The current live-final localisation audit records zero P0/P1/P2 findings. Event, evolution, incident, decisions, missions, focuses, ideas, achievements, scenario contexts, derivative identities, claimant identities, scripted GUI, logs, and details resolve. The 20 claimant profiles remain male and region-compatible; unsupported identity/region selection fails closed instead of using a mismatched name or image. |
| Super-event / world end | Complete by deliberate exclusion | The accepted design explicitly rejects a super-event and terminal world-end outcome because this Minor Repeatable event and SCN-013 are not campaign-ending presentations. No missing super-event, quote, audio, or terminal branch exists. |
| Advisor assets | Not applicable | Event 19 creates claimants, commanders, councils, and technical country leaders, but no political advisor, military high-command advisor, theorist, or advisor portrait dossier. Therefore the 65x67 advisor processor/render-v5.0, dossier manifest, identity-gate, style-band, reviewer, and DDS approval requirements are outside this event's asset surface. No advisor DDS claim is made. |

## Asset and wiring audit

The live asset manifest, GFX handoff, processed/runtime files, contact-sheet
evidence, machine validation, checksum file, and current independent reviews
cover every requested visual class:

| Asset family | Verified inventory and disposition |
| --- | --- |
| Report cards | 11 source PNGs, 11 processed cards, 11 runtime DDS files, and 11 registered report sprites: manifestation, four evolutions, and release/defeat for zombie, ghost, and golem. |
| Fixed identity scenes | 27 distinct ImageGen sources, processed 156x210 PNGs, pixel-equal legacy BGRA DDS files, and registered sprites: 20 claimant army/muster scenes, 6 derivative host/council mass scenes, and 1 neutral muster. Independent review found 0/27 focal individual people. The no-focal-person rule is explicitly limited to these fixed authority/identity slots; report, focus, decision, and achievement illustrations are separate asset classes. |
| Focus/decision/idea/UI | 45 focus DDS files, 47 decision DDS files with mission reuse, 9 idea icons, 3 category icons, 6 board markers, the board background, and all corresponding sprite registrations/consumers. |
| Animations | Three independent real-frame packages, 26 unique source frames and 26 unique processed frames, three sheets, three static fallbacks, previews, contact sheets, and exact runtime registration. |
| Achievements | 11 sources and 33 processed/runtime states with 33 explicit achievement aliases. |
| Regional flags | 91 unique built-in ImageGen full-flag raw records, 91 unique 820x520 RGB spot masters, and 273 native/runtime rows. The current JSON has 91 records and 273 tier entries; the checksum file has 273 rows. All normal/medium/small TGA rows are 32-bit uncompressed bottom-left-origin files and decode pixel-equal to their PNGs. |

The regional processor at
`docs/assets/019_infantry_spawn/_tooling/process_event_019_regional_flags.py`
matches the recorded SHA-256
`d87e879184d5a28a52736b80af4bc0ce70abd9744de47210b1f6a7c3db15ece6`.
The evidence records Python 3.9.12, Pillow 11.1.0, and NumPy 2.0.2. Its
immutable machine status remains
`candidate_requires_independent_visual_review`; approval is supplied by the
separate reviewer-produced PASS handoff
`019_regional_full_flag_postprocess_remediation_reaudit_2026_07_18.md`. That
reaudit independently recomputed all raw, spot-master, PNG, TGA, palette,
header, checksum, uniqueness, and decoded-pixel contracts and visually reviewed
all 91 rows. The seven GHOST_BASE prompt/archive records were independently
recovered and byte-matched. The approval record names no unapproved fallback.

The approved normalization is exactly the third exception named in the verdict.
The old 7/16 motif/composite inputs, processor evidence, validation files, and
contact sheets remain clearly marked archival/superseded and are not live
runtime sources.

## Documentation, plans, and catalog disposition

The accepted near-completion improvement addendum contained three findings.
All are implemented and promoted into the live specification/documentation:

| Accepted finding | Disposition |
| --- | --- |
| Mode-aware ordinary, anomalous-claimant, and claimant-independent releases | Implemented in the live triggers/effects, documented, and independently reaudited. |
| Pre-fire first-family reception | Implemented as frozen event `.105`, documented, localized, logged, and independently reaudited. |
| Documentation, asset, and controlled-trial inventory reconciliation | Implemented. Current regional evidence is independently approved, the two engine-constrained contracts are documented as accepted exceptions, and the package/catalog inventories are reconciled. |

No accepted plan remains queued, rejected without explanation, or absent from
the specs. No unresolved planner addendum exists.

The package contents manifest was independently recomputed during this audit:
33 listed source-package files, 33 present files, and zero line-count,
byte-count, or SHA-256 mismatches. The goal prompt remains 4,342 characters.

The workbook was opened read-only and the exact live rows were compared with
their exports:

- `Events!A20:M20` is Event 19, Minor Repeatable, unclustered, no world-end,
  with the live Event Details and four evolution records;
- `Scenarios!A12:F12` is SCN-013 with all four type labels, all four intensity
  impacts, immediate launch, connected-region/one-state safety, and explicit
  non-terminal wording;
- both rows currently and intentionally say `In progress`;
- both exported CSV rows equal their workbook rows field-for-field;
- the three current export hashes equal the reconciliation handoff:
  `7303641c56a4f5defe8827901ceda5717b1006ddd5936f76616733516fa999ce`,
  `f6f68b0bd3110ce63dc5a4c54303e9d85fb9ad859cb4b2d87897d067e1088c6f`,
  and `1b3a73517df6e97ad0237ef6c77f9d383a3e170eedf51a09f0f416448a70b5f8`.

Those status values are the final gate, not evidence of missing implementation.
This audit clears that gate and authorizes their promotion.

## Meaningful validation and limitations

The audit relied on direct live-source tracing plus the current independent
focus, decision/mission, country-package, localisation, registry/scenario,
evolution-counter, AI/balance/performance, fixed-scene, animation, achievement,
and regional-flag evidence. Task-specific recomputation confirmed the 45-focus
graph inventory, 68-decision/14-mission inventory, eleven achievements, 33/33
spec-package manifest, exact workbook/CSV parity, current export hashes, 91-row
regional JSON, 273 tier/checksum rows, current processor hash, 27 fixed-scene
files, 11 report files, 45 focus icons, 47 decision icons, 33 achievement DDS
files, and 8/8/10 animation frames.

A narrow `hoi4.event_inspect` request for Event 19 returned `INTERNAL_ERROR`
without scanning source or producing an artifact URI. Earlier independent
specialist attempts also recorded artifact-storage limits for event/focus
inspection. This is an unresolved viewer/tooling limitation, not a passing MCP
claim. It does not block the verdict because the same chains, scopes, registry
rows, graph, callbacks, and cleanup paths were inspected directly in live
source and by the current independent reaudits. No event-render or compare
artifact is claimed.

No in-game runtime result is invented. This is a source-, asset-, provenance-,
documentation-, and catalog-completion audit.

## Remaining blockers and recommended actions

There is no remaining implementation, asset, localisation, documentation,
catalog-content, plan-disposition, or validation blocker within the accepted
Event 19 scope.

Recommended closure actions for the parent:

1. promote the Event 19 and SCN-013 workbook statuses from `In progress` to the
   repository's completed status;
2. rerun `python .tools/export_event_catalog_csv.py` and verify the two promoted
   rows in the generated CSV snapshots;
3. update the small set of live documentation banners that currently say the
   final audit remains, pointing them to this PASS handoff;
4. preserve the three approved exception records and all archival/superseded
   regional evidence rather than rewriting history;
5. make the normal parent-owned commit after reviewing this handoff and the
   status-only closure diff.

`chaosx_improvement_loop_planner` is **not recommended** at closure. The event
meets the intended depth across manifestation, four evolutions, claimants,
families, derivative countries, focus routes, decisions/missions, AI,
achievements, UI, scenario, assets, and lifecycle integration, and the accepted
near-completion addendum is fully disposed. A future planner pass would be
appropriate only for a newly requested expansion outside this completed scope.

## Post-promotion closure re-audit

Date: 2026-07-18  
Scope: final stable repository state after catalog promotion and documentation
closure.

### Verdict

**PASS — post-promotion closure is complete. No blocker remains.**

Severity count: **P0: 0 · P1: 0 · P2: 0**.

- The editable workbook records `Events!M20 = Fully Functional` and
  `Scenarios!F12 = Fully Functional`. All non-status Event 19 and SCN-013 row
  fields remain identical to the content audited before promotion.
- The exported Event 19 and SCN-013 rows match the workbook field-for-field.
  Final export hashes are
  `9de3113ae365c141da13f65db6740894af8dcb4d43a1ed91e813db4179dd2d30`
  for events,
  `f6f68b0bd3110ce63dc5a4c54303e9d85fb9ad859cb4b2d87897d067e1088c6f`
  for clusters, and
  `2e73a9d27e4a16629aac2d2d503fdafc26eca41e56e3d7483023d37f5d84a0df`
  for scenarios.
- Current-state documentation consistently records Event 19 and SCN-013 as
  `Fully Functional` with no unresolved closure gate. The only remaining
  `In progress` phrases in the blocker dossier are inside explicitly labelled
  historical/superseded sections and are not current claims.
- The family registry documentation preserves the exact accepted regional
  asset chain: 91 independent unmodified full-flag ImageGen raws, 91
  deterministic `820x520` spot-palette masters, 273 native PNGs, and 273
  runtime TGAs. It also preserves the required explanation that
  `candidate_requires_independent_visual_review` is immutable processor-state
  metadata superseded for approval by the independent PASS review.
- `review/package_contents.md` matches the live specification package with
  **33/33 files**, no missing or extra package files, no line/byte/hash
  mismatch, and a `goal_prompt.md` size of 4,342 bytes.
- No Event 19 gameplay, localisation, or binary asset file changed after the
  original PASS audit. Post-PASS changes are confined to the approved workbook
  status promotion, generated catalog exports, closure documentation, review
  reconciliation, and package-manifest regeneration; no accepted mechanic was
  changed.
- The fallback and simplification recheck found exactly the three approved
  exceptions: controlled one-formation border trials for four combat
  achievements; exact recorded-formation recreate/prove/delete loyal transfer;
  and the Event-19-only 91-independent-ImageGen-raw regional flag workflow with
  deterministic full-colour spot-palette normalization. No additional fallback
  or simplification is present.

During this re-audit, stale current-state lines in the system registry/scenario
documentation and two review dossiers were identified, reconciled by the
parent, and then re-read in their final stable state. The regenerated package
manifest was independently rechecked after those changes and remains exact.

No further implementation, asset, localisation, documentation, catalog,
accepted-plan, or validation blocker remains. Event 19 and SCN-013 may remain
closed as **Fully Functional**. No further improvement-loop addendum is
recommended within the accepted scope.
