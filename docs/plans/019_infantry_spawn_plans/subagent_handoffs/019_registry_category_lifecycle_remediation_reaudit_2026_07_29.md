# Event 019 registry and category lifecycle remediation re-audit

> **Superseded provider-inventory notice (2026-08-09):** This re-audit predates
> provider 522, the expanded 18-ID owner-adapter census, and the provider-side
> management-cost display/profile-cache callback. Its 501-503 lifecycle scope
> remains historical evidence only; use `source_of_truth_map.md`,
> `docs/events/019_infantry_spawn/systems/unit_family_coverage.md`, and
> `.tmp/event19_docs_curator_current.md` for current provider facts.

Date: 2026-07-29
Role: `chaosx_event_completion_auditor`
Mode: focused read-only remediation re-audit
Audit-owned file: this handoff only

## Verdict

**PASS — the six requested registry and ordinary-category lifecycle checks are satisfied in the audited worktree.**

The prior P1 provider-selection gap and three P2 lifecycle/documentation gaps in `019_incremental_registry_and_category_lifecycle_completion_audit_2026_07_29.md` are closed.

| Severity | Open count | Result |
| --- | ---: | --- |
| P0 | 0 | No destructive registry, transaction, or scenario-isolation defect remains in this focused scope. |
| P1 | 0 | All three current providers can return the native receipt consumed by ordinary Evolution IV generation. |
| P2 | 0 | Success ordering, passive Board cleanup, direct-scenario exclusion, and current documentation are aligned. |

This is an incremental PASS for the registry-selection and ordinary-category lifecycle remediation.

It does not repeat or replace the broader Event 019 asset, focus-tree, country-package, balance, achievement, scenario-content, or catalog audits except where those systems directly intersect the six checks below.

## Snapshot audited

The decisive source and documentation hashes were:

- `common/scripted_effects/019_infantry_spawn_core_effects.txt`: `7C0C849A457C61E7D9ABF31A59B9C93268795544290B0ACFAE47B6C0B08F3978`;
- `common/scripted_effects/019_infantry_spawn_unit_registry_effects.txt`: `D613BA38DC31D9283021EF0CDA22A1CFA8AA30EC5381B73FF70073BFE7A0F081`;
- `common/scripted_triggers/chaos_unit_family_registry_triggers.txt`: `F8D845F42371BF0CE53448648E57F7EB870E36327BA208E455AEAFBC6E238514`;
- `common/scripted_triggers/019_infantry_spawn_triggers.txt`: `59E348746B8ED8F15BC7B155A7862BD697382D0F61C7DDF4937B70ECEA20AE39`;
- `common/scripted_effects/019_infantry_spawn_claimant_crisis_effects.txt`: `FB9D8AB76ACE065F687CE46AEDAA2EA015431E3E04BBE5B012A7F8FF0CE86FF3`;
- `common/scripted_effects/019_infantry_spawn_derivative_package_effects.txt`: `707A7EC5CE04B09E8BDA683A27B6B95E43FE6404510F72E1051B138ED9C0FE99`;
- `common/scripted_effects/019_infantry_spawn_muster_board_effects.txt`: `429E8B2F42F72C1A8F62F662CE2D7C22665983B1C6968509CB45C4BE0B6A2129`;
- `common/scripted_effects/019_infantry_spawn_pulse_effects.txt`: `4AE09E6D3A236C447C32A2A90406B8C0B5012C8BEDD4934366F2D30E0C098326`;
- `common/scripted_effects/019_infantry_spawn_scenario_effects.txt`: `9CD662C437740F33A884A4971046BA8CCFF4E64F4B0A75589CBB970DA9419A86`;
- `docs/events/019_infantry_spawn/overview.md`: `4ACA1E794BB0216328685223890661E370A35DF3C93C6E4D264912D995163ED4`;
- `docs/systems/cbrn_warfare/chaos_unit_family_registry.md`: `BB7E57A0678CF96BEDE8FC015927D875901588A1DDABE613C28C7734CE7D1394`;
- `docs/specs/019_infantry_spawn_specs/specs/019_infantry_spawn_spec_part_7_decisions_ui_ai_balance.md`: `D942C2BDCDF86EB392DA38571E1ACDD21F0115D399D5395409B92BB626DD29C0`;
- `019_registry_category_lifecycle_remediation_handoff_2026_07_29.md`: `23D674E2A8372316CFCFF7CE43475B795AEA790A27E9B1F47301DD23230BC50F`.

The worktree contains extensive unrelated changes.

This audit did not modify gameplay source and treated only the registry, category, Board, pulse, scenario-isolation, success-marker, and matching documentation surfaces as current evidence.

## Acceptance matrix

| Requested check | Status | Result |
| --- | --- | --- |
| Providers 501, 502, and 503 return native receipts for the ordinary Evolution IV selector | Pass | Each provider's Evolution IV eligibility branch now sets both `chaos_unit_family_candidate_eligible = 1` and `chaos_unit_family_candidate_native = 1`; provider 503 retains its documented industrial or forced-scenario condition. |
| Dynamic registry remains aligned-row and provider-ID based without a fixed family-list edit | Pass | Both weighted passes iterate the aligned global arrays, dispatch eligibility through the stored provider ID, and later dispatch template and spawn callbacks through the same provider ID. |
| Ordinary category and Board close correctly and exclude direct-scenario actors | Pass | One shared category gate owns player, AI, decision, and Board availability; scenario setup and persistent scenario actor flags exclude direct-scenario packages. |
| Claimant takeover and derivative revolt terminal markers and Board close are success-gated | Pass | Claimant closure remains inside its proved character-success branch; derivative parent marking and Board close moved from common initialization to the final exact-transfer and one-state proof branches. |
| Passive country pulse clears a stale Board-open flag | Pass | The pulse clears an already irrelevant Board before work and refreshes or closes it after management, including a crisis resolved during the same pulse. |
| Documentation and handoffs reflect the remediated state | Pass | Event documentation, registry documentation, the decision/UI specification, the remediation handoff, and this independent re-audit agree on receipts, extension ownership, scenario exclusion, success ordering, and passive closeout. |

## Provider receipt verification

The three initial providers continue to register one aligned row each from the sole consolidated Event 19 integration file.

Provider 501 is registered at `common/scripted_effects/019_infantry_spawn_unit_registry_effects.txt:4112-4127`.

Its eligibility callback at lines 4130-4142 sets both receipts for a Zombie Outbreak actor or zombie derivative and now also sets both receipts for an ordinary Evolution IV country.

Provider 502 is registered at `common/scripted_effects/019_infantry_spawn_unit_registry_effects.txt:4349-4364`.

Its eligibility callback at lines 4367-4379 sets both receipts for a Death actor or ghost derivative and now also sets both receipts for an ordinary Evolution IV country.

Provider 503 is registered at `common/scripted_effects/019_infantry_spawn_unit_registry_effects.txt:4573-4588`.

Its eligibility callback at lines 4591-4609 sets both receipts for KMB or a golem derivative and sets both receipts for an ordinary Evolution IV country when the existing forced-scenario or controlled-industrial-state condition passes.

The provider-503 condition is provider-owned eligibility, not an Event 19 family list or an undisclosed fallback.

The receipt matrix in the audited source is:

| Provider | Ordinary Evolution IV condition | `candidate_eligible` | `candidate_native` | Selector result |
| --- | --- | ---: | ---: | --- |
| 501 zombie | Evolution IV | 1 | 1 | Positive spawn weight enters both weighted passes |
| 502 weak ghost | Evolution IV | 1 | 1 | Positive spawn weight enters both weighted passes |
| 503 coal golem | Evolution IV plus forced-scenario or industrial-state condition | 1 | 1 | Positive spawn weight enters both weighted passes |

This closes the previous failure where all three Evolution IV branches left `candidate_native` at zero.

## Dynamic registry and future-provider proof

`common/scripted_effects/019_infantry_spawn_core_effects.txt:181-188` reads `global.chaos_unit_family_provider_id_entries` and meta-dispatches `chaos_unit_family_provider_[PROVIDER]_event19_evaluate_eligibility`.

`common/scripted_effects/019_infantry_spawn_core_effects.txt:191-265` performs both weighted passes over `global.chaos_unit_family_*` row indexes.

Both passes require an enabled, spawn-capable, visually supported row whose registration policy passes `chaos_unit_family_current_row_allows_event19_native_draw` and whose provider callback returns a positive native receipt.

`common/scripted_effects/019_infantry_spawn_generation_effects.txt:1634` dispatches template construction through the stored provider ID, and lines 2375-2383 dispatch unit spawning through the same provider ID.

No provider-501, provider-502, provider-503, zombie, ghost, or golem branch appears in the selector or generation dispatch.

The current registry file inventory remains:

- `common/script_constants/chaos_unit_family_registry_constants.txt` for the generic contract constants;
- `common/scripted_effects/chaos_unit_family_registry_effects.txt` for the aligned registrar;
- `common/scripted_triggers/chaos_unit_family_registry_triggers.txt` for generic row validation and the existing visual ownership contract;
- `common/scripted_effects/019_infantry_spawn_unit_registry_effects.txt` as the sole dedicated Event 19 integration file for the ordinary table and three initial providers.

The reserved 501/502/503 visual bindings remain the previously accepted initial-provider ownership contract.

A future external provider still uses the existing neutral visual profile 999 branch and therefore does not require a new Event 19 family-list, selector, name-map, picture-map, or registry-file edit.

The future provider owns its one aligned registration row, startup registration call, and complete provider-ID callback surface.

No synthetic provider was added merely to test that contract, so future-provider runtime behavior remains a structural proof rather than a live fourth-provider observation.

That is a validation limit, not a missing implementation surface in the current request.

## Ordinary category, Board, and direct-scenario exclusion

`common/scripted_triggers/019_infantry_spawn_triggers.txt:115-137` defines the live-crisis inventory without using Evolution III or IV capability flags as perpetual visibility.

The inventory covers unresolved generations, positive active-lot count, positive unaccounted formations, active claimants, deferred actions, the bounded opening, running management operations, and request cooldown.

`infantry_spawn_ordinary_management_category_is_relevant` at lines 142-153 also requires an active ordinary country and idle scenario transaction.

It rejects derivative classification, derivative creation in progress, all three persistent direct-scenario actor flags, a completed claimant takeover, and a successful derivative revolt history.

`common/decisions/categories/019_infantry_spawn_decision_categories.txt:11-18` uses only that shared gate and `visible_when_empty = no`.

`common/decisions/019_infantry_spawn_decisions.txt:10-24`, `common/scripted_triggers/019_infantry_spawn_triggers.txt:698-706`, and `common/scripted_triggers/019_infantry_spawn_muster_board_triggers.txt:10-14` reuse the same contract for the Board doorway, ordinary management actions, and Board availability.

`common/scripted_triggers/019_infantry_spawn_scenario_triggers.txt:13-21` makes the scenario transaction non-idle throughout global setup, same-tag provisional work, rollback cleanup, or dynamic-actor cleanup.

After setup, `infantry_spawn_scenario_actor`, `infantry_spawn_scenario_dynamic_breakaway`, or `infantry_spawn_scenario_takeover_actor` continues to exclude the direct-scenario package.

Same-tag scenario rollback restores the snapshot value of `infantry_spawn_claimant_takeover_complete` at `common/scripted_effects/019_infantry_spawn_scenario_effects.txt:2213-2214`, so a failed scenario transaction does not retain a newly synthesized claimant terminal marker.

Dynamic direct-scenario breakaway parent marking at `common/scripted_effects/019_infantry_spawn_scenario_effects.txt:2629-2648` occurs only in the validated branch after the actor has its derivative or claimant identity and a proved war with its former parent.

Direct-scenario actors therefore cannot expose or operate the ordinary category or Muster Board during either provisional setup or their persistent isolated package.

## Claimant and derivative success ordering

`common/scripted_effects/019_infantry_spawn_claimant_crisis_effects.txt:204-235` proves the selected claimant index and living selected character before executing the takeover.

Only that success branch sets `infantry_spawn_claimant_takeover_complete` and closes the Board at lines 219-220.

The common derivative initializer at `common/scripted_effects/019_infantry_spawn_derivative_package_effects.txt:475-594` no longer writes the former-parent revolt-history marker or closes its Board.

The exact multi-state natural derivative path performs the final actor ownership, selected-state, control, core, parent-state, global-accounting, and source-accounting proof at `common/scripted_effects/019_infantry_spawn_derivative_package_effects.txt:5310-5316`.

Only inside that passing branch does the former parent receive `infantry_spawn_achievement_mark_claimant_or_derivative_revolt` and `infantry_spawn_muster_board_close` at lines 5317-5318.

The actor history, lock release, transaction cleanup, and `infantry_spawn_natural_derivative_revolt_succeeded = 1` then complete in the same success branch at lines 5319-5329.

The one-state same-tag path performs its package, isolation, ledger, family/provider, nonce, row-count, and division-count proof at lines 5491-5509.

Only inside that passing branch does it mark the revolt and close the Board at lines 5510-5511, followed by history, unlock, cleanup, and success at lines 5512-5522.

Pre-commit recovery and post-commit quarantine branches contain no parent success marker or Board-close call.

The success marker is therefore no longer an initialization proxy and no failed natural derivative transaction records a successful revolt history.

## Passive Board cleanup

`common/scripted_effects/019_infantry_spawn_muster_board_effects.txt:456-464` defines `infantry_spawn_muster_board_close_if_irrelevant`.

It clears `infantry_spawn_muster_board_open` only when the flag is set and the shared ordinary category gate is false.

`common/scripted_effects/019_infantry_spawn_pulse_effects.txt:9-14` calls that helper at the beginning of the country pulse, covering a passive mission, deferred action, or lot closeout that completed before the pulse.

The same pulse calls `infantry_spawn_muster_board_refresh_if_open` at line 74 after management, claimant, family, and evolution work.

That refresh rebuilds a still-valid Board and closes an invalid Board through `common/scripted_effects/019_infantry_spawn_muster_board_effects.txt:541-550`.

The pulse entitlement at `common/scripted_triggers/019_infantry_spawn_triggers.txt:155-173` retains evolved ordinary countries through claimant-system or anomalous-registry receipts, and active lots or deferred actions retain the pulse while passive work can still resolve.

Scenario actor setup and same-tag transaction completion also schedule the same country pulse.

The stale Board-open state identified by the previous audit is therefore cleared without a new global recurring on-action.

## Documentation and handoff disposition

`docs/events/019_infantry_spawn/overview.md:380-397` now documents aligned-row discovery, row policy, the separate provider receipts, provider-ID dispatch, and future provider ownership.

`docs/events/019_infantry_spawn/overview.md:605-608` now documents quiet closeout, final-proven claimant or derivative closure, direct-scenario exclusion, and passive pulse cleanup.

`docs/systems/cbrn_warfare/chaos_unit_family_registry.md:13-32` replaces the stale pre-incremental completion paragraph with the current registry and lifecycle disposition.

`docs/systems/cbrn_warfare/chaos_unit_family_registry.md:46` documents the distinction between `candidate_eligible` and `candidate_native`.

`docs/specs/019_infantry_spawn_specs/specs/019_infantry_spawn_spec_part_7_decisions_ui_ai_balance.md:23-49` promotes the live category inventory, final-success ordering, quiet evolved closeout, and direct-scenario invalidation into the source specification.

`019_registry_category_lifecycle_remediation_handoff_2026_07_29.md` records the implementation changes and explicitly resolves the prior provider, success-ordering, and passive Board findings.

The amended remediation handoff now names `common/scripted_effects/019_infantry_spawn_muster_board_effects.txt` and accurately describes the shared irrelevant-close helper and availability-gated refresh behavior.

The amended implementation inventory and this independent re-audit now provide complete matching evidence for the Board helper and pulse call chain.

The earlier FAIL audit remains valid historical evidence for the pre-remediation hashes.

This re-audit supersedes its open findings for the current hashes and becomes the focused completion authority for the 2026-07-29 registry and category lifecycle increment.

## Meaningful validation performed

The audit re-read `AGENTS.md`, the Event 19, improvement-loop, subagent, event-planning, and decision/mission skills, the relevant mandatory offline wiki pages, and the official vanilla decision, effect, trigger, and script-concept documentation already selected for this incremental surface.

It traced both weighted selection passes, all three current registration rows and eligibility callbacks, provider-ID template and spawn dispatch, registry-file inventory, direct-scenario transaction and persistent actor exclusions, same-tag rollback restoration, claimant takeover, both natural derivative success paths, failure branches, Board visibility, and both pulse cleanup positions.

It compared the live source with the prior FAIL audit and the remediation handoff rather than accepting either document as implementation proof.

No `hoi4.event_inspect` run was needed because the decisive evidence is provider receipt assignment and synchronous effect ordering rather than an unresolved event-chain relationship.

No in-game validation was run because live consumer validation belongs to the user under repository rules.

## Remaining limits and blockers

There is no remaining implementation, documentation, asset, or accepted-plan blocker in this focused scope.

Provider 503 remains intentionally conditional on its provider-owned industrial or forced-scenario eligibility gate.

No future fourth provider exists in the worktree, so extensibility was proved structurally through aligned-row iteration and provider-ID dispatch rather than through a live fourth-provider run.

No fallback, fixed-tag substitute, family enumeration, second Event 19 registry file, undocumented simplification, or unresolved future-work placeholder was found in the remediation.
