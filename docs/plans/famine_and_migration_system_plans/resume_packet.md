# Famine and Migration Resume Packet

Status: The implementation has a reconciled documentation ledger, but parent-owned runtime and engine-evidence gates remain open.

## Current truth before resuming

- The category starts hidden and `famine_migration_refresh_decision_phase_from_state` reveals emerging only at sustained or large food, incident, flight, trapped, or reception thresholds.
- `famine_migration_retire_inactive_displacement_country` removes dormant scheduler entries without clearing integrated, resettled, or returned historical ledgers.
- The food evaluator uses the documented weighted formula, and trapped population contributes normalized pressure to both need and vulnerability.
- Exact transfer debits origin once, logs route deaths separately, credits survivors only, restores any short-credit residual to origin with manpower rollback, and requires a positive debit with zero conservation residual.
- The sparse scheduler processes registered arrays through the host-only coordinator and does not add a whole-world recurring scan.
- The ordinary decision package contains 26 supporting decisions and three missions, with no shared scripted GUI.
- Exactly two dedicated package mapmodes exist: `famine_state_map_mode` and `migration_state_map_mode`.
- All 15 historical profiles, eight achievements, CXT fixture, package assets, Deaths texticons, and Event 149 retirement status are documented in `docs/systems/famine_and_migration_system.md`.

## Parent decisions still required

1. Decide whether bounded relief/pressure is the accepted food-reserve representation or whether a distinct reserve stockpile is required.
2. Route owner-local pressure and direct-death adapters only where authoritative owners exist.
3. Resolve the report-picture carrier conflict and assign report-event registry and report-consumer ownership without creating a replacement random-event ID or pool entry.
4. Rerun the named 20 weighted scenarios through probability inspect and compare after a stable MCP route is available.
5. Complete the achievement audit for disqualifier producers and lifecycle evidence.
6. Retain or resolve the mapmode GUI/render and decision-file event-lint MCP blockers.

## Evidence to preserve

- Current map MCP artifact: `hoi4-agent://workspace/mod_chaos_redux_ea3b2d67c2c0/artifact/34498d56d4bf765796f793b12431c8e42bf07506d9484b1c7f3a961900f58b1d/66c0aca0d881147df54e388a8d987bf4f8422ed0c1b6fa779fc8ea008ddb3eb0/map-inspect.456c28c5a8e6bad1.json`.
- Event 149 catalog alignment is present in export commit `b09a91d59`; row 149 is marked unavailable and describes retirement/absorption with no replacement event.
- Decision-file event lint: accepted request, no diagnostics, timeout at 180 seconds with the explicit file selector and helper expansion documented in the curator handoff.
- Probability evaluation: post-change 20-scenario attempt timed out after 180 seconds and is not a balance result.
- Mapmode GUI route: hardcoded `mapmodes` window modeled zero elements and render timed out without a payload.

Do not treat source-only checks, asset round trips, or localisation-key coverage as gameplay completion.
