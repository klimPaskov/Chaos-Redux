# Famine and Migration Systems Completion Report

Date: 2026-08-26.

Verdict: incomplete. The accepted famine and migration designs are implemented as separate source-owned mechanics connected by explicit causal adapters and neutral conservation primitives. The current isolated exploration, architecture, decision/mapmode, probability, and completion audits have finished. Completion cannot be claimed until the unresolved exact-owner integrations, probability certification, and dynamic mapmode execution evidence listed below are closed.

## Ownership and changed surfaces

Famine runtime identifiers use `famine_*`; migration runtime identifiers use `migration_*`. Neutral infrastructure is limited to `civilian_transfer_*` and narrow `humanitarian_*` corridor, runtime, cost-presentation, validation, and achievement contracts. The former `civilian_response_*` cost-display namespace was atomically renamed to `humanitarian_cost_*` / `GetHumanitarianCost*`; its 15 selectors, 30 ready/blocked keys, and 81 famine/migration consumer calls now resolve under the approved neutral boundary. A current scan of `common`, `events`, `interface`, `localisation`, `gfx`, and `history` finds no `famine_migration_*` or `fm_*` runtime identifier across 2,188 searched files. The exact namespace boundary and historical-document exception are recorded in `namespace_separation_validation.md`.

The principal source packages are:

- Famine: `common/script_constants/famine_*.txt`, `common/scripted_effects/famine_*.txt`, `common/scripted_triggers/famine_*.txt`, `common/decisions/famine_decisions.txt`, `common/decisions/categories/famine_decision_category.txt`, `common/dynamic_modifiers/famine_state_modifiers.txt`, `common/scripted_localisation/famine_scripted_localisation.txt`, `common/scripted_guis/famine_report_header_scripted_gui.txt`, and the famine interface/localisation/GFX package.
- Migration: `common/script_constants/migration_*.txt`, `common/scripted_effects/migration_*.txt`, `common/scripted_triggers/migration_*.txt`, `common/decisions/migration_decisions.txt`, `common/decisions/categories/migration_decision_category.txt`, `common/dynamic_modifiers/migration_state_modifiers.txt`, `common/scripted_localisation/migration_scripted_localisation.txt`, `common/scripted_guis/migration_report_header_scripted_gui.txt`, and the migration interface/localisation/GFX package.
- Neutral contracts: `common/scripted_effects/civilian_transfer_effects.txt`, `common/scripted_triggers/civilian_transfer_triggers.txt`, `common/scripted_effects/humanitarian_corridor_effects.txt`, `common/scripted_triggers/humanitarian_corridor_triggers.txt`, `common/scripted_effects/humanitarian_runtime_effects.txt`, `common/on_actions/humanitarian_runtime_on_actions.txt`, and the humanitarian achievement files.
- Integration owners: Air Winter/Fallout, camps, CBRN occupation, Black Plague, natural disasters, genocide, Condemnation, Chaos Meter, achievements, mapmodes, and the affected Event 005/006/014/015/021/033/043/050/051/095 owner surfaces.

The deleted combined files are not compatibility shims. Permanent documentation is split between `docs/systems/famine_system.md`, `docs/systems/migration_system.md`, and `docs/systems/civilian_transfer_system.md`.

The post-separation lifecycle review removed broad cleanup coupling from both owner jobs. `famine_state_can_retire` now proves only famine recovery and retains the state while its famine-owned reception-demand projection is positive; the famine registered-state job no longer clears destination proofs, refreshes migration safety projections, reconciles or cleans cohorts, or refreshes the migration category. Its recovery paths call `famine_cleanup_state_registration` directly. A valid state leaving migration displacement uses migration-only spontaneous-movement, cohort, registry, modifier, and presentation cleanup. The dual-owner neutral cleanup wrapper is reserved for a physically invalid state that cannot remain in either sparse registry. Migration obligations and their sparse registry survive independent famine recovery, while famine evaluation and mortality survive ordinary migration retirement.

The same review found an undefined `migration_consume_famine_food_safety` call, unproduced `famine_to_migration_*` fields, and raw famine reads in migration capacity, destination, and decision safety. The parent patch added a famine-owned proven schema with monotonic generation/revision, bounded stage/pressure/component fields, explicit invalidation, and a migration-owned validator/consumer. Registered migration states, registered reception candidates, and adjacent destination prepasses now consume only that projection. Untracked states fail closed and are not coerced to stable. The reverse trapped-population connection uses a migration-owned versioned reception-demand receipt consumed into a famine-owned projection; famine no longer reads the migration trapped ledger. The dead, zero-caller migration reserve-donor pool was deleted; famine retains its separate live reserve-donor selector.

Famine survivor pressure and organized evacuation now cross explicit migration-owned request seams. Migration alone reconciles existing obligations, applies the protected population floor, accepts the exact flight amount, stages the cohort and trapped obligation, runs the exact transfer, arms the evacuation-protection mission, maintains mission slots and deadlines, and refreshes its presentation. The famine decision proves its own food-security cause and consumes the neutral transaction result without reading or mutating migration reception, preparation, displacement, cohort, mission, slot, or trapped-population ledgers.

The final runtime call/definition census found and removed stale split-era validation, registry, flight-request, reserve, corridor, forced-transfer, achievement, and cohort-history call names without adding compatibility aliases. General famine relief keeps its foreign-donor weighted pool, while same-country safer-state requisition uses a separate famine-owned deterministic selector over physical neighbors and existing positive reserves; it initializes no candidate and has no fallback. Migration corridor completion submits a neutral finalized receipt to the achievement owner rather than mutating famine achievement state directly. Famine relief, destination, capacity-dirty, and corridor AI consumers now cross narrow migration-owned policy, persecution, safety, availability, and candidate-guarded seams instead of reading migration ledgers.

## Famine formula and mortality

The state food-security score is:

`clamp(((1.15 * production) + (1.10 * transport) + (1.25 * extraction) + (1.00 * need) + (0.80 * environment) + (0.90 * vulnerability) + (0.95 * governance) - (1.20 * relief)) / 7.15, 0, 200)`.

Stage entry thresholds are 25, 50, 75, and 100 for supply strain, acute shortage, famine, and catastrophic famine. Recovery thresholds are 20, 40, 60, and 80 with longer confirmation windows. The reserve ledger stores amount, capacity, target, daily need, logistics factor, depletion, replenishment, relief consumption, imports, and exact reserve transfers; it is not a flat modifier fallback.

`famine_apply_mortality` reads live population as `state_population_k * constant:chaos_meter_deaths.people_per_k`. Acute shortage, famine, and catastrophic famine use population-scaled base pulse rates of 0.0004 per 14 days, 0.0015 per 7 days, and 0.0030 per 3 days after their minimum exposure. The request is multiplied by bounded exposure, vulnerability, access, extraction/policy, environment, governance, and relief factors, then clamped to the live population above a dynamic protected floor.

The physical debit is performed once through `apply_exact_state_civilian_population_loss`. The exact applied amount is written once to Chaos Meter under `constant:chaos_meter_deaths_reason.famine`, localized as `From famine`. No fixed historical death total is used.

Island blockade pressure requires war, island/isolation proof, maritime dependence, port or route disruption, convoy or escort shortage, inadequate local supply, and no proven relief corridor at the same time. An island or a war flag alone cannot satisfy it.

## Exact civilian transfer proof

`civilian_transfer_civilians_exact` validates the current origin host and route before population mutation, measures origin population before and after the one physical debit, clamps route deaths to that measured debit, and requests destination credit only for survivors.

The transaction enforces:

`actual origin debit = route deaths + exact survivor credit`.

If the destination credits fewer survivors than requested, the positive residual is restored to the origin before the transaction may validate. Route deaths and global conservation totals do not commit until reception, bind, and cohort finalization all return exact matching receipts; the Deaths call has population application disabled because the origin debit already contains them. If a post-debit receipt fails, the shared wrapper reverses the exact reception deltas, removes the destination survivor credit, restores the measured origin debit, zeroes the public transaction receipts, and consumes pending authority. A defensive partial-inverse branch quarantines both touched states and their owner countries so no later transfer can replay or compound an engine-level rollback failure. Movement without route deaths therefore changes no death total. Successful cohort bind, reception, history, reconciliation, reward, and cleanup paths are guarded by the finalized exact receipt.

The 2026-08-26 architecture audit found that forced movement, spontaneous movement, and corridor evacuation still called the undefined migration-era name `civilian_transfer_execute_exact_transaction`. All three owners and their helper documentation now call the sole canonical entry point `civilian_transfer_execute_transaction`. The parent review then reconciled each caller's complete input contract: forced and spontaneous movement save `civilian_transfer_route_destination` and provide the canonical route proofs, while the corridor supplies the canonical cohort id and destination food/reception proofs. The same reconciliation pass replaced eight stale constant-category names with the defined split categories: `humanitarian_runtime`, `civilian_transfer_route_result`, `humanitarian_population`, `migration_core_reconciliation`, `migration_presentation`, `migration_state_modifier`, `humanitarian_pressure_source`, and `famine_food_stage`. The neutral route-damage projection now lives in this transfer owner and is refreshed independently from both sparse state jobs. No compatibility alias, duplicate transfer primitive, combined namespace, or tuning-value change was introduced. The current transfer effect hash is `e8c9de30fba8dcfb20bc242bb094d8d435c9144143ae9906ca9313adc09d9a0a`; the trigger hash remains `0e7888e818fc2a63d70925e213faaa2057c2d51b773bd7a7d6521f13776d9db3`.

Migration-owned direct forced-displacement deaths use `constant:chaos_meter_deaths_reason.forced_displacement`, localized as `From forced displacement`; route owners cannot pass the same amount through a second direct-death helper.

## Migration lifecycle, borders, and capacity

Implemented migration paths cover internal displacement, cross-border flight, organized evacuation, deportation, transit, trapped populations, reception, controlled medical reception, distribution, local integration, third-country resettlement, voluntary return, and forced return.

Aligned sparse ledgers retain exact cohort identity, original state/country, current host, living amount, cause, status, generation, visit history, and terminal resolution. Reception capacity is recalculated from explicit shelter, transport, medical, administrative, food, war, control, contamination, outbreak, and current-load evidence and fails closed when its revision is absent or dirty.

Closed borders preserve the unresolved people obligation. Valid closure and enforcement paths can create trapped-population, humanitarian, diplomatic, security, political, food, reception, and mortality consequences; they do not silently delete a cohort. Ideology affects weights only after validity and is bounded beneath persecution, camps, genocide, bombing, famine, occupation conduct, contamination, route danger, and capacity.

## Player presentation and decisions

There is no combined category. `famine_decision_category` and `migration_decision_category` are independent and use `visible_when_empty = no`.

Famine is hidden while stable and reveals for a genuine starting problem or the first state reaching supply strain. Its three player-facing status outputs are Food Security, Food Reserves, and Relief Access.

Migration is hidden at campaign start unless a starting displacement problem already satisfies the same evidence contract. It reveals only through the centralized repeated/large/sustained threshold: repeated incidents, a valid corridor case, large flight, trapped population, or material reception load. A valid pending humanitarian-corridor offer is also an exact reveal fact so the accept/reject response pair cannot be hidden while the country is otherwise dormant. Durable resettled and returned population totals remain mapmode/history evidence but are excluded from the reveal gate, so a later bounded state refresh cannot reopen a resolved category from historical outcomes alone. Its three player-facing values are Displacement Load, Reception Capacity, and Border Policy. Phase and current-priority strings are guidance, not extra meters.

The two decision source files contain 28 weighted actions and six missions. Humans and AI share the same availability, route, capacity, safety, policy, target, and ownership gates. Famine role selectors mutually exclude overlapping reserve, maritime, diplomatic, evacuation, and safer-state responses so a valid state exposes no more than six primary actions. Migration's Boolean phase audit likewise has a six-action maximum: under an active controlled-border crisis with no current reception load, the compatible rows are close border, two evacuation choices, corridor negotiation, reception expansion, and transit. `migration_close_border` now hides once the reception policy is already closed, while `migration_transit_only` is visible only for controlled or humanitarian-open reception, matching its existing availability rule; the former closed-policy seven-row presentation witness is therefore impossible. Famine evacuation starts a migration-owned protection mission through migration's slot counter and timing table. `famine_register_initial_incident` and `migration_register_initial_incident` are accounting and presentation seams only; they do not create event objects, event IDs, event-pool entries, catalog rows, event-log rows, random events, or pacing pulses. Compact famine-owned and migration-owned report headers consume the seven report images; no shared full scripted GUI was introduced.

The final decision-owner patch closes five delayed-resolution no-op refund paths: `famine_evacuation`, `migration_evacuate_vulnerable`, `migration_evacuate_workers`, `migration_voluntary_return`, and `migration_forced_repatriation`. Each refunds the activation cost only when a delayed resolution completes with zero debit, without creating a transfer, death, event, or additional player-facing value. The current decision source hashes are `966b31a3e49d5a0ad11e3483997e5f0567d0d83837b3c074fa293766ea4cc491` for famine and `bd748779a4eb8a0037215a58be3f92597973f22be9e668a35efdc3cef9a3a6a6` for migration. The earlier post-density full-pool inspection returned `PROBABILITY_SOURCE_INSPECTED` with 18 candidates and source hash `ef14ce12e61338d733440d0fdc85bc1ede5a883b767cc7fe7ef48edf7327d779`; the reconstructed before/current comparison `probability-320ed8c0ab9e18a15234344a` returned `PROBABILITY_ANALYZED_PARTIAL`, `comparisonChanges=0`, and 94 explicit unresolved items. That artifact predates the final owner-separation weight patches and is retained only as historical partial evidence; the current comparison disposition is recorded in the probability section below.

### Authoritative value count

Famine has exactly three canonical player-facing values: Food Security, Food Reserves, and Relief Access.

Migration has exactly three canonical player-facing values: Displacement Load, Reception Capacity, and Border Policy.

Internal conceptual components and ledgers, including food inputs, reserves, relief, cohorts, routes, reception, custody, mortality, generations, revisions, transaction receipts, scheduler state, and diagnostic projections, are not additional player-facing mechanics.

Raw variables, flags, temporary values, and implementation-only receipts do not increase the count; mapmode, report, and tooltip context remains contextual presentation.

## Sparse runtime

Recurring processing remains on the existing host-only coordinator and sparse registered arrays. Exact callbacks enqueue or invalidate affected countries/states. Migration retirement and dormancy read only migration-owned obligations; a famine-only state or mission no longer keeps a country or state in the migration scheduler. Trapped-population pressure crosses through `migration_publish_reception_demand` and `famine_consume_migration_reception_demand`; famine reads only the validated famine-owned copy. The package adds no recurring whole-world daily, weekly, or monthly scan. The only broad iteration in `humanitarian_runtime_on_actions.txt` is the one-time `on_startup` country pass that freezes natural-campaign achievement baselines before famine mortality can begin; the CXT repair path is `on_daily_CXT` only.

Annexation, state-control change, war, peace, naval invasion, paradrop, and nuclear callbacks perform bounded cleanup or reassessment. Tag-switch disqualification is called from every authoritative `change_tag_from` continuation currently present in the repository.

## Adapters and exact blockers

Owner-local callers exist for Air Winter/Fallout, camps/Gulags/forced labor classification, CBRN chemical aftermath, Black Plague, Event 013 natural disasters, nuclear strikes, exact deportation, and famine/migration Condemnation wrappers. Famine-generated desired flight enters `migration_accept_famine_survivor_request`, where migration owns reconciliation, reservation, protected-floor capping, accepted amount, ledger writes, and category refresh. Public request APIs exist for occupation, bombing, nuclear/fallout, outbreaks, disasters, war, peace, events, clusters, scenarios, blockade, biological warfare, persecution, camp/custody, and migration route causes.

The adapter contract intentionally rejects calls lacking state, people/pressure amount, actor, causal dimension, generation, revision, request identity, route, or cohort proof. The following remain exact source-data blockers rather than guessed integrations:

- Generic occupation-law changes do not expose a state-local affected-people amount and responsible actor transaction. A dedicated state-control audit confirms that `on_state_control_changed` supplies the exact state and old/new controllers and permits reading the current occupation law, but does not distinguish hostile capture from peaceful transfer or allied liberation and supplies no replay-safe affected-people receipt; normalized occupation-profile points and total state population are not substitutes.
- Generic strategic bombing does not expose a complete exact attacker/state/amount receipt to this package.
- Country-level war and peace callbacks do not prove a state-local food loss or a cohort/destination/route transaction.
- Generic cluster and scenario queues carry identity/intensity, not exact state food or cohort facts.
- Several relevant event owners do not expose exact cohort, route, actor, and people receipts at their accepted call points; Events 118, 120, and 131 have no source and were not fabricated.
- Some existing external hazard calls reach the split adapter but still fail closed until their owner supplies the complete replay-safe proof envelope; an already-applied death amount is not reused as proof of a separate famine or migration transaction.

These are missing accepted integrations, so the goal remains incomplete.

The fresh isolated repo-explorer re-audit confirms all six generic/source-recovery blockers and preserves the exact narrow exceptions in `subagent_handoffs/current_owner_blocker_reaudit.md`. It also records partial Event 013, cluster, and scenario MCP traces/renders; dispatcher metadata remains insufficient as population or cohort proof.

A 2026-08-26 parent source-recovery pass checked the full current tree, all Git objects and filename history, and the editable catalog export for Events 118, 120, and 131. No historical root object or deleted event script was recoverable. The catalog marks `Plague of locust`, `Massive Volcano Eruption`, and `Widespread Mutiny` unavailable. Event 013 has complete volcanic-eruption and massive-eruption owners that already call the natural-disaster adapter, and Event 019 has a separate General Mutiny manual scenario, but neither source is treated as an alias for the missing Event 120 or Event 131 root. Creating those three event owners or merging their identities into existing content would require new accepted event design and is not inferred by this system implementation.

The same current callsite census confirms that `famine_request_occupation_pressure`, `famine_request_bombing_pressure`, `famine_request_war_pressure`, `famine_request_peace_pressure`, `famine_request_cluster_pressure`, `famine_request_scenario_pressure`, their migration counterparts, and `famine_condemn_relief_obstruction` remain definition-only APIs. No concrete caller now supplies the required state or cohort, positive amount, actor, cause, generation, revision, and replay identity. A dedicated relief-obstruction audit confirms that successful foreign and corridor relief measure reserve debit/credit rather than affected people, while corridor rejection carries route identity but no complete people-denominated condemnation receipt. Reserve units, trapped aggregates, evacuation amounts, survivor credit, and deaths are not substitute obstruction populations. Leaving these APIs fail-closed avoids duplicated population, synthetic casualty amounts, and dispatcher metadata masquerading as a civilian receipt.

## Historical profiles

Fifteen profiles are implemented: Soviet famine memory, Henan 1942, China policy famine, Bengal 1943, Vietnam 1944, Java 1944, Greece 1941, Leningrad siege, Dutch Hunger Winter, Spain in the early 1940s, Irish famine memory, Ceara, Congo interaction, Ethiopia policy, and global nuclear winter.

Each profile resolves current state, date, owner/controller, policy, war, route, food, memory, or Air Cleanliness context before registration. Profiles seed bounded causal context and never apply a fixed historical death total.

## Mapmodes

Exactly two dedicated state mapmodes exist and are intentionally understandable from campaign start:

- `famine_state_map_mode`: stable, supply strain, acute shortage, famine, catastrophic famine, plus famine-owned blockade, relief, and pressure-direction detail.
- `migration_state_map_mode`: lifecycle-priority state for preparation, internal/cross-border movement, evacuation, transit, trapped populations, reception/overcrowding, resettlement, and return, plus route/corridor/reception detail.

No combined or third mapmode exists. The migration mode does not read famine relief or food stage; neutral corridors appear only in the operation-specific presentation appropriate to each mode.

The mandatory map route returned `MAP_INSPECTED` for representative states 1, 64, 282, 290, and 452; definitions, geometry, state membership, adjacency, supply, and railway substrate checks passed. It cannot execute scripted mapmode colors or tooltips, and its workspace validation surfaced unrelated `MAP_BUILDING_POSITION_INVALID` and `MAP_PORT_ADJACENT_SEA_INVALID` diagnostics after truncation. The current `MapmodesInterface_Ingame` GUI route inspected 101 elements separately under `famine_mapmode_button_start` and `migration_mapmode_button_start`, then rendered the linked button-window layout for both scenarios at 1920x1080 and 2560x1440 across normal, hover, selected, and missing-localisation states. It cannot inject an active custom mapmode or evaluate state-scoped scripted colors and tooltips, so the retained result is static layout evidence rather than dynamic-color, selected-button, tooltip-expansion, or live click proof. Exact artifacts are recorded in `mapmode_validation.md`.

## AI probability evidence

The final 2026-08-26 isolated probability pass found complete declared lists of 10 famine candidates at MCP source hash `729c115b88697b86b2e70988fc1b093361a9220c673a94f2451b68f7613a5e74` and 18 migration candidates at `84fa372dc07e66a1b07c299486834ce3112db3b03b8ecae2e8ca2022f014665a`. The frozen local decision-file SHA-256 values are `966b31a3e49d5a0ad11e3483997e5f0567d0d83837b3c074fa293766ea4cc491` for famine and `bd748779a4eb8a0037215a58be3f92597973f22be9e668a35efdc3cef9a3a6a6` for migration. The `decision_ai_will_do` adapter exposes no rows, so the accepted manifest inspection uses the suggested `mission_ai_will_do` adapter, whose semantics are score-only with no normalized probability or time distribution.

Current flat-fact evaluations cover all four named famine fixtures and all eleven named migration fixtures at the final MCP source hashes. Famine analysis `probability-ee3074ce9bf0db80f330bf06` covers 40 candidate rows with 15 unresolved rows and two information diagnostics at scenario hash `638129d61ba378c2ecf61a438d17216d7a2dc976dcbd2e3e92b5e552e99fca6e`. Migration analysis `probability-967ff4fb246a0c8c86c5eeb4` covers 198 candidate rows with 60 unresolved rows and seven information diagnostics at scenario hash `8c8eb15e528b68603c943cfcf1b47708c56bd1e792b95059293dd62c1aa1da23`. This improves the historical unresolved counts from 23 to 15 and from 77 to 60. A bounded famine probe further isolates the special decision target `FROM`; even a schema-valid `eventTargets = { FROM = "state:1" }` fixture does not bind it. The final frozen sources therefore have materially stronger named-scenario score evidence, but not exact named-scenario certification.

Dynamic-pool inspection also remains incomplete: migration destination selection exposes four live declared entries but cannot discover dynamic neighbor candidates, famine opposition exposes seven declared channels but remains an incomplete custom pool, and famine relief-donor selection remains an incomplete dynamic registry with zero discovered candidates. No current sweep, normalized probability, timing distribution, donor/destination/opposition ranking, dominance, starvation, repetition, cleanup-sequence, or exploit-safety claim is certified.

Owner-applied weighted changes exist in the final separated sources: famine decisions now use famine-owned reception demand and famine-stage factors, migration corridor responses use the migration-owned humanitarian-open policy trigger, and famine relief selection uses narrow migration-owned persecution and reception-policy seams. The accepted distinct-path comparison used exact temporary pre-owner-separation copies and the frozen current files, then deleted the temporary runtime copies. Famine analysis `probability-4afd69e0e79bd79eefce0d1a` returned zero modeled score changes with 46 unresolved items across four named scenarios. Migration analysis `probability-35fd3b50d64d8a2cbbed5af4` returned six bounded rows with 134 unresolved items and four information diagnostics across three named scenarios; the six rows are two corridor modifiers repeated in all three scenarios. The later final-source flat-fact matrices reduce unresolved coverage to 15 famine rows and 60 migration rows without changing the comparison provenance conclusion. This closes before/current provenance ambiguity and improves current-source scenario evidence, but does not close exact eligibility, ranking, normalized probability, timing, dynamic-pool, dominance, repetition, or exploit-safety certification. Exact artifacts and scenario hashes are preserved in `ai_probability_current.md`, `subagent_handoffs/distinct_probability_recovery_0826.md`, and `subagent_handoffs/final_flat_fact_probability_0826.md`; the deleted incident-event layer has no option pool and is not a probability blocker.

## Assets

The documented asset packages contain exactly 61 declared final DDS assets: 50 in the root system manifest, seven report images, and four mapmode buttons. The category-asset closure reconciles all four famine/migration category consumers into the authoritative root manifest and records source, processing, provenance, and DDS round-trip evidence. Parent contact-sheet review accepted the category, achievement, report-art, decision, state-modifier, death-icon, and mapmode source, processed, and round-trip comparisons. Eight stale review-sheet labels were regenerated from current split inputs; visual review confirms only `famine_*`, `migration_*`, `report_event_famine_*`, and `report_event_migration_*` labels remain. Runtime assets were unchanged. Live runtime consumer validation remains user-owned.

Famine evacuation has its own `gfx/interface/decisions/famine/famine_decision_evacuation.dds`; it is not an alias to migration evacuation. Its processed PNG and decoded DDS round trip have identical SHA-256 `F87E6E44CF0221FE00221FFE8833F5F522D59F1A0A84162233264A384E74BC80`; the DDS hash is `A2BD983148FD76435D583C69879A18DB80DCF11384C221E49AFA6B81A039A2E1`.

## Achievements

All eight achievement registry entries, predicates, localisation keys, normal/grey/not-eligible DDS files, and sprite consumers exist: Break the Blockade, No One Left at the Gate, Roads Home, Bread Across the Front, Hungry Not Contagious, A Place at the Table, The Grain Stayed Home, and The Country Did Not Empty.

Achievement evidence is cohort/generation/state/country specific. Roads Home credits the exact earning host and exact return-origin owner when either matches the stored earning country. A Place at the Table disqualifies only the integrated cohort for forced labor, forced return, or violent pushback, rather than reading unrelated campaign-global policy flags. Annexation and tag-switch disqualifiers are wired. The fresh completion audit found no new achievement P0 defect, while live/save-reload reachability remains user-owned.

## Localisation and GUI evidence

English localisation is split into famine, famine missions, migration, migration missions, humanitarian costs, achievements, and mapmode files. The 34 decision/mission identifiers and both category identifiers have localisation, while report/presentation seams use current famine or migration text rather than incident-event titles or option pools. The 15 `GetHumanitarianCost*` selectors resolve all 81 icon-first cost calls through 30 ready/blocked `humanitarian_cost_*` keys. Deaths strings are `From famine` and `From forced displacement`. The category text exposes exactly three values per mechanic and no combined terminology.

Fresh split-header GUI inspection returned `GUI_INSPECTED` for both windows. Famine inspected 10 elements and produced `hoi4-agent://workspace/mod_chaos_redux_ea3b2d67c2c0/artifact/c8b9fa6a4edb903eaa2ab425c147520e0abd19ac5c9e85b40f2784275aa9a90b/62593d4feb0b1c6dcd018c915b8988776278fb401774750eabe16f10ad06fa24/gui-inspect.8b99482f700641c9.json`; migration inspected 13 and produced `hoi4-agent://workspace/mod_chaos_redux_ea3b2d67c2c0/artifact/8679266521fe619eeb1672971bbdc628aa873983d28eaf85a9a0993aa9367d51/92e12162d3ddc50a5790b2fe64162459bc4034f714a1c58f3116b4da9472aea5/gui-inspect.87f364f7340e68b2.json`.

Each header is attached only to its own decision category through `scripted_gui = famine_report_header_scripted_gui` or `scripted_gui = migration_report_header_scripted_gui`; there is no shared category or shared full scripted GUI.

Fresh renders returned `GUI_RENDERED` SVG artifacts for both windows across 1920x1080 and 2560x1440 and normal, long-text, and missing-localisation states. The famine artifact is `hoi4-agent://workspace/mod_chaos_redux_ea3b2d67c2c0/artifact/1722cda889710f0093df97c8db96b4a2b1b8d7066f314acd4bca27c1a8158ed4/ae4c701002a3ced79098a91150570243fa85b9d67c4ca2a91f37cab07838ad4a/famine_report_header_window-full.svg`; the migration artifact is `hoi4-agent://workspace/mod_chaos_redux_ea3b2d67c2c0/artifact/ccb4623ed97b2145c0de5861bbdce1ec9d12aa9ab2c0a2be3ee90ccd1cffb3d8/f29e3c53e17e92a3b0b0c6201180b1d248189fac79c94401080ec9ba8eb3b781/migration_report_header_window-full.svg`. The global GUI graph exceeded its 2,000-diagnostic ceiling, dominated by unrelated repository-wide symbol collisions and scripted-context diagnostics, so these are bounded source-graph/render artifacts rather than a clean validation or live-engine claim.

## Event 149, workbook, and documentation

Event 149 `Immigrations` has no event source, no replacement event ID, and no entry in the automatic major, fire-once, or repeatable arrays. Its editable workbook row and regenerated events CSV mark it unavailable and absorbed into the separate famine and migration mechanics through explicit adapters. System pulses are not event-log or event-pacing entries.

`famine_register_initial_incident` and `migration_register_initial_incident` are accounting and presentation seams only. They do not create event objects, event IDs, event-pool entries, event-log rows, random events, or pacing pulses, and they do not mutate population. `events/famine_incidents.txt`, `events/migration_incidents.txt`, and their constants were deliberately deleted; earlier incident-event and incident-option claims are superseded.

Fresh narrow `hoi4.event_inspect` and `hoi4.event_render` calls for `famine_incident.1`, `migration_incident.1`, and `chaosx.nr149.1` returned partial artifacts at the current event graph revision. All three selector-specific render manifests contain `branches=[]`, confirming no renderable chain for the deliberately absent IDs. The workspace graph still has unrelated global diagnostics, so this is qualified negative-selector evidence rather than a clean global validation. Exact artifacts are recorded in `event_free_validation.md`.

The editable source `docs/spreadsheets/chaos_redux_events_catalog.xlsx` was updated and `.tools/export_event_catalog_csv.py` regenerated the event, cluster, and scenario CSV exports. Event 149 now reads: `Retired and absorbed into the separate famine and migration mechanics through explicit adapters. Unavailable as a random event.` It has no incident event, replacement ID, event-pool entry, or pacing row. The current export contains 177 event rows at SHA-256 `1985a8fed50110098262e4268ccd99fe9c4e676e87666ba56944790446e7fbcb`, 14 cluster rows at `647c9206de61a70d7a0d7adf0740dc97c81c8e63d01fefac6549b430b666425b`, and 12 scenario rows at `c6231be89377eb4e5fdf35966b8493d8400b22fc8e082ac97e6ef9639e653e44`; workbook SHA-256 is `fdd74f4f2b7ee56d8f104d4587d77f7b1c5102a5e9be529469f047388fc1c36e`. The workbook contains zero formula cells and zero cached error values. No combined system event, cluster, scenario, or mapmode row was invented.

Permanent docs, dynamic helper contracts, asset manifests/prompts/round trips, source-of-truth map, mapmode validation, probability evidence, improvement addendum, 96 handoffs, and the one-row-per-handoff disposition ledger are present. All 96 current handoff filenames have exactly one recorded accepted, patched, superseded, blocker, or audit disposition. The binding specification headings and active guidance now describe separate famine and migration mechanics, separate categories, separate mapmodes, three values per mechanic, explicit causal adapters, and the neutral transfer primitive; stale singular shared-system/category guidance was removed. A supplementary documentation-curator namespace route failed to return a handoff after two timeboxed isolated attempts, so the completed parent exact-file census and the accepted localisation-auditor handoff are recorded rather than represented as specialist evidence. The final completion route authored `post_separation_completion_delta_0826.md`; `distinct_probability_recovery_0826.md` supersedes only its comparison-schema conclusion, and `final_flat_fact_probability_0826.md` records the stronger final-source scenario coverage without claiming numeric closure.

## Simplifications, omissions, and blockers

1. Exact external-owner facts are absent for the generic occupation, strategic-bombing, war/peace, cluster/scenario, relief-obstruction, and several event integrations listed above. Their APIs fail closed; this is a missing accepted integration, not an approved simplification. Repository history contains no recoverable Event 118, 120, or 131 root, and creating or aliasing those owners requires accepted event design.
2. The fresh isolated probability-auditor route completed and accepted the final-source flat-fact matrices, reducing unresolved rows from 23 to 15 for famine and from 77 to 60 for migration. A final adapter-capability inspection returned `PROBABILITY_ADAPTERS_LISTED` with eleven registered adapters but no newly exposed typed-scope binding capability. Named decision and dynamic-pool scenarios remain partial because the installed analyzer cannot bind special `FROM`, fully resolve scoped and compound triggers, or discover the three dynamic candidate pools. No exact eligibility, normalized probability, timing, ranking, dominance, repetition, or exploit-safety certification is available.
3. The mandatory distinct before/current comparison is present and provenance-reviewed. Its 46 famine and 134 migration unresolved items remain the comparison-run limits; the later final-source matrices improve current-state coverage to 15 and 60 unresolved rows but are evaluations, not replacement before/current comparisons. All three dynamic pools remain incomplete, so no exact eligibility, normalized probability, timing, ranking, dominance, repetition, or exploit-safety certification is available.
4. A final callable-route inventory confirms that Map MCP exposes only inspect, render, and rewrite operations for map data, while GUI MCP exposes inspect, render, and rewrite operations for interface layouts. There is no scripted-mapmode execution or active-mapmode injection endpoint. GUI MCP resolves and renders the mapmode button-window layout but cannot evaluate state-scoped branches; no live click-region, selected-button, or dynamic-color proof exists.
5. HOI4 live consumer testing remains user-owned under repository policy and is not claimed here.
6. Camp custody is wired from the Camp owner's exact staged cohort, current-host, actor, site/action, generation, revision, and dated request receipt. Deportation, deliberate starvation, concealment, forced return, and violent pushback condemnation are also wired from their exact successful owner branches. Verified relief obstruction remains fail-closed because no current owner exposes an exact affected-people receipt; the wrapper exists but has no caller, and no disaster refusal or relief-policy intensity is misrepresented as people.
7. Category asset production, provenance, DDS validation, and parent contact-sheet review are closed in `category_asset_closure.md`; live runtime consumer validation remains user-owned and is not claimed.

No fixed historical casualty total, duplicated population, fake route, placeholder final art, combined category, combined mapmode, whole-world recurring scan, or compatibility alias was accepted. The goal remains incomplete until every blocker above is closed.
