# Event 027 MCP and acceptance evidence

## Current refresh (2026-09-04)

This section is the current evidence authority and supersedes the older dated records below.

The native mastery implementation is not blocked by a missing command: the source uses the documented country effect `add_mastery`, explicit folder/subdoctrine/track/index arguments, a one-point increment, and exact native level readback after every transaction.

The current focused `hoi4.event_inspect` lint for `events/027_doctrine_research.txt` returned `EVENT_INSPECTED_PARTIAL` with status `ok` at revision `57d351df319dd5e7cdd1aa154584e6ac6e4bde5f1e6d0689ede14544ce2eb4cc`, zero direct Event 027 blocking diagnostics, and partial validation caused by deferred large-workspace helper and lifecycle projections.

Event 027 is now in `global.repeatable_events`, is in the default reworked-event allowlist, initializes its adapter registry during the existing bounded startup scope, and has a dedicated global-pool N/A reason when no live country has an eligible action.

The current source-linked options render returned `EVENT_RENDERED_PARTIAL` at revision `00d629f508b7c159079ad7d6f7eb13641489097d593b4c360ba2f464b7eb960f`, with layout hash `805b63c9307c8b209975af2767e8e7fe5111f7f746138061fd4f769fc276ff92`, 180 selected nodes, 42,308 omitted nodes, and zero direct Event 027 blocking diagnostics. Its PNG artifact is `hoi4-agent://workspace/mod_chaos_redux_ea3b2d67c2c0/artifact/7bc62a327a0a03d022f33110d945983b5b6a4ecc14b314bb2c21bf285a2c9774/df26072c7cd36514b1190ef778126d342c9e0436bb490fc7af336fe2a73d7c01/event-options-00d629f508b7.png`. It remains a source-linked partial render rather than a one-to-one in-game popup render and does not prove final clipping, pagination, Event Log, Event Details, or achievement presentation.

The attempted comparison between the earlier focused revision `fca01297b05d2df4d6d638282d986b847407830db4d98f3ff44b449fb0723dee` and the current revision returned `EVENT_REVISION_NOT_CACHED`; no accepted Event 027 before/after comparison is claimed.

The current doctrine route resolves all four supported domain folders and Chaos Warfare as a Land Grand Doctrine, while the aggregate index reports 1,408 blocking diagnostics and focused renders report `sourceAccurate: false`. The same-revision technology comparison returned zero changes and is only a route control because Event 027 does not edit doctrine definitions.

The current named probability evaluation covers DR-A01 through DR-G03, 37 scenarios, and 5,291 candidate rows, but remains partial with 144 unresolved inputs, one analyzer diagnostic, and withheld normalization. The same-source comparison control reports zero changes and uncertainty-visible output; it is not a genuine before/after balance audit.

The visual-asset audit in `docs/assets/027_doctrine_research/audit.md` found no missing final asset or wiring defect and verified pixel-equal DDS round trips for the report and all nine achievement files. Runtime consumer captures remain pending.

Source playability is therefore established, but acceptance is not complete while local engine traces, full event comparison, source-accurate doctrine evidence, complete probability comparison, and production consumer captures remain unresolved.

> **Superseded status notice (2026-09-01):** This 2026-08-31 MCP evidence record is preserved as historical evidence and is superseded as the current MCP status authority by documentation_state.md. Its artifact URIs and transport observations are historical, and the fresh 2026-09-01 references are recorded in the current ledger.

## Fresh 2026-09-01 parent refresh

The native mastery route is not blocked by an absent command. Installed vanilla documentation exposes the country effect `add_mastery`, the native `set_sub_doctrine` empty-track effect, and the `has_mastery_level` readback trigger; the separate console command named `mastery` is not used by the mod. Event 027 has explicit native `add_mastery` branches for all 107 registered subdoctrine/track tuples, applies one raw point at a time, and accepts a transaction only after the native readback equals the recorded pre-level plus one.

The direct installed `hoi4-agent-tools` v3.0.1 JSON-RPC runner returned a current focused `hoi4.event_inspect` lint result for `chaosx.nr27.1` with status `ok`, no direct Event 027 blocker, and artifact `hoi4-agent://workspace/mod_chaos_redux_ea3b2d67c2c0/artifact/369dd856871f7d8df97b96b5c8e99b7fe98ffd2f59713eb7d37b06e83632c7da/cf41115f5e4ea879ec3c1aa81bb80c699cb465cb016158e1df6353db0ed0077b/event-lint-2725045f62d1.json`. The file-selector overview render, reachability, scope, targets, terminals, and unresolved artifacts are listed in the parent refresh handoff.

The current technology route resolves Land/Chaos Warfare, Naval, Air, and Special Forces but remains aggregate-partial because the index reports 1,421 blocking technology diagnostics and doctrine renders are marked `sourceAccurate: false`. This does not indicate a missing `add_mastery` implementation.

The current probability inspect returned `PROBABILITY_SOURCE_INSPECTED` with 143 source candidates and no parser diagnostics at artifact `hoi4-agent://workspace/mod_chaos_redux_ea3b2d67c2c0/artifact/7186bf0fa1943427c965b57ede69a7e759568ca79af753c6a03f60187a573929/f2d2d205a68f210e50a5cfbb550c2eead3f2146632edb674a67578ad1bd96820/probability-inspect-cbf30e6c9ce4.json`. A current typed DR-A01 through DR-A06 domain evaluation returned `PROBABILITY_ANALYZED`, 30 candidate-scenario rows, zero unresolved inputs, and artifact `hoi4-agent://workspace/mod_chaos_redux_ea3b2d67c2c0/artifact/96bb7aaa2c95ff29097ac3a88cff79b84dffbc594602cf07dce3e156a9b752ac/9a97f53ba823a99337b92bb9ba80e091296b32c3b3d010d31a58bbbe54e2c6a5/probability-14eaa2038aab24ed85b57be6.json`. This is score-fixture evidence, not native country-state or live-campaign evidence.

The AI scorer now withholds Navy and Air owner-readiness weight unless native observable coastal/production/fleet/doctrine or air-force/production/doctrine evidence is present; the human validity pool remains unchanged. The app-facing bridge is still intermittent and returned `Transport closed` on the latest narrow call, but the direct local MCP runner is responding normally.

Event 027 is source-playable through manual or cluster dispatch, including the human chained pages and silent AI path. It remains acceptance-blocked by live mastery/empty-track and persistence traces, complete native probability coverage for DR-A01 through DR-G03, a genuine before/after comparison, full event/technology comparison evidence, and final consumer presentation validation. See `parent_mcp_refresh_2026-09-01.md` for the parent disposition.

## Status

This is the current 2026-08-31 evidence handoff for Event 027.

The implementation is source-playable, but it is not acceptance-complete because the mandatory local-engine mastery, persistence, and presentation traces and several complete MCP comparisons remain unavailable.

Workspace: `mod_chaos_redux_ea3b2d67c2c0`.

The agent does not launch Hearts of Iron IV.

The direct probability route now also resolves a bounded typed domain fixture. The six-scenario `DR-A01` through `DR-A06` probe used the current Event 027 score-variable outputs and returned `PROBABILITY_ANALYZED` with 30 candidates and zero unresolved rows. Artifact: `hoi4-agent://workspace/mod_chaos_redux_ea3b2d67c2c0/artifact/a1e330c26fc053574857c0658c8b77c790bc27cc6de37d236acc8384d9c5cb38/9ab672b4462ba8869a235d0ea1a7e3e83cdaeecf2cea37bcacb8c4f50b5dfda6/probability-92b7288ac52f2cc38acdb23b.json`. This proves the direct analyzer can consume the score outputs; it is not live-game evidence and does not close the remaining named groups, baseline/final comparison, or sequence audit.

The installed local `hoi4-agent-tools` v3.0.1 transport is responding to direct bounded JSON-RPC calls after restoring its missing `@modelcontextprotocol/sdk` runtime files. The app-facing MCP bridge remains intermittent in this session: earlier inspect/render calls produced partial artifacts, while the latest narrow bridge call returned `Transport closed`, so the two routes are recorded separately. A refresh for `chaosx.nr27.1` returned `EVENT_INSPECTED_PARTIAL`, revision `2725045f62d14f3536e32f1662ce2fae9f2fae9933ff462de6a8c867d1401570`, graph hash `e6c16ff300aa88dfed3e6f55481fdb8ad1e5bb697e6cf3ca888bd82178d7d62d`, and no direct Event 027 blocker. A current overview render returned `EVENT_RENDERED_PARTIAL` with layout hash `1729fe993aee0578ed8fd2b03d51b4155b6fda6c3d8b83e712401a363f89cc5c`, and a current options render returned `EVENT_RENDERED_PARTIAL` with layout hash `cb2c55195ec96915e60d93cc54311a2211c0a5187c2b6d4fb452fbb69f8d4318`. These are current-source partial artifacts; they do not certify the full workspace or replace the required live presentation trace.

The current source audit also closed a cross-domain selection defect that was not visible in the last successful artifact. The native `has_subdoctrine_in_track` trigger reports any instance of a track, while Army and Chaos Warfare share the `land` folder and the four track identities. Event 027 now uses explicit folder-owned emptiness adapters for every Army, Navy, Air, and Chaos Warfare track in both the scripted pool and the visible subdoctrine pages. Special Forces retains its native dual-track identity gate because its subdoctrine tokens are reusable between the two Special Forces tracks. The current source remains structurally balanced with 107 explicit mastery branches and 107 explicit empty-track `set_sub_doctrine` branches, and the event pages expose the same ordinary-domain guards as the AI and transaction paths.

## Event Chain Viewer

A persistent direct JSON-RPC session also completed a full current-workspace scan before attempting the comparison. It returned `EVENT_INSPECTED`, revision `509a362e58e478e10e1bf4665d9d168e687f092fa17fdaf7ea16521de6b3616d`, graph hash `41d96511df9f6b1aa718198257fba6ec14bb21c5a7d982adccea97873d1b8feb`, 9,722 events, 15,150 options, 18,835 helpers, 122,668 edges, and 3,942 blocking workspace diagnostics. The scan artifact is `hoi4-agent://workspace/mod_chaos_redux_ea3b2d67c2c0/artifact/0bee17887583063990dd0ac1c35fa69f2b0689ab495079924adb73fb5cdcb54e/dcf02b4982afde34a6a0cd2cabf548dd263a9aa9db29560b82cd9e835a296ac8/event-scan-509a362e58e4.json`.

The current focused lint for `chaosx.nr27.1` returned `status: ok`, `EVENT_INSPECTED_PARTIAL`, no direct Event 027 issues, revision `2725045f62d14f3536e32f1662ce2fae9f2fae9933ff462de6a8c867d1401570`, and graph hash `e6c16ff300aa88dfed3e6f55481fdb8ad1e5bb697e6cf3ca888bd82178d7d62d`.

The current lint artifact is `hoi4-agent://workspace/mod_chaos_redux_ea3b2d67c2c0/artifact/8347fde24000c0185219c2fcf8d72247ebaf8b0c9f50aeb59d2cf36469bfb9dd/d67a88c3255c39d8fcd3b8b052126e225e862abce642d856454a7d8300895c1b/event-lint-2725045f62d1.json`.

The selected report contains no direct issues or diagnostics. Its workspace-wide validation is partial because helper and lifecycle projections are deferred by the bounded large-workspace analysis.

Validation remains partial because the large-workspace analyzer deferred workspace-wide helper and lifecycle projection and bounded unresolved output to 2,000 rows.

The current state render returned `status: ok`, `EVENT_RENDERED_PARTIAL`, the current revision and graph hash, zero selected nodes after the bounded state projection, and no direct Event 027 blocker.

The current options render also returned `status: ok`, `EVENT_RENDERED_PARTIAL`, at the same revision and graph hash, with no direct Event 027 blocker. Its manifest is `hoi4-agent://workspace/mod_chaos_redux_ea3b2d67c2c0/artifact/18635a989f04015f3c404b47f76148e01bfcc66a1ce268d3f6e2aa4acc8fef26/0f237745df4eeaa6f915960d5620fc3fa334e318f61e9e57d7efafcb9a72a317/event-options-2725045f62d1-manifest.json`, JSON is `hoi4-agent://workspace/mod_chaos_redux_ea3b2d67c2c0/artifact/f9af9c774aad59a2bb0140ec8406747be3cb5d6ca4b0021b009afe7ccc88139f/2db4b834942036ad183d66aff0e006a6dc21abf8d04d126afb3f2a2ef145a76b/event-options-2725045f62d1.json`, SVG is `hoi4-agent://workspace/mod_chaos_redux_ea3b2d67c2c0/artifact/be5342d5a7628db7ea870e32562d750d6db39d8e92a4cf56afdc23c412d631b2/a4ecdde71d951f7101d5264a3f408c65f229be49d54c612ab948206c1ad3fd46/event-options-2725045f62d1.svg`, and PNG is `hoi4-agent://workspace/mod_chaos_redux_ea3b2d67c2c0/artifact/209d5abc10ce8f722941d8e2a1e82af9903e7ea885f34a319a2fd258d1159819/ae0af36f84342e49f8bb4bca92b3135a77d00e42cdeb7e86ed75de0b0a98853f/event-options-2725045f62d1.png`.

The current state-render artifacts are:

- Manifest: `hoi4-agent://workspace/mod_chaos_redux_ea3b2d67c2c0/artifact/97012fae2aea1697514cf4a548f6627417aab12c81ff26a7958a227666cc6159/1511e33619463636b23478eb744ebe2dc1283d8febbf699eb2c29f1c90d47312/event-state-2725045f62d1-manifest.json`.
- JSON: `hoi4-agent://workspace/mod_chaos_redux_ea3b2d67c2c0/artifact/d4e5e34f7a87cfbc38e3cc57ea103d476756a69b04f1ee32b61e197a51d41a07/515ca0c5e414d59dc03dc58093a1c3086e3157c7a68174078e76c0acea7195c5/event-state-2725045f62d1.json`.
- SVG: `hoi4-agent://workspace/mod_chaos_redux_ea3b2d67c2c0/artifact/e9f359c2e7bd94ee824788f2c4e2615851845e736bc8319b86965cf70536d919/88395fd93ea99dee5b07e4ea89e548dd5268d9b89241815a7db8c8a2b6845570/event-state-2725045f62d1.svg`.
- PNG: `hoi4-agent://workspace/mod_chaos_redux_ea3b2d67c2c0/artifact/5d36416254bba8e56c11b5509da17c2811d5a72e3c8ea625d291716ecc21f7ea/9558a0570152a7d1304fd4ebdb2e5d5eaae8b4abf27dfc5209e2c701d71eb52f/event-state-2725045f62d1.png`.

`hoi4.event_compare` still cannot produce an accepted comparison.

Explicit comparison against the focused revision returns `EVENT_REVISION_NOT_CACHED` because the Event Viewer comparison cache stores only a full-workspace graph. The persistent session then cached the full graph above, but its proposed pre-rework overlay exhausted the local Node process near 3.8 GB before returning a comparison result; the process was stopped cleanly. No before/after Event 027 comparison is claimed.

The earlier bounded scan artifact is `hoi4-agent://workspace/mod_chaos_redux_ea3b2d67c2c0/artifact/91cd9c352e176d5f3479285970da15965cd82c2f14a36749da01bf3d235087d8/ee76d42c954b0b7399d8d95cf86c66e033aba061f62a46044ddf28350122b5b1/event-scan-2725045f62d1.json`; it is a scan report, not a comparison-compatible Event Viewer graph resource. The new full scan is also a scan report and is not claimed as a comparison artifact.

## Doctrine and Technology Viewer

The current local MCP technology/doctrine route is responding for focused folder queries. `hoi4.tech_inspect` returns the supported Land, Naval, Air, and Special Forces folders and resolves Chaos Warfare as the `chaos_warfare` Grand Doctrine in the Land folder. The current index is revision `eee56cc199883b3c75a9370960df9ede77183d40c7c76de21771c93ca9e1bf7b`, graph hash `daf10545a9fe27decfe4a92ae5091c131f41179a658e9d40957a23483fc27764`, with 679 technologies, 18 folders, 475 placements, 457 edges, 857 unlocks, 520,298 references, and three unresolved index entries. Aggregate validation remains false because the large workspace reports 1,421 blocking technology diagnostics. The current technology lint artifact is `hoi4-agent://workspace/mod_chaos_redux_ea3b2d67c2c0/artifact/f0511954effab216123268d4dd73b69b95106c8a202da37c18aac2accf104ef6/65155c62e0553c849034eef94ccabdd184e61c7f240d60188e50c0ffdc81f4bb5/technology-lint-eee56cc19988.json`.

Focused doctrine renders are available for Land/Chaos Warfare, Navy, Air, and Special Forces, with selected node counts of 6, 4, 4, and 3 respectively, all at the current technology revision. Every render reports `sourceAccurate: false` and the aggregate validation check reports 1,421 blocking technology diagnostics, so these are current source-resolution artifacts, not final one-to-one engine presentation sign-off.

The current doctrine JSON hashes are Land/Chaos `82fc32dfe7e04be2a395e02cae9e4e54a778164ba96be357fc7f3881f7373bef`, Navy `615368f40dcc7a8727c2bf76971ed0aff9d81fb442796a9e990bd572f2c301c4`, Air `b2fe1b2daecf84c8843ee1ea46d73a1ee82c600d4c4a546fd061c29211ed990d`, and Special Forces `86ab479ab000358af0faca9988a0766bd6b6dad39c60c8ee12f66e3305b8efb1` at technology revision `eee56cc199883b3c75a9370960df9ede77183d40c7c76de21771c93ca9e1bf7b`. Current PNG artifacts are recorded for Land/Chaos `hoi4-agent://workspace/mod_chaos_redux_ea3b2d67c2c0/artifact/a7945b2bc3acf4ccc0cace4f91640955341bd003fb622adb74d981c7ca89100c/2cb92cf595569c7028641269386d77ffaca8422d3a4f249719037dded2fc033e/technology-doctrine-eee56cc19988.png`, Navy `hoi4-agent://workspace/mod_chaos_redux_ea3b2d67c2c0/artifact/3f6f0b8aba0b685076bced406ce562af286ec6a7a3e21ed2828d8f459d6feaf9/6360d74734a29f0b22f5b68ce88660d87904141eaa01a9725ba518612181ba36/technology-doctrine-eee56cc19988.png`, Air `hoi4-agent://workspace/mod_chaos_redux_ea3b2d67c2c0/artifact/0923e7ebc6eff7af4af87d24726105b47f526d9baae409c82d8650e0dfa078cb/89e15a2cde491215ae5d3c107411f862d524563e628b280ccee6f6d7e6785e3a/technology-doctrine-eee56cc19988.png`, and Special Forces `hoi4-agent://workspace/mod_chaos_redux_ea3b2d67c2c0/artifact/cd6a5c0a420efdbcd9a8bea861170c02eef00321c45f79e9976ea4600fd6974a/9f4e0248d85a4d9e10eb73a5b1ebcb400878a75ce3ef32ee3ff80b04bdedaf1c/technology-doctrine-eee56cc19988.png`.

The current same-revision `hoi4.tech_compare` control returned zero added, removed, renamed, moved, or regressed nodes. It verifies the comparison route, but it is not a before/after Event 027 change comparison because Event 027 does not modify the doctrine definitions.

This remaining evidence limitation is an MCP analysis-boundary issue, not evidence that the Event 027 mastery effect is unavailable.

The source and installed vanilla documentation independently confirm the native country effects and triggers used by Event 027: `add_mastery`, `set_sub_doctrine`, `has_mastery_level`, `has_completed_subdoctrine`, and `has_subdoctrine_in_track`.

The implementation calls `add_mastery` with an explicit folder, subdoctrine, track, index, and one-point increment, then reads the native level back after every point. The loop stops only when the observed native level equals the pre-transaction level plus one, preserving any banked mastery rather than dumping a fixed multi-level amount.

## Probability Viewer

The required read-only probability audit was attempted for all 37 named scenarios, DR-A01 through DR-G03, and all four chooser pools, but it is not accepted because the analyzer cannot materialize the typed country and doctrine fixtures.

The source pools are complete as five domains, 13 Grand Doctrines, 18 track rows, and 107 track-qualified subdoctrine rows.

The installed analyzer cannot materialize the typed country and doctrine state needed by those triggers.

The incomplete comparisons return unresolved candidates and `PROBABILITY_CANDIDATE_POOL_INCOMPLETE`; unresolved or zero-resolved rows are not interpreted as zero probability or acceptance evidence.

The fresh local source inspection returned `PROBABILITY_SOURCE_DISCOVERED` for the Event 027 AI file at source revision `af57b912ebbcb2104ab513e235374f99db82181003e4ade3bfccb3523ef0b6d5`, with 143 available source candidates, zero resolved country candidates, and no parser diagnostic. Its source hash is `db288f9569977cf12980a03908f6138a328ee3963f8df522a8ebecc6565e50a4`, and the JSON artifact is `hoi4-agent://workspace/mod_chaos_redux_ea3b2d67c2c0/artifact/3b6dd10e6edc5af84537b5dd7282990328b60d49c72892656713f659b069369e/25bf1be61d54b7e0f8e83cd8c696a35bec9c55facd3aab06dc37ddd8f849ff9a/probability-inspect-db288f956997.json`.

An earlier focused Grand Doctrine evaluation returned `PROBABILITY_ANALYZED_PARTIAL` with five candidate rows and five unresolved candidates. It remains historical partial evidence only; the current source inspection above is the authoritative refresh, and no named scenario is treated as accepted probability evidence.

The authoritative handoff is `docs/plans/027_doctrine_research_plans/subagent_handoffs/probability_current_2026-08-31.md`.

The implementation retains the audited scoring dimensions for forces, production, war, geography, theater, AI strategy, completion value, continuity, and owner-system readiness and recalculates after every successful choice.

The current source-level strategy implementation is `doctrine_research_ai_has_*_strategy_signal` in `common/scripted_triggers/027_doctrine_research_triggers.txt`. It derives the strategy contribution from native current doctrine, AI role-template, fleet, air-wing, naval-production, and Chaos Warfare readiness signals, and fails closed when no corresponding signal exists. This replaces the earlier unproduced country-flag-only strategy gates; the selected Grand Doctrine and track scorers also include the selected-domain strategy contribution. Vanilla documentation exposes no generic targetless trigger for introspecting arbitrary internal AI strategy-plan entries, so no undocumented getter or disconnected second doctrine table is used.

## Source-level completion evidence

The Event 027 package contains 31 unique event IDs and no duplicate top-level Event 027 scripted definition.

The human chain includes opening, domain, Grand Doctrine, track, subdoctrine, confirmation, result, continuation, summary, no-option, ambiguity, native banked-completion recovery, and deterministic pagination pages.

All 107 track-qualified mastery choices use dynamic status text.

The option and confirmation text reports the selected track, subdoctrine, current level, native maximum, next level, completion state, and milestone state.

The global firing path contains one bounded `every_country` fanout and the Event 027 on-actions contain no recurring whole-world pass.

The participant gate includes countries with an eligible Grand Doctrine adoption or at least one active or empty-track mastery action.

Country-owned append-only batch and receipt ledgers keep batch stage and size fixed at creation.

Later firings append independent rows, and annexation discards unused rows without transfer.

Confirmation revalidates the active batch and selected action, creates or finds a one-time receipt keyed by batch and choice, restores the receipt's recorded target when recovering, rereads an effect-applied mastery post-level before finalization, applies the native effect only for a new receipt, verifies the observed postcondition, and consumes the choice only after success. A durable pending marker keeps an interrupted confirmation on the recovery path even if the original target is no longer a valid fresh selection; terminal receipt states clear that marker.

AI resolution uses the same candidate predicates as the human flow.

An exhausted AI batch closes normally when no valid action remains; only a still-valid batch that exceeds the bounded guard is quarantined.

Human/AI transitions reached through the documented lifecycle callbacks prompt or resume once through `doctrine_research_active_batch_prompted_human`; government, exile, reinstatement, subject-annexation, and state-control callbacks now reconcile or discard only the exact affected country ledger.

Pure player tag-switch behavior is reconciled by the existing shared global-host selector: the former host is reconciled before its marker is cleared and the new human country is reconciled after the marker is set. Runtime proof of the switch sequence remains outstanding.

Special Forces fails closed when both native tracks are occupied and the selected token-to-track identity cannot be proven.

Vanilla defines each of its eight Special Forces subdoctrines as eligible for both track identities, while the available triggers expose only track occupancy and subdoctrine-wide mastery readback.

Empty-track selection remains exact through `set_sub_doctrine` with a folder index, and active Special Forces mastery remains available when exactly one track is occupied. Both occupied tracks contribute no Special Forces action, while valid Army, Navy, Air, and Chaos Warfare actions remain available.

First Lesson now checks the matching immutable batch-start empty-domain array before it accepts an adoption followed by a later mastery receipt in the same domain and batch.

The source contains no active-doctrine replacement, generic military-experience fallback, mastery-point dump, technology helper substitution, or ordinal first-valid AI fallback.

## Assets, achievements, localization, and catalog

The accepted report DDS and all three active, grey, and not-eligible achievement icon triplets exist and are wired.

Doctrine pages reuse verified doctrine-owned icons, including the Chaos Warfare Grand Doctrine and all four Chaos Warfare subdoctrines.

The Event 027 English localization file is UTF-8 with BOM, has no duplicate keys, and has no missing direct Event 027 reference in the current source audit. Every active opening, selection, confirmation, successful-action, continuation, summary, and no-option page reports the remaining-choice count where the active batch still exposes one. Adoption text uses player-facing subdoctrine language, and confirmation prose distinguishes adoption from mastery.

The focused postfix localisation audit found 338 unique Event 027 English keys, 119 matched scripted-localisation names and definitions, 107 matched option-status calls and definitions, three remaining-choice status calls backed by one definition, and no unresolved output key. Its source findings were closed after the audit: successful-action, continuation, and summary pages report the remaining count, adoption strings use player-facing subdoctrine language, and native banked-completion recovery has a localized report. The current localisation SHA-256 is `30c894d867252f715a8d81eed89458681e9c5856d7ecd04f1ed568ae85cfb923`.

The authoritative XLSX remains the editable catalog source and honestly marks Event 027 `Needs Testing` until runtime acceptance is supplied.

The export-only CSV files were regenerated from the workbook after the Event 027 catalog update.

The current workbook SHA-256 is `5453B77254A735465ABC8911F084AE90097E440782CAE1186A2D72E9B3E25A48`. The current export contains 166 physical Event rows with SHA-256 `8D8AB229416FB30AA0AE14720256583BF9E416542244248C5455F3B248814A88`, 20 physical Cluster rows with SHA-256 `4A3324F384F1B6180CB15F5B57B1F096405EF9F26F7FBD824EBCB1ABF69E7FF1`, and 15 physical Scenario rows with SHA-256 `05A5E47238CF23E12A3AC6BA09720106460B42A212B553563F1069E70F139EEE`.

## Remaining blockers

- Local-engine proof is still required for low, middle, final, fractional, and banked mastery, empty-track adoption plus the first Event 027 step, Special Forces track identity, Chaos Warfare identity, and native completion caused by banked progress. The native command route itself is implemented and documented; this blocker concerns transaction behavior under those runtime states.
- Save/reload interruption, receipt idempotency, overlapping queued batches, annexation, subject changes, controller changes, government-in-exile participation, and pure tag-switch behavior still need the accepted runtime scenario traces.
- The largest paginated pages, Event Log row, Event Details page, achievement positive/negative cases, and report/achievement assets still need final consumer presentation evidence.
- Full Event comparison remains blocked by the server closing the full-workspace baseline scan before comparison caching; the focused current-source lint and render are available.
- Focused technology/doctrine inspect and render are available for every supported domain, but the aggregate index remains validation-failing and the renders report `sourceAccurate: false`. The same-revision technology comparison is only a zero-delta control, not a before/after Event 027 change comparison.
- All 37 named probability scenarios remain unresolved because the analyzer cannot construct the required typed country/doctrine fixture.
- Event 027 is included in the default reworked-event allowlist. Manual settings dispatch and cluster dispatch remain wired, so the normal repeatable-event path and the controlled validation path are both available while the remaining acceptance evidence is collected.

No completion commit is permitted while these blockers remain.
