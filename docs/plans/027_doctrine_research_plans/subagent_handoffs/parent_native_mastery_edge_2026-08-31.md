# Event 027 native mastery edge handoff — 2026-08-31

> **Superseded status notice (2026-09-01):** This dated native edge handoff is preserved as historical source evidence and is superseded as a current MCP status authority by ../documentation_state.md. Its receipt-readback finding remains useful, while its old transport statement is stale.

## Scope

This handoff records the parent implementation pass for native banked mastery completing an empty track during `chaosx.nr27.1` confirmation.

## Changed files

- `common/script_constants/027_doctrine_research_constants.txt`
- `common/scripted_effects/027_doctrine_research_effects.txt`
- `events/027_doctrine_research.txt`
- `common/on_actions/027_doctrine_research_on_actions.txt`
- `common/on_actions/chaosx_on_actions_chaos_meter.txt`
- `common/scripted_localisation/027_doctrine_research_scripted_localisation.txt`
- `localisation/english/027_doctrine_research_l_english.yml`
- `docs/events/027_doctrine_research/overview.md`
- `docs/plans/027_doctrine_research_plans/mcp_evidence.md`

The dedicated lifecycle hook file also now reconciles government changes, exile and reinstatement, and subject annexation in the exact affected country. These callbacks preserve the country-owned ledger or discard it only for the annexed country; they do not add a recurring world scan.

## Result

An empty-track selection continues to use native `set_sub_doctrine` and the exact one-point native `add_mastery` route when the branch remains incomplete. If native banked mastery completes the branch during assignment, the receipt is stored as `native_adoption` with the observed post-assignment level. The batch choice remains available, no Event 027 mastery step is counted, and no compensation effect is used.

Human control receives a dedicated localized `.13` report and returns to the selected domain's valid track list. AI control clears the selection and recalculates the same valid pool. Reload or controller reconciliation resumes the human report from the durable transaction result.

The shared global-host tag-switch branch reconciles the former host before clearing its marker and the new human country after setting it. The country-owned ledgers are not moved between tags.

## Evidence

- The native route contains 107 exact mastery adapters and 107 exact empty-track `set_sub_doctrine` adapters.
- The event source contains 31 unique Event 027 ids with balanced blocks.
- The last successful MCP Event Viewer artifact predates this edge. A bounded post-edge retry returned `Transport closed`, so no post-edge MCP artifact is claimed.
- Live engine proof remains required for banked completion, save/reload interruption, receipt idempotency, and controller/tag transitions.

## Remaining risk

The source-level transaction is fail-closed, but the required local engine and MCP evidence is still outstanding. This handoff does not promote Event 027 to acceptance-complete and does not authorize a completion commit.

The final source hardening also gates the valid pool, adoption validation, empty-track validation, and one-step transaction validation on the initialized adapter registry flag. This keeps the explicit ordinary adapters available while failing closed before an uninitialized or malformed custom path can participate.

Receipt recovery now treats the batch ID and current choice number as the transaction key, then restores the recorded action and doctrine target before resolving an existing receipt. An effect-applied mastery receipt rereads its native post-level before finalization, and an unavailable readback quarantines the batch. A changed selection after reload, back-navigation, or controller transition therefore cannot create a second native effect for the same choice.
