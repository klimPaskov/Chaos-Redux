# Event 027 parent current status — 2026-08-31

> **Superseded status notice (2026-09-01):** This dated parent status is preserved as historical evidence and is superseded as a current status authority by ../documentation_state.md. Its claim that Event 027 is excluded from the default reworked-event allowlist is contradicted by the current source.

## Result

Event 027 is source-playable and the native mastery grant is implemented. It is not acceptance-complete because the required local-engine transaction traces, full comparison artifacts, and typed probability scenarios are not available.

The mastery path is not a placeholder or a generic experience substitute. Active-track transactions call the native `add_mastery` effect with the explicit folder, subdoctrine, track, index, and one-point increment, reread the native mastery level, and succeed only when the observed level is exactly the recorded pre-level plus one. Empty-track transactions call native `set_sub_doctrine` first and then use the same one-point transaction. Native completion caused by banked mastery is recorded as `native_adoption`, does not consume the choice, and returns the human flow to the track list.

## Current source evidence

The current source audit reports balanced braces in the Event 027 effect, exact-mastery, trigger, AI, and event files. The active path has 107 track-qualified mastery branches and 107 track-qualified empty-track `set_sub_doctrine` branches. The active effect path contains no active-doctrine replacement, generic military-experience fallback, technology helper substitution, or fixed multi-level mastery dump.

The global firing path performs one bounded `every_country` fanout and creates append-only country-owned batches with immutable stage and size. Later firings append separate rows. Lifecycle callbacks reconcile the exact country ledger, and annexation clears unused rows without transfer. No recurring Event 027 whole-world on-action was added.

The ordinary Army, Navy, Air, Special Forces, and Chaos Warfare adapters are explicit and fail closed. Army, Navy, Air, and Chaos Warfare track emptiness uses folder-owned checks because the native track trigger is not folder-qualified. Special Forces retains both native track identities and suppresses ambiguous mastery when both tracks are occupied. Future custom domains remain disabled until a complete adapter contract exists.

## MCP artifacts

The focused current Event Viewer lint returned `EVENT_INSPECTED_PARTIAL`, status `ok`, revision `2725045f62d14f3536e32f1662ce2fae9f2fae9933ff462de6a8c867d1401570`, graph hash `e6c16ff300aa88dfed3e6f55481fdb8ad1e5bb697e6cf3ca888bd82178d7d62d`, and no direct Event 027 issue. Its latest bounded artifact is `hoi4-agent://workspace/mod_chaos_redux_ea3b2d67c2c0/artifact/7f3ec0d4eab49d49775a1dca917d8384ed4e03e6bc71f5c7babf55c99ee63c78/0bc57b01d4d26833e8123406322e5aa61db8f79e95b53254efbce64f5a3bda9d/event-lint-2725045f62d1.json`; the same revision also has a bounded options render recorded in `docs/plans/027_doctrine_research_plans/mcp_evidence.md`.

A persistent direct JSON-RPC session completed a full current-workspace scan at revision `509a362e58e478e10e1bf4665d9d168e687f092fa17fdaf7ea16521de6b3616d` with 9,722 events, 15,150 options, 18,835 helpers, 122,668 edges, and 3,942 workspace blocking diagnostics. The session then attempted an in-memory pre-rework overlay for `events/027_doctrine_research.txt`; the comparison phase reached approximately 3.8 GB and was stopped cleanly without an artifact. The app-facing bridge remains intermittent, so the focused artifacts and full scan are evidence, not a comparison-compatible before/after result.

The focused state render returned `EVENT_RENDERED_PARTIAL` with layout hash `cb2c55195ec96915e60d93cc54311a2211c0a5187c2b6d4fb452fbb69f8d4318`. The selected-node projection was bounded and empty, so it is not a full visual sign-off. The focused render artifacts are recorded in `docs/plans/027_doctrine_research_plans/mcp_evidence.md`.

The live-source technology inspection returned revision `eee56cc199883b3c75a9370960df9ede77183d40c7c76de21771c93ca9e1bf7b`, resolved the Land, Naval, Air, and Special Forces folders, and resolved Chaos Warfare as the `chaos_warfare` Grand Doctrine in the Land folder. Focused doctrine renders exist for all four supported routes. Aggregate technology validation remains false because the workspace index reports 1,421 blocking technology diagnostics, and the renders report `sourceAccurate: false`. The same-revision technology comparison is a zero-delta route control, not a before/after Event 027 comparison.

The probability adapter responded to a fresh source inspection of the Event 027 AI file with 143 available source candidates, zero resolved country candidates, and no parser diagnostic. It proves the source inventory route only and not the full country-scoped Event 027 AI path. All 37 required named scenarios still lack resolvable typed country/doctrine fixtures and therefore remain open; the current JSON artifact is indexed in `docs/plans/027_doctrine_research_plans/mcp_evidence.md`.

## Acceptance blockers

- Local engine traces are still required for low, middle, final, fractional, and banked mastery, empty-track adoption plus the first Event 027 step, Special Forces identity, Chaos Warfare identity, and native completion caused by banked progress.
- Save/reload interruption, receipt idempotency, queued batches, annexation, subject/controller changes, government-in-exile participation, and pure tag switching still need runtime traces.
- Event comparison still lacks a comparison-compatible full-workspace baseline, and focused render artifacts are partial rather than final consumer evidence.
- The complete named-scenario probability baseline/final comparison remains unresolved because the analyzer cannot build the typed country/doctrine fixture.
- Special Forces intentionally fails closed when both tracks are occupied; this is now an explicit current-ruleset disposition in the doctrine registry spec, but local engine evidence is required before the supported unambiguous states can be promoted to full acceptance.
- The report-art provenance and runtime GFX handoff are now promoted into `docs/assets/027_doctrine_research/manifest.md` and `gfx_handoff.md`; final consumer presentation evidence remains open.
- Event 027 is excluded from the default reworked-event allowlist while those acceptance gates are open. Manual settings dispatch and National Breakthroughs cluster dispatch remain available, so the release guard does not disable the playable source path.

No completion commit is authorized while these items remain open.
