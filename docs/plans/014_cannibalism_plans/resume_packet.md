# Event 014 resume packet

Status date: 2026-08-26 approved vanilla-visual reuse amendment and parent runtime-wiring continuation.

This packet is a documentation handoff for the parent agent. It records the current accepted design and the work that remains queued or blocked; it does not claim gameplay, model, or live validation completion. The four Event 014 super-event audio registrations are closed at the source and asset boundary.

## Resume authority

Read `documentation_state.md` first, then `docs/specs/014_cannibalism_specs/README.md`, `docs/events/014_cannibalism/overview.md`, `014_cannibal_irregular_unit_model_plan.md`, `014_cannibal_model_source_direction_2026-08-22.md`, and the final audit handoff in `subagent_handoffs/event014_documentation_final_audit.md`.

The accepted current facts are Minor Fire-Once, no cluster, three origins only, a concealed Hannibal Lecter identity until `cannibalism_reveal_complete`, nine Event 014 gameplay formation families, seven dedicated bespoke model packages, approved vanilla `sprite = cavalry` reuse for Bone Riders, approved vanilla `sprite = infantry` reuse for Network Cadre, vanilla `elephantry` for Scavenged Elephant Column, no Event 014 animation toggle or animation preference, and 27 counter textures/registry consumers.

Unit model production is closed for this scope. The seven bespoke packages are installed, and the two amended consumers intentionally use vanilla cavalry and infantry visuals; any remaining parent live-consumer check is verification evidence only, not an open model-generation or 3D-workflow task.

Model inputs must start from actual Internet-sourced or user-supplied modern artwork, with ImageGen limited to faithful resolution, alpha, background, padding, or edge cleanup. ImageGen must not redesign the source, invent missing components, or create a from-scratch descendant for Meshy. Feast Guard is the current approved source at `docs/assets/014_cannibalism/models_3d/cannibal_feast_guard/refs/source/untouched.png` with faithful input SHA-256 `C67AF852A27E1379590BD84C5175C378D449AE226F895A2D326B45099040D8C9`.

## Already reconciled

- The Event 014 specs, event overview, unit/model source-direction plan, package-status and asset-inventory rows now distinguish frozen design scope from downstream implementation queues and the seven parent-installed runtime model packages.
- Counter documentation now records nine family triplets and 27 DDS/registry consumers as present, while retaining parent-owned live consumer review.
- Audio documentation now records seven installed bespoke model packages with their converted 44.1 kHz WAV files and country-level voice coverage for the gameplay family set. The four super-event tracks have complete source, sound-definition, settings-wrapper, catalog, and dispatch evidence; Bone Riders and Network Cadre require no custom model playback or action synchronization under the approved vanilla-visual reuse decision.
- Feast Guard manifests and handoffs now use the user-supplied source path and C67 hash; the old generated `6ACD...` input is superseded evidence.
- Generated or substantially redesigned model descendants, old adaptation briefs, and old provider tranches are marked rejected or superseded rather than presented as eligible inputs.
- Historical Prison Host, fourth-origin, and Lockhouse material carries superseded notices; current documents describe three origins only.
- Current GUI ledgers no longer describe an animation toggle or player animation preference.

## Open queues and blockers

1. The seven installed bespoke model/entity/action packages still need parent live consumer validation; source-side re-import and runtime file checks do not substitute for a live session. This is a verification gate after closed production, not a request to reopen the 3D workflow.
2. Feast Guard and Feast Cohort retain the documented binary-provenance reconciliation items and must not be described as byte-identical without receipts.
3. The portrait-worker handoff remains `BLOCKED / needs_user_review` for the missing worker receipt, durable archive or native-ImageGen route, reproducible source commands, rights basis, static Hannibal provenance, and live review.
4. Fresh Event 014 MCP inspection and overview rendering are partial because the large workspace deferred full validation; zero blocking diagnostics in that partial result is not full runtime proof.
5. Weighted-logic evidence remains scenario-specific and incomplete under `event014_probability_final_2026-08-25.md`; no aggregate probability completion claim is made.
6. Four super-event image/audio packages remain source-documented, with audio wiring closed in `event014_super_event_audio_wiring_audit_2026-08-25.md`; image registration and live presentation review remain part of the parent boundary.
7. Counter registry and DDS presence is documented, but live consumer review remains open.
8. `Events!N15` and `Scenarios!F10` remain `Needs Testing`; the workbook is source-owned by the spreadsheet worker and was not edited in this documentation pass.
9. Bone Riders and Network Cadre are not open model queues: the former uses vanilla `sprite = cavalry`, the latter uses vanilla `sprite = infantry`, and their removed model workspaces are preserved only through lineage handoffs.
10. The shared `chaos-redux-3d-model-pipeline` skill remains authoritative for the seven bespoke packages; no model workflow was run for the two approved vanilla-reuse consumers.

## Recommended continuation order

1. Keep the source gate and C67 Feast Guard provenance as the parent review baseline.
2. Audit parent-owned entity/action/sound wiring against the current seven-bespoke-model, nine-gameplay-family, vanilla-reuse, and no-toggle contracts; the four super-event audio registrations are already reconciled.
3. Re-run the required narrow MCP and live consumer checks after implementation changes, retaining the partial-workspace limitation in the handoff if it persists.
4. Close the portrait, probability, provenance, live-consumer, and super-event image gates through their owning specialist evidence.
5. Only then update package status or make a completion claim.

## Explicit boundary

The original documentation audit did not change gameplay, localisation, GUI, GFX, audio, image, DDS, or animation runtime files. The 2026-08-25 continuation also corrected the Event 014 catalog helper and workbook status cells through the required workbook exporter; those catalog changes are recorded separately from the model-package handoffs. This documentation reconciliation did not edit the workbook or the removed 3D workspaces.
