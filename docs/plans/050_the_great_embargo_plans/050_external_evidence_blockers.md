# Event 050 external evidence blockers

## Blocked in the planning runtime

- live repository inspection
- offline Paradox wiki inspection
- installed vanilla documentation and precedent inspection
- authoritative XLSX inspection and update
- HOI4 MCP event-chain evidence
- HOI4 MCP probability evidence
- actual project subagent execution
- asset generation and DDS production
- live in-game validation

## Required owner

The implementation agent owns repository, wiki, vanilla, MCP, and source validation.

The asset agents own generated art and icon production after stable runtime names are registered.

The spreadsheet worker owns the authoritative XLSX update after final localisation exists.

The user alone can explicitly invoke the autonomous debug playtest workflow when live testing is desired and the required desktop capability exists.

## Planning effect

The blockers prevent implementation claims. They do not leave an undefined event-design branch. Every blocked evidence item has a named implementation prompt or acceptance scenario in the source package.
