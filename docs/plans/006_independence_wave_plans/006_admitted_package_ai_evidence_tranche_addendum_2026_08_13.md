# Event 006 admitted-package AI evidence tranche addendum

Date: 2026-08-13

Status: EXECUTED / EVIDENCE-BLOCKED

Owner: main Event 006 implementation agent, with read-only probability review through `chaosx_ai_probability_auditor`

## Executive disposition

The next implementable Event 006 tranche should deepen AI evidence and package-aware route selection for the already admitted IW-040 Kuban and IW-044 Tatarstan packages.

It should not admit another package, add a focus node, create a bespoke tree, promote a formable, or work around the blocked ordinary super-event.

The executed pilot preserves the current authority of 32 content-attested selectable packages across 29 compatible reservation groups, 161 unattested selectable rows, and 40 runtime package adapters.

The static twenty-package witness and the automatic `3/4/5/7/10` ladder, including World Collapse at `10`, remain capacity evidence and tuning authority respectively.

The eight adapter-only IDs remain fail-closed, FORM-07, FORM-08, FORM-16, and FORM-48 remain behind their existing readiness contracts, ordinary super-event `23` remains blocked, and ordinary super-event `24` remains unchanged.

## Why this is the largest safe gap

The accepted specification says that a single generic AI table is insufficient and requires package type, government route, former-host threat, patron access, League state, geography, force strength, and chaos to affect behavior.

The shared focus tree and admitted packages already expose those state signals, but the current KUB and TAT probability receipts cannot prove their selection behavior because the installed probability adapter evaluated empty fixtures, reported incomplete pools, and exposed zero available candidates.

KUB and TAT are the correct pilots because both packages are already content-attested, use dormant vanilla carriers, use the full shared focus framework, expose two package ledgers, own ten paid projects and four government settlements, and have current completion handoffs.

No new leader, portrait, flag, state, country identity, or rights decision is required for this tranche.

IW-013 is not a substitute target because its identity and flag rights gates remain unresolved.

Promoting another unattested row would increase breadth without resolving the accepted AI-equivalence gap and would risk stacking another shallow package on top of incomplete quantitative evidence.

## Prior addendum disposition

The generic-focus closure remains authoritative for architecture and rejects broad route expansion or bespoke country trees.

The historical IW-033/IW-041 focus-depth queue is superseded as an implementation blocker because the parent waived their Level 2 country-specific-focus expectation.

That queue remains useful evidence that future differentiation must stay inside the accepted shared tree and must receive a named probability audit.

This addendum does not repeat the IW-033/IW-041 package plan.

It addresses the separate unresolved gap of proving and, only when the baseline supports it, refining package-aware AI selection through existing KUB/TAT surfaces.

## Accepted research basis

The Event 006 package research resolution gives IW-040 a high-confidence Kuban contract based on named local councils, district and municipal administration, veterans, border forces, transport authorities, and specifically sourced customary institutions.

That basis supports a civic-versus-mounted-readiness AI conflict rather than a generic ideology lottery.

The same research resolution gives IW-044 a high-confidence Kazan contract based on a founding congress that negotiates representation, veto, autonomy, revenue, and defense clauses before any wider federation can be considered.

That basis supports Civic Concord and River Security as the AI's organizing signals rather than a generic restoration score.

The eastern-European regional overlay connects Kuban play to frontier mobility, rail and border administration, former-imperial collision checks, and Event 005 origin separation.

The Volga regional overlay connects Tatarstan play to river and railway logistics, multiethnic administration, federal or religious settlement, and strict separation among distinct Volga identities.

These are accepted repository research directions, not permission to invent a new Kuban formable, claim Idel-Ural for TAT, or collapse Tatar, Chuvash, Bashkir, Mari, Udmurt, or other communities into an interchangeable package.

## Current source and MCP baseline

### Shared focus surface

The historical pre-cleanup `hoi4.focus_inspect` receipt resolves `independence_wave_focus_tree` at revision `041d42b297734fcde47f1b1794c33ce33cd5539acb370978ddae0a341b41014b` with 184 focuses, 206 connectors, zero crossings, zero node intersections, 12 long connectors, and 14 blocking diagnostics. The current post-cleanup tree is 184 focuses, 196 connectors, zero crossings, zero node intersections, and two long connectors; the current focus receipt is recorded in `006_post_iw045_focus_authored_diagnostic_closure_handoff_2026_08_14.md`.

The inspect artifact is `hoi4-agent://workspace/mod_chaos_redux_ea3b2d67c2c0/artifact/5330e8ebecfdccd17ff086ef6b0c4986b38f00a9f7d539443314f389807f6f69/2a54b80247613ad2ff79ff8a5dc5fc756dd6f0f9c0fceb47bc2735157374b126/focus-inspect.041d42b297734fcd.json`.

Fresh read-only `hoi4.focus_render` produced the current SVG at `hoi4-agent://workspace/mod_chaos_redux_ea3b2d67c2c0/artifact/c51a07eee7fcbe60e40231211902e20aa68aff6e2313b7484db6ee1705a5bf19/fb37f0693f97d8a192c53cf83d0026008436686e1659b4f526ef95db6375bc24/independence_wave_focus_tree.focus.svg`.

The diagnostics are three unrelated vanilla continuous-focus icon references plus authored transport, treasury, military-archetype, former-host, formable, and distant-package-root geometry warnings.

This surface remains HOLD, so the AI tranche must not add nodes, prerequisites, connectors, or remote package branches.

### Event surface

Fresh read-only `hoi4.event_inspect` state-flow analysis for `chaosx.nr6.1` returned `EVENT_INSPECTED_PARTIAL` at revision `cd96567987e11d9efc569ec6dab0c069c1a241905e9ee9789875835cec1c14e0` with zero blocking diagnostics.

The state-flow artifact is `hoi4-agent://workspace/mod_chaos_redux_ea3b2d67c2c0/artifact/c721fd3a3d5faae5d120d8f73497b1f272e85025059709df1134f0a8fb639b30/7901c42d85d0754490eae9615d6cc2b80f041a6c8d10986daf587c7289211c3a/event-state_flow-cd96567987e1.json`.

The paired overview render selected four nodes and produced `hoi4-agent://workspace/mod_chaos_redux_ea3b2d67c2c0/artifact/2076c1436468a0c8ce3e27ee4508222d92f3e29596bbae5f418212e6246fe249/436d2d6eb9892a83a11867128f117f4f77e3a79b0821b1d8c13a08b0d765d91b/event-overview-cd96567987e1.svg`.

Both event results remain partial because workspace-wide helper projection and lifecycle analysis were deferred.

No new event surface is proposed in this addendum.

### Weighted surfaces

The mandatory fresh `hoi4.probability_inspect` call used `national_focus_ai_will_do` on `common/national_focus/006_independence_wave_focus.txt`.

It returned `PROBABILITY_SOURCE_INSPECTED`, source revision `e27639e1cb1241b3a8ad25ac9316191768991d19b0df72de287fee7fa3cf402c`, source hash `c46802f6db53a1dd2099b4450eab8c99eabb48f841b575a59a146a31c2948bc7`, 184 candidates, 15 required inputs, zero unresolved source items, `poolComplete=false`, and zero available candidates without a world-state fixture.

The inspect artifact is `hoi4-agent://workspace/mod_chaos_redux_ea3b2d67c2c0/artifact/a6278d1595ecf6ddd5234cf5cb7351b348f670b0cae26bc7c1bfe0f4e2b0adff/47175fae71b2e74c7203fc8abe3cebcb089c4673c4f5b5b4e3be0fc03471da8a/probability-inspect-c46802f6db53.json`.

The current KUB mission audit discovered eleven candidates, but all six named scenarios used empty state fixtures, every candidate was never eligible under those fixtures, and the evaluation returned 116 unresolved items.

The current TAT mission audit has the same class of limitation, with an incomplete pool, zero available candidates, and a partial six-scenario evaluation.

Both `ai_strategy_factor` inspections returned `no_weighted_surfaces`, so the installed adapter cannot quantify the additive KUB and TAT strategy blocks.

No exact selection probability, dominance, starvation, rank reversal, timing, or balance conclusion is currently supported.

## Current refresh (2026-08-13)

The final KUB audit is recorded in `subagent_handoffs/006_current_iw040_probability_audit_2026_08_13.md`: mission inspection source revision `8a65689a54fa0b3f29389307df0e38a4966dd5fdeb79c8613e668646940d3bb5`, source hash `de8e919c4eae46c9abbb6fdb38703ccc0e59039dbd43c6eeeb120e3fe911a093`, 11 candidates, zero unresolved source inputs, zero available candidates, and `poolComplete=false`, with the exact inspect/evaluate/current-current comparison artifacts preserved there.

The final TAT audit is recorded in `subagent_handoffs/006_iw044_tatarstan_probability_audit_current_2026_08_13.md`: mission inspection source revision `91863468ab1c4cd7fe77881e9b9b1950c650b64c8ad516750a53ad5cde2e11a6`, source hash `fc2e09b238bd9aaaa328fb2cc8b7c942869d7f4618c4b8d8e63f03de2a48aeb2`, 11 candidates, zero unresolved source inputs, zero available candidates, and `poolComplete=false`, with the exact inspect/evaluate/current-current comparison artifacts preserved there.

Both final audits report empty-fixture evaluations with 116 unresolved rows and 11 never-eligible diagnostics, current-to-current `comparisonChanges=0`, and `ai_strategy_factor` `no_weighted_surfaces`; the KUB/TAT strategy and mission adapters therefore remain insufficient for quantitative balance claims.

## Tranche objective

Produce a complete same-scenario baseline and, only if that baseline exposes an actual ordering defect, a narrowly compared KUB/TAT AI refinement.

The tranche succeeds when every declared scenario either produces an engine-evaluable eligible pool or carries a precise adapter blocker, and no balance patch is accepted without a same-scenario before-and-after comparison.

## Phase A: typed scenario contract before any weight edit

The owner and probability auditor should keep the existing KUB and TAT candidate IDs and create a supported scenario set that resolves their real eligibility gates.

The scenario contract must provide exact package setup, capital control, anchor ownership and control, active-project state, cost affordability, former-host existence and war state, severe-host-threat state, route availability, route-government state, founding-settlement state, crisis resolution, both package ledgers, Network membership, League-route availability, and durable-sovereignty state.

The owner must not add gameplay flags or weaken triggers merely to make the MCP fixture pass.

If the installed adapter cannot accept one or more required fields, record the exact rejected path or schema result and leave the affected scenario unresolved.

The required scenarios are:

| Scenario ID | Package state | Expected eligible class | Required ordering statement |
| --- | --- | --- | --- |
| `KUB_FRAGILE_PEACE` | KUB active, capital and state 234 secure, both ledgers below stable, no war, no active project | depots, compact, and guard projects | the lower-ledger recovery action leads; no route settlement or corridor may bypass founding work |
| `KUB_SEVERE_HOST_WAR` | KUB active, war or severe former-host threat, mounted readiness below stable, capital secure | border guards and emergency settlement when its shared route is open | emergency security outranks peaceful host settlement; unaffordable actions remain zero |
| `KUB_STABLE_ROUTE_LOCK` | stable ledgers, one shared government route open, no package government installed | exactly the matching package government project | the matching government settlement wins; all incompatible settlements remain ineligible rather than merely low weighted |
| `KUB_NETWORK_READY` | founding and crisis resolved, stable ledgers, Network and League route active | durable sovereignty and Black Sea-steppe corridor | durable sovereignty must not be starved indefinitely by the corridor; the corridor remains impossible before its existing gates |
| `TAT_FRAGILE_PEACE` | TAT active, capital and state 249 secure, Civic Concord and River Security below stable, no war | river depots, community compact, and guard projects | the lower-ledger recovery action leads; wider Volga ambition does not enter the pool |
| `TAT_SEVERE_HOST_WAR` | TAT active, war or severe former-host threat, River Security below stable, capital secure | border guards and river emergency settlement when its shared route is open | emergency security outranks peaceful host settlement; unaffordable actions remain zero |
| `TAT_STABLE_ROUTE_LOCK` | stable ledgers, one shared government route open, no package government installed | exactly the matching constitutional, agrarian, socialist, or emergency project | the matching settlement wins and incompatible settlements remain ineligible |
| `TAT_NETWORK_READY` | founding and crisis resolved, stable ledgers, Network and League route active | durable sovereignty and Volga River corridor | durable sovereignty remains reachable and the corridor does not imply a formable or member-consent result |
| `BOTH_RESOURCE_STARVED` | either package active but post-spend reserves below the shared safe floor | only actions whose full cost preserves the accepted reserve floor | prestige, corridor, and major-security actions receive zero willingness rather than consuming the last usable reserve |
| `BOTH_IMPOSSIBLE_AMBITION` | no ready formable family or valid members, with ordinary regional ambition state absent | no formable or ambition action | the AI cannot pursue an unavailable union, and no formable readiness flag is synthesized |

The first accepted artifact for each package must report at least one available candidate in every scenario whose expected eligible class is nonempty.

An empty pool is acceptable only for `BOTH_IMPOSSIBLE_AMBITION` and only when the artifact proves that the empty result follows the declared fail-closed gates.

## Phase B: conditional package-aware AI refinement

Do not change weights merely because Phase A became evaluable.

Compare the resolved baseline against the ordering statements above and patch only proven defects.

If a patch is justified, reuse the centralized Event 006 AI constants and the existing lower-ledger, foundation-settlement, cost, and post-spend reserve-floor trigger patterns already accepted for IW-033/IW-041.

The patch may add or refine `ai_will_do` modifiers in the two package decision files and package-specific modifiers on existing shared government-route focus nodes.

It must not add a new focus, change focus geometry, introduce a new decision, create a package-only GUI, or copy the entire KUB formula into TAT under different names.

KUB focus preferences should derive from Civic Concord, Mounted Readiness, war, severe former-host threat, and the existing route-availability flags.

TAT focus preferences should derive from Civic Concord, River Security, war, severe former-host threat, and the existing route-availability flags.

Civilian route preferences should be suppressed during an unresolved existential military crisis without making a valid civilian route permanently impossible.

Emergency route preference should rise during war or severe former-host threat and should fall in stable peace.

The exact package-government project corresponding to the already selected shared route should dominate the remaining package projects until government installation completes.

Foundation recovery should prefer the weaker ledger while preserving the one-active-project lock and cost gates.

Peaceful former-host settlement should be preferred only when the host path is valid and non-belligerent, while the existing host-loss fallback remains available only through its exact depot and unsettled-host conditions.

Durable sovereignty should become urgent after the crisis, ledgers, route, and founding gates pass.

Network corridors should remain post-founding cooperation projects and must not write formable readiness, member consent, territorial proof, or super-event milestones.

Resource safety must use the existing centralized reserve-floor logic and must not invent package-local magic numbers.

## Same-scenario comparison contract

Every owner-applied weighted patch requires `hoi4.probability_compare` through `chaosx_ai_probability_auditor` with the exact Phase A scenario IDs, candidate pools, and scenario inputs.

The comparison must report eligibility, raw score or weight, normalized probability when proven, ordering, unresolved inputs, and the reason for every changed rank.

Acceptance requires no invalid candidate becoming eligible, no matching route settlement being starved, no emergency route dominating stable peace, no civilian route dominating an existential war, and no resource-unsafe project receiving positive willingness.

If the comparison remains partial because the adapter cannot evaluate the world state, the source patch may be retained as queued work but the AI tranche remains unresolved and must not be reported as balanced.

## Admitted-package parity follow-up

After the KUB/TAT pilot closes, produce a read-only 32-row parity matrix for the currently admitted set.

Each row should name the shared focus assignment, package decision or mission source, additive AI strategy source, actor profile, route-selection signals, reserve-floor gate, former-host response, League behavior, formable behavior, current probability receipt, and unresolved adapter limitation.

The parity matrix is evidence and prioritization only.

It must not alter attestation, Join order, allocator weights, candidate selection, country identity, or reservation groups.

Do not expand the pilot to all 32 packages in one gameplay patch.

Any later owner patch should be a separate bounded package family with its own baseline and same-scenario comparison.

## Formable and super-event boundaries

KUB has no admitted formable in its current package contract, and this tranche must not create one.

TAT's regional and Network corridor language does not establish Idel-Ural, Volga-Ural, or any other formable readiness.

FORM-07 remains behind the Iberian package and rights gates.

FORM-08 remains unavailable because its researched two-state candidate set cannot satisfy its three-state threshold.

FORM-16 remains behind its exact member, consent, route, and integration contract.

FORM-48 remains behind its carrier, member, identity, consent, and state-puzzle gates.

Ordinary super-event `23` remains blocked until the user selects and approves a rights-cleared cue after human listening and composition-jurisdiction review, followed by parent-owned wrappers, catalogue alignment, and firing.

Ordinary super-event `24` receives no new predicate, milestone writer, audio, or firing path from this tranche.

No silent cue, substitute track, focus-only trigger, or package corridor fallback is authorized.

## Files the implementation owner may touch

- `common/national_focus/006_independence_wave_focus.txt`, only for proven package-specific modifiers on existing route nodes and with no geometry change.
- `common/decisions/006_independence_wave_kuban_decisions.txt`.
- `common/decisions/006_independence_wave_tatarstan_decisions.txt`.
- Existing shared or package scripted triggers and script constants only when they centralize the accepted lower-ledger or reserve-floor contract.
- `common/ai_strategy/006_independence_wave_kuban.txt` and `common/ai_strategy/006_independence_wave_tatarstan.txt` only when source review proves a state-profile gap; the current MCP adapter cannot quantify these files.
- Matching Event 006 package documentation and the accepted AI sections of specification parts 4 and 7 after implementation evidence exists.
- A new read-only parity handoff under `docs/plans/006_independence_wave_plans/subagent_handoffs/`.

No gameplay file is changed by this addendum.

## Acceptance criteria

- The current 32/29/161/40 authority and deterministic Join order remain unchanged.
- The automatic ladder remains `3/4/5/7/10`, including World Collapse at `10`.
- KUB and TAT keep their existing carriers, anchors, forces, leaders, assets, ledgers, projects, cleanup, and five shared focus hooks.
- No focus node, connector, prerequisite, coordinate, icon, or localisation key is added by the AI tranche.
- Focus inspect/render remain no worse than the current 184-focus, 196-connector, zero-crossing, zero-intersection geometry, while the existing aggregate diagnostics remain visible until separate authored-layout decisions resolve them.
- Every nonempty named probability scenario resolves at least one eligible candidate or records an exact MCP blocker.
- No weight patch is accepted without the mandatory same-scenario probability comparison.
- Resource-starved scenarios prove zero willingness for actions that would breach the accepted reserve floor.
- Government settlement scenarios prove that only the already selected shared route's matching package settlement remains eligible.
- Impossible ambition scenarios do not create or infer formable readiness.
- FORM-07, FORM-08, FORM-16, FORM-48, super-event `23`, and super-event `24` retain the boundaries stated above.
- The final handoff distinguishes exact source evidence, partial MCP evidence, inferred design intent, and unresolved adapter behavior.

## What should not be added

Do not add KUB or TAT bespoke trees, country-specific focus groups, new route flags, new formables, new package events, a dedicated GUI, portraits, flags, advisors, achievements, super-events, or technologies.

Do not admit IW-013 or another package as a workaround for an AI evidence gap.

Do not widen the project into a whole-event weight rewrite before the KUB/TAT pilot produces a usable comparison.

Do not change weights to make a report look decisive when the adapter still returns an empty or incomplete pool.

## Tooling limits

The event inspection and render are partial and do not prove full lifecycle execution.

The probability adapter currently exposes source discovery but not a resolved KUB/TAT world-state pool, and the AI strategy adapter exposes no weighted surface for the package strategy files.

These are exact unresolved MCP limits and source review is not treated as equivalent quantitative evidence.

Technology work is outside this tranche.

The installed package has no Technology Tree Viewer, so no technology-tree evidence or conclusion is claimed.

No rewrite tool was used.

## Promotion recommendation

## Current sequential MCP retry (2026-08-13)

The dated sequential retry produced current TAT mission and KUB/TAT strategy discovery receipts. A later bounded recheck evaluated six named scenarios for each package and ran current/current comparisons, but typed fixtures were rejected or left unresolved, pools remained incomplete with zero available candidates, and no before/after balance claim follows. The detailed evidence is in `subagent_handoffs/006_admitted_package_ai_addendum_refresh_2026_08_13.md` and the current AI recheck handoff.

Keep this addendum in `docs/plans/006_independence_wave_plans/` as the dated evidence record while typed fixtures and a meaningful before/after comparison remain unresolved.

If the KUB/TAT pilot is accepted and produces a complete baseline and same-scenario comparison, promote only the general two-stage package-AI contract, scenario requirements, and reserve-floor rules into specification parts 4 and 7.

Keep KUB/TAT artifact URIs, scenario hashes, adapter limitations, and dated comparison results in plan or handoff evidence rather than turning them into permanent design prose.

Do not promote any formable or super-event change because this addendum accepts none.
