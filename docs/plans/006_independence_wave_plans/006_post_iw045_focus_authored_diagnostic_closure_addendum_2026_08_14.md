# Event 006 post-IW-045 focus authored-diagnostic closure addendum

Date: 2026-08-14

Status: **IMPLEMENTED AND MCP-VALIDATED**

## Decision

The smallest next high-impact Event 006 tranche is a bounded shared-focus connector cleanup, not another country package, probability-tuning pass, formable, or super-event wiring pass.

The implementation owner removed the ten redundant visible prerequisites that connected `independence_wave_prepare_capital_administration` to ten distant, already-wired package roots. Every affected root retains the parent-completion requirement and its exact package identity in `available`, and every root remains hidden behind its package-specific `allow_branch`. The edit removed only ten misleading long connectors. It did not change focus availability, package identity, rewards, AI, coordinates, route locks, admission, dispatch, attestation, preflight, or Join.

This addendum does not authorize a broad tree reflow. It does not authorize removal of any other prerequisite.

## Current authority and immovable boundaries

The current accepted Event 006 authority remains:

- 32 content-attested packages.
- 29 compatible reservation groups.
- 161 unattested selectable rows.
- 40 runtime adapters.

IW-045 Bashkiria establishes the current 32/29/161/40 boundary. Its package-local country, decision, focus-hook, localisation, and AI-strategy sources are present, but central adapter, content attestation, normal/scenario preflight, deterministic Join, and full quantitative AI evidence remain deliberately bounded by the current authority. This focus cleanup did not add any package to a central list or turn package-local content into an admission claim.

The automatic release ladder remains `3/4/5/7/10`, including World Collapse at `10`. No allocator, dispatcher, attestation, reservation, Join, event, on-action, or package registry source is in scope.

## Prior addendum disposition

`006_admitted_package_ai_evidence_tranche_addendum_2026_08_13.md` remains **QUEUED / EVIDENCE-BLOCKED**, not superseded and not rejected. Its KUB/TAT typed-scenario design is still valid, but the current mission inspections have incomplete pools and zero available candidates under empty fixtures, while the `ai_strategy_factor` adapter exposes no weighted surface. Starting another package-probability plan would duplicate that unresolved addendum. The parent should resume it only when the installed probability route accepts the required typed state or returns a new exact schema result.

Older accepted package and formable addenda retain their recorded implemented, queued, or fail-closed dispositions in `006_source_of_truth_map.md`. This plan does not reopen or redesign them.

## Evidence baseline

### Current focus source and MCP evidence

Fresh read-only `hoi4.focus_inspect` completed for `common/national_focus/006_independence_wave_focus.txt`, tree `independence_wave_focus_tree`, in workspace `mod_chaos_redux_ea3b2d67c2c0`.

- Revision: `4d270a35c303796596f39254ec287c181683e508ac50e141415888f39cc39c4b`.
- Focuses: 184.
- Connectors: 196.
- Connector crossings: 0.
- Focus-node intersections: 0.
- Long connectors: 2.
- Layout hash: `014c594a446087d67b6623767e34af4b83a026e7446235e5a3bd3cbc4eceef2a`.
- Validation aggregate: 14 blocking focus diagnostics.
- Inspect artifact: `hoi4-agent://workspace/mod_chaos_redux_ea3b2d67c2c0/artifact/2789a26798d07b748c1a70eddf26854a87a6bcb1877010f30bdcdf4096f1d017/6b0fee03d1b7f3a9c48a326c73173258d8a68398ff6502230327ef829a071e72/focus-inspect.4d270a35c3037965.json`.

Fresh read-only `hoi4.focus_render` returned the same layout hash and current source-linked artifacts:

- HTML: `hoi4-agent://workspace/mod_chaos_redux_ea3b2d67c2c0/artifact/b1bd43c2ce2d29af97948ad2eb42c6bb77d5f150b57a85e79c1ea13a65572f9e/9eaaecb297adb74d9ec065c1781477a8b8c76ee492a0ccb874dd67347ae3f6c3/independence_wave_focus_tree.focus.html`.
- SVG: `hoi4-agent://workspace/mod_chaos_redux_ea3b2d67c2c0/artifact/c51a07eee7fcbe60e40231211902e20aa68aff6e2313b7484db6ee1705a5bf19/deb9cdace10f3fd7ee26e980af0c0eee0aea2d76b9b464b04eb860e717c67e01/independence_wave_focus_tree.focus.svg`.
- JSON: `hoi4-agent://workspace/mod_chaos_redux_ea3b2d67c2c0/artifact/4414499d0057dcff6c2933035a687e82740c98d0b3695c25662498f649856367/9bd104665297975943e2a75d4622bd6e4feeafe8faf548b16420e3348939ef8a/independence_wave_focus_tree.focus.json`.

The twelve current long connectors consist of two shared-lane connectors and ten remote package-root connectors. The package-root group accounts for horizontal spans from 44 to 100 columns. All ten begin at `independence_wave_prepare_capital_administration`, while the child roots sit in distant package lanes.

The inspect also reports three missing icon references from installed vanilla continuous-focus inventory. Those are not Event 006-authored sprites and must not be repaired in this tranche.

### Current event MCP evidence

Fresh read-only `hoi4.event_inspect` state-flow analysis for `chaosx.nr6.1` returned `EVENT_INSPECTED_PARTIAL`, revision `10b71c98d51a708a5e4791ff29345586abd212f5a697848f4c9944cb20194de9`, graph hash `8a21ebae74c0fa858d0dd85821dc620e03629d6112a6b2f4d6ce8b59327ba42d`, and zero blocking diagnostics. Large-workspace helper projection and lifecycle analysis were deferred.

- Inspect artifact: `hoi4-agent://workspace/mod_chaos_redux_ea3b2d67c2c0/artifact/2b57ba4b7e2aa8d7bf57583699891a837571f2fc776adc1417b58ea437ac76e5/ce79cd433b5e77d8436a625ae8c29f4d841c39ee32f12c4604f47e25462f28f8/event-state_flow-10b71c98d51a.json`.
- Overview SVG: `hoi4-agent://workspace/mod_chaos_redux_ea3b2d67c2c0/artifact/1ab3f166f5f5cc5d0de7c3c1e6105e78a0d78dfe6feb3c4804b510088a8e4b73/74020a44b537aa9b7e0c095c947e7bc478fb1509cf182a5e0d80a57ac02fd438/event-overview-10b71c98d51a.svg`.

This partial result supports only the boundary that no event-chain edit is warranted by this focus-layout tranche. It is not full lifecycle evidence.

## Exact implementation tranche (completed)

Owner file:

- `common/national_focus/006_independence_wave_focus.txt`.

For each child below, the line `prerequisite = { focus = independence_wave_prepare_capital_administration }` was removed.

| Package root | Current horizontal span | Required gates that remain in source |
| --- | ---: | --- |
| `independence_wave_sco_reconnect_central_belt_focus` | 56 | `allow_branch = { is_independence_wave_sco_package = yes }`; `available` retains parent completion and the SCO package gate |
| `independence_wave_wls_reconnect_north_and_south_focus` | 62 | `allow_branch = { is_independence_wave_wls_package = yes }`; `available` retains parent completion and the WLS package gate |
| `independence_wave_ajx_keep_mines_breathing_focus` | 68 | `allow_branch = { is_independence_wave_ajx_package = yes }`; `available` retains parent completion and the AJX package gate |
| `independence_wave_bri_charter_ports_fisheries_focus` | 76 | `allow_branch = { is_independence_wave_bri_package = yes }`; `available` retains parent completion and the BRI package gate |
| `independence_wave_afx_charter_sambre_meuse_authority_focus` | 88 | `allow_branch = { is_independence_wave_afx_package = yes }`; `available` retains parent completion and the AFX package gate |
| `independence_wave_agx_chart_waterline_authority_focus` | 100 | `allow_branch = { is_independence_wave_agx_package = yes }`; `available` retains parent completion and the AGX package gate; its existing bypass remains unchanged |
| `independence_wave_rhi_establish_corridor_authority_focus` | 82 | `allow_branch = { is_independence_wave_rhi_package = yes }`; `available` retains parent completion and the RHI package gate |
| `independence_wave_bay_broker_civic_settlement_focus` | 94 | `allow_branch = { is_independence_wave_bay_package = yes }`; `available` retains parent completion and the BAY package gate |
| `independence_wave_arx_reconcile_municipal_ledgers_focus` | 44 | `allow_branch = { is_independence_wave_arx_package = yes }`; `available` retains parent completion and the ARX package gate |
| `independence_wave_asx_unify_palermo_port_books_focus` | 50 | `allow_branch = { is_independence_wave_asx_package = yes }`; `available` retains parent completion and the ASX package gate |

Do not change the ten roots' coordinates, icons, cost, search filters, rewards, `allow_branch`, `available`, `bypass`, or `ai_will_do` blocks. Do not move or re-anchor their child chains. Do not add a synthetic hub focus, an invisible bridge focus, or a focus-navigation shortcut as compensation.

The package roots remain honest module roots: the player sees only the package lane admitted by its existing `allow_branch`, and the selectable tooltip continues to require completion of `independence_wave_prepare_capital_administration`. The shared prerequisite remains a gameplay gate rather than a 44-to-100-column line across unrelated branch space.

## Acceptance and rollback contract

After the parent applies the exact ten-line removal, the focus owner must run fresh `hoi4.focus_inspect` and `hoi4.focus_render` against the same file and tree before accepting it.

The expected structural delta, assuming no concurrent focus-source edit, is:

- 184 focuses remain.
- Connector count falls from 206 to 196.
- Long-connector count falls from 12 to 2.
- Connector crossings remain 0.
- Focus-node intersections remain 0.
- All ten package roots, titles, icons, rewards, and child chains remain resolved.
- Every root retains exact package visibility and completed-administration availability gates.
- No `FOCUS_GAMEPLAY_REFERENCE_PARTIAL`, isolated-focus, dangling-connector, duplicate-coordinate, missing-localisation, or new icon diagnostic is introduced.

If the new inspect or normal-zoom render reports an isolated package branch, unreadable entry state, new crossing, node intersection, lost child chain, or new reference error attributable to the removal, restore the affected prerequisite and record that root as an intentional long connector. Do not repair a failed removal by moving the whole lane or weakening its gates in the same tranche.

Because this is a prerequisite-presentation change, the focus owner must manually compare each affected `available` block with its removed line in the source diff. A passing render alone is insufficient.

No probability comparison is required because no `ai_will_do`, strategy factor, random selection, MTTH, or custom weighted pool is changed. If the implementation owner changes any such surface, that change is outside this addendum and requires a new `hoi4.probability_inspect` baseline plus a `chaosx_ai_probability_auditor` same-scenario comparison.

## Candidate-surface dispositions

| Candidate next surface | Disposition | Reason |
| --- | --- | --- |
| Ten remote package-root connectors | **IMPLEMENTED / ACCEPTED** | One source file and ten redundant lines removed ten of twelve long connectors without changing mechanics or authority; post-change inspect/render receipts are recorded in the handoff |
| Remaining transport and treasury detours | **QUEUED, NOT IN THIS TRANCHE** | A previous economy-lane coordinate trial introduced a crossing and was reverted; these need a separately rendered local layout design |
| Military-archetype and independent-command geometry | **QUEUED, NOT IN THIS TRANCHE** | The independent-command visible edge was previously restored after removal isolated the choice; no repeat trial is authorized here |
| Former-host successor-ledger connector | **QUEUED, NOT IN THIS TRANCHE** | It is a real visible route split and needs a separate branch-anchor review, not mechanical prerequisite deletion |
| Formable discovery detour and formable gates | **QUEUED / FAIL-CLOSED** | FORM-07, FORM-08, FORM-16, FORM-48, and other families retain exact identity, member, consent, territory, integration, and readiness gates; this plan supplies no map or GUI conclusion |
| KUB/TAT typed probability fixtures | **QUEUED / EVIDENCE-BLOCKED** | The existing addendum remains unresolved; do not stack a second probability plan |
| IW-045 BSK probability tuning | **REJECTED AS NEXT TRANCHE** | `mission_ai_will_do` has an incomplete pool and zero available candidates without typed state, while `ai_strategy_factor` returns `no_weighted_surfaces`; no balance target is proven |
| IW-045 central adapter, attestation, preflight, or Join | **REJECTED BY SCOPE** | It would widen the exact 31/28/162/39 authority and violate the requested fail-closed boundary |
| Another package promotion | **REJECTED BY SCOPE** | It would widen central attestation/Join instead of closing a shared accepted surface |
| Super-event 23 audio, wrappers, catalogue row, or firing | **BLOCKED / REJECTED AS NEXT TRANCHE** | The candidate track still requires explicit parent/user selection, human audition, and CC BY-SA distribution approval; silence or a substitute cue is an unapproved fallback |
| Event-chain changes | **REJECTED AS NEXT TRANCHE** | Current event evidence is partial but has zero blocking diagnostics, and no event defect is tied to the focus connector problem |

## Historical and regional design basis

No new historical claim or regional route is introduced. The ten roots already express distinct Scottish, Welsh, Walloon, Breton, basin, waterline, Rhineland, Bavarian, Sardinian, and Sicilian institutions. The accepted focus architecture requires regional modules to attach near the lane they modify and not cross the whole tree. Removing only the redundant common connector preserves those distinct institutional identities while making their gated package modules visually self-contained.

This is a presentation repair, not permission to merge regional identities, reuse one package's effects for another, or convert a package-root lane into generic filler.

## What must not be added

Do not add focus nodes, coordinates, prerequisites, scripted effects, route flags, decisions, missions, events, countries, formables, GUI surfaces, technologies, icons, localisation, portraits, audio, super-event predicates, or achievements.

Do not edit package adapters, the dispatcher, attestation triggers, preflight, reservation, allocator, Join, automatic ladder, world-chaos logic, workbook, CSV exports, asset manifests, or sound definitions.

Do not use a hidden focus, duplicate parent, fake navigation node, silent audio, substitute track, generic country identity, placeholder asset, or weakened package gate as a fallback.

Do not use `hoi4.focus_rewrite` for this tranche. The implementation owner should make the exact source edit and use only the read-only focus inspection and rendering routes for evidence.

## Tooling limitations

The event viewer remains partial because large-workspace helper and lifecycle projections were deferred. This focus-only plan makes no event-lifecycle acceptance claim.

The installed MCP package exposes no Technology Tree Viewer. Technology and doctrine work are outside this tranche, and no technology-tree conclusion is claimed.

The installed probability adapters still do not provide a resolved typed KUB/TAT/BSK campaign pool or an `ai_strategy_factor` surface. Those limitations remain evidence blockers and are not repaired by focus layout work.

## Promotion recommendation

Keep this addendum as the dated implementation record. The exact ten-line source patch and its post-change focus inspect/render receipt are recorded in the companion handoff; the remaining focus diagnostics are outside this tranche.

If accepted, promote only the general rule into `docs/specs/006_independence_wave_specs/specs/006_independence_wave_spec_part_4_focus_tree_architecture.md`: a distant gated package module may use its exact completed-focus condition in `available` without a redundant visible connector when that connector crosses unrelated branch space. Record dated IDs, artifact URIs, and diagnostic counts in a handoff, not permanent design prose.

If the MCP comparison rejects any root removal, keep the plan and rejection evidence in `docs/plans`; do not promote the failed root as a general pattern.
