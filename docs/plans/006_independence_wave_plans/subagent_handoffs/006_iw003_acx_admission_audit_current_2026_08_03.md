# IW-003 Cornwall current admission audit

Date: 2026-08-03

Verdict: **HOLD / fail-closed; no safe narrow promotion.**

This audit uses only current repository and installed-map evidence. The obsolete pasted flag-log is excluded. No gameplay, map, country, asset, localisation, adapter, or attestation file was edited.

## Evidence that remains valid

`ACX` is a reserved collision-free Event 006 X carrier with a dormant country shell, neutral history record, country-name and ideology localisation, and a documented St Piran's Cross flag triplet. These are scaffolding and asset-layer evidence only; they do not constitute a playable country package.

## Hard blockers

1. The current binding row is `disabled_no_unique_current_state` and `unbound`. Exact Cornwall is province 6526, while Plymouth and province 11406 remain in vanilla state 123. The next contiguous state ID, 1082, is claimed by installed Workshop mods; all IDs through 2525 are occupied and 2526 would leave an illegal gap. The full map feasibility audit therefore requires ACX to remain dormant with no fallback state or renumbering.
2. No ACX package runtime exists beyond the shell: there are no characters or `.gfx` consumers, setup/final-validation/cleanup adapter, package triggers/effects, ideas, decisions/missions, focus assignment, force/OOB setup, AI profile, or central dispatcher branch. SCN-008 explicitly retains IW-003/ACX in blocked arrays.
3. Existing ACX leader/committee DDS files are unregistered generated readiness-pool art. The source-mode audit marks them orphaned and requiring a sourced identity/institution review before any admission. No generic portrait or advisor icon is an acceptable substitute.
4. Host-survival and playable package checks cannot be evaluated without a legal unique current-map anchor.

Adding ACX to central attestation or adapters would bypass the map and portrait gates. No such bypass, history rewrite, filler state, tag rewrite, fallback, or asset substitution is authorized.

## Reopen conditions

Reopen only after an accepted collision-free state-ID/map contract and sourced identity review exist. Then implement the complete ACX package surface and rerun the package, host-survival, collision, flag, asset, localisation, focus, decision/mission, AI, and attestation audits together.
