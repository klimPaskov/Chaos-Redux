# Event 006 focus radical-root prerequisite

Date: 2026-08-12

The generic Event 006 focus `independence_wave_reject_inherited_borders` now declares `prerequisite = { focus = independence_wave_complete_founding_settlement }` in `common/national_focus/006_independence_wave_focus.txt`.

Its existing `available` trigger and radical route lock remain unchanged, so the patch only makes the visible focus graph reflect the founding-settlement requirement that was already enforced by script.

No reward, AI weight, route trigger, mutual exclusion, localisation, or package admission behavior changed.

Current MCP focus inspection remains unavailable because the workspace returns `ARTIFACT_MANIFEST_INVALID` before source scanning.

The current Event 006 authority remains 29 admitted packages, 26 compatible reservation groups, 164 unattested selectable rows, and 37 central adapters.
