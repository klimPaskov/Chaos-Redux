# IW-051 Sakha route flag asset handoff

## Disposition

Package-local generated route ladders are prepared and remain `needs_user_review`.
No neutral 1936 flag claim, vanilla YAK overwrite, central admission, preflight,
or deterministic Join change is made.

## Outputs

- Native masters remain unchanged under `docs/assets/006_independence_wave/iw051_sakha_flags_2026_08_15/source_png/imagegen_raw/`.
- Four route basenames have normal, medium, and small TGA ladders under `gfx/flags/`, plus 12 uncompressed BGRA DDS evidence copies under the package `final_dds/` folder.
- `manifest.md`, `gfx_handoff.md`, `metadata/flag_validation.json`, `metadata/dds_validation.json`, `metadata/generation_evidence.json`, `build_flags.py`, and the contact sheet document the package.
- The generated source canvases included an unintended red remainder. Explicit route-specific upper-left crop boxes are recorded and applied mechanically; no local emblem redraw or copied historical art was added.

## QA and boundary

The generated flags are alternate-history route syntheses inspired by the
research-only 1926 Yakut ASSR Aurora/light-blue motif. They are not an
attested universal 1936 Yakut flag. Normal/medium TGA headers use descriptor
8 and small headers use descriptor 0, matching the installed vanilla ladder
convention. DDS validation records exact dimensions, BGRA masks, lengths, and
PNG round-trip equality for all 12 evidence files.

Prompt text was not present beside the supplied native masters, so the
package records that provenance gap instead of inventing prompt evidence.
Parent review is required before central wiring or any claim that YAK is
identity/asset-cleared. No portrait DDS/GFX was created; the separate grounded
Pavel Pevznyak source package remains source-placeholder only and retains no
156x210 archive file.
