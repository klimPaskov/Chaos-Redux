# Event 006 decision and mission static audit — 2026-08-01

## Disposition

The bounded pre-wave crisis transaction is source-closed. The whole decision and mission surface remains **STATIC HOLD / PARTIAL** because the 14- and 20-country bands have no admitted capacity path and IW-014 CAT remains deliberately outside runtime content attestation.

## Reviewed contracts

- The crisis decision uses concrete manpower, Army XP, command power, infantry/support-equipment, and stability costs, with active-mission, requester, queue, cooldown, cancellation, and durable-resolution receipts.
- CAT's founding mission has a 420-day window, ledger-based success, concrete timeout loss, and generation-safe cleanup.
- CAT exposes eleven serialized project decisions with administration, security, diplomatic, or strategic costs. The protected-customs patron route is reachable through its own paid project, while sovereignty codification is serialized behind the active-project gate.
- The CAT decision category is visible only after CAT setup completion. Generic shared cost localisation remains authoritative; no CAT duplicate cost keys remain.
- Route, former-host, league, network, affordability, and cleanup gates are source-present. CAT is still fail-closed by the shared formable readiness contract and is not part of the 13-package attestation set.

## Remaining blockers

1. The 14- and 20-country automatic bands remain fail-closed until the admitted pool and disjoint reservation capacity are expanded.
2. CAT requires a reviewed Iberian Federation identity, territory/member, integration, rollback, and cleanup adapter, or an audited Mediterranean carrier route, before admission.
3. Full runtime/save-load and player-owned decision observation remain future QA; this handoff is source/static evidence only.

## Validation evidence

- `python -B .tools/audit_event6_allocator.py` passes with 149 publishers, 126 automatic/high-chaos candidates, 138 SCN-008 candidates, 13 attestations, 12 compatible groups, and the 6/8/10/14/20 ladder.
- `python -B .tools/audit_chaosx_country_tags.py --surface-scan` reports 136 protected Event 006/Soviet tags and zero external country-definition or identity-surface collisions; the Random Event roots are intentionally excluded by the user decision.
- Focus inspection remains source-linked at 184 focuses and reports the pre-existing 14 blocking layout diagnostics; the CAT overlay imports all six package focuses and adds no new blocker class.
- Focused Event 006 event lint returns `EVENT_INSPECTED_PARTIAL` with zero blocking diagnostics; workspace-wide helper projections are deferred by the MCP boundary.
