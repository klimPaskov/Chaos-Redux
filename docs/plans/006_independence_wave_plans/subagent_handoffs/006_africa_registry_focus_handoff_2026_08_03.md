# Event 006/012 reusable carrier focus handoff

Date: 2026-08-03

## Scope

This bounded adapter repair aligns the Event 012 Africa priority-member loader with the reusable Event 006 country API. It does not create a country, cosmetic tag, history file, state transfer, or new focus tree for Event 006.

## Runtime contract

- `africa_priority_member_uses_independence_wave_country_tag` exposes the seven Event 006-owned African carriers (`DOX`, `DSX`, `DUX`, `DYX`, `DZX`, `EMX`, and `EQX`) through the canonical registry predicates.
- Those seven shells remain promotion-eligible only while `is_independence_wave_registry_event6_origin` is live. A bare registered X tag cannot masquerade as an Event 012 origin.
- `africa_priority_member_can_register_package` rejects Soviet Collapse origins before the package writes its Event 012 origin and package markers.
- `africa_priority_member_ensure_focus_tree_loaded` records `africa_priority_member_focus_tree_overlay_skipped` while Event 006 or Soviet Collapse owns the active focus surface, and also preserves any meaningful non-generic vanilla tree.
- After Event 006 cleanup writes `independence_wave_origin_ended`, the retry may load `africa_priority_member_focus_tree` for the registered X carrier with `keep_completed = yes`. Generic vanilla carriers follow the same loader; meaningful trees remain additive-only.

The Africa package still uses the existing tag and the `collection:chaosx_country_africa` lookup surface. Event 006 origin markers are never written by Event 012, and Event 006 cleanup never clears Event 012 package flags.

## Changed files

- `common/scripted_triggers/012_africa_priority_member_triggers.txt`
- `common/scripted_effects/012_africa_priority_member_effects.txt`

## Static checks

The edited Clausewitz blocks use balanced scopes, tabs, no unsupported comparison operators, and no new localisation or asset references. The existing Event 006 GUI, allocator, flag, scenario, and country-tag audits remain the parent authority. No live game or save/load claim is made.

## Remaining boundary

The whole Event 006 goal remains **HOLD / PARTIAL**. Fourteen of 193 non-overlay packages are attested, the accepted FORM-08 Vojvodina/Slavonia geography remains unresolved, and live carrier-by-carrier Africa handoff behavior still requires runtime validation outside this source-only tranche.
