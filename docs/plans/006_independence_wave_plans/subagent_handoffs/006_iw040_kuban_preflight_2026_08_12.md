# IW-040 Kuban Cossack Host preflight — 2026-08-12

> Historical pre-promotion preflight: this receipt predates the IW-040 admission and IW-044 promotion. The KUB binding and vanilla evidence remain traceability inputs, but its fail-closed and “not promoted” statements do not describe current routing.

## Disposition

At the preflight snapshot, IW-040 remained fail-closed and was not promoted. This bounded preflight records the source and vanilla evidence available then; it did not create gameplay, portrait, flag, or attestation changes.

## Current binding

The installed-map binding names KUB, state 234 Krasnodar, and reservation group `RG-DON-KUBAN`. The group is shared with IW-039 and therefore requires mutual exclusion and a protected Soviet host remnant. The current region-04 planner shell can calculate and reserve IW-040, but this is registry/planner readiness rather than content readiness.

Vanilla registers KUB as `countries/Kuban Republic.txt` and provides `KUB_ivanis_vasily_nikolaevich` with a vanilla portrait. Those facts do not by themselves establish an Event 006 additive package, a current-map state proof, or a cleanup-safe origin contract.

## Missing admission gates

At that preflight snapshot, no IW-040 runtime adapter, central setup/final-validation/cleanup family, content-attestation entry, Join probe wrapper, package-specific decisions, ideas, AI, localisation, focus hooks, formable route, or Event 006 asset/portrait admission receipt was present. The p40 force row and region-04 planner shell were planning inputs only.

The shared DON/KUBAN group also carries Event 005 state/identity interaction risk. Any future implementation must capture the actual host at runtime, preserve host survival, and prove that the Event 005 KHC route and IW-040 cannot mutate the same state or identity transaction.

## MCP limitation

The required current map inspection for state 234 returned `ARTIFACT_MANIFEST_INVALID` (`Artifact provenance manifest is invalid`) in workspace `mod_chaos_redux_ea3b2d67c2c0`. No current map, event, focus, or probability artifact is available; source review is not treated as equivalent engine evidence.

## Required next work

Before the later IW-040 admission, the documented next steps were to obtain a current-map/host receipt, a sourced and rights-reviewed Kuban identity/portrait/flag package, complete the package adapter and lifecycle, resolve the RG-DON-KUBAN/Event 005 collision contract, and rerun the mandatory MCP and weighted audits. Those preflight conditions are historical; current KUB package status is recorded in `docs/events/006_independence_wave/kuban_package.md` and the IW-040 current lifecycle/probability handoffs.

No gameplay or binary asset files were changed by this preflight.
