# Event 006 core contract re-audit v49 — 2026-08-01

## Scope

This parent-owned re-audit records the current core allocator, reusable country
registry/API, scoped tag-collision surface, crisis queue, and the bounded
attested-package contract after the 2026-08-01 source and package audits.

## Core evidence

- `python .tools/audit_event6_allocator.py` passes with 149 publishers, 126
  automatic/high-chaos selectable packages, 138 SCN-008 ranked packages, 13
  compile-time content attestations, 12 compatible reservation groups, and the
  two-member RG-RHINE-SAAR pair at anchors 51 and 42.
- The accepted doubled ladder is 6/8/10/14/20, with World Collapse at 20.
  Scenario intensities low/medium/high/maximum and all six scenario types are
  present; the source order remains anchors, compact territory, extended
  territory, then lock, with Event 005 anchors reserved before Event 006.
- The registry/API source of truth remains the 206-row registry with 102
  Event-006-owned `X` tags, 91 reuse rows, 13 overlays, 191 unique resolved
  tags, and public `chaosx_country_*` collections plus regional arrays.
- `python .tools/audit_chaosx_country_tags.py --surface-scan` passes for the
  accepted Event 006/Soviet Collapse scope: 136 protected tags, zero external
  country-definition collisions, and zero external identity-surface
  collisions. Random Events Mod remains excluded by the explicit user scope.
- Occupation-resistance and low-stability crisis pressure enters the normal
  Event 006 allocator queue and preserves visible receipts/cleanup on failure.

## Bounded repair in this tranche

The three currently attested packages that previously recruited characters from
scripted effects now call hidden synchronous event `chaosx.nr6.15`; its event
branches perform the recruitment and leave the package effects responsible for
role setup and attestation. This restores the repository event-skill contract
without changing package identity, costs, or dispatch gates.

## Current admission boundary

The core loop and reusable registry are source-closed, but the whole event is
not complete. Only IW-001, IW-002, IW-004, IW-006, IW-007, IW-008, IW-009,
IW-010, IW-012, IW-017, IW-019, IW-173, and IW-184 are content-attested.
IW-043 CHU, IW-058 ASY, IW-179 FSM, IW-093 DOX, and IW-098 SOK remain
fail-closed pending sourced identity/role evidence or package completion. The
14- and 20-country bands therefore correctly remain closed below admitted
capacity; no generic bulk package or fallback was introduced.

The shared focus audit reports 111 shared and 184 direct focuses reachable with
no missing localisation, icon, AI, reward, cost, or prerequisite references.
Fourteen geometry diagnostics remain blocking in the MCP layout view. Live
allocator execution, save/load, scenario transaction observation, and several
formable/super-event/asset/audio closures remain future or package-gated work.

## Validation and handoff

The focused Event Viewer lint scan completed as a partial workspace analysis
with no reported blockers; its validation remains partial because the tool
defers workspace-wide helper projections. Existing country-package, portrait,
focus, and asset handoffs remain the controlling evidence for their bounded
surfaces. This handoff does not claim whole-event completion or authorize
custom Event 006 advisor icons.
