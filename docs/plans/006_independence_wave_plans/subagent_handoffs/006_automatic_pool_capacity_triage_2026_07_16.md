# Event 006 Automatic-Pool Capacity Triage

Date: 2026-07-16
Scope: smallest coherent package tranche required to prove ten simultaneous World Collapse releases
Verdict: implement `IW-017`, `IW-018`, `IW-019`, and shared `FORM-05`; no admission is granted by this handoff

## Capacity proof

After `IW-006`, `IW-008`, and `IW-009` are added to the previously admitted set, eight package identifiers are attested. `IW-008` Rhineland and `IW-010` Saar both consume `RG-RHINE-SAAR`, so only one can enter a frozen wave. The maximum disjoint set is therefore seven.

World Collapse requires exactly ten releases. The smallest coherent increment is:

| Package | Tag | Anchor | Reservation group | Depth | First eligible band |
| --- | --- | ---: | --- | --- | --- |
| `IW-017` Corsica | `COR` | 1 | `RG-1` | Level 1 | Calm |
| `IW-018` Sardinia | `ARX` | 114 | `RG-114` | Level 1 | Gathering |
| `IW-019` Sicily | `ASX` | 115 | `RG-115` | Level 2 | Gathering |

These three groups are mutually disjoint and raise the simultaneous witness from seven to ten. France retains protected capital state 16. Italy retains protected capital state 2 even when Sardinia and Sicily both release.

## Why this tranche is coherent

All three candidates share `FORM-05`, the Mediterranean Island League. Completing one shared formable transaction is narrower than selecting three reserve candidates that each require a different formable family. Existing Region 02 loaders, weights, reservations, and force profiles are scaffolding only; none proves content readiness.

Each Level 1 package still requires complete Event 006 identity, setup, forces, ideas, visible values, shared focus use, AI, diplomacy, former-host relations, regional ambitions, leadership, portraits, flags, localisation, validation, and cleanup. Sicily additionally requires its bespoke Level 2 focus lane, decision family, starting problem, institution path, incidents, and Two Sicilies or league angle.

## Asset and identity constraints

- `COR` is a matching registered vanilla identity and may be used only while dormant. The later engine audit found that detached shared focuses cannot form an additive runtime layer. Vanilla COR has no unique tree, so the implemented adapter requires `generic_focus`, loads the complete Event 006 framework, and restores `generic_focus` on cleanup; any mod-provided meaningful tree fails setup instead of being overwritten.
- `ARX` and `ASX` are collision-audited Event 006 X tags and remain distinct from `SPM` and `TTS`.
- Real-country flags must follow researched historical designs and be generated as flat flag designs through ImageGen, never as illustrative artwork.
- The currently installed `ASX` S.015 art is constitutional-route-specific and is not an approved neutral baseline. The asset pass must research and generate a defensible neutral Sicilian baseline rather than silently reusing it.
- Historical leaders require sourced head-and-shoulders identity-preserving HOI4 treatment. Fictional leaders require distinctive ImageGen portraits. Gameplay advisor offices remain asset-neutral and must not receive custom portrait icons.

## Deferred reserve candidates

- `IW-015` Galicia is a sound reserve but requires `FORM-07`.
- `IW-021` Trieste is a sound reserve but requires `FORM-06`.
- `IW-030` Montenegro is a sound reserve but requires `FORM-09` and preservation of its meaningful vanilla focus selection.
- `IW-012` Iceland cannot support baseline capacity because its sole-state host-survival condition is not guaranteed.
- `IW-011` Faroe remains scenario-only.
- `IW-003` Cornwall remains disabled because no legal unique anchor exists on the installed map.
- `IW-005` Flanders remains a deliberate BEL overlay rather than a standalone release.

## Promotion boundary

Do not add content attestations, SCN-008 preflights, automatic-readiness branches, or capacity witnesses until all three packages, `FORM-05`, their assets, localisation, and AI have passed independent package, focus, decision/mission, localisation, formable, and collision audits.

## Simplifications, omissions, and blockers

No fallback is proposed. The Mediterranean gameplay and asset packages remain implementation work at the time of this handoff.
