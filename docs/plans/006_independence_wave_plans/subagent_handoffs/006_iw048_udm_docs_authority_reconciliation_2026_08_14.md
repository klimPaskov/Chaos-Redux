# IW-048 UDM documentation authority reconciliation

Date: 2026-08-14

## Disposition

IW-048 UDM is documented as package-local and unadmitted after its completed local tranche.

The reconciliation preserves the current Event 006 authority of 40 runtime adapters, 32 content-attested selectable packages, 29 compatible reservation groups, and 161 unattested selectable rows.

UDM is explicitly absent from central adapter dispatch, content attestation, normal and SCN-008 preflight, scenario admission, and deterministic Join.

## Current package facts

The package reuses vanilla `UDM`, vanilla history and politics baseline, vanilla capital/state 399 Izhevsk, vanilla `UDM_boris` / Boris Berman and its existing portrait token, and the installed vanilla normal, medium, and small ideology flag ladders.

The package-local source uses force profile p48 `industrial_security` and the existing shared `industrial_breakaway` archetype token for its initialization gate because no separate shared `industrial_security` archetype constant exists.

The local mission, ten timed projects, decisions, ideas, AI strategy, lifecycle cleanup, localisation, and five guarded shared-focus helper calls remain package-local.

## Blockers and bounded evidence

Admission remains blocked on acceptance of vanilla identity and asset reuse, state-399 host/anchor binding, the force/archetype mapping, typed probability fixtures with a same-scenario comparison, whole-event MCP evidence, and central wiring review.

The state-399 map receipt confirms selected state, region, and network checks after an allocation-request collision, while global map validation remains false because of unrelated workspace building/port diagnostics and truncated inventory.

The mission probability receipt reports eleven candidates, zero available candidates, fifteen required inputs, and an incomplete pool, and the AI-strategy receipt discovers no weighted surface; no quantitative balance claim is made.

The focused Event 006 inspect remains partial with zero selected blocking diagnostics, and the shared-focus receipt retains 184 focuses, 196 connectors, zero crossings, zero node intersections, and two long connectors without a UDM layout change.

The implementation and package-audit authorities remain `006_iw048_udm_package_local_implementation_2026_08_14.md` and `006_iw048_udm_package_audit_2026_08_14.md`.

## Files changed

- `docs/plans/006_independence_wave_plans/006_source_of_truth_map.md`
- `docs/plans/006_independence_wave_plans/006_independence_wave_resume_packet.md`
- `docs/specs/006_independence_wave_specs/quality/package_manifest.md`
- `docs/specs/006_independence_wave_specs/quality/simplifications_omissions_and_blockers.md`
- `docs/plans/006_independence_wave_plans/subagent_handoffs/006_iw048_udm_docs_authority_reconciliation_2026_08_14.md`

## Validation and boundary

Targeted searches confirmed UDM appears as package-local/unadmitted and did not add it to the current Join order or central attestation statements.

The current count and ladder searches retain `40`, `32`, `29`, `161`, and `3/4/5/7/10` in the edited authority blocks.

`git diff --check` is required on the five scoped files before parent review.

No gameplay, localisation, asset, workbook, central admission, or Join files were edited by this reconciliation.

The whole-event status remains **HOLD / PARTIAL**.
