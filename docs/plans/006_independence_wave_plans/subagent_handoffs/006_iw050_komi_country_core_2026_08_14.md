# IW-050 Komi package-local core handoff

## Disposition

IW-050 is package-local and remains fail-closed for central admission. The source tranche adds the country-local lifecycle, ledger, route, focus-helper, decision-alias, setup, validation, and cleanup surfaces without widening the central dispatcher, content-attestation OR list, scenario preflight, or deterministic Join sequence.

## Changed source

- `common/scripted_effects/006_independence_wave_komi_package_effects.txt`
- `common/scripted_triggers/006_independence_wave_komi_package_triggers.txt`
- `common/script_constants/006_independence_wave_komi_constants.txt` comment corrected to the accepted p55 force row
- `common/ai_strategy/006_independence_wave_komi.txt`

The package uses the installed KOM/state-397 identity, Northern Council and taiga/rail ledgers, four route government installers, generation-safe project failure, the vanilla `KOM_pavel_murashev` roster checkpoint, and five guarded shared-focus helper contracts. Route-specific cosmetic tags are intentionally not assigned because the neutral and route-flag audit remains fail-closed.

The prepared setup trigger now matches `independence_wave_force_package_military_tradition.p55` and no longer requires and forbids the same terrain reinforcement flag. A duplicate active-country array check was removed. The force profile remains `mountain_frontier` and the package remains tied to the region-05 IW-050 loader/reservation contract.

## Validation

The touched effects, triggers, and AI files have balanced blocks and no unsupported `<=` or `>=` operators. Decision references were checked against the package-local effect and trigger definitions after the decision file landed. The AI probability inspect returned `PROBABILITY_SOURCE_DISCOVERED` with `discoveryReason=no_weighted_surfaces`, zero candidates, and zero unresolved items. The named empty evaluation `IW050_KOM_STRATEGY_EMPTY_CURRENT_2026_08_14` / `KOM_STRATEGY_EMPTY` returned the exact blocker `PROBABILITY_SURFACE_EMPTY` (`No weighted blocks matched this request`); no quantitative AI claim is made.

## Remaining blockers

The exact Pavel Murashev portrait source/rights gate remains blocked. The neutral and route-specific KOM flag policy remains blocked, and the installed KOM ladder requires a stable re-hash before reuse. Central adapter/attestation/preflight/Join wiring, typed mission fixtures, and any formable admission remain parent-owned future work.
