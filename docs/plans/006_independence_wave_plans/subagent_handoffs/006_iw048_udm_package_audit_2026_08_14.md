# IW-048 UDM package-local audit

Date: 2026-08-14

## Verdict

UDM is package-local and remains fail-closed for central admission. The package has local mechanics, decisions, ideas, AI strategy, localisation, lifecycle cleanup, and shared-focus calls, but it is not in the central adapter/attestation/preflight/Join surfaces.

## Source coverage

- Carrier: vanilla `UDM`, original tag preserved.
- Anchor and capital: vanilla state 399, Izhevsk.
- Leader: vanilla `UDM_boris` / Boris Berman and its existing portrait token.
- Flags: installed vanilla UDM normal, medium, and small ideology ladders reused without overrides.
- Force mapping: p48 `industrial_security`; package-local archetype gate uses the existing shared `industrial_breakaway` token because no separate shared `industrial_security` archetype constant exists.
- Ideas: seven UDM-only ideas, all restricted to original UDM and civil-war-safe use.
- Decisions: one passive founding mission and ten timed projects with exact trigger/effect cleanup parity.
- Focus: five shared anchors have guarded UDM helper calls; no new nodes or icons.
- Localisation: category, mission, ten project name/description pairs, effect tooltips, seven ideas, route parties, and package-specific cost triplets are present in a BOM-encoded file.

## Cost and lifecycle review

Factory-bearing administration and strategic projects use `independence_wave_udm_cost_administration_light`, `independence_wave_udm_cost_administration_standard`, and `independence_wave_udm_cost_strategic`. Each has base, `_blocked`, and `_tooltip` siblings and discloses the package's one-factory modifier. The security-standard project intentionally uses the shared no-factory security key.

The package setup clears stale flags and ledgers, checks the vanilla roster, initializes the UDM political baseline, installs the shared force package, and publishes setup only after the full package-ready trigger passes. Cleanup removes the mission, all ten decisions, all seven ideas, route flags, ledgers, roster flags, and package lifecycle flags.

## MCP receipts

State 399 map inspection passed selected state, region, and network checks after the allocation-request form correctly returned `MAP_STATE_ID_COLLISION` for the already-installed state. Global map validation remains false because unrelated workspace building/port diagnostics are present.

The shared focus inspect/render reports 184 focuses, 196 connectors, zero crossings, zero node intersections, and two long connectors. The UDM helper calls do not alter the layout. Aggregate focus diagnostics remain outside this package scope.

The current Event 006 inspect for `chaosx.nr6.350` is `EVENT_INSPECTED_PARTIAL` with zero selected blocking diagnostics. Workspace-wide helper/lifecycle projection is deferred, so this is not a full event completion claim.

The current mission probability inspect is `PROBABILITY_SOURCE_INSPECTED` with eleven candidates, zero available candidates, fifteen required inputs, and `poolComplete=false`. The AI strategy inspect reports `PROBABILITY_SOURCE_DISCOVERED` with `no_weighted_surfaces`. No quantitative balance, ranking, timing, or AI dominance claim is supported.

## Admission blockers

1. A separate package admission packet must accept vanilla identity/asset reuse, state-399 host binding, and the industrial-security-to-industrial-breakaway archetype mapping.
2. Central adapter, content-attestation, normal/scenario preflight, scenario, and deterministic Join entries are intentionally absent.
3. Typed probability fixtures and a same-scenario compare are still unavailable.
4. Whole-event MCP remains partial/deferred, and the shared focus tree retains unrelated aggregate diagnostics.

No new portrait, flag, country-history, character, `.gfx`, map, central dispatcher, attestation, Join, workbook, or live-game changes were made.
