# Event 006 catalog audit (v97)

Date: 2026-08-02

Scope: read-only audit of `Events!A7:M7` (Event ID 6), `Clusters!A3:G3` (Cluster ID 2), and `Scenarios!A9:F9` (`SCN-008`) in `docs/spreadsheets/chaos_redux_events_catalog.xlsx` against the current Event 006 v97 authority and player-facing localisation.

## Result

No workbook edit was required. The three scoped workbook rows are current and match their export snapshots exactly.

- Event 006 remains `Independence Wave`, with the current event-detail text, all five evolution title/body pairs, an empty terminal world-end field, and status `In progress`.
- Cluster 2 remains `Liberations`, with members `5, 6`, `Minor Repeatable`, chaos level `1`, and status `In progress`.
- `SCN-008` remains `Every Banner Rises`, with eight player-facing mode names, the exact all-viable candidate description, four all-viable intensity contracts, and status `Needs Testing`.

The rows preserve the v97 blockers rather than implying completion. Current authority remains the 6/8/10/14/20 automatic ladder with World Collapse at 20, fourteen attested packages across thirteen compatible reservation groups, source-closed allocator/core/registry, whole-event `HOLD / PARTIAL`, and SCN-008's eight modes by four intensities with all viable candidates attempted. These implementation facts do not require new player-facing catalog cells.

## Source comparisons

- `Events!C7` matches `chaosx.events_log.window.event_details.independence_wave` exactly, including the two rival-bloc scripted-localisation lines.
- `Events!D7:H7` match `independence_wave.evolution.1` through `.5` title/body localisation exactly.
- `Events!I7` is correctly empty because Event 006 has no terminal world-end scenario field.
- `Scenarios!C9` matches `chaosx.scenarios.independence_wave.desc.sovereign_scatter` exactly (`Every researched independence movement...`, without the implementation label `Event 6`).
- `Scenarios!D9` matches the eight current mode localisation names, including the three separately selectable Universal Belligerence rules.
- `Scenarios!E9` matches the four current all-viable intensity localisation strings.

## Workbook/export status

Changed workbook path: none (read-only audit).

Changed sheets/rows/columns/event ids: none.

Exporter: not run because the workbook was not saved. Existing `chaos_redux_events_catalog.csv`, `chaos_redux_clusters_catalog.csv`, and `chaos_redux_scenarios_catalog.csv` rows were compared and match the workbook rows exactly.

Blocked or `needs_user_review` cells: none in the three catalog rows. The incomplete/blocked state is intentionally represented by the existing `In progress` and `Needs Testing` statuses; no completion promotion or blocker removal was made.
