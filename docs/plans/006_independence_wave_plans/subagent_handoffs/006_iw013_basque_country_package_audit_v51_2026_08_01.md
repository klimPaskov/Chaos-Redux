# Event 006 IW-013 Basque Country package audit v51 — 2026-08-01

## Scope

This bounded audit covers IW-013 Basque Country on the registered vanilla `NAV` carrier, with the accepted compact anchor at state 172 Navarra and reservation group `RG-172`.

## Current source surface

The registry and allocator surfaces are present in `common/script_constants/006_independence_wave_package_constants.txt`, `common/scripted_triggers/006_independence_wave_packages_region_02_triggers.txt`, and `common/scripted_effects/006_independence_wave_packages_region_02_effects.txt`.
The loader publishes package id IW-013, Mediterranean/Iberia region, mountain-industrial archetype metadata, `NAV`, anchor 172, and the host-remnant reservation contract.
The runtime dispatcher in `common/scripted_effects/006_independence_wave_package_dispatch_effects.txt` has no IW-013 setup, final-validation, or cleanup branch.
No Chaos Redux IW-013 package effect, package trigger, decision/mission surface, package idea lifecycle, focus overlay, AI profile, leader wiring, flag wiring, or localisation package was found.

## Vanilla identity boundary

The vanilla `NAV - Navarra.txt` history owns state 792, generic-focus startup, the vanilla Navarra flag, and the male leader roster Ramón Ormazábal Tife and Luis Urrengoetxea.
Event 006 must preserve that dormant vanilla history and use an additive overlay after a valid release rather than overwrite a living country or meaningful tree.
The accepted map contract uses state 172 as the Event 006 compact anchor, so the package must prove the installed state binding before reservation and must not silently substitute state 792.

## Required package work before admission

- Add a package-owned setup, final-validation, and cleanup adapter with the Event 006 origin/package/anchor guards.
- Track visible Basque language-institution, industrial capacity, fueros/municipal legitimacy, mountain-security, host-relations, network, patron, and league values with centralized constants and concrete-cost actions.
- Map the accepted force archetype to paid mountain infantry and industrial militia material and wire dynamic force receipts without free-unit loops.
- Add a safe additive focus overlay for constitutional, labor, nationalist, traditional, and patron-client routes, with language, industry, fueros, host settlement, regional ambition, league, and FORM-07 hooks.
- Add decisions, timed missions, idea lifecycle, AI strategy, diplomacy/host relations, cleanup, and complete localisation for the package.
- Verify the vanilla NAV leaders or replace them only after the source/date/identity audit accepts the exact grounded roster.
- Research and approve a historical Basque/Navarre flag design and male archival leader evidence before any DDS/GFX promotion.

## Readiness disposition

IW-013 remains outside runtime content attestation and must not enter automatic release merely because its registry row is `automatic_pool_ready_if_unique_state_exists`.
The allocator’s unique-anchor and host-survival gates remain necessary but are not sufficient until the package dispatcher and grounded identity/asset gates exist.
No advisor icons, generated historical flag claims, fallback leader, generic bulk package, or tag-registration change was introduced by this audit.

## Validation

The Event 006 allocator and protected external-tag audits remain the controlling core evidence and are unchanged by this read-only package audit.
The package gap is source-visible through the absence of `iw_013` branches in the central dispatcher and the absence of IW-013-specific scripted surfaces.
