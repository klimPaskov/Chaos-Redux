# Event 006 auxiliary origin gate

Date: 2026-09-01.

## Scope and disposition

This bounded safety tranche keeps Event 006 focus selection and AI strategy behavior behind the real Event 006 active-origin predicate. It removes an uncommitted Event 021 adapter widening that allowed package-content setup state to satisfy auxiliary Event 006 AI/focus surfaces before an Event 006 origin existed.

## Source change

The following surfaces now require `is_independence_wave_active_country = yes` again:

- `common/national_focus/006_independence_wave_focus.txt` shared focus-tree urgent AI modifier.
- `common/ai_strategy/006_independence_wave_ai_strategy_registry.txt` generic survival, recovery, and consolidation profiles.
- `common/ai_strategy/006_independence_wave_ai_strategy_registry.txt` FORM-05 island-union policy profile.

The transient `is_independence_wave_package_content_active` predicate remains available to package-side setup/effects where the Event 021 bridge explicitly needs it. Existing on-action adapter guards were left unchanged because they suppress Event 006 Join/evolution callbacks for an adapter rather than exposing Event 006 content.

## Safety rationale

The Event 021 adapter's transient setup and origin-neutral receipt branches intentionally do not create a committed Event 006 origin, lifecycle, evolution, or player-facing decision state. Allowing a generic focus AI modifier or AI strategy profile during that window could still make Event 006 focus/strategy behavior execute before the event fired. The strict origin gate preserves the user-required empty pre-event boundary while leaving the package-content bridge narrow and effect-local.

No package id, weight, cost, automatic count, category, decision, mission, queue, pressure, allocator, admission, Join, country identity, or formable was changed.

## Validation and limits

The parent reran the strict Event 006 allocator, country API, flags, FORM-16, SCN-008 scenario matrix, and GUI semantic matrix validators after this reversion; all passed with the existing `149` publishers, `126` automatic/high-chaos selectable packages, `138` SCN-008-ranked packages, `40` runtime adapters, `32` content-attested packages, `29` reservation groups, `102` complete flag families, and exact `3 / 4 / 5 / 7 / 10` automatic ladder.

Source search finds no package-content predicate in the Event 006 category registry, main decision file, national focus AI modifier, or Event 006 AI-strategy registry. The available Event MCP route remains partial and no live or save-load evidence is claimed. Whole-event completion remains `HOLD / PARTIAL` with the documented unattested package, identity/rights, probability, formable, audio, asset, and runtime evidence blockers.
