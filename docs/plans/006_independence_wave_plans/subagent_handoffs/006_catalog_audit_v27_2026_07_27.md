# Event 006 catalog audit v27

Date: 2026-07-27

Scope: read-only audit of `docs/spreadsheets/chaos_redux_events_catalog.xlsx` against the current Event 006 player-facing event-log/detail, evolution, Liberations-cluster, and SCN-008 localisation surfaces, with the current whole-event HOLD/PARTIAL boundary retained.

## Verdict

**PASS; no workbook update required.** The Event 006 catalog mirror is aligned with current in-game wording and accepted catalog status conventions. No workbook cell was changed, so the post-save CSV exporter was not run.

## Workbook rows and exact checks

- `Events!A7:M7` (Event ID `6`) was audited. `B7` matches `chaosx.event_name.6` (`Independence Wave`). `C7` exactly matches the static player-facing paragraph of `chaosx.events_log.window.event_details.independence_wave`; the runtime key appends dynamic rival-bloc lines, which are intentionally not flattened into the catalog summary. `D7:H7` exactly join each `independence_wave.evolution.<1-5>.title` and `.body` with one blank line. `I7` remains blank because no Event 006 terminal world-end branch is represented in the current event surface. `J7:M7` remain `Minor Repeatable`, cluster `2`, member severity `Medium`, and `In progress`.
- `Clusters!A3:G3` (Liberations, cluster `2`) was audited. `B3` matches `chaosx.event_cluster.liberations.name`; `C3` exactly matches `chaosx.events_log.window.cluster_details.description.liberations`; members remain `5, 6`; type, chaos level, and status remain `Minor Repeatable`, `1`, and `In progress`.
- `Scenarios!A9:F9` (`SCN-008`) was audited. `B9` matches `Every Banner Rises`; `C9` exactly matches the Sovereign Scatter premise; `D9` exactly joins all eight registered type names in UI order; `E9` exactly matches the four Low/Medium/High/Maximum impact paragraphs; `F9` remains `Needs Testing`.

The direct localisation comparison covered 13 mirror fields and produced 13 exact passes. Existing export snapshots also match the workbook for Event ID `6`, cluster ID `2`, and `SCN-008` after normalizing Excel numeric cells to CSV text; no export-only CSV was edited.

## Current status boundary

The workbook intentionally retains Event 006 and the Liberations cluster as `In progress`, and SCN-008 as `Needs Testing`. The current accepted whole-event authority remains **HOLD / PARTIAL**: static exact-ten package coverage is admitted, but live ordinary/joint execution, both IW-008/IW-010 orders and same-host remnant handling, Event 005 collision, rollback/save-load, SCN-008 runtime cells, focus-layout and reachability, AI/balance, league/achievement, and super-event evidence remain open. No status was promoted and no fallback or simplification was introduced.

## Export disposition

- Workbook path: `docs/spreadsheets/chaos_redux_events_catalog.xlsx` (unchanged).
- Sheets/rows audited: `Events!7`, `Clusters!3`, `Scenarios!9`.
- Changed sheets/rows/columns/event IDs: none.
- `python .tools/export_event_catalog_csv.py`: **not run** because no workbook save occurred; the required post-save export condition was not triggered.
- Remaining needs-testing/HOLD status: Event 006 and Liberations `In progress`; SCN-008 `Needs Testing`; whole event `HOLD / PARTIAL`.
