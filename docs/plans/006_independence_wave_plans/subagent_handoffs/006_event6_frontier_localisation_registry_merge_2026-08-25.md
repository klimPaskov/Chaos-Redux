# Event 006 Kurdistan/Kuban localisation registry merge

Date: 2026-08-25.

This source-layout merge combines the untouched Kurdistan and Kuban localisation files into `localisation/english/006_independence_wave_frontier_l_english.yml`.

The receiver keeps one UTF-8-BOM `l_english:` root with two source sections labelled `# KURD` and `# KUB`.

The two source files contribute 198 localisation keys in total, and the receiver contains 198 unique keys with no overlap.

The old files total 25,126 bytes and the receiver is 25,125 bytes, saving one source byte while removing one parser file.

Static key comparison preserves every source key and value, including the package-specific dynamic cost tokens and formable identity keys.

No localisation key, wording, icon token, package gate, decision identifier, or runtime behavior changed.

This is source-layout evidence only and makes no live parser, save/load, or in-game completion claim.
