# Event 006 latest-additions completion audit on the settled filesystem

Date: 2026-08-09

Scope: only (A) the grouped scripted-GUI state-puzzle/category workflow for the currently exposed Independence Wave formables and (B) Join the Independence Wave conversion for severely reduced countries. This is not a whole-Event-006 completion audit. No gameplay file was edited, staged, or committed.

## Current-filesystem override (2026-08-10)

This section supersedes the disposition below after the retry/loss/stale-plan patch, IW-031 promotion, spec/source-map promotion, overview correction, and generator wording correction. Historical findings remain below for traceability.

### A. Grouped state-puzzle workflow: PASS for accepted scope

Current recount gives 14/14 runtime-authored consumers in `independence_wave_formables`, 50 candidate-state rows, 100 DDS pieces, and 17 category attachments. Generated localisation has 442 `Formation status` rows and zero `Required control` rows. Accepted spec Parts 6 and 7, the spec README, source-of-truth map, overview, and formable-registry document now record the contract. FORM-07, FORM-08, and FORM-48 are explicitly accepted readiness-controlled fail-closed surfaces rather than defects in this addition. The user assigned live and family-isolated visual acceptance to later QA, so the aggregate GUI limitation is recorded but is not an in-scope completion blocker.

### B. Join the Independence Wave: PASS for accepted scope

The matching decline/timeout branch now requires the local pending flag plus owned/open plan identity, clears the contribution, aborts, clears Join runtime while the ownership proof still exists, and then resets the shared plan (`common/scripted_effects/006_independence_wave_join_effects.txt:448-465`). The guarded matching failure branch uses the same ownership-before-reset order at lines 410-425. The two transaction-ordering defects recorded in the preceding override are closed.

The busy-coordinator gap itself is closed structurally: hidden `.40` maintains a country-local seven-day retry while eligibility and threshold remain true, without a periodic/world scan. At this handoff's dated pre-IW-040/IW-044 snapshot, `python -B .tools/audit_event6_allocator.py` passed with 28 attested packages, 25 compatible groups, 165 unattested selectable rows, and 36 central adapters; the deterministic Join chain then included `iw_031` after `iw_030` and was not a weighted pool. The current source-of-truth authority supersedes those counts with 31/28/162/39 and places IW-038, IW-040, and IW-044 before IW-033.

The unsafe controller-change `num_owned_states + 1` helper and call are absent, with zero current `capture_state_loss_baseline` references. War entry remains the exact pre-war baseline producer. State-control callbacks re-evaluate the old controller and current owner without manufacturing an ownership loss. For a country with no earlier observation, the larger of current territory and core-state footprint remains the documented last-resort proxy; this can still miss former non-core holdings outside an observed war, but it is now an explicit accepted boundary rather than an undisclosed claim of universal exactness.

Probability behavior is unchanged: `.36` has no explicit `ai_chance`, so an eligible AI receives the engine-default 50/50 accept/decline split, while the 20-day timeout selects the first option, decline. Retry `.40` and the deterministic 28-package chain add no weighted surface.

### Current MCP evidence

Final owner-provided read-only MCP evidence covers the corrected source. Lint revision `8022fecdc251c2e85e825b76749ddb3c8e0ecf47e31d3b4c6dfbccabf528e074`, graph `78ebda2b4f6478c6339573d3ba75492690031d4f8ad2ed0873f7a0da1c83e8c3`: `hoi4-agent://workspace/mod_chaos_redux_ea3b2d67c2c0/artifact/f622c671955d4cb0b5496006b8f9c5650247a5e44027552c459229be40a120b4/25910b43c685d88d6404a8b9c017313963e2ca1c48ae0f4205d508e2b5ff560a/event-lint-8022fecdc251.json`. Final options/hidden-retry trace revision `e1ba28da7aaf310195c0ece2d75408cfad79c14f824ac966018c6cf14a171b73`, graph `9c15e8245925a6ef32422e84102fe84261d89c56a6f13eef9d0dfcbc5115b277`: `hoi4-agent://workspace/mod_chaos_redux_ea3b2d67c2c0/artifact/7bdeee1742c264f6326f1046d6761f41724991a653127fe56cf0a7960a6eb2a7/d19ab478b8dde1374066495bc18f8679b5ac00d5660cb562f6d08d265089bcf2/event-trace-e1ba28da7aaf.json`; options manifest: `hoi4-agent://workspace/mod_chaos_redux_ea3b2d67c2c0/artifact/a5d7fc5cb447a2af67f7b3ebbdb27982de3647e278657c988f864cf9a13a87bb/05d1f7c8d9d5830ebe83c8670637831960f3b410abae2a366ccc95d99891cbe3/event-options-e1ba28da7aaf-manifest.json`. Both are partial workspace projections with zero blocking diagnostics. The earlier compare attempt remains blocked because the requested revision was not cached. Live gameplay observation is user-owned and not required for this scoped claim.

Final scoped disposition: **both additions PASS for their accepted intended scope**. This does not change the whole-Event-006 HOLD/PARTIAL disposition. The Join source comment now matches the implementation: war entry supplies the exact baseline, while state-control callbacks only re-evaluate affected countries.

## Executive disposition

| Addition | Source implementation | Player-facing/docs | Required MCP evidence | Completion disposition |
| --- | --- | --- | --- | --- |
| A. Grouped state-puzzle workflow | **PASS for the fourteen currently exposed families and seventeen attached categories.** The same family territory helpers drive the GUI summaries and player/AI decision availability. | **PARTIAL.** Generated assets and localisation pass, but `docs/events/006_independence_wave/overview.md:15` still says fifteen attached categories while current source and `systems/formable_registry.md:272` say seventeen. Neither addition is promoted into the accepted seven-part spec package or current source-of-truth map. | **PARTIAL / acceptance proof blocked.** Current GUI inspect/render exists, but the available offline scenario activates mutually exclusive overlays together, produces truncated global diagnostics, and cannot prove family-isolated states, click regions, or overflow. | **PARTIAL, not complete.** Structural integration passes. FORM-07, FORM-08, and FORM-48 remain explicitly non-operational, and clean family-specific visual evidence is absent. |
| B. Join the Independence Wave | **PARTIAL.** The exact-package frozen transaction, decline/cooldown, failure receipt, history, and human continuation path are source-present. Two coverage gaps mean the implementation does not guarantee an offer to every qualifying severely reduced country. | **PASS for current localisation, Event Details, history payloads, docs, workbook, and CSV wording.** The accepted specs/source-of-truth promotion is still missing. | **PARTIAL.** Current event inspect/render resolved the chain with zero focused blockers, but large-workspace passes were deferred. The cached baseline required for `event_compare` is unavailable. | **PARTIAL, not complete.** The main transaction exists; first-observation baseline reconstruction and shared-coordinator contention can miss otherwise qualifying countries. Weighted-option evidence is recorded in the probability addendum below. |

## A. Grouped formable state-puzzle workflow

### Requirements that pass

1. **Fourteen current consumer definitions are grouped.** The consumers `docs/formables/state_registry/consumers/006_form01_state_puzzle.json` through the current FORM-48 subset all declare `group_id = independence_wave_formables`, `group_scripted_gui_id = independence_wave_formable_state_puzzle_scripted_gui`, `group_window_id = chaosx_independence_wave_formable_state_puzzle_window`, a family activation helper, and a finite summary count. The current set is FORM-01, 02, 03, 04, 05, 07, 08, 09, 12, 13, 16, 18, 39, and 48. The accepted matrix still contains 48 design rows; this addition covers the fourteen currently exposed/runtime-authored families, not the other 34 planned rows.
2. **One grouped decision-category binding exists.** `common/scripted_guis/chaosx_formable_state_puzzles.txt:1372-1590` declares the grouped binding with `context_type = decision_category`, the shared window, `is_ai = no`, the fourteen activation helpers, matching overlay properties, and `ai_enabled = { always = no }`. `interface/chaosx_formable_state_puzzle_group_independence_wave_formables.gui:8-699` contains one window with fourteen family overlays.
3. **All seventeen current category attachments are source-present.** Direct parsing found exactly seventeen top-level categories with `scripted_gui = independence_wave_formable_state_puzzle_scripted_gui` across eleven Event 006 category files: discovery, transaction, shared membership, FORM-01/02/03/04/05/08/09, Middle Volga, Council of Communities, FORM-16, both FORM-39 categories, and both FORM-48 categories. This agrees with `docs/events/006_independence_wave/systems/formable_registry.md:272` and the settled category audit.
4. **One authoritative territory contract gates presentation and execution.** `common/scripted_triggers/006_independence_wave_formable_state_puzzle_triggers.txt:14-345` defines fourteen activation helpers, fifty finite state wrappers, and fourteen family territory helpers. `common/scripted_triggers/006_independence_wave_formable_registry_triggers.txt:631-716` requires `has_independence_wave_selected_formable_state_puzzle_territory` before the generic formation transaction. `common/decisions/006_independence_wave_decisions.txt:3418-3435` uses that trigger for availability/cost and retains the AI willingness block. FORM-05's separate path calls its same territory helper through `common/scripted_triggers/006_independence_wave_form05_triggers.txt:489-494` and `common/decisions/006_independence_wave_form05_decisions.txt:262-300`.
5. **Invitation precedence is fail-closed.** Every family activation checks a live pending invitation first and puts selected/profile/post-formation alternatives behind `NOT = { has_pending_independence_wave_formable_founding_invitation = yes }`. FORM-12, FORM-13, and FORM-18 state rows require frozen accepted invitations; FORM-03 state 6 remains the documented Belgian delegation rather than a transfer fallback.
6. **Generated assets are complete and internally reproducible.** Fourteen manifests contain 50 state rows and 100 unresolved/qualifying asset rows. A fresh read-only audit found 100/100 source PNGs, processed PNGs, and DDS files present; all recorded PNG and DDS SHA-256 values match; all DDS files decode; and every decoded DDS is pixel-identical to its processed PNG. The 43 unique installed state IDs match the manifests.
7. **Localisation coverage passes.** The generated window references fourteen summary keys and all are defined. The fifty Event 006 hover keys, shared owner/controller/core/status functions, and generated count/status helpers are present. `localisation/english/chaosx_formable_state_puzzles_l_english.yml` has a UTF-8 BOM. The settled localisation handoff confirms all fifty Event 006 hovers use `Formation status` and no longer use the over-narrow `Required control` label.
8. **No dedicated event-UI-worker blocker applies.** This is the shared generated formable state-registry consumer window, not a dedicated mechanic window introduced and owned by one event. The `chaosx_event_ui_worker` requirement expressly excludes shared registries. No 3D unit, unit-audio, counter, portrait, super-event, or new sourced visual request belongs to this addition.

### Missing, simplified, or blocked requirements

1. **FORM-08 cannot ever satisfy its displayed contract.** The consumer displays states 84 and 82 with a required count of three, while `independence_wave_formable_state_puzzle_form08_territory = { always = no }` at `common/scripted_triggers/006_independence_wave_formable_state_puzzle_triggers.txt:276-280`. This is openly documented and avoids inventing a third state, but it remains an incomplete formable rather than a completed workflow outcome.
2. **FORM-07 and FORM-48 remain operationally unreachable.** Their UI, state helpers, and category attachments exist, but their accepted identity/package readiness remains fail-closed. This is a deliberate design gate, not an undisclosed fallback. It prevents the broad statement that all currently exposed formables are gameplay-complete.
3. **Family-isolated GUI acceptance is missing.** Fresh `hoi4.gui_inspect` returned `GUI_INSPECTED` for `chaosx_independence_wave_formable_state_puzzle_window`, but the selected offline scenario rendered mutually exclusive overlays together. It reported 1,756 visible-overlap diagnostics before truncation, 14 missing and 15 unresolved fidelity items, 192 invalid scripted-context diagnostics in the truncated global set, and workspace-wide source collisions. The linked artifact retains the bulk evidence, but the route cannot isolate FORM-01 through FORM-48 activation helpers. Fresh `hoi4.gui_render` covered 1920x1080 and 1366x768 with normal, hover, disabled, active, long-text, and missing-localisation states, yet it still produced one aggregate SVG. There is no clean per-family unresolved/qualifying/optional-hidden/long-text comparison, hierarchy proof, or tight hover-region proof. Source mutual exclusion is not equivalent visual evidence.
4. **The durable category crosswalk is less complete than the state-puzzle template contract.** The existing category audit lists the seventeen category IDs and one shared artifact, but it does not give a per-row manifest path/category identity/context/artifact result. Direct source parsing closes the attachment count for this audit, not the absent family-by-family consumer proof.
5. **The overview is stale.** `docs/events/006_independence_wave/overview.md:15` says fifteen categories. The current source, formable-registry document, and two settled audits all prove seventeen.
6. **Accepted-design promotion is missing.** Neither `docs/specs/006_independence_wave_specs/` nor `docs/plans/006_independence_wave_plans/006_source_of_truth_map.md` mentions the grouped state-puzzle addition. The implementation handoffs therefore remain working-plan evidence instead of a promoted accepted-spec requirement/disposition.

### Map and GUI evidence

- `hoi4.map_inspect` inspected all 43 manifest state IDs at map revision `cb98dccb88bf94091d86a67ca2013b9dd452aebc99fdabba8d6df64338eee69d`: `hoi4-agent://workspace/mod_chaos_redux_ea3b2d67c2c0/artifact/67fb709820f6c9dab6f752114fb4fb0d53f1332750a209f34e76f34e36633408/604e738a7865f3c6024326eba7ad40da42a9aedfafc5c0ca2d42fba8f5901035/map-inspect.cb98dccb88bf9409.json`. State/region membership and bitmap geometry checks passed. The global map inspector also reported truncated installed-map building/port-position errors outside the selected Event 006 geometry, so its aggregate validation is false.
- `hoi4.map_render` produced the current state layer with coast, port, and victory-point overlays at the same revision: `hoi4-agent://workspace/mod_chaos_redux_ea3b2d67c2c0/artifact/24f66076c537b9030cd14c38066bd763d40b1462f65a5872796ceec4222530d9/9deb4e439e3d92d112a5f7aa1c12deece9210b19bf13ab09ab07cb5d642d0d58/map-state.png`.
- Fresh grouped GUI inspect: `hoi4-agent://workspace/mod_chaos_redux_ea3b2d67c2c0/artifact/cd983b420d9ccd75f2ea8606014bf267c470a946bb72ad0e547c484391515115/0ea44f233c0cc88939427ba1b45e163ed0989ff1f12994731a10a890c1704860/gui-inspect.9b9b463db38aa280.json`.
- Fresh grouped GUI render: `hoi4-agent://workspace/mod_chaos_redux_ea3b2d67c2c0/artifact/e5cf4a0e6908e754ba9e1bae29b05ae013092399118afcfe42cedf62d2833fe9/1e7656005c68675c1fdf3bbdcde7811991528aec6bfea0492ddc948204d53cc0/chaosx_independence_wave_formable_state_puzzle_w-full.svg`.

## B. Join the Independence Wave

### Requirements that pass

1. **Scoped discovery exists without a world scan.** `common/on_actions/006_independence_wave_join_on_actions.txt:10-50` observes both belligerents on war relation creation and re-evaluates affected country scopes on state-control, peace-conference, capitulation, and release callbacks. No daily/weekly/monthly/on-start world iteration was added.
2. **The public threshold is centralized and implemented.** `common/script_constants/006_independence_wave_constants.txt:21-42` sets 50%, at least two states lost, a 90-day refusal cooldown, and event/history IDs. `common/scripted_effects/006_independence_wave_join_effects.txt:36-73` calculates the peak/current/loss/scaled values. `common/scripted_triggers/006_independence_wave_join_triggers.txt:9-45` requires a living independent country at peace, excludes Event 005/Event 006/registry/Event 12 priority carriers and active join state, requires remaining territory, at least two states lost, and `owned * 100 <= peak * 50`.
3. **All currently attested packages are probed.** `common/scripted_effects/006_independence_wave_join_effects.txt:184-216` explicitly probes 27 package IDs. A fresh `python -B .tools/audit_event6_allocator.py` pass reports exactly 27 attested packages across 25 compatible reservation groups, so the fixed list is current on this filesystem.
4. **Exact remaining-territory proof exists.** `independence_wave_join_try_package` compares the new reservation-row count with current owned-state count and runs `every_owned_state` to reject any unreserved owned state (`common/scripted_effects/006_independence_wave_join_effects.txt:101-180`). The package reservation wrappers remain authoritative for identity, anchor, territory, and setup.
5. **The zero-host exception is narrowly tied to the join plan.** The join conversion flag and plan ID are established before package probing. The shared package-planner and release validators accept equality only for that active plan; ordinary releases still require a surviving host. The current-plan trigger verifies owner, source, allocating phase, selected count, and package ID.
6. **Offer, accept, decline, expiry, and failure events are wired.** `events/006_independence_wave_join.txt:8-49` defines visible `.36`, hidden `.37/.38`, and failure `.39`. The decline option is first, so the 20-day timeout follows the fail-safe refusal path. Acceptance revalidates, locks, invokes the normal frozen release executor, requires the committed phase, and switches a human player only after setup/validation/commit (`common/scripted_effects/006_independence_wave_join_effects.txt:306-385`). Decline clears only a matching plan and applies the dynamic cooldown (`:387-408`). Failure uses the existing compensating rollback when execution started.
7. **Event Log/history coverage is present.** Offer, accepted, declined, and failed payload constants feed `record_events_log_system_history_entry` (`common/scripted_effects/006_independence_wave_join_effects.txt:218-242`). `common/scripted_localisation/chaosx_scripted_localisation_events_log.txt:6110-6137` maps all four payloads, and `localisation/english/006_independence_wave_join_l_english.yml:10-13` defines the history text.
8. **Player-facing alignment passes.** The report, Event Details (`localisation/english/chaosx_gui_l_english.yml:956`), `docs/events/006_independence_wave/join_wave.md`, workbook `Events!C7`, and regenerated events CSV all state the 50% plus two-state plus exact-homeland contract. The workbook and current CSV Event 6 row match and retain `Partially Available`. Current events CSV SHA-256 is `0c309f8bea4b45136b38d99c52eecaadfb197b493e49ea00142a787ad7fbc0f8`; the earlier catalog handoff's `e1be...` hash is superseded, not current evidence.
9. **Asset reuse is valid.** `.36` and `.39` reuse `GFX_report_event_006_asset_001_wave_summary`, defined in `interface/006_independence_wave_event_pictures.gfx:2-5`, present at `gfx/event_pictures/006_independence_wave/report_event_006_asset_001_wave_summary.dds`, and documented in the Event 006 generated-scenes manifest. No new art, portrait, animation, 3D unit, counter, or audio package is required by this addition.

### Missing, simplified, or blocked requirements

1. **A busy shared release coordinator can permanently miss an otherwise valid offer.** `independence_wave_join_offer_if_eligible` rejects the callback when any release plan is valid/active (`common/scripted_effects/006_independence_wave_join_effects.txt:246-264`). The only callers are the bounded callbacks in `common/on_actions/006_independence_wave_join_on_actions.txt`; there is no queue, pending-retry flag, or later retry callback for coordinator contention. If the country's last qualifying callback occurs while another Event 005/006 release plan is active, the country receives no guaranteed later offer. This is a functional coverage gap for “severely reduced countries,” not merely missing evidence.
2. **The first post-loss baseline is a proxy, not necessarily the former owned-state count.** If war-entry observation exists, the peak owned-state value is exact. Otherwise `independence_wave_join_update_peak_baseline` reconstructs the baseline as the larger of current territory and total cores (`common/scripted_effects/006_independence_wave_join_effects.txt:32-73`). A country that formerly held substantial non-core territory can be severely reduced without the proxy reaching its true former footprint, producing a false negative. This simplification is disclosed in `join_wave.md`, but the public wording says “former states” without describing the proxy.
3. **The package list is manual maintenance.** It is complete for the current 27 attested packages, but `independence_wave_join_probe_attested_package` must be edited when a new package is admitted. The implementation handoff discloses this limitation. It is not a current mismatch, but it is a future-staleness risk.
4. **The report holds the global release coordinator open for up to twenty days.** The plan remains in `allocating` while `.36` waits. That preserves frozen evidence but serializes unrelated shared release work. No bounded contention scenario or queue behavior has been validated.
5. **A stale-plan refusal/failure history row can lack the secondary actor.** The decline path rebuilds `independence_wave_join_target` only if the current-plan predicate still passes, but records the declined payload regardless (`common/scripted_effects/006_independence_wave_join_effects.txt:387-405`). The history localisation always names the secondary actor. Shared single-flight design makes this an edge case, but no explicit fallback text or scenario proof exists.
6. **Accepted-design promotion is missing.** No accepted Event 006 spec, spec matrix, source-of-truth map, or resume authority mentions Join the Independence Wave. The runtime, event docs, Event Details, and catalog are aligned, but the accepted design source remains stale.
7. **No complete transaction scenario evidence exists.** The implementation handoff is source-based. There is no engine-backed scenario proving exact acceptance, human continuation, AI continuation, timeout, refusal, coordinator contention, stale-plan response, pre-mutation failure, compensating rollback, or post-finalization failure. Repository policy assigns live consumer validation to the user; this audit does not substitute source inspection for it.

### Event MCP evidence and comparison blocker

- Fresh `hoi4.event_inspect(mode = trace)` on `chaosx.nr6.36` returned `EVENT_INSPECTED_PARTIAL` at revision `550da12aba6a66219fd9c18d562b6ffe63db4870cd36848223b6f0e39744cbd4`: `hoi4-agent://workspace/mod_chaos_redux_ea3b2d67c2c0/artifact/c7f6c2ea67af1b35b806eb3bca179104d54d7518d94128796c12d0e466d46d15/b4e644b2ee48f37bb1aef7295b9341224f4b057ed22f3902f734816cae6b5021/event-trace-550da12aba6a.json`. The full workspace graph has fourteen blocking diagnostics, while the focused render below reports zero blocking diagnostics; large helper/lifecycle projections remain deferred.
- Fresh `hoi4.event_render(view = options)` on `.36` returned `EVENT_RENDERED_PARTIAL`, five selected nodes, and zero focused blocking diagnostics: `hoi4-agent://workspace/mod_chaos_redux_ea3b2d67c2c0/artifact/ed4e0544b04f43871d7fde78307e70ecd6c02e541a1681927b566c18647fad0d/b5e62aa1a035a00ab9e63352a03ba7697ec9ce4b10dd7595acd84a5f00ee7f70/event-options-550da12aba6a.svg`.
- For the state-puzzle addition's owning Event 006 root, `hoi4.event_inspect(mode = state_flow)` and `hoi4.event_render(view = overview)` on `chaosx.nr6.1` returned focused partial evidence with zero blocking diagnostics. The event graph does not project decision-category/helper semantics, so this is event-chain coverage only: `hoi4-agent://workspace/mod_chaos_redux_ea3b2d67c2c0/artifact/4663c313ccf5001f09eab48410a20401a612fb61cffbd1d15501732eabbec2e0/796dfb93b0ca64cf79b251b2f4d3fce7c18c81cc2366702047901d2cbed1b913/event-state_flow-550da12aba6a.json` and `hoi4-agent://workspace/mod_chaos_redux_ea3b2d67c2c0/artifact/df9d473623bf996aeed77371b77422d5f5607c27f2d0ce4efa4b827d3177838a/6bb5a1ff5d5af7dd402d86c1fbab46677605e33b1fc897d323e3a69717775c75/event-overview-550da12aba6a.svg`.
- `hoi4.event_compare` was attempted between the last documented pre-addition Event 006 graph revision `be8a459e712970f13eb790070ee12b3a049d0cca9adde963a523ddcd7e71f529` and current revision `550da12aba6a66219fd9c18d562b6ffe63db4870cd36848223b6f0e39744cbd4`. It returned `EVENT_REVISION_NOT_CACHED` with the exact blocker `Requested event graph revision is not cached`. Source diff review is not equivalent compare evidence.

### Probability evidence

- The required weighted-surface pass was routed through `chaosx_ai_probability_auditor`. Exact inspection of the visible `.36` option pool `{ chaosx.nr6.36.b, chaosx.nr6.36.a }` found two candidates, zero unresolved candidates, and no explicit `ai_chance`; the engine therefore assigns raw weight 1 to each option. The current evaluation under `E6_JOIN_EVENT_OPTIONS_CURRENT_2026_08_09` resolves the two options to `1/2` and `1/2` for each adapter scenario: `hoi4-agent://workspace/mod_chaos_redux_ea3b2d67c2c0/artifact/8e230aefc0bd5583cfda804284d4b6280eab91360173422e694ca4268526ea91/7d0503cea8373501455b85433fdd3e1521add96053a0cb96e43a1761929a7b04/probability-6f69065b3162dfa8b9977bef.json` (scenario hash `030f5aaf6ac39ddaefe6ede2234ca78ba83621b48321f23abfb3edea9f1e2a0b`).
- That `1/2` split applies to an eligible AI country that reaches the report: accept `.a` fires `.37`, while decline `.b` fires `.38`. It is the current engine-default policy, not an explicitly tuned or accepted-spec target. This is not by itself a conversion blocker, but it should be treated as intentional 50/50 behavior before completion is claimed.
- Human timeout is not a random 50/50 selection. Because `.b` is the first source option and `timeout_days = 20`, timeout selects decline and the cooldown path with effective probability 1. Human clicks are manual and outside the AI-option distribution. The current structural render confirms `.b` at index 0 leading to `.38` and `.a` at index 1 leading to `.37`: `hoi4-agent://workspace/mod_chaos_redux_ea3b2d67c2c0/artifact/0b42b662917420156463ac613796770c2697290c6e8a79d4a8dd42ccced75aa1/9f3572efa81de97e39e010162af2cb6f3ac287400b3728a59bfc17548fd9b499/event-options-04d30e623b4e.json`.
- The adapter's stale/ineligible scenario also displays `1/2` versus `1/2`, but it does not model the root event trigger. Source requires a pending offer and current plan, so the row is not runtime reachability evidence and does not close the stale-plan scenario gap.
- The generic registry proclamation decision and FORM-05's dedicated proclamation use their same family territory helpers in `available`/commit gates, so their AI score cannot bypass the state-puzzle proof in source. The generic decision reduces willingness when its pursue helper is false; FORM-05 adds an urgent severe-threat modifier. Current probability evaluation/comparison remained partial because the adapters could not resolve the large trigger-input surface, so it provides structural consistency rather than quantitative balance proof. The join package probe itself is a deterministic first-success chain over 27 IDs, not a `random_list`, direct-random, or declared custom weighted pool.

## Accepted-plan disposition

- The grouped compiler/generator architecture, gameplay-integration handoff, category audit, settled filesystem audit, join implementation handoff, settled localisation audit, and catalog handoff all have corresponding current source or documentation.
- The early category count and early catalog SHA evidence are superseded by the settled filesystem: seventeen categories and events CSV `0c309f...` are current.
- Neither addition has been promoted into the accepted spec package or the Event 006 source-of-truth map. Their current disposition is therefore **implemented working-plan material awaiting promotion**, with the functional and evidence blockers above retained rather than hidden as future polish.

## Recommended next actions

1. Promote both accepted additions, their exact scopes, exclusions, and deliberate fail-closed cases into the appropriate Event 006 specs and `006_source_of_truth_map.md`.
2. Correct `overview.md:15` from fifteen to seventeen attached categories.
3. Decide whether FORM-07/08/48 should remain exposed-but-blocked; FORM-08 needs an approved third member/state or an explicit permanent unavailable disposition.
4. Add registered family-isolated GUI scenarios and rerun inspect/render for each family at both supported resolutions, including unresolved, qualifying, optional-hidden where applicable, hover/click regions, long text, and missing localisation.
5. Add a bounded retry/queue policy for a qualifying join source when the release coordinator is busy, without introducing a world scan.
6. Decide whether the non-core first-observation false-negative is accepted. If not, add a scoped earlier baseline producer rather than weakening the 50% threshold.
7. Add source-owned deterministic scenario receipts for accept, AI path, timeout/decline, stale plan, contention, rollback, and finalization failure; rerun `event_compare` when a baseline is cached.

## Simplification statement

No fallback was invented by this audit. The implementation itself contains disclosed or source-visible simplifications: FORM-08 is impossible at 2-of-3, FORM-07/48 are fail-closed, the join package set is explicitly enumerated, first-observation loss uses a core-footprint proxy, and coordinator contention has no guaranteed retry. Those items prevent a full completion claim.
