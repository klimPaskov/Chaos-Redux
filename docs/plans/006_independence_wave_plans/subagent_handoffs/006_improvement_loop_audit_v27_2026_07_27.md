# Event 006 Improvement-Loop Audit v27

Date: 2026-07-27

Event: `006` — Independence Wave

Mode: read-only improvement-loop retry and bounded planning handoff

Whole-event disposition: **HOLD / PARTIAL**

Expansion disposition: **STOP broad expansion; execute the bounded closure addendum below**

## Executive disposition

Event 006 already has sufficient breadth across independence packages, focus routes, decisions, the Independence League, formables, achievements, SCN-008, scripted GUI, and super-event surfaces.

Another country, route, formable, decision family, scripted GUI surface, achievement family, super-event, or visual layer would add review debt without closing the current player-facing risks.

The useful improvement-loop action is therefore not another content expansion.

It is a bounded closure addendum that converts the current accepted design into finite runtime, reachability, research, and documentation gates.

The event remains **HOLD / PARTIAL** until those gates have evidence.

This is not a completion handoff because material acceptance blockers remain.

## Retry and tooling disposition

This audit is the requested retry after an earlier Meshy initialization failure.

Meshy was not used again as a production dependency because the retry was explicitly bounded to read-only repository, specification, skill, and current v27 authority evidence.

No image, model, texture, animation, portrait, icon, or other asset was generated or modified.

The earlier Meshy failure does not weaken this planning result because none of the recommended closure work requires asset production.

The installed HOI4 tooling package has no Technology Tree Viewer.

No technology or doctrine change is proposed, so no technology-tree evidence is required for this addendum and no substitute viewer was inferred.

## Previous addendum disposition

The parent instruction establishes the following implementation addenda as completed:

- `IW-043` / `IW-058`
- `IW-093` / `IW-098`

Their design loops are **CLOSED FOR IMPROVEMENT-LOOP PURPOSES**.

Do not produce another country-package, route, formable, visual, or research addendum for those four packages in response to this audit.

Their ordinary participation in whole-event runtime, focus reachability, achievement, AI, or save/load tests does not reopen their design addenda.

No prior addendum in those two families remains unresolved for this planner.

Other current HOLD items identified below remain queued because their acceptance or research conditions are still unresolved.

## Current authority and evidence basis

This disposition follows the current repository authority rather than inventing another design layer.

Primary current evidence:

- `docs/plans/006_independence_wave_plans/subagent_handoffs/006_event_completion_audit_v23_2026_07_27.md`
- `docs/plans/006_independence_wave_plans/subagent_handoffs/006_documentation_curator_reconciliation_v28_2026_07_27.md`
- `docs/plans/006_independence_wave_plans/subagent_handoffs/006_focus_audit_v17_2026_07_27.md`
- `docs/plans/006_independence_wave_plans/subagent_handoffs/006_decision_mission_transaction_audit_v27_2026_07_27.md`
- `docs/plans/006_independence_wave_plans/subagent_handoffs/006_catalog_audit_v27_2026_07_27.md`
- `docs/plans/006_independence_wave_plans/subagent_handoffs/006_parent_achievement_system_audit_2026_07_22.md`
- `docs/plans/006_independence_wave_plans/subagent_handoffs/2026-07-22_event006_league_dm42_dm62_decision_mission_audit.md`
- `docs/plans/006_independence_wave_plans/subagent_handoffs/006_iw157_iw178_new_guinea_source_research_2026_07_26.md`
- `docs/plans/006_independence_wave_plans/subagent_handoffs/006_super_event_audio_research_v22_2026_07_27.md`
- `docs/plans/006_independence_wave_plans/subagent_handoffs/006_source_of_truth_map.md`
- `docs/specs/006_independence_wave_specs/specs/006_independence_wave_spec_part_6_formables_league_and_scenario.md`
- `docs/specs/006_independence_wave_specs/specs/006_independence_wave_spec_part_7_ai_balance_assets_and_acceptance.md`
- `docs/specs/006_independence_wave_specs/matrices/006_achievement_matrix.csv`
- current SCN-008 constants, triggers, effects, scripted localisation, and catalog text

The accepted source state contains substantial implementation, but current authority still lacks enough runtime evidence to promote the event, the Liberations cluster, or SCN-008 from their present statuses.

## Design problem

The remaining gap is not a shortage of content.

It is the absence of one finite, cross-system acceptance ledger that proves:

1. the allocator and accepted decision matrix behave correctly across ordinary and collision cases;
2. every intended focus route is actually reachable without destructive tree replacement or geometric blockers;
3. every selectable SCN-008 mode works at every intensity;
4. league transactions and their achievement witnesses remain truthful through success, failure, rollback, and save/load;
5. research-gated packages and rights-gated media remain fail-closed until their exact evidence is accepted.

Those five points are the closure addendum.

## Closure addendum A — current runtime acceptance matrix

### A1. Allocator and release transaction matrix

Create one row-level evidence ledger for the current allocator rather than another prose-only audit.

At minimum, the ledger must disposition:

| Matrix family | Required rows |
| --- | --- |
| Ordinary wave bands | successful planned counts at `3`, `4`, `5`, and `7` where the current candidate pool supports them |
| Exact-ten baseline | one accepted exact-ten order with the expected package ledger |
| Exact-ten reverse order | the opposite order for the `IW-008` / `IW-010` Rhine-Saar pair |
| Same-GER-host remnant | both pair orders while the same German host must retain a safe remnant |
| Split-host | the paired packages sourced from different valid hosts where current setup permits it |
| Protected anchors | state `51` and state `42` protected-state behavior in both relevant orders |
| Host exhaustion | one-safe-remnant and no-safe-remnant rejection cases |
| Event 005 collision | living origin, reserved anchor, already transferred homeland, and overlapping reservation cases |
| Transaction failure | lock, stale plan, execution failure, rollback, and retry behavior |
| Persistence | save/load before commit, after commit, and after a rejected or rolled-back attempt |
| World Collapse | coexistence and rejection behavior under the current World Collapse interaction |

The exact-ten source-capacity result is already accepted.

Do not reopen package-capacity design or generalize the Rhine-Saar capacity rule beyond its admitted pair.

The missing evidence is execution truth, order invariance, host-remnant safety, collision safety, and persistence.

Each ledger row must record setup, selected packages, accepted packages, rejected packages, rejection reasons, host remnant, transferred states, final ownership/controller, transaction state, retry result, and evidence status.

### A2. Accepted decision and mission matrix

The accepted decision/mission matrix contains 80 rows, but current authority does not contain a complete row-by-row runtime disposition.

The parent should extend the existing matrix or add an adjacent evidence ledger with exactly one status for every accepted row:

- source-only pass;
- runtime pass;
- blocked by a named upstream gate;
- intentionally unreachable with accepted reason;
- failed and returned for implementation.

Priority runtime families are:

- `DM-58` valid target, invalid target, target collision, owner/controller change, no-witness, AI selection, callback, cleanup, and save/load;
- League contribution, arbitration, rescue, expulsion, leadership challenge, dissolution, rival-bloc transition, and war mandate;
- `FORM-39` fail-closed admission and rejection reasons;
- allocator-dependent releases and collisions;
- focus-to-decision reveal, activation, timeout, completion, and cleanup;
- AI selection under valid and invalid resource states.

Do not add a replacement decision to cover a failed row.

Fix or explicitly reject the existing row.

### A3. Runtime evidence ownership

The main agent owns source review, setup documentation, result comparison, and final acceptance.

Live consumer validation remains with the user.

The ledger must distinguish source inspection from live result evidence so a source-only pass cannot be mistaken for runtime proof.

## Closure addendum B — focus reachability and geometry

### B1. Preserve the existing route design

The current tree has 184 focuses and 223 connectors.

The read-only focus audit found 49 connector crossings, 18 node intersections, 27 long connectors, and 14 blocking geometry findings.

All major route families exist, so no new focus family is justified.

The parent should resolve the 14 blockers as coupled connector clusters rather than moving isolated nodes without reviewing dependent branches.

After each coupled reflow, preserve:

- prerequisite meaning;
- mutual-exclusion meaning;
- route locks;
- decision reveal timing;
- country identity;
- AI route intent;
- existing meaningful country trees.

### B2. Make additive content reachable

The shared additive overlay contains eight focuses, but no current tree imports its root `independence_wave_overlay_take_stock_of_independence`.

`FSM` and `FIJ` set additive flags without a demonstrated load or insertion path.

The accepted post-formation overlay mode also lacks a demonstrated producer callsite.

The implementation review must choose and document one truthful disposition for each carrier:

1. a reviewed shared-focus insertion path that preserves the carrier's existing meaningful tree;
2. an accepted per-carrier connection using existing focus content;
3. explicit non-participation with the unreachable feature and its flags removed.

Do not load a duplicate vanilla `generic_focus` tree.

Do not replace a meaningful carrier tree as a shortcut.

Do not keep an additive or post-formation mode that has no producer.

### B3. Route reachability matrix

Create a deterministic reachability matrix covering:

- survival;
- government;
- economy;
- army/security;
- diplomacy;
- former-host/expansion;
- network/league;
- formable/high-chaos;
- package-specific modules;
- shared additive overlay;
- post-formation overlay;
- all admitted regional-formable families;
- `FORM-48` and any current carrier/member admission gate;
- focus-to-decision and focus-to-mission links;
- mutually exclusive route ends;
- AI-selected paths.

For every route end, record the entry node, prerequisites, mutually exclusive nodes, required flags/variables, decision consumers, reachable carrier set, AI weight condition, and a result.

### B4. Required reinspection

After source work, rerun read-only focus inspection, render, and lint.

Use weighted-logic inspection for representative AI route scenarios rather than treating the existence of `ai_will_do` as evidence that the AI can reach the route.

The acceptance state is:

- zero blocking node intersections;
- zero blocking connector crossings;
- no unproduced overlay mode;
- no flag-only additive carrier;
- no destructive tree replacement;
- every accepted route end reachable by at least one admitted carrier;
- every AI-usable route has a valid weighted scenario.

## Closure addendum C — SCN-008 current mode matrix

### C1. Correct the matrix cardinality

Current source has six numeric scenario families:

1. Sovereign Scatter;
2. Common Congress;
3. Wars of Separation;
4. Universal Belligerence;
5. Patron Worlds;
6. Great Partition.

However, Universal Belligerence has three independently selectable and separately localized rules:

1. Former Hosts;
2. Neighboring Releases;
3. Nearby Nonleague States.

The selector deliberately walks those three rules before advancing to the next family.

The catalog therefore truthfully exposes eight player-facing selectable modes.

The current full runtime matrix is **eight selectable modes by four intensities, or 32 cells**.

The older `6 x 4 = 24` shorthand is insufficient for runtime acceptance because it collapses three materially different belligerence target rules into one family cell.

No new scenario type is proposed.

This is a test-matrix correction based on current source and current catalog text.

### C2. Required 32-cell evidence

For each of the eight selectable modes at Low, Medium, High, and Maximum, record:

- requested mode and intensity;
- committed numeric family and belligerence rule;
- candidate count;
- accepted count;
- blocked count;
- per-package rejection reasons;
- protected hosts and retained remnants;
- transferred territory;
- starting force and institutional tuning;
- league setup;
- patron setup;
- former-host or bounded belligerence setup;
- partition setup;
- war declarations and targets;
- summary text;
- achievement eligibility/disqualification state;
- retry, rollback, and save/load outcome.

Universal Belligerence must have three separate target-selection result columns.

Do not accept one rule as a proxy for the other two.

### C3. Collision and deterministic sweeps

In addition to the 32 primary cells, run bounded sweeps for:

- Event 005 origin and anchor collisions;
- living or already released origins;
- unsafe or exhausted hosts;
- unique-anchor conflict;
- invalid current-map binding;
- insufficient candidate pool;
- repeated execution;
- selector wrap in both directions;
- save/load between selection and execution;
- save/load after a committed summary;
- representative deterministic seeds where weighted target choice exists.

The scenario must remain direct and inspectable.

It must not silently require an unrelated chaos band, evolution, date, or manual state edit to launch.

The summary must explain blocked packages with current rejection reasons.

Keep SCN-008 as `Needs Testing` until all 32 cells and the bounded collision/persistence sweep are dispositioned.

## Closure addendum D — League and achievement truth

### D1. No new League layer

The League already has a deep accepted design: formation, charter values, contributions, arbitration, rescue, expulsion, leadership contest, war mandate, rival pressure, and dissolution/failure paths.

Another League currency, office, charter pillar, congress route, or decision family would be bloat.

The improvement needed is a finite transaction matrix for the existing system.

### D2. League runtime matrix

Record complete success, refusal, expiry, cancellation, invalidation, AI, cleanup, exploit, and save/load evidence for:

- network-to-League formation;
- founder count and willingness;
- contribution acceptance and reserve movement;
- rescue request, valid response, refusal, abandonment, and completion;
- arbitration start, agreement, refusal, breach, and completion;
- factual expulsion grounds, target persistence, vote/result, and aftermath;
- leadership challenge, incumbent/challenger invalidation, and succession;
- dissolution and post-dissolution cleanup;
- rival-bloc creation or explicit non-creation under the accepted rule;
- faction war and war-mandate targeting;
- cohesion, common cause, patron capture, shared reserve, and member confidence;
- member annexation, puppetry, host proxy, and sponsor change;
- rollback and save/load across every timed transaction;
- repeated-click, duplicate-target, stale-target, and insufficient-resource exploits.

Every result must name the flags, variables, targets, costs, cooldowns, achievement witnesses, and cleanup state that should remain.

### D3. Achievement reachability matrix

The current achievement surface contains 16 definitions and 48 DDS icon files.

Do not add another achievement or icon family.

Reconcile the current writers once before runtime testing because the earlier achievement audit predates later League transaction work.

Then disposition for every achievement:

- qualifying path;
- at least one near-miss;
- each accepted disqualifier;
- blocked-path tooltip;
- carrier and route reachability;
- proof/witness persistence;
- save/load;
- scenario interaction;
- AI or player ownership assumptions.

League-linked achievements require explicit runtime evidence for founding, cross-regional membership, rescue, and arbitration.

Scenario survival requires a valid SCN-008 cell and its disqualification rules.

Regional-formable, Volga Bulgaria, Assyria, and radical-bloc achievements must use currently admitted carrier and route truth.

The completed `IW-043` / `IW-058` and `IW-093` / `IW-098` design addenda must not be reopened merely to make an achievement easier.

If an achievement has no currently admitted qualifying path, retain the HOLD and fix the existing carrier/reachability chain or explicitly queue the achievement for rejection.

Do not introduce a fallback carrier.

## Closure addendum E — research and rights gates

### E1. `IW-157` and `IW-178`

Keep both packages fail-closed.

`IW-157` still needs:

- exact Yapen/Serui community-to-HOI4-state containment;
- a rights-clear, period-suitable named adult male leader or authentic institutional alternative;
- an exact historically attributable local symbol;
- explicit disposition of later or reconstructed Koreri/Biak symbolism.

`IW-178` still needs:

- exact Dogura/Milne Bay or Motu-Koitabu/Hanuabada community-to-HOI4-state containment;
- a rights-clear, period-suitable named adult male leader or authentic institutional alternative;
- an exact community symbol with source and attribution;
- explicit separation of later national biography from a valid 1936 carrier.

The Biak/Yapen/Koreri, Motu-Koitabu/Hiri/lakatoi, Dogura/Milne Bay, and Hanuabada leads are research directions, not accepted facts for implementation until exact attribution and state containment are proved.

Do not use a generic pan-Papuan identity, modern reenactment, generated depiction of a real person, broad carrier substitution, or unsourced reconstructed flag.

### E2. `FORM-39`, MFX, and admission evidence

Keep `FORM-39` fail-closed while the MFX review flag and the six writer-dependent evidence gates remain unresolved.

The parent should produce a row ledger for each admission input:

- intended writer;
- source evidence;
- carrier;
- trigger;
- runtime witness;
- cleanup;
- admission or rejection result.

MFX must receive an explicit accepted/rejected/needs-more-research disposition.

Do not infer admission from the presence of a package, flag, portrait, or formable shell.

### E3. `FORM-48`, HAW, and FSM reachability

Current authority describes `FORM-48` as implemented but unreachable through a compliant carrier/member chain, with `HAW` and `FSM` not admitted.

Resolve this through the reachability and evidence ledgers.

Do not create a fallback carrier or bypass membership evidence.

If no compliant carrier exists after review, keep the feature unreachable and queue a design-level accept/reject decision instead of silently weakening the gate.

### E4. Super-event 6001 audio

Keep super-event `6001` audio blocked pending an explicit user rights/tone choice or an accepted United States rights waiver for the preferred source.

The existing organ and harpsichord candidates do not automatically satisfy the accepted brass-and-organ tone.

Do not select one as a fallback.

No audio asset should be created or wired from this audit.

Super-event `6002` still needs its existing predicate/playback acceptance evidence, but it does not need another concept or visual family.

## AI and balance attachment

The accepted AI design describes 24 representative profiles.

Do not add a second AI taxonomy.

Attach those profiles to the finite matrices above and require at least one valid and one invalid-resource scenario for each AI-usable system:

- route selection;
- League formation and membership;
- contributions;
- rescue;
- arbitration;
- expulsion;
- leadership challenge;
- war mandate;
- formable pursuit;
- scenario behavior.

Record actual eligibility, selected weight branch, selected action, resource result, and cleanup.

The result must show that the AI can reach the content, afford it without self-destruction, refuse it under invalid conditions, and avoid repeated or stale transactions.

Balance promotion requires campaign-level evidence for fragile, viable, armed, high-legitimacy, strong-unrecognized, patron-aligned, League, radical, host, neighbor, and patron actors as already defined by the accepted profiles.

No extra country content is required to obtain that evidence.

## Asset and visual boundary

The current Event 006 portrait shelf and icon inventory are evidence surfaces, not authorization to invent new consumers.

Preserve the accepted policy:

- no custom Event 006 advisor art;
- no advisor dossier family;
- no inferred advisor or commander-small consumer;
- no placeholder portrait;
- no fallback flag;
- no generated depiction of a real person;
- no new achievement icon family;
- no new SCN-008 visual family;
- no Meshy production for this closure addendum.

The Statehood Ledger and existing super-event/achievement consumers need runtime acceptance, not another visual concept pass.

## Explicit non-goals

Do not add:

- a new independence package;
- a new country route;
- a new focus family;
- a new formable;
- a new League currency, office, or charter pillar;
- a new decision or mission family;
- a new scripted GUI;
- a new scenario family;
- a new achievement;
- a new super-event;
- advisor art;
- replacement audio;
- a technology or doctrine branch;
- a fallback carrier, flag, portrait, state mapping, or route.

If an existing accepted surface fails, repair that surface or return it for an explicit accept/reject decision.

## Ordered implementation and validation handoff

1. Freeze the current accepted package, decision, focus, League, achievement, scenario, and research registries.
2. Reconcile any stale audit shorthand, especially SCN-008's 24-cell shorthand versus the current 32 selectable mode/intensity cells.
3. Build the allocator and 80-row decision/mission evidence ledgers.
4. Repair focus geometry and additive/post-formation reachability without replacing meaningful trees.
5. Rerun focus inspection, render, lint, and representative AI probability scenarios.
6. Execute and record the 32 SCN-008 cells plus bounded collision, selector, rollback, and persistence sweeps.
7. Execute and record the League transaction and 16-achievement reachability matrices.
8. Retain fail-closed research and rights gates for `IW-157`, `IW-178`, `FORM-39`, MFX, `FORM-48`, `HAW`, `FSM`, and super-event `6001`.
9. Rerun the focus, decision/mission, localisation, country-package, and whole-event completion audits.
10. Update event docs, the source-of-truth map, and the workbook only from accepted implementation and runtime facts.
11. Promote Event 006, the Liberations cluster, or SCN-008 only if no named HOLD, fallback, missing AI evidence, unreachable accepted path, stale documentation, or unresolved accepted plan remains.

## Acceptance criteria

The bounded closure addendum is complete only when all of the following are true:

- every accepted allocator row has source and runtime disposition;
- ordinary `3` / `4` / `5` / `7` and exact-ten behavior is evidenced;
- both `IW-008` / `IW-010` orders and all named host-remnant/collision cases are evidenced;
- all 80 accepted decision/mission rows have an explicit disposition;
- focus blockers are cleared without route or prerequisite regression;
- additive and post-formation focus modes have real producers or are explicitly removed/rejected;
- every accepted route end has a reachable admitted carrier and AI-valid scenario where applicable;
- all 32 SCN-008 selectable mode/intensity cells are dispositioned;
- all named SCN-008 collision, selector, rollback, summary, and save/load cases are dispositioned;
- League transactions have truthful success, refusal, failure, cleanup, exploit, AI, and persistence evidence;
- all 16 achievements have qualification, near-miss, disqualification, reachability, persistence, and scenario disposition;
- the 24 accepted AI profiles have representative behavioral and resource evidence;
- `IW-157`, `IW-178`, `FORM-39`, MFX, `FORM-48`, `HAW`, `FSM`, and super-event `6001` are either accepted with exact evidence, explicitly rejected, or remain visibly fail-closed;
- no fallback or unapproved simplification is introduced;
- no custom Event 006 advisor art or dossier consumer is introduced;
- the final audits agree with the docs, source-of-truth map, and spreadsheet statuses.

## Promotion disposition

This file should remain in `docs/plans/006_independence_wave_plans/`.

It is a working closure and acceptance handoff, not a replacement source specification.

The parent should promote only accepted design changes into `docs/specs/006_independence_wave_specs/`.

The SCN-008 matrix clarification should be merged into the relevant spec acceptance section if the parent accepts the distinction between six numeric families and eight selectable player-facing modes.

Runtime results, audit dispositions, and evidence links belong in implementation handoffs, event docs, the source-of-truth map, and the workbook rather than being copied wholesale into the design spec.

## Parent handoff

### Design problem

Event 006 is broad and mechanically deep, but its current authority is fragmented across source-only passes, incomplete runtime matrices, focus reachability gaps, League/achievement witness gaps, scenario acceptance gaps, and fail-closed research items.

### Proposed disposition

Stop broad expansion and execute the five bounded closure workstreams in this file.

### Research and regional basis

The accepted Event 006 concept already draws on local independence institutions, anti-colonial congress behavior, host-remnant politics, regional formable traditions, and exact community identities.

The unresolved New Guinea leads demonstrate why exact regional attribution matters: Biak/Yapen/Koreri and Motu-Koitabu/Hiri/lakatoi material cannot be generalized into a pan-Papuan fallback.

Those claims remain inspiration or research leads until exact state containment, period relevance, rights, and symbol attribution are established.

### Files written

- `docs/plans/006_independence_wave_plans/subagent_handoffs/006_improvement_loop_audit_v27_2026_07_27.md`

### Implementation surfaces affected if accepted

- allocator and release transaction evidence;
- accepted decision and mission evidence matrix;
- focus geometry, shared additive insertion, post-formation producer, and route reachability;
- AI weighted-route and transaction scenarios;
- SCN-008 selector, execution, summary, collision, and persistence evidence;
- League transaction and cleanup evidence;
- achievement writers, reachability, and disqualifier evidence;
- `FORM-39`, MFX, `FORM-48`, HAW, FSM, `IW-157`, and `IW-178` admission evidence;
- super-event `6001` rights decision and `6002` playback acceptance;
- event docs, source-of-truth map, audit reports, and workbook status after evidence exists.

### Open questions requiring parent or user disposition

1. Will the parent accept the current SCN-008 runtime grid as 32 player-selectable mode/intensity cells while retaining six numeric type families in implementation?
2. Will each unproduced focus overlay receive a real non-destructive producer, or should the unreachable mode be explicitly removed/rejected?
3. Does current League source now provide every achievement writer identified by the older achievement audit, especially the expulsion-during-term witness, or does one narrow source fix remain?
4. Will `FORM-48` receive a compliant admitted carrier/member chain, or remain queued for explicit rejection?
5. What is the explicit MFX review disposition?
6. What source or rights path, if any, will be accepted for `IW-157`, `IW-178`, and super-event `6001`?

### Prior-addendum status

`IW-043` / `IW-058` and `IW-093` / `IW-098` are closed by parent instruction and must not receive duplicate addenda.

The runtime, focus, scenario, League/achievement, and research/rights dispositions in this file remain queued.

### Final recommendation

Keep the whole event at **HOLD / PARTIAL**.

Do not run another Event 006 improvement-loop expansion pass while this bounded closure addendum is unresolved.

After its finite gates are complete, run final audits and write a closure handoff rather than another expansion unless a specific failed acceptance row exposes a genuinely new design defect.
