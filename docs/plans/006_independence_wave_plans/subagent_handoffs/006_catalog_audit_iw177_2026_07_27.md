# Event 006 catalog audit after IW-177 Fiji tranche

Date: 2026-07-27

Scope: Read-only audit of the Event 006 catalog workbook against the current IW-177 Fiji package tranche and its player-facing Event 006 sources. The workbook remains the only editable catalog source. No gameplay or localisation files were edited.

## Verdict

**PASS with no workbook change.** Event 006 remains `Minor Repeatable` and `In progress`; cluster 2 remains `Liberations`, `Minor Repeatable`, and `In progress`; SCN-008 remains `Needs Testing`. The IW-177 Fiji tranche does not stale any shared Event Details, evolution, cluster, or scenario mirror field and does not provide the runtime evidence needed for promotion.

## Workbook rows and fields checked

- `Events!7` (`ID 6`, `Independence Wave`): ID/name, static event-detail paragraph, all five evolution mirrors (`Evo I` through `Evo V`), world-end field, type, cluster ID, member severity, and status.
- `Clusters!3` (`Cluster ID 2`, `Liberations`): cluster name, exact cluster-detail wording, member IDs, type, chaos level, and status.
- `Scenarios!9` (`SCN-008`, `Every Banner Rises`): scenario name, sovereign-scatter premise, all eight type names in UI order, all four intensity paragraphs, and status.

## Exact comparison evidence

The workbook was compared to the current sources below.

- `localisation/english/chaosx_event_names_l_english.yml` (`chaosx.event_name.6`) matches `Events!B7`.
- `localisation/english/chaosx_gui_l_english.yml` (`chaosx.events_log.window.event_details.independence_wave`) matches the static paragraph in `Events!C7`; the runtime rival-bloc suffix remains intentionally outside the generic catalog mirror.
- `localisation/english/006_independence_wave_evolutions_l_english.yml` title/body pairs match `Events!D7:H7` exactly with the existing blank-line separator.
- `localisation/english/chaosx_gui_l_english.yml` (`chaosx.events_log.window.cluster_details.description.liberations`) matches `Clusters!C3`.
- `localisation/english/006_independence_wave_scenario_l_english.yml` matches `Scenarios!B9:E9` for the name, sovereign-scatter premise, eight type labels, and Low/Medium/High/Maximum paragraphs.

All 25 checked mirror fields passed exact comparison. The source-of-truth map still records the shared catalog surfaces as aligned and package IDs as deliberately absent from generic Event Details.

## Changes

None. The workbook at `docs/spreadsheets/chaos_redux_events_catalog.xlsx` was not saved or modified. Existing workbook structure and formatting were left untouched.

## Export evidence

`python .tools/export_event_catalog_csv.py` was **not run** because no workbook save occurred. The export-only CSV snapshots were not edited.

## IW-177 and completion blockers

- The Fiji package remains fail-closed and outside canonical Event 006 runtime content attestation because the FORM-39 Melanesian Federation adapter, member transaction, X-tag identity, and PNG/WPG package surfaces are not implemented.
- The Sukuna portrait source/date gate remains open: the current archival source is catalogued circa the 1940s against the event's 1936-centered baseline.
- Whole-event completion and SCN-008 live execution evidence remain open, so no status promotion is justified.

No stale wording, contradictory status, or needs-user-review catalog cell was found.
