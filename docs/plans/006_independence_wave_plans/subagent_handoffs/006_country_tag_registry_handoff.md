# Event 006 country-tag reservation handoff

## Scope

This tranche registers the collision-free X-ending tags needed by the accepted
Event 006 candidate registry without pretending that a registered tag is a
playable package. It supplies dormant map-color/culture shells and final public
country-name localisation only where the accepted research establishes a safe
public identity. Runtime selection remains controlled by the separate
`independence_wave_package_content_ready` gate.

Files in this tranche:

- `common/country_tags/006_independence_wave_countries.txt`;
- 107 individual files under `common/countries/006_independence_wave_<TAG>.txt`;
- 107 dormant history shells named `<TAG> - Event 006 Country Shell.txt`;
- `common/countries/006_independence_wave_unresearched_reservations.txt`;
- `localisation/english/006_independence_wave_countries_l_english.yml`.

## Registration result

The accepted registry needs 128 new tags. All 128 are registered, are unique in
the installed vanilla/mod registry, and end in `X`. The 107 identities with an
accepted public-name direction receive an individual country definition,
dormant history shell, and fifteen country-name localisation keys covering the
base name plus four ideology forms. The shells deliberately contain no
politics, leader, commander, force, idea, focus, AI, flag, or readiness
assignment; those surfaces belong to the full package implementation.

The following 21 research-sensitive identities remain inert and have no history
or localisation:

`DJX`, `DMX`, `DNX`, `ENX`, `EXX`, `EYX`, `FPX`, `FWX`, `GAX`, `GDX`,
`GGX`, `GHX`, `GLX`, `GPX`, `GVX`, `HHX`, `HMX`, `HQX`, `HTX`, `HWX`,
and `HXX`.

Each points only to
`common/countries/006_independence_wave_unresearched_reservations.txt`. The
accepted package research must first settle its named polity/community,
territorial distinction, symbols, and political authority. No generic color,
leader, emblem, or substitute public name has been assigned.

## Readiness contract

No country in this tranche receives
`independence_wave_package_content_ready`. That flag may be granted only after
the package has all of the following wired and audited:

- accepted identity and territorial binding;
- political authority, leader, and command roster;
- sourced or generated final flags and required portraits with provenance;
- dynamic starting-force mapping and materialization;
- Event 006 ideas and visible values;
- full Event 006 tree for a new X tag, or safe additive overlay for a reused
  registered tag;
- package AI, diplomacy, former-host relation, regional ambition, routes, and
  formable access;
- complete player-facing localisation and all required assets.

This keeps registration from becoming permission for shallow bulk content and
makes every incomplete or sensitive package fail before allocation.

## Validation evidence

- 128 registered tags, 128 unique tags, zero non-X tags, and zero collisions
  against every other installed country-tag registry file;
- 107 individual country definitions and 107 matching dormant history shells;
- 21 inert reservations, exactly matching the unresolved research set;
- 1,605 unique localisation keys (`107 × 15`), UTF-8 with BOM, with no `:0`
  suffixes;
- zero readiness-flag assignments in the dormant histories.

## Remaining package work

This handoff does not claim a playable country package. Every registered
identity still needs the complete readiness list above. The 21 inert identities
also require the accepted research resolution before any of those surfaces may
be created. No fallback or substitute was used.
