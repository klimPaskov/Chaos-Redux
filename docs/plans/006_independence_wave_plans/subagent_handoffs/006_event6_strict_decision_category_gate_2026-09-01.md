# Event 006 strict decision-category gate

Date: 2026-09-01.

## Scope

This bounded safety tranche keeps Event 006 decision categories behind the real Event 006 active-origin predicate after the Event 021 adapter introduced the narrower package-content predicate for package-side setup effects.

## Source change

`common/decisions/categories/006_independence_wave_categories.txt` keeps the founding, government, security, host-relations, and Scotland category wrappers on `is_independence_wave_active_country`. It also adds the same strict active-origin requirement to the network, league, and high-chaos category wrappers, whose phase predicates now accept the package-content helper for the adapter's bounded setup window.

This prevents transient or proven origin-neutral Event 021 adapter receipts from exposing Event 006 decision categories before a committed Event 006 origin. The package-content helper remains available to package-side setup and effect consumers; no category, mission, cost, queue, pressure, allocator, admission, Join, or country package was added.

The concurrent uncommitted substitution that changed the main Event 006 decision file from `is_independence_wave_active_country` to `is_independence_wave_package_content_active` was mechanically removed. Direct decision activation, visibility, cancellation, and target gates therefore remain strict, including the existing IW-095 security-cost display change.

## Validation and limits

Run the focused Event 006 allocator, country API, strict flags, FORM-16, SCN-008, and GUI-matrix validators after parent review. Source search must find no package-content predicate in the Event 006 category registry or main decision file. This is source/static evidence only; no live Event 021 or Event 006 UI rendering is claimed.

The Event 006 boundary remains HOLD / PARTIAL at 32 content-attested selectable packages, 29 compatible reservation groups, 40 runtime adapters, and 161 unattested selectable rows. No simplification or fallback was introduced.
