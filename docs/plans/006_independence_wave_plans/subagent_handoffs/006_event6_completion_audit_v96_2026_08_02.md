# Event 006 Completion Audit v96

Date: 2026-08-02

Mode: read-only whole-event completion audit with this handoff as the only authorized repository change.

Audited baseline: `81b5a80745b0cf2770fb17a5b270a7cfd47b1ef7`.

Requested commits confirmed as ancestors of the audited baseline: `d52c8404a`, `307a06a43`, `7331e3215`, and `61dffe6f2`.

Current-state additions confirmed as ancestors of the audited baseline: Barsoum runtime DDS promotion `4b8c82faa` and documentation supersession `706c159be`.

Overall completion verdict: **partial and blocked**.

Event 006 has a connected entry path, event log and detail surfaces, five evolution incident families, a mature allocator, fourteen compile-time-attested country packages, substantial focus, decision, mission, league, formable, achievement, and super-event source, and matching current catalog exports.

It is not complete because 180 selectable candidate packages remain unattested, the full 14 and 20 launch bands lack complete compatible runtime capacity evidence, focus geometry has no successful post-reflow MCP validation, CAT has no valid owning-tree contract, accepted formable and decision matrices are only partially closed, AI and balance scenarios remain unevaluated, visual families and consumer handoffs remain incomplete, `6001` is blocked, and several documentation surfaces are stale.

No package is newly admitted by this audit.

No fallback or unauthorized simplification is accepted by this audit.

## Completion status by surface

| Surface | Status | Current evidence | Completion gap |
| --- | --- | --- | --- |
| Entry event, automatic dispatch, log actor, and details | Source-connected | `events/006_independence_wave.txt` keeps root `chaosx.nr6.1`; `common/scripted_effects/chaosx_events_log_effects.txt` maps actor and event detail content; `common/scripted_effects/chaosx_logic_effects.txt` contains the automatic branch | The Statehood Ledger and event detail consumer still lack a current focused render or interaction-evidence pass |
| Evolutions and Armed Birth follow-through | Source-connected | Five evolution incidents remain in `events/006_independence_wave_evolution_incidents.txt`; detail entries remain in `common/scripted_effects/chaosx_events_log_effects.txt` | No current scenario matrix proves route interaction, repeated launch behavior, and all high-chaos predicates together |
| Allocator and launch contracts | Partial | Fresh `python -B .tools\audit_event6_allocator.py` passed with 149 publishers, 126 automatic/high-chaos selectable packages, 138 SCN-008 ranked selectable packages, 14 attestations, and 13 reservation groups | Complete compatible capacity remains unproven for the 14 and 20 launch bands under living-host, protected-remnant, reservation, collision, and transaction conditions |
| Country tags and protected identities | Static pass | Fresh `python -B .tools\audit_chaosx_country_tags.py --surface-scan` passed with 136 protected tags, zero external country-definition collisions, and zero external identity-surface collisions | This pass does not prove package completeness, package admission, focus ownership, or runtime release viability |
| Candidate registry and package admission | Partial | `docs/specs/006_independence_wave_specs/matrices/006_candidate_country_registry.csv` contains 206 rows, including 193 selectable rows after excluding 13 vanilla-route-overlay-only rows | Only 14 selectable packages are compile-time attested; 180 selectable rows remain unattested |
| National focus system | Partial | The current tree contains the bounded v82 coordinate reflow and retains its route semantics | Current MCP inspection fails at `SCAN_BYTE_LIMIT`; static after-state still has 53 straight-segment crossings, 2 through-node hits, and 28 long connectors |
| CAT focus carrier and FORM-07 | Design gap and fail-closed | `common/scripted_effects/006_independence_wave_catalonia_package_effects.txt` assigns the full framework; `common/scripted_triggers/006_independence_wave_catalonia_package_triggers.txt` requires assignment and FORM-07 readiness; `common/scripted_triggers/006_independence_wave_focus_triggers.txt` gives additive-carrier access only to the ICE carrier | CAT is on vanilla `generic_focus`; an additive overlay would not insert Event 006 roots, and replacing the owning tree would violate the vanilla-preservation contract without an accepted redesign |
| Decisions and missions | Source-rich but acceptance-incomplete | The latest bounded source audit covers 31 decision files, 59 timed missions, 19 selectable missions with AI, 133 custom-cost localisation triplets, five evolution incidents, DM-22, and DM-58 without finding a bounded source defect | The accepted 80-row matrix has no single current row-by-row disposition, and the full SCN-008 scenario matrix is not evaluated |
| Formables | Partial | Source transactions exist for FORM-01, FORM-02, FORM-03, FORM-04, FORM-05, FORM-07, FORM-12, FORM-13, FORM-18, FORM-39, and FORM-48 | The accepted 48-formable set lacks complete identity, transaction, AI, asset, localisation, member-admission, and readiness closure |
| AI and balance | Partial | Event 006 has 13 AI strategy files, focus and decision weighting, route logic, and 24 accepted AI profiles in the spec matrix | There is no consolidated 24-profile source mapping or named scenario evaluation for selection order, dominance, starvation, timing, and world-state inputs; fresh probability inspection also fails at `SCAN_BYTE_LIMIT` |
| Achievements | Source-complete, validation-incomplete | Fresh source counts find 16 `chaosx_006_*` achievement definitions and 48 matching DDS files | The 16 accepted achievements have no current row-level runtime-state and scenario proof in the completion record |
| Visual assets | Partial | The current pre-resize shelf has 78 physical PNGs, 73 indexed unique rows, zero missing indexed files, and five unindexed physical files | The 48 accepted asset families and all runtime consumers do not have a complete crosswalk; current manifests contain stale Barsoum and shelf-count statements |
| ASY Barsoum portrait | Narrow runtime promotion complete | v93 passed identity/likeness, style/framing, provenance, and the stated PD-1923/1921 rights/date basis; v94 promoted the stable concordat-council DDS consumer | The v93 no-wire boundary is historical; v94 does not create an advisor or small portrait and does not close the wider IW-058 roster, formable, or package audit |
| MNT portrait work | Source-only and blocked | v90 provides a Janko Vukotić archival source and crop with `needs_user_review`; v89 leaves the Mitar Martinović v5 candidate on identity hold | No repaint, DDS, GFX, runtime wiring, or identity relabel is authorized; the 1927 death date also requires an explicit 1936 consumer decision |
| Super-event `6001` | Blocked | A dormant DDS and registered sprite exist for the League Formation concept | There is no runtime dispatch, localisation package, authorized audio, WAV, wrappers, or firing chain; the accepted London Brass Players recording remains blocked by redistribution rights and no fallback is authorized |
| Super-event `6002` | Partial and source-wired | Slot 24, payload `6002`, history one-shot logic, localisation, WAV, base sound, and six wrappers exist; four qualification routes are reachable | The hidden-formable route depends on incomplete FORM-42 or FORM-48 proof, and the 20-country qualification is unreachable with current admitted capacity |
| Workbook and CSV catalog | Current but honestly incomplete | Fresh read-only comparison shows the workbook and exports match exactly for Event 6 `In progress`, Cluster 2 `In progress`, and SCN-008 `Needs Testing` | These statuses correctly prevent a completion claim; the dirty workbook and Event CSV belong to concurrent Event 016 work and were not touched |
| Documentation and handoffs | Partial and stale in places | Current source map acknowledges the Barsoum v94 narrow promotion and CAT fail-closed state | Asset manifests, focus-validator wording, older completion summaries, and `6001` wording still need reconciliation |

## Compile-time attestation and capacity evidence

The exact compile-time attestation set remains defined in `common/scripted_triggers/006_independence_wave_package_dispatch_triggers.txt`.

The fourteen attested packages are IW-001, IW-002, IW-004, IW-006, IW-007, IW-008, IW-009, IW-010, IW-012, IW-017, IW-018, IW-019, IW-173, and IW-184.

The broader runtime adapter OR must not be treated as package admission because it also contains partial and fail-closed package surfaces.

The fresh allocator audit reports 13 compatible reservation groups because RG-RHINE-SAAR has capacity two and contains distinct IW-008 and IW-010 anchors.

The audit proves configured automatic counts of 6, 8, 10, 14, and 20 and the World Collapse count of 20.

It also proves the expected allocation sequence of anchors, compact territory, extended territory, and lock, plus the Event 005 anchor phase before Event 006 optional territory and lock.

It does not prove that fourteen or twenty complete packages can simultaneously survive live source, host, reservation, collision, and transaction constraints.

The 14 and 20 bands must therefore remain fail-closed until a complete compatible runtime capacity matrix exists.

## Focus and CAT findings

Fresh `hoi4.focus_inspect` against `common/national_focus/006_independence_wave_focus.txt` and tree `independence_wave_focus_tree` returned `SCAN_BYTE_LIMIT` with no scanned files, artifacts, or diagnostics.

Fresh focus and decision probability inspection returned the same `SCAN_BYTE_LIMIT` limit.

The last successful tool-level focus evidence predates the v82 coordinate-only reflow and reported 14 blockers, 45 crossings, 7 node intersections, and 28 long connectors.

That evidence is a pre-reflow baseline and must not be presented as the current post-reflow MCP result.

The v82 static after-state reports 184 regular nodes, 223 prerequisite connectors, 53 straight-segment crossings, 2 point-on-segment hits, 28 long connectors, 5 same-row gaps under two units, and zero duplicate coordinates.

Only four coordinates changed in v82, and the reflow did not change focus ids, prerequisites, rewards, AI, localisation, or icons.

The current exact MCP blocker count is unknown because the post-reflow source cannot be scanned by the current inspection limit.

CAT remains a design gap rather than a safe local patch.

The existing CAT setup attempts to assign the full Event 006 framework, but the prepared contract requires that framework and FORM-07 readiness.

The additive carrier trigger only recognizes the existing ICE carrier, while CAT still consumes vanilla `generic_focus` and cannot see the shared Event 006 roots by flag alone.

No focus ownership change, generic-tree replacement, or CAT admission is authorized without a parent-approved carrier design that preserves the vanilla contract.

## Formable disposition

FORM-01 through FORM-05 have operational source tranches but still belong in the broader acceptance and validation matrix.

FORM-12, FORM-13, and FORM-18 have exact signature transaction source in the CHU and ASY systems, but their packages remain unadmitted.

FORM-07 has a bounded adapter draft but remains blocked by CAT focus ownership, the Iberian X identity and flag contract, incomplete NAV/IW-013 and GLC/IW-015 package adapters, and member-readiness closure.

FORM-39 is implemented as fail-closed and remains blocked by IW-157 and IW-178 research, the MFX identity and flag package, and six writerless inputs recorded by its current handoff.

FORM-48 is implemented, but only IW-184 HBX and IW-173 HAW are admitted while FSM/IW-179 is not, so the complete admitted member set is unreachable.

FORM-42 remains explicitly blocked.

Most other FORM-06 through FORM-47 families lack one or more of complete identity, transaction, AI, assets, localisation, package admission, and readiness evidence.

Current documentation should distinguish the bounded drafts for FORM-07, FORM-12, FORM-13, FORM-18, and FORM-39 from the broader fail-closed remainder.

## Decisions, missions, AI, and balance

The bounded v71 source audit remains the strongest current decision and mission evidence because no later Event 006 decision source change supersedes it.

Its clean bounded result does not close the accepted 80-row decision and mission design matrix.

A single current row-by-row disposition is still required for every accepted entry, including implementation identifier, availability, cancellation, timeout, AI, localisation, cost, cleanup, and validation state.

SCN-008 still needs the full 32-cell source scenario matrix covering zero-ready cases, living tags, protected hosts, reservation conflicts, Event 005 collisions, repeated launches, intensity and mode combinations, and final lock behavior.

The 24 accepted AI profiles need a current mapping to source files and named scenarios.

The required evaluation must cover selection order, mutually competing weights, starvation, route timing, world-state inputs, former-host survival, and formable pressure.

The current presence of AI weights and strategy source is not sufficient evidence for a balance conclusion.

## Asset findings

The physical `docs/assets/006_independence_wave/pre_resize` shelf contains 78 PNGs and 81 total files with no child directories.

`PRE_RESIZE_MANIFEST.md` parses to 73 indexed unique rows, and every indexed row has a physical file.

The five unindexed physical PNGs are:

- `CHU_middle_volga_congress_mirsaid_sultan_galiev_hoi4_repaint_v1.png`
- `portrait_ARX_gioacchino_solinas_hoi4_master.png`
- `portrait_ARX_independence_wave_emilio_lussu_hoi4_master.png`
- `portrait_ARX_luigi_mella_santelia_hoi4_master.png`
- `portrait_ARX_vittorio_verne_hoi4_master.png`

The current asset manifest contains a correct 78 physical and 73 indexed addendum, but it also retains an older current-authority paragraph that reports 68 physical and 63 documented files.

The current asset manifest and pre-resize manifest also retain no-runtime-promotion wording for Barsoum that is superseded by v94.

The source-of-truth map now carries explicit v94 supersession wording, but the asset manifests still need the same current-state correction.

The Post-Release Instability registry row remains a final/handoff asset without an assigned ASSET id.

ASSET-046 provides only partial formable flag and emblem coverage.

The active asset workspace cannot be retired until the 48 accepted asset families, stable consumers, GFX references, manifests, and handoffs have a complete crosswalk.

### Barsoum v93 and v94

The v93 independent visual audit passed identity/likeness, HOI4 leader style/framing, provenance, and the stated PD-1923/1921 rights/date basis with a documented low-resolution group-photo caveat.

The v93 handoff correctly stopped at a no-wire boundary at that time.

Commit `4b8c82faa` later promoted only the stable ASY concordat-council DDS consumer at 156 by 210 pixels.

The promoted DDS is a DirectXTex BGRA file of 131168 bytes with SHA-256 `5c034700247de09480eedd294ca192045c18dd8b9582fe236dda776e7d67ad06`.

The promotion changed no GFX, character, localisation, advisor, small portrait, package trigger, or attestation source.

IW-058 remains unadmitted because the wider grounded roster, formable, package, and runtime acceptance surfaces are incomplete.

### MNT v89 and v90

The v90 Janko Vukotić package is source-only and remains `needs_user_review`.

It records a circa-1910 Commons source, a PD-Montenegro basis, immutable master and crop evidence, an unknown original photographer and first-publication caveat, and a 2018 repost provenance limitation.

Because Janko Vukotić died in 1927, the parent must also approve the intended 1936 consumer identity before production.

The v89 Mitar Martinović v5 candidate passes processing and provenance and passes style with a caveat, but identity and cap/award source linkage remain on hold.

Neither handoff authorizes repaint production, DDS conversion, GFX wiring, runtime replacement, or relabeling of existing MNT identities.

## Super-event findings

### `6001`

No Event 006 runtime identifier using literal `6001` was found across current Event 006 common, event, interface, localisation, or sound consumers.

The unrelated zombies priority value `6001` is not an Event 006 super-event consumer.

Dormant League Formation art exists at `gfx/super_events/006_independence_wave/super_event_006_asset_005_league_formation.dds`.

The DDS is 599712 bytes with SHA-256 `E9F4A4F24D7F134E8BD3EF04724A7A04B020F0D9DC559C3C0928AC464CD79781`.

The sprite `GFX_super_event_006_asset_005_league_formation` is registered in `interface/006_independence_wave_event_pictures.gfx`.

Older wording that `6001` has no image is stale.

The accurate current statement is that dormant art and a sprite registration exist, but runtime dispatch, localisation, authorized audio, WAV, wrappers, and a firing package do not.

The accepted London Brass Players recording remains blocked by US redistribution rights, and no fallback track is authorized.

### `6002`

The current source defines constants slot 24, audio slot 24, and history payload `6002`.

`common/scripted_effects/006_independence_wave_super_event_effects.txt` includes five qualification packages, a history one-shot, and super-event emission.

Slot-24 localisation exists.

The WAV at `sound/006_independence_wave/super_event_006_02_every_border_a_casus_belli.wav` is 19402828 bytes with SHA-256 `3A7C58C94016EDA80842E328DEBC3D00B2D1755085F0C87F580AAAB3B4E0BC08`.

The base sound and six wrappers remain registered in the sound asset source.

Four qualification routes are currently reachable.

The hidden-formable route remains conditional on incomplete FORM-42 or FORM-48 proof, and the 20-country route remains unreachable at current admitted capacity.

## Catalog findings

The workbook remained read-only during this audit.

Fresh comparison against the three export-only CSVs found exact row agreement for the current Event 006 records.

The Events sheet and `chaos_redux_events_catalog.csv` both report Event 6 Independence Wave as `In progress` with matching detail and evolution wording.

The Clusters sheet and `chaos_redux_clusters_catalog.csv` both report Cluster 2 Liberations as `In progress`.

The Scenarios sheet and `chaos_redux_scenarios_catalog.csv` both report SCN-008 Every Banner Rises as `Needs Testing` with matching mode and intensity wording.

The workbook and Events CSV were already modified by unrelated concurrent Event 016 work and were not saved, exported, staged, or edited by this audit.

## Accepted-plan disposition

| Accepted plan or addendum | Current disposition |
| --- | --- |
| Seven-part Event 006 source specification | Partially realized and still authoritative |
| Core loop v35 | Implemented and source-closed |
| Dynamic systems v35 | Implemented and source-closed |
| Registry and API v35 | Implemented and source-closed |
| SCN-008 former-host uniqueness v67 | Implemented and promoted |
| Five evolution incidents and Armed Birth follow-through | Implemented and source-wired |
| FORM-03 plan | Source implemented; broader promotion and acceptance-matrix debt remains |
| IW-012 AI plan | Source implemented; profile mapping, probability, and scenario evaluation remain open |
| CHU and ASY signature addendum | Substantial source implemented; CHU remains unadmitted; ASY has one narrow Barsoum DDS promotion but remains unadmitted |
| DOX and SOK addendum | Substantial source implemented; roster, flags, formables, and package admission remain blocked |
| FORM-39 addendum | Implemented as fail-closed; research, identity, flag, and writer inputs remain blocked |
| FORM-48 addendum | Implemented; FSM/IW-179 admission prevents a complete member set |
| CAT and FORM-07 carrier work | Bounded draft implemented; deliberately fail-closed pending owning-tree design and package readiness |
| Focus v82 coordinate reflow | Static improvement retained; current post-reflow MCP acceptance is unavailable because of `SCAN_BYTE_LIMIT` |
| `6001` research | Rights blocker established; no replacement or fallback accepted |
| Barsoum v93 audit | Accepted as a visual and rights audit with its original no-wire boundary |
| Barsoum v94 promotion | Implemented as one stable DDS consumer only; no package admission follows |
| MNT v90 Janko source package | Queued at `needs_user_review`; no production or runtime promotion authorized |

## Documentation gaps

The source-of-truth map should promote this completion audit as the latest whole-event status authority instead of continuing to point readers to the older v33 completion audit.

The map and current reports should separate the pre-v82 MCP focus baseline from the current static after-state and the current `SCAN_BYTE_LIMIT` limitation.

The asset manifest and pre-resize manifest should adopt the Barsoum v94 runtime DDS state and remove or mark historical their older no-promotion wording.

The asset manifest should reconcile its internally conflicting 78/73 and 68/63 shelf counts.

The Post-Release Instability asset needs a stable ASSET id and consumer record.

The super-event documentation should state that `6001` has dormant art and sprite registration but no runtime package, rather than saying that no image exists.

The formable documentation should distinguish implemented fail-closed drafts from families with no bounded implementation.

The accepted decision, AI, achievement, formable, and asset matrices need current row-level dispositions with exact source identifiers and blockers.

## Meaningful validation performed

- Fresh allocator audit passed with source counts, reservation-group capacity, configured launch bands, scenario intensities and modes, and the two-stage Event 005 then Event 006 allocation order.
- Fresh protected-tag and identity-surface audit passed with 136 protected tags and zero external definition or identity collisions.
- Fresh exact attestation extraction confirmed fourteen and only fourteen compile-time-attested package ids.
- Fresh candidate registry accounting confirmed 206 total rows, 193 selectable rows, and 180 selectable unattested rows.
- Fresh focus inspection and focus and decision probability inspection recorded the current `SCAN_BYTE_LIMIT` blocker rather than reusing pre-reflow validator evidence as current.
- Fresh asset shelf and manifest comparison confirmed 78 physical PNGs, 73 indexed unique rows, zero indexed-missing files, and five unindexed physical PNGs.
- Fresh Barsoum v94 runtime file inspection confirmed the narrow stable DDS promotion and confirmed that no package attestation or broader consumer source changed.
- Fresh `6001` and `6002` consumer scans distinguished dormant art from runtime wiring and confirmed the current `6002` audio and wrapper package.
- Fresh read-only workbook and CSV comparison confirmed exact current Event 6, Cluster 2, and SCN-008 row agreement.
- Fresh achievement source counts confirmed 16 Event 006 definitions and 48 DDS files.

## Validation not available or still required

The HOI4 focus and probability MCP tools could not inspect the large current Event 006 focus and decision sources because of `SCAN_BYTE_LIMIT`.

No current post-reflow focus render, click-region inspection, or exact current MCP blocker inventory is available.

No full SCN-008 32-cell source scenario matrix is available.

No declared complete candidate-pool evaluation proves 14 or 20 compatible launches.

No consolidated 24-profile AI and balance evaluation is available.

No current Statehood Ledger interaction evidence covers resolutions, click regions, threshold transitions, toggle persistence, and semantic return.

No consolidated League and rival lifecycle matrix covers formation, reform, leadership, exit, collapse, escalation, disqualifiers, and all routes.

Optional live consumer testing belongs to the user and is not itself treated as a repository completion blocker.

## Remaining blockers and recommended next actions

1. Reconcile the documentation and asset manifests first so Barsoum v94, the 78/73 shelf, the v82 focus evidence boundary, and dormant `6001` art have one current statement.
2. Keep IW-058, IW-030, CAT, FORM-07, FORM-39, FORM-42, FORM-48, and the 14 and 20 launch bands fail-closed until their exact requirements close.
3. Obtain an explicit parent design decision for CAT focus ownership before changing any carrier, focus-tree assignment, or vanilla preservation contract.
4. Restore bounded MCP focus inspection or split the inspection target safely, then perform a coordinated whole-lane geometry pass instead of another isolated node move.
5. Build current row-level source matrices for all 80 decisions and missions, 24 AI profiles, 16 achievements, 48 formables, 48 asset families, and the 32 SCN-008 scenario cells.
6. Declare complete candidate pools and named world-state scenarios before making probability, balance, ordering, starvation, or 14 and 20 capacity claims.
7. Treat Barsoum v94 as one completed runtime portrait consumer only, then close the rest of the ASY grounded roster and package contract before considering IW-058 admission.
8. Resolve the MNT 1936 identity choice and source caveats before authorizing any Janko repaint or runtime consumer.
9. Resolve `6001` redistribution rights or approve a specific replacement cue; do not introduce a fallback implicitly.
10. Close CHU and ASY admission before promoting FORM-12, FORM-13, or FORM-18; close FSM before FORM-48; close research and MFX identity inputs before FORM-39.

## Scope and file changes

This audit did not edit gameplay, localisation, GFX definitions, assets, audio, workbook, CSV exports, or existing documentation.

The only file created by this task is this handoff.

Skills applied: `chaos-redux-events`, `chaos-redux-event-planning`, `chaos-redux-improvement-loop`, `chaos-redux-subagents`, `chaos-redux-event-assets`, `chaos-redux-focus-trees`, `chaos-redux-decisions-missions`, `chaos-redux-super-events`, and `xlsx`.
