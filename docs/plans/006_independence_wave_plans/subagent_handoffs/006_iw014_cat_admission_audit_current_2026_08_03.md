# IW-014 Catalonia current admission audit

> Current-status amendment notice (2026-08-05): This dated audit remains the CAT admission authority, but the NAV/GLC adapter wording in its original blockers is updated by the amendment at the end of this file. CAT remains HOLD/fail-closed.

Date: 2026-08-03

Verdict: **HOLD / fail-closed; no CAT promotion is safe in this tranche.**

This is a source audit only. It made no gameplay, map, history, flag, leader, portrait, advisor, localisation, or attestation edits. The obsolete pasted flag-log was not used.

## What is present

The CAT carrier has coherent dormant wrappers in `common/scripted_triggers/006_independence_wave_catalonia_package_triggers.txt` and `common/scripted_effects/006_independence_wave_catalonia_package_effects.txt`, central setup/final-validation/cleanup dispatch, package `iw_014`, tag `CAT`, anchor state `165`, reservation group `RG-165`, force profile `p14`, tradition `72`, the shared generic focus-tree CAT branch, CAT decisions and projects, seven lifecycle ideas, localisation, and CAT-specific AI strategy layers. Vanilla CAT history, state 165, Lluís Companys, vanilla flags, and meaningful identity surfaces are preserved; no duplicate CAT history, country definition, flag family, leader, portrait, or advisor asset is introduced.

The installed binding is coherent: state 165 is owned and controlled by SPR, SPR retains its capital state 41, and the CAT reservation row uses the unchanged state/anchor IDs. The source-only audit found no safe map rebinding or narrow CAT gameplay patch.

## Admission blockers

1. CAT setup requires `has_independence_wave_formable_commit_readiness = yes` before it can set its setup-complete receipt. The current FORM-07 readiness trigger remains false because the Iberian X identity, flag package, member-policy proof, identity/integration adapters, and the NAV/GLC corridor package surfaces are not accepted.
2. `iw_014` is present in the adapter allowlist and exact CAT identity branches, but it is absent from the central content-attestation set. The runtime and SCN-008 preflights require both the adapter and the exact attestation, so the carrier remains unreachable by design.
3. The CAT package has no independent event/incident art or bespoke asset family. Shared icons and preserved vanilla CAT assets are not a substitute for the unresolved Level-2 FORM-07 identity and package contract.
4. Static source wiring does not prove live host survival, force setup, supply, AI selection, or save/load behavior.

Removing the formable readiness predicate or adding `iw_014` to the attestation list would weaken the accepted identity/member/collision gates. No fallback, generic identity, advisor asset, history copy, or tag rewrite is authorized.

## Parent-owned next gate

Complete and independently review the Iberian identity/flag/member package and the source-wired NAV/GLC package surfaces, then rerun the full CAT package audit and promote `iw_014` only if the complete country, formable, asset, force, AI, localisation, cleanup, and collision contracts pass together. Until then, CAT remains a registered reusable API carrier with fail-closed runtime admission.

## Current amendment — 2026-08-05

IW-013/NAV and IW-015/GLC now have source-level setup, final-validation, and cleanup adapters, with NAV compact anchor 792 and optional 172/806 extensions and GLC compact anchor 171.

The central content-attestation, independent source/identity/flag/portrait, and country-package gates remain unresolved, so the adapters do not promote NAV, GLC, CAT, or FORM-07 to runtime or scenario capacity.

No advisor icons or advisor portrait assets were created or authorized, and no generic identity, flag, portrait, or attestation fallback is permitted.
