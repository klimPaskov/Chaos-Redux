# Full implementation prompt

Use the following as the implementation task after placing this package's `docs/` folders in the Chaos Redux repository.

```text
Implement Event 79, The Master, from docs/specs/079_the_master_specs/ and docs/plans/079_the_master_plans/. Treat the supplied user premise as authoritative and the package's new numeric values as the initial tuning candidate. Preserve Minor Repeatable, Chaos level 1, Diplomacy, Medium member, and To Be Reworked.

Read the current AGENTS.md, all must-read project skills and relevant helper contracts fully before implementation. Read the source-coverage audit and complete the required references it lists as unread or partial. Inspect the actual installed game definitions, not only the offline wiki. Do not claim a source was read if a tool output truncated it.

Start with the engine proof gates. Prove real direct puppeting, player-subject sponsorship, hierarchy-cycle behavior, faction members and leaders, preservation of unrelated wars, and political-state restoration. If the universal participant promise conflicts with the engine, report the precise observed conflict and ask for an explicit design decision. Do not exclude a player, free it, give the reward to its overlord, dissolve a faction, or end unrelated wars silently.

Implement separate generation-safe races, one seed per sponsor per race, live rankings, the 100 Influence threshold, and same-flow takeover before notifications. Implement all five positive action families, rival interference, domestic institutions, both evolution entry paths, and Great Game multi-target play. Complete actual factory commitments, persistent partial projects, equipment conservation, advisory work, receipts, actual-paid refunds, and once-only delivery. A static spirit, opinion modifier, faction invitation, or popup-dependent effect is not the required result.

Use the named subagents in the dispatch plan with fork_turns="none" and self-contained tasks. Keep shared files single-owner. The architecture, decision, AI, country, localization, improvement, and completion reviewers must provide actual evidence, not invented worker summaries. The independent near-completion improvement planner is mandatory. Merge accepted design improvements into the source specs before calling the design final.

Use the current universal cost framework for supported payable components. Preserve direct material conservation and the documented factory-day discount boundary. Apply the actual Chaos impact map once through shared world-change ownership. Integrate target reservation, repeat rules, cluster metadata, settings, evolutions, Event Log, Event Details, achievements, and the CXT extension.

Build the native Event 79 decision-category board from the approved visual reference. Keep each player's view selection separate from authoritative action targets. Inspect and render the owning GUI through the real tools at the specified resolutions and states. Generate required art only after reading the canonical visual references. Produce correct DDS consumers and immutable-template achievement triplets. Write finished localization from the direction-only writing spec, without raw IDs, fake quotes, or invented historical characters.

Run the complete runtime matrix, source validators, probability inspection and named-scenario analysis, save/load tests, multiplayer cases, and ordinary campaign tests. Run the included reference model only as a supplementary arithmetic check. Do not label a forced CXT scenario or Python pass as natural-play or HOI4 proof.

Update the authoritative XLSX catalog through its assigned worker and regenerate CSV exports. Do not directly edit export snapshots. Preserve catalog status until the user authorizes a change. Deliver changed paths, actual evidence, test results, remaining blockers, and a clear implemented-versus-unverified summary. Do not finish with a claim of release readiness while a critical gate remains open.
```
