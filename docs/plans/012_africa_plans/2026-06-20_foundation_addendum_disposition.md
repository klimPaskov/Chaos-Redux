# Event 012 Africa Foundation Addendum Disposition Ledger

Date: 2026-06-20

Scope: documentation-only disposition of `docs/plans/012_africa_plans/2026-06-16_foundation_gap_improvement_addendum.md`.

This ledger records the disposition of the 2026-06-16 foundation improvement addendum as of 2026-06-20. The addendum is not erased, deleted, or treated as if it never existed. It remains useful historical planning evidence, but the main agent should stop treating the whole file as one unresolved broad blocker. Its items are split below into implemented/folded, superseded/modified, queued/still open, and rejected/not pursuing now.

This ledger is not a claim that Event 012 Africa is complete.

## Source-of-Truth Map

Current accepted source package:

- `docs/specs/012_africa_specs/CURRENT_SOURCE_OF_TRUTH.md`
- `docs/specs/012_africa_specs/specs/`
- `docs/specs/012_africa_specs/matrices/`
- `docs/specs/012_africa_specs/prompts/`

Dispositioned working plan:

- `docs/plans/012_africa_plans/2026-06-16_foundation_gap_improvement_addendum.md`

Relevant audit evidence:

- `docs/plans/012_africa_plans/subagent_handoffs/2026-06-20_012_africa_completion_gap_audit_handoff.md`

The source-of-truth file records many later implementation tranches that reduce the addendum blocker list, while the completion audit records that scenario validation, country-package depth, GUI proof, asset proof, AI/balance proof, and spreadsheet/catalog alignment remain incomplete.

## Implemented/Folded

These items from the 2026-06-16 addendum are no longer broad unresolved blockers. They are implemented or folded into later Event 012 Africa source-of-truth notes, though final completion still depends on validation and source-spec cleanup.

| Addendum item | Current disposition |
| --- | --- |
| Selected dossier survey layer | Implemented/folded. The source-of-truth records the selected-dossier survey/case layer and the historical case mission layer between old-seat guards and settlement. |
| Historical dossier case types | Implemented/folded. The source-of-truth records site, route, and hearing cases with stored dossier id, seat state, reusable slots, costs, visible value gates, success/failure state, AI exposure, and World Is One certification use. |
| Authority Atlas selected-dossier profile and status surface | Implemented/folded. The source-of-truth records Authority Atlas lifecycle, dossier AI profiles, selected settlement summaries, counters, case status, resistance watch context, and arbitration details exposed in the header or Continental Congress panel. |
| Settlement fork expansion | Implemented/folded. The addendum's settlement fork concept is represented by observer seats, protected seats, regional authority offices, direct Archive seals, and rejected counterfeit claims with separate costs, counters, profile movement, and resistance-watch follow-up. |
| Resistance watches | Implemented/folded. Observer, protected-seat, rejected-claim, regional-office, and direct Archive settlements can produce timed mediation or enforcement watches with stored dossier/seat context and profile-specific outcomes. |
| Forgery and museum crisis | Implemented/folded. Failed direct Archive seal play can create the 90-day forgery/museum crisis, with a paid investigation path and Ananse Ledger intercept variant. |
| Old-seat arbitration | Implemented/folded. Six once-only rivalry pairs or clusters can enter the Authority Atlas arbitration calendar with costs, stored seat context, visible gates, and retryable failure. |
| High-chaos catalog safety and actor packages | Implemented/folded in parts. The source-of-truth records high-chaos actor packages and later capstone parity for `BON`, `HYR`, `BIR`, and `SAO`, plus earlier created actor packages for several Bestiary rows. Remaining Bestiary depth belongs to disaster events, settlement hooks, and longer consequence chains rather than keeping the catalog unresolved. |
| Created country role packages | Implemented/folded in bounded form. The completion audit records broad created-tag coverage from a country-package sidecar, and the source-of-truth records selected-unifier origin packages and created regional authority mandate play. This closes the "only tag/flag" foundation complaint for the current tranche, but not all route-specific depth. |
| Regional authority mandate cycle | Implemented/folded. Regional authority subjects can be commissioned through a mandate mission with resource transfer, success/failure checks, visible counters, and role-specific outcomes. |
| Icon v5 asset packages | Implemented/folded for the current focus/goal and idea/national-spirit icon surfaces. The source-of-truth marks the v5 icon packages as the current live icon source and supersedes earlier no-white-background icon passes. |
| Triggerable scenario validation scaffolding | Implemented/folded as scaffolding only. The Continental Pole triggerable scenario was expanded to open late-route validation gates at high and maximum intensity. It is not a completed scenario validation report. |

## Superseded/Modified

These addendum statements should no longer be read literally, because later implementation or source-of-truth notes changed the shape of the work.

| Original addendum direction | Disposition |
| --- | --- |
| The broader addendum remains unresolved for package-specific missions, deeper settlement forks, local resistance events, and richer per-package AI. | Modified. Those areas are partly implemented through dossier cases, settlement forks, resistance watches, arbitration, forgery/museum crisis, dossier AI, regional authority mandates, and actor-package work. Remaining gaps are narrower validation, route-depth, source-asset, and cleanup items. |
| Replace thin generic decisions entirely with new families. | Modified. The current layer subordinates the generic queue through selected-dossier cases, settlement decisions, watches, investigations, arbitration, and Authority Atlas presentation rather than treating the old generic decisions as the only playable layer. |
| Implement exact suggested focus clusters and focus IDs next. | Modified. Later work implemented the design through Authority Atlas, Continental Congress, old-seat case, settlement, mandate, high-chaos parity, and scenario scaffolding surfaces. The exact suggested focus-id list is no longer a required blocker unless the parent explicitly reaccepts those IDs. |
| High-chaos package list should implement at least the first 8 packages. | Modified. The live source records a broader actor-package/capstone-parity state. Remaining Bestiary work is about disaster hooks, settlement hooks, validation, and route consequence depth, not simply unlocking the first eight rows. |
| Worktree high-chaos tags must be reviewed before treating them as implemented. | Superseded where later handoffs/source-of-truth explicitly close current actor packages. It remains valid as a rule for any future or unverified created tag. |
| Earlier icon regeneration status. | Superseded by the 2026-06-20 v5 icon packages recorded as the current live icon source. |

## Queued/Still Open

These items remain queued and should be reported as open until a later implementation, validation, or documentation pass closes them with evidence.

| Open item | Reason it remains open |
| --- | --- |
| Targeted scenario validation matrix | The completion audit found no current eight-scenario validation matrix for ordinary unifier, weak/small unifier, RSA in Allies, African ally under attack, high-chaos Green Covenant, full Africa unification, cross-continent union, and World Is One gate. |
| GUI/static/animation prompt coverage and live proof | Continental Congress presentation exists and has improved, and the prompt-named charter seal animation is wired. The broader Charter/Authority/Bestiary visual strip is still not accepted as full prompt-equivalent coverage for the background, header, meter, regional-card, warning-border, Green Covenant seal, formable-emblem, and remaining prompt-named animation families. `docs/plans/012_africa_plans/2026-06-21_continental_congress_gui_animation_gap_handoff.md` is the current queue. Live screenshot/readability/playback proof also remains required after those families are implemented or explicitly accepted as equivalent. |
| Plan/doc stale cleanup | The plan folder still contains stale or partially superseded handoff claims. The parent should reconcile source-of-truth and plan disposition after reviewing this ledger. |
| Historical old-seat source assets where weak | Historical dossier flags, symbols, and some portrait/source rows remain weak, low-confidence, or not downloaded. Neutral Archive UI remains a queue item unless the parent explicitly approves it for a specific row. |
| Deeper host/country-package route depth | Selected-unifier origin cases, regional-authority mandates, current dossier slot families, and created-actor role packages reduce the gap but do not close deeper bespoke host routes, created-authority consequence chains, advisor/event hooks, reinforcement paths, or long-form country-package routes. |
| AI, balance, and exploit validation | Static AI and weighting surfaces exist, but no current report proves route-specific AI, decision costs, repeated GUI clicks, dossier retry, settlement watches, Bestiary actions, RSA treaty, living-core conversion, or sponsor proofs under scenario pressure. |
| Spreadsheet/catalog alignment | The spreadsheet handoff intentionally left Event 012 as `Needs Testing`. Catalog/spreadsheet completion should wait for validation and final wording alignment. |
| World Is One terminal proof | Scenario scaffolding opens validation gates, but source-of-truth explicitly says the triggerable scenario does not set the proof-verified flags, all-continent world-end readiness, gate-prepared flag, or terminal World Is One flags. |

## Rejected/Not Pursuing Now

These items should not be started as part of resolving the older foundation addendum unless the parent explicitly changes scope.

| Held or rejected item | Disposition |
| --- | --- |
| Another broad `chaosx_improvement_loop_planner` pass before this disposition is accepted | Hold. The improvement-loop rule says not to stack another broad planner pass while the previous addendum is unresolved. This ledger is intended to remove that blocker once accepted by the parent. |
| New broad catalog IDs | Reject for this pass. The addendum explicitly said not to add another catalog on top of the 32 historical IDs and 11 high-chaos IDs; current work should deepen and validate the existing catalog. |
| Unsourced real historical flags, quotes, portraits, or symbols | Reject for this pass. Historical and real-person material must remain source-reviewed; generated or invented assets are only appropriate for fictional, supernatural, or nonhuman surfaces. |
| Daily or weekly world iteration | Reject without explicit parent approval. Dossier and Archive maintenance should use targeted decisions, focus/event completion, missions, scoped helpers, and stored targets instead. |
| Full bespoke focus tree for every dossier row in this pass | Reject for this pass. The accepted bounded direction is selected deeper packages, shared regional/authority scaffolding where appropriate, and clear reporting of remaining country-package depth. |
| Treating triggerable scenario scaffolding as completed validation | Reject. The scaffolding is useful, but validation must be recorded separately against concrete scenarios and exploit checks. |

## Unresolved Plan and Handoff Disposition Table

| File | Ledger disposition |
| --- | --- |
| `2026-06-16_foundation_gap_improvement_addendum.md` | Dispositioned by this ledger. Partly implemented/folded, partly modified/superseded, partly queued, and partly rejected/held. No longer one undifferentiated broad blocker. |
| `2026-06-20_012_africa_completion_gap_audit_handoff.md` | Remains current evidence that Event 012 is not completion-ready. Its recommendation for a plan disposition tranche is addressed by this ledger only; validation and implementation blockers remain open. |
| Earlier high-chaos capstone-missing handoff claims | Superseded where the current source-of-truth records high-chaos capstone parity for `BON`, `HYR`, `BIR`, and `SAO`. |
| Spreadsheet alignment handoff | Still open as `Needs Testing` until scenario validation and catalog alignment are completed. |

## Duplicate or Superseded Document List

- Older icon-regeneration notes are superseded by the current v5 icon package evidence in the source-of-truth map.
- Earlier high-chaos capstone-missing notes are superseded where the current source-of-truth records the later parity tranche.
- The 2026-06-16 addendum remains the canonical older foundation addendum, but this ledger supersedes reading it as one unresolved blocker.

## Contradictions and Stale Instructions

- The old addendum says the broader foundation addendum remains unresolved for settlement forks, local resistance, package missions, and richer AI. The current source-of-truth records later implementation of several of those systems, so the older statement is stale as a blanket blocker.
- The current source-of-truth still says Event 012 is not closed and lists broader completion blockers. This does not contradict the implemented/folded dispositions above; it means this ledger resolves only the broad-addendum bookkeeping problem.
- Any older prompt or handoff that requests a new broad Event 012 improvement-loop pass before disposition should be treated as stale until the parent accepts this ledger.

## Recommended Parent Decisions

- Accept or revise this ledger, then update `docs/specs/012_africa_specs/CURRENT_SOURCE_OF_TRUTH.md` to point to the accepted disposition.
- Decide whether to explicitly accept a narrower Continental Congress panel as equivalent to the older prompt's regional-card/meter/static/animation wording. Until that decision has written evidence and live render proof, the prompt-named GUI/static/animated families remain queued by `2026-06-21_continental_congress_gui_animation_gap_handoff.md`.
- Decide which historical old-seat source-asset rows should receive source-research priority and which, if any, may use neutral Archive placeholders.
- Keep spreadsheet/catalog status at `Needs Testing` until targeted scenario validation is recorded.

## Validation Notes

- Confirmed the requested ledger path did not already exist before creation.
- Confirmed the new file contains the requested disposition sections and completion-audit note.
- Did not inspect gameplay, localisation, GUI, GFX, scripts, assets, spreadsheets, or binary files beyond the user-named documentation evidence.

## Completion-Audit Note

This ledger only classifies the 2026-06-16 foundation improvement addendum. It does not certify gameplay completion, balance, live UI proof, scenario validation, source asset completion, spreadsheet alignment, country-package depth, or final Event 012 readiness.
