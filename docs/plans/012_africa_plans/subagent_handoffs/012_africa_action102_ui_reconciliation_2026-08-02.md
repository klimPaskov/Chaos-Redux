# Event 012 Action 102 UI reconciliation

Date: 2026-08-02.

Status: Accepted list-only UI disposition.

Action 102 uses the established Charter target-selection surface rather than a duplicate regional control. The player refreshes the bounded African roster, selects the candidate through `africa_select_charter_target`, opens `africa_open_regional_restorations_page`, and selects `africa_select_promote_priority_member_package`. The shared quote then exposes the exact target, row-specific duration and objective contract, dynamic cost, target-state requirements, and the existing execute/cancel lifecycle.

The regional-restorations category remains a list-only action page by design. It does not add a second candidate card, a second target store, or a parallel promotion decision. The Action 102 validator reruns `africa_priority_member_evaluate_promotion_candidate` against the selected target before launch, so stale survey evidence cannot promote an invalid carrier. Full, partial, and failure outcomes continue through the shared result and cleanup kernels.

No country tag, carrier, focus surface, opinion rule, ownership transfer, core grant, or asset path changed in this reconciliation. The acceptance ledger now records the existing shared selector and quote lifecycle as the owner-complete UI path.

Validation evidence: source inspection confirms the regional category, shared target selector, selected-action quote, state-target selector, execution decisions, mission family, and cleanup effects are all present. Live in-game interaction remains user-owned.
