# Event 012 Africa — Disaster Wardens model handoff

Date: 2026-08-26. Status: `vanilla_infantry_reuse_parent_override_applied_live_validation_pending`.

No Meshy call is required or authorized for this asset. The parent-owned division-template consumer now binds subunit `disaster_wardens` to the vanilla `infantry` model token in `common/scripted_effects/012_africa_action_effects.txt`, which resolves to `infantry_rifle_entity` / `generic_western_european_rifle_infantry_mesh`. Historical custom-model evidence is superseded and retired. Manifest flags remain the existing shared package contract; live consumer validation is still parent-owned.

The former custom route is intentionally not a completion candidate: the accepted Meshy 7 geometry and firearm gate were never followed by a terminal rig/action lineage, so no custom `.mesh` or `.anim` is promoted. The old provider task records, rejected checkpoints, counters, and sourced audio remain quarantined under the Africa asset job root for provenance only. This parent integration changes the model token only; it contains no new provider spend and no custom runtime/GFX/sound-definition asset edit. In-game visual and sound validation remain open.
