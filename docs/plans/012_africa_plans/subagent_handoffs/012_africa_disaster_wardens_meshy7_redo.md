# Event 012 Africa — Disaster Wardens model handoff

Date: 2026-08-26. Status: `custom_model_not_required_existing_infantry_reuse`.

No Meshy call is required or authorized for this asset. Parent should bind subunit `disaster_wardens` to the vanilla `infantry` model consumer, which resolves to `infantry_rifle_entity` / `generic_western_european_rifle_infantry_mesh`. Historical custom-model evidence is superseded and retired. Parent owns the `override_model` change, manifest gating, and live validation.

The former custom route is intentionally not a completion candidate: the accepted Meshy 7 geometry and firearm gate were never followed by a terminal rig/action lineage, so no custom `.mesh` or `.anim` is promoted. The old provider task records, rejected checkpoints, counters, and sourced audio remain quarantined under the Africa asset job root for provenance only. This handoff contains no new provider spend and no runtime/GFX/sound-definition edit.
