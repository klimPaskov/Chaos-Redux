# Event 012 Africa priority-member terminal cleanup handoff — 2026-08-01

## Scope

This tranche closes the terminal caller for the existing priority-member runtime cleanup helper. It does not add a country tag, cosmetic carrier, model, store, or recurring scan.

## Runtime owner

`common/on_actions/012_africa_world_order_on_actions.txt` now calls `africa_priority_member_cleanup_runtime` from `on_annex` when the annexed victim still carries `africa_priority_member_package_active`. The call is ordered after the reserve capital-loss owner, coercive-annexation achievement owner, and world-package actor-loss owner, so those consumers read the immutable proof state before transient package state is cleared.

The cleanup removes the active withdrawal mission, package variables, and relationship/withdrawal runtime flags. It deliberately retains lifetime origin, political-route, settlement, completion, and victory proof flags defined by `common/scripted_effects/012_africa_priority_member_effects.txt`.

## Acceptance boundary

Static source ownership is now present for the annexation terminal. Ordinary peace and capitulation do not invoke this cleanup path, and no broad country scan or replacement tag is introduced. A live annexation consumer pass remains open, as does the wider thirteen-reachable-package acceptance gate.

## Validation

The touched on-action file remains balanced by the repository brace/quote scan and has no blocking diagnostics in the focused HOI4 event inspection. Hearts of Iron IV was not launched; live consumer validation remains user-owned.
