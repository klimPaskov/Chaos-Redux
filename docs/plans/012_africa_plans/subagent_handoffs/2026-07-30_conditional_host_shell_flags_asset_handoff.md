# Event 012 African host-shell flag presentation handoff

Date: 2026-07-30.

This handoff records the Event 012 presentation dependency identified by the African country-package audit. The existing Event 006 host shells `HZX`, `EUX`, and `ELX` now have complete base flag ladders for any future approved host instantiation.

## Runtime coverage

| Tag | Identity | Normal | Medium | Small |
|---|---|---|---|---|
| `HZX` | Basotho / Basutoland host shell | `gfx/flags/HZX.tga` | `gfx/flags/medium/HZX.tga` | `gfx/flags/small/HZX.tga` |
| `EUX` | Eswatini / Swaziland host shell | `gfx/flags/EUX.tga` | `gfx/flags/medium/EUX.tga` | `gfx/flags/small/EUX.tga` |
| `ELX` | Zanzibar host shell | `gfx/flags/ELX.tga` | `gfx/flags/medium/ELX.tga` | `gfx/flags/small/ELX.tga` |

Source masters, prompts, processing evidence, validation, and the permanent asset manifest are under `docs/assets/006_independence_wave/conditional_host_shell_flags_2026_07_30/`.

## Integration boundary

The flags are dormant shell presentation assets only. They do not create or rename tags, add history, assign map state, set capitals, add cores, create a priority-member origin, or provide a release fallback. Keep the Event 012 host eligibility requirements and Event 006 map-binding ledger authoritative. No `.gfx` sprite registration is needed because HOI4 resolves country flags by tag filename.

## Status

Complete asset package; no remaining asset blocker. Host-shell playability and materialization remain intentionally unresolved and parent-owned.
