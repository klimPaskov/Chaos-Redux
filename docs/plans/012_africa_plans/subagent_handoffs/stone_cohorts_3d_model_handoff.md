# Stone Cohorts 3D Model Subagent Handoff

## Outcome

The package is `blocked` at the mandatory context-complete intake gate. The Meshy API-key check passed, but the bounded brief omitted required scale, consumer, sound, counter, dependency, and paid-recovery inputs. No provider, web-audio, counter-art, Blender, exporter, or runtime operation was attempted.

## Files created

- `docs/assets/012_africa/models_3d/stone_cohorts/job.yaml`
- `docs/assets/012_africa/models_3d/stone_cohorts/manifest.md`
- `docs/assets/012_africa/models_3d/stone_cohorts/validation/intake_blocker.md`
- `docs/assets/012_africa/models_3d/stone_cohorts/runtime/handoff.md`
- `docs/plans/012_africa_plans/subagent_handoffs/stone_cohorts_3d_model_handoff.md`

## Completed evidence

- First process check: `MESHY_API_KEY` was present and non-blank; its value was not exposed.
- Required offline wiki core pages plus Graphical Asset Modding and Entity Modding were opened before asset-file work.
- `chaos-redux-3d-model-pipeline`, `chaos-redux-event-assets`, and `chaos-redux-subagents` were applied.
- The deterministic working root and exact handoff path were created.
- Credits estimated and consumed: `0` and `0`.

## Blocking inputs required from the parent

1. State the exact asset profile.
2. Pass exact installed vanilla infantry/cavalry mesh, asset/entity paths, measured source height, entity scale, effective runtime height, axes, contact plane, and expected scale relationship.
3. Pass the exact runtime unit/subunit consumer id.
4. Name the sourced-audio role to produce, exact vanilla sound/voice/entity precedent, and action/frame or phase synchronization requirement.
5. Pass every exact counter consumer/token, required states/sizes, final sprite and DDS targets, installed vanilla definition and DDS paths, matching skill-local family, and icon-artist handoff path.
6. State the baseline planned paid operations.
7. State the maximum extra-recovery credits and extra paid attempts allowed after a failure or rejection.
8. Name the dependency lock explicitly.

## Provider, Blender, export, and checksum results

- Provider task lineage: none.
- Credits consumed: `0`.
- Blender and extension versions: not inspected after intake failure.
- Geometry/material/rig/weight/action results: blocked; no accepted candidate exists.
- `.mesh` and `.anim` files: none.
- Reimport/parse proof: none because no exports exist.
- Source/final texture hashes: none because no reference or provider source exists.
- Audio URLs, attribution, licenses, files, transformations, and checksums: none; research was blocked before browsing.
- Counter consumer inspection, green-palette evidence, art outputs, and checksums: none; exact required paths and tokens were absent.
- Runtime synchronization: not performed; there is no selected source and parent owns active runtime copying.

## Meaningful validation skipped

Dependency-lock verification, live Meshy schema inspection, balance checks, reference preflight, vanilla model measurement, counter DDS inspection, web audio source review, provider geometry review, Blender multi-view QA, topology/material/rig/action checks, export, and reimport were skipped because the intake gate failed. These checks cannot be treated as passing.

## Parent-owned remaining work

Provide a context-complete follow-up brief, then rerun this bounded package from the dependency-lock gate. After source production, the parent must still wire `.asset`, entity, GFX/counters, sound definitions, gameplay, localisation, runtime copies, live consumers, and in-game validation. No gameplay-readiness setter was created.

## Simplifications and fallbacks

None. The requested model was not replaced with a static asset, older Meshy model, guessed scale, generated or placeholder audio, copied counter, or alternate export route.
