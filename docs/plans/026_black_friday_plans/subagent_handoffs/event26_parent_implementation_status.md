# Event 26 parent implementation status

Audit date: 2026-08-30.
Documentation refresh: 2026-09-02 against the current worktree; this refresh changes documentation only.

## Implemented source surfaces

The old desert Event 26 source and localisation file are replaced by `events/026_black_friday.txt` and `localisation/english/026_black_friday_l_english.yml`, while `chaosx.nr26.1` remains the canonical entry. The event is registered as Minor Fire-Once, assigned the Gathering Storm/tier-one requirement, and intentionally omitted from the default reworked-enabled allowlist.

The actorless reservation, Friday activation, same-day activation, Chaos-change hook, event-system resume hook, disable/re-enable handling, manual and force dispatch, tag/join status refresh, terminal cleanup, one-day expiry, report broadcast, event-log history, Evolution I mapping, debug selectors, and CXT carrier wiring live in the Event 26 effect/trigger/on-action files and the shared event-system integration points.

The universal cost source framework registers family-mask sources by deterministic priority, composes ratios, rounds positive components upward to a registered quantum, exposes native quote/affordability/payment/credit helpers for the supported resource kinds, and persists payer-scoped transaction receipts for settlement and refund. The bounded owner adapters now cover the five Communist-spread logical actions, seventeen reachable Fury decisions, the Japan chemical campaign attack, biological medical-capacity expansion, CBRN civilian-shelter movement, two Japan biological campaign agents, four Germany Mengele command-power actions, the D'Rhondan alien-infantry landing reservation, ten Random Faction paid actions, and the five-component Africa Elephant logistics contract. The exact source and transaction contract is documented in `docs/systems/universal_cost_modifier.md` and the adjacent helper references.

Native country and unit-leader sale modifiers, required report/idea/achievement art, event list and Event Details localisation, the Event 26 achievement contract, and the authoritative catalog row are installed. The catalog exporter has regenerated all three CSV snapshots and the row remains `Needs Testing`.

## Current completion blockers

The current source scan found 2,224 Chaos Redux custom-cost consumer occurrences and 2,225 custom-cost text references across 81 decision files, plus 1,328 native decision cost declarations across 62 files, with 1,033 declarations not written as the literal `cost = 0` and 295 literal zero/free sentinels. The one-count difference is the intentional text-only disclosure at `common/decisions/020_black_plague_rat_decisions.txt:923`; the nuclear-bomb inventory is paired at its current 63/63 count, and the eight Brilliant Scientist technology-action rows are paired. Bounded adapters now cover 104 logical components across ten owner tranches: the five Communist-spread logical actions, seventeen reachable Fury decisions, the Japan chemical campaign attack, biological medical-capacity expansion, CBRN civilian-shelter movement, two Japan biological campaign agents, four Germany Mengele command-power actions, the D'Rhondan alien-infantry landing reservation, ten Random Faction paid actions, and the five-component Africa Elephant logistics contract; the remaining owners still do not.

Engine-inaccessible or not-yet-adapted surfaces include native one-time decision costs, equipment-upgrade XP, guarantees, operation costs, flat license-purchase fields, special projects, assignable unit-leader traits, flat leader and preferred-tactics cost fields, factory/dockyard commitments, stability/war-support commitments, custom currencies, MIO assignment and policy costs, and scripted GUI purchases. Event 26 does not install a generic MIO route, and no live MIO-panel evidence exists. The installed equipment, module, and unit design-type fields are natively covered in both sale snapshots, with exact 195-equipment, 312-module, and 157-unit inventories recorded in `event26_design_modifier_inventory.md`. The remaining surfaces and all bounded component rows are recorded with evidence in `event26_cost_surface_registry.md`, `event26_cost_surface_custom_inventory.md`, and `event26_cost_surface_native_inventory.md`.

The installed HOI4 MCP event route returned selector-scoped partial artifacts with workspace-wide diagnostics. An earlier pure-acknowledgement warning was addressed in the current report option with an idempotent status-refresh effect, but no warning-clearing comparison artifact exists. The current probability pass proves only the one report acknowledgement option at 100 percent in the eight named scenario fixtures; no required owner candidate pools are exposed, so a comparison pass cannot be honestly claimed. Live time, displayed-price, payment, save/reload, multiplayer, and user-owned in-game validation have not been performed.

The reservation path now relies on candidate rejection while reserved and leaves the stored event weight unchanged, so disable/re-enable cannot strand Event 26 at a synthetic weight. Natural selection resolves the current timer only while the reservation remains pending; a Friday activation that clears the reservation does not recalculate that timer. A persistent reservation-origin flag preserves natural-selection provenance for delayed Friday activation across transient dispatch-mode cleanup and save/reload.

Event 26 must stay disabled by default. Do not re-enable it, freeze the registry to a commit, or claim universal coverage until these blockers are resolved with owner-level adapters/static variants or exact engine evidence.

The requested isolated follow-up audits for Event 5, Event 6, and Event 15 were attempted with `fork_context=false`, but none returned a reviewable handoff or a source-provable adapter closure. Their unsubstantiated helper edits were removed, so those owners remain in the blocked registry.
