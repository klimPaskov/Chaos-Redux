# Event 23 near-completion contract closure

Date: 2026-09-19.

Owner: parent implementation agent.

Disposition: implementation tranche closed at source level with explicit live-engine and MCP evidence blockers retained.

This handoff closes the accepted near-completion addendum after the parent reviewed the current source, the postpatch decision/mission audit, the postpatch event/localisation/architect audits, the asset package, and the workbook surface.

## Contract dispositions

| Contract | Disposition | Current evidence | Remaining limit |
| --- | --- | --- | --- |
| C1 shared action boundary | implemented; live evidence blocked | `common/scripted_effects/023_sov_nuclear_bombs_runtime_effects.txt` owns one reservation, native `launch_nuke`, exact state/nonce callback correlation, expected native stockpile-after validation, disputed-delivery disposition, and a persisted 14-day SOV-only no-callback closeout to disputed/missing custody; timeout clears the correlation so a late callback cannot double-commit | Native callback order, mixed-stockpile attribution, and live save/reload remain unobserved because HOI4 was not launched. |
| C2 settlement and exchange receipts | implemented; live evidence blocked | Reciprocal major-use receipt, human Evolution III world-gate first-use route, Evolution IV AI severe first-use gate, one nonterminal super-event, and stand-down records are wired in Event 23 and shared neutral hooks | MCP event execution/compare and live reciprocal timing remain unavailable. |
| C3 custody and conservation | implemented; live evidence blocked | Global/site/actor buckets now include reserved and disputed handling, Event 5 pre-transfer snapshots carry generation receipts, pending native reservations remain attached to their source state, breakaway operationalization requires the native-custody-removed receipt even after SOV disappearance, and annex cleanup moves unresolved reservations to missing custody. If SOV disappears before that receipt exists, the site deliberately remains transferred physical custody and cannot auto-operationalize. | Save/reload conservation and engine callback semantics still need live confirmation. |
| C4 decisions, missions, and ordinary presentation | implemented; render evidence blocked | Source audit keeps the intended phase action cap, active mission cap, four cost-type cap, fail-closed endpoint validation, response-mission serialization, dynamic phase/site/target/deadline header, and locked reserve-dispersal source | Native production render remains clipped/partial in the installed MCP representation, so visual alignment and live mission lifecycle are not certified. |
| C5 reactor and production progression | implemented; consumer evidence blocked | The baseline and +75/+100/+125/+200 ladder, reactor entitlements, bounded production maintenance, and disabled-evolution no-op behavior are source-wired | Native construction-job identity, queue timing, and live reactor persistence remain unverified. |
| C6 documentation, assets, workbook, and audits | implemented; external acceptance blocked | Asset manifest/GFX handoff, DDS round-trip notes, unique licensed/public-domain audio, Event Details/evolution/localisation docs, authoritative workbook, CSV export, and current audit handoffs are present. The fresh completion audit also caused the workbook status validation list to be aligned with its Legend and the Event 23 Details/Evolution cells to be synchronized to the final localisation before CSV regeneration. | Probability compare has unresolved nested inputs, full event MCP scans are partial or timed out, live asset/audio consumer review is pending, and the standalone technology viewer is absent from the installed package. |

## Decision and mission repairs included in closure

Back Down is unavailable while the ultimatum response mission is active.

Reserve dispersal selects and locks one valid source depot when the player-selected destination is accepted, and it fails closed without changing the ledger if either endpoint is invalidated.

Collapse command, disputed-depot selection, recovery, disablement, compromised-site destruction, and dismantlement availability now mirror their lifecycle, custody, and no-active-project gates.

Invite Observers now respects the shared active-project lock.

The narrow-demand AI modifier now reaches its intended maximum-demand case.

The ordinary category header reports actual remaining mission timeout through the vanilla `days_mission_timeout@...` dynamic variable family.

## Validation boundary

The parent reran the bounded source structure check after the code tranche and found balanced braces, no forbidden comparison operators, and no unterminated quoted strings in the touched Event 23 script files.

The required offline wiki and installed vanilla documentation were read before source changes, and vanilla nuclear, decision, mission, event, callback, and dynamic-timeout precedents were consulted.

The read-only Event MCP routes returned partial inspection/render evidence without a current full lifecycle graph, and event comparison could not use the uncached dirty revision.

The read-only probability route returned source/score evidence with unresolved nested state bindings and no same-revision compare result.

The asset audit opened the event, category, decision, mission, idea, and achievement review images and verified processed-to-DDS round trips and runtime GFX paths; live HOI4 consumer review remains pending.

No unapproved fallback, new tag, portrait, flag, focus tree, custom unit, 3D model, counter, animation, scripted GUI, Arms-race cluster, Event 23 world-end row, or Event 23-owned Fallout path was added.

## Parent completion boundary

The source implementation is ready for the focused commit, but the overall acceptance claim must retain the live HOI4, save/reload, native callback, MCP probability-compare, and ordinary-decision render limitations above.

Parent repair after the closure audit: `sov_nuclear_bombs_action_major_first_use_gate_is_valid` now requires the explicit exchange world gate for confirmed enemy use, so enemy-use evidence cannot bypass the world-state branch. `sov_nuclear_bombs_event_target_is_nuclear_major` classifies a major by owner/controller, installed atomic capability, and nuclear technology rather than requiring an unspent stockpile, so a major that expends its last device remains a valid nuclear-major endpoint for the receipt gate.

Parent repair after the post-final read-only audit: human Evolution III first use now requires only the explicit exchange world gate, while the Evolution IV severe-loss branch remains AI-only for first use without prior enemy detonation. Breakaway operationalization now requires the native-custody-removed state receipt regardless of whether SOV still exists. A persisted SOV-only callback-age counter closes a native request that receives no callback after `constant:sov_nuclear_bombs_tuning.native_delivery_timeout_days` by committing the reserved device once to disputed/missing custody without replaying shared consequences.

Parent disposition of the remaining SOV-extinction question: an incomplete native-custody transfer remains a non-operational transferred ledger cohort after SOV disappears. No native stockpile grant, automatic operationalization, or substitute delivery route is created by disappearance; only a later verified custody, recovery, or dismantlement action can resolve that cohort. This preserves valid local custody tracking while keeping the breakaway launch boundary closed.
