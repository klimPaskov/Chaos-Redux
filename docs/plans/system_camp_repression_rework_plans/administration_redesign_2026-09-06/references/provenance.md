# Camp administration GUI reference provenance

Status: accepted regenerated reference package for native implementation and MCP comparison.

Acceptance basis: the user approved the regenerated direction, requested that the positive economic contribution use dark green, and requires the scripted-GUI workflow to create an ImageGen reference before native implementation and then autonomously compare MCP renders against it.

Generation date: 2026-09-12.

Source mode: official built-in ImageGen. The Overview and Locations compositions were generated as independent full-canvas references. The crisis composition was regenerated as a focused state of the same visual family. The final Overview was edited from the accepted Overview candidate so only the `20%` economic contribution changed to a restrained dark positive green. The built-in tool exposed no selectable backend model, so this record does not claim an unreported Flare or Sunburst execution; the skill retains Sunburst as the required named route whenever the active tool exposes model selection.

All three final files are opaque 1672x941 RGB compositional references. They are kept at generated resolution so native implementation can inspect typography, padding, hierarchy, control faces, painted bounds, and surface texture without introducing a downsampling pass.

The references use a restrained HOI4 document-and-steel visual language with three tabs, `Overview`, `Locations`, and `Records`. Their generated labels and values define hierarchy and density; native runtime controls and localisation replace every interactive or dynamic element.

## References

### Overview

File: `camp_admin_administration_reference.png`

The selected Overview tab presents three immediate monthly summaries, three national directive rows, one policy button, one economic project, and one institutional program area. The positive `20%` economic contribution is dark green, monthly deaths remain a muted red, and the screen avoids permanent cost grids, raw variable labels, and generic status text.

Initial ImageGen source: `C:\Users\klimp\.codex\generated_images\01a06e59-6833-78d3-b115-dd6227e7761e\exec-3edd1e63-09e4-4955-bb25-7bf0d7c927b3.png`.

Focused green-value edit: `C:\Users\klimp\.codex\generated_images\01a06e59-6833-78d3-b115-dd6227e7761e\exec-6c97136c-29b8-46be-bcf3-2e7cebe8067d.png`.

Final SHA-256: `4DEE417454685B4AD55BE57FA758CF1B276A3A3E313AEC2A5BD71E3E16FD2D40`.

### Locations

File: `camp_admin_locations_reference.png`

The selected Locations tab uses a left registry with a compact role/status row for each site and a right dossier for the selected location. The dossier keeps the map, primary role, detained population, economic contribution, and exactly three contextual actions visible without repeating costs beneath the buttons.

ImageGen source: `C:\Users\klimp\.codex\generated_images\01a06e59-6833-78d3-b115-dd6227e7761e\exec-fdae2c1c-d44b-4211-89f5-6838d4d6a2c7.png`.

Final SHA-256: `975F23C69A4570F231EFF545E1D1A4013C9839B1AF2B7F91CDF1904BF4D9C99B`.

### Crisis

File: `camp_admin_crisis_reference.png`

The crisis state preserves the Overview behind one focused `Supply Commitments` modal. It reports the material problem, number of affected locations, suspended project, and remaining reserve duration, then offers exactly two consequential responses: `Adjust Budget` and `Pause Development`.

ImageGen source: `C:\Users\klimp\.codex\generated_images\01a06e59-6833-78d3-b115-dd6227e7761e\exec-7079e10c-61a4-48bf-a87d-ad7fe910319c.png`.

Final SHA-256: `8D8E357ADEF8AFC4101CECFDF4D7459865466658740274EE5C0ADEBD701E9374`.

## Handoff boundary

These are reference images only. The parent owns the native `.gui`, `.gfx`, localisation, gameplay, and MCP implementation. No DDS conversion is supplied because the references are compositional guides. The native build should follow their hierarchy, spacing, visual density, status colours, and interaction budget while adapting decorative art and generated text to supported HOI4 elements. Matching MCP renders must be reviewed at the required states and resolutions until visible alignment, scaling, clipping, overlap, background, hover, and click-region defects are resolved.
