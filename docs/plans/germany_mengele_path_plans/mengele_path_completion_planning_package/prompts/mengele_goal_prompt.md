Finish the Germany Mengele path to full Chaos Redux standards. Use the extracted package at `docs/plans/germany_mengele_path_plans/mengele_path_completion_planning_package/` or the verified repo-specific plan folder.

Read first: `AGENTS.md`, `chaos-redux-events`, `chaos-redux-event-planning`, `chaos-redux-super-events`, `chaos-redux-event-assets`, `chaos-redux-improvement-loop`, `chaos-redux-subagents`, `chaos-redux-decisions-missions`, and `chaos-redux-focus-trees` if the clone army tree is touched. Read the required offline Paradox wiki pages and vanilla documentation before editing.

Then read these package files: `specs/current_implementation_map.md`, `specs/mengele_path_completion_spec.md`, `specs/mengele_super_event_acceptance_criteria.md`, `specs/mengele_improvement_loop_addendum.md`, `matrices/mengele_event_inventory_matrix.md`, and `matrices/mengele_test_matrix.md`.

The uploaded source says this is a Germany gameplay chain in `events/germany_mengele.txt`, not a random-event pool entry. It starts when fascist Germany controls Auschwitz after 1940.06.13. Auschwitz is state `88`, province `9412`. Verify this in the repo before patching.

Finish all connected `germany_mengele.*` events, effects, triggers, constants, decisions, ideas, AI, focus tree content, special projects, opinion modifiers, localisation, assets, super-events, docs, and spreadsheet surfaces. The event inventory must include `.1`, `.10` through `.14`, `.17`, `.20`, `.22`, `.23`, `.24`, `.37`, `.38`, `.40`, `.120`, `.121`, and every additional repo event in the namespace.

Non-negotiables: keep the path tied to genocide-crisis discovery rules, Deaths integration, hidden internal damage, concrete evidence exposure, and delayed external condemnation. Do not make hidden atrocities create automatic passive foreign condemnation while the regime controls the site unless another concrete evidence route exists. Do not treat Nazi racial claims as true. Do not use gore, victim spectacle, perpetrator glamour, or joke rewards.

Finish the Angel Directorate super-event. The current source names slot/audio ID `12`, `GFX_super_event_angel_directorate`, `sound/003_holy_realm/super_event_12_angel_directorate.wav`, and `chaosx_super_event_angel_directorate_track`. It also says the registered image currently contains default super-event art, so replace it or prove it was already replaced. Research real quote and button text through `chaosx_super_event_text_researcher`, verify or replace audio through `chaosx_super_event_audio_researcher`, and create final image assets through the proper asset subagent. Use settings-aware playback and prevent duplicates.

Verify the later `Angelic World Order` world-end branch and `Aryan Supremacy` title variant if live. Test the normal path, forced path, duplicate prevention, discovery consequence, hidden deaths before discovery, decision cleanup, AI validity, super-event UI, audio format, event log, docs, and spreadsheet alignment.

Run the improvement-loop pass near completion with `chaosx_improvement_loop_planner` and `fork_context=false`. Resolve its addendum or closure handoff before any completion claim. Run localisation, decision, focus, country package, documentation, spreadsheet, and final completion audits as relevant.

Keep iterating until the implementation satisfies the spec. Do not claim completion while placeholders, default art, undocumented audio, missing localisation, dead decisions, stale docs, unresolved plans, untested route gates, or skipped meaningful validation remain.

Character count: 3531
