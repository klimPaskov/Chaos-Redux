# Base zombie regeneration: dependency gate handoff (2026-09-19)

## Current documentation disposition: clean final v3, 2026-09-20

Disposition: **implemented as an offline runtime candidate, with validation limits**. This top section records the parent-supplied final evidence; the dated worker and parent continuations below remain historical. It supersedes their claims that the dependency gate, topology, winding, action deformation, death contact, or material preparation are still blocking the current export, and supersedes their older “final” mesh, action, material, runtime hash, and 35-credit claims. It does not constitute live HOI4 or listening acceptance.

The source-of-truth map is: model and UV source `docs/assets/002_zombie_outbreak/models_3d/zombies/blender/checkpoints/93_clean_uv_final_v3.blend`; locked exports `docs/assets/002_zombie_outbreak/models_3d/zombies/export/mesh/chaosx_zombies_clean_final_v3.mesh` and `export/anim/clean_final_v3/{attack,defend,idle,move,retreat,support_attack,training,death}.anim`; material source `export/runtime_materials_v6/`; canonical runtime `gfx/models/units/chaosx_zombies/`; concise evidence index `docs/assets/002_zombie_outbreak/models_3d/zombies/validation/clean_final_v3_summary.json`; package status `docs/assets/002_zombie_outbreak/models_3d/zombies/manifest.md`. Relative export and material paths in this map are under the zombie asset root.

Checkpoint 93 derives from checkpoint `91_death_backface_repair_v2.blend`, which added 37 death-pose face flips and left already-correct faces `10418` and `10911` unchanged. The source has one UV layer, 30,436 triangles, and 15,021 vertices. The final runtime mesh SHA-256 is `342FF7A8F28AD07D04C70A4686A064602DB03E9C65E6893E1032F6E901F2D698`; the diffuse, normal, and specular DDS hashes are `EFFE1B8D9D2013E6DBF680AC344F0943EDACF45B3AAA3D457856D77A763D7DA3`, `143BCFEB0C4827CE25CAAEA9E2558B12C15C7446656A5EACD8CEF1C01ABD44D0`, and `1BA6E652A9ED2F3E375BE6FA09585C65D65B6183D89B79ACFC6D8B62FE0A5267D0`. The smart-project/packed UV evidence is `export/runtime_materials_v6/uv_overlap_report.json`: 3,263 cross-category overlap texels out of 334,514 covered (0.975%), zero four-way overlap, versus 91.58% in the earlier layout.

Actual-byte action and mesh reimport proofs are `validation/reimport_clean_final_v3_{attack,defend,idle,move,retreat,support_attack,training,death}.json` and `blender/checkpoints/reimport_clean_final_v3_*.blend`. The adapter export report is `logs/adapter/add82a35f9af4f2586d92f07a7fce1ee.result.json`; material previews are `blender/previews/runtime_materials_v6_*`, and culling source audits are `blender/previews/orientation_audit_v5_*`. The attack, move, and retreat close culling comparisons have `0`, `7`, and `0` pixels respectively above the 40-difference threshold. Death frame 37 retains `612` such pixels on two folded inner faces; this is an explicit visual limit, and the parent must decide whether to accept it after final review.

The Meshy attempt ledger closes at **40 credits**: 30 body, 5 remesh, 5 rig; no paid animation attempt followed the rejected rig. The canonical rig and actions are Blender recovery work. User-owned audio listening and live HOI4 validation remain pending; the earlier audio provenance, contact-timing, and wrapper issues below were not reaudited by this documentation pass. No gameplay, GFX, entity, sound, binary, or spreadsheet files were edited for this reconciliation.

## Status and ownership

**Blocked before provider work.** This handoff covers `chaosx.event002.zombies` at `docs/assets/002_zombie_outbreak/models_3d/zombies/`. The parent owns active runtime, GFX, entity, sound-definition, gameplay, and final review. No new-lineage model or animation may be promoted from this handoff.

## Source selection and lineage

- Source mode: `reference_only_user_authorized`; the user approved a Blender-render reconstruction after asking for closer fidelity to the in-use dark-olive base zombie. `job.yaml` and `refs/original/input_manifest.json` record that approval and `pose_preparation_mode=blender_render_reconstruction`. The exact native ImageGen prompt is absent from those current records, so prompt-level provenance is still unresolved.
- Sole selected provider input: `refs/original/meshy_input.png`, 1024×1536 RGBA, SHA-256 `3f9e12f20c4d9e5533ccc3af7a59a8c994bc77c3c77801f9ba17743edf796f8b`. The on-disk hash matched the parent-provided hash. The approved derivative is `refs/derived/base_zombie_meshy_reference_blender_reconstructed_dark_olive_candidate_20260919.png`; the manifest names current Blender front and three-quarter renders as design sources. The earlier provider image is retained as `refs/original/meshy_input_prior_20260919.png`.
- Design to preserve: narrow adult male, short dark hair, gray-green skin, dark desaturated olive work jacket and trousers, charcoal shirt, dark boots, closed clothing surfaces, relaxed front-facing A-pose, no firearm or prop. The exact source-to-prepared visual comparison was not repeated after the dependency stop.
- All files already in `provider/tasks/`, `provider/downloads/`, `provider/requests/`, and `provider/responses/` predate this approved-input lineage and are superseded evidence. New-lineage task IDs, response IDs, downloaded artifacts, and artifact checksums: **none**.

## Lock and route gate

The process `MESHY_API_KEY` was nonblank; its value was never read or recorded. `.tools/3d_pipeline/config/dependencies.lock.json` requires official `@meshy-ai/meshy-mcp-server` 0.4.0 (git head `d8c77d1cb897e345eb41d38b510b8391b1664346`) and `@modelcontextprotocol/sdk` 1.29.0 (git head `e12cbd7078db388152f6e839abdbe09ba01f3f32`), both of whose installed package manifests matched their locked versions. The locked SDK server entry points exist. The repository lock and tool schema require `ai_model=meshy-7`, one of `file_path`, `image_url`, or `input_task_id`, no multi-view input, and a paid `meshy_image_to_3d` call; this run did not issue a fresh live `tools/list` proof or paid call.

The locked `chaosx_blender_hoi4` adapter version and config both report 1.10.49; operation arrays agree, and every locked adapter source SHA-256 matched the on-disk file. The installed Blender executable reported 5.1.2, and the Blender Lab repository checkout was at the locked `03004fd0216bfe5e0a3d9ac9b47d5efadc3d78c4` head. The `io_pdx_mesh` 0.91.0 installed manifest matched its locked version, and the archived extension SHA-256 matched `A683DF08318CB700014C7FE9A3D15139E5FB2313C7E98715204263E48931F7C2`.

**Hard blocker:** the installed Blender MCP add-on at `C:/Users/klimp/AppData/Roaming/Blender Foundation/Blender/5.1/extensions/lab_blender_org/mcp/blender_manifest.toml` reports version **0.2.0**; the lock-selected vendor manifest at `.tools/3d_pipeline/vendor/blender_mcp/addon/blender_mcp_addon/blender_manifest.toml` reports **1.0.0**. `127.0.0.1:9876`, the locked bridge socket, was not listening. The developer's dependency gate requires a stop on a version or capability mismatch, without substituting another installation. The bridge was not launched because the installation version already failed the lock. The locked adapter's live health, Blender operation schemas, Meshy wrapper's fresh tool declarations, and Meshy live balance remain unverified for this run. This is `required installation/verification`, not a provider failure.

No unrestricted Blender Python, shell-based Blender modification, alternate provider API, or unlocked exporter was used. The standalone Technology Tree Viewer is unrelated to this model package and was not claimed available.

## Credit and action ledger

The approved baseline is one Meshy 7 textured body (30 estimated credits), at most one supported humanoid rig attempt (5 estimated credits), and at most one attempt for each of idle, move, attack, defend, support_attack, retreat, training, and death (3 estimated credits each), total **59 estimated credits** if all eight action calls are appropriate. Fresh balance: **not checked** because the dependency gate failed first. Credits consumed by this new lineage: **0**. No rig or action attempt allowance was used. The historical 382-credit figure in the old manifest is not a fresh balance.

All eight actions remain **blocked** for this new lineage. No source `.blend`, working checkpoint, custom mesh, texture material, rig, vertex weights, semantic action, rendered action review, `.mesh`, `.anim`, export log, or reimport proof was produced from the new input. The named vanilla crosswalk in `job.yaml` is `western_european_infantry.mesh` source height 7.351824797689915, forward `-Y`, up `+Z`, `infantry_rifle_entity` scale 0.8, and effective runtime height 5.881459838151932. Those inherited figures were not independently remeasured in Blender after the stop and must not be treated as this run's calibration proof.

## Sound and counter status

`evidence/audio/source_ledger.md` contains prior OpenGameArt CC0 candidate pages and direct-download URLs, and existing source ZIPs and extracted clips remain in the job as historical source evidence. This run did not audit the actual source licenses, listen to or select clips, derive runtime audio, calculate current original/derived SHA-256 values, or map exact frame synchronization. Selection, acknowledgement, idle groan, move/retreat footsteps, attack vocal and impact, defend/support contact, injured, and death roles remain **blocked** for new-lineage acceptance. The parent must not wire an older candidate as accepted audio solely from its filename or prior ledger row.

The parent-provided counter consumers are `interface/chaosx_subuniticons.gfx` token `GFX_unit_zombies_icon_medium` to `gfx/interface/counters/divisions_large/zombies_icon.dds` (two frames), and token `GFX_unit_zombies_icon_medium_white` to `gfx/interface/counters/divisions_small/onmap_unit_zombies_icon.dds` (two frames). This run did not inspect the installed vanilla `interface/subuniticons.gfx`, exact vanilla DDS pixels, matching skill-local family, palette samples, or current counter artist output after the dependency stop; bespoke-counter review and `chaosx_icon_artist` handoff remain **blocked** here. Prior counter evidence under the job remains unpromoted. The parent owns final counter art routing and GFX wiring.

## Requirement status and continuation

| Requirement | Status | Evidence or blocker |
| --- | --- | --- |
| Approved one-image source identity | complete | Matching selected-image hash and current `job.yaml`/input manifest; exact ImageGen prompt unresolved |
| Locked dependency gate | blocked | Installed Blender MCP add-on 0.2.0 versus locked 1.0.0; bridge closed |
| New Meshy 7 body and geometry QA | blocked | No new task submitted; 0 credits consumed |
| Eight distinct skeletal actions and Blender playback QA | blocked | No new rig or action task; Blender route unverified |
| PDX textures, `.mesh`/`.anim` export and actual-byte reimport | blocked | No new-lineage Blender or exporter operations |
| Sourced zombie sound package and action sync | blocked | Prior candidates not reaccepted against current actions/consumers |
| Bespoke vanilla-green counter package | blocked | Exact installed-vanilla and current output inspection not performed |
| Runtime copy synchronization | blocked | No final source exports selected; parent owns runtime copies |

After the exact installed add-on version and bridge are verified against the lock, resume from the approved input, fresh Meshy 7 schema and live balance, then start a new provider lineage. Preserve the older tasks and the selected image. The user performs live in-game validation; none is claimed here.

## Changed files

- `docs/assets/002_zombie_outbreak/models_3d/zombies/manifest.md`: appended current approved-source selection and dependency blocker.
- `docs/assets/002_zombie_outbreak/models_3d/zombies/history.jsonl`: appended the blocked gate record.
- This handoff.

No model, runtime, sound-definition, counter DDS, or gameplay file was changed. The missing installed add-on match made Blender rendering, action playback, export, and reimport meaningful validations unavailable; the hard gate made a live balance or paid provider call impermissible. No simplification was adopted.

---

# New lineage continuation and final worker status, 2026-09-19

This section supersedes the earlier dependency-stop status above. **Package status: blocked before model export.** The parent repaired the exact locked Blender MCP add-on and bridge, then the worker ran a fresh Meshy 7 body generation, one recovery remesh, and the bounded adapter weld sweep. Neither body is closed. No rig, action, export, reimport, game-ready sound, or bespoke counter is accepted from this lineage. Existing stage-named `03_rig_approved.blend` and `04_actions_approved.blend` are empty placeholders: the adapter report explicitly says `armatures=[]` and `actions=[]`.

## Source, dependency and calibrated baseline

The only generation input was `docs/assets/002_zombie_outbreak/models_3d/zombies/refs/original/meshy_input.png`, SHA-256 `3f9e12f20c4d9e5533ccc3af7a59a8c994bc77c3c77801f9ba17743edf796f8b`. The exact native reconstruction prompt **was recovered** at `refs/derived/base_zombie_meshy_reference_blender_reconstructed_dark_olive_prompt_20260919.txt`; it names the front and three-quarter Blender renders, A-pose, dark olive jacket/trousers, narrow adult male silhouette, and closed backed fabric wear. `job.yaml` and `refs/original/input_manifest.json` record the user's 2026-09-19 approval and `pose_preparation_mode=blender_render_reconstruction`. This is user-owned current-mod Blender art reconstructed by ImageGen in `reference_only_user_authorized` mode; source/reconstruction are non-shipping evidence. The previous image at `refs/original/meshy_input_prior_20260919.png` and provider tasks outside `provider/new_lineage_20260919/` remain superseded. The approved image is 1024×1536 RGBA with opaque neutral background, full single body, no firearm or prop. The reconstructed reference resolves front-facing pose and palette; its back geometry had to be evaluated in Blender.

The repaired installed Blender MCP manifest is 1.0.0, Blender is 5.1.2, the bridge listens at `127.0.0.1:9876`, and the repository verifier `python -B .tools/3d_pipeline/verify_environment.py --probe-meshy` found no lock mismatches. Live `chaosx_blender_hoi4_health(job_id="zombies")` reported `io_pdx_mesh` 0.91.0 loaded and adapter 1.10.49. The checked lock is `.tools/3d_pipeline/config/dependencies.lock.json`; Meshy schema is `.tools/3d_pipeline/config/meshy_tool_schema.lock.json` and adapter allowlist is `.tools/3d_pipeline/config/blender_hoi4_adapter.json`. Official `@meshy-ai/meshy-mcp-server` 0.4.0 (`d8c77d1cb897e345eb41d38b510b8391b1664346`), MCP SDK 1.29.0, and Meshy 7 identifier were verified. `io_pdx_mesh` 0.91.0 archive hash `A683DF08318CB700014C7FE9A3D15139E5FB2313C7E98715204263E48931F7C2` matched. The process Meshy key was nonblank; its value was never read or logged. The repository lock chose the adapter version; no alternate provider, unlocked Blender Python, direct Blender shell edit, or exporter route was used.

Read-only adapter import of installed `C:/Program Files (x86)/Steam/steamapps/common/Hearts of Iron IV/gfx/models/units/western_european_infantry.mesh` measured object `polySurface106` with collision `pCube1` excluded: source height 7.351824797689915, ground z 0.008419, forward `-Y`, up `+Z`. `gfx/entities/units_infantry.asset#infantry_rifle_entity` uses scale 0.8, so expected effective runtime height is 5.881459838151932. The custom prepared mesh height was 7.351824760437012 with ground 0 and intended entity scale 0.8 once, matching the source rather than applying 0.8 twice. The vanilla mesh copy in job evidence hashes `F00FBADFDACDD1046F7119E62E2C47D644EA7A92D0F686B71D230BC843AEF8BA`.

## Provider and Blender attempt ledger

| Stage | Exact route, task/request and credit evidence | Artifact/result |
| --- | --- | --- |
| Pre-generation balance | Official `meshy_check_balance`: 318 live credits | Preceded paid body tranche |
| Body | Official `meshy_image_to_3d`, task `01a0ba94-9607-729b-92e5-53951c59e8e1`; one `file_path` input; `ai_model=meshy-7`, `target_formats=[glb,fbx]`, `target_polycount=30000`, `topology=triangle`, `should_remesh=true`, `save_pre_remeshed_model=true`, `should_texture=true`, `enable_pbr=true`, `pose_mode=a-pose`, `image_enhancement=false`, `remove_lighting=true`, `multi_view=false`, `alpha_thumbnail=true`; 30 credits consumed | `provider/new_lineage_20260919/downloads/body_meshy7.glb` 11,330,736 bytes SHA `B9CDC71C2D117A4F34BB6BE47457FC81F985A90E2E1A103DDFCFC4D8674BF7CF`; FBX 13,244,300 bytes SHA `07ABEDB0F2DF1F74563AC32CFEB2669DFE450F381CEE41E985C35C28A87FFEB8` |
| Initial Blender prep | Locked `chaosx_blender_hoi4_prepare_candidate`, first configuration used raw provider texture names instead of required diffuse/specular/normal roles | Source checkpoint retained; failed material mapping was corrected in a separate isolated copy |
| Original corrected prep | Same adapter, request `4497ba7979954f2e914d597768235e76`, namespace `provider/new_lineage_20260919/attempt2`; mapped diffuse=`base_color.png`, specular=`roughness.png`, normal=`normal.png` | 30,000 triangles, 14,920 vertices, one mesh, UVMap, zero degenerate faces and zero zero-length normals; **486 open boundary edges**, 93 components, 83 closed/10 branched; hole fill rolled back because it introduced non-manifold geometry |
| Pre-remesh balance | Official `meshy_check_balance`: 288 live credits | Preceded paid recovery tranche |
| Recovery remesh | Official `meshy_remesh` on new body task, triangle target 30,000, GLB/FBX, task `01a0ba9f-8b53-77e5-a132-ea513bcd0672`; 5 credits consumed | `body_remesh_recovery.glb` 19,958,452 bytes SHA `9AD0994538F0C9801DE16E6FA3607416BD0253EFFFFEF202A53D75B5F2CB96706`; FBX 16,595,212 bytes SHA `C6DC8B7B4042CC26DCD15B39628F53F0D12413766A816BF0D06077F55B66E5C3` |
| Remesh prep | Same adapter, request `7ace3f5d7ee643db864942f0bd7556a6`, `attempt3_remesh` | 29,955 triangles, 14,919 vertices, **509 open edges**, 93 components (82 closed/11 branched), zero non-manifold; worse than original and rejected |
| Final bounded weld at `5e-5` | Same adapter `prepare_candidate`, `repair_before_reduction=true`, request `266e9b29c2b84b3fa77181754ba32ccb`, isolated `attempt4_weld_5e5` | **486 open edges**, 93 components (83 closed/10 branched), largest component 33 edges, zero non-manifold: unchanged |
| Final bounded weld at `1e-4` | Same adapter `prepare_candidate`, `repair_before_reduction=true`, request `ee1df818f4cd46a6a8e8b8c22b2ebd6f`, isolated `attempt5_weld_1e4` | **486 open edges**, 93 components (83 closed/10 branched), largest component 33 edges, zero non-manifold: unchanged |

The current body source maps include 2048² base color SHA `366A28C5C073DA4E07A94B2A36CF661E9A505AEF5E98D28734F0F56FB77DE4EB`, metallic `6C0168...`, roughness `3DE72B...`, and normal `979231...`; these are provider PBR maps, not accepted PDX DDS. The original front/left/rear previews at `provider/new_lineage_20260919/attempt2/blender/previews/` looked largely complete, but topology is determinative here. An adapter `review_humanoid_components` wrote a report and three silhouettes under `provider/new_lineage_20260919/blender/`; its oversized response exceeded the 64 MB IPC return limit and `semantic_component_acceptance=false`, so no anatomical component approval is claimed. No screenshot represents animation QA because no action exists.

The repository adapter config/code names `chaosx_blender_hoi4_repair_explicit_mesh_patch`, but the actual MCP tool list exposed to this worker has no callable operation by that name (`typeof tools[...]` was undefined). The available `prepare_candidate` auto hole fill rolled back; weld distances 5e-5 and 1e-4 did not reduce a single open edge. The 486 edge / 93 component topology cannot be mapped confidently to exact anatomical surfaces from the exposed reports, and closing branched loops blindly would risk damage. This is the **exact tool capability blocker** for further locked Blender repair. Per parent instruction there were no further Meshy calls, no unrestricted Blender operations, no rig on an open candidate, and no model export. Total new-lineage Meshy spend: **35 credits**. The observed balance before remesh was 288; 283 is only arithmetic after the reported 5-credit spend, not a fresh balance measurement. One Meshy rig and each of the eight action attempts remain unused.

## Action, export and runtime coverage

| Requirement | Status | Evidence/next prerequisite |
| --- | --- | --- |
| Closed body, hands, rear garments, no disconnected pieces/holes | blocked | Best original still has 486 boundary edges; visual preview cannot certify manifold topology |
| Rig and vertex weights | blocked | No armature, zero weights/action approval; closed geometry and callable exact patch repair route needed first |
| idle, move, attack, defend, support_attack, retreat, training, death | blocked, each | Zero Meshy animation calls and zero manually authored skeletal actions; no static aliases accepted |
| Rest/action screenshots, playback, contacts, frame-1 facing and bounds | blocked | No valid rig or actions; only rest candidate previews exist |
| PDX material/DDS, final `.mesh` and eight `.anim`, actual-byte reimport | blocked | No final closed mesh or actions, no io_pdx_mesh export and no reimport proof/hash |
| Runtime model copy synchronization and in-game consumer | blocked | No final exports; parent owns wiring and user owns live game review |

The existing `gfx/entities/chaosx_zombies.asset` binds defend/support to attack, retreat to move, and training to idle. These existing aliases do not satisfy the eight distinct requested actions; parent runtime wiring remains separate. Proposed final IDs after a viable package are `chaosx_zombies_mesh` and `chaosx_zombies_{idle,move,attack,defend,support_attack,retreat,training,death}`. No new runtime candidate was copied or promoted.

## Audio and counter handoffs

Audio evidence is in `docs/assets/002_zombie_outbreak/models_3d/zombies/evidence/audio/source_ledger.md` and `source_design_20260919.md`. Source pages and direct URLs were reviewed on 2026-09-19. The four prior OpenGameArt originals and new CC0 qubodup `Impact` archive remain under `evidence/audio/original/` with creator, terms, hashes, extracted candidate hashes and clip metadata in that design document. The new `original/qubodupImpact.7z` has SHA-256 `98D72B55D6F78A32072439ED4D052FC43FDD80B8B409D18A9BB3150FC2CCD97B`; its `qubodupImpactMeat01.ogg` and `Meat02.ogg` are 0.5-second candidate contacts. Five mechanical Opus review previews, not runtime assets, are under `evidence/audio/previews_20260919/`. The agent could not listen through the available tool output, so all sonic-role choices require review and none is accepted as final. No final game-ready audio, `sound.asset`, `soundeffects.asset`, entity change, or exact frame synchronization was produced. The role/sync table in `source_design_20260919.md` covers idle, footsteps, attack vocal/impact, defend/support contact, hurt, death, and selection/acknowledgement; frame points remain blocked until distinct actions exist. Vanilla unit/entity sound precedent is `units_infantry.asset#infantry_rifle_entity` → `infantry_move_animation` in `soundeffects.asset` → variants in `sound.asset`; `vo.asset` uses country/infantry scoped selection and does not prove a zombie-only subunit selector. Stable soundeffect IDs are proposed in the audio design document for parent wiring.

The installed vanilla `interface/subuniticons.gfx` infantry family was inspected with exact DDS files and skill-local land counter contact sheets. Vanilla large `gfx/interface/counters/divisions_large/unit_infantry_icon.dds` SHA `B33A8E3B69CC789EB0E31BA99F4E5BA4E5B0A8B51EC1A7A7F709C3516F720C23` is 152×42, two 76×42 frames; its primary green includes RGB (73,106,73), (74,107,74), (83,114,83), (100,128,100) with dark outline and transparent outside. Vanilla small `gfx/interface/counters/divisions_small/onmap_unit_infantry_icon.dds` SHA `58AB78662C2A64A519B8D5D144582E7B2785915BD0A0A822696D87A9DE6F766C` is 60×12, two 30×12 frames, pale/white infantry mark with black outline and transparent outside. Both have nontrivial alpha; frame 2 shows the pale/white alternate map/counter state. The matching skill families are `.agents/skills/chaos-redux-event-assets/assets/vanilla_reference/units/land/counters_large/contact_sheet.png` and `.../land/map_counters/contact_sheet.png`. The old `counters/source/zombies_icon_source.png` does not prove a bespoke current-design processed counter. The `chaosx_icon_artist` brief is one original, closed-jacket, narrow zombie silhouette with exact vanilla canvas, frame order, alpha, border and sampled palette, producing source/processed PNG, two-frame DDS, comparison and gfx handoff. The exact parent consumers are `interface/chaosx_subuniticons.gfx`: `GFX_unit_zombies_icon_medium` → `gfx/interface/counters/divisions_large/zombies_icon.dds`, two frames, and `GFX_unit_zombies_icon_medium_white` → `gfx/interface/counters/divisions_small/onmap_unit_zombies_icon.dds`, two frames. **Bespoke counter output remains blocked/pending artist; no generic or vanilla duplicate is accepted.** Parent owns artist routing and GFX review.

## Files, limits and parent continuation

Worker-owned new files/paths include `provider/new_lineage_20260919/` requests, responses, downloaded GLB/FBX and texture evidence, isolated `attempt2`, `attempt3_remesh`, `attempt4_weld_5e5`, `attempt5_weld_1e4` adapter checkpoints/reports/previews, `evidence/audio/original/qubodupImpact.7z`, `evidence/audio/extracted/qubodupImpact/`, `evidence/audio/previews_20260919/`, and `evidence/audio/source_design_20260919.md`. This handoff and append-only `manifest.md` / `history.jsonl` were updated. No gameplay, sound-definition, localisation, GFX, GUI, entity, `.asset`, event, focus, decision, country, history, AI, on_action or spreadsheet file was edited. No Git commit was made for this incomplete package.

Meaningful checks performed: dependency lock/schema and bridge health, exact input and output hashes, new provider statuses/downloads, read-only vanilla import and source/runtime scale crosswalk, adapter topology reports for original/remesh/two weld distances, visual rest previews, license page verification, source archive extraction/hashes, vanilla sound and counter consumer/reference inspection. Meaningful checks skipped because prerequisites failed: anatomical open-edge patching through an exposed callable operation, accepted material/DDS QA, weight/rig/deformation, eight-action playback/render/contact QA, io_pdx_mesh export, actual-byte reimport, audio listening and frame sync, bespoke counter comparison, and live game review. The precise next gate is an exposed, lock-verified explicit mesh-patch operation that can close the 93 boundary components without introducing non-manifold geometry; only then can the authorized one-attempt rig/action route and export continue. The model package remains **blocked**, audio **needs_user_review/blocked for sync**, counters **blocked/pending artist**, and parent wiring **blocked by missing final assets**. No in-game completion is claimed.

---

# Corrected v2/v5 package continuation and parent closeout, 2026-09-19

This section supersedes the earlier blocked status. The parent completed the bounded Blender recovery after the Meshy body and remesh both failed topology acceptance. The original Meshy 7 task `01a0ba94-9607-729b-92e5-53951c59e8e1` consumed 30 credits and the one recovery remesh `01a0ba9f-8b53-77e5-a132-ea513bcd0672` consumed 5 credits. No rig or action was paid for through Meshy because the body failed the closed-mesh gate; total new-lineage Meshy spend is 35 credits.

## Accepted geometry and deformation correction

The accepted body checkpoint is `provider/new_lineage_20260919/clean_repair_reprepare/blender/checkpoints/13_region_caps_v1.blend`, SHA-256 `6708F73BA48653FDBD6F6E68C7C9287AF2051556A0C4D1486A640EA3655E6FC3`. Its adapter report `blender/reports/13_region_caps_v1.json` records one 30,438-triangle, 15,021-vertex `Mesh_0.001` body with zero loose boundary edges, non-manifold edges, degenerate faces, and boundary components after the explicit region cap patch. Calibrated height is `7.351824760437012`; runtime scale is `0.8` once, matching the inspected vanilla infantry height and scale crosswalk.

The user identified the visible back/jacket deformation that the first animation review missed. The cause was the v1 arm-first weight boundary. The accepted rig checkpoint is `provider/new_lineage_20260919/clean_repair_reprepare/blender/checkpoints/23_rig_v2_weight_boundary.blend`, SHA-256 `3DDF62DE7F50308D824BF6739096BDAAA56CC8783C2D10EDAAD030A447E1DAA8`. The v2 spec `rig/base_zombie_rig_spec_v2_weight_boundary.json` assigns torso/pelvis regions before narrower arm bounds. Rear and three-quarter v2 action renders show a continuous jacket surface through the shoulder and back; the old triangular collapse is rejected evidence and is not promoted.

## Actions and death recovery

The corrected rig produced distinct locked-adapter actions for `idle`, `move`, `attack`, `defend`, `support_attack`, `retreat`, and `training`. Checkpoints are `blender/checkpoints/24_idle_v2.blend` through `30_training_v2.blend`; source previews are the matching `base_zombie_{role}_v2_*` images and reimport previews are `reimport_chaosx_zombies_{role}_v2_*`. Each action is 0–48 at 24 fps, with role-specific motion and reimport evidence. The exported files are `export/anim/chaosx_zombies_{idle,move,attack,defend,support_attack,retreat,training}.anim`.

The death action was measured through four manual candidates after the legacy vanilla-bone animation failed import. v2, v3, and v4 are explicitly rejected: reimport showed leg-driven triangular pulls. v5 is accepted as the clean runtime candidate because it keys root/spine/chest/neck/head/arms and leaves the legs unrotated. Its spec is `rig/actions/base_zombie_death_v5.json`, checkpoint `blender/checkpoints/39_death_v5.blend`, export report `blender/reports/export_anim_base_zombie_death_v5_76a533db0e.json`, canonical export `export/anim/chaosx_zombies_death.anim`, and reimport report `validation/reimport_chaosx_zombies_death_v5.json`. Reimport frame 49 left and three-quarter captures show no triangular leg spikes. This is a conservative slump/death motion rather than a prone ragdoll.

## Export, reimport and runtime synchronization

The partitioned export checkpoint is `blender/checkpoints/31_export_partitioned_v2.blend`, SHA-256 `951FDC1AB89A3A2A50838E7B4716BDFE6FD0DC80D425F53E66AAD3068C5BFC6E`. `export/mesh/chaosx_zombies.mesh` contains object `Mesh_0.002` split into four streams with 10,161 / 11,340 / 10,730 / 8,633 vertices and 8,000 / 8,000 / 8,000 / 6,438 triangles. The 31 partition report records 24,000 index entries for each of the first three streams and 19,314 for the last, keeping each stream within the exporter index budget. Source export and all promoted runtime model, DDS, and animation files match byte-for-byte; the parent recorded their SHA-256 values in `docs/assets/002_zombie_outbreak/models_3d/zombies/manifest.md`.

Reimport reports exist for every v2 action and death v5 under `docs/assets/002_zombie_outbreak/models_3d/zombies/validation/`. Raw imported geometry retains UV/normal seam vertices, so raw loose-edge counts are not treated as holes. The diagnostic position weld reports zero non-manifold and degenerate faces and three residual boundary edges; this is documented exporter seam evidence. The source checkpoint remains the closed-topology acceptance surface. Source textured renders and reimport action screenshots were reviewed for the formerly visible back deformation and the rejected death spikes.

An independent read-only audit of the exported `chaosx_zombies.txt` streams welded positions at tolerances from `1e-7` through `1e-4` and found zero loose or non-manifold edges at every tested tolerance. This supports treating the three-edge reimport result as importer-side numeric seam residue rather than a source/export hole; no hole is visible in the textured source or reimport frames.

Runtime files are promoted under `gfx/models/units/chaosx_zombies/`: one partitioned `.mesh`, three DDS maps, and eight distinct `.anim` files. `gfx/entities/chaosx_zombies.gfx` registers four mesh streams and eight animation types; `animation_chaosx_zombies.asset` registers the files; `gfx/entities/chaosx_zombies.asset` uses distinct attack, defend, support_attack, move, retreat, training, death, and idle states with the 0.8 scale. The bespoke green counter files remain installed at the large and small counter paths from the counter handoff.

## Remaining acceptance limits

The runtime WAV package and sound definitions are present and format-checked, but `evidence/audio/source_design_20260919.md` still marks sonic clip choice as **needs_user_review**. Exact per-action frame synchronization and a subunit-specific acknowledgement selector remain **blocked**. No live HOI4 consumer run is claimed; the user owns that final validation. The model, repaired rig, eight final actions, controlled death v9, exports, reimports, textures, and counter assets are otherwise promoted with traceable evidence.

## Parent audio continuation and documentation disposition, 2026-09-19

This continuation supersedes the audio-wiring absence described in the earlier worker sections, while preserving their source-search and candidate-stage evidence. The parent installed nine additional mechanical derivatives under `sound/002_zombie_outbreak/zombies/`: three `zombie_attack_vocal_*`, two `zombie_attack_impact_*`, three `zombie_death_vocal_*`, and one `zombie_hurt_01.wav`. `docs/assets/002_zombie_outbreak/models_3d/zombies/evidence/audio/runtime_derivations_20260919.json` records each extracted CC0 candidate source, source/runtime SHA-256, and conversion; all nine installed runtime hashes matched that record, and all 24 installed zombie WAVs probed as `pcm_s16le`, 44.1 kHz, mono, 16-bit.

`sound/chaosx_zombies_sound.asset` declares the new sources and vocal, impact, defend-contact, support-contact, death-vocal, death-impact, and hurt wrappers. `gfx/entities/chaosx_zombies.asset` invokes attack vocal at 0.3333/frame 8 and impact at 0.6667/frame 16, defend contact at 0.5000/frame 12, support vocal at 0.3333/frame 8 and contact at 0.6667/frame 16, and death vocal at 0.5000/frame 12 before the sourced death-impact event at 1.5000. Those are implemented numeric timings, not listening or visual-contact proof. The hurt wrapper lacks a verified entity consumer. The six movement runtime files now have a durable crosswalk in `evidence/audio/older_move_runtime_crosswalk_20260920.json`; the five older attack/death runtime files still lack one, while the three older idle/selection files retain their existing crosswalk.

Audio status remains **needs_user_review** for sonic clip choice and playback, and **blocked** for proven contact synchronization, the five remaining older attack/death provenance mappings, and subunit-specific acknowledgement selection. The model and action promotion above is unchanged; no live HOI4 consumer run is claimed.

## Parent geometry continuation: v4 clean export and runtime promotion, 2026-09-20

This section supersedes the older v2 export and reimport paragraphs above wherever they describe the accepted mesh, the three-edge diagnostic, or the death export. The v2 evidence remains retained as history, but it is not the current acceptance surface.

The visible lower-jacket pull was traced to 109 exact lower-jacket vertices carrying upper-leg influence. The parent wrote the SHA-bound repair specification `provider/new_lineage_20260919/clean_repair_reprepare/rig/base_zombie_lower_jacket_skin_repair_v3.json` (SHA-256 `9FA3426A3A19FC8EB64A00A08004FEDF4C87057FE1513231064D11FF6C1C4E7F`) and applied it to the partition source. The repaired source checkpoint is `provider/new_lineage_20260919/clean_repair_reprepare/blender/checkpoints/40_lower_jacket_skin_v3.blend` (SHA-256 `DD0245DB08140DDA75CE492088B348759FB35F78DA6ED0CA1800B9490CAF832C`), with report `blender/reports/40_lower_jacket_skin_v3.json`; the reopened source preserved the protected rig, actions, materials, and closed geometry. The same 109-vertex repair was applied to the death source in `blender/checkpoints/42_death_v5_lower_jacket_skin_v3.blend` (SHA-256 `E9232ACE5FB394EF32417D8E52288C437E60EAD24D3D4E418CEB4F8DCFA5FD2A`) with report `blender/reports/42_death_v5_lower_jacket_skin_v3.json`.

The v3 partition source then exposed one independent geometry defect during actual-byte reimport: faces `[9138, 9214]` were duplicate copies of the isolated triangle on vertices `[10322, 10325, 10333]` with different UV corners. Every edge of that triangle belonged only to the duplicate pair, so it was a disconnected two-sided component rather than a garment seam. The parent added the reviewed adapter operation `remove_explicit_duplicate_faces`, bound it in `.tools/3d_pipeline/config/blender_hoi4_adapter.json`, and required the complete reviewed face group plus the source SHA before mutation. The repaired base checkpoint is `blender/checkpoints/43_duplicate_triangle_removed_v3.blend` (SHA-256 `E9DB2DD5EF2E1337F1CFFFF7D163E030DB3AF83B31DCE88C606560798F24E1B4`) with report `blender/reports/43_duplicate_triangle_removed_v3.json`; it removed exactly two faces and reopened with zero boundary, non-manifold, and degenerate geometry. The death repair used the same reviewed group and produced `blender/checkpoints/45_death_skin_duplicate_removed_v4.blend` (SHA-256 `3A4D5AB6539F6CB049038D2C212523B96B55C45279D48C1E2B5994C8175B6817`) with report `blender/reports/45_death_skin_duplicate_removed_v4.json`.

The final partition checkpoint is `blender/checkpoints/44_export_partitioned_v4.blend` (SHA-256 `85283622C9C048B99B1BC8B05A8589F06056E8E7E8A4F0C1BE8FD8A53CEEDC95`) and its partition report is `blender/reports/44_export_partitioned_v4_partition.json`. It contains 30,436 triangles and 15,021 vertices in four streams with 8,000, 7,998, 8,000, and 6,438 triangles. The source and reopened partition remain closed, manifold, and free of degenerate faces.

The v4 model export is `export/mesh/chaosx_zombies_v4.mesh` (3,636,942 bytes). The eight v4 action exports are `export/anim/chaosx_zombies_{idle,move,attack,defend,support_attack,retreat,training}_v4.anim`, and the death export is `export/anim/chaosx_zombies_death_v4.anim`. Actual v4 mesh bytes were imported with the v4 textures and each action was rendered in Blender at rest and role-specific poses. The proof reports are `validation/reimport_chaosx_zombies_{idle,move,attack,defend,support_attack,retreat,training,death}_clean_v4.json`; every report records 30,436 triangles, zero position-welded loose boundary edges, zero non-manifold edges, and zero degenerate faces. The textured v4 proof frames and zoomed jacket/death reviews are under `blender/previews/` and show no lower-jacket triangular pull, hole, or white outline.

The v4 export bytes are promoted to the canonical runtime paths under `gfx/models/units/chaosx_zombies/`. The parent rechecked byte equality for the mesh and all eight actions; the runtime SHA-256 values are mesh `B578240DE7CB05C284A37D0E48204C8E78D521A8779404B5785A5C81C3B809A5`, idle `E3381115733FAF760A12CB61579AF54D431AEE980D42B5B87FA7ACB98CA25BD9`, move `539F4A53B5BB188DF9003CD9A5FFF105884669F8FF59934E22D149A0A735E90D`, attack `B2868A0E25969E17DDC99CE6A3E4668170122A647E21DD0FAD291EF866BCBB9F`, defend `4E73FCEE5095248E39AE8EE9415DC848325409B6E93881805CFFA1E0BBD42FC3`, support attack `0B5DBCED55730C3F5FE032E85003939298D22B757985B534DAE5A2E02AF14863`, retreat `BB58E51EE71FECEAFCFD7E15E434C9CBD6302AE12190CE29AB86370F706E2910`, training `DC5F5CE5B16A2EB9033AE6D283B801DE3BF7AE377DAE7F202EDE112A37AAB9C1`, and death `64A063195463184B7933F5C032BB4A19822BF7A40B6E2C5AD7FED0317B3CC1C8`. The entity and animation registries already reference these canonical names at scale `0.8`.

The v4 model and skeletal-action defect queue is clear by offline source, export, actual-byte reimport, textured playback, and runtime hash evidence. Remaining acceptance limits are outside geometry repair: audio sonic fit and full older-file provenance still need review, exact contact timing needs listening or live playback, the hurt wrapper has no verified entity consumer, and the user owns final in-game validation. No live HOI4 consumer run is claimed.

## Independent consumer-defect audit, 2026-09-20

This section supersedes the preceding claim that the model and action defect queue is clear. The current runtime mesh has a reproducible visibility failure despite closed-topology reports. A locked Blender `material_visibility` probe of `44_export_partitioned_v4.blend` and the actual-byte attack v5 reimport shows large jacket, head, and trouser patches disappearing when opaque clay backface culling is enabled. The same mesh is visibly complete with culling disabled. The four source materials disable backface culling, whereas the reimported PDX materials enable it. Runtime diffuse DDS alpha is uniformly 255, excluding transparent alpha as the cause. See `docs/assets/002_zombie_outbreak/models_3d/zombies/validation/material_visibility_audit_20260920.md` for exact source/runtime hashes, eight preview hashes, material findings, and the bounded repair route.

The current death v6 actual-byte side preview at frame 37 shows a nearly horizontal body suspended by toe-only ground contact. The report's minimum ground-contact height near `z=0.001` does not prove torso or head contact; forward bounds extend to about `y=-6.65` by frame 37 and `y=-7.12` by frame 49. A controlled collapse and settling action remains **blocked** pending role-specific repair and actual-byte reimport review. The current v4 working mesh has 15,021 weighted vertices, no zero-weight vertices, and an 18-bone rig; the earlier 24-bone target needs a recorded supersession or correction before final model acceptance.

The repository-owned adapter defines hash-bound `chaosx_blender_hoi4_inspect_mesh_winding` and `chaosx_blender_hoi4_repair_mesh_winding`, but these operations were not callable in this worker's exposed MCP tool inventory. No unsupported Blender route was used, no Meshy call or credit was spent, and no runtime model or animation bytes were changed by this audit. Package geometry/action status is **blocked** until outward winding, culling-on visibility, death body contact, and post-repair export/reimport evidence pass. The parent owns final runtime wiring and review; the user owns live consumer validation.

# Final offline repair, action promotion, and actual-byte reimport, 2026-09-20

This continuation supersedes the earlier visibility-audit blocker and the death-v6 contact blocker. The final source checkpoint is `provider/new_lineage_20260919/clean_repair_reprepare/blender/checkpoints/77_final_zombie_actions_v1.blend` (SHA-256 `531EE4EA8EAC98DFD0CA6A07B8DBCC2300C087E4E4CA8B59250F85DE2C91CF55`).

The parent applied the SHA-bound smooth-greedy face-winding selection from `rig/mesh_winding_repair_smooth_greedy_v3.json` and reopened it through `blender/reports/63_winding_smooth_greedy_v3_repair_explicit_mesh_winding_batch.json`. The final mesh has 30,436 triangles and 15,021 vertices with zero source boundary edges, zero non-manifold edges, and zero degenerate faces. Culling-on opaque-clay source renders and actual-byte reimport previews show complete jacket, head, trouser, and limb surfaces; the earlier black-hole visibility defect is not present.

The parent applied `rig/mesh_root_to_pelvis_v1.json` to transfer exactly 347 root-weighted vertices from the nondeforming root group to pelvis. `blender/reports/72_rootpelvis_skin_v1_repair_explicit_skin_batch.json` records an unchanged topology hash and `reopened_invariants_passed_requires_visual_export_review`. The final audit `blender/reports/77_final_zombie_actions_v1_audit.json` records zero zero-weight vertices, zero over-four-influence vertices, four source materials with backface culling disabled, and all eight action hashes.

The action set in the final checkpoint is `base_zombie_attack`, `base_zombie_defend`, `base_zombie_idle`, `base_zombie_move`, `base_zombie_retreat`, `base_zombie_support_attack`, `base_zombie_training`, and `base_zombie_death_clean_v9`. The reviewed attack, support-attack, and defend candidates were promoted into the repaired checkpoint; the death action was rebuilt with a measured fall and floor-settling phase. Source renders cover every role, and the death transition was checked at frames 0, 8, 16, 24, 32, 36, 40, 44, and 48 from three camera views.

The final `.mesh` and eight `.anim` files were exported through the locked `io_pdx_mesh` adapter, then reimported as actual bytes with staged default textures. `docs/assets/002_zombie_outbreak/models_3d/zombies/validation/final_reimport_summary_v1.json` records the proof blend SHA-256 values and fifteen preview frames per role; its position-welded diagnostics are zero for loose boundaries, non-manifold edges, and degenerate faces for all eight reimports. `validation/runtime_sync_final_v1.json` records byte equality between each canonical export and the runtime files under `gfx/models/units/chaosx_zombies/`.

The corrected package preserves 40 total Meshy credits consumed for the body, one remesh, and the one required rig attempt. The Meshy rig failed the topology and required-action gates, so Blender authored the canonical rig and skeletal actions after geometry repair. Audio files and entity sound events remain installed, but listening review, exact contact timing, the five legacy specialized-wrapper mappings, and the hurt wrapper consumer remain open. The bespoke base-zombie counter is installed, and the seven specialized counter packages from the artist handoff still need the user's visual review. The user owns final live HOI4 validation; none is claimed here.

## Single Meshy rig attempt, 2026-09-20

The required one-time Meshy rig attempt was executed after the repaired body package was available. Task `01a0be3d-955a-7743-960b-45c494c2cf2b` used body task `01a0ba94-9607-729b-92e5-53951c59e8e1`, succeeded at `1.8` meters, and consumed 5 credits. The primary rig artifact and bundled walking artifact are retained under `provider/new_lineage_20260919/clean_repair_reprepare/provider_rig_attempt/`; hashes, response receipts, Blender inspection reports, and review renders are indexed by `meshy_rig_once_handoff.json`.

The Meshy result has 24 bones, zero zero-weight vertices, no over-four-influence vertices, and normalized weights, but its imported character retains 157 position-welded boundary edges and 190 position-welded non-manifold edges at `1e-5`. It supplies a bundled walking clip but no required attack, defend, support_attack, retreat, training, idle, or death action set. Its 24-bone skeleton also differs from the accepted 18-bone runtime skeleton, so promotion would invalidate the already-proven eight final actions. The Meshy rig is therefore **retained as a rejected provider candidate**, while the closed Blender checkpoint remains canonical.

This completes the single Meshy rig attempt without a paid retry. Total lineage spend is now **40 credits**: 30 body, 5 remesh, 5 rig. No paid Meshy animation attempts were made because this rig failed the topology gate and did not provide the required role set; the authorized Blender recovery route remains the final action source.

## Movement audio provenance closure, 2026-09-20

The six movement files now have a durable source-to-runtime crosswalk at `docs/assets/002_zombie_outbreak/models_3d/zombies/evidence/audio/older_move_runtime_crosswalk_20260920.json`. Each preserved `01-footstep.ogg` through `06-footstep.ogg` source has its direct CC0 URL, original SHA-256, exact FFmpeg command, runtime path, runtime SHA-256, and byte-for-byte reproduction result. A fresh derivation reproduced all six installed hashes exactly and each runtime file remains `pcm_s16le`, 44.1 kHz, mono, 16-bit.

This closes movement provenance and the `zombie_attack_01.wav` mapping. The base entity now uses the later sourced `chaosx_zombie_death_impact` wrapper for its death impact event; the five older attack/death files remain specialized-wrapper assets and are still open for per-file provenance. Sonic listening, exact movement and combat contact timing, the unused hurt wrapper, tag-scoped acknowledgement selection, bespoke specialized-counter visual review, and live HOI4 validation remain open. No model, animation, or texture bytes changed in this evidence-only continuation; the base death sound consumer changed from the legacy wrapper to the verified impact wrapper.

## Action deformation repair v1 independent audit, 2026-09-20

This section supersedes the preceding claim that all culling-on trouser surfaces and all eight actions are accepted. A close read-only paired culling probe on `blender/checkpoints/78_action_deformation_repair_v1.blend` (SHA-256 `D41CD2B213A2DA61759DBB3933791077D07EDD4B944C2983D8E2D5E3BB74479D`) shows missing knee and shin polygons in attack and move when opaque clay culling is enabled; the same polygons fill with culling disabled. The whole-body culling images were too small to expose these spots. Source-material renders also show pronounced dark trouser-cuff/boot bands. Checkpoint 78 still references unpacked provider PNGs, and no packed runtime-material checkpoint existed during this audit, so the final texture behavior remains unverified. Exact paired image paths/hashes and source/material limits are in `docs/assets/002_zombie_outbreak/models_3d/zombies/validation/action_deformation_repair_v1_audit_20260920.md`.

The uniform `0.15` quaternion scaling of upper/lower legs weakens action semantics. Move and retreat keyed upper-leg motion fell `40°→6°` and lower-leg motion `25°→3.75°`, leaving almost no visible shuffling or backward step in frames 10/19/28. Death v9 leg motion fell `14°→2.1°` upper and `20°→3°` lower; frame 36 is a toe-led airborne dive and frame 48 is a rigid face-down plank with little torso/hip settling. The action repair is **blocked for promotion** pending targeted mesh/weight correction, restoration of role-appropriate leg motion, close culling-on review, packed-material proof, and actual-byte export/reimport. This worker made no runtime `.mesh`, `.anim`, entity, GFX, gameplay, or texture changes in this audit.
