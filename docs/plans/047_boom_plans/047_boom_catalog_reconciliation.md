# Event 47 BOOM catalog reconciliation

## Documentation disposition

Disposition: **unresolved**.
Catalog work awaits an attributable acceptance decision and implementation evidence.

Basis: the “Accepted event row direction” section records proposed catalog facts, but the scoped files contain no attributable user or parent acceptance decision.

The authoritative workbook has not been reconciled, and the proposed Various Anomalies relationship remains blocked until a stable cluster identity and implementation evidence exist.

## Current exported event row

The current event CSV identifies:

- ID 47
- BOOM
- a mysterious explosion that destroys a random province and may resemble a nuclear strike
- Minor Repeatable
- Chaos level 1
- no cluster ID
- no member severity
- To Be Reworked

## Accepted event row direction

The authoritative workbook should be updated after implementation with these facts:

- ID: 47
- Event name: BOOM
- Details: an unexplained blast destroys one random eligible state, kills civilians, damages the surrounding region, and is heard around the world without registering as a nuclear strike
- Evolution I: Bigger BOOM strengthens the epicenter, increases first-ring losses, and adds a weaker second ring
- Evolution II: BOOM BOOM BOOM permits two or three distant epicenters during one incident
- Type: Minor Repeatable
- Chaos level: 1
- Cluster: Various Anomalies only after that cluster receives a stable ID
- Member severity: Severe
- Status: remains To Be Reworked until implementation and required acceptance evidence are complete

The word state should replace province because the event's population, buildings, adjacency, and Deaths transactions are state-owned.

## Severity correction

The submitted rough entry proposed Low severity. This package recommends Severe.

The classification follows the actual member effect. Baseline BOOM can kill 55 to 80 percent of an epicenter's population, damage every major building family, and harm surrounding states. Bigger BOOM widens the footprint. BOOM BOOM BOOM can strike several distant regions in one event firing. Low would understate the member's campaign impact and would weaken cluster danger reporting.

The event remains Minor Repeatable because its lifecycle is short and self-contained. Member severity and event type describe different properties.

## Current cluster export

The current cluster CSV contains a Various Anomalies row with:

- no stable cluster ID
- Minor Fire-Once type
- Chaos level 4
- Unavailable status
- no member list

Do not invent a cluster ID or silently change that wider row from this event package. The cluster needs a separate registry decision that reconciles its type, unlock tier, members, and status.

Event 47 should remain independently implementable at Chaos level 1. Its prospective cluster membership does not raise its own event level.

## Workbook workflow

Edit only `docs/spreadsheets/chaos_redux_events_catalog.xlsx` after final implementation facts are available. Then run `python .tools/export_event_catalog_csv.py` from the mod root.

Do not edit the three catalog CSV files directly.
