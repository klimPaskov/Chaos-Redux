# Event 046 subagent routing handoff

## Documentation disposition

Disposition: **unresolved**.

Basis: this file defines a required specialist order, scope exclusions, and parent-review boundary, but it contains no specialist execution handoffs or attributable user or parent acceptance decision.

Its package-generation environment statement is historical evidence and does not establish current MCP/server health or current subagent availability.

## Current environment

No project subagent was executed while this specification package was produced.

The available environment exposed the supplied subagent definitions as files, but it did not expose the Codex Task runtime or the HOI4 MCP server.

The routes below are required downstream handoffs, not completed work.

## Required order

### 1. `chaosx_repo_explorer`

Run before editing with `fork_turns="none"` only when file locations, patterns, dependencies, vanilla precedents, or edit order remain unclear.
If those inputs are already known, the parent uses the existing map and proceeds to the required audit and implementation owners.

Provide the full spec pack, current Event 46 identity, cluster conflicts, and exact request for a repository and vanilla precedent map.

Expected output includes touched files, current Event 46 remnants, event and cluster registration, Event Log and evolution hooks, Chaos history path, population effects, equipment and state setters, achievement patterns, assets, docs, workbook, Event 43 evidence, and implementation order.

### 2. `chaosx_ai_probability_auditor` baseline

Run read-only before any weight or pacing patch.

Use `prompts/046_the_great_shuffle_probability_audit_prompt.md` and every `P46-*` scenario.

Record exact source revision and scenario hashes.

### 3. `chaosx_scripted_system_architect`

Use `prompts/046_the_great_shuffle_registry_architecture_prompt.md`.

The parent decides allowed files from the existing repository map or the bounded explorer handoff.

The architect can implement bounded helpers and registry surfaces or return a broader plan when needed.

Any patch requires a handoff in this folder.

### 4. Parent event implementation

The parent owns event chain, registration, family integration, report, Event Log, evolution, Chaos, clusters, achievements, docs, workbook coordination, final wiring, and completion claims.

### 5. Asset subagents

Use `chaosx_generated_event_art` for the report image.

Use `chaosx_icon_artist` for six achievement icon packages.

Give each a separate context-complete prompt derived from the asset prompt.

No portrait, flag, animation, 3D, counter, sound, decision, focus, or UI agent is in scope.

### 6. `chaosx_localisation_auditor`

Run after final gameplay and report surfaces exist.

It can patch bounded keys and scripted localisation.

### 7. `chaosx_ai_probability_auditor` comparison

Run after the owner applies weights and pacing changes.

Use `hoi4.probability_compare` on the same baseline scenarios.

### 8. `chaosx_spreadsheet_doc_worker`

Run after final localisation and implementation facts exist.

Edit only the authoritative workbook and run the exporter.

### 9. `chaosx_documentation_curator`

Use when implementation produced several plans, handoffs, asset records, or conflicting docs.

It can reconcile documentation only.

### 10. `chaosx_improvement_loop_planner`

Run near completion with `fork_turns="none"`.

Provide implemented mechanics, unresolved conditional families, prior plans, audits, asset state, docs, and catalog state.

Do not run a second pass until the first output is implemented, promoted, queued with a reason, or rejected.

### 11. `chaosx_event_completion_auditor`

Run read-only after all accepted implementation and resolved improvement work.

Compare every spec, matrix, prompt, asset, achievement, doc, cluster, and workbook surface.

## Explicitly out-of-scope subagents

Do not route Event 46 to:

- `chaosx_event_ui_worker`, because no event-owned GUI is accepted
- `chaosx_focus_tree_auditor`, because no focus tree is accepted
- `chaosx_decision_mission_auditor`, except a later audit finds an actual shared report issue within its domain
- `chaosx_country_package_auditor`, because no country is created or transformed
- `chaosx_portrait_creator`, because no portrait is accepted
- `chaosx_3d_model_pipeline`, because no 3D consumer exists
- super-event text or audio researchers, because no super-event exists

## Parent review rule

Every subagent handoff is evidence, not completion.

The parent reviews scope, changed files, IDs, validations, blockers, plan disposition, and final repo state before relying on it.
