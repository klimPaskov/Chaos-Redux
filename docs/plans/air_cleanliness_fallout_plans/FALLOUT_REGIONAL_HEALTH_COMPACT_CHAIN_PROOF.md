# Fallout Regional Health Compact chain proof

## Scope

This tranche adds the dormant Regional Health Compact survivor chain for candidate `754`, transaction `710079`, route `7190`, event ids `chaosx.fallout.754` through `chaosx.fallout.760`, and survivor Event Log history `9185`. The chain belongs to the Quarantine government archetype and follows a completed Health Without Borders memory in a state with a foreign neighbor.

The Fallout consequence remains a terminal transition. It is not an ordinary event, an evolution, or an ordinary super-event. These blocks represent only survivor-country memory after the consequence.

## Authored implementation

- `common/script_constants/fallout_world_end_regional_health_compact_constants.txt` owns branch, timing, cost, grading, callback, modifier, and history values.
- `common/scripted_effects/fallout_world_end_regional_health_compact_event_effects.txt` owns deterministic registry snapshots, branch costs, state and neighbor reservations, Air Winter and Supply Access writes, Deaths-backed failures, bilateral opinion, Event Log payloads, delayed lanes, and cleanup.
- `common/scripted_triggers/fallout_world_end_regional_health_compact_event_triggers.txt` owns current-state, current-country, affordability, delayed-result, callback, and cleanup gates.
- `common/dynamic_modifiers/fallout_world_end_regional_health_compact_dynamic_modifiers.txt` owns branch result and callback presentation modifiers.
- `common/opinion_modifiers/fallout_regional_health_compact_opinion_modifiers.txt` owns dedicated bilateral compact memories.
- `events/fallout_world_end_events.txt` owns the human opening, hidden AI opening, delayed result pair, callback pair, and cleanup under `chaosx.fallout`.
- `common/scripted_localisation/chaosx_scripted_localisation_events_log.txt` and `common/scripted_localisation/fallout_world_end_regional_health_compact_event_log_scripted_localisation.txt` route history `9185` to the dedicated detail resolver.
- `localisation/english/fallout_world_end_regional_health_compact_l_english.yml` provides all player-facing event, tooltip, memory, and Event Log text.

## Static evidence

The candidate producer selects the lowest valid owned state with a foreign neighbor and requires a closed Health Without Borders memory, current Air Winter ledgers, durable survival rows, Quarantine government, current health-authority ledgers, and one affordable branch. The registry freezes the target state's owner, controller, population-facing Air Winter values, Supply Access, and the foreign neighbor's state, owner, controller, and health values before a branch cost is accepted.

The four branches are Share the clinic registers, Exchange clinic teams, Grant corridor authority, and Enforce a border quarantine. Each has distinct costs, deterministic grading thresholds, success, partial, and failure effects, authored hidden-AI priority, bilateral opinion, survivor memory, delayed callback behavior, and idempotent cleanup. Result failure and callback failure route bounded population loss through the existing Fallout aftermath Deaths contract.

The human and hidden AI event tokens are `754` and `755`, result tokens are `756` and `757`, callback tokens are `758` and `759`, and cleanup token is `760`. The ordinary candidate producer uses only the global route and transaction identities for this chain. No scheduler activation flag is set.

The dedicated report image is generated fictional art and is processed to a 210 x 176 transparent report card. The runtime DDS is uncompressed A8R8G8B8 and is registered through the dedicated sprite key `GFX_report_event_fallout_regional_health_compact`. Its source, processed, runtime, hash, and path evidence is in `docs/assets/754_regional_health_compact/manifest.md`.

## Engine-sensitive boundary

The read-only event inspector returned `EVENT_INSPECTED_PARTIAL` for `chaosx.fallout.754` with zero blocking diagnostics. Its artifact is `event-lint-461c9570c127.json`. The report deferred workspace-wide helper projections and lifecycle passes, so it is source evidence rather than runtime proof. It does not prove runtime dispatch, delayed queue delivery, save recovery, host authority, multiplayer input blocking, Event Log presentation, Deaths readback, or asset display. Hearts of Iron IV is not launched. The chain remains dormant and contributes zero release-floor credit until the wider activation audit is accepted.
