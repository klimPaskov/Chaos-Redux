# Fallout The Failed Calculation chain proof

## Candidate and receipt contract

The candidate registry owns id `831`, transaction `710090`, route `7212`, and Event Log history `9196`. The candidate is produced only for a current East Asian Technate country carrying the Manchurian Reactor Keeps memory, the closed Grid Vote state memory `fallout_event_824_memory_closed`, durable survival resources, produced Air Winter values, bounded Disease pressure, native industrial or arms capacity, and one authenticated foreign neighboring state. The lowest eligible owned state id wins deterministically.

The candidate row uses event ids `831` and `832` for human and hidden AI openings. The result lane uses `833` and `834`. The callback lane uses `835` and `836`. Cleanup uses `837`. The candidate is dormant and the Fallout-owned scheduler remains the only intended activation owner.

## Authenticated state flow

The opening freezes country ledgers for calculation legitimacy, inquiry pressure, operator trust, safety margin, recovery capacity, research confidence, and technical memory. It also freezes the target state owner, controller, generation, shelter, Supply Access, Adaptation, Exposure, Disease, and the lowest current foreign neighbor. Every delayed trigger rechecks the country, state, neighbor, owner, controller, generation, target type, branch, transaction ticket, and dispatch envelope.

The four branches are public inquiry, sealed calculation, operator accountability, and model revision. The result resolves after `49` days. The technical audit callback resolves after `300` days. Result modifiers last `240` days. Callback modifiers last `390` days. Failure damages native infrastructure and requests bounded Deaths against the authenticated target state. Cleanup releases reservations and clears the chain exactly once.

## Event Log and localisation

`fallout_event_831_log.history_id = 9196`. The detail router in `common/scripted_localisation/chaosx_scripted_localisation_events_log.txt` maps the history to `fallout.event_log.failed_calculation.detail`. The dedicated scripted localisation returns branch and outcome payloads from `fallout_event_831_log`. All visible event, tooltip, callback, modifier, opinion, Event Log, and cancellation keys are in `localisation/english/fallout_consolidated_l_english.yml` with UTF-8 BOM encoding.

## Asset proof

The dedicated asset package is `docs/assets/831_failed_calculation/`. The registered sprite is `GFX_report_event_fallout_failed_calculation`, backed by `gfx/event_pictures/fallout/report_event_fallout_failed_calculation.dds`. The source, processed preview, DDS, processor settings, and hashes are listed in the asset manifest. No prior Fallout, zombie, final silence, or blackout asset is reused.

## Status boundary

Static source review covers the candidate row, event ids, helper definitions, constants, localisation keys, Event Log route, sprite path, and cleanup references. The chain remains dormant and contributes zero release-floor credit. Scheduler activation, blackout host authority, save recovery, multiplayer delivery, live Event Log rendering, and full engine execution remain unproven because Hearts of Iron IV was not launched.
