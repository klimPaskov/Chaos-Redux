# Fallout Desalination Night Shift chain proof

Status: implemented as a dormant reviewed tranche, not release-floor credit.

Events `chaosx.fallout.485` through `.491` are defined once in `events/fallout_world_end_events.txt`. The root uses the Fallout namespace and the opening has a dedicated report picture and concrete regional text.

Candidate `485` uses transaction `710043`, route `7143`, and history `9148`. The producer selects the lowest owned Middle Eastern coastal state that passes `fallout_event_485_desalination_state_is_current`. That trigger requires a produced current-generation Air Winter snapshot, surviving state population, water-security evidence, a native naval-base port proxy, a supported urban state category, current Fallout identity and resource rows, and low enough supply access. The producer copies the water-security value into the country-owned pressure row before appending the candidate.

The four branches are full-shift operation, desert technicians, clinic-power reserve, and guarded rationing. The result waits `42` days. The second-shift callback waits `300` days. Human and hidden-AI lanes share the deterministic viability calculation, resource costs, branch memories, Deaths-system failure route, and authenticated cleanup.

The chain changes Water, Power, Medicine, Cohesion, Recognition, operation memory, water reserve, technical credit, stability, war support, Air Winter exposure, adaptation, reclamation, and supply. Failure damages native infrastructure, naval-base, or industrial capacity in order and requests exact state population loss through the Deaths system. Event Log history `9148` maps twelve branch payloads and three callback payloads through the shared name and detail routes.

The dedicated report image was generated through the approved event-art workflow, processed at `210x176`, converted to a one-level uncompressed BGRA DDS, copied into the runtime Fallout picture directory, and registered as `GFX_report_event_fallout_desalination_night_shift`. The asset manifest records source, processed, preview, and runtime hashes.

Static checks for this tranche cover seven unique event definitions, matching event constants, candidate and Event Log wiring, balanced edited script blocks, no raw unsupported comparison operators, no predecessor False Spring identifiers in the new runtime files, and BOM encoding for the localisation file.

The chain is dormant and earns zero countable blocks because scheduler activation, host authority, save recovery, multiplayer delivery, full-screen Fallout blackout, and runtime Event Log delivery remain unproven. No HOI4 runtime was launched.
