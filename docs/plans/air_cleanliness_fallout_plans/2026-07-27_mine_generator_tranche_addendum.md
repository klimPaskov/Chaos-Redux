# Mine Generator tranche addendum

Status: accepted design handoff for a bounded dormant implementation tranche.

The Sub-Saharan African Mine Generator reserves candidate `642`, events `642` through `648`, transaction `710063`, route `7163`, and Event Log history `9169`. It follows the implemented Mountain Pass Census row and remains outside release-floor credit until the ordinary Fallout scheduler, host authority, save recovery, delayed delivery, multiplayer behavior, and Event Log runtime surfaces are proven.

The candidate selects the lowest current-generation owned and controlled state in `fallout_region.sub_saharan_africa` with a produced Air Winter snapshot, surviving population, native infrastructure, current Fallout state ledgers, weak but positive Food reserve, and at least one native `steel`, `tungsten`, `chromium`, `aluminium`, or `coal` deposit. The installed trigger documentation exposes those resources directly in state scope through the resource-count trigger surface.

The title describes the settlement operating the authenticated resource works with country Power and Scrap. It does not claim that HOI4 exposes a generic mine or generator building. Specific mine, company, union, commodity, language, or local-government names require a current country-memory receipt and researched localisation.

The four branches are Trade Mineral Shares, Conscript the Labor Shift, Establish Engineer Rule, and Evacuate the Works. They use distinct Power, Scrap, Recognition, Food, Fuel, Command Power, Shelter Capacity, and Medicine costs and write extraction continuity, food debt, worker trust, labor coercion, engineer authority, evacuation readiness, and foreign concession pressure as clamped country ledgers.

The result is due after 42 days and the operating-season callback after 270 days. Both grades are deterministic. Failure may damage native infrastructure or a present industrial complex, apply a bounded local resource penalty, and request Fallout Deaths through the shared exact-state population-loss helper, but it never creates, removes, or renames a native resource deposit.

The chain has human and hidden-AI openings, human and hidden-AI results, human and hidden-AI callbacks, authenticated Event Log payloads, generation-safe refund and cancellation, exact cleanup, dedicated localisation, and a dedicated report asset.

No scheduler activation setter, on-action caller, country or partner substitution, cross-state population transfer, government-archetype rewrite, claim, core, tag, focus tree, decision category, scripted GUI, generic fallback, zombie surface, or engine runtime claim is included.

The accepted source design is promoted into `docs/specs/air_cleanliness_fallout_specs/specs/64_reviewed_regional_mine_generator.md`. This addendum remains the implementation handoff and should be marked implemented only after the main agent completes gameplay, localisation, assets, Event Log wiring, spreadsheet alignment, and focused static proof.
