# Counterfeit Antibiotics chain proof

## Scope

Candidate 387 is a dormant medicine-market chain under `add_namespace = chaosx.fallout`. It has human and hidden AI openings, four authored policies, a delayed result, human and hidden AI result handling, a 330-day callback, Event Log history, and cleanup.

## Static wiring

- Event ids `chaosx.fallout.387` through `.393` are unique in `events/fallout_world_end_events.txt`.
- `fallout_event_id` exposes the seven event tokens and `fallout_event_candidate_pilot` exposes candidate 387, transaction 710029, and route 7129.
- The candidate builder selects the lowest valid native state id and freezes country medicine pressure. It clears working variables after appending the complete typed row.
- The state and country triggers require current Fallout and Air Winter receipts, native owner and control, accepted state grades, population, exposure, and non-recovery supply pressure. Branch gates use medicine, food, scrap, recognition, and cohesion rather than political power.
- Result and callback effects write through `apply_exact_state_civilian_population_loss`, update state reclamation, supply access, exposure, building damage, survival ledgers, stability, war support, and medicine provenance ledgers.
- `chaosx_events_log_effects.txt` and `chaosx_scripted_localisation_events_log.txt` recognize history 9134. The dedicated scripted localisation maps all fifteen payloads.

## Asset proof

The dedicated generated source, processed preview, prompt, manifest, GFX handoff, and runtime DDS are under `docs/assets/air_cleanliness_fallout/fallout_counterfeit_antibiotics/`. The runtime sprite is registered as `GFX_report_event_fallout_counterfeit_antibiotics`.

The event catalog workbook row `FALLOUT-387` is aligned with the opening choices, callback, type, and `Needs Testing` status. The approved CSV export was regenerated after the row update.

## Engine boundary

The chain is statically wired to the Fallout delayed-result scheduler and does not claim runtime activation or delivery. No HOI4 runtime was launched by request. A bounded engine inspector request may be attempted later, but a timeout is not treated as proof of native validity.

## Release disposition

The chain remains dormant and non-countable until the Fallout host-authority, save-recovery, blackout delivery, and scheduler activation audit passes. Its seven authored blocks are part of the reviewed pilot inventory, not the activated release-floor count.
