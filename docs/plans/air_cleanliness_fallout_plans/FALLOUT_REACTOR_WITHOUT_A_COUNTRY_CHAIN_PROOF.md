# Fallout Reactor Without a Country chain proof

## Static wiring

Candidate `366` uses transaction `710026`, route `7126`, event ids `366` through `372`, and Event Log history `9131`.
The candidate producer selects a current-generation native nuclear-reactor state with a deterministic score of reactor level times `5` plus infrastructure and industrial capacity.
The lowest native state id resolves an exact score tie.
The row stores the selected state id, score, and Air Winter exposure before append, then clears its working variables.

The chain has one human opening, one hidden-AI opening, one human delayed result, one hidden-AI delayed result, one human callback, one hidden-AI callback, and authenticated cleanup.
Four branches carry separate costs, AI priorities, thresholds, state memories, timed modifiers, delayed receipts, and localized result text.
Failure uses the Deaths system and damages the nuclear reactor before infrastructure and industry.

## Asset proof

The generated source, processed 210 by 176 preview, runtime uncompressed 32-bit BGRA DDS, prompt, manifest, and GFX handoff are under `docs/assets/air_cleanliness_fallout/fallout_reactor_without_a_country/`.
The runtime sprite is `GFX_report_event_fallout_reactor_without_a_country` in `interface/fallout_consolidated.gfx`.

## Engine-sensitive boundary

No HOI4 runtime was launched.
The bounded `hoi4.event_inspect` lint request for `chaosx.fallout.366` used helper expansion disabled, depth one, twenty nodes, and forty edges.
It returned `status: error`, `code: EVENT_ISSUE_LIMIT`, `workspaceId: mod_chaos_redux_ea3b2d67c2c0`, `filesScanned: []`, `proposedFiles: []`, `changedFiles: []`, `diagnostics: []`, `artifacts: []`, `validation.passed: false`, and one blocker stating `Event-chain issue count exceeds the fixed result ceiling` with `count: 23102` and `maximum: 20000`.
Scheduler reachability, delayed delivery, host authority, save recovery, multiplayer ownership, and Event Log opening remain unproven for this chain.

The candidate is dormant and contributes zero countable blocks to the `0 of 660` release-floor total.

## Catalog alignment

The editable event catalog workbook carries `FALLOUT-366` on row `218` with the same title, branch summary, 35-day result, and 240-day inspection wording.
The required export command was run after the workbook update.
The exported Events CSV reports `1015` rows and SHA-256 `260f286e55ddf945508b6f2183e0f7b0a6e5abe525b1f83f35fce9bb80a017d9`.
The workbook and export files remain outside the scoped chain commit because the workbook contains unrelated dirty user edits.
