# The Children From Elsewhere chain proof

## Implemented surfaces

- Candidate row `436`, transaction key `710036`, route `7136`.
- Human and hidden-AI event lanes `436` through `442`.
- Four government-aware identity policies with separate costs.
- Frozen citizenship, foster, school, and return ledgers.
- Durable cohort memory and a 365-day generation review.
- State memory, dynamic modifiers, Deaths-system failure handling, and authenticated cleanup.
- Event Log history `9141`, localisation, dedicated report art, DDS, sprite registration, manifest, and handoff.
- The event catalog workbook row `FALLOUT-436` and its generated CSV export use the same opening, four policy branches, and 365-day review wording.

## Static checks

The chain was reviewed for unique ids, branch and outcome key coverage, cross-file constants, Event Log mappings, localisation references, dedicated asset paths, and the scheduler candidate boundary.

The result and callback lanes both update the selected state's Adaptation, Reclamation, Supply Access, and Exposure values, apply the matching dynamic modifier, and record a state flag before cleanup.

The report DDS is 210 by 176 with one uncompressed BGRA image level and an 840-byte row pitch.

The localisation file is UTF-8 with BOM.

The player-facing text contains no em dashes or semicolons.

## Dormant boundary

The chain remains dormant because Fallout scheduler activation, host authority, save recovery, multiplayer delivery, full-screen blackout ownership, and runtime Event Log delivery are not proven.

Native character creation, dynamic successor tags, and an actual return-country allocation are not claimed.

No HOI4 runtime was launched, following the user instruction.
