# Fallout Ammunition Winter chain proof

Status: dormant, statically reconciled, not release-floor credit.

The chain owns candidate id `534`, transaction key `710050`, route `7150`, event ids `chaosx.fallout.534` through `chaosx.fallout.540`, and Event Log history `9155`. It is implemented under `add_namespace = chaosx.fallout` and never writes either Fallout scheduler activation flag.

## Route proof

The candidate producer initializes five durable country ledgers, selects the lowest valid owned Air Winter state, and appends one complete typed row to the reviewed candidate arrays. The country gate requires Equipment below `14` and either `has_war = yes` or crime pressure at the authored threat threshold. Four human branches and one deterministic hidden-AI chooser reserve the same 60-day delayed result. The result reserves one 180-day callback. The cleanup event releases the result and callback receipts in order and clears only transaction state.

The result and callback use the Deaths system for failure losses with an explicit minimum remaining population, apply conditional infrastructure or arms-factory damage, update Supply Access, Air Winter exposure and reclamation, attach branch-specific military modifiers, and clamp all durable ledgers. No generic political-power store, reward loop, invented tag, automatic war declaration, or variable-only population loss is used.

## Engine-sensitive boundary

Static source review confirms the country and state scope of the low-equipment and active-threat gates, the owner-bound `var:` target, the ordinary receipt authentication, the delayed result and callback tokens, and the hidden cleanup token. No HOI4 runtime was launched, so popup ordering, save recovery, multiplayer behavior, and live candidate issuance remain unobserved.

## Asset and Event Log evidence

The dedicated report image, processed PNG, DDS, sprite registration, source prompt, hashes, and handoff are recorded under `docs/assets/air_cleanliness_fallout/fallout_ammunition_winter/`. The shared Event Log recognizes history `9155`, and the dedicated scripted localisation maps fifteen branch and callback payloads to concrete text.

## Review boundary

The chain is manually authored and remains dormant until the scheduler activation audit passes. It adds seven defined event blocks to the documented living-world total, but the countable Fallout total remains `0 of 660`. The exact engine-native all-valid-province manual thermonuclear sweep remains a separate blocker.
