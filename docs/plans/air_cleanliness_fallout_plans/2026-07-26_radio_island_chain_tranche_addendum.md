# Radio Island Chain implementation addendum

Date: 2026-07-26

Status: accepted bounded tranche for implementation. The row remains dormant and is not release-floor credit until the Fallout scheduler activation, host authority, save recovery, multiplayer delivery, and Event Log runtime surfaces are proven.

## Collision-checked identities

The new ownership set is candidate and opening `600`, hidden AI `601`, result `602` and `603`, callback `604` and `605`, cleanup `606`, transaction `710057`, route `7157`, route upper bound `7158`, and history `9163`. These values are appended after Old Airfield and do not overlap existing Fallout or zombie ids.

## Manual implementation boundary

The chain is a seven-block ordinary regional incident under `add_namespace = chaosx.fallout`. It uses one deterministic coastal host state in `fallout_region.oceania_remote_islands`, four authored branches, generation-bound delayed rows, hidden AI parity, a 35-day result, a 240-day callback, durable memory, Event Log routing, a dedicated report image, and exact cleanup.

It does not activate the scheduler. It does not create a bilateral partner or a second recurring world iterator. It does not use variable-only fallout, one-strike-per-state effects, or a super-event surface.

## Review gates

- all event, transaction, route, history, branch, and asset identities are unique
- the region trigger uses the live Oceania and Remote Islands id
- the state selector chooses the lowest eligible native id and freezes only that state
- all four costs use survival resources or native Command Power and War Support effects
- result grading is deterministic and admits success, partial, and failure
- delayed result and callback reauthenticate generation, target, owner, controller, mode, and ticket
- generation change refunds unresolved costs and releases reservations
- durable branch, outcome, callback, and institution memories survive cleanup
- hidden AI and human lanes use the same branch helper and child receipts
- no scheduler activation flag is written
- the dedicated report image is source, processed 210x176, BGRA DDS, registered, localised, and documented
- workbook row and generated CSV exports match the in-game wording

## Open runtime surfaces

No Hearts of Iron IV process is run. Event inspection remains read-only and may report a transport or issue-limit error on the large Fallout event source. Popup order, host authority, save interruption, multiplayer delivery, and ordinary scheduler counts remain user-owned runtime validation surfaces.
