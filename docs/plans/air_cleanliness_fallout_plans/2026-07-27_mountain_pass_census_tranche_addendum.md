# Mountain Pass Census tranche addendum

Status: accepted implementation handoff for a dormant authored tranche.

The Middle East and North Africa Mountain Pass Census reserves candidate `635`, events `635` through `641`, transaction `710062`, route `7162`, and Event Log history `9168`. It follows Canal Schedule and remains outside release-floor credit while scheduler activation is closed.

The target is the lowest current owned and controlled highland state with a produced Air Winter snapshot, surviving population, shelter capacity, Supply Access, Reclamation, Exposure, Disease Pressure, refugee pressure, and native infrastructure evidence. The row uses the existing `air_winter_presentation_is_highland` receipt and makes no generic pass-topology claim.

The four branches are Local Citizenship, Return Plan, Military Colony, and Open-pass Community. Each branch has a distinct cost, deterministic result and callback effects, concrete MENA text, hidden-AI parity, state and country memory, bounded Deaths failure, exact `35` day result and `210` day callback timing, and generation-safe cleanup.

The chain must not set scheduler activation flags, add an on-action caller, create a country tag, invent a mountain-pass building, reuse another Fallout or zombie asset, or claim runtime delivery. Native infrastructure reads and damage, current Air Winter highland presentation, ordinary and delayed receipts, Event Log, and Deaths are the engine-sensitive surfaces to record.
