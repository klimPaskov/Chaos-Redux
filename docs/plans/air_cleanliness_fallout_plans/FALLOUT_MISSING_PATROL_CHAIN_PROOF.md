# Fallout The Missing Patrol chain proof

Status: dormant reviewed tranche, not release-floor credit.

The Missing Patrol is candidate `527`, transaction `710049`, route `7149`, event blocks `527` through `533`, and Event Log history `9154` in `events/fallout_world_end_events.txt` under `add_namespace = chaosx.fallout`. The chain is Fallout-owned and uses a dedicated report image, sprite, localisation file, constants, triggers, effects, dynamic modifiers, and Event Log payload mapper.

## Eligibility and ownership

The candidate producer initializes five durable country ledgers and selects the lowest owned state that has a produced current-generation Air Winter receipt, current state identity and resource rows, surviving population, usable Supply Access, an exposed grade, and a supported native category. The row records candidate `527`, transaction `710049`, route `7149`, Air Winter pressure, state target, crisis class, winter phase, and equipment pressure. It does not set either scheduler activation flag.

The chain keeps the state pointer, transition generation, country owner, candidate id, target type, and frozen resource rows in a country-owned registry. Result, callback, and cleanup triggers re-authenticate those values before consuming a receipt. Cleanup releases the result and callback tickets, clears frozen and transient registry values, preserves durable patrol cause memory, and leaves scheduler activation unset.

## Human and AI lanes

The opening at `527` offers search, limited border retaliation, cover-up, and waiting for contact. Each branch has a government-aware gate and resource costs. The hidden AI opening at `528` selects the same four branches from deterministic intelligence, war-support, fear, contact, and resource thresholds. Human and AI lanes share the same viability score, delayed effects, result outcomes, callback grading, Event Log payloads, and cleanup effects.

The result at `529` or hidden AI result `530` resolves after 60 days. It updates Food, Fuel, Equipment, Scrap, Recognition, Cohesion, Stability, War Support, Command Power, Army Experience, manpower, intelligence, fear, reputation, contact, state Supply Access, Air Winter exposure, reclamation, buildings, military modifiers, state memory, and Deaths-backed failure. The callback at `531` or hidden AI callback `532` resolves after another 180 days and grades current intelligence, fear, reputation, and contact. Cleanup `533` releases both delayed receipts and closes the memory only after authenticated terminalization.

## Engine-sensitive boundary

The tranche does not claim native patrol character creation, live neighbor selection, automatic war declaration, dynamic tag allocation, host-authoritative blackout presentation, save recovery, multiplayer input blocking, or runtime Event Log rendering. It uses a no-war retaliation memory and explicit cause-memory ledgers until those engine surfaces are proven. It does not replace the exact native all-valid-province thermonuclear sweep or manual scenario proof.

## Review evidence

Static review covers event id uniqueness, constants and modifier references, candidate route and transaction identity, human and hidden-AI token parity, trigger and effect brace balance, localisation coverage and UTF-8 BOM, dedicated sprite registration, asset dimensions and hashes, and the absence of zombie asset paths. No HOI4 runtime was launched, as required by the user.
