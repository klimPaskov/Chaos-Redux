# False Spring Losses major-arc stage-consumer addendum

Date: 2026-07-26

Status: accepted for a dormant fourth major-arc stage-consumer pilot. This addendum does not authorize scheduler activation, a new event range, or release-floor credit.

## Scope

False Spring Losses is the final authored major-arc chain in the current four-row candidate pool. Its existing seven-event chain already owns the human and hidden-AI opening, delayed result, callback, cleanup, Event Log payload, and dedicated report image. This tranche connects that chain to the existing capped arc ledger through a separate major-stage dispatch source.

## Contract

The False Spring Losses candidate id remains the immutable arc identity and cleanup token. A major-stage envelope must match the exact ticket, generation, opening stage, identity, mode-specific token, state target, and candidate state proof. The opening is consumed without creating an ordinary receipt. Result and callback rows retain the major parent ticket, and cleanup releases the row only after both delayed cleanups are complete.

The pre-existing ordinary receipt path remains accepted for compatibility with legacy authored callers. It does not create a major ticket and follows the existing ordinary cleanup behavior.

## Validation boundary

Static review must confirm balanced Clausewitz blocks, no new ids, no ordinary wrapper call on the major branch, exact False Spring Losses identity and state fields, major-stage token matching, one-step stage progression, delayed-row parent linkage, cleanup release, no activation setter, and no release-floor credit. Runtime save, multiplayer, host-authority, Event Log delivery, and live presentation remain unproven because HOI4 is not run.

The dedicated proof is `FALLOUT_FALSE_SPRING_LOSSES_MAJOR_ARC_STAGE_PILOT_PROOF.md`.
