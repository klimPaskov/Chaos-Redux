# Metro Republic Below tranche addendum

Date: 2026-07-26

Status: accepted for a bounded dormant implementation pass. The source authority is `specs/60_reviewed_regional_metro_republic_below.md`.

This tranche fills the unimplemented Europe row in the regional event matrix. It is intentionally one authenticated state transaction. It does not claim province-level metro districts, a successor tag, or scheduler activation.

Reserved ownership is candidate `614`, events `614` through `620`, transaction `710059`, route `7159`, route upper bound `7160`, and Event Log history `9165`.

The implementation must use dedicated constants, triggers, effects, localisation, Event Log routing, a dynamic modifier file, a report DDS manifest, the candidate registry, and the shared generation reset hook. The authoritative workbook is updated only after the gameplay wording is final, followed by the repository export command.

The release floor remains `0 of 660`. The exact native all-valid-province thermonuclear sweep, blackout host authority, save recovery, multiplayer delivery, scheduler activation, successor allocation, and runtime Event Log proof remain outside this tranche.
