# Tunnel Ward Committees tranche addendum

This addendum promotes the reviewed East Asia Tunnel Ward Committees row into the dormant Fallout candidate pilot. It uses candidate `607`, event ids `607` through `613`, transaction `710058`, route `7158`, and Event Log history `9164`. Suffixes `593` through `599` remain vacant because the committed Radio Island tranche owns the higher event block `600` through `606`.

The row remains dormant behind the existing scheduler activation gate. It adds no scheduler setter, no new on-action, no country creation, and no fallback for missing shelter capacity. The state selector is fail-closed when `air_winter_shelter_capacity` is absent from the current Fallout state snapshot.

The implementation owns dedicated constants, triggers, effects, dynamic modifiers, Event Log payloads, localisation, report art, source manifest, candidate admission, central reset dispatch, source-of-truth ledgers, workbook row, and static proof. The human and hidden-AI paths share branch costs, frozen grading, delayed results, callback timing, cleanup, and cancellation.

Release-floor accounting remains `0 of 660` while scheduler activation is unset. After this tranche the reviewed ordinary pilot should contain `58` rows and the living-world ledger should contain `488` defined blocks, all dormant and uncounted.

Engine-sensitive surfaces remain runtime observations. The implementation records static evidence for native Command Power, War Support, state category, shelter-capacity snapshot, Deaths, regular target retention, delayed receipt reauthentication, Event Log routing, and dynamic modifier scope. No HOI4 process is launched.
