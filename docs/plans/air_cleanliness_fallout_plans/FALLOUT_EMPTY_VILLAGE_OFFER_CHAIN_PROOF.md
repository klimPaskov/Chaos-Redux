# The Empty Village Offer chain proof

## Implemented surfaces

- Candidate row `443`, transaction key `710037`, route `7137`.
- Human and hidden-AI event lanes `443` through `449`.
- Four government-aware settlement policies with separate Food, Medicine, Recognition, and Cohesion costs.
- Frozen settlement, land-legitimacy, border-security, and integration ledgers.
- Bounded migration and manpower effects, rural or pastoral state-category consequences, and a 540-day village review.
- State memory, temporary dynamic modifiers, Deaths-system failure handling, Event Log history `9142`, and authenticated cleanup.
- Dedicated report art, DDS, sprite registration, asset manifest, and GFX handoff.
- Catalog row `FALLOUT-443` is aligned in the authoritative workbook and generated CSV export.

## Static checks

The chain was reviewed for unique ids, branch and outcome key coverage, cross-file constants, Event Log mappings, localisation references, dedicated asset paths, state-category branches, and the scheduler candidate boundary.

The result and callback lanes both update the selected state's Adaptation, Reclamation, Supply Access, and Exposure values. Each lane applies its matching dynamic modifier and records a state flag before cleanup.

The state-category branch follows the installed vanilla `set_state_category = rural` and `set_state_category = pastoral` effect precedent. The migration path remains a ledger and manpower reserve because no native state-population relocation contract has been proven.

The report DDS is 210 by 176 with one uncompressed BGRA image level and an 840-byte row pitch.

The localisation file is UTF-8 with BOM.

The player-facing text contains no em dashes or semicolons.

## Dormant boundary

The chain remains dormant because Fallout scheduler activation, host authority, save recovery, multiplayer delivery, full-screen blackout ownership, and runtime Event Log delivery are not proven.

Native state-population relocation, native character creation, and dynamic successor tags are not claimed. The migration ledger and manpower reserve are the bounded engine-safe representation for this tranche.

No HOI4 runtime was launched, following the user instruction.
