# Rail Spine Vote tranche addendum

Date: 2026-07-26

Status: accepted for a bounded dormant implementation pass. The source authority is `specs/61_reviewed_regional_rail_spine_vote.md`.

This tranche fills one uncovered Eurasian interior regional row with a single authenticated railway state transaction. It does not activate the scheduler, create a successor tag, create a new government effect, enumerate provinces, or replace the native railway surface with a variable-only proxy.

## Collision-checked ownership

The reserved candidate is `621`. The event block is `621` through `627` under `add_namespace = chaosx.fallout`. The transaction key is `710060`, the scheduler route is `7160`, the new route upper bound is `7161`, and Event Log history is `9166`. These identities do not overlap the prior Fallout rows through Metro Republic Below or the zombie namespace.

The human opening is `621`, hidden AI opening is `622`, human result is `623`, hidden AI result is `624`, human callback is `625`, hidden AI callback is `626`, and cleanup is `627`. The country owns the transaction while the authenticated native state is the secondary actor.

## Gameplay contract

Country admission requires a current Fallout identity and generation, `fallout_region.eurasian_interior`, ordinary-event eligibility, current resource rows, and one affordable branch. The candidate producer chooses the lowest owned and controlled native state with a current Fallout row, a produced Air Winter snapshot, a non-damaged railway surface, a non-damaged infrastructure level, surviving population, workable Supply Access, shelter, reclamation, low Exposure, and controlled Disease Pressure.

The four manually authored branches are Open the central rail board, Grant the depots regional shares, Put the line under military guard, and Lease the line to merchant convoys. Each branch has a distinct Food, Fuel, Scrap, Recognition, Support Equipment, or Command Power cost. The chain freezes route trust, depot autonomy, timetable compliance, security control, merchant access, representation, faction pressure, native railway level, infrastructure, shelter, supply, reclamation, Food, Fuel, Scrap, Recognition, Cohesion, Exposure, Disease Pressure, and population before payment.

The result resolves after `35` days and the depot inspection callback resolves after `300` days. Deterministic scores use the frozen registry and branch-specific thresholds. Success, partial success, and failure change state Supply Access, Reclamation, railway or infrastructure condition, Exposure, Disease Pressure, Recognition, Cohesion, and branch-specific ledgers. Result and callback failure request population loss only through the shared Deaths contract.

## AI, receipts, and cleanup

The human and hidden AI lanes share affordability, branch payment, snapshot grading, delayed result scheduling, result application, callback grading, Event Log payloads, cancellation, and cleanup. AI scoring is government-aware and uses War Support, Cohesion, Fuel, Scrap, railway level, and faction pressure. It must never choose an unaffordable or stale branch.

Every delayed row reauthenticates owner, controller, target state, transaction ticket, event token, mode, branch, and Fallout generation. A stale target, changed owner, generation change, or failed payment cancels the exact receipt, clears the state reservation, records cancellation history, and leaves no branch modifier behind. Cleanup releases result and callback tickets independently and closes the durable memory only after both rows have settled.

## Implementation and review surfaces

The implementation owns a dedicated constants file, state and country triggers, effects, dynamic modifiers, Event Log payload localisation, event blocks, player-facing localisation, candidate registry row, shared generation reset hook, source spec index line, dedicated report DDS, source manifest, proof file, workbook row, and exporter output. The report card is fictional and contains no readable generated text, identifiable real people, real flags, or reused zombie assets.

The release floor remains `0 of 660` because scheduler activation is still unset. The exact engine-native all-valid-province thermonuclear sweep, blackout host authority, save recovery, multiplayer input blocking, runtime Event Log delivery, successor allocation, and live delayed-event delivery remain outside this tranche and must remain explicit blockers in the proof.
