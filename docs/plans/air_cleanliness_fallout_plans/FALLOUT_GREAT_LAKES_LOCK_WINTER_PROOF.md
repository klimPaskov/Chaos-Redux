# Great Lakes Lock Winter tranche proof

The Great Lakes Lock Winter tranche is reserved as candidate `663` with opening, hidden AI, delayed result, callback, and cleanup ids `663` through `669`. It is dormant while Fallout scheduler activation remains unset.

Static ownership checks found the ids, transaction key `710066`, route `7166`, history `9172`, constants, effects, triggers, events, localisation, report sprite, and asset manifest in one ownership set. The trigger proof requires a native coastal state with a non-damaged naval base and infrastructure. The source spec is `docs/specs/air_cleanliness_fallout_specs/specs/67_reviewed_regional_great_lakes_lock_winter.md`.

The cleanup gate admits a result-only schedule-error receipt without requiring a callback cleanup ticket. Callback-scheduled and callback-resolved paths still require that ticket, and all paths retain the candidate, route, transaction, owner, target, branch, and result receipts. The hidden-AI lane scores all four affordable branches from authored base priorities, then applies high-pressure, government-archetype, and convoy-orientation factors before selecting the highest score with deterministic tie order and an affordability fallback.

The authored choices cover joint ice authority, unilateral opening, seasonal closure, and rival lock sabotage. Each branch has concrete costs, deterministic affordability parity, a delayed 35-day result, graded success, partial, and failure outcomes, and a 270-day thaw callback. The result and callback reauthenticate owner, controller, native state id, naval-base and infrastructure surfaces, current generation, Air Winter values, branch, token, and delayed ticket before writing state or country ledgers. Failure routes bounded population loss through `apply_exact_state_civilian_population_loss` and the Fallout aftermath Deaths cause.

The dedicated report card is a fictional generated scene. It is processed at 210x176, converted to DDS, registered as `GFX_report_event_fallout_great_lakes_lock_winter`, and documented under `docs/assets/663_great_lakes_lock_winter/manifest.md`.

Runtime delivery, save recovery, host authority, multiplayer input blocking, delayed dispatch, Event Log rendering, normal-map presentation, and full-screen blackout remain user validation surfaces. The exact engine-native all-valid-province thermonuclear sweep remains unproven and is not claimed by this tranche.
