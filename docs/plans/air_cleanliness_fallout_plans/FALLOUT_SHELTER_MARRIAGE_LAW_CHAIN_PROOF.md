# Shelter Marriage Law chain proof

Status: dormant, statically reconciled, not release-floor credit.

## Source evidence

- Events `303` through `309` are each defined once under
  `add_namespace = chaosx.fallout`.
- The candidate row uses country id `303`, transaction `710017`, route `7117`,
  and history `9122`. It has no state, province, actor, or bilateral target.
- The gate requires closed Empty Ward memory, generation count at least two,
  the 720 through 1500 campaign window, Cohesion, Food, Shelter, Recognition,
  and one branch cost.
- Four manually authored policies have separate costs, success thresholds,
  partial thresholds, durable law ledgers, failure Deaths, and route-specific
  modifiers.
- The opening freezes Cohesion, Food, Shelter, Recognition, generation count,
  legitimacy, integration, fertility, and exposure before the delayed result.
- The result and callback carry explicit resource, cohesion, population,
  stability, legitimacy, integration, fertility, and exposure deltas.
- History `9122` has fifteen payloads. Shared Event Log type, detail, and name
  selectors route to the Shelter Marriage Law localisation.
- Cleanup releases exact result and callback receipts, then clears transaction
  variables and transient flags while retaining the selected law and durable
  family ledgers.
- A dedicated report image is registered as
  `GFX_report_event_fallout_shelter_marriage_law`.

## Static audit record

No HOI4 runtime was launched. The engine-sensitive ordinary dispatcher,
delayed-result queue, hidden-AI delivery, save recovery, host authority, and
multiplayer behavior remain unobserved. The exact engine-native all-valid-land-
province sweep blocker for the manual Fallout scenario is unchanged.

A read-only `hoi4.event_inspect` lint request for `chaosx.fallout.303` was
attempted with helper expansion disabled and a bounded one-level graph. The
tool returned `Transport closed`, so this tranche claims no engine inspection
result. Direct source audits remain the authoritative evidence for this
dormant chain.
