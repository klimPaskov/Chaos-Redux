# Authoritative catalog update

Role: `chaosx_spreadsheet_doc_worker`.

Execution status in this package: NOT EXECUTED.

## Context

Event 073 Mongols Rise restores a cavalry empire with rapid conquest, persistent defensive weakness, three political routes, four regional khanate identities, accountable tribute, Karakorum, connected routes, three Evolutions, succession, collapse and recovery. The main specification is under `docs/specs/073_mongols_rise_specs/`. Plans and evidence are under `docs/plans/073_mongols_rise_plans/`.

## Required reading

This role’s limited reading contract, the provided catalog snapshots, intended 073 row and actual authoritative workbook. Follow the current installed profile’s reading limits and referenced-file requirements. Record unavailable files explicitly. Do not treat a search snippet or a truncated response as a full read.

## Assigned work

Inspect the actual workbook before any write. Preserve its structure, formulas and formatting. Update Event 73 and Formables membership only to the accepted design and achieved status, then regenerate exports through the existing process. CSV snapshots are read-only evidence.

## Ownership

Spreadsheet and documentation work only after an explicit catalog assignment and workbook access. Use a fresh role context and `fork_turns="none"` when the actual interface supports it. The parent owns shared integration and the disposition of proposals. If this is still a planning-only task, make no gameplay edits.

## Return contract

Return inspected workbook identity, exact approved cells or rows, preserved structure proof, regenerated export provenance, and any unavailable source.

Separate source-derived facts, authored proposals, verified implementation, and blocked work. Include actual paths and evidence for completion claims. Do not claim another role ran, a render passed, a model was generated, a recording was licensed, or a game test succeeded unless that action actually occurred.
