# Current local evidence and identity inventory

Role: `chaosx_repo_explorer`.

Execution status in this package: NOT EXECUTED.

## Context

Event 073 Mongols Rise restores a cavalry empire with rapid conquest, persistent defensive weakness, three political routes, four regional khanate identities, accountable tribute, Karakorum, connected routes, three Evolutions, succession, collapse and recovery. The main specification is under `docs/specs/073_mongols_rise_specs/`. Plans and evidence are under `docs/plans/073_mongols_rise_plans/`.

## Required reading

AGENTS.md, the event skill, this package’s repository evidence and integration files. Follow the current installed profile’s reading limits and referenced-file requirements. Record unavailable files explicitly. Do not treat a search snippet or a truncated response as a full read.

## Assigned work

Inspect current 073 consumers, the dispatcher, Evolution and Chaos helpers, existing country identities, character candidates, regional map sets, and the exact Karakorum site. Compare the pinned findings with the actual local commit. Separate fully read files from search-only or truncated results.

## Ownership

Read-only inventory and source findings. Do not implement the rework. Use a fresh role context and `fork_turns="none"` when the actual interface supports it. The parent owns shared integration and the disposition of proposals. If this is still a planning-only task, make no gameplay edits.

## Return contract

Return exact paths and versions, reused tag candidates, verified mechanics, unresolved local references, and the narrow blockers that affect the next stage.

Separate source-derived facts, authored proposals, verified implementation, and blocked work. Include actual paths and evidence for completion claims. Do not claim another role ran, a render passed, a model was generated, a recording was licensed, or a game test succeeded unless that action actually occurred.
