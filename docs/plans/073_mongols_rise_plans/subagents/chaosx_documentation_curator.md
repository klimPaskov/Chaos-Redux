# One coherent source of truth

Role: `chaosx_documentation_curator`.

Execution status in this package: NOT EXECUTED.

## Context

Event 073 Mongols Rise restores a cavalry empire with rapid conquest, persistent defensive weakness, three political routes, four regional khanate identities, accountable tribute, Karakorum, connected routes, three Evolutions, succession, collapse and recovery. The main specification is under `docs/specs/073_mongols_rise_specs/`. Plans and evidence are under `docs/plans/073_mongols_rise_plans/`.

## Required reading

The documentation profile and Event 073 source, requirements and disposition records. Follow the current installed profile’s reading limits and referenced-file requirements. Record unavailable files explicitly. Do not treat a search snippet or a truncated response as a full read.

## Assigned work

Keep accepted design in the owning specification files, preserve implementation and evidence history in plans, and remove contradictory addendum trails only after promotion is recorded. Update indexes, crosslinks, source provenance, and achieved status accurately.

## Ownership

Documentation only within the assigned package and approved central docs. Use a fresh role context and `fork_turns="none"` when the actual interface supports it. The parent owns shared integration and the disposition of proposals. If this is still a planning-only task, make no gameplay edits.

## Return contract

Return promoted changes, preserved decisions, repaired navigation, remaining contradictions, and truthful read and execution status.

Separate source-derived facts, authored proposals, verified implementation, and blocked work. Include actual paths and evidence for completion claims. Do not claim another role ran, a render passed, a model was generated, a recording was licensed, or a game test succeeded unless that action actually occurred.
