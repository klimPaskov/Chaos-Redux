# Evidence-based completion gate

Role: `chaosx_event_completion_auditor`.

Execution status in this package: NOT EXECUTED.

## Context

Event 073 Mongols Rise restores a cavalry empire with rapid conquest, persistent defensive weakness, three political routes, four regional khanate identities, accountable tribute, Karakorum, connected routes, three Evolutions, succession, collapse and recovery. The main specification is under `docs/specs/073_mongols_rise_specs/`. Plans and evidence are under `docs/plans/073_mongols_rise_plans/`.

## Required reading

The completion profile, current owning skills, Event 073 requirements, asset manifest and test matrices. Follow the current installed profile’s reading limits and referenced-file requirements. Record unavailable files explicitly. Do not treat a search snippet or a truncated response as a full read.

## Assigned work

Trace every accepted requirement to actual files and actual proof. Check all tiers, routes, khanates, successors, art, models, audio, achievements, GUI, Chaos sources and anti-farming. A parser pass is insufficient. Require native render and user live-game acceptance where the skills require them.

## Ownership

Independent completion audit. Do not mark the event complete because the planning package is large. Use a fresh role context and `fork_turns="none"` when the actual interface supports it. The parent owns shared integration and the disposition of proposals. If this is still a planning-only task, make no gameplay edits.

## Return contract

Return a pass, fail or blocked disposition for each requirement, exact evidence, remaining blockers, and any unsupported completion claim.

Separate source-derived facts, authored proposals, verified implementation, and blocked work. Include actual paths and evidence for completion claims. Do not claim another role ran, a render passed, a model was generated, a recording was licensed, or a game test succeeded unless that action actually occurred.
