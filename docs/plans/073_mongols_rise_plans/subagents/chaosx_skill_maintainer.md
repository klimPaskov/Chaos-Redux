# Resolve recurring skill conflicts without scope creep

Role: `chaosx_skill_maintainer`.

Execution status in this package: NOT EXECUTED.

## Context

Event 073 Mongols Rise restores a cavalry empire with rapid conquest, persistent defensive weakness, three political routes, four regional khanate identities, accountable tribute, Karakorum, connected routes, three Evolutions, succession, collapse and recovery. The main specification is under `docs/specs/073_mongols_rise_specs/`. Plans and evidence are under `docs/plans/073_mongols_rise_plans/`.

## Required reading

The skill-maintainer profile and the specific conflicts recorded for Event 073. Follow the current installed profile’s reading limits and referenced-file requirements. Record unavailable files explicitly. Do not treat a search snippet or a truncated response as a full read.

## Assigned work

Review the firearm geometry wording, audio duration conflict, runtime model configuration, and GUI rewrite wording only if assigned by the parent. Propose narrowly scoped corrections backed by the current owning skills. Do not use one event to rewrite unrelated project policy.

## Ownership

Skill maintenance is a separate authorized task, not an automatic side effect of planning Event 073. Use a fresh role context and `fork_turns="none"` when the actual interface supports it. The parent owns shared integration and the disposition of proposals. If this is still a planning-only task, make no gameplay edits.

## Return contract

Return the exact conflict, authoritative basis, minimal proposed wording, affected readers, and approval status.

Separate source-derived facts, authored proposals, verified implementation, and blocked work. Include actual paths and evidence for completion claims. Do not claim another role ran, a render passed, a model was generated, a recording was licensed, or a game test succeeded unless that action actually occurred.
