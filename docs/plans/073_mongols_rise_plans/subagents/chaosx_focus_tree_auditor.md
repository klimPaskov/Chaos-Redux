# Three political routes and flexible campaign tree

Role: `chaosx_focus_tree_auditor`.

Execution status in this package: NOT EXECUTED.

## Context

Event 073 Mongols Rise restores a cavalry empire with rapid conquest, persistent defensive weakness, three political routes, four regional khanate identities, accountable tribute, Karakorum, connected routes, three Evolutions, succession, collapse and recovery. The main specification is under `docs/specs/073_mongols_rise_specs/`. Plans and evidence are under `docs/plans/073_mongols_rise_plans/`.

## Required reading

The focus-tree skill and all Event 073 focus_tree and expansion design files. Follow the current installed profile’s reading limits and referenced-file requirements. Record unavailable files explicitly. Do not treat a search snippet or a truncated response as a full read.

## Assigned work

Check rapid opening, route exclusivity, distinct political consequences, flexible regional order, meaningful capstones, lost-target recovery, three-spirit budget, and the inlay’s reserved space. Inspect the actual tree and render it when implementation exists.

## Ownership

Review the assigned tree and its narrow supporting consumers. Do not impose an arbitrary focus count. Use a fresh role context and `fork_turns="none"` when the actual interface supports it. The parent owns shared integration and the disposition of proposals. If this is still a planning-only task, make no gameplay edits.

## Return contract

Return route-by-route gaps, a prerequisite and bypass review, real native layout evidence when available, and the smallest repairs that preserve depth.

Separate source-derived facts, authored proposals, verified implementation, and blocked work. Include actual paths and evidence for completion claims. Do not claim another role ran, a render passed, a model was generated, a recording was licensed, or a game test succeeded unless that action actually occurred.
