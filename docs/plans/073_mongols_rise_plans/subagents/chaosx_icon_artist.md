# Readable icon families and achievements

Role: `chaosx_icon_artist`.

Execution status in this package: NOT EXECUTED.

## Context

Event 073 Mongols Rise restores a cavalry empire with rapid conquest, persistent defensive weakness, three political routes, four regional khanate identities, accountable tribute, Karakorum, connected routes, three Evolutions, succession, collapse and recovery. The main specification is under `docs/specs/073_mongols_rise_specs/`. Plans and evidence are under `docs/plans/073_mongols_rise_plans/`.

## Required reading

The event-assets skill, exact local reference templates, and Event 073 asset and achievement registries. Follow the current installed profile’s reading limits and referenced-file requirements. Record unavailable files explicitly. Do not treat a search snippet or a truncated response as a full read.

## Assigned work

Create separate focus, idea, decision, counter, template-emblem and achievement compositions for assigned consumers. Preserve immutable achievement state templates. Review every icon at native size and ensure that tiny details do not carry essential meaning.

## Ownership

Assigned icon assets and their manifest. No portrait work. Use a fresh role context and `fork_turns="none"` when the actual interface supports it. The parent owns shared integration and the disposition of proposals. If this is still a planning-only task, make no gameplay edits.

## Return contract

Return explicit per-consumer rows, native-size previews, alpha and DDS checks, all achievement states, and correct counter frames.

Separate source-derived facts, authored proposals, verified implementation, and blocked work. Include actual paths and evidence for completion claims. Do not claim another role ran, a render passed, a model was generated, a recording was licensed, or a game test succeeded unless that action actually occurred.
