# Legal targets and measured AI choices

Role: `chaosx_ai_probability_auditor`.

Execution status in this package: NOT EXECUTED.

## Context

Event 073 Mongols Rise restores a cavalry empire with rapid conquest, persistent defensive weakness, three political routes, four regional khanate identities, accountable tribute, Karakorum, connected routes, three Evolutions, succession, collapse and recovery. The main specification is under `docs/specs/073_mongols_rise_specs/`. Plans and evidence are under `docs/plans/073_mongols_rise_plans/`.

## Required reading

The AI probability profile and Event 073 AI design and scenario matrix. Follow the current installed profile’s reading limits and referenced-file requirements. Record unavailable files explicitly. Do not treat a search snippet or a truncated response as a full read.

## Assigned work

Filter valid pools before scoring. Test the twenty scenarios, normal mounted recruitment, safe staging, tribute feasibility, succession preparation, and post-collapse survival. Separate raw scores from normalized probability and observed behavior.

## Ownership

Read and test assigned AI logic. Do not invent probabilities from a single weight. Use a fresh role context and `fork_turns="none"` when the actual interface supports it. The parent owns shared integration and the disposition of proposals. If this is still a planning-only task, make no gameplay edits.

## Return contract

Return scenario inputs, exclusions, raw modifiers, normalized pools where supported, observed choices, and reproducible defects.

Separate source-derived facts, authored proposals, verified implementation, and blocked work. Include actual paths and evidence for completion claims. Do not claim another role ran, a render passed, a model was generated, a recording was licensed, or a game test succeeded unless that action actually occurred.
