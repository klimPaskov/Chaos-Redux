# Event 073 asset production prompt

Use the current chaos-redux-event-assets skill and all applicable frame-animation, portrait, 3D, GUI, and super-event requirements. Read `docs/specs/073_mongols_rise_specs/presentation/02_asset_design.md`, the presentation and achievement files, and both asset handoffs under `docs/plans/073_mongols_rise_plans/assets/`.

Produce the complete assigned visual and audio asset package for Mongols Rise. The event is a twentieth-century cavalry restoration. Its images must distinguish military command, law, Chinese industry, Central Asian routes, western campaigning, Persian administration, southern logistical challenges, succession and political fragmentation.

Begin by expanding the family manifest into explicit per-consumer rows after the current tree, country, character and GUI inventories are bound. Record exact source, creator, rights, attribution, source hash, dimensions, alpha, owner, output, consumer and evidence. An unexpanded family row is not a completed asset. Preserve the three-spirit budget and the distinct seven template-family identities without creating seven redundant unit models.

Create every required report, news image, super-event still and required animated variant, flat imperial and khanate flag, focus icon, idea icon, decision icon, achievement state, counter, template emblem, portrait handoff, GUI element, mounted model, animation and sound. Use the native dimensions and exact reference templates from the manifest and current local skills. Inspect final-size results. Do not resize one large composition into every icon family.

Research symbols and real identities before generation. All new flags follow the required ImageGen workflow. Portrait work belongs only to chaosx_portrait_creator. Preserve its durable source archive and leave the user-controlled RunPod styling step to the user. Do not invent a descendant’s face or use an arbitrary person as a historical candidate.

The required mounted unit follows the full owning 3D skill. Use eligible modern professional concept art, fresh Meshy 7 weapon-free body geometry, and separate Meshy 7 firearm geometry. Then use direct Blender assembly, rigging, materials, manual animation, attachments and export. Prove horse and rider contact, scale, every consumed animation state, firing, effects, sound, reimport and actual runtime selection. No placeholder model or missing state is accepted as final.

Use real licensed online audio only. Check both composition and recording rights for super-events. Do not generate or self-record replacements. Do not overwrite unrelated infantry selection voices to hide a custom-unit consumer limitation.

Animations require real source frames, an actual recorded backend, correct sheets, native playback and static fallbacks. A pulsing still is not a substitute. Use the repository DDS converter and verify alpha and output format.

Keep final text separate from image directions until research is approved. Use the event UI worker only within its assigned ownership. Obtain actual native GUI and focus renders and live runtime evidence. Preserve sources and hashes before temporary cleanup. Return completed, failed and blocked rows honestly. Do not claim full asset completion while research, user portrait handoff, model, sound, animation, or runtime proof remains open.
