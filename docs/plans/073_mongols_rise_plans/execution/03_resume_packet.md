# Durable resume packet

## What exists

The package contains the expanded Event 073 specification, three political path designs, shared development and all requested regional directions, four khanate identities, action and mission matrices, achievements, production briefs, source records, implementation stages, twenty subagent handoffs, and six final task prompts.

This is a durable project handoff. It is not a temporary chat continuation prompt and does not instruct an assistant to reconstruct missing conversation state.

## Next valid action

An implementation agent starts at Stage 0. It reads the current local instructions and linked references, verifies the current repo against the pinned evidence, binds country identities and map sets, confirms cavalry-category and capital capabilities, and discovers actual tools. It then builds the baseline vertical slice.

Do not begin by copying planning labels into localisation or by updating the catalog to a more complete status. Do not skip missing local source references on the assumption that they were included in the supplied archive.

## Important open gates

The complete local reference library, exact map and capital bindings, country and character inventory, final media sources and rights, custom model production, actual independent subagents, native rendering, game tests and user live-game acceptance remain open. See the conflict register for B01 through B12.

## Change discipline

Change the owning specification section when an accepted design decision changes. Record why in the disposition log. Do not add a second contradictory mechanics document or silently weaken the core cavalry and defensive-weakness requirements.

Preserve one-time entitlement and payment history through future implementation changes. Keep package checks distinct from runtime validation. A resumed implementation must report what it actually verified in its own environment.
