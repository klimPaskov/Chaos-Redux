# Event 018 icon provenance repair handoff

## Disposition

The completion-audit provenance finding is repaired by `docs/assets/018_resources_found/icon_generation_provenance_ledger.md`. The specialist subagent produced the exhaustive ledger and the two missing decision-consumer mappings before its final response was interrupted by the Codex usage limit. The parent therefore re-audited the saved files directly and owns this closure handoff; the interrupted response itself is not treated as evidence.

## Delivered records

- 65 focus icon rows, `F001` through `F065`
- 36 unique idea/state icon rows, `I001` through `I036`
- 44 decision or category-icon rows, `D001` through `D044`
- 5 category-picture rows, `C001` through `C005`

Every row supplies the exact runtime, source, and processed basenames; asset type and use; sprite; family-specific prompt plus asset-specific subject; generated source provenance; reference-inspection disposition; processing path; originality basis; and status. The family conformance table supplies exact dimensions, target GFX file, related live-surface rule, surviving uncertainty, and the asset skill's valid missing-reference-folder disposition for each row range.

The original per-call API request bodies were not retained and have not been reconstructed as quotations. The ledger records the surviving pre-production family prompt contract plus each final asset-specific subject. Source history is independently preserved in the source files: all 150 contain C2PA assertions naming the OpenAI Media Service API and `trainedAlgorithmicMedia`. The three named skill reference folders are absent in this checkout; under the asset skill's explicit missing-folder rule, the delivered family contact sheets and corresponding vanilla focus, idea, and decision libraries were inspected instead. This is disclosed per family and is not represented as a production-time inspection of a nonexistent folder.

## Parent validation

A complete physical and registration scan on 12 July 2026 found:

| Family | Ledger rows | Source C2PA assertions | Processed dimensions | Runtime dimensions | Processed/runtime pixel parity |
| --- | ---: | ---: | --- | --- | ---: |
| Focus | 65 | 65 | `94x86` | `94x86` | 65/65 |
| Idea/state | 36 | 36 | `64x64` | `64x64` | 36/36 |
| Decision/category icon | 44 | 44 | `32x32` | `32x32` | 44/44 |
| Category picture | 5 | 5 | `114x101` | `114x101` | 5/5 |

The scan also found 150 unique source SHA-256 hashes, 150 unique processed hashes, 150 unique runtime hashes, one and only one sprite definition for every row, no missing file, and no cross-row pixel duplicate.

The live decision script has 125 explicit decision/mission icon assignments. `gfx_handoff.md` now contains all 125 exact category/consumer/sprite triples, plus the five category icon and five category-picture mappings. The previously omitted mappings are:

- `resources_found_dispute_settlement_stability_mission` to `GFX_decision_resources_found_commission_arbitration`
- `resources_found_burrow_objective_window_mission` to `GFX_decision_resources_found_cave_assault`

The earlier wording “39 decision/category icons” is resolved as 39 action-family icons plus 5 category icons, for 44 files in the provenance ledger. Five category pictures are a separate family.

## Files changed

- `docs/assets/018_resources_found/icon_generation_provenance_ledger.md`
- `docs/assets/018_resources_found/gfx_handoff.md`
- `docs/plans/018_resources_found_plans/subagent_handoffs/icon_provenance_repair_handoff.md`

No runtime art or gameplay file needed replacement. No placeholder, resized cross-type icon, duplicated source, missing mapping, or unreported provenance uncertainty remains in this tranche.
