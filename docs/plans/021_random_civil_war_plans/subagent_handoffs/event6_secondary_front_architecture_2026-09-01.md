# Event 021 Event 006 secondary-front architecture

Date: 2026-09-01.

Owner: parent implementation pass after two bounded `chaosx_scripted_system_architect` attempts remained nonresponsive and produced no handoff.

Status: implemented current-source architecture; no live engine sequence claimed.

## Ownership boundary

`event021_parent_start_event6_front` remains the primary-opening adapter and is not reused for an additional front because its rollback deliberately tears down the whole opening crisis and its host flag identifies Event 006 as the primary claimant.

The additional-front path is owned by `event021_parent_try_event6_secondary_front`, `event021_parent_abort_event6_secondary_front`, `event021_parent_review_event6_secondary_front`, and `event021_parent_finalize_event6_secondary_actor`.

## Admission and package safety

The original government host can attempt one package-owned additional front only while the ordinary crisis is active, the bounded Evolution I or High/Maximum scenario setup gate is available, a host belligerent slot and global front slot remain, the host controls more than one state, and Event 006 reports a dormant complete package.

The existing 32-package loader remains authoritative for tag, carrier, anchor, identity, leader, focus, decision, formable, force, reinforcement, AI, and asset selection. Secondary search adds an exact anchor constraint: the package anchor must remain owned and controlled by the host, cannot be its capital, and cannot already occupy a reserved or registered front state.

The adapter does not call Event 006 fired-count, active-origin, network, evolution, or league registration. It records Event 021 origin through the existing package-owned adapter contract.

## Transaction and rollback

The additional front allocates one front ID, keeps the current crisis ID and generation, transfers one package anchor, runs complete Event 006 package setup, and requires an actual war with the original host before registering the actor and incrementing the belligerent count.

Failure returns the anchor, unregisters only a partially registered package front/theater, clears adapter runtime flags, and annexes only a carrier newly released for the failed transaction. It never rolls back the ordinary primary opening or any committed front.

The shared package-transaction cleanup consumes loader selections, package-pending globals, host package variables, and short-lived selected actor/anchor targets after success or rollback. It deliberately preserves the ordinary crisis identity and force receipts required by the remaining front setup.

The primary Event 006 opening was tightened in the same review: it now requires declaration eligibility and proves `has_war_with` before registering the actor, publishing the host pointer, or recording the opening. A failed declaration returns through the complete opening rollback instead of leaving a registered nonbelligerent package actor.

## Lifecycle separation

Primary Event 006 ownership continues to use `random_civil_war_event6_host_side` and `random_civil_war_opposition_actor_scope`.

An additional package uses `random_civil_war_event6_secondary_host_side`, `random_civil_war_event6_secondary_front_active`, and `random_civil_war_event6_secondary_actor_scope`. The bounded review clears the additional-front activity when its war ends or its actor disappears. Settlement and reconstruction finalize that actor through its own pointer, so the ordinary primary claimant is never mistaken for the package country.

## Evidence boundary

Focused helper lints after the final transaction cleanup returned partial workspace analysis with no Event 021 blocker:

- Parent effects: `hoi4-agent://workspace/mod_chaos_redux_ea3b2d67c2c0/artifact/b5b4ab6b2d2c5a33cf378e265714c21057d07f536100fb7994c7b971e787b065/54b920001ae899366e944b5d54ad16683cfbdeb16b458a5d089119b4d33d8956/event-lint-f91c43e25a67.json`.
- Parent triggers: `hoi4-agent://workspace/mod_chaos_redux_ea3b2d67c2c0/artifact/7005cfa69e77f279d76d1434ac04728547b1ad9a6b7ca9d6379f0c03d372eba5/2c912cdce127ec6db105bfc25fcf4a672c97458a6ee85434dc8cfcceedb4a325/event-lint-f91c43e25a67.json`.

The installed analyzer still defers workspace-wide helper lifecycle projection. A live package release, war, settlement, and cleanup sequence is not claimed.

A subsequent helper-expanded `state_flow` request for country flag `random_civil_war_event6_secondary_front_active` ran beyond four bounded waits and then failed with `Transport closed`, producing no artifact. This is recorded as a tooling blocker and is not treated as lifecycle proof.
