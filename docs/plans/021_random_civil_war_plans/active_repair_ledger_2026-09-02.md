# Event 021 active repair ledger

Status: Needs Testing for the rework catalog state; acceptance remains incomplete and the release gate remains closed.

This ledger supersedes source-complete claims in the September 1 completion audit and August 31 resume packet wherever they conflict with the source findings below.
Existing visual reviews remain useful for the exact files inspected, but do not certify the gameplay consumers or reused Event 006 packages.

## Implemented repairs awaiting full certification

- The six exact achievement IDs have registry consumers in `common/achievements/chaos_redux_achievements.txt`; the eighteen icon states previously existed without those consumers.
- The achievement predicates name the six requested routes rather than six unrelated generic accomplishments, and Event 006 postwar survival no longer invents a package milestone.
- The existing Chaos global-host pulse calls `event021_parent_global_scheduler_pulse` on its normal and host-recovery branches.
  The scheduler checks the actual `is_global_host` country flag, the release-or-committed-campaign gate, and one global date receipt to suppress duplicate same-day work.
- The invalid active-front helper alias and target-pool flag-as-trigger call are corrected.
- `event021_parent_copy_opening_state_receipts` copies global transaction arrays into the current country through an explicit local event target.
  Its five callers cover the original host, primary ordinary claimant, secondary ordinary claimant, primary Event 006 actor, and secondary Event 006 actor.
  The two secondary paths retain their exact anchor as their detached-state receipt.
- Achievement reintegration checks use the explicit receiving country instead of scheduler ROOT, reject an empty original-core array, and clear recomputable positive control receipts before reevaluation.
- `021_random_civil_war_achievement_effects.txt` captures a decisive-capital pointer at commitment, latches loss through state-control callbacks and bounded reviews, records external capitulation through its event callback, and tracks the authority minimum through the shared updater.
  Original-host commitment clears stale settlement-recorded, reconstruction-complete, internal-resolution, and victory receipts; the six registry consumers also recognize their legitimate latched award flags.
  This is implemented source, not successor or runtime certification.
- The nested-sovereignty receipt snapshots an existing normal human Event 006 identity from either ordinary Independence Wave origin or Event 021 origin before opening.
  A Flag of Our Own remains restricted to Event 021 origin, and original-player-government eligibility is refreshed for each opening rather than inherited from an earlier human-controlled crisis.
- Provisional generation allocation has a rollback snapshot, successful commitment consumes the previous recurrence window, and cleanup releases the generation-seeded flag.
  Failed preflight no longer intentionally consumes recurrence dates or leaves its provisional generation as the committed count.
- Earlier owner patches add the reduced major-target factor, sampled and bounded active evolution timing, recurrence final-window retry, and sponsor viability/alignment filters.
  Their required same-scenario probability certification is not complete.
- Shared opening-receipt paths propagate sticky debug, force-trigger, scenario, and original-host achievement disqualification to ordinary and Event 006 actors.
- Ordinary successor promotion copies predecessor opening cores, opening count, original-capital pointer, external-war/capitulation facts, harsh-settlement and multi-front receipts, and disqualification before predecessor cleanup.
  It does not grant original-player-government status or transfer earned awards, and the predecessor white-peace target is explicit rather than scheduler ROOT.
  Remaining-front transfer and signatory succession are not certified by this history-only repair.
- Settlement receipt refresh no longer labels every current capitulation as external defeat; the victor-aware callback owns that distinction and its failure latch.
- Scripted ordinary successor promotion keeps predecessor/front records intact until `on_annex`, uses an explicit local annex target, saves both the history and treaty predecessor pointers, prepares successor roster adoption before white peace/annex, and requires matching nonmissing crisis IDs before accepting either a durable predecessor pointer or the global-pointer recovery path.
  The successor marker is set before white peace and annexation; the old pre-annex core cleanup and unguarded duplicate fallback branch are removed.
  Natural engine annexation, surviving-front settlement obligations, actual external-war continuity, and callback execution remain unproven.
- `on_annex` also recognizes natural ordinary-claimant victory before predecessor cleanup through an explicit host pointer, government role, and matching crisis IDs.
  It transfers history and marks legitimate succession without guessing from mere annexer status.
  The effect does not itself establish remaining wars; that continuity is still an open lifecycle check.
- Internal-war-ended pointer branches reject self, missing/dead, and different-crisis countries.
  A successor's rebound host pointer to itself can no longer count as a peacefully resolved opponent, and another theater's cached global actor cannot resolve this crisis.
  The surviving-front roster adoption helper and natural end hook are now wired, but actual war continuity, remaining-front rebinding in a live save, and terminal resolution remain separate unfinished requirements.
- Committed-opening history clears prior cleanup-complete/requested and settlement pending/signed/resolved/talks UI commands, plus completed reconstruction phase receipts.
  This prevents a previous crisis from hiding renewed settlement actions or supplying an old signed command to a recurrence.
  Agreement history, violations, obligations, recurrence memory, and earned awards are deliberately not cleared by this narrow command-state repair.
- Ordinary core cleanup preserves completed Event 021-origin Event 006 package identity receipts while still clearing incomplete adapter receipts.
  `is_independence_wave_event021_package_country` extracts the existing proven-package branch from the package-content predicate; the strict Event 006 active-origin classifier remains unchanged.
  The focus tree, shared category registry, package-specific category gates, and the local sections of the aggregate Event 006 decision file now use the origin-neutral local-content predicate where appropriate; network/league, adapter-only, evolution, and other explicitly global gates remain strict. Reachability and engine acceptance for reused content are still unproven, and no reused content is certified.
- The constants architect supplied 231 previously undefined unique keys and reports zero missing Event 021 tokens across its 487-token inventory, with fractional action tuning changed to `fixed_point`.
  Parent review confirms the declared namespaces and preserved major factor/evolution timing, but nine inferred tuning groups remain provisional pending owner balance review and the required probability comparisons.
- The current automatic opening severity source is a deterministic pressure-and-viability ladder: Limited, Serious, Severe, or Critical, with one-state and all-island routes forced back to Limited and major bands evolution-gated.
  The master spec describes expected bands while the earlier improvement addendum describes weighted severity pools; this source/design discrepancy is recorded rather than silently certified.
- The sponsor audit reproduces the SPN-01 through SPN-05 raw-score relationships, including hard-zero armed support for the mediator profile, but cannot compare to the inaccessible September 1 artifact.
  Complete pre-resource-guard decision/trigger/constants bodies are preserved under `probability_snapshots/sponsor_resource_guard_before_2026-09-02/`.
  The owner then mirrored existing political-power and convoy availability gates in both support scores, and political-power/command-power/convoy gates in mediation scores.
  Costs and thresholds did not change; this repairs score/availability consistency, not a proven ability to purchase unavailable decisions.
  The same-scenario post-guard comparison is recorded in `subagent_handoffs/probability_sponsor_resource_guards_2026-09-02.md` using the actual saved-before source and unchanged SPN-01 through SPN-05 bodies.
  MCP emitted comparison `probability-248f44a08b739e9eaafee94a` with twelve changes, but direct political-power, command-power, and surrender inputs remain unresolved; this is a completed partial comparison, not balance certification.
  The synthetic mediation convoy edge produced a score change from 1.5 to zero; support-convoy isolation remained confounded, and PP/CP edge outcomes are not certified.

## Current validation

The opening-array source check finds one copy-helper definition, five callers, zero implicit state-scoped opening-array writes, and zero ROOT-owned opening-array writes in the parent effects file.
This confirms the specific source repair, not engine execution.

Successor source-order assertions confirm both predecessor/fallback paths require matching crisis IDs, the successor marker precedes white peace, the annex target is explicit, and no pre-annex core cleanup remains.
The natural-victory hook is ordered before FROM teardown.
Committed-opening assertions confirm the five action-blocking cleanup and settlement command receipts are cleared.
These are call-order and receipt checks, not engine execution or remaining-war continuity proof.

The September 2 `hoi4.event_inspect` call used selector `{ kind: "event", eventId: "chaosx.nr21.1" }`, downstream depth 1, twenty nodes, thirty edges, and no helper expansion.
It returned `EVENT_INSPECTED_PARTIAL`, revision `c9e9ac572a640a5ffaf82e4cd3c6b88ce5293ff4ded2a168fb5fb0beb1576d9c`, graph hash `3b58bf4742ed67a19507df014d16b03e31f695e9e863102f7fd3721ac93d2467`, with `validation.passed = false` because large-workspace helper projections and lifecycle analysis were deferred.
Its artifact is `hoi4-agent://workspace/mod_chaos_redux_ea3b2d67c2c0/artifact/78fd252db94e7a9fc3472730e3070e637d25863087c05751b259761767a7929a/afedb8854233295029bdb804f2e32fb4b1296afb2a756df26e43d2f457c73bda/event-trace-c9e9ac572a64.json`.
That artifact predates the opening-array repair and is not a post-change comparison.

A subsequent `hoi4.event_inspect` lint request for the same entry with helper expansion, depth two, forty nodes, sixty edges, and refresh enabled did not return a structured result within the bounded attempt and was terminated.
It produced no post-change artifact or validation verdict; source review is not a replacement for the missing helper-expanded evidence.

A narrower September 2 `hoi4.event_render` options request for `chaosx.nr21.3` returned three selected nodes and a complete set of five artifact files, but code `EVENT_RENDERED_PARTIAL` and `validation.passed = false` because workspace-wide helper and lifecycle passes remained deferred.
The revision is `23d07f38466bd55877a4f79f36a99c34bb9b7f0790f582a264bb307cc60e4646`; the graph hash is `38c248ff95d2c1efe04ff3c5a340f4af0f65f201989d8a235d0aec2d3c68519f`.
The manifest is `hoi4-agent://workspace/mod_chaos_redux_ea3b2d67c2c0/artifact/909f0e9c8fb99fb70f04ec8d4c2aa2aa30e4261e3fe37f69a441b9b872dcc2f9/044b9532f9aa9bcc64c1eaaa36af1adba12cbd37fe1d3aeb46331770861defc8/event-options-23d07f38466b-manifest.json`.
The initial full PNG resource response contained an ellipsis truncation inside its base64 payload, despite byte-range metadata saying the entire 47,036-byte artifact was returned, so two image-display attempts failed.
Six bounded byte-range reads (five 9,000-byte ranges and a final 2,036-byte range) recovered valid base64 without the injected truncation, and the parent opened the resulting 948×4,596 options diagram.
It shows `chaosx.nr21.3`, its `.a` option, and its terminal node; the very sparse diagram contains no gameplay asset or event-card presentation.
This is an event-chain options diagram, not a rendered in-game event card, and cannot certify report-card crop or presentation by itself.

After the successor-ordering and command-reset repair, an explicit `hoi4.event_compare` request used `before.revision = 23d07f38466bd55877a4f79f36a99c34bb9b7f0790f582a264bb307cc60e4646`, `refresh = true`, and `render = false`.
The installed MCP implementation confirms that `before.revision` is the supported cached-graph input.
The service returned `EVENT_REVISION_NOT_CACHED` with zero artifacts, so no semantic post-change comparison is claimed.

A subsequent helper-specific `hoi4.event_inspect` impact request targeted `event021_parent_prepare_annex_successor`, restricted the selector to the Event 021 on-action file, and used helper expansion with depth one, eight nodes, twelve edges, and cached refresh mode.
It timed out at the configured 180-second tool-call limit and returned no artifact or validation verdict.
The narrower helper-specific route therefore did not resolve the missing expanded lifecycle evidence.

## Open implementation and evidence gates

- Verify the final constants inventory after remaining callers stabilize, and resolve the nine provisional tuning groups in `subagent_handoffs/script_constant_completion_2026-09-02.md`.
  Target-ticket rounding of score divided by 100, followed by a minimum of one ticket, can erase the reduced major weight at low scores; the actual final pool requires scenario-specific probability review before acceptance.
- Certify the restored shared `adjust_individual_crisis_candidate_ticket_weight` dependency before accepting any Event 021 target draw.
  The bounded dependency handoff also identifies missing `adjust_individual_crisis_existing_provider_candidate_ticket_weight` and `individual_crisis_load_is_below_cap`; the first is Random Terror's exact one-provider exclusion, not an alias to the generic curve.
  The architect's plan is `subagent_handoffs/individual_crisis_dependency_restore_2026-09-02.md`; the bounded restore now exists in `common/scripted_triggers/individual_crisis_targeting_triggers.txt` and `common/scripted_effects/individual_crisis_targeting_effects.txt`, pending MCP/parser and probability evidence.
  The current workspace contains Event 021 automatic/scenario callers and several other event callers, plus `docs/systems/event_system/individual_crisis_targeting.md`, shared constants, and the restored helper source.
  The documented contract multiplies owner tickets by 4/2/1/0 for active individual loads 0/1/2/3+, derives load from fourteen existing package lifecycles, and exempts all-eligible world systems.
  An Event 021-only bypass is not an approved substitute for restoring that contract; cross-system consumers require baseline and post-change probability evidence.
- The separate fixed-target companion `apply_individual_crisis_fixed_target_event_pressure` remains undeclared and unconsumed for the fixed-target package owners named by the shared targeting documentation.
  It is tracked as an explicit shared-framework blocker and has not been replaced by candidate-ticket weighting.
- The lifecycle architect's final handoff implements all four helpers, same-crisis transfer proof, self-opponent clearing, and current-proof-only transfer state.
  Parent review found and the architect fixed a same-chain stale-local-target hazard in the transfer latch.
  Remaining successor work includes actual war continuity and adoption of the predecessor's unresolved-front roster/host role, not just registry pointer rebinding.
  The installed `add_to_war` documentation offers `targeted_alliance`, `enemy`, and `single_target_only`, but the current source does not snapshot the complete predecessor enemy roster or verify surviving wars after annexation.
  Do not infer war transfer from `annex_country`, `transfer_troops`, an external-war history flag, or front registration.
- Certify recurrence-generation lifecycle and finish the full per-crisis reset, including prior settlement type, signatory obligations, and successor state.
  Original-host commitment clears terminal receipts, generation rollback has a provisional snapshot, and cleanup releases the generation seed; their complete multi-route runtime behavior remains unproven.
  Preserve recurrence memory and completed achievement awards while separating per-crisis state, and do not consume a generation on failed preflight.
- Certify the continuous capital, authority-minimum, and external-capitulation hooks against loss/recapture and capitulation/recovery cases.
  The hooks exist, but their successor and multi-front lifecycle coverage remains unproven.
- Transfer relevant historical receipts to legitimate successors and propagate scenario/debug disqualification to all actors.
- Certify ordinary-origin and Event 021-origin human Event 006 nested-achievement cases against the new opening identity snapshot, including generation rollback and all homeland states.
- Verify The Terms Hold against every signatory and the full obligation period, not only the current country's recurrence flag.
- Trace cleanup of Event 021 adapter flags against Event 006 package-content gates so recognized countries retain their tree, decisions, AI, and assets.
  The local-content trigger, focus-tree modifier, shared category registry, admitted package category visibility, and local sections of the aggregate Event 006 decision definitions are source-patched; network/league, evolution, adapter-only, and other explicitly global gates remain intentionally strict and require final reachability review.
- Complete the reused-package-to-asset crosswalk and individually inspect every reachable reused asset, retaining source-placeholder, rights, formable, and consumer-render blockers.
- The additional original/processed review covers all forty owned rows.
  Alpha-composited source review found white fringe or stray white-pixel defects in previously fallback-treated achievement layers despite their clean-looking native-size outputs.
  The bounded icon artist supplied v2 candidate repairs for Fractals of Sovereignty grey and The Terms Hold not-eligible after the parent rejected the first edge review.
  The parent opened the v2 source, processed, final PNG, and decoded DDS dark/light 4x evidence, confirmed the intended silhouette/bevel/subject preservation, and promoted both repaired states to their existing `gfx/achievements/` paths plus corresponding final PNG and evidence DDS paths.
  Exact v2 hashes and previous runtime backups are recorded in `docs/assets/021_random_civil_war/validation/alpha_edge_repair_2026-09-02/parent_promotion_v2.json`; original source lineage remains untouched.
  The two earlier accepted not-eligible candidates remain frozen and promoted, while no companion state was changed by this v2 repair.
- Thirteen shared reused Event 006 focus icons and eight shared idea icons received individual original/processed/decoded-DDS and enlarged dark/light review, recorded in `docs/events/021_random_civil_war/reused_asset_individual_review.md`.
  Their pixels and direct sprite paths pass that limited review; inherited magenta-chroma processing is disclosed and no source regeneration occurred.
  Required focus inspection and decoded-icon raster requests each timed out after 180 seconds, so no rendered-tree acceptance is inferred.
- The skill maintainer added generic bounded binary-MCP retrieval guidance to the subagent skill, reviewed by the parent; no event-specific instructions or runtime configuration were added.
- Finish the mapped probability comparisons, acceptance and performance scenarios, user-owned live evidence, workbook alignment, resolved improvement-loop output, and fresh completion audit against the final source state.
  The Event021-owned visual audit is complete for its 40 references, while the reused Event006 32-package visual/provenance audit remains incomplete and the six reviewed RHI/BAY portraits remain provisional or blocked.
- The SCN-018 Maximum path now separates the confirmation-time preflight-selected array from the frozen commit-eligible array, adds an opening-state preflight to the normal-human candidate predicate, and records preflight, plan-eligible, and preflight-skipped counts. MCP lint is clean at the focused structural boundary; live all-eligible commitment, performance, and final probability comparison remain open.

## Integrity and reporting

No replacement artwork, custom GUI, animation, super-event, or 3D family is introduced by this repair tranche.
The forty Event 021-owned DDS reviews are not a count of all reused package assets.
No temporary asset evidence is deleted while the event remains incomplete.
The dirty worktree contains unrelated edits; no whole-worktree staging or completion commit is authorized by this ledger.

## Current bounded work ownership

The parent owns integration and all existing gameplay files unless a narrower explicit grant says otherwise.
No completion claim or release-gate opening follows from any individual handoff.

- `01a060f9-679a-78e0-aeb2-a0140742aa67`: read-only target-pool probability baseline; asked to freeze current partial evidence before further sweeps, expected `subagent_handoffs/target_pool_baseline_2026-09-02.md`.
- `01a060eb-c157-70b2-84d9-686c632d5461`: dependency-restore architecture handoff only; temporary-variable scope wording and installed trigger-side calculation references were corrected, shared source remains parent-owned.
- `01a06135-83c6-7d72-850a-63f0f9981607`: new treaty helper effect/trigger files and signatory handoff only; parent owns their integration and freeze-before-cleanup ordering.
- `01a060ed-e1a4-7300-a4a2-35eadc805c22`: new successor roster-adoption helper effect/trigger files and handoff only; no war mutation is authorized in that tranche, actual war continuity remains open.
- `01a06114-8e51-75a1-acde-25efe30ffcb7`: remaining Fractals grey and Terms Hold not-eligible alpha repairs in separate v2 evidence paths; the already-promoted frozen pair must stay immutable and the worker owns no runtime paths.
- `01a0616f-8b69-7082-8e66-b4527a4ac3ab`: six reused RHI/BAY portrait rows, source/crop/identity/consumer audit and candidate evidence only; no live DDS/GFX replacement before parent review and no RunPod operation.

The reused-package crosswalk worker and MCP-retrieval skill maintainer completed their bounded handoffs and were closed after parent review.

The final completion-auditor workers were spawned with `fork_context=false`, but both remained in bounded scans and were shut down without a durable handoff. The parent-owned disposition is recorded in `subagent_handoffs/event_completion_parent_audit_2026-09-02.md`; it keeps the release gate closed and does not treat the worker shutdown as completion evidence.
