# Event 021 Documentation Cleanup Handoff

Date: 2026-08-31

Scope: documentation-only reconciliation under `docs/events/021_random_civil_war/` and `docs/plans/021_random_civil_war_plans/`, including subagent handoffs.

The source-of-truth ledger is `docs/plans/021_random_civil_war_plans/source_of_truth_map.md`.

## Files changed

- `docs/events/021_random_civil_war/overview.md`
- `docs/events/021_random_civil_war/acceptance_evidence.md`
- `docs/plans/021_random_civil_war_plans/improvement_loop_addendum.md`
- `docs/plans/021_random_civil_war_plans/post_fix_improvement_loop_closure_addendum_2026-08-31.md`
- `docs/plans/021_random_civil_war_plans/subagent_handoffs/icon_artist_handoff.md`
- `docs/plans/021_random_civil_war_plans/subagent_handoffs/generated_event_art_handoff.md`
- `docs/plans/021_random_civil_war_plans/subagent_handoffs/ai_probability_audit.md`
- `docs/plans/021_random_civil_war_plans/subagent_handoffs/localisation_auditor.md`
- `docs/plans/021_random_civil_war_plans/subagent_handoffs/scripted_system_architect_handoff.md`
- `docs/plans/021_random_civil_war_plans/source_of_truth_map.md`
- `docs/plans/021_random_civil_war_plans/documentation_cleanup_handoff.md`
- `docs/plans/021_random_civil_war_plans/resume_packet.md`

## Disposition summary

The accepted specification package remains the design source of truth and was not edited.

The current overview and acceptance ledger now use the supplied current Event 021 lint revision, graph hash, overview layout hash, targeted decision and mission inspections, strange-incident evaluation, and map artifact.

The current acceptance ledger distinguishes zero Event 021 lint issues from the deferred workspace-wide helper and lifecycle projection that makes the MCP result partial.

The current closure addendum records the targeted evidence refresh and no longer asks the parent to repeat completed decision, mission, map, focus, or Event 021 overview checks.

The icon handoff retains historical provenance but has one current status: all 40 current GFX references resolve, including all 18 achievement states and the relief and reconstruction action sprites, while user live consumer validation remains pending.

The generated-art, probability, localisation, and scripted-system handoffs now identify old status statements as historical where current evidence supersedes them.

The improvement-loop ledger retains the concurrent implementation-resolution edits already present in the worktree, while its stale focus-viewer blocker and old MCP section are now explicitly superseded or historical.

The asset manifest and asset GFX handoff were left unchanged because they are outside the granted write scope, so their old achievement-path language remains an explicit open documentation contradiction for the asset owner.

## Contradictions resolved

The missing `fractals_of_sovereignty` and `the_terms_hold` achievement-triplet claims were moved into a superseded historical section in the icon handoff and replaced with current runtime/GFX status.

The missing relief/reconstruction icon claim was moved into the same superseded historical section and replaced with current runtime/GFX status.

The obsolete root-versus-interface achievement path concern is now resolved for current consumer documentation: current GFX uses root `gfx/achievements/021_random_civil_war_*.dds`.

The old Event MCP revision, stale-analysis, transport-closed, and no-artifact claims are retained only as historical context or replaced by current evidence.

The old focus scan-byte-limit claim is replaced with current focus inspection and render evidence.

The improvement-loop addendum's current P2 snapshot now points to the current focus evidence and the remaining full-matrix, runtime-fixture, and shared-owner GUI gates; its older MCP section is labeled as a historical pre-patch snapshot.

The old no-map-artifact claim is replaced with current map inspection evidence and an explicit separation of unrelated map diagnostics.

## Duplicate and superseded document audit

`improvement_loop_addendum.md` repeats tranche-level `Acceptance checks`, `Current contradiction`, and `Required design` headings by design; the file's top resolution ledger and historical snapshot notice prevent them from acting as duplicate active specifications.

`event_completion_auditor.md` is explicitly superseded, while the icon, generated-art, probability, localisation, and scripted-system handoffs are bounded historical reports with current-status notices.

## Open contradictions and blockers

The asset-owner `manifest.md` and `gfx_handoff.md` still describe the older interface-achievement path and remain outside this cleanup write scope.

Event compare against an older graph revision remains unavailable because the prior graph revision is not cached, and no completed Event compare is claimed.

The full named probability matrix and same-scenario comparisons remain pending even though current decision, mission, and strange-incident targeted inspections report zero unresolved inputs.

Runtime acceptance fixtures for scheduler, cluster, state survival, settlement, recurrence, maximum-generation, and annex-cleanup sequences remain pending.

The shared Event Details collision and click-region finding remains queued to the shared GUI owner, and the current bounded GUI route rejected inspection without both `windowName` and `scenario`.

User live-session visual and consumer validation remains unclaimed.

## Plan and handoff dispositions

`improvement_loop_addendum.md` is implementation-resolved historical planning material and is not a second active design source.

`post_fix_improvement_loop_closure_addendum_2026-08-31.md` is the current closure plan with evidence gates still open.

`icon_artist_handoff.md` and `generated_event_art_handoff.md` are historical production handoffs with current runtime/GFX coverage recorded.

`ai_probability_audit.md`, `localisation_auditor.md`, and `scripted_system_architect_handoff.md` retain bounded historical evidence and now point current status to the acceptance ledger.

`event_completion_auditor.md` remains superseded by the current closure evidence.

`spreadsheet_doc_worker_handoff.md` remains the current catalog handoff with deliberately non-final workbook statuses.

## Markdown hard-wrap issue list

No accidental mid-sentence or mid-clause hard-wrap issue was found in the scoped Markdown files.

Intentional headings, lists, tables, code blocks, block quotes, and paragraph boundaries were preserved.

## Validation performed

Read-only Event MCP lint and overview render were refreshed for `chaosx.nr21.1`.

Read-only probability evidence was reconciled from the supplied current decision, mission, and strange-incident artifacts without spawning or waiting for a duplicate probability auditor.

Read-only map inspection was refreshed at revision `fa76cadf611a1bf937556584a71b1e1e0bbde4b0a9b8364ca86614218e0918be`.

Read-only focus inspection and render were refreshed at revision `55a900cb833aadee310a6c5b0f480af8cf0ea73522de74cef01f334788f351ae`.

The current interface file and runtime asset names were checked read-only, confirming 40 unique texture references and no missing current GFX target.

Scoped grep checks were used for old revisions, stale path language, missing icon claims, no-map-artifact claims, duplicate headings, and obsolete compare/status wording.

The scoped Markdown hard-wrap audit found no accidental sentence or clause splits.

## Skipped validation

No live Hearts of Iron IV run, save/reload sequence, frame-time measurement, or user-owned visual consumer validation was performed because those actions belong to the user and are outside this documentation-only task.

No Event compare was attempted as a completion claim because the prior graph revision is not cached.

The full named probability matrix and runtime fixtures were not rerun because the user explicitly identified them as pending and supplied targeted current evidence instead.

The asset manifest and asset GFX handoff were not patched because they are outside the granted write scope.

## Proposed cleanup actions outside write scope

If the write boundary is not expanded, the asset owner should update `docs/assets/021_random_civil_war/manifest.md` and `gfx_handoff.md` in a separate asset-scoped pass to use the current root achievement paths and record the historical interface-path note.

If the shared GUI owner does not accept the Event Details finding, the owner should record an explicit rejected or stale disposition rather than routing the issue back to Event 021.

The spreadsheet owner should reconcile the non-final Event 021 and SCN-018 workbook statuses after the named probability and runtime fixture gates close.

## Parent handoff

Use `resume_packet.md` for the short continuation checklist and `source_of_truth_map.md` for the full ledger.

Do not claim Event 021 completion from this cleanup; the documentation now clearly separates implemented source work, current focused evidence, historical handoffs, and open certification gates.
