# The Failed Calculation report art

The source image was generated for the Fallout Technate chain `chaosx.fallout.831`. The scene shows an East Asian reactor cooling relay being inspected by engineers and a hospital technician while a calculation ledger is checked against a regional repair map. It contains no readable text, logos, flags, real people, zombies, gore, or watermark.

The generated source is `failed_calculation_source.png`. The processed runtime image is `failed_calculation_processed.png`. The runtime sprite is `GFX_report_event_fallout_failed_calculation`.
