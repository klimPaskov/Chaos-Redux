# The Captain Refuses GFX handoff

The sprite name is `GFX_report_event_fallout_captain_refuses`.

The DDS belongs at `gfx/event_pictures/fallout_world_end/report_event_fallout_captain_refuses.dds`.

Register the sprite in `interface/fallout_world_end.gfx` and use it from `events/fallout_world_end_events.txt` for events `chaosx.fallout.520`, `522`, and `524`.
