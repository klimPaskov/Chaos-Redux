# The Missing Patrol GFX handoff

The sprite name is `GFX_report_event_fallout_missing_patrol`.

The DDS belongs at `gfx/event_pictures/fallout_world_end/report_event_fallout_missing_patrol.dds`.

The sprite is registered in `interface/fallout_world_end.gfx` and used by events `chaosx.fallout.527`, `529`, and `531`.
