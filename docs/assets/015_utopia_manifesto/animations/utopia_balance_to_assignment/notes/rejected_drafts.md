# Rejected ImageGen Drafts

One generated draft was rejected before the accepted source-frame chain began:

- Handle: `exec-bc6f22dd-8477-4350-a992-d5ce9e2d41f9.png`.
- Rejection reason: it showed six tokens instead of five and drifted toward a top-down presentation, weakening continuity and the requested sorting narrative.
- Correction: accepted frame 000 was a built-in ImageGen precise-object edit that restored the strict front-on shallow rail, removed the extra token, and retained exactly five irregular tokens with the comb retracted.

The rejected image remains only in the ImageGen session output. It is not copied into this package and was not used in processing, sheet assembly, preview creation, or runtime DDS conversion.
