# Base zombie 3D package

## Current source of truth: clean final v3, 2026-09-20

Status: **offline runtime candidate promoted** for the base zombie mesh, materials, and eight skeletal actions; death-pose culling has a measured residual, while audio listening and live HOI4 validation remain user-owned and unverified. The earlier dependency stops, v2/v4 exports, `77_final_zombie_actions_v1.blend`, and the action-deformation blocker in the linked pipeline handoff are historical stages and do not identify the current runtime bytes. This section supersedes their model, material, action, cost, and acceptance status claims; it does not erase their repair evidence.

The final pre-export checkpoint is `blender/checkpoints/93_clean_uv_final_v3.blend`, derived through `87_selective_backface_repair_v3` → `89_nonoverlap_uv_v1` → `91_death_backface_repair_v2` → `93_clean_uv_final_v3`. Checkpoint 91 adds 37 death-pose face flips while leaving already-correct faces `10418` and `10911` unflipped. The final source has one UV layer, 30,436 triangles, and 15,021 vertices; `validation/clean_final_v3_summary.json` records zero boundary edges, non-manifold edges, and degenerate faces.

The locked export is `export/mesh/chaosx_zombies_clean_final_v3.mesh` with eight role files at `export/anim/clean_final_v3/{attack,defend,idle,move,retreat,support_attack,training,death}.anim`. Those exports are promoted to the canonical `gfx/models/units/chaosx_zombies/chaosx_zombies.mesh` and `chaosx_zombies_{attack,defend,idle,move,retreat,support_attack,training,death}.anim` runtime names. `validation/clean_final_v3_summary.json` records byte equality, SHA-256 values, and a proof checkpoint for each role. The runtime mesh SHA-256 is `342FF7A8F28AD07D04C70A4686A064602DB03E9C65E6893E1032F6E901F2D698`.

The `export/runtime_materials_v6/` package contains `base_color.png`, `normal.png`, `roughness.png`, and corresponding runtime DDS maps. Its smart-project/packed UV arrangement reduced measured cross-category raster edge-sample overlap from the earlier 91.58% to 3,263 of 334,514 covered texels (0.975%); no four-way overlap was measured. The promoted runtime diffuse, normal, and specular SHA-256 values are `EFFE1B8D9D2013E6DBF680AC344F0943EDACF45B3AAA3D457856D77A763D7DA3`, `143BCFEB0C4827CE25CAAEA9E2558B12C15C7446656A5EACD8CEF1C01ABD44D0`, and `1BA6E652A9ED2F3E375BE6FA09585C65D65B6183D89B79ACFC6D8B62FE0A5267D0`. The overlap report is `export/runtime_materials_v6/uv_overlap_report.json`; final material renders are `blender/previews/runtime_materials_v6_*`, and source culling comparisons are `blender/previews/orientation_audit_v5_*`.

Actual-byte proofs are `validation/reimport_clean_final_v3_{attack,defend,idle,move,retreat,support_attack,training,death}.json` and `blender/checkpoints/reimport_clean_final_v3_*.blend`. The culling-on/off comparisons count pixels with difference greater than 40 as attack `0`, move `7`, retreat `0`, and death frame 37 `612`. The death residual is concentrated on two folded inner faces already correct in rest and locomotion; it remains a documented offline visual limit rather than a zero-defect claim. The locked adapter export report is `logs/adapter/add82a35f9af4f2586d92f07a7fce1ee.result.json`; `validation/clean_final_v3_summary.json` is the concise final validation index.

Meshy lineage spend is **40 credits**: 30 body, 5 remesh, and 5 for the one rejected rig attempt. No paid Meshy animation attempt was made; the Blender recovery supplies the canonical rig and actions. Audio listening, contact timing, and live HOI4 behavior have no acceptance evidence in this documentation pass. The older audio provenance and wrapper limits below remain open until their owners reconcile them.

## Historical lineage and superseded status claims

The sections below preserve dated production and repair evidence. Read any statement that calls an older checkpoint, export, texture, action, or runtime hash “final,” “accepted,” “blocked,” or “current” as a statement about that historical stage only; the clean final v3 section above and `validation/clean_final_v3_summary.json` identify the present offline runtime candidate.

The user selected the exact image in `refs/source/user_selected_original.png` for the base zombie. `refs/original/meshy_input.png` is byte-identical (SHA-256 `7e702c098884d221b000bb6e8764677489e7a4c22d26f6637d8a2e002ab60a43`). Prior arm-proportion iterations are rejected. The screenshot of the defective old model is defect evidence only.

The provider route passed repository environment verification, Meshy 7 schema inspection, two concurrent wrapper probes with no surviving owned processes, and a live balance of 382 credits on 2026-09-19. The planned maximum for the first complete provider attempt is 59 credits: 30 for body, 5 for rig, 3 each for eight actions; actions that prove unusable transition to Blender instead of paid retries. Final model, animation, texture, audio, counter, runtime, and in-game evidence are pending.

## 2026-09-19 approved-reference regeneration gate

Status: **blocked before new provider lineage**

The process `MESHY_API_KEY` was present and nonblank. The adapter lock and source hashes, Blender 5.1.2 build line, `io_pdx_mesh` 0.91.0 archive SHA-256, Meshy MCP package 0.4.0, and SDK 1.29.0 were checked. The installed Blender MCP add-on manifest at `C:/Users/klimp/AppData/Roaming/Blender Foundation/Blender/5.1/extensions/lab_blender_org/mcp/blender_manifest.toml` reports **0.2.0**, while `.tools/3d_pipeline/config/dependencies.lock.json` requires **1.0.0**. The locked bridge at `127.0.0.1:9876` was not listening. This mismatch triggers the dependency stop gate. No live Meshy balance call, Meshy generation, rig, action, Blender mutation, export, or reimport was performed for the new input, and new-lineage credit consumption is **0**. The earlier 382-credit figure and older task files are historical only.

Worker handoff: `docs/plans/002_zombie_outbreak_zombies_plans/subagent_handoffs/zombies_3d_pipeline_20260919.md`. Geometry, eight actions, sound role selection, bespoke counter handoff, export/reimport, and runtime synchronization remain blocked or unverified for this new lineage.

## 2026-09-19 current new-lineage result

This section supersedes the earlier dependency-gate status for this approved input. The lock-matched Blender MCP 1.0.0 bridge and adapter 1.10.49 became healthy; the exact reconstruction prompt was recovered at `refs/derived/base_zombie_meshy_reference_blender_reconstructed_dark_olive_prompt_20260919.txt`. Official Meshy 7 body task `01a0ba94-9607-729b-92e5-53951c59e8e1` succeeded and consumed 30 credits. Recovery remesh task `01a0ba9f-8b53-77e5-a132-ea513bcd0672` succeeded and consumed 5 credits. Downloads, exact hashes, request IDs and Blender reports are preserved in `provider/new_lineage_20260919/` and the current worker handoff. Total new-lineage spend is **35 credits**.

The original candidate remains **open** after the locked adapter preparation and weld distances 5e-5 and 1e-4: 486 boundary edges, 93 components (83 closed/10 branched), zero non-manifold edges. The remesh candidate was worse at 509 boundary edges and 93 components. Automatic hole fill rolled back rather than introducing non-manifold geometry. The configured `repair_explicit_mesh_patch` route was not exposed as a callable MCP tool. Package status is **blocked before rig and model export**; no Meshy rig/action attempts, manual Blender rig/actions, final `.mesh`/`.anim`, or actual-byte reimport exist. Stage-named rig/action checkpoints are placeholders, not approval.

The sourced-audio evidence is in `evidence/audio/source_design_20260919.md`, including a newly downloaded CC0 meat-impact archive and immutable hashes. Sonic fit is **needs_user_review** and action-frame synchronization is **blocked**. Exact vanilla large/small land counter definitions and palette were inspected for the artist handoff; no bespoke counter DDS is accepted yet. The current handoff at `docs/plans/002_zombie_outbreak_zombies_plans/subagent_handoffs/zombies_3d_pipeline_20260919.md` supersedes its own earlier dependency-stop section and lists every remaining requirement status. No in-game validation is claimed.

## 2026-09-19 corrected runtime package

This section records the parent-applied continuation after the worker's earlier topology stop. The accepted source checkpoint is `provider/new_lineage_20260919/clean_repair_reprepare/blender/checkpoints/13_region_caps_v1.blend`, SHA-256 `6708F73BA48653FDBD6F6E68C7C9287AF2051556A0C4D1486A640EA3655E6FC3`. It contains one closed `Mesh_0.001` body at 30,438 triangles and 15,021 vertices, with zero loose boundary edges, non-manifold edges, and degenerate faces in the adapter report. The calibrated height is `7.351824760437012`; the runtime entity applies the vanilla-matched scale `0.8` once.

The user-reported back deformation was traced to the first weight-boundary layout. The accepted rig is checkpoint `provider/new_lineage_20260919/clean_repair_reprepare/blender/checkpoints/23_rig_v2_weight_boundary.blend`, SHA-256 `3DDF62DE7F50308D824BF6739096BDAAA56CC8783C2D10EDAAD030A447E1DAA8`. Its 18-bone `BaseZombieRig` assigns torso/pelvis regions before narrowed arm bounds, eliminating the jacket triangle that appeared when the old arm-first weights were animated. Corrected source previews include `blender/previews/base_zombie_move_v2_rear.png`, `base_zombie_attack_v2_rear.png`, `base_zombie_defend_v2_rear.png`, and matching front/three-quarter captures.

Seven role-specific actions were authored from the corrected rig and exported through the locked adapter: `idle`, `move`, `attack`, `defend`, `support_attack`, `retreat`, and `training`. The death action went through measured v2, v3, and v4 candidates; those were rejected after reimport showed leg-driven pulls. The accepted death action is `rig/actions/base_zombie_death_v5.json`, checkpoint `blender/checkpoints/39_death_v5.blend`, export `export/anim/chaosx_zombies_death.anim`, and reimport evidence `validation/reimport_chaosx_zombies_death_v5.json` plus the five frame triptychs under `blender/previews/reimport_chaosx_zombies_death_v5_frame_*`. Death v5 keeps the fall in root/spine/chest/neck/head/arm motion and does not rotate the legs, so its reimported silhouette has no triangular leg spikes.

The partitioned mesh export is `export/mesh/chaosx_zombies.mesh` and uses object `Mesh_0.002` with four streams: 10,161 / 11,340 / 10,730 / 8,633 vertices and 8,000 / 8,000 / 8,000 / 6,438 triangles. The largest stream stays below the 16-bit index limit. The source mesh report is `blender/reports/export_mesh.json`; partition evidence is `blender/reports/31_export_partitioned_v2_partition.json`. Reimport reports for all seven v2 actions and death v5 are under `validation/reimport_chaosx_zombies_*.json`. The imported reports retain normal/UV seam vertices; their position-weld diagnostic reports zero non-manifold or degenerate faces and three residual boundary edges after welding, so the source checkpoint's closed topology and the exporter seam behavior are both recorded rather than conflated.

An independent read-only weld audit of the exported text streams found zero loose or non-manifold edges from `1e-7` through `1e-4` position tolerance. The three-edge reimport diagnostic is therefore recorded as importer-side numeric seam residue; no hole is visible in the textured source or reimport frames.

The promoted runtime bytes are synchronized with the export sources by SHA-256. The mesh is `gfx/models/units/chaosx_zombies/chaosx_zombies.mesh` (`fa965bb755fb11efcc287e13ef8b1a285706da2d17e89ce7c4859fd4d7ae5267`). DDS maps are `chaosx_zombies_diffuse.dds` (`8377ce52bd12d039d0b624ad17f671730a5f6808d7fcf9b78ebb06da66784920`), `chaosx_zombies_normal.dds` (`b9c10ae2e739fe3392ce9aa154877734a6ed272b514b6bc8b8dbd153ca66595b`), and `chaosx_zombies_specular.dds` (`68934da0f132ebe3d80b4eb240906558c8f93e2c52bf843d2b51162d845ed758`). The eight runtime action files are `gfx/models/units/chaosx_zombies/chaosx_zombies_{idle,move,attack,defend,support_attack,retreat,training,death}.anim`; each matches its canonical export byte-for-byte. The parent wiring registers all eight animation types, all four mesh streams, distinct entity states, grounded move/retreat step events, and the vanilla reference scale in `gfx/entities/chaosx_zombies.gfx`, `gfx/models/units/chaosx_zombies/animation_chaosx_zombies.asset`, and `gfx/entities/chaosx_zombies.asset`.

Counter handoff is promoted at `gfx/interface/counters/divisions_large/zombies_icon.dds` and `gfx/interface/counters/divisions_small/onmap_unit_zombies_icon.dds`, with existing `interface/chaosx_subuniticons.gfx` registrations. Runtime zombie sounds and WAV format checks are present under `sound/002_zombie_outbreak/zombies/` and `sound/chaosx_zombies_sound.asset`. Their source-role choice still requires listening review, and exact action-frame synchronization plus a subunit-specific acknowledgement selector remain blocked by `evidence/audio/source_design_20260919.md`. The package therefore has accepted model/rig/action/export/counter evidence, but is not claimed as fully live-game accepted until audio review and user-owned in-game validation are complete.

## 2026-09-19 audio runtime continuation

This section supersedes the audio-wiring status in earlier manifest sections. The parent installed nine additional sourced mechanical derivatives and expanded `sound/chaosx_zombies_sound.asset` and `gfx/entities/chaosx_zombies.asset` to declare and invoke attack vocal/impact, defend/support contact, death vocal, and sourced death-impact sounds at numeric action times. The nine source-to-runtime paths, checksums, and conversions are in `evidence/audio/runtime_derivations_20260919.json`; all nine installed hashes matched that record and all 24 installed WAVs probed as signed-16 PCM, 44.1 kHz, mono. The three older idle/selection WAVs and six older movement WAVs now have exact source-to-runtime crosswalks, and `evidence/audio/older_attack_death_partial_crosswalk_20260920.json` closes `zombie_attack_01.wav`; the five remaining older attack/death WAVs still need one. The new hurt wrapper has no verified entity consumer. Sonic role fit is **needs_user_review**; visual/audio contact synchronization, the remaining older-file provenance, a subunit-specific acknowledgement selector, and user-owned live-game validation remain **blocked or unverified** as described in `evidence/audio/source_design_20260919.md`.

## 2026-09-20 v4 geometry and actual-byte reimport acceptance

This section supersedes the earlier corrected-runtime paragraphs wherever they describe the accepted mesh, jacket skin, duplicate-face diagnostic, or death export. The older v2 and death-v5 records remain historical evidence only.

The lower-jacket defect was repaired at the reviewed skin-weight boundary by removing upper-leg influence from 109 exact vertices. The base repair specification is `provider/new_lineage_20260919/clean_repair_reprepare/rig/base_zombie_lower_jacket_skin_repair_v3.json` (SHA-256 `9FA3426A3A19FC8EB64A00A08004FEDF4C87057FE1513231064D11FF6C1C4E7F`), and the reopened base repair checkpoint is `provider/new_lineage_20260919/clean_repair_reprepare/blender/checkpoints/40_lower_jacket_skin_v3.blend` (SHA-256 `DD0245DB08140DDA75CE492088B348759FB35F78DA6ED0CA1800B9490CAF832C`). The death source received the same repair at `blender/checkpoints/42_death_v5_lower_jacket_skin_v3.blend` (SHA-256 `E9232ACE5FB394EF32417D8E52288C437E60EAD24D3D4E418CEB4F8DCFA5FD2A`).

Actual-byte v3 reimport identified one isolated duplicate triangle: faces `[9138, 9214]` on vertices `[10322, 10325, 10333]`, with different UV corners and no body-surface edge adjacency. The parent added the SHA-bound adapter operation `remove_explicit_duplicate_faces` and removed only that reviewed group from the base and death sources. The base output is `blender/checkpoints/43_duplicate_triangle_removed_v3.blend` (SHA-256 `E9DB2DD5EF2E1337F1CFFFF7D163E030DB3AF83B31DCE88C606560798F24E1B4`), the death output is `blender/checkpoints/45_death_skin_duplicate_removed_v4.blend` (SHA-256 `3A4D5AB6539F6CB049038D2C212523B96B55C45279D48C1E2B5994C8175B6817`), and both reopened with zero boundary, non-manifold, and degenerate geometry.

The final partition checkpoint is `provider/new_lineage_20260919/clean_repair_reprepare/blender/checkpoints/44_export_partitioned_v4.blend` (SHA-256 `85283622C9C048B99B1BC8B05A8589F06056E8E7E8A4F0C1BE8FD8A53CEEDC95`). It contains 30,436 triangles and 15,021 vertices across four export streams with 8,000, 7,998, 8,000, and 6,438 triangles. The v4 mesh export is `export/mesh/chaosx_zombies_v4.mesh`; all eight role actions and death were exported as v4 `.anim` files and imported back with the actual v4 mesh bytes and textures.

The eight v4 reimport proof reports are `validation/reimport_chaosx_zombies_{idle,move,attack,defend,support_attack,retreat,training,death}_clean_v4.json`. Every report records 30,436 triangles, zero position-welded loose boundary edges, zero non-manifold edges, and zero degenerate faces. Textured Blender playback proofs and zoomed jacket/death frames under `blender/previews/` show no visible lower-jacket deformation, holes, or white outline. The v4 mesh and all eight v4 actions were promoted to the canonical runtime paths and rechecked byte-for-byte; the canonical runtime names and entity scale remain the ones registered in `gfx/entities/chaosx_zombies.gfx`, `gfx/models/units/chaosx_zombies/animation_chaosx_zombies.asset`, and `gfx/entities/chaosx_zombies.asset`.

At the v4 checkpoint, offline model, action, export, reimport, texture, scale, and counter evidence was clean for this lineage. That checkpoint recorded 35 credits and no paid rig or action attempts because the manual Blender route was used after the provider geometry required repair. The final continuation below supersedes that cost/status record with the one required Meshy rig attempt and the current 40-credit total.

## 2026-09-20 final offline repair and eight-action promotion

This section supersedes the preceding 2026-09-20 visibility-audit and death-v6 blocker for the current zombie runtime package. The accepted offline source is `provider/new_lineage_20260919/clean_repair_reprepare/blender/checkpoints/77_final_zombie_actions_v1.blend` (SHA-256 `531EE4EA8EAC98DFD0CA6A07B8DBCC2300C087E4E4CA8B59250F85DE2C91CF55`). It carries the formal smooth-greedy winding repair, the reviewed root-to-pelvis skin repair, the reviewed role-specific action set, and the repaired controlled collapse.

The winding repair used the SHA-bound selection in `rig/mesh_winding_repair_smooth_greedy_v3.json` (selection SHA-256 `274F16D0E0DECA9A3EB79753EA6D622086401F0B6D0FB9571FA1D010AF0C3C70`) and report `blender/reports/63_winding_smooth_greedy_v3_repair_explicit_mesh_winding_batch.json`. The reopened geometry has 30,436 triangles, 15,021 vertices, zero boundary edges, zero non-manifold edges, and zero degenerate faces. The source materials retain backface culling disabled, while the exported actual-byte reimports render complete silhouettes with culling enabled; the prior jacket, head, and trouser disappearance is no longer present in the culling-on review frames.

The root-weight defect that caused the death spike was repaired with `rig/mesh_root_to_pelvis_v1.json` (SHA-256 `06D778619F64B44E63D9D0ABF97770BC7745C2DFF5C47672F69E613D8AC4C181`) and report `blender/reports/72_rootpelvis_skin_v1_repair_explicit_skin_batch.json`. Exactly 347 nondeforming root assignments were transferred to pelvis; the final audit records zero zero-weight vertices, zero vertices over four influences, and weight sums within `0.9999999553` to `1.0000000447`.

The final action audit is `blender/reports/77_final_zombie_actions_v1_audit.json`. It records `base_zombie_attack` 0–36, `base_zombie_defend` 0–36, `base_zombie_idle` 0–48, `base_zombie_move` 0–36, `base_zombie_retreat` 0–36, `base_zombie_support_attack` 0–36, `base_zombie_training` 0–40, and `base_zombie_death_clean_v9` 0–48 at 24 fps. The death action was rendered at frames 0, 8, 16, 24, 32, 36, 40, 44, and 48 and visually reviewed from front, rear, and three-quarter views; it transitions to a floor-settled horizontal corpse without the former black spike, holes, or warped clothing.

The final PDX mesh is `export/mesh/chaosx_zombies_final_v1.mesh` (SHA-256 `C5BFF200D76C3C1A2CE9998708251C931BF321A3D076E1F49159D828774C7616`). All eight final animations were exported from the same checkpoint and reimported with this actual mesh and staged textures. `validation/final_reimport_summary_v1.json` records five-frame front, left, and three-quarter preview triptychs for every role, zero position-welded loose boundary edges, zero position-welded non-manifold edges, and zero position-welded degenerate faces. `validation/runtime_sync_final_v1.json` records byte equality between every canonical export and its promoted runtime file.

The runtime files are `gfx/models/units/chaosx_zombies/chaosx_zombies.mesh` and `chaosx_zombies_{idle,move,attack,defend,support_attack,retreat,training,death}.anim`; entity and animation registrations remain in `gfx/entities/chaosx_zombies.gfx`, `gfx/entities/chaosx_zombies.asset`, and `gfx/models/units/chaosx_zombies/animation_chaosx_zombies.asset` with the single vanilla-matched scale `0.8`. The exported texture bytes match the existing canonical diffuse, normal, and specular DDS files exactly.

Meshy spend for this lineage is 40 credits: 30 credits for the body, 5 credits for the recovery remesh, and 5 credits for the one required rig attempt. The returned rig failed the topology and required-action gates, so the reviewed rig and all skeletal actions use the authorized Blender recovery route after the body geometry required repair. The remaining acceptance limits are audio listening and exact contact-timing review, the five legacy specialized-wrapper mappings, the unconsumed hurt wrapper, user review of the seven specialized counter packages, and the user's live HOI4 validation. The bespoke base-zombie counter is installed. No live HOI4 run is claimed here.

## 2026-09-20 single Meshy rig attempt

The required single Meshy rig attempt ran on the completed Meshy 7 body task as `01a0be3d-955a-7743-960b-45c494c2cf2b` and consumed 5 credits. The primary and bundled walking artifacts, hashes, response receipts, Blender inspection reports, and review renders are recorded in `provider/new_lineage_20260919/clean_repair_reprepare/provider_rig_attempt/meshy_rig_once_handoff.json`.

The returned rig has 24 bones and normalized weights, but its imported character retains 157 position-welded boundary edges and 190 position-welded non-manifold edges at `1e-5`, and it provides no required custom action set. Its skeleton is incompatible with the accepted 18-bone Blender runtime actions, so the Meshy rig is retained as a rejected provider candidate and is not promoted. The repaired Blender checkpoint remains canonical. Total Meshy spend for this lineage is **40 credits**: 30 body, 5 remesh, 5 rig; no paid Meshy animation attempt or retry was made.

## 2026-09-20 movement audio provenance closure

`evidence/audio/older_move_runtime_crosswalk_20260920.json` closes the six-file movement provenance gap. It records all six preserved Footsteps originals, their CC0 source page and direct download URLs, original SHA-256 values, the exact FFmpeg conversion, runtime SHA-256 values, and byte-for-byte reproduction checks. All six runtime files meet the installed signed-16 PCM, 44.1 kHz, mono contract.

The base entity now uses the later sourced `chaosx_zombie_death_impact` wrapper at the death impact event; the five older attack/death files remain installed for specialized wrappers and are still open for per-file provenance. Audio remains open for sonic listening and exact contact timing, the unused hurt wrapper, the tag-scoped acknowledgement limitation, and user-owned live HOI4 playback. No model or animation bytes changed in this audio evidence pass.
