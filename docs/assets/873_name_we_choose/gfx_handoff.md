# The Name We Choose GFX handoff

Register `GFX_report_event_fallout_name_we_choose` in `interface/fallout_world_end.gfx`.

The sprite points to `gfx/event_pictures/name_we_choose/report_event_fallout_name_we_choose.dds`.

The event range `chaosx.fallout.873` through `chaosx.fallout.879` uses this sprite for the human-facing opening, delayed result, and generation review. Hidden AI and cleanup blocks retain the same picture reference for consistent asset audits.
