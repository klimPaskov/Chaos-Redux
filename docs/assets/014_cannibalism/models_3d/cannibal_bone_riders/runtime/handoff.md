# `cannibal_bone_riders` runtime handoff

Status: professional horse and rider motion sources selected; blocked pending an authoritative adapter reload and final export/reimport. Do not wire the model as an animated unit yet.

Proposed frame entity identifier: `cannibal_bone_riders_entity`. Proposed child identifiers are `cannibal_bone_riders_horse_entity` and `cannibal_bone_riders_rider_entity`. Proposed mesh identifiers are `cannibal_bone_riders_horse_mesh`, `cannibal_bone_riders_rider_mesh`, and a package-owned frame mesh. Horse and rider each require distinct `idle`, `move`, `attack`, `defend`, `support_attack`, `retreat`, `training`, and `death` animation identifiers. None has a final exported file yet.

The geometry candidate and local checkpoints are valid evidence, not a runtime deliverable. Final segmentation, separate horse/rider armatures and weights, `Saddle_Node`, source-action retargeting, DDS channel packing, io_pdx_mesh export, and reimport require a fresh process exposing registration recovery commit `7e3af24ac`. Blender must not author replacement motion.

Proposed sounds are `cannibal_bone_riders_select`, `cannibal_bone_riders_move`, `cannibal_bone_riders_idle`, `cannibal_bone_riders_sling_release`, `cannibal_bone_riders_stone_impact`, `cannibal_bone_riders_training`, and `cannibal_bone_riders_death`. Six sourced WAV candidates cover all required sound roles. `audio/derived/stone_impact.wav` is a public-domain stones-tapping derivative, PCM S16LE 44100 Hz mono, SHA-256 `F36A4426138E4F5D546EE6D17E8A5D902B59C962A45A1956630FE5E23EFD0BAE`. Attribute the CC BY recordings as documented in `audio/audio_manifest.md`. The impact belongs at the projectile-contact phase after the attack or support-attack release; exact action frame synchronization cannot be finalized until compliant action frame ranges exist.

Use the installed vanilla `units_cavalry.asset` architecture: a frame entity propagates all eight states to a separate horse entity and a separate rider entity; the rider attaches at the horse mesh `Saddle_Node`. The horse uses the parent-approved CC0 Mesh2Motion action sources documented in `evidence/mesh2motion_horse_action_audit.md`. The rider uses eight distinct Meshy actions documented in `evidence/meshy_rider_action_research.md`. Attack and support-attack remain conditional on visible sling/pouch preservation and two distinct sling-stone volleys. Do not use a static horse, local transform keys, action aliases, merged skeletal actions, or vanilla horse animation.

Existing counter consumers are `GFX_unit_cannibal_bone_riders_icon_medium`, `GFX_unit_cannibal_bone_riders_icon_medium_white`, and `GFX_unit_cannibal_bone_riders_icon_small`. Runtime DDS paths are `gfx/interface/counters/divisions_large/unit_cannibal_bone_riders_icon.dds`, `gfx/interface/counters/divisions_small/onmap_unit_cannibal_bone_riders_icon.dds`, and `gfx/texticons/unit_cannibal_bone_riders_icon_small.dds`. All three pass header, two-frame, alpha, and decoded-roundtrip validation. The parent owns final definitions and in-game validation.
