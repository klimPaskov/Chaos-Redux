# Paid v9 dependency and capability report

The mandatory process key gate passed before repository intake. The first locked environment probe returned a valid Meshy response but observed its probe-owned PowerShell PID 16580 and Node PID 26872 at the immediate cleanup snapshot. Both exited naturally within the documented three-second interval. The second `verify_environment.py --probe-meshy` run passed with `findings: []`; its report SHA-256 is `62059171D51622F8BBC39078CBB58A35CB3927C3FD885AD1B55C58894B7F25DC`.

The verified dependency-lock SHA-256 values are `58FA9FC6486FDA304FDC344962C63ADD276938E2D4402D0E44ACA02A392F1286` for `dependencies.lock.json`, `E45FE80F3B8AC49A365EA2D4221E82E969AE55279639F817BB6FA75407D1C233` for `meshy_tool_schema.lock.json`, and `1ADD938E9FCECB9410AA0430629A4E78E956ADB78362004E075481D6484081BB` for `blender_hoi4_adapter.json`.

The locked live routes are official `@meshy-ai/meshy-mcp-server` 0.4.0 at git head `d8c77d1cb897e345eb41d38b510b8391b1664346`, MCP SDK 1.29.0 at git head `e12cbd7078db388152f6e839abdbe09ba01f3f32`, compatibility revision `meshy-7-v4`, exact generation model `meshy-7`, repository adapter `chaosx_blender_hoi4` 1.10.0, Blender 5.1.2 build `ec6e62d40fa9`, Blender MCP add-on 1.0.0 on listening socket `127.0.0.1:9876`, and io_pdx_mesh 0.91.0 with archive SHA-256 `A683DF08318CB700014C7FE9A3D15139E5FB2313C7E98715204263E48931F7C2`. The live Meshy balance was 1320 credits.

The live schema exposes `meshy_rig` with exclusive `input_task_id` or `model_url` input and `meshy_animate` with required `rig_task_id` and integer `action_id`. Meshy's current official Rigging API states that programmatic rigging is for standard humanoid bipeds and is unsuitable for nonhumanoid assets. It identifies HTTP 422 as pose estimation failure when the model may not be a valid humanoid. The official Animation API accepts only a successfully completed Meshy rig task ID. Sources: `https://docs.meshy.ai/en/api/rigging` and `https://docs.meshy.ai/en/api/animation`, retrieved 2026-08-24.

The accepted mounted asset contains a living quadruped horse and a humanoid rider. A provider-prepared separation could make the rider eligible for humanoid rigging, but the horse remains explicitly outside Meshy's rigging surface. Because every mounted runtime role requires substantive synchronized horse motion as well as rider motion, a rider-only Meshy rig cannot satisfy idle, move, attack, defend, support_attack, retreat, training, or death. There is no verified Meshy quadruped rig or animation operation in the locked or current official surface. Another compound or horse rig call would repeat a documented incompatible input class rather than exercise a compliant recovery path, so no additional paid call was made.

Provider lineage remains generation task `01a03404-f74d-7d5b-876d-5f426afe11f6` at 30 credits and remesh task `01a03418-57e3-7399-bf55-2d769bedabee` at 5 credits. Both rig requests failed before billing. Total consumed credits remain 35. No rig, animation, conversion, Blender mutation, texture packing, io_pdx_mesh export, or reimport occurred in v9.

The only admissible future resolutions are a verified future Meshy quadruped/mounted capability or an explicitly user-approved professional mounted horse-and-rider animation source. A static horse, local Blender-authored motion, whole-rig transforms, semantic aliases, or silent vanilla horse action reuse do not satisfy the brief.
