# D’Rhondan portrait package

This workspace contains the native ImageGen source masters, deterministic full-canvas `156x210` processing, canonical native `65x67` dossier-card compositions, decoded DDS evidence, prompts, manifests, review sheets, and runtime handoff for the fictional Empire of D’Rhonda roster defined by the Event 016 addendum.

Source mode is `fictional_high_chaos` for every subject. No Internet identity source, archival placeholder, RunPod job, or grounded-person replacement is involved.

The exact roster is the user-locked set of three regime leaders, five advisors, one high-command figure, and three commanders. Stable runtime sprite names and paths are recorded in `metadata/portrait_manifest.json`, `interface/016_dhrondan_portraits.gfx`, and the parent-plan handoff.

The three regime-leader portraits were visually approved by the parent agent. The remaining full portraits and all nine role-card variants are complete producer-side assets with exact DDS round-trip evidence, but remain `needs_user_review` in the manifest until the parent performs the final visual review. There is no replacement-pending state because every subject is fictional and native-ImageGen generated.
