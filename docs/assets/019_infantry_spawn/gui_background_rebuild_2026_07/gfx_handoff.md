# Infantry Spawn Muster Board Background Handoff

Asset status: complete.

- Runtime DDS: `gfx/interface/019_infantry_spawn/infantry_spawn_muster_board_background.dds`
- Sprite name: `GFX_infantry_spawn_muster_board_background`
- Target surface: direct scripted GUI Muster Board background for Event 019 Infantry Spawn.
- Target size: `1120x760`.
- Suggested target `.gfx`: the existing Infantry Spawn interface sprite definition owned by the main agent. No `.gfx` file was edited in this asset-only pass.
- Source mode: generated ImageGen fictional/symbolic UI panel, with retained raw source, exact prompt record, deterministic processing evidence, and review sheet in this package.
- Canonical reference family inspected: `.agents/skills/chaos-redux-event-assets/assets/vanilla_reference/event_art/report/contact_sheet.png` for subdued period paper/brass surface treatment only; no vanilla art was reused.
- Review image: `docs/assets/019_infantry_spawn/gui_background_rebuild_2026_07/review/muster_board_background_contact_sheet.png`.

Ready-to-copy sprite definition snippet for the main agent:

```text
spriteType = {
	name = "GFX_infantry_spawn_muster_board_background"
	texturefile = "gfx/interface/019_infantry_spawn/infantry_spawn_muster_board_background.dds"
}
```

Composition handoff: the regenerated art is intentionally simple and calm. It uses one restrained charcoal/brass dossier surface, one broad uninterrupted central paper/map field, quiet edge framing, one shallow header band, and one shallow lower action band. There are no decorative slots, repeated wells, button-like boxes, portraits, humans, readable words, pseudo-text, seals, emblems, icons, grids, filing pockets, stacked documents, or tactical map diagram. The negative space is reserved for parent-owned text, cards, and controls and must not be treated as interactive art.

Processing handoff: ImageGen produced a `1522x1033` RGB source at `docs/assets/019_infantry_spawn/gui_background_rebuild_2026_07/source_png/infantry_spawn_muster_board_background_imagegen_1522x1033.png`. The full source canvas was resized with Pillow `Image.Resampling.LANCZOS` to the exact `1120x760` processed preview. The existing package's legacy source filename `infantry_spawn_muster_board_background_imagegen_1536x1024.png` was overwritten with the same generated bytes for compatibility; the manifest records both paths and the shared SHA-256.

DDS QA: the repository converter produced a 128-byte-header, one-level uncompressed BGRA DDS with declared `1120x760` dimensions, exact length `3404928`, pixel format `(32,65,0,32,0x00FF0000,0x0000FF00,0x000000FF,0xFF000000)`, texture caps `0x1000`, and alpha range `255..255`.

Remaining uncertainty: the main agent should check the GUI's exact overlay rectangles and close-button position against the open central field at runtime scale. Keep the sprite name and stable DDS path unchanged. Functional UI wiring remains parent-owned.
