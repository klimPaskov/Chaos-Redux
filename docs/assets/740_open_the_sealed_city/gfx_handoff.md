# Open the Sealed City GFX handoff

The interface registration lives in `interface/fallout_world_end.gfx`.

The event picture references `GFX_report_event_fallout_open_the_sealed_city`. The sprite points to `gfx/event_pictures/fallout/report_event_fallout_open_the_sealed_city.dds`.

The event file uses the same sprite for the human opening, the delayed result, and the delayed callback. The hidden AI lanes never display the picture, but retain the same event definition so the scheduler has one token family.

No Metro Republic, zombie, school, border inspection, clean certificate, returning disease, or ordinary super-event asset is used by this chain.
