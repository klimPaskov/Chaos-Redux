# Frame plan: covenant_obligation_review_states

Each of the 8 frames is an independently authored panel in the retained ImageGen storyboard source atlas. Frame order is left to right, then top to bottom. Local processing only removes the flat chroma key, normalizes the shared center anchor to 64x64, builds the horizontal 512x64 sheet, and produces a review GIF/contact sheet.
