# The Fertility Question GFX handoff

Register `GFX_report_event_fallout_fertility_question` in `interface/fallout_world_end.gfx` and use it in every event block from `chaosx.fallout.970` through `chaosx.fallout.976`.
